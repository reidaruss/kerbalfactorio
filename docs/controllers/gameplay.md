# Gameplay, Progression & UI — Master Controller Context

> **Domain owner:** `gameplay-controller` · **Reports to:** Admin · **Phase:** 1→2 · **Status:** Phase-1 designed + **Wave-0 gameplay-logic core BUILT & GREEN**; **Phase-2 RESEARCH / TECH TREE BUILT & GREEN** (`research.h` realizes GP-1 produce-science→unlock + GP-2 off-world gating); **slice-e2e gaps CLOSED — GP-17 deposit id unified to `worldgen::DepositId` (uint64) + direct-`FDepositNode` mining (GAP-1); GP-18 science recipes authored as data incl. off-world Cinderite→CinderScience (GAP-3)**; **WP1 PRIMITIVE-SURVIVAL SLICE BUILT & GREEN — GP-19 survival content (raw resources, tools, structures, ingots) + tool-assisted hand harvest + all-or-nothing hand-crafting + a fuel-driven `Furnace`/`Smelter` (gameplay-layer type, not a factory-sim machine)** · **all 15 ctest suites pass** · **UE survival slice through WP2.8 — dropped-item entities + walk-over pickup + inventory capacity + drop-from-menu + full-pack mining spill (PIE-verified)** · C-3 pinned, C-4/C-5 finalized, slice content pinned · **PHASE R (2026-07-05, [RETHINK.md](../review-2026-06-16/RETHINK.md)):** the research/tech core is green headless but **not yet wired into UE play** (the factory dead-ends: no science-pack chain, no unlock gating in-engine); **wiring research into UE play is Phase P1**, placement costs + real power are P2, the "build a launch pad" objective is P3. The two visible smelting models (survival `Furnace` fuel-pool vs the powered factory `Smelter`, GP-19) reconcile in R2 as one model (fuel tier / powered tier, shared recipe data). · **WEB (Three.js/WASM) ERA:** the playable client is `web/`; **an ore deposit is now a PATCH OF GROUND with a mining drill on top of it (GP-20, DW-25), 2026-07-26** · **BASE BUILDING SHIPPED (GP-21 to GP-24, DW-24), 2026-07-26: foundations, floors, walls and doors, snapped or free, paid for out of /core, resting on the terrain and never deforming it** · **CONTROLS BROUGHT IN LINE WITH THE GENRE (GP-25 to GP-27), 2026-07-26: Escape closes any menu from a DERIVED modal list, a nine-slot hotbar on the number keys and the wheel decides what the LEFT BUTTON does, E is interact and no longer harvest, holding the left button drag-places a belt run, and BELTS LINE UP because machines moved off /core's voxel lattice onto the same metric site grid the base uses (worst tile-to-tile tangential error 4.006e-6 m against the lattice's 0.41 m)** · **SANDBOX MODE SHIPPED (GP-29, DW-31), 2026-07-26: everything placeable at no cost with no research gate and no pack requirement, from the Tab menu and from `?sandbox=1`, with its own save slot key so a sandbox world can neither be mistaken for a survival one nor write over it, and a `SANDBOX` chip that stays on screen even behind an open panel** · **THE ASSEMBLY BAY SHIPPED (GP-30 to GP-35, DW-29/DW-30), 2026-07-26: a KSP-style VAB you ENTER and LEAVE, WASM ABI 6 publishing the whole vessel surface, parts snapping at their authored sockets to a measured 2.4e-08 m, and per-stage delta-v always on screen and exact against /core's own fixture (1857.79 + 3065.12 = 4922.91 m/s, 9845 kg, pad TWR 1.6567)** · **BASE BUILDING MADE USABLE (GP-36 to GP-40), 2026-07-27: the founding plane is CHOSEN TO FIT its footprint instead of being pinned to its low point, which took the buildable share of 81 sampled origins from 14.8% to 58.0%; a new part PREFERS an existing part's published socket to the bare grid and the ghost names what it caught (deck to deck 1.4e-12 m, a wall 0.000e+0 m from the deck socket it said it caught, two panels of a run 0.000e+0 m end to end); DW-32's CANTILEVER exists at last, one storey of hang carried by a neighbour with the run capped at 3 cells and a same-cell negative control that refuses the identical 2.00 m hang the moment its carriers are pulled; a machine over a deck sits on the deck's own `socket_top` to 6.7e-12 m; and the four structural costs are re-priced for the 4 m module** · **PLAYER PROGRESSION LANDED HEADLESS (GP-41 to GP-43), 2026-07-27: `core/include/of/progression.h` gives the art lane's four armour meshes the referent `contracts.json` correctly said they did not have, plus five skills whose level 0 is bit-exactly neutral so the layer is optional for every existing caller, and a five-byte palette-indexed appearance; `progression_tests` 10 tests / 823 checks, 29/29 ctest. THE CLIENT WIRING IS NOT DONE and needs a bridge surface plus an equip panel, both in files this lane does not own** · **Last updated:** 2026-07-27 (base building, then player progression)
> Read alongside: [MASTER_PLAN](../MASTER_PLAN.md) · [AGENT_ARCHITECTURE](../AGENT_ARCHITECTURE.md) · [ADMIN](ADMIN.md) · **[Phase-1 design: gameplay-phase1.md](../phase1/gameplay-phase1.md)** (the playable-loop design) · [PHASE1-PLAN](../phase1/PHASE1-PLAN.md)

## 1. Mission
Turn the simulation substrate into a *game*: research progression, an exploration/questline that pushes players off-world, loot and structures, and the UI that makes building, flying, and managing legible. Own the player-facing loop.

## 2. Scope & owned subsystems
- **Research / tech tree** — Factorio-style: factory produces science packs → unlock recipes/tech; gate off-world tech behind off-world resources.
- **Exploration & science** — KSP-style science from biomes/anomalies; scanning; discovery rewards.
- **Quests / narrative spine** — a lightweight thread guiding progression across bodies.
- **Loot & structures** — content for world-gen's POIs: ruins, derelicts, blueprints, lore.
- **Player systems** — inventory, hand tools, build/placement UX, vehicle control mapping.
- **UI/HUD** — build menus, logistics/production overview, **map & maneuver-node view** (consumes physics trajectories), power/research dashboards. *(UI is folded here for v1; may graduate to its own controller later — Admin Q3-adjacent.)*
- **Non-goals:** the sims that produce the numbers (factory-sim, physics, world-gen); rendering the 3D scene (→ rendering). Gameplay defines *rules, content, and screens*.

## 3. Key design decisions
| # | Decision | Rationale | Status | Date |
|---|----------|-----------|--------|------|
| GP-1 | Research = produce science items in factory → unlock (Factorio model) | Ties progression to automation depth | **Accepted — REALIZED in code** (`research.h` `ResearchState::tryResearch`: spend science `ItemStack`s from an `Inventory` → unlock recipes/entities; all-or-nothing, idempotent, monotonic) | 2026-06-15 |
| GP-2 | Off-world resources gate mid/late tech | Forces the KSP×Factorio fusion: you *must* leave the planet | **Accepted — REALIZED + PROVEN** (`CinderiteRefining` tech costs a Cinderite-derived science pack; unresearchable with only Forge science, researchable once the off-world item is in the pool — `research_tests` asserts the gate) | 2026-06-15 |
| GP-3 | Exploration science from biomes/anomalies + loot from procedural POIs | Dual incentive (KSP + Subnautica/NMS) | Proposed | 2026-06-14 |
| GP-4 | Lightweight questline (spine, not AAA campaign) (D-005 spirit) | Directs progression without huge content cost | Accepted | 2026-06-14 |
| GP-5 | UI folded into gameplay for v1 | Scope; revisit if UI complexity grows | Accepted | 2026-06-14 |
| GP-6 | **Two avatar modes, one body:** first-person on foot / third-person+IVA in craft (implements P1-D1) | First person suits build/mining precision; third person suits flight readability | Accepted | 2026-06-14 |
| GP-7 | **Grid-snap-to-surface-tangent + free-rotate, ghost-first** placement (implements P1-D6) | Recovers a placeable surface grid on curved terrain; free-rotate for headings | Accepted | 2026-06-14 |
| GP-8 | **Color-coded validity ghost** (green/blue valid · red hard-invalid · yellow warning) + reason label | Satisfactory-proven placement legibility; reasons from slope/overlap/no-deposit | Accepted | 2026-06-14 |
| GP-9 | **Small fixed-slot inventory + unified build/mine tool hotbar**; item = {ItemId,Count} shared with factory-sim | Minimal, data-driven, persistence-friendly; one item-id space hand↔machine | Accepted | 2026-06-14 |
| GP-10 | **Map = in-world scaled-space overlay, no scene transition; read-only (no maneuver nodes in slice)** (R3 first cut) | Honors no-loading-screen pillar; draws physics-supplied conic; node-editing is Phase 2 | Accepted | 2026-06-14 |
| GP-11 | **Scripted linear objective chain, not a quest engine** (within P1-D4 / GP-4) | Self-teaching spine for the slice; predicates read from pinned streams; no quest assets yet | Accepted | 2026-06-14 |
| GP-12 | **Slice items/recipes are data assets**; Cinderite = off-world identity hook only (not a tech gate yet, P1-D4) | Balance iterates without code; the gate is Phase-2 when research lands | Accepted | 2026-06-14 |
| GP-13 | **`ItemId = uint16`, opaque, hand-assigned stable constants** (never reused/renumbered; new items append) — the SINGLE id space across player inventory, machine inventories (factory-sim), deposits (world-gen→item map), and persistence (C-3) | Stable across saves with no remap table; matches factory-sim's on-belt `uint16` width; opaque so consumers never decode bits | Accepted | 2026-06-14 |
| GP-14 | **All player intents carry `FPlayerId`; intents are commands not state writes** (C-5) | One surface serves single-player and D-004 networked play unchanged; sim stays authoritative | Accepted | 2026-06-14 |
| GP-15 | **Wave-0 gameplay-logic core is header-only `core/include/of/gameplay.h`, consuming the green cores verbatim** (factory-sim recipes/machines, world-gen `SampleTerrainHeight`+body params, `ItemId`=factory-sim `ItemId` u16) | Mirrors the other 5 Wave-0 cores' pattern; the slice content (§7) is DATA in a `SliceRegistry`; build/recipe logic DRIVES the factory sim rather than re-implementing it (GP-12 data-driven, prime directive: thin layer over closed substrate) | Accepted | 2026-06-14 |
| GP-17 | **GAP-1 (slice-e2e) CLOSED — deposit id unified to ONE type.** `gameplay::DepositId` is now a shared `using = worldgen::DepositId` (the `FDepositNode::Id` type, `uint64`) instead of a parallel narrower `uint32`; `gameplay.h` includes `deposits.h` (acyclic: gameplay→world-gen, deposits never includes gameplay). Added `mineDeposit(worldgen::FDepositNode&, Inventory&, baseRate)` so the slice mining loop consumes the catalog's own `FDepositNode` DIRECTLY (read/decrement `RemainingAmount` in place, identical grade-scaled extraction semantics) — the e2e test's `bridgeNode` truncating bridge is gone, and the deposit id carries verbatim from the catalog through mining into the persistence depletion diff with NO narrowing. Backward-compatible: the `gameplay::DepositNode` overload + `Builder`/`findDeposit` unchanged; `gameplay_tests`/`persistence_tests` green (literal ids fit `uint64`). | A uint64 catalog key silently truncated to uint32 at the gameplay/persistence boundary was a latent save-key collision risk and forced a bridge struct in the loop. One shared `using` + a direct-`FDepositNode` mining overload removes both, making mine→persist a clean typed hand-off. | Accepted | 2026-06-15 |
| GP-18 | **GAP-3 (slice-e2e) CLOSED — science recipes authored as data.** Added to `research.h` (in the reopened `gameplay::recipes` namespace, ids `0x0120+` mirroring the `0x0020+` science-item block): `CraftAutomationScience` (Ferrite plate → AutomationScience, basic) and the OFF-WORLD `RefineCinderScience` (Cinderite → CinderScience). Each is a `RecipeDef` (→ `factory::Recipe` via `toFactoryRecipe()`) registered through the new additive `SliceRegistry::registerRecipe` extension point (same append-only discipline as `registerItem`); `RegisterScienceRecipes(reg)` registers both. `RefineCinderScience` is the conversion the off-world gate (GP-2) needs — CinderScience can ONLY be refined from Cinderite, which is Cinder-only (WG-4), so the mine-Cinder → refine → research-off-world-tech chain is now real content the factory crafts (proven in `slice_e2e`: the refinery outputs CinderScience, `producedCountOf(CinderScience) > 0`). | The off-world *science item* existed and the tech costed it, but the recipe turning mined Cinderite into that science was never authored — the e2e capstone had to credit the science off-table. Authoring it as data (GP-12) closes the content gap so the off-world payoff is a genuine factory output, not a test fixture. | Accepted | 2026-06-15 |
| GP-19 | **WP1 primitive-survival slice authored additively on `gameplay.h`/`deposits.h`; the furnace is a FOCUSED gameplay-layer type, NOT a factory-sim machine.** New content (all append-only, pinned ids untouched): 13 items at `0x0030+` (7 raw — wood/stone/coal/raw_iron/raw_copper/water/oil; 2 ingots — iron/copper; 2 tools — crude_pickaxe/crude_axe; 2 structures — primitive_furnace/smelter), 2 entity types `0x30+`, 2 fuel-driven smelt recipes `0x0130+`, registered via `RegisterSurvivalContent(reg)` (same additive `registerItem`/`registerRecipe` discipline as the science layer). Mechanics: **(a)** `worldgen::survival::LayoutTestArea` lays out a SMALL deterministic test-area patch of terrestrial nodes (one per `NodeKind`; position-hashed from seed+index, surface-snapped) — NOT a planet-wide pass; **(b)** `survival::harvestNode(FDepositNode&, NodeKind, Inventory&, baseYield, toolYield)` — hand harvest ALWAYS works (no bootstrap deadlock), the matching tool (axe→wood, pickaxe→stone/coal/ore; water/oil need none) raises yield (`assistingToolFor`); **(c)** `survival::HandCrafter::craft(CraftRecipe, Inventory&)` — multi-input bill-of-materials, ALL-OR-NOTHING (tools/furnace/smelter are made this way; `handRecipes()` lists them); **(d)** `survival::Furnace{FurnaceTier}` — a tick-driven smelter holding an ore buffer + a **fuel-burn-time pool** (`loadOre`/`loadFuel`/`tick`/`run`/`takeOutput`); each progressing tick burns one fuel tick, ore→ingot at the tier's `ticksPerSmelt` (furnace 180, smelter 60). **The furnace is its own type, NOT a `factory::FactorySim` machine**, because factory-sim gates throughput on a POWER NETWORK (supply/demand→brownout) whereas survival smelting is gated by a consumable SOLID-FUEL pool with no power representation — bolting fuel onto the SoA power model would distort the hot path, so a small self-contained `Furnace` is cleaner and the only furnace↔smelter difference is one data parameter (`ticksPerSmelt`). FUEL MODEL: `fuelTicksPerUnit` — wood=360, coal=1440, so coal yields more smelts/unit (furnace: wood ~2, coal ~8) and the faster smelter gets more smelts/unit of the same fuel. Tested by `survival_slice_tests` (12 tests / 1246 checks). | Survival-crafting is the WP1 vertical slice; authoring it additively keeps the pinned slice/science content + id space untouched (GP-12/GP-13), and the dedicated furnace type avoids distorting factory-sim's deterministic power/brownout SoA model with an orthogonal fuel mechanic. | Accepted | 2026-06-15 |
| GP-20 | **An ore deposit is a PATCH OF GROUND with ONE pool, and a mining drill is placed on top of it** (`deposits.h` §P + `gameplay.h` §S.5, web `OrePatches`/`OreField`, ABI 3, DW-25). A patch is an irregular lobed area 6 to 11 m across, deterministic from the world seed, with coverage falling from a rich centre to a thin rim; that ONE coverage function is both the drill's units-per-second multiplier and the tint the ground is drawn with. Outcrops (the pieces that break the surface) are ordinary `/core` harvest nodes LINKED to the patch, so hand mining is the same `harvestNode` call a tree takes, with the patch's pool handed in and exactly what was removed taken back out of it. Placing a drill off every patch is REFUSED with "you cannot place a drill here, there is no ore" on the ghost before the key is pressed; several drills on one patch is allowed. Balance is authored data in §P: `kUnitsPerSquareMetreM2` 16, hand yields 3 bare / 9 with the pickaxe, `kDrillUnitsPerSec` 3.0, radius 6 to 11 m. | The deposit was a boulder on the grass and that fails three ways at once: a player cannot tell a resource from scenery (the complaint that started the work), a machine binding to the nearest pebble within 3.2 m is not standing ON anything so "put a drill on the ore" has no referent, and a pebble has no interior so richness, extent and the choice of WHERE to build cannot exist. A patch gives all three and costs one draw call. The refusal is the teaching moment, the same argument as DW-24. Bare hands always paying is the no-bootstrap-deadlock invariant (GP-19) restated where it matters most: a drill is the thing you cannot build until you have mined by hand. | Accepted | 2026-07-26 |
| GP-21 | **A structure is NOT a `BuildKind`.** `StructureKind {Foundation, Floor, Wall, Door}` is its own enum in `gameplay.h` section S.6, with its own ItemIds (0x0040+), TypeIds (0x40+) and `CraftRecipe` costs, and the factory sim never sees one. | `automation.h`'s `BuildKind` feeds `BuildableNetwork`, which places into `FactorySim` and TICKS it. A foundation never ticks, holds nothing, has no ports and draws no power, so putting it in that enum would force every switch in the factory sim to grow a case that does nothing, would put non-ticking rows into the tick loop's SoA arrays for as long as a base stands, and would make the render key (the dense entity index) carry hundreds of entities the simulation has no opinion about. The precedent is set and was already right: GP-19 made the survival `Furnace` a gameplay-layer type rather than a factory-sim machine for the same reason, that an orthogonal mechanic bolted into a hot path distorts the hot path. The cost of the separate kind is one extra `BatchedMesh`; the cost of the shared kind is a permanent tax on the factory tick. | Accepted | 2026-07-26 |
| GP-22 | **A structure snaps to a SITE, a local metric tangent frame anchored on one world lattice cell, NOT to /core's 1 m voxel lattice.** Inside a site the spacing is exactly the module the assets ship; the site is capped at 32 m and a part beyond it founds a new one. Free placement (B) is the same parts with the rounding removed. | Measured, not assumed: one unit step of a /core cell key covers **0.588 to 1.017 m** of ground depending on the axis, because a 1 m body-frame cube grid is cut obliquely by the ground sphere (four sites sampled; the same defect that split a belt run into three, ARCHITECTURE 15.2 item 102). A foundation is a 1.00 m mesh, so on cell centres a 20 x 20 m platform tears open by 0.41 m on one axis and overlaps by 0.02 m on another. The site frame makes adjacent parts meet at **1.176e-12 m**, and the curvature it ignores is r^2/2R = 0.85 mm at the 32 m cap. "Snap to the world grid" is honoured where it means something: the site's ORIGIN is a world lattice cell centre, so a base and a factory share one origin. | Accepted | 2026-07-26 |
| GP-23 | **DW-24's tolerance is ASYMMETRIC and is a number about the levelling TOOL, not about the terrain: FLOAT 0.55 m, BURY = the deck thickness (0.50 m, read off the asset).** A site's plane is founded on the LOWEST ground under its first cell rather than on its centre. | Measuring the terrain gives a comfortable answer: the worst spread across a deck's five footprint points over 400 sampled footprints is 0.0013 and 0.0069 m on two plains and 0.118 and 0.127 m on two slopes. Measuring what Q can deliver does not: it edits whole 1 m voxel cells, so it has a **dead band of half a cell in which it changes nothing at all**, and one press left **0 of 12** refused cells buildable at a 0.22 m tolerance (residual over a levelled disc: p05 -0.536, p50 +0.027, p95 +0.588 m). A tolerance tighter than that dead band makes DW-24's own loop unclosable by construction, however good the terrain is. The two failures are also not the same failure: ground BELOW the base is a gap of daylight under a hovering slab and must stay tight, while ground ABOVE it disappears inside 0.50 m of slab and reads as a pad set into soil, which is what a real foundation looks like. Founding the plane on the low point makes a base lean towards the invisible failure. **FLOAT should come down the day the levelling tool can fill less than a whole cell** (raised to Admin as a cross-domain need). | Accepted | 2026-07-26 |
| GP-24 | **The walker learns about a base through a PORT (`KinematicBody.solids`), resolved after the terrain and before the ground snap; a door's THREE collision boxes stay three, and the leaf gets a derived box that exists only while the door is shut.** | A port rather than an import because a structure rests on the terrain and must never become a second definition of it (DW-26's lesson, which cost five multi-hour bugs). After the terrain and before the snap because a deck is a floor ABOVE the ground: settling onto the terrain first and then noticing the foundation drops the player through their own base for a tick. Three boxes because a convex hull of a doorway is a sealed wall, and the only way to catch that is to make a walk through the opening an assertion, which `probes/build.js` does. The leaf is present-or-absent rather than a swinging collider because DW-12 says there is no physics engine and a kinematic walker cannot honestly resolve a moving panel. | Accepted | 2026-07-26 |
| GP-25 | **Escape closes the top open menu, from a DERIVED registry, and it does NOT fight the browser's own pointer-lock exit.** `ui/ModalStack.ts`: every panel joins the list in its own constructor by extending `Modal`, the app hands each one its own pointer transition through `closer`, and one handler in `GameplayInput.escape` closes the most recently opened. With nothing open Escape drops the part in hand; with nothing in hand it lets the browser's exit stand and REPORTS that it did (`lastEscape`), so the key is never silently a no-op. | The guarantee the player asked for is "whatever is open, this key shuts it", and five per-panel handlers give you four that work and one written after the rule was forgotten. Derivation is the enforcement: `probes/controls.js` walks the live list and FAILS on an entry it cannot open, so a menu added next month cannot escape the guarantee quietly. Not fighting the browser is not politeness but necessity: Chrome rejects a `requestPointerLock` made outside a user gesture and the rejected promise is a console error, which fails every driven probe in the suite (ARCHITECTURE 15.2 item 106). | Accepted | 2026-07-26 |
| GP-26 | **A nine-slot HOTBAR decides what the left button does, and E becomes interact.** `game/Hotbar.ts`, contents authored as data (`DEFAULT_BAR`), chosen with the number keys or the mouse wheel. A slot holds `hand`, a `part`, a `furnace` out of the pack, or nothing. A part places; the bare hand swings at a node and digs; an empty slot does NOTHING. E stops being harvest and becomes open-a-furnace / take-a-machine's-stock / work-a-door. Every consumer asks for an ACTION and never a key (`player/Bindings.ts`), and mouse buttons are codes in the same held set so a tape can click. | The old scheme decided the verb from what happened to be under the crosshair, which is backwards: the HAND decides, and that is the convention the whole genre has taught. The empty slot doing nothing is not pedantry, it is the assertion that proves the hand is really driving, because a build that placed on every click passes every positive test. The binding table exists because roughly twenty probes were pressing E and G by name; a probe that presses `use` survives the next remap and one that presses KeyG has to be found. | Accepted | 2026-07-26 |
| GP-27 | **Machines and belts snap to the SAME metric SITE grid base building uses, not to /core's voxel lattice; holding the left button DRAG-PLACES a run; and a run is a RAMP rather than a staircase.** `game/MachinePlacement.ts` addresses a machine as (site, i, j); the two tangent coordinates are the site's, exact to the module, and the radius is the oracle's, so a belt still follows the ground. The drag turns every tile to point at its successor, fills in cells the crosshair skipped, ends on a reversal rather than turning the tail around, and commits ONCE per tick. | Measured, not assumed, and it is the SAME measurement that condemned the lattice for structures (GP-22). `__of.latticeCell` on the shipped world at the spawn: one unit step of a cell key covers **0.5903, 0.8110 and 1.0167 m** of ground on the three body axes, so 1.00 m belt tiles laid on it overlap by up to **0.41 m**. When the same measurement condemns two systems, fix them with one mechanism: reusing the site frame also means a base and a belt run agree about where a metre starts, which matters the first time anybody runs a belt into a building. Measured after, over a 15-tile dragged run: worst tangential deviation from the module **4.006e-6 m**. The SECOND half of the same complaint survived that fix and had to be found in a screenshot: a belt follows the ground, so upright 1 m tiles on an 11 degree slope step by 0.189 m each and the run reads as a flight of stairs. Tiles now take the run's own 3D direction (`Grid.frameOf`), leaving a 0.018 m kink; `orient` still only yaws and is still right for a machine, which should stand upright whatever the slope. ONE commit per drag tick because a commit rebuilds the /core network and loses whatever is riding the belts, so per-tile commits would silently eat ore as the run was laid. | Accepted | 2026-07-26 |
| GP-28 | **The whole build system reads the LIVE ground, and `snapToGround` takes the edit set as a REQUIRED argument with no default.** `game/Grid.ts`, `game/Machines.ts` and the build ghost's aim march passed a literal 0 for `_of_surface_radius`'s edit set while `game/OrePatches.ts` and `game/Structures.ts` passed the real handle; the ghost's march now goes through `Structures.groundRadius`, the one call site that was always right, and `Machines` takes an edits port. | Standing rule 1 broken inside a single directory, and MEASURED rather than argued (the rendering lane's `probes/beltfloat.js`): the surface under the feet moves 4.00 m after eight strikes, and a -25 degree aim across edited ground put the belt ghost **1.807 m** from where the aim actually meets the ground, which is a cell and a half of targeting error. Now 0.459 m, which is the grid snap itself and not error (half a 1 m cell diagonal is 0.707 m). The HEIGHT half of the same defect was closed incidentally by GP-27, because the site anchor routes through `Structures.groundRadius`. No default on the parameter, because a default is exactly what let three call sites make the same mistake without anyone noticing. | Accepted | 2026-07-26 |
| GP-29 | **SANDBOX IS A MODE WITH ITS OWN SAVE SLOT, and the mode is answered by ONE object with NAMED questions.** `web/src/game/GameMode.ts` holds the only `sandbox === true` test in the client; `ModeRules.freeBuild` and `ModeRules.fullCatalogue` are what every gate asks. Three gates ask it today: a structural part's `/core` cost (`Structures.canAfford`/`pay`), a hand furnace's pack requirement (`Machines.place`, both the check and the spend), and hand crafting (`GameplayActions.craft` GRANTS rather than calling `of_gp_craft`). Machines and belts already cost nothing so they needed no change; research is not wired into the web client at all yet, and asks the same question when it lands. **The save slot is KEYED by mode (`auto` / `auto-sandbox`) and also RECORDS it.** A `SANDBOX` chip is always on screen, outside `GameHud.setVisible`. The menu entry lives at the foot of the Tab panel and RELOADS. | Three arguments, in order of weight. **(1) The autosave is the contaminant, not the loader.** The obvious design is one slot with a `mode` field and a refusal to load a mismatch, and that is exactly half a fix: `Gameplay` autosaves every 20 s on the sim clock, so a survival boot over one shared key would correctly refuse to LOAD the sandbox world and then DESTROY it inside half a minute. Two keys makes contamination structurally impossible; the field is kept as belt and braces because a key that has itself gone wrong (a hand-edited store, a migration) is a different failure and needs a different catch. **(2) Named questions, not a boolean.** DW-31 asks for no cost AND no research gate AND no pack requirement, and research does not exist here yet. `freeBuild` gives the right answer to a branch written next month instead of relying on someone remembering to add `|| sandbox` to it, which is ModalStack's derivation argument applied to progression. **(3) A reload, not a runtime toggle.** A world that spent five minutes in each mode has no honest label to write on its slot, so the mode is fixed at boot and there is deliberately no `sandbox(true)` setter anywhere, including on the debug surface. **The one thing worth carrying forward** is `Structures.affordInCore`: a mode that lifts a rule must publish the answer it is overriding, because "12 foundations placed" is indistinguishable from a broken affordability check. It reads false in BOTH modes with an empty pack while the sandbox placement succeeds, which is DW-26's two-named-shapes rule applied to progression, and it is what lets a single page carry its own negative control. | Accepted | 2026-07-26 |
| GP-30 | **The assembly bay is a MODE, not a panel: it REPLACES the four render passes rather than compositing a fifth.** `Frame.vabActive` clears and draws `scenes.vab` with `rig.vabCam` and returns; the bay owns the pointer through `VabPointer` and hands it back on exit; its panel joins the DERIVED modal stack so Escape closes it with no second handler (GP-25). | A rocket on a stand and a planet at 600 km share no depth range, no lighting and no camera, so compositing them buys nothing but a way for one to seam into the other. It also makes `info().calls` in the bay the BAY'S draw-call count rather than the planet's plus a bit, which is the only way the number means anything: measured 26 calls with an 11-part rocket and 3 with an empty pad, against a 150 budget. The alternative, a big dialog over the world, is what KSP explicitly is not: the VAB is a place you go. | Accepted | 2026-07-26 |
| GP-31 | **The ItemId block for the item form of a part is `0x0050..0x006A`, defined as the pure function `ItemId = 0x0050 + (PartId - 0x0100)`, and the costs are authored in the shim as `CraftRecipe`s payable through `HandCrafter`.** Class L costs about 4x class S. The reference vehicle totals 38 Iron, 8 Copper and 4 Wood. | Physics R9 asked for the allocation and `vessel.h` deliberately has no opinion about cost. A pure function of the PartId is stable, append-only and checkable by inspection, and it deliberately does NOT equal the PartId, because `vessel.h`'s own reason for starting at 0x0100 was that a PartId and an ItemId must never be confusable in a debug print. It is allocated in `web/wasm/of_vessel_api.inc` rather than `gameplay.h` only because this lane may not touch `/core`; it should be PROMOTED the next time `/core` is opened, and until it is, a part item cannot enter the pack, which costs nothing because a part is PAID FOR and placed, exactly the `of_gp_structure_pay` contract. | Accepted | 2026-07-26 |
| GP-32 | **A DECOUPLER, and only a decoupler, may hang under an engine bell.** `VesselNodes` offers a terminator's bottom face as an `interstage` node, and `fitAt` refuses everything but a decoupler there. | The art contract says an engine publishes no `socket_stack_bottom` because nothing may be bolted under a bell, and it is right about structure. But `/core`'s OWN reference vehicle hangs a stack decoupler under the vacuum engine (`test_vessel.cpp`), which is the interstage every KSP stack is built with, and refusing it would have made the SHIPPED fixture unbuildable in the SHIPPED assembly view. The cost is one honest gap: that joint has a socket on the decoupler's side and none on the engine's, so the measurement reports it `unmeasurable` rather than pretending it closed, and `probes/vab.js` asserts there is EXACTLY ONE so a second silent gap cannot appear. Raised as a cross-lane question rather than settled here. | Accepted | 2026-07-26 |
| GP-33 | **Staging is DERIVED on every placement until the player reorders by hand, and then it is theirs.** `of_vs_autostage` rebuilds the table from the tree; the moment a stage arrow is pressed, `handStaged` latches and nothing rewrites it again. | An assembly view that made the player hand-build the stage table before it would show a delta-v number would be unusable, and getting staging wrong by one is the classic KSP failure this screen exists to prevent. But silently re-deriving over a table the player has just edited is worse than not deriving at all, because it discards a decision without saying so. One latch buys both. | Accepted | 2026-07-26 |
| GP-34 | **A design library is NOT world state.** Named designs live in their own `localStorage` store (`VabStore`), not in the world save slot, and the work in progress is written to a reserved key on every change. | A rocket you liked should survive a new world, a mode switch and a slot version bump, which is what a blueprint means and what KSP's craft folder does. Keeping them out of the slot also avoids a second writer inside `SaveGame`'s IndexedDB store. The work-in-progress key is what makes closing the tab mid-build lossless without coupling the bay to the autosave. Deliberately stated because it leaves one balance hole: loading a design neither charges nor refunds, so a saved rocket is a drawing rather than a bill. | Accepted | 2026-07-26 |
| GP-35 | **In survival the bay offers the DW-29 Tier-1 block (PartId 0x0100..0x010C, 13 parts); sandbox offers all 24.** The question asked is `ModeRules.fullCatalogue`, never a boolean. | Research is still not wired into the web client, so `fullCatalogue` needs a survival answer that is a real gate rather than a placeholder, and DW-29's own tiering is the honest one: control, power, docking and the whole class-L stack are later tech. When research lands it replaces the `tier1` predicate and nothing else moves, which is GP-29's named-question argument doing its job. Measured both ways: 13 offered in survival against 24 in sandbox, and with an empty survival pack the identical seven-part build sequence places NOTHING and refuses seven times by name. | Accepted | 2026-07-26 |
| GP-36 | **THE FOUNDING PLANE IS CHOSEN TO FIT ITS FOOTPRINT (DW-33).** `StructureTolerance.fitPlane(lo, hi, floatM, buryM)` returns `clamp(hi - buryM, lo, lo + floatM)`, and `makeSite` moves the site plane by it. Three regimes: a spread inside BURY keeps the old rule exactly (`d = lo`, everything buried, which is how a base looks best); a spread between BURY and BURY+FLOAT spends the bury budget to the last millimetre and hangs only the remainder; a spread past the budget leaves the plane where the float side is exactly satisfied, so the refusal names the HIGH ground, which is the half Q can cut. **Measured, same 81 origins out to 6.4 km, one probe run: 14.8% of origins buildable under the pinned rule against 58.0% under the fit** (3.9x), and the `mid` site went 70.6% to 100.0% of its 812 footprints. **`PLANE_MARGIN` = 2%, and the first version without it was BROKEN.** Spending the bury budget to the last millimetre puts the worst point at exactly `buryM`, and `checkGround` re-derives that number by a different arithmetic path: both are `ground(x) - |x|`, a difference of two doubles of magnitude 600,000, so the cancellation carries about 1e-10 m and a slack ratio of 1 + 2e-10 is greater than 1. On the coarse height field this lane started against, spreads mostly fitted inside the bury bound so the exact-fit branch was rarely taken and the defect hid; the hour the terrain lane gave the noise stack real detail (WG-25) it became the normal case and **every one of 88 scanned candidate sites refused with the sentence about the ground standing 0.50 m into it, against a 0.50 m bound**. 2% rather than an epsilon because there are two problems and only one is arithmetic: a plane exactly on the bury bound also leaves the SECOND cell of the site no room at all. It costs 10 mm of an invisible budget. On the terrain that was shipped when the work started the spawn itself read **0 of 812 footprints buildable** with the bury side reading 1.0123 m p50 against a 0.50 m bound. **Bias towards BURYING rather than splitting the error proportionally**, which DW-33 permits and which is not a wash: both accept exactly the same ground, because feasibility is `spread <= FLOAT + BURY` for every choice inside the band, but they differ in which side the SECOND and later cells of the site land on, and the two sides are not equally rescuable. A float past the bound can be carried by a neighbour (GP-38); a bury past it has no relief but a shovel. So the precious budget is spent first and the renewable one is spilled into. | The old constant did not encode a tolerance, it encoded "the error all lands on one side", which is a SCALE ASSUMPTION in disguise: defensible at a 1 m module where a footprint spread 0.127 m, and silently a bug at 4 m where the same ground spreads 1.01. `probes/buildtol.js` now scores the pinned rule and the fit side by side in one run over the same footprints, so the before and the after are one measurement rather than two runs a day apart, and a regression to the old rule cannot pass quietly. | Accepted | 2026-07-27 |
| GP-37 | **A NEW PART PREFERS AN EXISTING PART'S SOCKET TO THE BARE GRID, and the ghost NAMES the socket it caught.** `game/StructureSnap.ts`: the four `socket_edge_n/e/s/w` on a deck and the two `socket_end_l/r` on a wall are read out of the shipped `.glb` files ONCE at load; the nearest one within `cellM * 0.75` (3.00 m) overrules the grid address whenever the cell it offers is free. A deck's edge offers a WALL that edge and another DECK the cell across it; a wall's end offers the next wall along the same line, inheriting the run's flip. **Nothing here maps a socket by its NAME**: `socket_edge_e` is at local +X in the asset's frame and a deck yawed onto a site has local +X pointing site WEST, so the direction comes from the socket's own local position rotated by the part's quaternion and resolved against the site's east and north. **Measured (`probes/basesnap.js`, driven, at least one placement through a real `PointerEvent`): deck to deck gap 1.392e-12 m against the module read off the socket; a wall's origin 0.000e+0 m from the deck socket the ghost SAID it caught, and 0.000e+0 m from the nearest one, which are two separate assertions; two panels of a run 0.000e+0 m end to end.** | The arithmetic was never wrong and that is exactly why the complaint survived: adjacent foundations already met at 1.2e-12 m (GP-22), so every existing probe was green while Reid reported that nothing snapped. What was missing is that the address came from wherever the AIM RAY stopped, and a ray pointed at a placed foundation stops on its top face, so the answer was "already built here" for the whole 4 x 4 m of the thing you were standing on. The structures lane had published the machinery for this in ASSET-SPECS 4.23 and never wired it. Reading the socket rather than recomputing `cellM * 0.5` is what makes the claim measurable at all: an anchor recomputed from the module agrees with itself whatever the assets ship. | Accepted | 2026-07-27 |
| GP-38 | **THE CANTILEVER EXISTS. A deck with an orthogonally adjacent deck on the same level and site may hang one STOREY (4.00 m, read off `module.storey`) instead of `FLOAT_TOLERANCE_M` (0.90 m), the run is capped at 3 cells from the nearest deck that genuinely rests on the ground, and BURY IS NOT RELAXED.** `carryRun` is a breadth-first walk over the deck lattice recomputed per ghost frame rather than a stored depth: no save-schema change, and a base whose hillside has since been mined away is re-judged honestly, which is GP-28's live-ground argument applied to support. `restsOnGround` re-measures a placed deck's own footprint against the live oracle. **Measured (`probes/cantilever.js`, on ground the probe scanned for rather than assumed, 1.07 m of drop per cell): cells at runs 1, 1, 2 and 3 accepted hanging 0.50, 2.00, 3.35 and 3.91 m, the fourth refused at run -1 with "nothing under this and no solid ground within 3 cells", and DW-32's pillar drawn under the overhang at 3 decks / 9 pieces / 3.152 m tallest.** THE NEGATIVE CONTROL IS THE PART WORTH KEEPING: the same cell, `d:0,2,0`, with its three carrying decks pulled up and nothing else changed, hangs the identical 2.0014 m and is refused with "it would hang 2.00 m clear". | `supported()` returned `true` unconditionally for every level-0 deck, so DW-32's headline feature had no neighbour concept behind it at all. The cap is not pedantry: with a pillar drawn under it a cantilever reads as engineering, and without a bound a player steps one deck at a time across a canyon for ever, which is the thing the pillar was meant to make honest rather than to enable. The negative control is what makes the other two assertions mean anything, because a build that had simply raised `floatToleranceM` to one storey for everything would pass both of them perfectly; doing it on the SAME cell rather than on some other one is what removes the ground from the comparison, leaving only whether a neighbour was standing there. | Accepted | 2026-07-27 |
| GP-39 | **A MACHINE PLACED OVER A DECK SITS ON THE DECK'S TOP SURFACE, and the hand furnace finally leaves /core's voxel lattice.** `MachinePlacement.anchorIn` asks the base `deckTopAt(site, e, n, u)` and takes the level's base plane plus the asset's own `socket_top` height when a deck covers the cell, with the SITE's up rather than the local radial so the machine is flush with the plane instead of with the sphere it is tangent to. **The two lattices are NOT re-unified**: the structural cell stays 4 m and `MACHINE_TILE_M` stays 1 m, deliberately different constants sharing one site frame, and the machine's tangent coordinates are simply divided down into the structural cell containing them. `MachineAddr.u` carries the aim's own height so a furnace aimed at the ground floor of a two-storey base lands on the ground floor. **Measured: `onDeck` true and 6.7e-12 m above the deck's own `socket_top`.** | "Items like smelters dont sit ontop of the foundation" was exactly true and it was one line: a foundation deliberately writes nothing to the voxel layer (DW-24), so a machine taking its radius from `of_surface_radius` stood on the ground UNDER the deck and sank half a metre into it. The second half is that `Machines.ts` was the LAST thing in the build system still snapping to `of_cell_for_pos` after GP-27 moved drills, belts and factory smelters onto the site frame, so a hand furnace and the belt running into it disagreed about where a metre starts by up to 0.41 m. The lattice fallback is not kept for a host-less client: a grid nothing else uses any more is worse than no grid, so that path is the live oracle at the point itself. | Accepted | 2026-07-27 |
| GP-40 | **STRUCTURAL COSTS RE-PRICED AT THE 4 m MODULE: foundation 40 Stone, floor 20 Wood + 20 Stone, wall 12 Wood, door 16 Wood + 4 Iron.** 10x on the two decks and 4x on the two wall parts, **deliberately short of area parity**. `structureDefs()` in `gameplay.h`; `Demolition` refunds follow for free because they read the same def. **Measured end to end in survival: 40 Stone charged per foundation, read from /core's own quoted cost rather than from a literal in the probe, and a short pack refused by name with "need 16 Wood + 4 Iron".** | DW-32 made every part sixteen times the area in plan, so a 20 x 20 m platform silently fell from 400 foundations at 1,600 Stone to 25 at 100: a 16x discount nobody chose. Full parity would have restored 1,600 and made the rescale purely cosmetic, and DW-32's stated purpose was to cut the grind rather than to relabel it, so 10x lands the same platform at 1,000. The two families scale differently because they are different shapes: a deck is an AREA and grew 16x, a wall is a LINE and grew 5.6x (1 m to 4 m wide, 2.50 m to 3.50 m tall), so 4x is the same discount applied to the same growth. The door's iron goes 1 to 4 because iron is the gate on a door and a gate that did not move would have been a 16x discount on the only interesting ingredient in the set. The `/core` test now pins the RELATION (a deck costs more than a wall, a door costs more than a wall) as well as the values, because the values will be rebalanced again and the ordering is the design decision. | Accepted | 2026-07-27 |
| GP-41 | **Player progression is a NEW header, `core/include/of/progression.h`, not a section of `gameplay.h`.** Additive and header-only over `gameplay.h`, exactly the pattern `research.h` established, carrying equipment, skills and appearance plus a `PlayerProfile` that holds all three so persistence has one thing to serialise and one thing to version. `progression_tests`: 10 tests / 823 checks. | Two reasons, and the second is the durable one. **(1)** `gameplay.h` had three lanes editing it in one night (survival content, the smelting ladder, the structural set) and a fourth simultaneous editor is how commits start sweeping each other, which happened to this very lane's re-price. **(2)** The layering is genuinely one-way: equipment reads the item registry and nothing in the item registry needs to know equipment exists, so a separate header states that rather than relying on section discipline inside a 1,400-line file. | Accepted | 2026-07-27 |
| GP-42 | **FOUR EQUIP SLOTS (head, chest, legs, feet), armour ItemIds at 0x0070+, and the mesh node name is a PURE FUNCTION OF THE SLOT.** `armourNode(EquipSlot)` returns the same four strings `assets/models/dist/player/armour_set.glb` ships, because the art lane's own contract says the node names are SLOT names and not SET names, so a second armour set is a second file carrying the same four nodes and nothing in the client moves. A suit SUMS damage reduction, MULTIPLIES encumbrance and is capped at `kMaxDamageReduction` 0.80; the tier-1 iron set totals 0.40 reduction at 0.892 move speed. **The ItemId block deliberately SKIPS 0x0044..0x006F**: 0x0050..0x006A is already spoken for by GP-31's vessel part items (`ItemId = 0x0050 + (PartId - 0x0100)`), which live in `web/wasm/of_vessel_api.inc` only because that lane could not touch `/core` and are due to be promoted. Minting armour on top of them would turn a scheduled promotion into a renumbering of saved worlds, and an ItemId is opaque (GP-13) so a gap costs exactly nothing. **Armour publishes `damageAfter(raw)` and does not apply it**, because `/core` has no damage model yet and the enemies lane is building one; a layer that silently changed numbers under twenty pinned tests would be a worse gift than an interface. | The art lane shipped four skinned armour meshes and left the honest note in `contracts.json`: "NO REFERENT IN THE HEADLESS HEADERS ... grepping core/include for armor\|armour\|equip returns nothing." This is that referent. The equip guard against a displaced piece that will not fit back CANNOT FIRE while armour stacks to one, because removing the incoming piece empties exactly the slot the outgoing one needs, and the test says so out loud rather than leaving a branch nobody can explain: `unequip` is the direction that genuinely refuses, because it adds without removing. | Accepted | 2026-07-27 |
| GP-43 | **Five skills, one per verb the game ALREADY has (Mining, Forestry, Smelting, Building, Piloting), a QUADRATIC curve (`100 * n^2`, cap 10), and LEVEL 0 IS EXACTLY NEUTRAL. Appearance is five bytes of PALETTE INDICES, not free colour.** `skillMultiplier(0) == 1.0f` bit-exactly, which is what makes the whole layer optional: a caller that never grants a point gets the numbers it has always had, so `harvestNode` and `Furnace` can opt in through `Skills::applyYield` without any pinned test moving. Rounding is DOWN and is published as one call rather than left as `int(n * mul)` at four call sites, three of which would round the other way within a month. | A skill with no action behind it is a line in a menu, so there are five and not fifteen. Quadratic rather than exponential because an exponential last level costs more than the whole game before it, and this project's progression spine is the tech tree rather than a level bar. A fixed palette is five bytes in a save, is trivially validated by `sanitise` (which is idempotent, so a load-save-load cycle cannot walk a player's face across the palette), gives the art lane a finite set of combinations to actually look at, and cannot produce a player the exact grey of the rock they are standing on, which is free RGB's most likely first use. | Accepted | 2026-07-27 |
| GP-16 | **Research / tech tree is a header-only additive layer `core/include/of/research.h`, consuming `gameplay.h` verbatim.** `TechId` (u16, opaque, stable, mirrors GP-13) + `TechDef{id,name,prereqs,cost,unlockRecipes,unlockEntities}` as a DATA `TechTree` (GP-12); `ResearchState` flips unlock bits the build/recipe UX gates on (`isRecipeUnlocked`/`isEntityUnlocked`). **Science is just `ItemStack`s in an `Inventory` pool** (no new mechanism — the factory produces science like any item); science packs append in the SAME `ItemId` space at `0x0020+` (C-3, never renumbering the §7.1 slice block) via the one additive `SliceRegistry::registerItem` extension point. The OFF-WORLD GATE (GP-2) is a tech whose `cost` names a Cinderite-derived science pack — pure data, no special-case code. | Mirrors the Wave-0 cores' thin-layer-over-closed-substrate pattern; keeps the pinned slice content & id space untouched; tech/science are data so balance iterates without code (GP-12); the gate is a cost row, not a branch | Accepted | 2026-06-15 |

## 4. Architecture & approach
- **Progression loop:** explore → find resources/POIs → automate extraction (factory-sim) → produce science → unlock tech → build bigger/leave the planet → repeat off-world. The tech tree is the backbone; quests are signposts on it.
- **Off-world gating:** balance recipes so essential mid-game items need resources only found on the moon/other bodies (world-gen WG-4 resource identity). This is the mechanism that *makes the crossover matter*.
- **Map/maneuver UI:** consume physics' orbital state + predicted trajectories to render an interactive map view with maneuver nodes (KSP's signature UI). One of the highest-value, highest-effort UI pieces.
- **Loot/POIs:** define content tables + rarity; world-gen places, gameplay binds contents + looted-state (a diff to persistence).
- **Data-driven:** recipes, tech, quests, loot tables as data assets so balance iterates without code.

## 5. Interfaces & dependencies

> **Phase-1 detail pinned in [gameplay-phase1.md §8](../phase1/gameplay-phase1.md#8-interfaces--consumed-pinned-and-provided-new).** Below is the controller-level summary.

**Consumes (PINNED Phase-0 contracts — honored verbatim):**
- physics: **`FVesselOrbitalState`** (Mode, DominantFrame, Elements, Position, Velocity, `PredictedPath`, `NextSOITime/Frame` — [spike2 §8.2](../spikes/spike2-physics.md)) for flight HUD + map overlay (*we draw, physics computes*); character↔craft state change ([spike2 §6](../spikes/spike2-physics.md)) for the board/disembark handoff.
- factory-sim: **entity-state stream** (`FFactoryEntityState`/`FBeltFlowState`) + **delta/event stream** (`MachineCompleted`/`PowerStateChanged`/`InventoryChanged`/`EntityPlaced` — [spike3 §6](../spikes/spike3-factory-sim.md)) for feedback, production rates, brownout HUD, objective predicates.
- world-gen: **`FBodyParams`** (altitude/datum) + **`RaycastTerrain`/`GetGroundContact`** (+`SlopeRad`/`SurfaceHardness`, RC-7) for ghost surface projection + slope validity ([spike1-worldgen §4–5](../spikes/spike1-worldgen.md)).
- core-engine: `FUniverseCoord`/`FFrameId`, `IFrameGraph`/`FReferenceFrame.ToUniverse`, `OnSOIChange`, `ISimClock` ([spike1-core §5, §2.3](../spikes/spike1-core-engine.md)).
- persistence: loads/saves gameplay's owned `Persistable` (see Provides).
- rendering: gameplay *requests* camera/overlay modes (build-overlay camera §3.3.3, map overlay §5.3) — confirm "gameplay can request a camera mode" is in rendering's surface.

**Phase-1 cross-domain contracts (full detail [gameplay-phase1.md §8](../phase1/gameplay-phase1.md#8-interfaces--consumed-pinned-and-provided-new)):**
- **C-3 `ItemId`/`FItemDef` (§8.C) — gameplay OWNS · PINNED.** `ItemId = uint16`, opaque, hand-assigned stable ids (GP-13). The SINGLE id space: player inventory + factory machine inventories + deposit→item map + persistence. Consumers hold the `uint16` opaquely. Canonical id block in [phase1 §7.1].
- **C-4 `FGameplayPersistState` (§8.D) — gameplay OWNS · FINALIZED.** Exact fields pinned (avatar pos/rot/`FFrameId`/mode/control-mode/craft, inventory `(ItemId,Count)[]`+hotbar, objective step/flags, `SeenPrompts`). persistence accepts; gameplay implements `IPersistable` for exactly this (C-6).
- **C-5 intents (§8.E) — gameplay PROVIDES · FINALIZED.** All 9 fully-fielded + `QueryEntityInventory` read; each carries `FPlayerId` (GP-14). Awaiting physics + factory-sim field confirmation.
- **C-1 `FDepositNode` (§8.A) — world-gen OWNS** (three-way w/ factory-sim) · **PINNED (WG-11).** `Resource` field IS the gameplay `ItemId` directly (C-1/WG-11: Resource is the ItemId directly — the proposed `DepositTypeId` indirection was dropped; no separate deposit-type id space).
- **C-2 `FRecipeDef`/`FEntityDef` (§8.B) — factory-sim OWNS;** gameplay authored slice content (§7) against the shape. Awaiting factory-sim schema pin.

**Provides (outbound — player intents/commands; sim stays authoritative, D-004-clean):**
- **→ physics:** `ICraftControlIntent`, `ICharacterMoveIntent`, `IBoardIntent`, `IDisembarkIntent`. *(fully fielded, [phase1 §8.E])*
- **→ factory-sim:** `IFactoryBuildIntent`, `IPlaceBeltIntent`, `IRemoveEntityIntent`, `ISetRecipeIntent`, `ITransferItemsIntent` (+ a `QueryEntityInventory` read).
- **→ persistence:** `FGameplayPersistState` (player state only; factory/orbit/terrain diffs belong to their owning domains).
- **Content data (PINNED, [phase1 §7]):** 12-item `ItemId` set (Ferrite ore/plate, Frame part, Cinderite, Combustite + 7 buildables), 5 recipes (`FRecipeDef`), 7 entity defs (`FEntityDef`), slice objective chain — data assets consumed by factory-sim (recipes/entities) + world-gen (deposit types).
> *Tech/quest/loot data and the research-gate version of off-world resources are Phase-2+ (P1-D4) — not provided in the slice.*

## 6. Task backlog / roadmap
- [Phase 1] **Headless gameplay-logic core — DONE (Wave-0, green).** `core/include/of/gameplay.h`: item registry + slice content, inventory, node mining, build validation+placement (into factory-sim), recipe I/O, objective FSM. Tested by `core/tests/test_gameplay.cpp` (`gameplay_tests`, all 6 ctest suites green).
- [Phase 1] **Remaining (needs rendering/UE):** the build/placement *UX* (ghost, ports, build-overlay camera), inventory/hotbar UI, HUD + map overlay — the presentation layer over this logic core.
- [Phase 2] **Research tree + off-world gating — LOGIC CORE DONE (green).** `core/include/of/research.h`: `TechId`/`TechDef`/`TechTree` (data, GP-12), `ResearchState::tryResearch` (produce-science→unlock, GP-1), the `CinderiteRefining` off-world gate (GP-2). Tested by `core/tests/test_research.cpp` (`research_tests`, all 12 ctest suites green). **Remaining (needs rendering/UE):** the research-screen UI (tech-tree graph, affordability/prereq greying, "queue research"), science-pack production recipes wired into the factory palette, and binding the unlock bits to the live build palette / craft menu. **Still Phase 2:** map/maneuver-node UI v1.
- [Phase 1] **WP1 primitive-survival-crafting slice — HEADLESS CORE DONE (green).** `gameplay.h` §S (`survival` ns) + `deposits.h` §S (`worldgen::survival` ns): raw-resource items + terrestrial node kinds, deterministic test-area layout (`LayoutTestArea`), tool-assisted hand harvest (`harvestNode`), all-or-nothing hand-crafting (`HandCrafter`), and a fuel-driven `Furnace`/`Smelter` (GP-19). Tested by `core/tests/test_survival_slice.cpp` (`survival_slice_tests`, all 15 ctest suites green).
- [Phase 1] **WP2 primitive-survival-crafting PLAYABLE SHELL (UE) — DONE.** `/Game/Maps/SurvivalTest` is a walkable flat plot bound end-to-end to the WP1 `/core` API. C++ in the `OrbitalFoundryCore` plugin: `AOFSurvivalCharacter` (WASD + mouse-look + E-to-harvest; legacy `BindAxisKey`/`BindKey`, no input assets) carrying a `UOFSurvivalComponent` (pImpl over `gameplay::SliceRegistry`+`Inventory`, `RegisterSurvivalContent` once, UFUNCTIONs for inventory rows / hand-recipe rows / craft / harvest); 7 `AOFResourceNode`s (one per `NodeKind`, tinted engine shapes) that `survival::harvestNode` into the pack and deplete; two `AOFFurnaceActor`s (furnace + faster smelter, each owning a `survival::Furnace` via pImpl, **ticking in PIE** to burn fuel + smelt ore→ingots). `AOFSurvivalGameMode` (runtime module) sets the character as the SurvivalTest default pawn (per-map WorldSettings override; global flight GameMode untouched). UMG `WBP_SurvivalHUD` shows the live inventory + the 4 `handRecipes()` with craftable flags. Verified in PIE: harvest→inventory→craft tools/furnace→smelt→ingots, all through the `/core` rules. Screenshots `docs/screenshots/M2.4_{survival_plot,inventory_hud,crafting_menu,furnace_smelting}.png`. **Residual:** craft + furnace ore/fuel loading are driven via component/actor UFUNCTIONs (no per-recipe Craft *button* graph nor walk-up furnace-load overlap wired yet).
- [Phase 1] **WP2.5 primitive-survival VISUAL + PLAYER-FEEL pass (UE) — DONE.** Skinning/feel only (no `/core` or WP1-API change): first-person-default camera with smooth captured mouse-look + **Numpad-3** third-person toggle (fixes the old drag-to-look from the GameMode forcing a cursor), a real (blocky, Mannequin-ready) player model visible in TP, a **meadow** floor (`M_Meadow` noise material + scattered flowers/tufts), and **ore-patch** node visuals (clustered rock chunks per kind, tree/pools) with "+N <resource>" harvest feedback + depletion shrink. Screenshots `docs/screenshots/M2.5_{firstperson,thirdperson,meadow,ore_patches,ore_ring}.png`. **Deferred to Admin-coordinated next pass:** inventory/craft menu UI overhaul, dropped-ore item entities + pickup, mining/tool animations.
- [Phase 1] **WP2.6 SECOND visual pass — real Mannequin + stylized environment (UE) — DONE.** Skinning only (no `/core`/WP1-API change; keeps all WP2.5 camera/input fixes). **(1) Real player:** migrated the engine UE5 Mannequin (`SKM_Manny_Simple` + `SK_Mannequin` skeleton, `ABP_Unarmed` locomotion anim BP, `BS_Idle_Walk_Run` blendspace, mats/textures/rigs) from `Templates/TemplateResources/High/Characters/` into `/Game/Characters/Mannequins/` (filesystem copy — internal refs already `/Game/Characters/...`, resolved clean, no migration tool). `AOFSurvivalCharacter` now sets `SKM_Manny_Simple` + assigns `ABP_Unarmed_C` as AnimClass → idle/walk/run play from CharacterMovement; shown in TP, hidden in FP. **(2) Stylized meadow:** `M_Meadow` rebuilt — three-tone green grass + DESATURATED warm-brown dirt patches (old Voronoi read RED; fixed with thresholded gradient-noise mask, grass-dominant); the sphere-on-stick tree → a real low-poly conifer (tapered trunk + 3 stacked faceted canopy cones); +~420 clustered wildflowers (5 colours) + ~960 grass tufts; sun raked DOWN (was pitched up → unlit). **(3) Faceted ore:** `AOFResourceNode` ore kinds now build procedural low-poly faceted-rock boulders (deformed icosahedra in a `UProceduralMeshComponent`) + emissive mineral crystal accents, via new `M_OreRock` (BaseColor/Metallic/Roughness) + `M_OreCrystal` (emissive); iron=rust+metallic, copper=grey+teal patina, coal=black glossy, stone=grey. Harvest feedback + shrink-as-depleted preserved. Screenshots `docs/screenshots/M2.6_{mannequin_thirdperson,firstperson,meadow,ore_patches}.png`. Mannequin content adds ~98 MB to `ue/Content/`. **Residual:** ABP_Unarmed migrated WITHOUT ControlRig plugin enabled, so foot-IK post-process may be inactive (core locomotion fine); water/oil still flat tinted discs (not glossy); no mining/tool anim (Admin's next phase).
- [Phase 1] **WP2.7 PLAYER MENU — toggleable inventory + crafting + drag-to-move + polish (UE) — DONE.** UI/UX only (no `/core`/WP1-API change; keeps all WP2.5/2.6 camera/input fixes). **(1) Polish:** the red debug-sphere domes drawn around every `AOFResourceNode` (r=250) + `AOFFurnaceActor` (r=300) interact volume are gone — `SetHiddenInGame(true)` + `bDrawOnlyIfSelected` + `SetVisibility(false)` (editor wireframe now only when the actor is selected); the play view is clean. The third-person key now binds **BOTH `EKeys::Three` (top row)** and `EKeys::NumPadThree` (users without an active numpad couldn't toggle before) — PIE-confirmed top-row 3 switches FP↔TP both ways. **(2) Toggleable menu (`UOFPlayerMenuWidget`, built in C++ — no authored WBP):** opened/closed with **Tab / I**; on open shows the cursor + `GameAndUI` input, on close hides cursor + `GameOnly` (restores smooth FP look — the menu is the ONLY time the cursor appears); free-look/move/interact are suppressed while open. **Inventory tab** = a `UUniformGridPanel` of coloured slot tiles (per-item-id hue + name + count from `UOFSurvivalComponent`), live via `OnInventoryChanged`. **Crafting tab** = the `GetHandRecipes()` rows (name + input requirements + a Craft button enabled only when `bCanCraft`, calls `CraftHandRecipe`). **(3) Drag-to-move:** a UE-side ordered slot model on the component (`SlotOrder` + `GetInventorySlots` + `MoveSlot`), reconciled with the live `/core` pack each change so the player's dragged layout persists across harvest/craft; tiles implement UMG drag-detect/drop (`UOFSlotDragDropOp`) → `MoveSlot` swaps slots. **Verified in PIE:** harvest fills the grid; crafting a Crude pickaxe decremented Wood+Raw iron and added the tool live; `MoveSlot(0,3)` swapped tiles; top-row 3 toggles TP; no debug spheres. 2 commits (`821eba4` debug-sphere removal, `5a28f7c` menu+key+drag). Screenshots `docs/screenshots/M2.7_{inventory_menu,crafting_menu,thirdperson_fixed,no_debug_spheres}.png`. **Residual:** the always-on `WBP_SurvivalHUD` text overlay still exists (menu augments it; a trimmed hotbar/health-only HUD line is a future pass); drag is swap-only (no stack-split/partial-drag); water/oil nodes still flat discs; dropped-ore entities + mining/tool anims still deferred to Admin's next pass.
- [Phase 1] **WP2.8 DROPPED-ITEM ENTITIES — physical drops + walk-over pickup + capacity + full-pack mining spill (UE) — DONE.** Item ENTITIES only (no `/core`/WP1-API change; keeps all prior camera/input/menu fixes). **(1) `AOFDroppedItem`:** a physical item lying in the world — holds an `ItemId`(+count) and renders a small per-item **"item skin"** (faceted ore-chunk + mineral colour for ores/coal/stone, a short log for wood, a metal bar/ingot for iron/copper, a crude tool proxy for tools, a tinted blob for water/oil, generic chunk fallback). Spawned with a forward/upward toss so it arcs out. **The survival floor is QueryOnly (no physics body) so a simulating rigid body falls straight through** — the item instead integrates its own ballistic arc + line-traces DOWN (query traces hit the floor) to land + settle (rest z≈11). A per-frame distance scan auto-collects it into a nearby character's pack (`AddItem`) and destroys it, with a ~0.5 s spawn-protection so a just-dropped item isn't instantly re-grabbed; if the pack is full it's left on the ground. `AOFDroppedItem::Spawn(World,ItemId,Count,Loc,TossDir)` is the shared spawn helper (reused by drop-from-menu + mining spill). **(2) Capacity:** the `/core` `Inventory` is ALREADY a fixed-slot pack (20 slots, per-item `stackMax`, `add()` returns overflow), so capacity is *surfaced* not duplicated — `UOFSurvivalComponent` gains `GetSlotCapacity`/`GetUsedSlots`/`IsFull`/`CanAcceptItem` + a `DebugFillInventory`; "full" is a real, testable state. **(3) Drop-from-menu:** three routes, all through the character's `DropItemFromInventory` (remove from pack → spawn entity) — drag a slot tile OUT of the grid onto the world backdrop (`NativeOnDragCancelled`, guarded by a `bDroppedOnSlot` flag so a slot-to-slot rearrange doesn't also drop), click-select a slot + the menu's **Drop 1** button, or press **Q** to quick-drop the selected/first item; the inventory page gains a selected-item footer + Drop button, tiles gain click-select highlight. **(4) Full-pack mining spill:** `UOFSurvivalComponent::HarvestIntoOrDrop` mirrors `/core` `harvestNode`'s yield rule but ALSO extracts the units that don't fit + reports the overflow, and `AOFResourceNode::Interact`→`SpillToGround` spawns one dropped entity per spilled unit fanned around the node, so mining a full pack rains items onto the grass instead of silently leaving them in the node. **Verified in PIE:** 10 items of every kind fell + settled + were all auto-collected on walk-over; pack fills to 20/20 (`IsFull` true); dropping a Wood from the menu removed it (12→11) + spawned an entity; mining an Iron-ore node 3× with a full pack depleted it 250→241 and spawned 9 dropped iron-ore chunks on the ground. 3 commits (`ebc50f6` entity+pickup, `30a7f27` capacity+drop-from-menu, `161a951` full-pack spill). 5 screenshots `docs/screenshots/M2.8_{dropped_items,pickup,menu_inventory,menu_drop,full_inventory_spill}.png`. **Residual:** drops use a custom ballistic settle (no rolling/bounce physics — the world floor is QueryOnly); pickup is auto-on-proximity (no manual key); drop-from-menu spawns one entity per unit; **mining/pickup ANIMATIONS + tool-in-hand skins still deferred to Admin's next pass.**
- [Phase 1] **WP2.9 TOOLS-IN-HAND + HARVEST SWING ANIMATION (UE) — DONE.** Character/anim/mesh only (no `/core`/WP1-API change; keeps all prior camera/input/menu/dropped-item fixes). **(1) Tools in hand:** a new `UProceduralMeshComponent ToolMesh` on `AOFSurvivalCharacter` builds a **pickaxe** (double-spike metal head) or **axe** (flat blade + poll) from flat-shaded boxes/wedges (wooden handle section + metal head section, per-section MID off `M_FactoryColor`). It's attached to the Mannequin's **`hand_r` bone** via `AttachToComponent(..., HandBone)` (no socket asset needed — the bone exists on the Manny skeleton) and laid into the grip with a tuned relative transform (pitch −60°, loc (−8,6,0), scale 1.3 — tuned live in PIE). `EquipToolFor(node)` picks the tool from the node kind — **axe for `Tree`(wood), pickaxe for `Rock`/`CoalSeam`/`IronOre`/`CopperOre`, none for water/oil** — and `SetHeldTool` (re)builds + shows/hides it. Shown in TP, attaches to the capsule at hand height in the blocky-body fallback. The tool is equipped on harvest (E) and persists. **(2) Harvest swing:** `PlaySwing()` plays one of the migrated engine Mannequin **`Attack` AnimSequences** (`MM_Attack_01/02/03`, 1 s each on SK_Mannequin, cycled round-robin) as a **dynamic montage on `DefaultSlot`** (`UAnimInstance::PlaySlotAnimationAsDynamicMontage`, blend 0.12/0.18, rate 1.4×) — no ABP graph edit, no authored montage asset. It overrides the upper/whole body for the ~0.7 s chop then blends out, leaving `ABP_Unarmed` idle/walk/run driving again (CharacterMovement is independent, so the character still WALKS during a swing); locomotion composition confirmed (montage finishes → loco resumes, max-walk-speed 600 intact). Tied into `HarvestNearest` so pressing E equips the right tool AND swings it. **Verified in PIE (third person):** harvesting a tree equipped the axe (`HeldTool.AXE`), harvesting iron/stone/coal/copper equipped the pickaxe (`HeldTool.PICKAXE`), both with 2 mesh sections + visible; `HarvestNearest` returns `is_any_montage_playing()==True` mid-swing on the loco anim instance. 2 commits (`27fa93b` tools-in-hand, Task-2 swing). 3 screenshots `docs/screenshots/M2.9_{tool_in_hand,tool_axe,mining_swing}.png`. **Residual:** swing montage plays on `DefaultSlot` (full-body override for ~0.7 s — fine for a stationary harvest; an upper-body-only slot in `ABP_Unarmed` would let the legs keep walking under the chop); the small pickup-bend nice-to-have was not added; tool is shown on harvest, not gated on owning the crafted tool.
- [WEB] **Ore deposits are PATCHES, and a drill goes on one: DONE & driven-verified (2026-07-26).** `/core`: `deposits.h` §P (`OrePatch`, `lobeShape`/`coverageAt`/`richnessAt`, `extract`, `sampleDisc`, `outcropAt`, `LayoutPatchField`, `findPatch`) + `gameplay.h` §S.5 (`harvestPatch`, which reuses `harvestNode` verbatim so no rule is restated). WASM ABI 3: the `of_gp_patch_*` surface + `of_gp_node_add_outcrop`; a linked node's state is re-derived from its patch on every read and `of_gp_node_drain` mutates the patch, so one deposit has one number. Web: `game/OrePatches.ts` (typed face), `game/OreField.ts` (the ground skin, one merged geometry / one material / ONE draw call, plus the outcrops), `game/NodeArt.ts` (art + the four ore-ground colours, split out of NodeField), `Factory`/`BuildMode` bind a drill to a patch and report its rate, save slot v3 keys the depletion diff by patch. Tests: 4 patch suites in `deposits_tests` + 1 in `survival_slice_tests`, 22/22 ctest, self-determinism 119/119, cross-toolchain 94/94. Driven: `web/tools/smoke/probes/deposit.js` `valid: true` (extent 8.14 m radius / 11.99 m span / 1,531 units; hand +3 and the patch -3; drill 17 = drain 17 = patch loss 17 = buffer +17; the off-deposit refusal at 14.26 m clear with 0 buildings placed; depletion 1,516 -> regrown 1,531 -> loaded 1,516). Draw calls 40 surface / 47 with a drill on screen, budget 150. Screenshots `docs/screenshots/W7_ore_patch.png`, `W7_ore_patches_wide.png`.
- [WEB] **BASE BUILDING: DONE and driven-verified (2026-07-26).** `/core`: `gameplay.h` section S.6 (`StructureKind`, `StructureDef`, `structureDefs()`, four ItemIds at 0x0040+, four TypeIds at 0x40+, `CraftRecipe` costs, `HandCrafter::payInputs`), 4 new tests in `survival_slice_tests`, 22/22 ctest, parity 119/119 and 106/106. WASM ABI **5**: `of_gp_structure_count/_info/_can_afford/_pay`. Web: `StructureGrid` (the site frame, module constants measured off the shipped sockets), `StructureBody` (the `col_*` proxies as boxes plus the walker port), `StructurePlacement` (the four refusals, each with a sentence on the ghost before the key is pressed), `StructureView` (one `BatchedMesh`, the ghost, the door swing driven off the authored clip with no `AnimationMixer`), `Structures`, `StructureSave`; `player/HeightfieldWalk.ts` split out of `KinematicBody` to make room for the port. Keys 4 to 7 take a part in hand, B takes the snap off, R flips a wall, E opens a door, X takes a part back with a full refund. Slot version 4 carries the parts and their site frames. Costs: foundation 4 Stone, floor 2 Wood + 2 Stone, wall 3 Wood, door 4 Wood + 1 Iron. Probes `build.js` (`valid: true`), `buildtol.js` (the DW-24 measurement), `buildshot.js`. Screenshots `docs/screenshots/W8_base.png`, `W8_door.png`. Draw calls 31 surface / 51 with a base, budget 150.
- [WEB] **Digging into an ore body pays: DONE and driven-verified (2026-07-26).** `game/DigOre.ts`, handed to `DigAction` as a port so `src/player` still knows nothing about deposits: `of_gp_patch_find` on the strike centre, `of_gp_patch_drain` for the amount, and the grant IS the drain, so a deposit keeps one number (DW-25). Yield 2 units a strike at full coverage against a bare swing's 3, a pickaxe swing's 9 and a drill's 3.0 a second, so it obsoletes no tool. `probes/digore.js` `valid: true` (10 strikes on a patch centre, 10 paid, pack +20 and patch -20 exactly; 6 strikes 20.14 m clear of every patch, +0 and -0).
- [WEB] **THE CONTROLS, and belt alignment: DONE and driven-verified (2026-07-26).** Left click uses what is in hand (swing, dig, place, and HOLD to lay a run); E interacts and never harvests; Escape closes any menu; 1 to 9 and the wheel choose a hotbar slot. New: `player/Bindings.ts` (the one binding table), `ui/ModalStack.ts`, `ui/HotbarBar.ts` + `styles/hotbar.css`, `game/Hotbar.ts`, `game/GameplayInput.ts`, `game/MachinePlacement.ts`, `game/FactoryCommit.ts`. Machines moved off the voxel lattice onto the base's metric site grid, so `Factory.snap` addresses (site, i, j) and a cell key is `m<site>:<i>,<j>`; save slot version 5. Probe `controls.js` `valid: true` (1,180 ticks): wheel 4 -> 7 -> 4; bare hand dug 7 cells and placed nothing; a drill off the ore refused by name and built nothing; E opened a furnace and granted 0 of 27; one held press laid **15 belts in ONE run**; Escape closed 3 of 3 modals from the DERIVED list. a furnace held down dug 0 cells and an empty slot 0 where the bare hand dug 37; and the loadout survived a save with the live bar SCRAMBLED in between. **Worst tile-to-tile tangential error 4.006e-6 m against the old lattice's 0.4098 m**, and a run on an 11 degree slope is now a RAMP (residual kink 0.018 m) rather than a staircase of 0.189 m steps. Draw calls 41 surface / 49 with a 16-tile run, budget 150. 22/22 ctest, self-determinism 119/119, cross-toolchain 106/106, no ABI change. ARCHITECTURE 15.2 items 105 to 107.
- [WEB] **SANDBOX MODE: DONE and driven-verified both ways (2026-07-26).** DW-31 / GP-29. New `game/GameMode.ts` (`GameMode`, `ModeRules`, `urlForMode`); `?sandbox=1` in `app/Config.ts`, decided once in `Boot.ts`; gates in `Structures` (cost + the new `affordInCore` that publishes /core's un-overridden verdict), `Machines.place` and `GameplayActions.craft`; `GameplayViews.recipeRows(…, all)` offers the whole catalogue; the `SANDBOX` chip in `ui/GameHud.ts` (outside `setVisible`); the mode row and its switch at the foot of `ui/InventoryPanel.ts`; `SaveGame.slotKey` splits the slot into `auto` / `auto-sandbox` with `readSlot(mode)` returning a typed refusal, and `Persist` carries the mode into the ledger and flashes a refused one. Read-only `__of.sandbox()`; **no `sandbox(true)` setter anywhere, on purpose.** Probe `sandbox.js` `valid: true` in BOTH modes: with an empty pack a foundation is refused `need 4 Stone` / 0 placed in survival and accepted `free (sandbox)` / 1 placed with the pack still empty in sandbox; /core's own affordability reads FALSE in both; the craft panel offers 0 of 4 against 4 of 4 and a REAL DOM click on the Craft button gives 0 -> 0 against 0 -> 1; a decoy survival slot survives a whole sandbox session byte for byte; a mislabelled slot is refused with `slotRefused: 'mode'` and the world untouched; the mode survives save + reload. `build/controls/persist/inventory/realclick/objectives` re-run green. No `/core` change, ABI still 5, save version still 5 (the `mode` field is additive). Screenshots `docs/screenshots/W9_sandbox_base.png`, `W9_sandbox_menu.png`. ARCHITECTURE 15.2 items 117 to 118.
- [WEB] **THE ASSEMBLY BAY: DONE and driven-verified in BOTH modes (2026-07-26).** DW-29 / DW-30 item 4, GP-30 to GP-35. **WASM ABI 6** (`web/wasm/of_vessel_api.inc`, `of_staging_api.inc`, `of_flight_api.inc`, included into `of_core_api.cpp`): the 24-part catalogue as data, part ItemIds `0x0050..0x006A` with authored build costs, the vessel tree, `of_vs_autostage` / `of_vs_stage_move`, the per-stage delta-v, mass and TWR figures, the atmosphere as pure functions, and a `FlightSim` pass-through landed in the SAME bump for the flight lane. Client: `sim/wasm/vesselabi.ts`, `game/VesselCatalogue.ts`, `VesselDesign.ts`, `VesselNodes.ts`, `Vab.ts`, `VabView.ts`, `VabPaint.ts`, `VabCamera.ts`, `VabInput.ts`, `VabRows.ts`, `VabStore.ts`, `ui/VabPanel.ts` + `styles/vab.css`, `app/DebugVab.ts`; `Scenes`/`CameraRig`/`Frame` gained the bay's scene, camera and one boolean. C opens and closes it, Escape closes it through the derived modal list. **Measured, driven, in the browser:** the reference vehicle assembled through the UI reads per-stage **1857.7928 + 3065.1153 = 4922.9081 m/s** vacuum against /core's 1857.79 + 3065.12 = 4922.91, launch mass **9845 kg**, pad TWR **1.6566671** against 1.6567, length **12.10 m**, static margin **-0.6007 m** (stable); the worst stack-joint gap measured on the DRAWN scene over 5 joints is **2.38e-08 m**; four fins land from ONE press at symmetry 4; a stage swap takes the budget **4922.91 -> 3445.45 m/s** and moving it back restores **4922.908064601136 bit-identically**; a design survives save, Clear and reload with 11 parts, 2 stages and its delta-v exact; survival offers 13 parts and builds NOTHING from an empty pack with 7 refusals by name while sandbox offers 24; and one whole placement is driven by a REAL `PointerEvent` on the canvas. Draw calls **26** with the rocket, 3 empty, budget 150. Probes `vab.js` (`valid: true` both modes) and `vabshot.js`; screenshot `docs/screenshots/W10_vab_assembly.png`. ARCHITECTURE 15.2 items 124 to 127.
- [WEB] **Belt spacing restored after the DW-32 rescale.** `MachinePlacement` was reading `module.cellM` for its tile pitch, so the structural module going 1 m to 4 m silently put belts 4 m apart and `probes/controls.js` measured one dragged run of four tiles reported as FOUR transport lines. `MACHINE_TILE_M` is now its own 1.00 m constant; machines and structures still share the site FRAME (GP-27) and no longer share the cell SIZE. Worst tile-to-tile tangential error back to **3.168e-06 m**, `controls.js` `valid: true`.
- [Phase 4] Questline, POIs/loot, exploration-science depth, structures.
- [Phase 5] UX polish; consider splitting UI into its own controller.

## 7. Open questions & risks
- **R1 (Admin Q3) — SETTLED:** first-person on foot / third-person+IVA in craft, via **P1-D1 / GP-6**. Control mapping + camera + handoff designed in [gameplay-phase1.md §1](../phase1/gameplay-phase1.md). Closed.
- **R2 (3D build legibility) — FIRST CUT DONE, playtest-validated:** recovered via **highlighted I/O ports + connection preview + a pulled-back in-world build-overlay camera + a flow/`VisualState` tint mode** (GP-7/GP-8, [§3.3](../phase1/gameplay-phase1.md)). Validate in playtest (P1-D6 / factory-sim R3); cheap escalation = stronger build camera before any flatten-pad feature.
- **R3 (no-transition map) — FIRST CUT DONE:** map = in-world **scaled-space overlay** (no scene swap), drawing physics-supplied `PredictedPath`/`NextSOI` conic; **read-only, no maneuver nodes in the slice** (GP-10, [§5.3](../phase1/gameplay-phase1.md)). Maneuver-node *interaction* deferred to Phase 2 with the research/map UI. Residual: overlay legibility vs live scene → fallback dimmed backdrop (still no swap).
- **R4 — gameplay's side CLOSED; awaiting consumer confirmation.** The contracts gameplay *owns* are pinned/finalized: **C-3 `ItemId`/`FItemDef`** (PINNED), **C-4 `FGameplayPersistState`** (FINALIZED), **C-5 intents** (FINALIZED, provider side). Remaining: world-gen pins **C-1 `FDepositNode`**, factory-sim pins **C-2 `FRecipeDef`/`FEntityDef`** schema (gameplay's slice content §7 is authored against the proposed shape and will confirm against the final), and physics+factory-sim confirm **C-5** field-for-field. None break a pinned Phase-0 contract — routing flagged to Admin in the Completion Report.

- **R5 (NEW, for physics) - TWO `PartDef::asset` STRINGS DO NOT MATCH THE SHIPPED GLB.** `vessel.h` says `LiquidEngineVacuumSmall` and `DecouplerRadial`; `assets/models/dist/rocket/rocket_parts.glb` ships `EngineVacuumSmall` and `RadialDecoupler`. 22 of 24 agree. `PartDef::asset` is documented as "the glb node name, EXACTLY", so the header is the side that is wrong. The client carries a two-entry `ASSET_RENAMES` table in `game/VabView.ts`, named as a bounded debt rather than a second naming authority (DW-26), and `probes/vab.js` FAILS if it grows past two. Two string edits in `vessel.h` delete it.
- **R6 (NEW, for physics) - TWO PARTS SHIP THE SAME DISPLAY NAME.** 0x0102 `TankLiquidSmallLong` and 0x0117 `TankLiquidLarge` are both "Fuel Tank (large)". They are different diameter CLASSES, so the client appends the class letter and the probe asserts no two labels collide. A distinct name in `vessel.h` makes that line a no-op.
- **R7 (NEW, cross-lane: physics and art) - THE REFERENCE VEHICLE HANGS A DECOUPLER UNDER AN ENGINE BELL, WHICH THE ART CONTRACT FORBIDS.** ASSET-SPECS 3.3 gives an engine no `socket_stack_bottom` because nothing may be bolted under a bell; `test_vessel.cpp` attaches a stack decoupler there anyway, and that joint is the interstage every KSP stack is built with. GP-32 allows it for a decoupler ONLY. The cost is that one joint of six has a socket on one side and none on the other, so it is reported `unmeasurable` rather than measured, and the probe asserts there is EXACTLY ONE. Either an engine grows a bottom socket or the reference vehicle stops using one; this lane should not pick.
- **R8 (NEW, for Admin) - THE PART ItemId BLOCK LIVES IN THE SHIM AND SHOULD NOT.** `0x0050..0x006A` is allocated in `web/wasm/of_vessel_api.inc` because this lane may not touch `/core`. It belongs in `gameplay.h`'s pinned table beside every other block (GP-13, physics R9). Until it is promoted, a part item cannot be added to the pack, which costs nothing today because a part is PAID FOR and placed.
- **R9 (NEW, for Admin) - THE BAY HAS NO IN-WORLD ENTRANCE.** It opens on the C key and closes on C or Escape, and that is the whole affordance. DW-29's actual entrance is a launch pad gated behind ground progression, which does not exist at any layer: `rocket/launch_pad.glb` is shipped art with no placement, no cost and no objective. Deliberately not invented here, because the gate is what ties the two halves of the game together and is an Admin sequencing call.

## 8. Subagent delegation log
| Date | Subagent task | Status | Outcome |
|------|---------------|--------|---------|
| 2026-07-26 | **The assembly bay's DOM panel** (scoped to two NEW files only, `web/src/ui/VabPanel.ts` + `styles/vab.css`, against an exact data contract) | Done | Briefed with the full `VabPartRow`/`VabStageRow`/`VabStats`/`VabPanelHooks` surface, the two mechanical rules (`src/ui` imports zero three.js, 400-line cap), `InventoryPanel` as the worked example, and the one thing that is easy to get wrong: the 3D rocket renders BEHIND the panel, so the root must be `pointer-events: none` with only the rails taking events back, or the player cannot click their own rocket. Delivered 375 + 375 lines, `npm run check` green first run, a clear middle gap of at least 44% of viewport width, and a `data-vab` attribute vocabulary that let the probe drive Save, Clear, the stage arrows, the symmetry buttons and the design chips as REAL DOM clicks. It also chose two warning states nobody asked for (pad TWR under 1.0, a stage with no engines) and flagged them as removable, which is the right way to exceed a brief. |
| 2026-07-26 | **The DW-32 structural rescale in the client** (1 m to 4 m module), scoped to `web/src/game/Structure*.ts` and two probes, explicitly FORBIDDEN `core/**` and every Vab/Vessel file | See report | Briefed with the art lane's authored constants, the instruction to RE-MEASURE `FLOAT_TOLERANCE_M` rather than scale it (a 4 m footprint spreads four times as far over the same slope, which alone consumes the old 0.55 m allowance), the GP-23 dead-band argument, and the call I had already made on the pillar: it is NOT a `StructureKind`, it is an automatic visual support, because DW-32 asks for pillars where the gap is large and that is a consequence of a placement rather than a placement of its own. Also told to REPORT rather than fix the structure costs, which are now wrong by a factor of 16 in area and live in `/core` where this lane may not go. |
| 2026-07-26 | **Sandbox mode (DW-31): no subagent spawned** | Done | Recorded because the choice not to delegate is itself a decision. The work was one small mode object plus one branch in each of three existing gates, and the expensive part was not the code but knowing WHICH gates exist and which are already free (machines and belts cost nothing; research is not in the client at all). A subagent would have had to be briefed with most of that survey to do anything useful, and the briefing is the work. The one place a subagent would have paid is a second pair of eyes on the negative controls, and that was covered instead by making the probe refuse to trust its own setup (it fails if the URL flag and the game disagree) and by publishing `affordInCore` so the page carries its own control. |
| 2026-07-26 | **Migrate the driven probe suite to the new bindings** (scoped to `web/tools/smoke/probes/` only) | See report | Roughly twenty probes drove the game by pressing E to harvest and G to place, and the remap broke all of them. Briefed with the old-to-new mapping, the hotbar loadout, the new `__of` surface (`hotbar`, `assignSlot`, `modals`, `escape`, `latticeCell`, `factory.list[].pos`), `probes/controls.js` as the worked example, and the three traps that have cost agents hours (`of.game()` rebuilds its report; input drains on the 60 Hz tick and not the render frame; the vite probe server caches modules until it is restarted). Given exclusive use of the 5199 probe server and forbidden from touching `web/src`. |
| 2026-07-26 | **Structural content in `/core` plus the WASM surface** (scoped to `gameplay.h`, `test_survival_slice.cpp`, `of_core_api.cpp`, `WASM-BRIDGE.md`) | Done | Four items at 0x0040+, four TypeIds at 0x40+, `StructureKind`/`StructureDef`/`structureDefs()`, and `HandCrafter::payInputs` expressed through the existing `canCraft` so the all-or-nothing rule is stated once. `handRecipes()` deliberately untouched, so the structural set never leaks into the Tab craft menu: a structure is placed from the build hotbar against its cost, not crafted into an item first. Exports `of_gp_structure_count/_info/_can_afford/_pay`, ABI **5**. 22/22 ctest, parity 119/119 and 106/106. **The one thing that went wrong is the one worth keeping:** the shim went to ABI 5 in a commit that left the client's `OF_ABI_VERSION` at 4, and HEAD stopped booting for everyone until it was closed. An ABI bump is atomic across the bridge, and it is now an Admin standing rule. |
| 2026-07-26 | **Restore `probes/demolish.js` to green against the patch model** (second attempt) | Done | The handed-down diagnosis was WRONG and the subagent said so, which is the right outcome: the drill measured 1.16 ore/s, which is rich patch centre. Two other things were broken. **(a)** `FactoryWiring.aheadOf` needs BOTH halves of "is this my neighbour", distance and alignment, and the probe was testing only distance; on this world two of the three lattice directions sit at dot 0.99 to 1.00 with the flow and the third sits at 0.02, dead perpendicular and well inside 1.35 m, which silently ends a run. **(b)** A SMELTER IS A SOURCE to `wire()`, so with the smelter within 2.25 m of the tail of the line feeding it, it inserts its own ingots back onto that belt; one ingot at the head jams the line for ever, because the head inserter only lifts the ore the recipe wants. The old line spanned 2.25 m end to end and both fired at once. Green at `valid: true`: 840/840/840 ticks, ingots 4/0/4, runs [5] to [2,2] and back, drill 1.05 ore/s, tail-to-smelter 3.61 m. Nothing relaxed. **(b) is a real trap in the wiring rather than a probe bug, and is raised to Admin.** |
| 2026-07-26 | **Restore `probes/demolish.js` to green against the patch model** (first attempt, scoped brief with `autoline.js` as the worked example) | Superseded | Same two causes: the drill went to the first accepted pitch, which is the poorest ground on the patch, and the coarse belt sweep skips lattice cells. The topology half of the probe (a run of 4 splitting to 2+1 when its middle is pulled) already reports correctly after the `chainRuns` fix; what fails is that no ore reaches the smelter, so all three windows produce 0 ingots. |
| 2026-07-26 | **Restore `probes/autoline.js` to green against the patch model** (scoped brief: one probe file only) | Done | The W6 auto-line acceptance broke on the shape change: a drill can now be placed anywhere on a 6 to 11 m patch including the far rim 9 m out, and the belt sweep's 1.2 degree pitch step then skips whole lattice cells so the tiles are not neighbours and `chainRuns` correctly reports 2 or 3 transport lines. Conservation still passes (patch loss == drill extraction); what fails is that no ore reaches the smelter. Brief carried the diagnosis and the three approaches already tried and rejected. **Green: `valid` true, 6 buildings, 4 belts, drill at 1.16 ore/s, beltPeakItems 1, smelterOutput 14, conservation exact (patch -29, drained 29, extracted 29; pack +14, buffer -14), and `closesTheLoop` still crafts a Smelter from automated iron. Nothing relaxed.** Its finding was worth more than the fix: **a lattice step is not a metre.** The 1 m body-frame grid is cut obliquely by the ground sphere, so one unit step of a cell key is 0.59, 0.81 or 1.02 m of ground depending on the axis. That turned out to be a real defect in `FactoryWiring.chainRuns` (it asked for the cell one metre along the flow and overshot its own neighbour), now fixed on the engine side as a distance-and-alignment test; ARCHITECTURE 15.2 item 102. |
| 2026-06-14 | Research thread (2 WebSearches, no subagent) — 3D factory-builder placement legibility (Satisfactory / DSP) | Done | Adopted into [gameplay-phase1.md §3.1–3.3](../phase1/gameplay-phase1.md): perspective split confirms P1-D1 (Satisfactory FP build / DSP TP); color-coded ghost (blue/red/yellow); **highlighted I/O ports** = the 3D-legibility key; snap-to-guideline + vertical modes. Sources: Satisfactory Wiki Build Gun, FICSIT modding holograms, Steam DSP-vs-Satisfactory, Wikipedia DSP. |
| 2026-06-15 | **WP1 — headless `/core` primitive-survival-crafting slice** (Admin build brief) | Done | Extended `gameplay.h` (new `survival` namespace §S) + `deposits.h` (new `worldgen::survival` namespace §S) + `core/tests/test_survival_slice.cpp` (12 tests / 1246 checks) + `survival_slice_tests` target. Authored the survival content set (13 items / 2 entity types / 2 smelt recipes, all append-only at `0x0030`/`0x30`/`0x0130`) registered via `RegisterSurvivalContent`; `LayoutTestArea` (deterministic small node patch); tool-assisted-but-not-required hand harvest (`harvestNode`/`assistingToolFor`); all-or-nothing `HandCrafter`; and a fuel-driven `Furnace`/`Smelter` gameplay-layer type (decision GP-19 — solid-fuel pool, NOT a factory-sim power machine; smelter faster via the `ticksPerSmelt` data param). **All 15 ctest suites pass** (the 14 prior — incl. unchanged `gameplay_tests`/`research_tests`/`slice_e2e_tests` — + survival). No pinned-id/interface change. **WP2 (UE layer) entry points:** `worldgen::survival::LayoutTestArea(...)` → nodes; `gameplay::survival::harvestNode(FDepositNode&, NodeKind, Inventory&, base, tool)` → harvest; `gameplay::survival::handRecipes()` + `HandCrafter::canCraft/craft(CraftRecipe, Inventory&)` → list/craft; `gameplay::survival::Furnace{FurnaceTier}` with `loadOre`/`loadFuel`/`tick`/`run`/`takeOutput` → smelting. |
| 2026-06-15 | **WP2 — UE playable shell for the survival slice** (Admin build brief) | Done | Bound a UE5 playable loop to the WP1 `/core` API (no rules duplicated). New plugin C++: `UOFSurvivalComponent` (pImpl `SliceRegistry`+`Inventory`, `RegisterSurvivalContent`; UFUNCTIONs harvest/craft/inspect), `AOFResourceNode` (NodeKind→tinted shape, `Interact`→`survival::harvestNode`), `AOFFurnaceActor` (pImpl `survival::Furnace`, ticks in PIE), `AOFSurvivalCharacter` (WASD+mouse+E, legacy key binding). New runtime-module `AOFSurvivalGameMode` (SurvivalTest default pawn, per-map override). Authored `/Game/Maps/SurvivalTest` (flat plot, 7 nodes, furnace+smelter, lighting) + `/Game/UI/WBP_SurvivalHUD` (inventory + craft-recipe panels). **Verified in PIE**: harvested all 7 kinds → inventory; crafted pickaxe/axe/furnace (inventory deducted, craftable flags flipped); loaded furnace ore+coal → 3 iron ingots (smelter 3× faster). 4 screenshots in `docs/screenshots/M2.4_*`. Build via `Build.bat` (editor closed) succeeded first try; no `/core` or pinned-interface change. **Gotchas recorded in `ue/CLAUDE.md`:** PIE renders black with a manual-exposure PPV mis-set / movable lights needed; PIE viewport-mode + editor-window slate capture (`--source window --unsafe-slate-window-capture`) is the only working PIE+UMG screenshot path; `set_control_rotation` pitch is unreliable through the spring-arm (use yaw-only, look at eye-level). **Residual:** no per-recipe Craft button / walk-up furnace-load overlap wired (driven via UFUNCTIONs). |
| 2026-06-15 | **WP2.6 — second visual pass: real Mannequin + stylized environment** (Admin build brief) | Done | Skinning-only (no `/core`/WP1-API change; all WP2.5 camera/input fixes kept). **Mannequin:** migrated the engine UE5 Mannequin (SKM_Manny_Simple + skeleton + ABP_Unarmed locomotion BP + BS_Idle_Walk_Run + mats/textures/rigs) from `Templates/TemplateResources/High/Characters/` into `/Game/Characters/Mannequins/` by filesystem copy (internal refs already `/Game/Characters/...` → resolved clean, no migrate tool needed). `AOFSurvivalCharacter::SetUpPlayerBody` sets the SKM mesh + assigns `ABP_Unarmed_C` AnimClass → real idle/walk/run from CharacterMovement; new `MannequinAnimClassPath` UPROPERTY. **Meadow:** rebuilt `M_Meadow` (3-tone grass + desaturated warm-brown dirt via thresholded gradient mask — the old Voronoi dirt read RED; fixed), real low-poly conifer tree (trunk + 3 stacked faceted cones, replacing sphere-on-stick), +~420 clustered wildflowers / ~960 tufts (`densify_meadow.py`), and the survival sun re-pointed DOWNward (it was pitched UP → whole scene unlit/black). **Ore:** `AOFResourceNode` ore kinds now generate procedural faceted-rock boulders (deformed icosahedra) + emissive crystals in a `UProceduralMeshComponent`, per-kind via new `M_OreRock`/`M_OreCrystal` (iron=rust+metallic, copper=grey+teal patina, coal=black glossy, stone=grey); harvest "+N" + shrink-as-depleted preserved. **Verified in PIE** (FP/TP) + editor close-ups. 3 commits (`0fc39a3` Mannequin, `d32b444` meadow+ore). 4 screenshots `docs/screenshots/M2.6_*`. Mannequin content +~98 MB. **Residual:** ControlRig plugin not enabled so ABP_Unarmed foot-IK post-process may be inactive (locomotion otherwise fine); water/oil still flat discs; mining/tool anims deferred to Admin's next phase. |
| 2026-06-15 | **WP2.7 — player menu (inventory + crafting) + drag-to-move + polish** (Admin build brief) | Done | UI/UX only (no `/core`/WP1-API change; all WP2.5/2.6 camera/input fixes kept). **Polish:** removed the red debug-sphere domes around `AOFResourceNode`/`AOFFurnaceActor` interact volumes (`SetHiddenInGame`+`bDrawOnlyIfSelected`+`SetVisibility(false)`); bound **top-row `EKeys::Three`** alongside Numpad-3 for the FP↔TP toggle (PIE-confirmed). **Menu:** `UOFPlayerMenuWidget` + `UOFInventorySlotWidget` built ENTIRELY in C++ (`RebuildWidget`, no authored WBP) — Inventory (uniform grid of coloured id-hued tiles + name + count) / Crafting (recipe rows + per-recipe Craft button gated on `bCanCraft`) tabs via `UWidgetSwitcher`, live off `OnInventoryChanged`. `AOFSurvivalCharacter::ToggleMenu` (Tab/I) flips cursor+`GameAndUI` on open / hidden+`GameOnly` on close; look/move/interact suppressed while open. **Drag-to-move:** ordered UE-side slot model on `UOFSurvivalComponent` (`SlotOrder`/`GetInventorySlots`/`MoveSlot`, reconciled with the live pack each change) + UMG drag-detect/drop (`UOFSlotDragDropOp`) → `MoveSlot` swap. `Build.cs` +UMG/Slate/SlateCore. **Verified in PIE:** grid populates on harvest; Crude-pickaxe craft decremented Wood+Raw iron and added the tool live; `MoveSlot(0,3)` swapped tiles; top-row 3 toggles TP both ways; play view free of debug spheres. 2 commits (`821eba4`, `5a28f7c`); 4 screenshots `docs/screenshots/M2.7_*`. **Residual:** always-on `WBP_SurvivalHUD` text overlay still present (menu augments, not yet replaced by a slim hotbar); drag is swap-only (no stack-split); water/oil discs + dropped-ore entities + tool anims still Admin's next pass. |
| 2026-06-15 | **WP2.8 — dropped-item entities + pickup + capacity + full-pack mining spill** (Admin build brief) | Done | Item ENTITIES only (no `/core`/WP1-API change; all prior fixes kept). New `AOFDroppedItem` actor: per-item **item skin** (faceted ore-chunk/mineral colour for ores/coal/stone, log for wood, bar/ingot for iron/copper, crude tool proxy for tools, blob for water/oil) tossed out on spawn. **Floor is QueryOnly → a simulating rigid body tunnels through**, so the item runs its own ballistic arc + downward line-trace to settle on the ground (rest z≈11) and a per-frame distance scan auto-collects it (`AddItem`, ~0.5 s spawn-protection, left on ground if pack full). `AOFDroppedItem::Spawn(...)` shared helper. **Capacity** surfaced from the already-fixed-slot `/core` Inventory (`GetSlotCapacity`/`GetUsedSlots`/`IsFull`/`CanAcceptItem`+`DebugFillInventory`) — no `/core` change. **Drop-from-menu** via character `DropItemFromInventory`: drag a tile OUT of the grid (`NativeOnDragCancelled` + `bDroppedOnSlot` guard), a click-select + **Drop 1** button, or **Q** quick-drop. **Full-pack mining spill:** `HarvestIntoOrDrop` mirrors `/core` `harvestNode` yield but extracts+reports overflow, `AOFResourceNode::SpillToGround` spawns one entity per spilled unit around the node. **Verified in PIE:** 10 kinds fell+settled+all picked up on walk-over; pack 20/20 `IsFull`; menu drop Wood 12→11 + entity spawned; full-pack mine of Iron ore 250→241 spawned 9 ground chunks. 3 commits (`ebc50f6`/`30a7f27`/`161a951`); 5 screenshots `docs/screenshots/M2.8_*`. **Residual:** custom ballistic settle (no roll/bounce — QueryOnly floor); auto-pickup (no key); mining/pickup anims + tool-in-hand still Admin's next pass. |
| 2026-06-15 | **WP2.9 — tools-in-hand + harvest swing animation** (Admin build brief) | Done | UE character/anim/mesh only (no `/core`/WP1-API change; all prior fixes kept). **Tools:** new `UProceduralMeshComponent ToolMesh` on `AOFSurvivalCharacter` builds a procedural **pickaxe** (double-spike head) or **axe** (flat blade) from boxes/wedges (wood handle + metal head sections, MIDs off `M_FactoryColor`), attached to the Mannequin **`hand_r` bone** via `AttachToComponent` (no socket asset) with a PIE-tuned grip transform (pitch −60°, loc (−8,6,0), scale 1.3). `EquipToolFor(node)` chooses axe for `Tree`/pickaxe for rock/coal/ore (none for water/oil); `SetHeldTool` (re)builds+shows; equipped on harvest (E) + persists; blocky-body fallback hangs it off the capsule. **Swing:** `PlaySwing()` plays the migrated engine `MM_Attack_01/02/03` AnimSequences (1 s, cycled) as a **dynamic montage on `DefaultSlot`** (`PlaySlotAnimationAsDynamicMontage`, blend 0.12/0.18, rate 1.4×) — no ABP edit / no authored montage. Overrides the body for ~0.7 s then blends out; locomotion still drives (CharacterMovement independent; loco resumes, max-walk-speed 600 intact). Wired into `HarvestNearest`. **Verified in PIE (TP):** axe on tree, pickaxe on iron/stone/coal/copper (each `HeldTool` correct, 2 sections, visible); `is_any_montage_playing()==True` mid-swing on `ABP_Unarmed_C`. 2 commits (`27fa93b` + Task-2 swing). 3 screenshots `docs/screenshots/M2.9_*`. **Residual:** swing on `DefaultSlot` is a full-body override for ~0.7 s (an upper-body slot in `ABP_Unarmed` would keep legs walking under it); no pickup-bend; tool shown on harvest, not gated on crafting it. |
| 2026-06-15 | **WP2.5 — survival slice visual + player-feel pass** (Admin build brief) | Done | Pure skinning/feel pass (no `/core` or WP1-API change). **Camera/input:** `AOFSurvivalGameMode` now sets `InputModeGameOnly` + hidden cursor (was GameAndUI + visible cursor → the old drag-to-look). `AOFSurvivalCharacter` rebuilt: first-person eye-height camera by default (smooth captured mouse-look), spring-arm third-person camera toggled on **Numpad-3** (`bDoCollisionTest=false`), body faces controller yaw (FPS-style). **Player model:** engine Mannequin if present (`MannequinMeshPath`), else a blocky humanoid (torso/head/arms/legs from basic shapes) — this install ships no Mannequin/foliage/StarterContent, so the blocky body is what renders; shown only in TP via `ApplyBodyVisibility` (NOT OwnerNoSee — that hides from both pawn cameras, the bug that first made TP empty). **Meadow:** new `M_Meadow` material (Voronoi dirt patches + two-tone grass via world-position noise, no textures), floor reskinned, ~300 grass tufts + ~110 flowers scattered as small static actors, warmer raked sun. **Ore:** `AOFResourceNode` reskinned per kind — ore/coal/stone = clustered rock-chunk patches (instanced, brightened mineral colours, Factorio-style); tree = trunk+canopy; water/oil = tinted disc; harvest shows "+N <resource>" + ore patches shrink as depleted. **Verified in PIE** (FP/TP/meadow/ore/harvest). 5 screenshots `docs/screenshots/M2.5_*`. 3 commits. **Residual:** `set_control_rotation`/`Rotator` Python arg-order is `(roll,pitch,yaw)` — pitch/yaw scramble if positional (use kwargs); meadow dirt patches read slightly red under the warm sun; on-screen harvest text doesn't appear in RT screenshots (renders for a live player); no real animation yet (Mannequin path set up for it). |

## 8.1 Phase-1 design log
| Date | Event | Outcome |
|------|-------|---------|
| 2026-06-14 | **Phase-1 design pass** (net-new domain, against pinned contracts) | Wrote [gameplay-phase1.md](../phase1/gameplay-phase1.md): avatar+camera+controls+handoff (§1), mining UX (§2), 3D build/placement+R2 (§3), inventory+tools (§4), HUD+map+R3 (§5), slice objective+onboarding (§6), item/recipe stubs + Cinder hook (§7), interfaces (§8). Settled GP-6…GP-12; R1 closed; R2/R3 first cuts; raised R4 (contract routing). Status → Phase-1 designed. |
| 2026-06-14 | **Pin C-3, finalize C-4/C-5, specify slice content** (Admin brief) | [phase1 §7](../phase1/gameplay-phase1.md#7-slice-content-pinned--item-set--recipes--entity-defs--the-cinder-hook) rewritten stubs→**pinned content**: 12-item canonical `ItemId` block (0x0001…), 5 `FRecipeDef` recipes (0x0101…), 7 `FEntityDef` entity defs (0x10…). [§8.C](../phase1/gameplay-phase1.md#8a--8d-phase-1-cross-domain-contracts) **C-3 PINNED** — `ItemId=uint16` opaque/hand-assigned/stable, single id space; [§8.D](../phase1/gameplay-phase1.md) **C-4 FINALIZED** — exact `FGameplayPersistState` fields; [§8.E](../phase1/gameplay-phase1.md) **C-5 FINALIZED** — 9 intents + `QueryEntityInventory`, all carry `FPlayerId`. Added GP-13 (`ItemId`), GP-14 (intents=commands). R4 → gameplay side closed, awaiting consumer confirmation. |
| 2026-06-14 | **Wave-0 gameplay-logic core BUILT & GREEN** (Admin build brief) | Implemented the headless player-facing rules as `core/include/of/gameplay.h` (header-only, consumes the green factory-sim + world-gen cores) + `core/tests/test_gameplay.cpp` (14 tests / 137 checks) + `gameplay_tests` target in `core/CMakeLists.txt`. Built: **mapped §7 slice content to a `SliceRegistry` DATA table** (12 items / 5 recipes / 7 entity defs, canonical ids); **`Inventory`** (fixed-slot add/remove/stack honoring `StackMax`, GP-9); **`mineDeposit`** over a `DepositNode` (Resource=`ItemId` directly per C-1/WG-11; grants→inventory, depletes `remaining`, stops at 0, respects grade); **`Builder::validate`/`place`** (the §2.2/§3.2 rules: no-deposit / wrong-deposit / slope-too-steep / insufficient-items rejects with `BuildReject` reason; on success places into the `FactorySim` via its add API + deducts cost; miner binds Ferrite/Cinderite recipe by node Resource); **recipe I/O** driven through factory-sim's `Recipe` model (smelt 1 ore→plate, assemble 2 plate→part); **`ObjectiveTracker`** — the §6 linear 7-step FSM, pure predicates over an `ObjectiveContext` snapshot, terminal "outpost on the moon" fires only when all conditions hold. `gameplay_tests` green; **all 6 ctest suites pass** (core-engine, factory-sim, world-gen, physics, gameplay, integration). No core-header edits needed — `producedCount()`/`machineOutput()`/`feedMachine()`/`addMachine()`/`addGenerator()` sufficed. |
| 2026-06-15 | **Phase-2 RESEARCH / TECH TREE BUILT & GREEN — GP-1/GP-2 realized in code; off-world gating PROVEN** (Admin build brief) | Implemented the progression spine the slice deferred (GP-1/GP-2) as additive `core/include/of/research.h` (header-only, consumes `gameplay.h` verbatim) + `core/tests/test_research.cpp` (7 tests / 69 checks) + `research_tests` target. Built (GP-16): **`TechId` (u16, opaque, stable) + `TechDef`** {id, name, prereqs, `cost` = science `ItemStack`s, `unlockRecipes`/`unlockEntities`} as a DATA `TechTree` (GP-12) — a handful of techs: `BasicSmelting` (cheap, no prereqs → unlocks smelter/assembler recipes+entities), `Logistics` (needs BasicSmelting → unlocks belt/box), and the **off-world `CinderiteRefining`** (needs BasicSmelting; `cost` includes a **Cinderite-derived** science pack `0x0022` → unlocks the Mine-Cinderite recipe). **`ResearchState::tryResearch(TechId, Inventory& science)`** (GP-1): all-or-nothing — checks prereqs unlocked + cost affordable from the science pool, then consumes the cost and applies the unlocks; `isUnlocked`/`isRecipeUnlocked`/`isEntityUnlocked` query so the build/craft UX gates on it; monotonic (never re-locks, dedup-safe) + deterministic. **Science = ordinary `ItemStack`s** in an `Inventory` (the factory produces them like any item); science packs append at `0x0020+` in the SAME `ItemId` space (C-3) via ONE additive `gameplay.h` extension point (`SliceRegistry::registerItem`) — the pinned §7.1 12-item block is untouched. **OFF-WORLD GATE PROVEN:** `research_tests` asserts `CinderiteRefining` is unresearchable with only Forge-derived science (prereqs met, automation science plentiful, but the Cinderite-derived item absent) and becomes researchable the instant the off-world item enters the pool — the gate that requires reaching Cinder. `research_tests` green; **all 12 ctest suites pass** (the 11 prior — incl. unchanged `gameplay_tests` 137 checks — + research). |

## 9. References
MASTER_PLAN §6 (progression), §7 (KSP science / Factorio research / NMS-Subnautica loot), D-005. Factorio tech tree; KSP science & maneuver-node UI.
