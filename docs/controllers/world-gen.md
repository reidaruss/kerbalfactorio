# World Generation & Terrain — Master Controller Context

> **Domain owner:** `world-gen-controller` · **Reports to:** Admin · **Phase:** web pivot, W5/W8 · **Status:** **WG-141 to WG-148: THE MOON IS REACHABLE AND IT IS MADE OF CRATERS, AND THREE THINGS ABOUT CINDER HAD SIMPLY NEVER BEEN REVISITED (2026-08-02).** Reid: *"go ahead and start working on the moon. This will be a fully traversable thing, we will be landing on it later."* **SIZING FIRST, because it sizes everything: Cinder was 100% real in `/core` and 0% reachable in the CLIENT.** `PlanetBody.cinder()` existed with ZERO callers, `Boot.ts:107` built Forge unconditionally, and `terrain.worker.ts:42` plus `oracle.worker.ts:50` hard-coded `_of_body_create_forge` behind a `TerrainInitMsg` that had no body field at all. Every WASM call site already threaded a handle, so the job was **plurality, not un-hardcoding**: one `BodyId`, one `createBodyHandle` rule in `PlanetBody.ts`, the field on the two worker protocols, and a `?body=cinder` flag. No ABI change, and deliberately none: **physics held ABI 22 uncommitted while I worked, so I took no ABI number.** **THE TERRAIN WAS THE PRE-WG-25 STACK: Forge got the 2026 rework and Cinder never did.** Its finest content was an 1,111 m wavelength (`terrain_probe --body cinder`, measured against Forge's 60 m), so the smallest landform on the whole moon was a kilometre wide. It is now a **nine-rung crater ladder from 28 km down to 1.8 m**, frequency tripling and amplitude thirding, which reproduces the N(>D)~D^-2 production law and the constant 0.12 depth-to-diameter ratio in one construction; amplitudes come from two closed forms (`14800/f` full, `6080/f` confined) rather than from taste. Four rungs use the full 27-cell neighbourhood and five a **CONFINED single-cell variant that costs a twelfth**, which is the only reason nine rungs are affordable at all: measured 492,507 verts/sec against the old 1,086,656, i.e. **2.21x**. **SLOPE WAS THE WRONG INSTRUMENT AND IT NEARLY GAVE ME A CONFIDENT WRONG ANSWER: a crater wall is a 24% to 48% grade at EVERY scale, so the old moon reported a healthy 0.554 m median step over 4 m and the rewrite looks like a 1.3x tweak on slope. On CURVATURE, which is what separates "tilted" from "featured", it is 33x.** **THREE REAL DEFECTS FOUND AND FIXED, none of them what I set out to do.** (1) `craterField`'s 3x3x3 neighbourhood **was too small for the radius it allowed**: reach was 1.2 cells, so a crater two cells away could be clipped mid-rim, and the measured worst case is a **470 m cliff out of nothing**, 346 times in 160,000 samples. Capping the radius at 0.625 makes reach exactly 1.0 and 3x3x3 exactly sufficient; `crater_neighbourhood_is_sufficient` proves it against a 5x5x5 sweep AND fires on the old coefficients in the same breath. (2) The crater PROFILE was **discontinuous at t == 1**, stepping half the layer amplitude at every rim on every body with craters: a **1,400 m vertical wall ringing every crater** on the old moon. (3) The moon's design layer **still switched gain on the DISCRETE biome**, WG-25's planet pathology surviving one body over, putting 120 m and 40 m walls along contour lines; now a continuous `designedGainForMoonRelief`, worst step at a crossing 173.8 m against the retired form's 287.5 m. **THE PICTURE IS THE ARBITER AND IT OVERTURNED THE READING TWICE.** From 12 km with `forgeAtmosphere()` the moon is a **white fog bank with no visible craters**; the identical camera with `--atmos=0` is a black sky, stars, a hard terminator and overlapping craters at four scales. **The airless look is not polish, it is the difference between a moon and a fog bank, and it is ROUTED TO RENDERING, not built here.** Determinism is untouched (WG-6 position-hash preserved, no LOD gating, both bitwise seam proofs still pass) and **FORGE IS BIT-IDENTICAL: of the 345 leaf values in `expected.json`, exactly 24 moved and every one of them is a moon value.** 35/35 ctest, parity Tier 0 126/0 and Tier A 108/0. **OWED: the wasm binary and `expected.json` are regenerated in the working tree but DELIBERATELY NOT COMMITTED, because two physics commits landed after my build; the moon reaches the browser at the next rebuild from a settled tree. Also owed: 51 starter nodes including broadleaf trees place on Cinder regardless of body, ambient collapses to near-black with the sky off, and the airless profile itself.** Previous status, **WG-116 to WG-122: THERE ARE NO SCENERY TREES. EVERY TREE IN THE WORLD IS A HARVEST NODE, AND THE PASS IS NET CHEAPER THAN THE FAKES IT REPLACES (2026-08-01).** Reid's ruling verbatim: "there should be no scenery trees. all trees should be minable." `TreeField` streams wood harvest nodes in a 620 m ring as pure functions of (seed, 28 m lattice cell), through the SAME `/core` calls a rock uses; the scenery canopy tier is retired to a `?canopy=0` default rather than deleted, so `?canopy=620` still reproduces the WG-59 world in the SAME binary. **Determinism proven by round trip at two sites: 2,233 of 2,233 trees and 813 of 813 bit-identical after leaving and returning.** The density table is the retired tier's OWN asks row for row, and that is a CORRECTION the picture caught and no counter did: a first table derived to match the canopy's ring AVERAGE delivered 0.9891 and looked wrong, because the canopy is at full density inside 300 m and only thins beyond, so its average was never the number the eye read. Size is now yield: a tree's scale is a linear map of /core's grade, which is what `InitialAmount` is multiplied by, and the band was re-derived off the SHIPPED BYTES after the first one (built from a docstring's "6.5 m tree") put the median tree a third too short. **THE CRUX IS WG-118 AND IT IS AN ASSET ALREADY PAID FOR: `NodeBatch` matched `_LOD0` only, while `_LOD1` and `_LOD2` meshes ship in every node .glb in the project.** Naively, 2,226 tree nodes cost 7,558,472 triangles at Forest; loading those LODs takes it to 1,895,584 (-74.9%) and per-instance culling to 1,365,739 (-81.9%), and even sites with NO trees gain 111 k to 135 k because the rocks, the spires and the clearing get the tiers too (this closes R10). Against HEAD the shipping world is **-112,306 triangles at Forest for 2,226 real trees against 1,560 fakes, -15,527 at the RN-15 camera for 799 against 462**, and bit-identical at Beach, Beach2, Hills and the Mountains spawn. Programs, geometries and VRAM do not move anywhere. **NEGATIVE CONTROL: `--trees=0 --canopy=620 --nodelod=0 --nodecull=0` reproduces pure HEAD IDENTICALLY on every published column at all eight sites, 8 of 8.** Persistence is cell-keyed and additive (`SaveSlot.trees`, SAVE_VERSION deliberately unmoved), proven by a REAL `page.reload()`: chop 18 wood off a streamed tree 70.2 m from spawn, reload, and the tree returns at the same position and `remaining` 12.358138084411621 exactly, drained from the PENDING path. Refusals: the treeline emptied 4,633 cells at the Mountains spawn, the pond refused 4, the clearing keep-out refused 14 at a Forest spawn (and read 0 until a frame bug in it was found by expected-count), and **the slope gate CANNOT FIRE: a 26,120-sample hill-climb finer than its own arm puts the steepest sub-treeline ground on Forge at 29.15 degrees against a 43.95 degree gate.** Trees do not respawn, matching the rocks. **OWED: props_canopy's three tall species want Full/Half/Low variants so the parasol forms can come back as node art; the far-field impostor ask (WG-63) still stands; the ring edge still pops; and `RockField` should inherit `TreeField`'s forget path.**
> Read alongside: [MASTER_PLAN](../MASTER_PLAN.md) · [AGENT_ARCHITECTURE](../AGENT_ARCHITECTURE.md) · [ADMIN](ADMIN.md) · **[Spike 1 world-gen design](../spikes/spike1-worldgen.md)** · [Spike 1 core-engine](../spikes/spike1-core-engine.md) (contract built against)

## 1. Mission
Procedurally generate believable, deterministic-from-seed planets and moons: terrain, biomes, resource deposits, and points of interest — streamable as LOD chunks, deformable where needed, and cheap to persist (seed + diffs).

## 2. Scope & owned subsystems
- **Cubed-sphere quadtree terrain** (KSP PQS-style) — six quadtree-subdivided cube faces on a sphere; noise-driven heightfield; streaming LOD.
- **Biomes & surface materials** — climate/latitude/altitude driven; feeds rendering materials + gameplay science.
- **Resource deposits** — seeded node placement; per-body resource identity (this moon = X-rich) to drive logistics/exploration.
- **Voxel/SDF deformation patches** — overlay only where players dig; keep heightfield for the untouched bulk.
- **POI / structure placement** — ruins, anomalies, loot sites (content authored by [gameplay](gameplay.md); placement here).
- **Body parameters** — radius, gravity (μ), atmosphere profile, rotation — shared with physics/rendering.
- **Non-goals:** rendering the terrain (→ rendering consumes our data); what loot/quests POIs contain (→ gameplay).

## 3. Key design decisions
| # | Decision | Rationale | Status | Date |
|---|----------|-----------|--------|------|
| WG-1 | Cubed-sphere quadtree heightfield for bulk terrain | Proven KSP-scale LOD; cheap; seed-deterministic | **Accepted** (designed in Spike 1: 6-face cube + per-face quadtree, `FQuadKey`) | 2026-06-14 |
| WG-2 | Voxel patches **only** at dig sites; heightfield elsewhere | Keeps 99.9% cheap; v1 may ship node-mining, no deform (D-005) | Accepted (seam pinned in Spike 1 §4.4: `bFromVoxelPatch` in every result; stub no-op) | 2026-06-14 |
| WG-3 | Deposits = seeded nodes; only depletion state is saved | Seed+diff persistence (D-005, persistence) | Accepted | 2026-06-14 |
| WG-4 | Per-body resource identity to force interplanetary logistics | Exploration/automation incentive (DSP-style) | **Accepted** (pinned as the Cinderite off-world hook — Spike 1 §4.5.3; Cinderite `ItemId` placed only on Cinder, P1-D4) | 2026-06-14 |
| WG-5 | Cube→sphere uses **tangent (equal-angle) warp**, not raw normalize | Uniform LOD triangle density; warp cost is gen-time only; inverse closed-form (lat/lon↔quad cheap) | **Accepted** (Spike 1 §1.1) | 2026-06-14 |
| WG-6 | Height is **position-hashed** `(bodySeed, dir)`, never sequence-hashed | Shared edge verts bit-identical between neighbors/parent-child → crack-free seams; order-independent regen | **Accepted** (Spike 1 §1.3–1.4) | 2026-06-14 |
| WG-7 | Noise = **3 layers/body** (base + signature + detail), octave-gated by LOD; planet=ridged mountains, moon=crater field | Recognizable planet + cratered moon at minimal cost; gated octaves keep low-LOD cheap (R2) | **Accepted** (Spike 1 §2) | 2026-06-14 |
| WG-8 | Seam strategy = shared-edge determinism + **skirts** + balanced quadtree (≤1 level edge-neighbor diff); edge-stitch index buffers deferred | Standard chunked-LOD trio; skirts need no per-frame neighbor bookkeeping | **Accepted** (Spike 1 §1.4) | 2026-06-14 |
| WG-9 | Chunk centers stored as `FUniverseCoord` (+`FFrameId`); verts body-center-relative; rebase = re-project transform only | Honors core-engine §1.4/§8 floating-origin contract; rebase is O(resident quads), no vertex rewrite | **Accepted** (Spike 1 §3.3–3.4) | 2026-06-14 |
| WG-10 | **Append-only** additive fields on pinned contracts (RC-2, RC-7): `FAtmosphereProfile` += Mie (`MieScaleHeightM`/`MieScatteringScale`/`MieAnisotropy`); `FTerrainContact` + `FTerrainHit` += `SlopeRad` (rad) + `SurfaceHardness` (normalized [0,1] `double`) | Both derivable from data the gen/query already produces (normal→slope, material/biome→hardness; Mie is a per-body atmo constant world-gen owns per D-006). End-of-struct appends → no caller breaks. Hardness normalized [0,1] (not physics's first `uint8 0..255` sketch) so it lerps directly into friction/restitution | **Accepted** (reconciliation; Spike 1 §4.3, §5, §5.1–5.2) | 2026-06-14 |
| WG-11 | **C-1 — `FDepositNode` pinned** (Spike 1 §4.5): id / `Position`(`FUniverseCoord`+`FFrameId`) / lat-lon / `SurfaceNormal` / `FBodyId` + **opaque gameplay-owned `ItemId` resource** + Grade / InitialAmount / single-mutable `RemainingAmount`. Catalog **deterministically seed-generated** per body (position-hashed, WG-6 discipline). `ItemId` belongs to gameplay's C-3 registry — referenced, not defined; **collapses** gameplay's proposed `DepositTypeId` into the one shared id space. Cinderite `ItemId` placed **only on Cinder** (WG-4 hook). | One struct serves all three consumers (gameplay UX, factory-sim extraction, persistence depletion). `ItemId` as the type keeps one id space (deposit==belt==smelter==pack) — no translation table. Seed-generated catalog → regenerates bit-identically (WV1), only depletion diffs. | **Accepted** (Phase-1 C-1; Spike 1 §4.5, §5.1–5.2) | 2026-06-14 |
| WG-12 | **C-6 (world-gen side) confirmed** (Spike 1 §4.6): world-gen implements persistence's `IPersistable` (chunk-scoped, `DomainId=WorldGen`) over **deposit depletion only** — `(depositId, remaining)[]` per region, `depositId = hash64(bodySeed, region, localIndex)`. Terrain + deposit *placement* regenerate from seed (PS-7/WV1) — not serialized; voxel seam empty (WG-2). | World-gen's entire Phase-1 mutable surface is depletion. Stable hashed id = exactly the `(bodySeed, FQuadKey, localIndex)` id persistence §2.1 asked for → depletion re-binds on reload with no fuzzy match. No new ask of persistence beyond implementing the interface. | **Accepted** (Phase-1 C-6; Spike 1 §4.6) | 2026-06-14 |
| WG-14 | **Phase-1 deposit gen + mining query surface BUILT + GREEN** (C-1 / WG-11 realized) — `core/include/of/deposits.h` + `core/tests/test_deposits.cpp` (`deposits_tests` target). Implements the pinned `FDepositNode` (Spike 1 §4.5.1; `Resource` = bare `uint16_t` `ItemId` per WG-11, **not** including gameplay.h — Ferrite `0x0001`, Cinderite `0x0004`), `GenerateDeposits(body, bodySeed, bodyFrame)` (a **deterministic** position-hashed pass over a Fibonacci-sphere candidate lattice; each kept node snapped to the terrain via the SAME `sampleHeightField` the mesh uses; stable `Id = hashCombine(mix64(bodySeed), face, candidateIndex)`), and `DepositCatalog` with the full query surface (`GetDeposits`/`QueryDepositsNear`/`GetDeposit`/`ExtractFromDeposit`). `PickResource` enforces **WG-4**: Forge nodes are Ferrite-only and **never** Cinderite; Cinderite is returned **only** for Cinder (off-world hook). `ExtractFromDeposit` is the **only** mutator — grants ≤ `RemainingAmount`, clamps at 0 (no negative, no over-grant). | Realizes the three-way contract (gameplay mining UX, factory-sim extraction, persistence depletion) in real headless C++, mirroring the green Wave-0 pattern (header-only `of::worldgen::` + `test_framework.h`, parallel `deposits_tests` target only — `cubed_sphere.h` consumed READ-ONLY, untouched). Determinism proven **bitwise** (same seed → identical catalog field-for-field; diff seed → different) and the **seed+diff property** proven (regenerate from seed + re-apply recorded `(depositId, extracted)[]` → live state bit-identical — `RemainingAmount` is the only thing that ever changes, WG-3/C-6). On-surface proven (`|Position|` ≈ `radius + SampleTerrainHeight(lat,lon)` within 1 m, matches WV7). **Result: 7 tests / 1620 checks pass; all 11 ctest suites green.** Per body (world seed 20260615; counts are seed-dependent): **Forge = 732 deposits, all Ferrite, 0 Cinderite**; **Cinder = 210 deposits (133 Cinderite + 77 Ferrite)**. Cinderite-only-on-Cinder confirmed; determinism confirmed (same seed → bit-identical catalog). | **Accepted** (Wave-2 build; Spike 1 §4.5, WG-11/WG-4/WG-3) | 2026-06-15 |
| WG-15 | **GAP-1 (slice-e2e) CLOSED — `FDepositNode::Id` is now the ONE shared deposit-id type.** Gameplay's deposit reference (`gameplay::DepositId`) was a parallel narrower `uint32`; it is now `using DepositId = worldgen::DepositId` (the `uint64` `FDepositNode::Id` type), so a deposit key carries from `DepositCatalog` → gameplay mining → the persistence depletion diff **with no truncation**. Gameplay now consumes `FDepositNode` directly (new `gameplay::mineDeposit(worldgen::FDepositNode&, …)` overload reads/decrements `RemainingAmount` in place); the slice's truncating `bridgeNode` is removed. **World-gen header itself is UNCHANGED** — it still defines `worldgen::DepositId`/`FDepositNode` exactly as pinned (WG-11) and never includes gameplay.h (the dependency is one-way: gameplay→world-gen). | The catalog's stable id is a `hash64(bodySeed, region, localIndex)` `uint64` (WG-11/C-6); narrowing it to `uint32` at the gameplay/persistence boundary risked save-key collisions and forced a bridge in the loop. Making gameplay alias world-gen's type (rather than the reverse) keeps world-gen's WG-11 boundary intact while giving the whole slice one faithful id. **13/13 ctest suites green.** | **Accepted** (slice-e2e gap closure; WG-11/C-6) | 2026-06-15 |
| WG-16 | **Wave-1 terrain STREAMING + LOD manager BUILT + GREEN, and the procgen hot path OPTIMIZED bit-identically** — `core/include/of/terrain_stream.h` + `core/tests/test_terrain_stream.cpp` (`terrain_stream_tests` target, 8 tests / 52,465 checks) + `core/tools/procgen_bench.cpp`. **(a) The headless terrain streamer the UE renderer binds to** (Spike 1 §3, §4.1–§4.2): `TerrainChunk` (== the pinned `FTerrainChunk` — `key`/`centerUniverse`(`UniverseCoord` anchor)/`chunkRadiusM`/`gridDim`/body-center-relative `positions`+`normals`/`skirtPositions`/`materialId`/`contentHash`); `selectResidentSet(body, observer, cfg)` = the DETERMINISTIC distance-based LOD quadtree (subdivide when `quadEdgeLengthM/dist > splitRatio`, capped at `maxDepth`), then **balanced** (`<=1`-level edge step, cascade-split coarser neighbours) so skirts cover at most a 1-level seam; `TerrainStreamer::updateStreaming(observer)` → `StreamUpdate{ready[], evicted[], generated, converged}` maintaining the resident set across motion under a **per-update `genBudget`** (build ≤N quads/call; converges over several calls to the bit-identical unbudgeted set); `makeObserverLatLonAlt`, `onOriginRebased()` (O(resident) re-anchor, positions rebase-invariant per §3.4). LOD runs on the 64-bit authority position (§3.1), not the floating-origin engine position. **Crack-free = shared-edge HEIGHTS bit-identical across a LOD step (WG-6 position-hash, proven bitwise) + a radial SKIRT** (`skirtDepthM = skirtFraction·quadEdgeLengthM`, dropped inward, proven strictly-inward by exactly `skirtDepthM`). **(b) Procgen optimization:** `valueNoise` rewritten to hoist the shared hash-chain prefixes — the 8 cube corners share `base = mix64(seed^channel·C)` (was 8× → 1×) and per-axis partial combines (x-stage 8→2, xy-stage 8→4); identical operands in identical order ⇒ **bit-identical** but ~half the `mix64`/`hashCombine` calls. `procgen_bench` times the optimized sampler vs a self-contained verbatim copy of the original in ONE binary: **Forge 909,893 → 2,385,621 verts/sec (2.62×), Cinder 1,060,332 → 1,554,351 verts/sec (1.47×)** (moon lower because its cost is dominated by the untouched `craterField`), checksum match. A ctest (`procgen_optimized_is_bit_identical`) re-asserts every vertex height bit-for-bit vs the original chain. `cubed_sphere.h` `valueNoise` changed in place (bit-output preserved → `world_gen_tests`/`deposits_tests` still green); everything else additive. | Realizes the Wave-1 streaming/LOD half of the §6 roadmap (the `UTerrainStreamer` row) headlessly so the UE renderer (rendering M2.1 currently renders ONE static `generateQuadMesh`) can bind a real LOD/streaming/skirt provider next. The streamer is a pure function of (body, observer, cfg) → testable determinism without an engine. The procgen hoist matters because per-vertex noise is the streaming budget's bottleneck (R2): 2.6× more quads/sec for the SAME bits = more terrain per tick or a smaller budget for the same coverage. Mirrors the green Wave-0/Wave-2 pattern (header-only `of::worldgen::` + `test_framework.h`, parallel `terrain_stream_tests` target). **Result: 8 tests / 52,465 checks pass; all 17 ctest suites green.** | **Accepted** (Wave-1 build; spike §3, §4.1–§4.2; R2) | 2026-06-15 |
| WG-17 | **Wave-3 BIOME + terrain-design + planet-wide RESOURCE layer BUILT + GREEN** — `core/include/of/biome.h` + `core/tests/test_biome.cpp` (`biome_tests` target, 11 tests / 8361 checks). The layer that turns uniform noise into a *designed*, varied planet. **(a) Biome classifier** `Biome biomeAt(const BodyParams&, const Vec3& dir)` (+ `biomeAtLatLon`) → enum `{Ocean, Beach, Plains, Forest, Hills, Mountains, Polar, Regolith, MoonHighland, CraterFloor}`; planet biomes are climate-driven (latitude→temperature with poles cold, position-hashed temperature/moisture fBm WG-6 style, elevation from the canonical `sampleHeightField`), moon biomes are airless elevation bands (crater-floor / regolith / highland). Pure & deterministic. **(b) Terrain design** `double sampleDesignedHeight(const BodyParams&, const Vec3& dir)` (+ `SampleDesignedTerrainHeight(lat,lon)`): per-biome relief modulation layered **ADDITIVELY on top of the unchanged canonical `sampleHeightField`** — Mountains ×1.6 (ridge high), Hills ×1.25, Plains ×0.45 (flatten), Beach ×0.15, Forest ×1.05, Polar flatten + ice-cap dome, **Ocean carved into a real basin below the datum** (low-freq fBm depth). Because biome + modulation are pure functions of the (bit-identical) shared dir, designed height is **bit-identical across a LOD/neighbour seam too** (proven in `designed_height_preserves_shared_edge_bit_identity`). **(c) Material mapping** `uint16_t materialForBiome(Biome)` (pinned ids 0..9 + `kMatUnknown`) + `double hardnessForBiome(Biome)` ([0,1], feeds physics `SurfaceHardness` WG-10). **(d) Planet-WIDE resource distribution** `GenerateBiomeDeposits(body,seed,frame)` — a planet-scale (8192 planet / 4096 moon) Fibonacci-sphere pass whose resource + spawn-density are biome-biased (iron/copper/coal/stone concentrate in Hills/Mountains; Forest=wood/oil; Plains/Beach/Polar sparse; Ocean none), reusing the survival raw-ore `ItemId` block, surface-snapped to the **designed** height; stable hashed `Id`. **Cinderite stays moon-ONLY (WG-4)**. Queryable by region via `BiomeResourceField::QueryRegionDeposits(body, FQuadKey, marginRad)` (angular cube-quad footprint) so the streamer/renderer populates the planet on the fly (+`GetDeposit`/`ExtractFromDeposit` depletion mirror). **(e) Wiring:** `terrain_stream.h` `TerrainChunk.materialId` **widened `uint8_t`→`uint16_t`** and now set from `materialForBiome(biomeAt(quadCenterDir))` (+ a `Biome biome` convenience field) so each chunk textures by biome — additive (no test pins `materialId`; all prior suites unaffected). | Realizes the §2 "biomes & surface materials" + the planet-scale generalization of §4.5 resources. The additive `sampleDesignedHeight` keeps the headline crack-free determinism intact: `cubed_sphere.h` is **UNTOUCHED** (consumed read-only), so the pinned WV1–WV3 bit-identity and the deposit catalog see the identical canonical heights — **NO canonical height bits were re-baselined, NO pinned test changed**. The renderer binds `biomeAt`/`materialForBiome`/`sampleDesignedHeight`/`BiomeResourceField::QueryRegionDeposits` exactly as terrain_stream now does. **Result: 11 tests / 8361 checks pass; all 18 ctest suites green** (was 17). | **Accepted** (Wave-3 build; spike §2, §4.5; WG-4/WG-6) | 2026-06-15 |
| WG-18 | **TERRAIN-DEFORMATION layer (Valheim-style digging) BUILT + GREEN** — `core/include/of/terrain_deform.h` + `core/tests/test_terrain_deform.cpp` (`terrain_deform_tests` target, 7 tests / 251 checks). The deterministic, persistable terrain-EDIT layer the UE pickaxe + automated-miner + collision pass binds to — the first concrete fill of the long-deferred R1/WG-2 dig seam, done as a **heightfield-lowering DIFF** (not full voxels): cheap, persistable, and additive. **(a) Edit store** = a sparse `std::map<uint64_t,double>` from a **quantized cubed-sphere deform cell** (`DeformCell{faceId,ix,iy}` at a fixed deep lattice `kDeformLevel=18` ≈ 3.6 m/cell on Forge; packed id `(face<<58)|(ix<<29)|iy` via `deformCellId`; `cellForDir` snaps a dir to its canonical face + nearest lattice point reusing `faceOfDir`/`unwarp`/`latticeDir`) → accumulated **lowering in metres**. **(b) Deformed sampler** `double deformedHeight(body, dir, deform)` = `sampleDesignedHeight − edit(cell)`, **clamped to bedrock** `base − maxDigDepthM` (default `kDefaultMaxDigDepthM = 80 m`, DEEP, configurable); where a cell is unedited it returns the base **BIT-IDENTICALLY** (subtraction is exactly 0.0 — proven via uint64 bit `==`, NO regression to existing terrain). **(c) Dig brush** `digBrush(body, centerDir, radiusM, amountM)` lowers cells within the radius with a **soft `(1−t²)` falloff** (centre deeper than rim → natural sloped holes), clamped to bedrock, returns the total lowering applied (0 at bedrock). **(d) Drill column** `drillStep(body, centerDir, columnRadiusM, depthPerStepM)` sinks a flat-profile narrow **shaft** toward bedrock over many calls (`shaftDepthAt`/`shaftBottomedOut` queries). **(e) UE queries** `editAt(cell)`, `depthDugAt(dir)`, `atBedrock(dir[,maxDepth])`, and `editedCellsInQuad(FQuadKey,marginRad)` / `quadHasEdits(...)` (angular cube-quad footprint over the sparse edit set — tells the renderer which resident chunks to **re-mesh** after a dig). **(f) Persistence** `serialize(Writer)`/`deserialize(Reader)` templated over the persistence.h `SaveWriter`/`SaveReader` cursor style (NO dependency on persistence.h — stays leaf): `[f64 maxDigDepth][varint count]{[varint cellId][f64 lowering]}` — the player's terraforming as a compact diff. **Determinism:** edits are EXPLICIT ops, so the same op sequence yields the bit-identical edit map + heights (proven). | Realizes Valheim-style digging headlessly as a **lowering diff over the procedural base** — exactly the seed+diff discipline persistence asked for (the edit map IS the diff, like deposit depletion WG-3/C-6). A heightfield-lowering diff (vs full SDF voxels) keeps the untouched bulk free and the dug regions a tiny sparse map, while still reading as real holes/shafts in mesh + collision once `terrain_stream`/UE sample `deformedHeight`. **`cubed_sphere.h` + `biome.h` consumed READ-ONLY (UNTOUCHED) — NO canonical/designed height bits re-baselined, NO pinned test changed; the undeformed path is bit-identical.** Mirrors the green Wave-0..3 pattern (header-only `of::worldgen::` + `test_framework.h`, parallel `terrain_deform_tests` target only). The UE dig/mine + collision layer binds `deformedHeight`/`digBrush`/`drillStep`/`editedCellsInQuad`/`serialize`. **Result: 7 tests / 251 checks pass; all 20 ctest suites green** (was 19). NOTE: this advances the WG-2 voxel seam from a no-op stub toward a working deform layer; the `bFromVoxelPatch` collision-query branch should now route through `deformedHeight` (follow-up wiring in physics/terrain_stream). | **Accepted** (terrain-deform build; R1/WG-2 seam, WG-3/WG-6 discipline) | 2026-06-16 |
| WG-20 | **TRUE VOXEL TUNNELING layer (1 m³ destruction) BUILT + GREEN** — `core/include/of/voxel_terrain.h` + `core/tests/test_voxel_terrain.cpp` (`voxel_terrain_tests` target, 9 tests / 5761 checks). The first real fill of the WG-2/R1 **voxel** seam (vs WG-18's heightfield-lowering diff): a sparse **1 m³ voxel destruction layer** that lets the player dig DOWN *and sideways* — tunnels, overhangs, caverns the heightfield can't express. **(a) Cell lattice** = a plain Cartesian **body-frame** lattice at `kVoxelSizeM = 1.0` m: `VoxelCell{cx,cy,cz} = floor(bodyPos/1)` via `cellForPos`, centre via `cellCenter`, packed to a stable `uint64` id (`voxelCellId`/`voxelCellFromId`, 21 bits/axis bias-encoded — covers Forge 600 km / Cinder 200 km). Cartesian (NOT the angular deform lattice) is what lets a removed cell sit ANYWHERE in the solid volume, incl. horizontally below the surface. **(b) Solidity = procedural-solid XOR removed (seed+diff):** `isProcSolid(body,cell)` = cell-centre radius ≤ `surfaceRadiusAt` (= `radiusM + sampleHeightField` at the cell-centre's **unit** dir — the input is normalized so 1 m-adjacent cells don't alias height); `VoxelEdits::isSolid(body,cell)` = `isProcSolid AND NOT removed`. Cells above the surface are AIR. Only the sparse REMOVED set is stored — never the full volume. **(c) Dig brush (tunnel maker)** `int VoxelEdits::dig(body, centerWorldPos, radiusM)` removes every currently-solid cell whose centre is within radius, returns the count (harvest yield); `digCell`/`digCellId` for the atomic carve. Idempotent (re-dig removes 0). **(d) Mesh extraction** `std::vector<FaceQuad> exposedFaces(body, edits, regionAABB | center+radius)` emits a `FaceQuad{cell,axis,sign}` per SOLID cell face whose 6-neighbour is AIR (the visible voxel surface) — UE builds the unit quad + `faceNormal(axis,sign)` per quad; `forSolidCellsInRegion(...)` iterates the same solid set so UE collision is built from it (region scanned ONCE). **(e) Dirty region** `dirtyRegion()`→ inclusive touched-cell AABB after a dig (UE re-meshes only there), `clearDirty()`. **(f) Persistence** `serialize(Writer)/deserialize(Reader)` of the removed set, sorted-id varint stream, templated over persistence.h's cursor (NO dependency — stays leaf). **Determinism:** procedural-solid is a pure fn of the height field, edits are explicit → same ops = same state bit-for-bit (proven). **TUNNEL proof:** a 12 m horizontal bore dug 5 m below the surface is AIR while the rock 4 m ABOVE it stays SOLID — a genuine overhang. | Realizes the long-deferred WG-2 **voxel** seam (Phase-4 in the roadmap) as a real 1 m³ destruction layer, complementing — not replacing — WG-18's heightfield deform: heightfield stays for the cheap untouched far-field + dig-DOWN, voxels carve the sideways/overhang topology only voxels can. Same seed+diff discipline (the removed set IS the diff, like deposit depletion WG-3/C-6 + deform WG-18). **`cubed_sphere.h` consumed READ-ONLY (UNTOUCHED) — NO height bits re-baselined, NO pinned test changed.** Mirrors the green Wave-0..3 pattern (header-only `of::worldgen::` + `test_framework.h`, parallel `voxel_terrain_tests` target only). The UE cube-mesher/dig/voxel-collision layer binds `cellForPos`/`isSolid`/`dig`/`exposedFaces`/`FaceQuad`/`dirtyRegion`/`serialize`. **Result: 9 tests / 5761 checks pass; all 21 ctest suites green** (was 20). | **Accepted** (voxel-tunneling build; R1/WG-2 voxel seam, WG-3/WG-6 discipline) | 2026-06-16 |
| WG-21 | **SINGLE SURFACE AUTHORITY ("surface oracle") BUILT + GREEN, the RETHINK R1 consolidation.** `core/include/of/surface_field.h` + `core/tests/test_surface_field.cpp` (`surface_field_tests` target, 7 tests / ~1900 checks). Replaces the FIVE competing surface definitions (RAW `sampleHeightField` · DESIGNED `sampleDesignedHeight` · DEFORMED · the MESH which subtracted edits from RAW · VOXEL-SOLID from RAW) that were the deterministic tax behind every floating/air-gap experience bug (RETHINK §3 RC-A; core-docs-audit Task B). **ONE truth, three layers:** (a) **`baseHeight(body,dir)` ≡ `sampleDesignedHeight`**: the DESIGNED relief is the canonical surface everyone samples; RAW survives ONLY as an internal ingredient of the designed shaping, never a surface anyone stands on. (b) **Voxel solidity derived from the SAME base:** `voxel_terrain.h`'s `isProcSolid`/`surfaceRadiusAt` switched **RAW to DESIGNED** (now sample `sampleDesignedHeight`), so the voxel solid shell agrees with the mesh/collision/walker, retiring the root cause of the UE 18 m "surface-snap" hack (the raw-vs-designed gap). (c) **Derived far-field lowering** `derivedLoweringAt(body,dir,voxelEdits[,maxDig])` = the depth of the **top-anchored contiguous removed-voxel run** in that column (the UE `OpenColumn` logic **promoted into core**, testable + single-sourced): a dig-down pit lowers the heightfield; a sideways TUNNEL under solid ground has a solid top cell, so run 0, so **NO lowering (ceiling intact)**. **`surfaceHeight(body,dir,voxelEdits) = clamp(baseHeight − derivedLowering, baseHeight − maxDigDepth)`**: the ONE surface + the ONE bedrock clamp (`kSurfaceMaxDigDepthM = 80 m`), defined here and nowhere else. A `SurfaceField{const BodyParams&, const VoxelEdits*}` binder exposes `heightAt`/`baseHeightAt`/`loweringAt`/`radiusAt`/`solid`/`loweringFn()`. **Consumers wired IN CORE:** `terrain_stream.h buildChunk` now samples the DESIGNED surface as the mesh base (via `generateQuadMesh`'s new optional `HeightFieldFn`, default null = RAW, bit-identical, so the header stays leaf), so the streamed mesh + cooked collision finally match the walker; `surface_walk.h` `surfaceHeightM()` reads `surfaceHeight` (new borrowed `setVoxelEdits(const VoxelEdits*)`, so the player drops into their own digs; null = undug designed base, bit-identical); `deposits.h GenerateDeposits`/`LayoutTestArea`/`DepositCatalog::ForBody` gained an optional `SnapHeightFn` (default RAW; the oracle-aligned UE/biome path passes `sampleDesignedHeight`) and `biome.h GenerateBiomeDeposits` already snapped to designed. **DEFORM DEMOTED:** `terrain_deform.h` is kept COMPILING (deprecated header note) but is **no longer an edit authority**: the oracle reads voxel-derived lowering, NOT the independent deform store. The two false-consistency comments the audit flagged (`surface_walk.h:29`, `terrain_deform.h:18`) are corrected. **RE-BASELINE (the RAW to DESIGNED switch):** `test_terrain_stream`'s `deform_lowering_…` case re-based its undug reference from `generateQuadMesh` (RAW) onto the designed base and its lowered-resident check from `sampleHeightField` onto `sampleDesignedHeight`. **NO determinism/seam assertion deleted:** both seam bit-identity proofs (`stream_crackfree_across_lod_boundary`, `stream_same_depth_edge_bit_identical`) still pass **bit-for-bit against the designed base**, proven, because designed height is a pure function of the (bit-identical shared) dir, so the position-hash seam guarantee is base-agnostic. `procgen_bench` still bit-identical (it benches the untouched RAW sampler). **R2 migration note:** the UE pickaxe/miner today write BOTH the voxel set and the deform store; after R2 they write voxels ONLY, and the far-field deform view is regenerated from the voxel removed-set on load (`derivedLoweringAt`), not persisted. | The worst experience bugs (floating player M4.0/M4.3, floating nodes M4.4, dig-in-air + the 18 m surface-snap M5.2) were the deterministic tax of four/five surface definitions each patched at the symptom (trace-snap nodes, drop-the-player, probe-inward digging, per-column reconciliation) instead of consolidated at the source. One oracle in `/core` + voxels-as-sole-edit-truth removes the whole hack family, at the one-time cost of the RAW to DESIGNED re-baseline. **Visible intended consequence: the planet's relief becomes the DESIGNED terrain everywhere, mountains actually mountainous, because the mesh/collision/voxel shell now read the designed surface, not raw.** Additive + leaf-preserving: every existing public signature stays alive (delegating/deprecated) so the UE layer keeps compiling for R2. **Result: 7 new tests pass; all 22 ctest suites green** (was 21) from plain PowerShell (post-R3 static link). | **Accepted** (RETHINK R1; RC-A consolidation) | 2026-07-05 |
| WG-22 | **TERRAFORMING BUILT + GREEN — fill is representable, and `levelArea` flattens a disc.** The voxel layer was **SUBTRACTIVE BY CONSTRUCTION**: `VoxelEdits` held ONE set (`removed_`), so "the player put a cell of ground here" was not a sentence the data model could say, and levelling a slope (cut the high side, fill the low side) could not be expressed at all. **(a) `voxel_terrain.h`:** a second sparse set `added_`; solidity = `added OR (procSolid AND NOT removed)`; the two sets are **DISJOINT by construction** (filling a dug cell erases it from `removed_`, digging a placed cell erases it from `added_`), so undoing an edit SHRINKS the save; `fillCell` / `fill(body, centre, radius)` mirror the dig brush exactly, same dirty-AABB and idempotence; persistence is **self-describing** (magic varint `0x4F464532` + version, then both sets) and `deserialize` branches on the first varint so **every slot written before today still loads**, including VoxelSave's single-zero-byte reset. All `added_` tests are behind an `empty()` guard, so a world nobody has filled takes the branch it took before. **(b) `surface_field.h`:** `derivedRaisingAt`, the mirror of `derivedLoweringAt` (contiguous run of ADDED cells anchored AT the base; a filled cell floating over a gap raises nothing, exactly as a sideways tunnel lowers nothing); `surfaceHeight = clamp(base − lowering + raising, base − 80 m, base + 24 m)` with `kSurfaceMaxFillM = 24`, **bit-identical where nothing is filled**; `surfaceOffsetAt`, the SIGNED offset the chunk mesher subtracts, so ONE callback carries both halves; **`levelArea(body, edits, centre, radius, targetHeight)`** — inside a cylinder about the local up, every cell above the target becomes air and every cell below it becomes solid, so cut and fill fall out of ONE predicate rather than being two tools. Deterministic and **idempotent**, so a held key cannot make the pad creep. **(c) `cubed_sphere.h`:** the mesh's offset guard `dug > 0.0` became `dug != 0.0`; it would have silently discarded every fill and left terraformed ground RENDERING at the height it used to be while the walker stood on the new one, the five-surfaces failure with the sign flipped. Undug path still bit-identical. **(d) Bridge, ABI 3 to 4:** `of_edits_fill`/`_fill_cell`/`_added_count`/`_is_added_cell`, `of_level_area`, `of_streamer_level`, `of_streamer_load_edits`, `of_derived_raising`, `of_surface_offset`, `of_max_fill`. Also fixed a LIVE mismatch: the shim already returned 3 while `OfCore.ts` expected 2, so `loadOfCore` threw on any freshly synced wasm. **(e) Client:** hold **Q**; the floor is **LATCHED on the press** from the player's own feet (reading it live would chase the player — levelling raises the ground they stand on, which raises the target, and the pad would climb for as long as the key was held); `world/LevelRing.ts` draws the footprint through the SAME oracle, so it lies on ground already terraformed; `TerrainEditChannel` splits ops (live play) from state (restore). | Realizes the user-facing half of the WG-2 voxel seam: a player can flatten a spot and build on it. Done as a core data-model change rather than a client trick because the model is what was wrong, and a reserved-for-later note in a comment is not a seam. **Measured:** `/core` 7.703 m spread to 1.511 m on a 26 deg hillside; browser 2.893 m to 1.279 m (2.26x), 60 cut + 99 placed over 2,815 scanned in 0.4 ms, outside the radius 0.000000 m over 12 points, 17/17 surface-vs-solid agreement, 3.68 m walked with eye-altitude spread 0.000 m, 1,439 save bytes restoring to 0.000000 m. **A pad is flat to about ONE VOXEL and never to zero** — a Cartesian 1 m lattice cut by a non-axis-aligned plane terminates on a staircase. That is the medium, not the algorithm (ARCHITECTURE 15.2 item 99). **NO re-baselining: 22/22 ctest, parity self-determinism 119/119, cross-toolchain exact 94 to 106/106, and the ONLY change to `expected.json` is the 7-byte save header.** | **Accepted** (terraforming; WG-2/WG-21 line) | 2026-07-26 |
| WG-23 | **THE LEVELLING TOOL COULD NOT BE REACHED, AND IT LEFT HOLES IN ITS OWN PAD.** Reid: "i tried the click q to level the ground thing and it didnt really work at all", against WG-22, which measured a 2.3x spread collapse and passed nine assertions. **Three causes; the two that mattered were not the voxel resolution.** **(a) UNREACHABLE.** `LevelAction.groundHit` centred the disc on the first ground the aim ray hit within 9 m of the eye. On the 29 degree slope the tool exists for, looking downhill, the ground falls away faster than the ray descends: MEASURED, at pitch 0, -10, -20 and -30 the ray found NOTHING, the footprint ring hid itself, and the key did nothing at all; ground was first found at **-45 degrees**. The WG-22 probe had aimed at -72 throughout and its own comment recorded that a shallower aim "cost a run to learn": the harness met the bug, worked around it, and wrote the workaround down as a lesson about framing. **Fixed:** `discCentre` falls back to the player's own feet, which is the rule the tool already stated (the target HEIGHT always came from the feet; only the centre came from the ray), and the ring follows the same centre so it cannot vanish at the moment it is needed. **(b) FULL-DEPTH HOLES.** `levelArea` stored only cells whose solidity CHANGED, while `derivedRaisingAt` / `derivedLoweringAt` rebuild the surface from a CONTIGUOUS run of explicitly edited cells along each column. The procedural surface is a 1 m staircase around the smooth designed height, so a column's probe regularly lands in a cell already air on the cut side or already rock on the fill side; nothing was stored, the run ended at step one, and that column kept its ORIGINAL height while its neighbours moved metres. **Measured: 8.7% of 253 columns did not move at all, worst 2.5 m below target**: not a rough floor, holes, each drawn as a wide gash at the terrain's 1.8 m vertex spacing. **Fixed:** `levelArea` RECORDS its decision over the band it is responsible for (`kLevelRecordBandM` = 1.5 cells about each cell's own procedural surface, read through the memo `isSolid` already paid for) via new `VoxelEdits::markAdded` / `markRemoved`. **Recording cannot change `isSolid`**, so the voxel shell, mesher, collision and every voxel pin are untouched: `expected.json` moves exactly ONE line and `changed` / `dug` / `filled` / `scanned` / `centreH` on it are unchanged. `procSolidMemo` now caches the surface RADIUS rather than the solid bit so the band test costs nothing (10.2 ms a press back to 0.9 ms). **(c) NO FEEDBACK.** Every press now quotes the flatness achieved, measured back through the oracle over 0.7R, and a no-op quotes it too, so a held key shows the number stop improving. **MEASURED, same site, same camera:** oracle spread over the pad 5.314 to 2.029 m; **DRAWN worst step within one 4 m foundation module 1.883 to 0.973 m, where the OLD code made it WORSE at 2.727 m**; per-column residual over 253 columns min -2.518 to -0.954, max +1.562 to +0.979, spread 4.08 to 1.93 m. In `/core` over 441 columns: spread 6.74 to 2.16 m, within 1 m 95% to 99%, worst stuck column 3.45 to 1.08 m. Save per pad 1,439 to 6,445 B, the price of recording the band. **PERCEPTUAL THRESHOLD, chosen and NOT met: 0.25 m over a 4 m span** (4 m is the DW-32 module, so it is the span a structure bridges; 0.22 m is the unevenness the build system already measured as placeable; 0.25 m at 1.6 m of eye height is a step you do not notice, 0.5 m is knee height and reads as a terrace). A 1 m Cartesian lattice cut by a non-axis-aligned plane cannot reach it, so `probes/level.js` REPORTS it instead of asserting it, and DW-32 foundations remain the answer to a slope. **Sub-cell fill assessed and REJECTED**: partial occupancy turns two sparse id SETS into maps, replaces the cube mesher with surface nets, and redefines `solidCell`, which is the shape DW-26 pins and asserts a bound on; it would re-baseline every voxel pin, the near mesher and collision, for a job DW-32 has already reassigned to foundations. New `of.meshVerts(x,y,z,r)` returns the vertices the GPU has this frame: the first instrument here able to tell the oracle and the picture apart. 23/23 ctest, parity 119/119 + 106/106, npm run check green (133 files). | A tool can pass every test of its EFFECT and still be unreachable, and the measurement that catches that is about the INPUT: the pitch at which the tool first finds ground, swept, against the pitch a player looks from. And spread is a claim about the middle of a distribution while a surface is judged by its worst neighbouring pair, which is why the fix is stated as a local bound over a named span, measured on the drawn geometry (ARCHITECTURE 15.2 item 123). | **Accepted** (levelling repair; WG-22 line, DW-24 / DW-32 context) | 2026-07-26 |
| WG-24 | **THE VOXEL REPRESENTATION CHANGED: signed density at lattice CORNERS, meshed by SURFACE NETS.** `core/include/of/voxel_field.h` + `core/include/of/surface_nets.h` + `core/tests/test_voxel_field.cpp` (143 checks). Three separate user complaints had ONE cause and it was the representation, binary occupancy on a 1 m lattice drawn as cubes: "the way the ground breaks when you dig is kinda fucky ... all these sharp edges" IS the mesher; "Q still makes the ground more rough not flat" is the same lattice seen from above, because a whole-cell edit can only put a surface on a lattice plane so a pad cut by a non-axis-aligned target plane terminates on a staircase; and DW-33's 1.01 m spread under a 4 m module is that staircase measured by the build system. WG-23 had ASSESSED sub-cell fill and REJECTED it, correctly for what was being asked then and not for three complaints plus an explicit request for smoothing. **(a) The field.** The untouched planet ALREADY HAD a smooth signed field, `procDensity(p) = surfaceRadiusAt(p) - |p|`, and the old code was discarding it by keeping only the sign; along a radial that expression is exactly a signed distance (`d/dr == -1`), so nothing had to be invented for 99.99% of the world. An edit is a sparse per-corner override and the ops are SDF CSG: dig is `min` with a sphere, fill is `max` with a sphere, levelling ASSIGNS the plane. Any point's density is the TRILINEAR interpolation of its cell's eight corners and that interpolation is THE authority, so the mesher, collision, the aim ray and the column height query cannot disagree about where the surface is. **(b) The mesher.** Naive surface nets: one vertex per crossed cell at the mean of that cell's edge crossings, one quad per lattice edge that changes sign, normals from the trilinear gradient AT the vertex so neighbouring cells shade smoothly. Chosen over marching cubes for one vertex per cell instead of five triangles, no 256-entry case table, no ambiguous-face resolution, and trivially watertight connectivity. **(c) The oracle.** `surface_field.h` root-finds the field instead of walking a contiguous run of edited CELLS, which DELETES WG-23's `kLevelRecordBandM` prosthetic entirely: that band existed only because a run of cells breaks whenever one cell is already in the state the op wanted, which on a staircase happens constantly. A sideways tunnel still lowers nothing, now automatically. `surface_walk.h` follows. `voxel_terrain.h`'s `VoxelEdits` is DEMOTED exactly as `terrain_deform.h` was in WG-21: still compiles, still has its suite, used by nothing live. **(d) Bridge, ABI 6 to 7.** `of_exposed_faces` GONE, replaced by `of_surface_nets` + `of_surface_nets_brick`; four signatures gained a `body` because "is this cell removed" is now DERIVED against the procedural surface; new self-describing save format (`0x4F464633`, v3) that still honours the client's single-zero-byte reset. **(e) Client.** `VoxelMesh.ts` consumes triangles; `VoxelGreedy.ts` DELETED with the cubes it merged; `VoxelSkin.filterDrawnFaces` deleted because the ownership question moved into `/core` where it can be asked of the edit store directly; `flatShading` removed, since it would have thrown away the gradient normals; **`SurfaceOracle.solidForAim` collapsed to one line returning `solidAt`**, retiring ARCHITECTURE 15.2 item 110's client-side reconciliation at its root rather than keeping the sixth surface definition alive. **MEASURED. Q flattening, worst height step over a 4 m span (the DW-32 module, so the span a foundation bridges), on a site with 7.03 m of spread across 12 m: 2.1408 m before, 0.0000 m after**, against the 0.25 m threshold WG-23 defined and reached only 0.973 m on; 0 of 49 columns off target; the DRAWN pad's radial spread 0.000001 m over 223 vertices. **DW-26 re-derived and still asserted:** cell-versus-point 0.4012 m worst against the unchanged 0.8660 m half-cell-diagonal bound, and the bound that actually hurt, drawn-versus-collided, **0.8660 m (a whole cell face) to 0.087116 m**. Digging: the carved sphere is the surface to 0.0957 m, 77 distinct normals where a cube mesher emits exactly 6. Tunnel residual 0.0000 m. Save 11,082 B per 10 m pad against WG-23's 6,445. **Two bugs worth keeping, both found by measurement and neither visible in the design:** the CSG form of levelling (min above the target, max below) gets every SIGN right and is still wrong, because surface nets interpolates VALUES and a corner half a metre under the target keeps its procedural +2.78 against the plane's -0.4 above it, so the crossing lands at 12% of the edge and the pad comes out 1.24 m rough; and `unitOf(unitOf(v))` is not bit-identical to `unitOf(v)`, so re-normalising an already-unit direction inside the column query made it disagree with the oracle in the last bits at every untouched column, which is a second surface definition arriving by rounding. | A representation that cannot express the answer cannot be tuned into expressing it. WG-22 measured a 2.3x spread collapse and WG-23 a further 2x, and the user still said the tool did not work, because both were improving a staircase. The general form, which is DW-26's lesson pointed at the storage rather than at the API: **when three separate complaints share a root, the root is usually the data model, and the cheapest honest fix is to stop quantising a quantity you already have.** | **Accepted** (WG-22/WG-23 line; DW-26 re-derived) | 2026-07-27 |
| WG-25 | **THE NOISE STACK HAD NO DETAIL, AND IT HAD KILOMETRE WALLS** (`cubed_sphere.h`, `biome.h`, `core/tools/terrain_probe.cpp`). Two defects behind "mountains look like shit lol", and only one was the one I sent a subagent to find. **(a) Expected:** no high-frequency content at all. The finest octave, f=320, is a 1.9 km lattice cell at about 30 m of amplitude, so at the shipped 1.8 m LOD the ground was a PLANE. The wavelength sweep is unambiguous: RMS neighbour difference over spacing 9.02% at 1 m, 9.01% at 2 m, 9.01% at 5 m. The slope spectrum on a peak ran p50 0.616 to max 0.630, a 1.3% spread between the median and the worst case. A mountain was a 30 km dome because `ridged()` starts at amplitude 0.5 and halves, so its FIRST octave carried half the relief. **(b) NOT expected, and larger:** `designedReliefFactor` switched on the DISCRETE biome, so the Hills/Mountains boundary was a **985 m vertical wall between two samples 100 m apart** (2,700 m of base relief read 3,375 m as Hills at gain 1.25 and 4,320 m as Mountains at 1.60). Coastlines carried a ~1.2 km wall where Ocean jumped to a separately generated basin, the moisture threshold about 860 m, the ice cap 240 m. So the complaint was flat domes separated by sheer unclimbable steps. **FIXED:** a smoothstep orogeny mask replacing `max(0,L0)`; a ridged multifractal on a domain-warped dir replacing `ridged`; detail from f=80 to f=2500; every hard biome gain replaced by `designedGainForRelief`, a monotone C1 smoothstep blend, so `rel*gain` has no step anywhere; the sea clamp removed, which let the ocean-basin branch and its coastline wall go. **MEASURED:** worst grade over 100 m **985.37% to 87.54%** (home 985.37% to 108.07%); slope p90/p50 at 2 m **1.013 to 2.28**; wavelength coverage **240 km-1.9 km to 240 km-60 m**; Mountains biome 3.4% to 7.0%. **COST WENT THE RIGHT WAY:** raw `sampleHeightField` 11 to 19 valueNoise calls, 1.93x slower and inside the 2x gate, but **`sampleDesignedHeight`, the one authority every consumer reads, 40 to 19 calls and 2.00x FASTER**, because it had been evaluating the height field THREE times per vertex (directly, inside `biomeAt`, and inside `temperatureAt`); the browser HUD shows the oracle at **2.23 to 0.97 us/call**. A DW-14 win too: `temperatureAt` computed `cos(asin(dir.y))` where `dir.y` IS `sin(lat)`, so it is `sqrt(1-y*y)`, removing two libm calls and one real mingw-versus-musl divergence from the classifier. **`test_world_gen.cpp` needed NO change** (WV1-WV3 and the relief bound pass unmodified), the cleanest evidence the crack-free property survived, because height stayed a pure function of the shared dir. Exactly one re-baseline, `orig::sampleHeightField` in `procgen_optimized_is_bit_identical`, with `orig::valueNoise` deliberately UNTOUCHED so the test still proves the WG-16 hoist; `procgen_bench` re-baselined the same way, still bit-identical, 2.60x. | The visible complaint was one defect and the measurement found two. And a benchmark's reference copy is a second thing to re-baseline: re-baselining the wrong half would have silently retired the proof. | **Accepted** | 2026-07-27 |
| WG-26 | **THE FLAT START AREA, and it goes through the oracle.** Asked for explicitly, "for simplicity". `BodyParams` gains `homeDir` / `homeFlatRadiusM` / `homeBlendRadiusM` (zero on Cinder) and `sampleDesignedHeight` becomes a strict wrapper over `designedHeightNoPad` adding a smoothstep blend. It lives in the SURFACE FUNCTION, so mesh, collision, walker, voxel solidity, deposit snapping and build placement all get it with **no special case in any consumer** (standing rule 1). Flat to 150 m, blended to nothing by 600 m. **Worst height step over a 4 m span inside the pad: 0.000000 m, exactly**, asserted as a uint64 bit compare over 5,000+ samples, because smoothstep returns exactly 0.0 inside the flat radius and the blend is written pad-first. Outside the blend radius the height is BIT-IDENTICAL to the unpadded field over 20,000 Fibonacci dirs, and the effect provably reaches zero at 601 m. `homeDir` is a hard-coded literal with a test asserting it matches `latLonToDir(2,144)` to 1e-12, because computing it with trig would reintroduce the DW-14 divergence. `worldSig` in `voxel_terrain.h` AND `fieldWorldSig` in `voxel_field.h` both hash the new fields: a BodyParams field that world generation reads and a memo key does not is a cache that answers for the wrong planet, silently. **OPEN for Reid:** at this seed the fixed spawn is now Mountains at 4,668 m on a 42% mountainside, so the pad is a plateau perched on a mountain rather than a clearing. Widening does not help; moving the spawn means `Config.ts`, which this lane does not own. | | **Accepted** | 2026-07-27 |
| WG-27 | **ABI 7 then 8: the client reads the one signed surface, and the level op reports the diff it made.** ABI 7 wired the field through the bridge and the client (WG-24). ABI 8 followed within the hour because a WORKING levelling tool drew nothing: `of_level_area` returned CELLS whose centre crossed the zero level, and `VoxelWorld.level` returned a null dirty box when that was 0, so the near mesh never rebuilt. On a binary lattice that was sound; on a signed field it is not, because shaving 40 cm off a slope moves the surface under the whole disc without carrying one cell CENTRE across zero. **Measured in the browser: one press reported dug 0, filled 0, scanned 8,797, and had flattened the ground.** The op now counts CORNERS whose stored distance moved, and that is what the bridge returns and what the client gates on. ABI 8 rather than a silent change because the RETURN VALUE changed meaning, and nothing in the version guard can catch a client reading an old contract from a new build at the same number. Also `LEVEL.radiusM` 6 to 10 m, for a reason about the INSTRUMENT rather than the tool: the field is flat to 0.0000 m through the oracle and 0.000001 m on the extracted 1 m mesh, but the streamed chunk samples it at the shipped 1.8 m LOD, so a 6 m disc is about seven chunk vertices across and its rim wall falls inside any 4 m span; 10 m is also 2.5 DW-32 modules, so a foundation fits INSIDE the pad. **MEASURED, driven through a real DOM key event, same site and same probe WG-23 used, on the DRAWN chunk geometry: worst step within one 4 m foundation module 1.788 m to 0.000 m**, drawn spread 3.327 to 0.000 m, oracle spread 4.029 to 0.000 m. WG-23's numbers on that instrument were 1.883 to 0.973 m against a 0.25 m threshold it defined and could not meet; **it is met**. 29/29 ctest, self-determinism 119/119, cross-toolchain exact **108/108** (105/106 when the night started, the vessel lane's ABI 6 bump having run with `-SkipNative`; that pre-existing gating blocker is cleared). **OPEN:** the probe's negative control `outsideUntouched` now fails at 2.779 m on a ring at 2.5x the pad radius where it read 0.000000 m at 6 m, while `/core` proves the same property bit-identically at 30 m. Two instruments disagree and one is wrong; recorded in ROADMAP-W11 rather than smoothed over. | WG-23's lesson one layer down: **a tool can do its work correctly and still read as dead, if the number the client watches is not the number that changed.** WG-23 was an unreachable tool; this was an invisible one. | **Accepted** | 2026-07-27 |
| WG-29 | **DW-36: ONE MAP, CENTRED ON THE PLAYER, DISCOVERABLE, WITH ORE COUNTS, and discovery is a NEW `/core` field.** `core/include/of/discovery.h` + `discovery_tests` (25 tests / 484 checks), ABI **12** (`of_disc_*` in `web/wasm/of_discovery_api.inc`, typed in `web/src/sim/wasm/discabi.ts`), and the map WIDENED rather than rebuilt: the mode, the modal registration, the `MAP_ALLOWED` list and the node UI all survive. **(a) THE CENTRE IS A PARAMETER.** `MapScene` gained `centreM` and the change is absorbed by `toPx`, which is the property MapDraw already claimed ("the projection lives in one place"); `paintBody` stopped stamping its arc at the canvas middle and projects `toPx(0,0,0)` like everything else. Focus switching is then not a feature, it is which number goes in that field. **R17's bug had a second head:** `apsis()` nudged its label along `p − (cx,cy)`, which means "away from the body" only while the origin IS the body, so centred on a ship a PE label walks into the planet; it now nudges away from the projected body centre. `fitSpanM` likewise measured from the body and now measures from the centre to the NEAREST limb, proven identical to the old function on five scenes at `centreM=[0,0,0]` by compiling both and running them side by side. **(b) THE ZOOM CONTINUUM IS A PROPERTY, NOT A CLAIM.** No span threshold exists anywhere in the map: one exported ramp, `MapPaint.sizeAlpha(sizeM, m2p, minPx, fullPx)`, fades a layer in on ITS OWN feature size (a 9 m ore patch fades in when 9 m is a few pixels; a 9,375 m survey cell when that is), and `MapDrawReport.alphas` publishes what was painted so a probe sweeps the zoom and asserts it. Measured over 20 decades of pixels-per-metre: monotone non-decreasing, exactly 0 and 1 at the knees, largest step per 0.01 decade **0.0310**: a `if (span < X) return` would read as exactly 1.0. The giant-circle problem (centred on the player the body's projected radius is ~1e9 px and Canvas 2D is wrong past 2^24) is solved with **zero tuning** by `discCoversCanvas`, true exactly when the disc covers every canvas corner, so filling and stroking provably paint the same pixels. **The AXIS rides on the FOCUS, not on the zoom**: a first draft slewed it with scale, which would have made a maneuver planned at one zoom foreshorten at another. **(c) THE RULE, and the third option again.** A cell is discovered when it was above the observer's horizon. That alone fails the way Reid predicted, at 80 km it takes 3.7% to 7.5% of Forge per instant and ~40% per lap, and the answer is NOT a smaller radius: **height buys EXTENT and costs RESOLUTION**, which one radius cannot express. So ONE rule at TWO resolutions: **SURVEY** (9,375 m cells, uncapped, the shape of the world, what the map shades) and **EXPLORE** (293 m cells, capped at a 10 km ground chord, the detail, what gates an ore patch). A lap gives you the continents and a 20 km thread under the ground track. Every number is in `DiscoveryTuning` with its derivation; `horizonFraction` defaults to **1.0**, the geometric horizon, because the derived value is the honest default and the dial exists for balance rather than for making a test pass. **NO TRANSCENDENTALS**: the horizon is `dot >= R/(R+h)`, the cap is the chord form `dot >= 1 − (c/R)²/2`, both exact arithmetic, because CE-11/DW-15 make `tan/asin/atan2/cos` a cross-toolchain hazard and 1 ULP would flip a boundary cell in a SAVED set. **(d) THE LATTICE IS enemies.h's, CHECKED NOT CLAIMED.** `SurfaceCellGrid` is a second copy of `PollutionField`'s raw-gnomonic grid (enemies.h is another lane's and a spatial index should not drag the enemy model in), so the suite includes BOTH headers: **560,000 directions, 0 `cellOf` mismatches, 0 `cellCentreDir` BIT mismatches** (uint64 via memcpy, never `CHECK_NEAR`), **62,940 neighbour cells including all 72 cube-corner cells, 0 mismatches**. Flagged not taken: enemies.h should adopt this grid so there is one copy rather than two agreeing ones. **(e) THE FLOOD IS PROVEN BY BRUTE FORCE.** 72 cases, **1,105,920 cells enumerated exhaustively, 50,023 inside the cap, 0 MISSING and 0 unsound**; the unconditionally-seeded observer cell is asserted as a NAMED exception rather than absorbed as slack. `accepted` is pinned against the exact continuum cap-cell count to −2.9/+4.5/+26.0 against perimeters of 161/322/643, grows as r² (4.008x, 4.001x), and is **2.7879x** at a face-edge midpoint and **5.0618x** at a cube corner against the exact gnomonic Jacobians 2^1.5 and 3^1.5, which pins the 2.12x non-uniformity the lattice pays for being transcendental-free **and would fail loudly if anyone swapped in `cubed_sphere.h`'s tan() warp**, since both ratios would collapse toward 1. **(f) THE SUITE FOUND A REAL DEFECT AND CONTRADICTED TWO OF MY OWN COMMENTS.** The deserialize guard read `if (i > 0 && d == 0)`, but the accumulator is `uint64` and the delta comes off a save file, so a delta near 2^64 walks `prev` BACKWARDS with `d != 0`: the guard waves it through, the sort never runs, and every `has()` becomes a binary search over an unsorted vector. Measured: a 3-cell stream deserialized to `[1000, 499, 509]` and returned `true`, exactly the failure the comment claimed to prevent. Fixed to `next <= prev`, free on the legal path, **verified by reverting: exactly 1 test and 3 checks fail by name.** Also fixed on review: `cells_.reserve(n)` took a hostile varint straight off the stream (a corrupt save became `bad_alloc` rather than a refusal) and now reserves against the lattice's own cell count; and `WorldDiscovery::deserialize` was `a && b` over two layers, which put a HALF-LOADED world behind a `false`, and is now all-or-nothing. **Two header comments were wrong and are now the measurements**: the 80 km horizon is **291,042.8 m**, not the "285 km" I wrote from memory, and "re-observing known ground merges nothing, so standing still is one dot product" is **false**: a warm pass measured 1.04x to 1.40x MORE than a cold one, because a cold store is empty and there is nothing to search. The true property, now asserted, is `added == 0` AND the store's buffer address does not move: a `set_union` into a fresh vector must move it, so a no-op provably cannot have run. **That is a structural check where a stopwatch would have been a threshold**: the same lesson as the inverted winding and the stale control. **(g) COST, and the claim narrowed to the true one.** The FLOOD is O(AREA) and specifically not O(machines/patches/vessels), because `observe` takes a direction and an altitude and cannot see an entity: **0.69 µs on foot, 59 µs at 10 km, 388 µs at 80 km** (survey); ~420 µs from any altitude for the capped explore layer. The MERGE is not: 557 µs at 6.8k cells held rising to 1,890 µs at 387k, so 56x the store for 3.39x the cost, sublinear, asserted, bounded at 3.5 ms once a second on a fully lapped Forge, and improved on review by walking one advancing cursor instead of a fresh binary search per key. **Save cost 1.0398 bytes/cell** (delta-varint over a sorted key set, derived ceiling 1.1172): a whole 80 km lap, 40.8% of Forge surveyed, is **1.33 MB for both layers**; a 100 km walk on foot is 3,679 bytes. Round trip exact; the byte stream is a pure function of the SET (proven by filling in two orders); a stream cut at a different cell size is REFUSED rather than reinterpreted onto the wrong lattice. **(h) ORE COUNTS, Factorio's model, ONE POOL.** The map draws `OrePatch::RemainingAmount` straight through the bridge, unrounded and unformatted, and `MapDrawReport.oreDrawnRows` publishes the raw integers so a probe compares field for field rather than parsing "2.4k" back into a number, the method the power panel was proven with. **(i) THE GATE IS ONE PLACE.** `MapWorld.ore()` drops a patch whose ground is unexplored, so an undiscovered patch is ABSENT from the array the painter receives rather than present-and-skipped: a drawing bug cannot leak one because the painter never held one. The mode is asked BY NAME through a new `ModeRules.fullMapRevealed` (DW-31/DW-36), never a raw `sandbox` boolean. Persistence is additive and deliberately does NOT bump `SAVE_VERSION` (a bump refuses every existing world, and nothing here would MISREAD an old slot); `RestoreLedger.discovery` has THREE states because a silent 0 would make a world `/core` refused look like a new game. **31/31 ctest** (was 30), TIER 0 **126/126** and TIER A **108/108** unmoved, `expected.json` moved by exactly one line (`"abi": 11 -> 12`, which `dump_expected.cpp` prints and `parity.mjs` asserts, so it is an artifact an ABI bump must move). **(j) THE DRIVEN VERIFICATION, and THREE MORE DEFECTS it found, two of them mine.** `probes/discovery.js` (new, 847 lines). **The negative control, a 26-rung fixed ladder at bearing 45 degrees with a `forget` and one observation per rung:** survival draws 0 of 4 patches from 2,600 m out to 2,100 m, **1 of 4 at 2,050/2,000/1,950 m (the partial frame, which is the rung that proves the gate rather than the absence)**, and 4 of 4 from 1,900 m in; sandbox draws 4 of 4 at every rung including 2,600 m. Survival's drawn set is a STRICT SUBSET of sandbox's at every rung, compared by raw `resource` + raw `centre` + raw `initial` tuples rather than by count; `drawn + hidden` equals `/core`'s patch count at all 26 rungs in both modes; and `spanM`, `focusActive`, `pixelsPerMetre`, `discoveredQuads`, `currentPoints`, `plannedPoints`, `surveyCells` and `exploreCells` are IDENTICAL between the two modes at all 26 rungs, **the same world in the same frame differs in nothing but which patches reached the painter.** (One last-ULP difference in `focusCentreM` at one rung, 3e-10 m, is float noise in the physics settle and is reported rather than absorbed.) **The shading property turned out NOT to be what the brief assumed**: `discoveredQuads` is EQUAL between the modes, not smaller in survival, because `MapWorld.shading` has no mode gate at all, it draws only what IS discovered, so there is nothing to hide. Asserting it smaller would have been inventing an assertion to make a number match, and the probe says so instead. **The zoom sweep, 110 notches out at x1.15 and 110 back, 600 m to 2.85e9 m:** every alpha monotone in pixels-per-metre out and back; worst single-notch move ore 0.18374 / discovered 0.18685 / body 0.16121 against bounds **0.18687 / 0.18687 / 0.16124 DERIVED from `sizeAlpha`'s own ramp under a x1.15 pixel step** rather than chosen; every alpha reaches exactly 0 and exactly 1; **660 of 660 sampled alphas `===` `sizeAlpha(feature size, reported ppm)`, so one ramp and provably nothing else**; reversible to 2.35e-15 relative over 220 notches against a 9.77e-14 float budget; `bodyFilled` flips once each way in the SAME bracket both ways with no hysteresis, and the marker delta at the flip is exactly `["air"]`, correct, because once the disc provably covers the canvas the annulus is entirely off it. **Ore, field by field:** all 4 patches match `/core` before and after mining, `===` on `resource` and `centre` and the exact inverse of `OreField.report`'s known rounding on the amounts (never a tolerance, never a parsed string), and the painter's own `oreDrawnRows` matches the handed rows field for field; 12 strikes paid 12 times, pack +24 Raw iron, pool 1812.5088938553483 -> 1788.5088938553483, fell exactly 24. **Frame cost:** map closed 8.12 ms; open at a 600 m span **+0.85 ms**; open at a 1,729,530 m whole-body span **+0.69 ms**: the SURFACE framing is the expensive one, and the cost is the ore labels and the label avoider rather than the quads. **THE THREE DEFECTS. (D1, fixed by the probe lane)** `alphas.discovered` stepped 0 to 1 in ONE zoom notch, because `of_disc_window` selects cells on their CENTRE and the margin was a multiple of the SPAN only, so a cell WIDER than the view was dropped entirely: **the surface map drew with no discovered shading at all** at the 600 m span it opens on. Fixed by adding the cell's own half-diagonal to the window radius. Note that my own earlier fix to that same constant, 0.75 to 1.6 spans, moved the step rather than removing it, a margin expressed only in spans cannot be right for a feature measured in cells. **(D2, mine)** the 1 Hz sample interval WAS NOT IN FORCE: `Systems` passes `loop.simSecs`, which is a CUMULATIVE clock because `flight.frame` on the line above wants one, and `MapMode.frame` treated it as a delta, so from one sim second on, every frame added at least 1.0 and the gate opened every frame. **Measured 144.0 passes per sim second**, an orbital pass costing ~2.2 ms in every FRAME instead of once a second, which is exactly what `Discovery.ts`'s own header rules out. And the check that existed to catch it could not: `gapRatio` was then measuring frame-to-frame motion, ~8 cm against a 2 km sweep, which `toFixed(4)` rounds to exactly 0. **A guard rounded into silence by the very defect it was watching for**: the sharpest instance of standing rule 11 this lane has produced. `MapMode.frame(nowS)` now differences the clock itself and the parameter is named for what it is. **(D3, mine, and the one that mattered)** discovery DID NOT SURVIVE A PAGE RELOAD: `restored.discovery = -1`, because `Gameplay.create` runs `g.load()` at Boot line ~267 while `bootMap` constructs the field at line ~370, so `of_disc_deserialize` was called before the field existed and `bootMap` then built an empty one over the top, after which the 20 s autosave wrote the empty set back and it was gone for good. **The in-page round trip PASSED (279 cells, byte-identical), which is precisely why an in-page probe alone would have shipped it**, and `reload.mjs` did not assert discovery at all. Both halves are being fixed: the save is made SELF-DESCRIBING (it carries its own body radius, so a load can happen before anything has told the field which body it is on and the boot order stops mattering), and `reload.mjs` gains the assertion, required to FAIL against the old code before it is believed. **Acceptance:** `ascent.js` green, `flightabuse.js` green, `maneuver.js` green at 65/65. `maneuver.js` did NOT survive the centring change and should not have: rows B1/B2 asserted "the map key off the vessel does not open the map" and "the HUD says so", which DW-36 deliberately reverses. The probe's expectation was stale, not a map regression, and it was replaced with SIX STRONGER rows, including **R17's own negative control, `|centreM|` = 604,668 m, within 1% of the body radius and NOT ~0**, which is the one number that distinguishes a player-centred projection from a body-centred one. `controls.js`'s map opener had been permanently SKIPPED because the map refused on foot, so that modal's Escape derivation had never once run; it now runs and passes. | The map was the last place this project contradicted its own premise: the engine spends a year making surface-to-orbit continuous and then the map hard-cut into a separate mode, which is why DW-36 calls the continuum "not a UI flourish". Making the CENTRE a parameter collapses four features into one, focus switching, re-centring, the plan view and the orbital view are one projection with a different origin. Discovery belongs in `/core` and not in the UI because it is world state: it must persist, be deterministic and be cheap, and the enemies lane had already solved that exact shape. Two resolutions rather than two rules is the DW-7/DW-26 move again, when both options are unattractive, find the third, and it is what makes orbit worth flying without making it a cheat. | **Accepted** (DW-36; physics R17; DW-17/DW-31; standing rules 4, 9, 10, 11) | 2026-07-27 |
| WG-28 | **THE ADVERSARIAL PASS OVER THE FIELD, AND THE INSTRUMENT THAT WAS WRONG.** Standing rule 11 applied to WG-24's headers, which five systems now stand on. **(a) THE TWO INSTRUMENTS, resolved: THE PROBE WAS WRONG AND THE LEVEL OP IS CORRECT.** `probes/level.js`'s negative control read 2.779 m of movement "outside the radius". It sized its ring at 2.5x **its own local copy of the pad radius, `R = 6.0`**, so 15 m. WG-27 widened `LEVEL.radiusM` to 10 and the disc is centred on the AIM POINT, up to `reachM` = 9 m downhill of the player, so the pad reached **16.279 m from the feet** and the control ring was policing ground **1.17 m inside it**. Measured, with the discs the tool reported: the two ring points that moved were **8.844 m and 9.968 m from the disc axis, and the next point out at 10.386 m moved 0.000000 m**, against a radius of 10.000. `levelDisc`'s corner loop rejects on the unpadded radius, so the op's footprint is right; the control's geometry was a remembered constant. **Fixed at the root rather than by re-tuning a number**: `LevelStats` now publishes `lastCentreM` / `lastRadiusM` / `maxRimFromFeetM`, `terraform()` publishes `limits`, and the probe derives its ring from `reachM + radiusM` and **asserts it cleared every disc the tool cut** (`controlRingClearsThePad`, ring 20.000 m against a rim of 16.279 m, clearance 3.721 m). `outsideUntouched` is back to **0 of 12 points moved, 0.000000 m**. **The general form: a control whose geometry is a constant copied from the thing it watches stops being a control the moment that thing is retuned, and it fails in the direction that looks like a real defect.** **(b) EVERY TRIANGLE THE MESHER EMITTED WAS WOUND INSIDE OUT.** Found by the review, confirmed independently two ways before touching it: stepping 0.35 m along each triangle's geometric normal landed in ROCK for **258 of 258** crater triangles and **259 of 260** on a mound, and the geometric normal disagreed with the mesher's own per-vertex gradient normal on **100%**. The client draws this mesh `MeshLambertMaterial`, no `side` override, so three.js back-face-culls it, and an inverted CLOSED surface does not vanish, it draws its **far side through its near side**. **That is what the "isolated pale fragments inside the crater" were**, and `docs/screenshots/WG28_crater_before.png` against `WG28_crater_after.png` is a black void full of pale shards against a lit, readable bowl. It survived 143 green checks because winding is invisible to every question the suite asked, and the one test that reads `indices` past a count **sorts each triple**, erasing winding by construction. New `mesh_triangles_face_out_of_the_rock`; **verified by reverting the fix in a scratch copy, where it fails 9 checks in exactly one test and nothing else in the suite notices**. **(c) THE COST, 4.6x cold and 12.8x warm, with a byte-identical fixture.** `levelDisc` asked EVERY cell in the padded box for its solidity, twice, to compute `dug` / `filled`: about 32,000 `solidCell` calls where the op writes ~2,000 corners. A cell reads exactly its own eight corners, so a cell none of whose corners the op writes cannot change; the cell passes are now restricted to that candidate set, marked while the corner pass is already visiting each corner. Writes are deferred to their own pass so "before" is still read against an untouched field. **Native A/B, same binary, same sites: cold 42.7 to 9.3 ms, warm 12.9 to 1.0 ms, and `expected.json` is byte-identical**, which is the proof the output did not move. In the browser a press is now **39.7 ms cold (op 15.2, near-mesh rebuild 25.6, quote 1.2) and 7.0 ms held (op 2.4, rebuild 2.8)**; the remaining cold cost is the re-mesh, not the terraforming. **(d) `of_streamer_level` still gated on `res.cells()`**, so ABI 8's fix was applied to the main thread and not to the worker that owns the drawn chunk. Corrected to `res.corners`. Honest about magnitude: a sweep from 0.5 m down to 0.001 m of edit could not produce a `cells == 0, corners > 0` press at the test site, so this removes a class rather than a measured symptom. **(e) `setCorner` never updated `airCount_` on an OVERWRITE**, so the air/rock split drifted the moment a second op touched a corner the first had written: dig 2.5 m then fill 3.2 m reported **257 air / 187 rock against a true 212 / 232**. The client uses those two numbers for drift detection between its copy of the edit set and the worker's, so a counter that lies disarms a detector. The shipped round-trip assertion applies exactly ONE op, the only case where the bug is invisible. **(f) `fieldWorldSig` omitted two BodyParams fields world generation reads**: `kind`, which dispatches the entire height stack, and `homeBlendRadiusM`. Warmed on one body the memo answered for another, out by **2,778 m across `kind`** and **28.7 m inside the blend annulus**, and the bridge makes it reachable because `of_edits_create` binds a field to no body. The identical omission sat under the identical warning comment in `voxel_terrain.h`; both fixed, `fieldWorldSig` made public because the only way to observe a memo key is to compare two of them and it had no test at all. **Two fixture lines move and both ARE the fixes** (`voxel.faceHash`, the winding; `digColumn.removed` 9 to 29, the air counter). 29/29 ctest, self-determinism **119/119**, cross-toolchain exact **108/108**, `npm --prefix web run check` green at 166 files. **NOT FIXED, logged with numbers in ROADMAP-W11**: the floating-slab bypass in `columnSurfaceHeight`'s first sample (a slab 21 to 27 m up pins the heightfield to the +24 m cap), `runIsAnchored`'s floor being the designed base (so nothing below the base can read as floating; a slab in a pit refills it by 10.9 m), `columnTouched` documented as exact and missing 19.29% of the true stencil (sub-millimetre today), and `surfaceNets` returning an empty mesh when `maxCells` is exceeded, which is byte-identical to "there is no surface here". | The review found more in one pass than the implementation lane found in a night, on a header the implementation lane had just measured to four decimal places, which is standing rule 11 stated as an outcome rather than a principle. Two lessons are worth more than the defects. **A negative control that starts failing after a rewrite is a claim about the CONTROL as often as about the code, and telling which apart requires the control to name its own subject.** And **a proxy metric can rank a broken frame better than a fixed one**: counting bright outliers inside the crater read 4 before and 9 after, because a black void has nothing to be an outlier against, and only looking at the two captures explained it. DW-7 for the third time: structural validation cannot replace looking at the thing. | **Accepted** | 2026-07-27 |
| WG-30 | **D3: WHAT THE PLAYER EXPLORED DID NOT SURVIVE A RELOAD, AND THE 20 s AUTOSAVE THEN MADE IT PERMANENT. Fixed by making the save SELF-DESCRIBING, which removes the ordering dependency instead of managing it.** `reload.mjs --phase=ground` reported `persist.restored.discovery = -1` while the in-page `of.save()`/`of.wipe()`/`of.load()` round trip PASSED byte-identically at 279 cells, which is exactly why an in-page probe alone would have shipped it. **The cause was BOOT ORDER, not the format.** `Boot.ts` applies the save early (`Gameplay.create` → `Persist.apply` → `_of_disc_deserialize`) and builds the map late (`bootMap` → `new Discovery(...)` → `_of_disc_reset`). A layer stream carried `bits`, which says what the keys MEAN but not what BODY they are on, so a reader had to be told the radius by somebody else before it could read: at load time the `/core` field did not exist, `of_disc_deserialize` returned -1 immediately, and `bootMap` then built a fresh empty field over the top, which the next autosave wrote back to disk. **THE FIX IS TWO HALVES AND EITHER ALONE STILL LOSES THE WORLD. (a) `WorldDiscovery` gained a stream preamble**: magic `'OFDW'`, a version, and the BODY RADIUS as `w.f64`, and `deserialize` REBUILDS both `DiscoveryGrid`s at the stream's radius when it differs, so a load may happen before anything has said which body this is. The all-or-nothing rollback got wider with it: a rebuild before the layers are read means a refusal must put the LATTICE back as well as the keys, or the cells come back into the wrong grid, which is precisely the "world that never existed" the method exists to prevent. A wrong magic is refused without consuming a payload it cannot measure. New public `bodyRadiusM()`. The format was changed outright rather than versioned around, because ABI 12 is unlanded and no shipped build has ever written a save with it. **(b) new export `of_disc_ensure(bodyId)`**, which resets ONLY when there is no field or the existing one is cut for a different body, so the LATE construction cannot wipe what the EARLY load restored; `Discovery.ts`'s constructor calls it instead of `_of_disc_reset`, and `of_disc_reset` stays unconditional because probes and new worlds want exactly that. `of_disc_deserialize` now CONSTRUCTS a placeholder field when there is none and lets the stream correct it (not a guess: the radius comes out of the bytes), drops the placeholder on a refusal, and refuses an empty arena before building anything, the reader throws past the end and this module is built with exceptions off, a case the old `if (!g_disc)` guard had been covering by accident. **NO ABI BUMP: 12 is uncommitted and unlanded, so extending it is not a contract change**; the changelogs in `of_core_api.cpp` and `OfCore.ts` now describe what 12 actually is. **THE ASSERTION THAT WAS MISSING IS WHY IT SHIPPED**: `reload.mjs` did not check discovery at all. It now does (`the discovered world came back`), and it was **verified in both directions**: it FAILS `-1` against the old build and PASSES at **`restored.discovery = 378725`** against the new one, on this working tree. Two new core tests, `a_stream_carries_its_own_body_into_a_field_built_for_another` (a Cinder-cut field, 1,532 explore cells at 11 bits, loaded into a `WorldDiscovery` built at Forge: cells, lattice, cell size and every walked direction all restore correctly) and `the_self_describing_preamble_refuses_without_eating_the_payload`; the first was **negative-controlled by disabling the rebuild, where it fails by name on 28 checks and nothing else in the suite notices**. **27 discovery tests / 752 checks, 31/31 ctest, TIER 0 126/126, TIER A 108/108 (both gating, both unmoved), TIER B 148/3 informational, `expected.json` diff is the single `"abi": 11 → 12` line.** **Also found and corrected, unasked**: the `-1 IS NOT A ROLLBACK` warning in `of_discovery_api.inc` and `discabi.ts` was STALE, `WorldDiscovery::deserialize` had already been made all-or-nothing, so the comment had been wrong since it was written. | A save that cannot be read without being told what it is has an ORDER dependency, and an order dependency in a boot sequence is a defect waiting for the sequence to change. Putting the body in the bytes is not a format preference; it is the difference between a load that works wherever it is called and a load that works where it happens to be called today. **The general lesson is about the INSTRUMENT: the in-page round trip and the reload ask different questions, and only the second one is the question the player asks.** A round trip inside one page is a test of the FORMAT; only a real reload tests the LIFECYCLE, and this defect lived entirely in the lifecycle. Standing rule 11: an assertion that has never been seen to fail is not yet an assertion, so the new check was run against the broken build first. | **Accepted** | 2026-07-27 |
| WG-31 | **THE SINKING WAS BACK, IN TUNNELS, AND IT WAS GP-53's DEFECT IN A SECOND CODE PATH.** Reid: "standing still in the tunnel im getting the same sinking thing where it snaps up every few seconds." "The same" was exact. GP-53 fixed `StructureBodies.deckUnder`, a march down from the feet clamped with `Math.min(rFrom, r + 0.05)`; `VoxelCollision.floorBelow` ended `return Math.min(r, rr + 0.1)`, so with the first solid sample within one march step of the feet, or with the feet inside rock at all, the query handed back `r` itself. **The clamp is not the same line of code and it is the same sentence: a floor query that clamps its answer to the querier's own position ratifies the querier's error, and gravity supplies a fresh error every tick.** **MEASURED, per TICK, player stationary and grounded on a dug tunnel floor at 0.00 m/s, 600 ticks (`probes/tunnelsink.js`, new).** The instrument is `StandTrace` widened with `preSnapR` (the radius the walker ARRIVED at), `underRock`, `pushM` and `pushUpM`, because `groundR` alone cannot tell a ratified floor from a correct one: both are "the ground the snap used", and only `terrainR - preSnapR` separates them, with no threshold, since a floor that is a property of the world cannot equal a moving querier tick after tick by coincidence. **Site A, the sawtooth: standing spread 0.249535 m, 6 snap-ups at a mean period of 93.2 ticks, biggest snap 0.247317 m, sinking 0.002683 m EVERY tick, which is exactly one tick of free fall and exactly the number GP-53 measured on the deck. Ratification 594 of 600 ticks. Site B, the pure sink: spread 0.053811 m, ratification 600 of 600, the feet parked 0.261495 m BELOW the world's own floor with nothing ever putting them back. After, at both sites: spread 0.000000, sink 0.000000, snap-ups 0, ratification 0 of 600, worst floor error 0.000000, 600/600 grounded and under rock.** **THE CORRECTOR WAS NOT THE ONE I BRIEFED MYSELF FOR, and the trace named it in six rows**: `resolveEmbedded` pushed on **0** ticks throughout, so the snap is NOT the mouth push. It is `resolveStep`: the ratchet sinks the feet until the capsule's lowest sample at 0.15 m reads solid, `free(q)` goes false, the ledge climb lifts a full 0.55 m rung, and `floorBelow`'s clamp immediately pulls that back down to 0.3 m under the lift, netting +0.247 m. The two authorities are visible side by side at t=659/660 and invisible in any per-FRAME reading, which is how this survived GP-53's pass. **THE FIX.** `floorBelow` is now the highest air-to-rock crossing of the signed field on that radial, **bisected**, with no clamp: if the feet are in AIR the floor is strictly below them, full stop; if the feet are IN rock it looks up by at most `VOXEL_STEP_UP_M[0]` (0.55 m, the walker's own first rung, read not recited) so a floor you have sunk into seats you on top of itself; and buried past a rung it returns **null**, because that is an embedded capsule and `resolveEmbedded` owns it, and answering anything at all there is what ratified the sink. 30 halvings take the 0.25 m march bracket to 2.3e-10 m, **below the f64 spacing at Forge's 604 km radius**, so the answer is bit-independent of the bracket and the spread is 0 rather than 1e-7. Bisection also retires a cell-quantisation assumption the old march carried: since WG-24 the floor under a tunnel is an interpolated iso-surface, not a cell face, and the old code could only ever land on a multiple of its own step. **MY FIRST CUT OF THE FIX WAS WRONG AND THE DRIVE CAUGHT IT**: looking up for air unconditionally found rock ABOVE feet that were standing in air, read a ceiling fragment as a floor, and walked the player **8 m up and out of their own bore over 14 strikes**, the 0.1 m ratchet in this function's own comment history, rebuilt from the other side. **THE PROBE WAS ALSO WRONG TWICE, both times in the direction that looks like a real defect**: its step-up control walked one way, measured 0 climbs, and the reason was that it had walked 5.24 m DOWNhill; and its under-rock search only walked forward, so it reported "never got under rock" for the FIXED build while the broken one measured cleanly, which reads exactly like the fix breaking tunnels. Both now try both directions and the probe reports how the spot was reached. Gates: **31/31 ctest**, `npm --prefix web run check` green at 206 files including the boot gate, and `tunnelwalk` (13.98 m walked, 8/8 grounded under rock, push 0.000), `walkfeel`, `jump` and `walk` (100% grounded over 83.9 m) all green. | Two lessons. **The first is that GP-53's general form was the finding, not GP-53's fix**: the same sentence was true of a different function in a different lane, and the only reason it was found in one pass is that the sentence had been written down as a sentence rather than as a patch. The second is about the instrument: `resolveEmbedded` was the obvious suspect, the brief named it, and it fired zero times. Recording the CANDIDATES separately instead of the answer is what let six rows of trace overrule a hypothesis. | **Accepted** | 2026-07-27 |
| WG-32 | **A TUNNEL MOUTH READS AS SEALED FROM OUTSIDE, AND IT IS THE HEIGHTFIELD CHUNK SHEET. DIAGNOSED AND MEASURED, DELIBERATELY NOT FIXED.** Reid: "I dug a hole through this mountain, when out the other side looking back into the tunnel it looks like its filled in but when you actually go into it you can see that its a tunnel." **The layer is the streamed CHUNK mesh, not the near voxel mesh, and the number that carries it has its control in the same frame:** over a 13x13 m patch of 169 columns straddling the mouth, at chunk depth 14 (1.798 m vertex spacing), the drawn chunk surface stands above the outermost solid cell by **0.19 m on the 146 untouched columns and 4.04 m on the 23 dug ones**. At the mouth column the pooled buffer draws relief 4649.02 m while the last solid cell is at 4644.98 m: four metres of hillside painted across open air, and it is what the crosshair lands on. The field itself is fine, and says so three ways: the aim ray from outside runs **10.25 m of continuous air** to a point under 1.5 m of real roof; the oracle DID lower, by **3.912 m** at that column, on 23 of 169; and walking in takes `underRock` false to true with `skyVis` 0.999 to 0.057. **The distance dependence Reid noticed is real and measured: mouth-core tile luminance 48.07 at 4 m, 49.86 at 6 m, 85.29 at 8 m against a hillside ring of 94.82 and a tunnel interior of 43.07 measured from inside on the same run. The hole is visible to 7 m and gone at 8.** **THREE HYPOTHESES REFUTED BY MEASUREMENT, INCLUDING THE ONE I BRIEFED AS PRIME SUSPECT.** LOD/staleness: **refuted**, every drawn vertex equals `of.surface` at its own direction to **0.000 m**, 15 of 15 digs applied, depth is 14 from 4 m to 60 m of standoff with the 4.04 m identical at every one, and COARSER LOD makes it SMALLER (3.75 m at depth 13, 1.87 m at depth 12). The mechanism is SAMPLING RATE: a roughly 3 m lowered footprint on a 1.798 m vertex grid, where the five vertices contributing to the mouth column read 4649.15 / 4648.51 / 4648.51 / 4649.76 / 4646.22 against an oracle base of 4648.98 and an oracle surface of 4645.07, so exactly ONE vertex in reach caught any lowering and the triangles interpolate a shallow dimple over a 3.9 m hole. The `editedOnly` / `growCells=1` filter: **refuted**, `--voxelskin=0` moves the mouth core by 0.000 / 0.000 / 0.182. Depth precision: **refuted**, the disagreement is 4.04 m of geometry with no z-fight speckle. **THE A/B, per tile, same camera to the byte:** `--voxelnear=0` changes a third to a half of the whole frame (mean abs 11.667 / 8.898 / 5.209 per tile) and changes the MOUTH CORE by **0.047 / 0.065 / 3.736**, and removing the near mesh makes the sealed core BRIGHTER (85.29 to 89.03), never open. **The near voxel mesh is radially INSIDE the sheet by the same 4.04 m, and structurally must be: surface nets extracts the zero level of the field `solidAt` reads, and a dig only REMOVES, so the edited iso-surface can never rise above the pristine base the sheet draws when a column fails to reconcile. The voxel lip can never stand proud.** That is why RN-9's rule ("the near mesh draws only faces the heightfield cannot show") has no answer here: the heightfield cannot show this, and it draws it anyway. **NOT FIXED, and the reason is scope rather than difficulty:** the two candidate fixes are (a) make the chunk mesh resolve the notch it already knows about, by forcing a deeper split for a near chunk containing an edited column or by snapping the nearest vertices onto lowered columns, which touches `TerrainStream.ts`, `terrain.worker.ts`, `ChunkGeometryPool.ts` and `EdgeStitch.stitchEdges` and therefore crosses into rendering's chunk format and the LOD seam proofs; or (b) PUNCH the sheet, degenerating chunk triangles whose columns are fully open and letting the voxel mesh own those pixels, which also needs `editFacesOnly` to stop restricting the near mesh to `growCells = 1` around a broken column or the aperture comes out empty. (b) is architecturally the right answer and is the inverse of RN-9's rule stated properly: **a surface that cannot represent a feature must not draw over it.** Neither is a change to make without a seam and determinism pass, and NEITHER is a depth bias: `polygonOffset` and `renderOrder` are wrong here because the two surfaces are 4 m apart, not coplanar. **`probes/tunnelmouth.js` ships with the diagnosis and TWO assertions that both FAIL on this build**, verified by me independently of the subagent that wrote them: `sheetTracksFieldOverDugGround` (no threshold at all, the control is the untouched half of the same patch in the same frame at the same LOD: 4.04 against 0.19) and `mouthReadsAsOpenFromOutside` (the mouth's tiles must read nearer the tunnel interior, measured from inside on the same run, than the hillside beside them: 85.29 against 43.07 and 94.82). The probe reports `valid` for THE RUN SET UP and a separate `pass` for THE ASSERTIONS HELD, because a probe whose assertions are false under a true top-level boolean is precisely how `tunnelwalk.js` stayed quietly red for days. | The prime suspect in the brief was a coarse or stale chunk, and the distance dependence looked exactly like an LOD boundary. It is neither: the LOD is constant across the whole range where the symptom appears and goes the wrong way when it does change. What looks like an LOD artefact is a Nyquist argument, and the two are only distinguishable by reading the CONTRIBUTING VERTICES rather than the chunk depth. | **Accepted** (diagnosis; fix deferred, cross-domain with rendering) | 2026-07-27 |
| WG-13 | **Wave-0 headless core BUILT + GREEN** — `core/include/of/cubed_sphere.h` + `core/tests/test_world_gen.cpp` (`world_gen_tests` target). Crack-free determinism proven **BITWISE** (uint64 bit-reinterpret `==`, not `CHECK_NEAR`): neighbour same-depth shared edges (both axes) + parent edge-midpoint vs child corner + same-seed regen are all bit-identical. Mechanism = an **integer vertex lattice**: a vertex's `unitDir` is a pure function of `(faceId, ix, iy, level)` via `latticeCoord(i,L) = -1 + 2·(i/2^L)` (identical operands/order from any quad), then height is **position-hashed** `(bodySeed, dir)` (WG-6). Implements the tangent cube→sphere warp (WG-5), the 3-layer noise stack (WG-7; planet ridged mountains / moon crater field), `SampleTerrainHeight`, and Forge/Cinder body params (Spike 1 §5). | The bit-identity guard is the only robust crack test — a hairline crack is a last-bit disagreement. Lattice-point naming (not quad-local index/sequence) is what makes shared `dir`s bit-identical regardless of which quad/depth/order samples them; position-hashing then makes the height follow. Validates WG-1/WG-5/WG-6/WG-7 in real compiled+tested C++ on g++/Ninja. Mirrors the green core-engine/factory-sim Wave-0 pattern (header-only `of::` + `test_framework.h`); added a parallel `world_gen_tests` target only — no existing target/header touched. **Result: 10 tests / 3582 checks pass; all 3 ctest suites green.** | **Accepted** (Wave-0 build; spike WV1–WV3, WV7) | 2026-06-14 |
| WG-30 | **DW-37: THE MAP DRAWS THE WORLD. A new `/core` export samples the ground over the VIEW, and the discovery quads are gone.** Reid, on the map WG-29 shipped: *"cant see the terrain from the map, even in sandbox."* Every instrument was correct - POSITION, FOCUS, the discovery counts, the scale bar, the zoom continuum - over an empty grey plane. **Discovery REVEALS terrain, so a map with no terrain has nothing to reveal and the feature could not be evaluated at all.** **(a) THE EXPORT, ABI 12 to 13.** `web/wasm/of_map_api.inc` §19: `of_map_sample(body, centre, planeU, planeV, spanM, aspect, n)` writes an `n` by `round(n*aspect)` grid of `[biomeId, heightM, surveyed]` into the f64 arena, row-major, top row first. It is specified in **view-region terms rather than pixel terms** because DW-37 turns the map into a rotatable 3D camera next: the PAINTER downstream is expected to be replaced and the SAMPLING is not, and a centre plus two axes plus a span plus a density is a question a camera asks too. The ray solve is **exact and transcendental-free** (CE-11/DW-15): the projection is orthographic, so the ground under a sample is a line meeting a sphere - `pPerp = p - a(p.a)`, `t = sqrt(R^2 - |pPerp|^2)`, near root, dot products and one sqrt, no trig and no marching. **Standing rule 1 held and is MEASURED**: the centre sample's height is **bit-identical** to `of_base_height` at the centre's own direction (`4667.788837614854`, `===`), its biome equals `biomeAt`, and an independent JS re-derivation of the same ray solve over 2,500 samples found **0 biome mismatches**; the on-body fraction matches the analytic disc area to **0.09 pp** at three framings (78.630% vs 78.540%, 39.202% vs 39.270%, 34.987% vs 34.907%); the worst `dot(dir, axis)` over an on-body grid is **+0.028**, so every solved direction is on the viewer's side and the far root is never taken. **(b) THE QUAD LAYER IS DELETED, NOT LAYERED UNDER.** A per-sample survey bit is a finer and simpler mask than a 9,375 m discovery quad, so `MapLayers.strokeDiscovered` and `Discovery.window()` are gone with their cache, their row cap and their two counters. `of_disc_window` STAYS in the ABI - it is not wrong, it is simply no longer the map's shading source, and it is still the right call for a cell overlay (the pollution layer the enemies branch will want). This deletes a whole bug class with it: the old window selected cells by their CENTRE, so it had to reason about how far a cell WIDER than the view could reach, and getting that wrong put a 0 to 1 step in `alphas.discovered` inside one zoom notch (WG-29 D1). A sample has no extent and the grid is cut to the canvas, so every sample is inside the view by construction. **(c) THE PAINTER IS ONE BLIT.** An `ImageData` at the grid's resolution into a scratch canvas, then `drawImage` scaled with smoothing - one composite instead of thousands of paths. Biome colour is a **copy by value** of `render/materials/BiomePalette.ts`'s ten hex values (src/ui may not import three.js and the render palette builds `THREE.Color` at load; copying keeps the painters pure functions of their arguments). An unpainted sample carries SURFACE's colour at **zero alpha**, so the scaled blit fades into the body fill beneath it instead of a black fringe, and the blit is CLIPPED to the body disc unless `discCoversCanvas` proves the disc covers every corner - the same fact `paintBody` already uses. **(d) THE PLAIN HEIGHT RAMP WAS NOT ENOUGH, AND ONLY LOOKING SHOWED IT (DW-7).** The first version shaded by elevation alone, exactly as briefed, and the picture was a single pale rectangle: at a 400 m span the whole view is ONE biome with 44 m of relief and the gradient is too gentle to read as ground. Every count said terrain was drawn and the picture still said nothing, which is the failure DW-37 was raised about. Added a **hillshade**, computed from the same heights, which is what the eye actually reads as terrain. **The first hillshade was ALSO blank, and its mistake looked principled**: it normalised the per-sample rise by the view's total RELIEF, but a smooth 44 m spread over 36 samples is 1.2 m a sample, so every gradient came out near 0.03 and every lambert near the same number. Real terrain slopes are SMALL at map scales - which is why cartographic relief has always used a vertical exaggeration - so the scale has to come from the view's own **typical slope**, one extra pass over the grid. Then a 400 m hillside and a whole-body mountain range light with the same contrast and there is no threshold anywhere. **(e) `n` CHOSEN BY MEASUREMENT, NOT BY TASTE.** A sample costs **2.37 us** (biomeAt + sampleDesignedHeight; an off-limb sample costs a dot product and is skipped, which is why the same grid is 5x cheaper zoomed out). Swept on the shipped wasm at a 2.9:1 aspect: n=16/736 samples/1.7 ms, 24/1,680/3.5, 32/2,976/6.4, 36/3,744/8.9, 40/4,640/11.7, 48/6,672/14.3, 64/11,904/26.7, 96/26,688/58.7. The shipped panel is **1.19:1**, not 2.9:1, so re-measured in the browser on the real panel **n = 48 is a 58x48 grid, 2,784 samples, 5.2-10.0 ms a rebuild** - the same ~8 ms budget at the shape the map actually has. Steady-state **cache hit rate 0.96**. **(f) A CACHE KEYED ON A RAW POSITION IS A CACHE THAT MISSES EVERY FRAME.** Measured: a player merely SETTLING on uneven ground moved `centreM` by centimetres per frame and rebuilt the whole grid at 60 Hz, **+7.32 ms of map cost with the map open and standing still**, against +0.84 ms on a run that happened not to jitter - a factor of nine decided by noise. The request is now SNAPPED to a thirty-second of one sample, and the snapped values are what is SAMPLED and not merely what is keyed, so the grid is a deterministic function of its key rather than of whichever frame missed. **Cost after: +0.575 ms zoomed in and +0.575 ms at whole-body in sandbox, +0.582 / +0.248 in survival** - against WG-29's quad layer at **+0.85 / +0.69**. The terrain map is CHEAPER than the empty one it replaces. Rebuilds over the same 12,866-call run fell 3,459 to 507. **(g) THE NEGATIVE CONTROL, EXTENDED TO THE GROUND.** `probes/discovery.js` gained section 3b, and the ladder gained three rows. The old file asserted `discoveredQuads` IDENTICAL between the modes and was right to: quads are by definition ground you have seen, so there was nothing to hide. Terrain is not, so the number moved to the mode-DEPENDENT half and the claim sharpened. **At a 139,755 m span on the same world and the same frame: sandbox paints 2,784 of 2,784 on-body samples; survival paints 274, which is EXACTLY `/core`'s own `seenOnBody` count and strictly greater than zero and strictly fewer than the ground that is there.** Both halves are asserted in EACH run, so neither run can pass by being the other one, and the painter's count is checked against `/core`'s count of what the mask allows - two independent reads of one answer. `terrainSamples` and `sampleSizeM` are mode-blind and join the identical-across-the-pair list. At the on-foot span the mask CANNOT bite in sandbox (the whole 600 m view sits inside one 9,375 m survey cell) and the probe says so rather than leaving it unexplained: 26/26 rungs fully painted in sandbox, **16/26 in survival**, where the view straddles a cell boundary. **(h) THE ZOOM CONTINUUM SURVIVED, IN A STRONGER FORM.** The terrain layer's feature is a SAMPLE of ground and the grid is cut to the canvas, so a sample's ground size scales with the span exactly as pixels-per-metre falls: **its size in PIXELS is invariant**. Its alpha is therefore constant rather than ramping, which is a stronger statement of "no threshold" than a ramp - there is nothing left to step. Asserted as exactly constant over all 220 notches, as `=== sizeAlpha(sampleSizeM, ppm)` at every one of them, and the invariance is checked on the pixel size itself. The other two alphas still reach 0 and 1 (ore worst step 0.18374 against a derived bound 0.18687; body 0.16121 against 0.16124). **(i) AND ONE STALE ASSERTION REPLACED BY A STRONGER ONE.** `the fill flip draws exactly the same content` compared `discoveredQuads` across the `discCoversCanvas` notch and failed at 2,784 -> 2,716. It was not a defect: `discCoversCanvas` is true exactly while the body covers every canvas CORNER, so the notch that flips it IS the notch at which the corners leave the body, and a sample off the limb has no ground under it. The row now asserts that the on-body count MUST move there, and in which direction - the same geometric fact computed in two entirely separate places, the painter's projected radius and `/core`'s per-sample ray solve. **Green:** 31/31 ctest, TIER 0 **126/126** and TIER A **108/108** unmoved, `expected.json` moved by exactly the `"abi": 12 -> 13` line, `probes/discovery.js` **valid in BOTH modes with 0 fails**, `ascent.js` green, `reload.mjs` `fails: []` with `restored.discovery = 378174`, `npm run check` clean for every file this lane touched. New `probes/mapshot.js` frames the three screenshots by driving the real M key and the real zoom hook, because a picture is a thing you look at and take again and the acceptance run is twenty minutes. | The complaint was one sentence and the fix had three layers, and only the first was the one that was briefed. The EXPORT was the easy half. What was not: an elevation ramp that is the honest shading of a height field draws nothing a person recognises as ground, and its replacement's first normalisation was dimensionally correct and equally blank - both were found by looking at the picture, twice, after the counts already said the layer was alive (DW-7 for the fourth time in this lane). And a cache keyed on a raw player position is not a cache; the difference between +7.3 ms and +0.58 ms was a decision about what counts as the same request. | **Accepted** (DW-37; WG-29 line; standing rules 1, 5, 9, 11) | 2026-07-27 |
| WG-33 | **THE CLOSE-IN MAP WAS READING THE WORLD BEFORE THE PLAYER TOUCHED IT, AND SHADING ONE BAND OF A FIELD THAT HAS NO CONTENT AT ITS OWN SAMPLE SPACING. ABI 13 to 14.** Reid pressed M at the default surface view and saw a flat slate field with ore circles floating on it. Every count was green: 2,784 painted of 2,784 on-body, alphas 1, nothing refused. `painted == onBody` was true and worthless, which is the first half of `mapshot.js`'s own header proving itself (DW-7). **(a) ADMIN'S HYPOTHESIS WAS REFUTED BY MEASUREMENT.** The proposed cause was a planet-scale ramp keyed to ABSOLUTE elevation. It was not: DW-37's ramp was already normalised to the frame's own relief and its hillshade to the frame's own mean slope, and the numbers say so. Over the painted samples the blank 454 m frame scored lumaSd 22.96, lumaSpread 66 and 10 tone buckets against the LEGIBLE 52 km frame's 21.87, 70 and 11. **The close-in map was already using the full tone range.** What it had no trace of was LOCAL contrast: the mean tone difference between ADJACENT samples read 1.94 blank against 8.61 regional, a factor of 4.4 where the global numbers differed by 5%. **(b) THE MAP READ THE DESIGNED BASE, WHICH IS BY DEFINITION THE WORLD BEFORE THE PLAYER TOUCHED IT.** `of_map_sample` called `sampleDesignedHeight`, so a dug hole, a levelled pad and a tunnel mouth could not reach the map AT ALL, however the painter shaded them, and a close-in map's whole job is to show you your own work. ABI **14** puts an `edits` handle in second position and reads `surface_field.h`'s `surfaceHeight` instead, WG-21's bedrock clamp included. Breaking on that ONE signature, one day old, one caller. With `edits <= 0` the value is bit-identical to ABI 13's by the oracle's own undug overload. The handle is taken from the `SurfaceOracle` OBJECT and read every sample, never cached, because the handle is bound after boot and a copy taken in the constructor would be a permanent 0. The two sparse-set sizes ride in the grid cache key, so a dig invalidates the picture. **(c) TWO RELIEF BANDS, not one.** `MapRelief.ts`: `litFull` is the field's own hillshade, which draws a mountain range; `litDetail` is the same lambert on the field with its LOCAL TREND removed (three masked box passes, an eighth of the frame across), which draws a levelled pad, a dug trench and the microrelief of a desert. A hillshade of a SMOOTH field is a smooth wash however well its magnitude is normalised, because every sample then has nearly the same slope DIRECTION as its neighbour. **One box pass stamped soft axis-aligned RECTANGLES across the map and three do not; found by LOOKING, because a rectangle of trend has exactly the contrast statistics of a hill.** Both windows are fixed counts of SAMPLES and never distances, so there is no zoom threshold (DW-36); a weight keyed on `sampleSizeM` was considered and rejected as a span threshold with a smooth face on it. **(d) `MapContrast.ts`, the number that can fail a blank picture**, measured off the FINAL RGB bytes rather than the heights, because a shading bug, a palette bug and a flat world all produce the same blank rectangle and the player cannot tell them apart either. `lumaSd` / `lumaP5` / `lumaP95` / `lumaSpread` / `lumaStep` / `buckets`. **MEASURED, one static build against another differing ONLY in the shading, identical world data in every frame** (relief and stepM bit-identical, painted == onBody in all six): surface at +1.4 km **1.914 to 4.193 lumaStep** (+119%), regional **9.698 to 11.555** (+19%), orbital **26.139 to 26.733** (+2.3%). **Nothing regressed anywhere.** ABI bump coherent across the bridge (standing rule 9): `of_core_api.cpp` 14, `expected.json` 14 as a deliberate ONE-LINE edit, `OF_ABI_VERSION` 14, `discabi.ts` signature, the rebuilt `dist` reporting `abi=14` at handshake, and the `edits` parameter proven LIVE because passing 0 against the real handle changes the output. Parity 126/0 self, **108/108 cross-toolchain exact**. | Inherited uncommitted from a lane that was killed mid-flight and **finished rather than reverted**, on the evidence above: the ABI change is a real defect fix and not a refinement, the two bands improve every zoom and regress none, and the reasoning was already anchored in real frames. What was missing was the last mile, which is WG-34. | **Accepted** (recovered lane; DW-37, DW-7, standing rules 1 and 9) | 2026-07-27 |
| WG-34 | **THE CONTRAST ASSERTION, ITS TWO NEGATIVE CONTROLS, AND THE FINDING THAT THE PROBE WAS AIMED AT THE ONE PIECE OF GROUND GUARANTEED TO BE FLAT.** **(a) `mapshot.js`'s surface shot framed the START PAD.** Its anchor is the ore cluster beside the spawn, and `BodyParams.homeFlatRadiusM` is 150 m of DEAD-LEVEL ground blended back to natural by 600 m, so a 454 m frame taken there is mostly an artificial apron. **Measured on the middle row of that frame: 34 of 58 samples sit at one height to within 0.1 m.** The map was correctly drawing a flat apron as a flat wash. That is half of what Reid saw, it is not a shading defect, and it is the instrument pointed at the wrong ground: the WG-28 pattern again. `mapshot.js` gains `offsetM`, defaulting to 0 so every existing invocation frames exactly what it framed before. **(b) RESOLUTION IS NOT THE LEVER, and that was measured rather than assumed.** The natural field has no content below roughly 60 m (WG-24) while a 454 m frame samples every 9.45 m, so raising `TERRAIN_N` puts adjacent samples closer together on a band-limited field and **lowers** every contrast number while costing 5x to 10x: at one site n=48 scores 4.224 lumaStep at 9.27 ms a rebuild, n=96 scores 2.373 at 49.1 ms, n=144 scores 1.662 at 94.0 ms. `TERRAIN_N` stays 48. The corollary is that **`lumaStep` is resolution-dependent and every floor below is pinned to n=48**, which is written into the probe that uses them. **(c) `probes/mapcontrast.js`, a floor that fails a flat wash BY NUMBER.** It asserts over a LADDER of eight sites along one meridian out to 40 km, not one frame, because one frame is not a sample of anything and this world contains a frame that is SUPPOSED to be blank. Site 0 is the start pad and is kept deliberately: it is the frame Reid reported and the one the fix helps least, and a ladder that dropped its hardest case would be marking its own homework. Two floors, on the WORST site and on the MEDIAN site. **`lumaSd` and `lumaSpread` are deliberately NOT asserted on**, because they were measured and they do not work: a global spread cannot tell a relief map from a smooth gradient of the same range. **THE THRESHOLDS ARE DERIVED, NOT CHOSEN.** Each is the GEOMETRIC MEAN of the defect's score and the fix's score on that ladder, the point of maximum EQUAL log-margin: the fix clears the floor by exactly the factor the defect misses it by. Ladder, DW-37 one band: `2.251 3.355 1.863 4.609 2.599 4.983 1.533 0.853`. Ladder, two bands: `2.587 5.431 4.201 6.233 5.570 6.640 4.689 3.674`. Worst **0.853 to 2.587**, floor `sqrt(0.853 x 2.587) = 1.4855`, margin **1.742x each way**. Median **2.425 to 5.060**, floor `sqrt(2.425 x 5.060) = 3.5029`, margin **1.445x each way**. Two runs of the fixed build returned the eight numbers identically, so the floors are not straddling noise. **(d) THE NEGATIVE CONTROLS, which are the deliverable.** Reverting `relief()` to DW-37's single band takes the ladder to **min 0.853 / median 2.425 and both assertions go RED by name**; restoring takes it to min 2.587 / median 5.060 and both go GREEN. **(e) `mapwork.js` PASSED WITHOUT PROVING ANYTHING AND NOW CANNOT.** Its last check was `after.lumaStep >= before.lumaStep`: a frame-wide mean over 2,784 samples, asked to notice a pad that moves 4 of them. Measured on the run that passed, a level press of 315 dug and 119 filled cells plus 24 strikes carried lumaStep from 4.214 to 4.300, clearing by 0.086 on a number whose own noise is larger, and **any change of sign passes a `>=`**. A global statistic is the wrong unit for a local question. `MapContrast` now retains its per-sample tones (`lastLuma`, cleared wherever the painter reports ZERO_CONTRAST) and `__of.map('grid')` publishes them aligned sample for sample, so the probe reads the painter's OWN tones at exactly the samples whose height moved. Two more derived floors, each the geometric mean of the value at which the claim is EMPTY and the value measured: `toneMoved` empty at the frame's own mean adjacent step, `sqrt(4.214 x 28) = 10.8624`, cleared by 2.578x; `standOut` empty at 1.0, which is 'the work differs from its neighbours by as much as neighbours normally differ', `sqrt(1 x 9.07) = 3.0116`, cleared by 3.012x. `workContrast` is compared against the frame's own step rather than an absolute byte count, so the claim survives a change of palette, zoom or biome. **THE ACCEPTANCE BAR, MET AND LOOKED AT.** At a 129 m span the levelled pad and the dug trench move 52 of 2,784 samples, the worked sample stands **70 luma** from its neighbour against a natural mean step of **2.705**, a ratio of **25.88x**, and the pad reads as a bright disc inside a dark cut rim. **The ABI 13 negative control is the same work, the same site, the same zoom, and a featureless wash: 0 samples moved, 0 tone, five checks red by name.** Pictures: `docs/screenshots/WG34_{before,after}_{surface,regional,orbital}.png`, `WG34_after_mapwork_120.png` against `WG34_negctl_mapwork_120.png`, `WG34_{after,negctl}_contrast.png`. | Admin: an assertion without a negative control is not evidence, and a probe audit had just found one that passed BETTER when nothing was built. Both new assertions therefore ship with the reverted build measured beside the fixed one, and every floor carries its derivation in the file that uses it. | **Accepted** (DW-7, DW-37) | 2026-07-27 |
| WG-35 | **THE BLUE BLOBS ARE NOT WATER, AND THE BRIEF'S PREMISE WAS WRONG. THEY ARE `Mtn_SnowPatch` ICE PROPS, AND THE ROOT CAUSE IS THAT SPAWN IS IN THE MOUNTAINS BIOME.** Reid: "idk what these blue blobs are but i assume water". The brief named `NodeKind::WaterPool` and its `col_WaterPool` proxy. **`NodeKind::WaterPool` places EXACTLY ZERO nodes anywhere in the browser**: `deposits.h` has no scatterer that ever chooses it, the only two kind lists in TS are `NodeField.plan()` (14 Trees) and `PATCH_KINDS` (iron, coal, copper, rock), `NodeArt.ART` has no entry for kind 5 so `NodeField.build` would silently drop one, and `water_pool.glb` ships on disk unreachable by any loader. **ISOLATION LADDER, three frames, same seed, same camera** (standing rule 7): default draws the blob at 19,281 props; `?props=0` REMOVES IT at 0 props; `?gameplay=0` KEEPS IT at 19,281 props with the trees and ore gone. So it is a prop. `props_mountains.glb` contains exactly one blue material, `OF_Ice` at linear (0.624, 0.7913, 0.8714), carried only by `Mtn_SnowPatch_LOD0/2`, contract dims **2.60 x 0.22 x 2.10 m**. **Measured off the frame by projecting the camera: the near blob spans about 2.5 m in depth and 1.75 m across, which is that asset and not a coincidence.** `Registry.ts` scatters it at 900/km^2 in biome index 5. **The world-gen finding underneath: the spawn's own site notes are stale by 1.7 km.** `Config.ts` documents the start as "a Hills valley floor at 2,963 m" and `spawn_probe.cpp` scored it DRY and explicitly "not a lake bed"; the live site reads **biome 5 (Mountains) at 4,667.79 m**. WG-24 to WG-27 rewrote the noise stack and nobody re-ran the spawn probe, so the home pad drifted uphill into the snow line. Snow patches at 4.7 km are CORRECT world generation; the defect is where the player starts. **NOT FIXED THIS PASS, and deliberately not**: the one-line fix is in `web/src/assets/Registry.ts`, which is the live vegetation lane's table, and the root fix is moving `homeDir`, which re-baselines the ground under five live lanes and every pad probe. Escalated to Admin with both options priced. | The instruction's premise was false and its intent was not. Reporting the false premise and delivering the intent where it does not collide is what the protocol is for, and DW-29a records a lane being right to flag rather than act silently. Worth recording HOW it was found, because reading could not settle it: three isolation frames and one projection measurement, against two plausible candidates that reading had produced (the ore-patch ground skin at 0x53687d, and Ocean-biome terrain at 0x14406e, which is dry blue-tinted ground you walk on). | **Accepted** (diagnosis; fix escalated, cross-lane) | 2026-07-27 |
| WG-36 | **THE POND IS A BASIN CUT INTO `sampleDesignedHeight`, AND THE WATER LEVEL IS A SEPARATE PUBLISHED QUANTITY WITH ITS OWN BOUND (DW-26 applied to a third surface).** `BodyParams` gains `pondDir` / `pondRadiusM` / `pondDepthM` / `pondFreeboardM`; `biome.h` gains `pondBasinDropM` = `depth * (1 - smoothstep(0,1,dist/radius))`, and the old `sampleDesignedHeight` body is RENAMED `designedHeightNoPond` so the new one is `designedHeightNoPond - pondBasinDropM`. Because the basin lives in the ONE surface authority, the mesh, collision, the walker, voxel solidity, deposit snapping and build placement all see it with no special case. **Forge: pondDir a literal unit vector 55.000000 m from `homeDir` (DW-14 forbids runtime trig here and `test_water_field.cpp` pins both the unit length and the separation), 22 m basin, 4.0 m deep, 0.60 m freeboard.** Wholly inside the 150 m dead-flat pad, so the ground the basin is cut into is the pad's own bit-exact constant and the shoreline is a circle rather than a contour of the noise. **NEW HEADER `core/include/of/water_field.h`, and it is a separate file on purpose.** It publishes `levelM` (ONE scalar per body), `levelAt`, `depthAt`, `shorelineM`, `maxDepthM`, `submersionM`, `kNoWater`. There is no `surfaceHeight` in it and no water function on `surface_field.h`; a caller that wants the ground holds one, a caller that wants the water holds the other, and the separation is enforced by the type system rather than by discipline. **`levelM` reads `designedHeightNoPond`, never `sampleDesignedHeight`, and the circularity that would follow is a tested defect**: making it read the basined height puts the water 0.600 m BELOW its own floor and fails 6 tests / 8,651 checks. **`kNoWater` is a large NEGATIVE (-1e30) rather than NaN or 0**, so a caller who forgets the sentinel test reads "the water is unreachably far below you", which is the correct sense; NaN would silently take the dry branch and 0 is a legal height. **MEASURED**: pad 4667.788838 m, basin floor 4663.788838 m (cut exactly 4.000000), `levelM` 4667.188838 m, `shorelineM` **16.623148 m** solved by bisection from the profile and never stored, `maxDepthM` 3.400000 m, steepest grade **0.272727** against the analytic 1.5*d/r. **Bit-identity outside the radius is exact**: the basin term returns bitwise 0.0 at and beyond 22.0 m (last nonzero drop 21.9 m, first bitwise zero 22.0 m, 0 leaks), and `x - 0.0 == x`. The two `fieldWorldSig` memo fingerprints grew from 8 doubles to 13; a test asserts each of the three ground-affecting pond fields changes the key and that `pondFreeboardM` provably does NOT, because "forgot the field" and "excluded it deliberately" look identical in a hash. | Three answers to "where is the surface" is the exact ambiguity that cost this project the sinking deck and the sinking tunnel. DW-26's ruling is that when one authority must answer in two shapes you publish both shapes and test the bound between them; a third shape gets the same treatment or it becomes the sixth definition of the surface. Note also that the basin had to be terrain rather than decoration for a reason that is not aesthetic: a decal has no floor to stand on, no slope to wade down and no volume, so "see the bottom" and "depth increases as you wade in" are not achievable by any amount of shading. | **Accepted** (DW-26, DW-33, DW-14, standing rule 1) | 2026-07-27 |
| WG-37 | **`water_field_tests`: 17 tests, 35,619 checks, and BOTH reversions verified by name.** Registered in `core/CMakeLists.txt`; full suite **34/34 ctest**. Asserts the basin (exact cut, bitwise identity outside the radius, monotone descent on 8 bearings, a TWO-SIDED grade bound so the test fails if anyone flattens the bowl back into a decal, Cinder untouched over 1000 directions), the water level (the DW-26 bound in both directions over 4,000 directions, freeboard exact at the rim, `kNoWater` on 512 rim directions and 512 beach directions, depth strictly increasing over 4,008 samples), and **the negative control that matters: `surfaceHeight` at the pond centre equals the basin FLOOR and differs from the water level by at least 3.0 m**, so if anyone ever makes a height function return the water level, that check goes red by name. **Standing rule 11, both reversions run in a scratch copy: `pondBasinDropM` returning 0.0 always fails 7 tests / 8,590 checks; `levelM` reading `sampleDesignedHeight` fails 6 tests / 8,651 checks.** Two existing `biome_tests` cases legitimately moved and were fixed at the property rather than at the threshold: the pad-flatness test now asserts the STRONGER exact statement that the set of directions differing from `padH` is EXACTLY the pond disc and the difference there is EXACTLY `pondBasinDropM`; and the pad-blend test switched its reading to `designedHeightNoPond`, which is precisely what the rename exists for, because three of its eight rays cross the pond and it was reporting the pad blend as broken because a different feature was working. **The suite contradicted three of my own header comments and all three were mine to fix**: the shoreline comment said 16.11 m and "a 32 m pond" from an arithmetic slip in a solve the code does correctly (16.623148 m, 33.2 m); the INVARIANT block used "inside the pond" and "inside the rim" interchangeably, which makes its left inequality FALSE over the whole dry beach annulus; and `submersionM`'s justification argued something the code does not do, since it never consults the ground and therefore reports positive submersion for a point below the beach surface (which is the water-table reading, and is now stated as that). | The header comments were wrong in the direction that reads as confident. A suite that PRINTS a derived quantity instead of asserting the number a comment claims is what caught all three, which is the general lesson: pin the property, print the value. | **Accepted** (standing rule 11) | 2026-07-27 |
| WG-38 | **ABI 15 to 16, atomic across the bridge (standing rule 9).** `of_core_api.cpp` section 3b adds `of_water_no_value`, `of_water_level`, `of_water_level_at`, `of_water_depth_at`, `of_water_submersion`, `of_water_disc`. The EXPORT LIST is purely additive, but **the bump is load-bearing rather than cosmetic**: the basin changes `sampleDesignedHeight`, which is what `of_base_height` / `of_surface_height` / `of_surface_radius` / `of_solid_at` and every streamed chunk return, so an old wasm under this client would mesh a pond nothing collided with. Refusing to boot is the correct outcome. **What is deliberately NOT in the ABI: there is no `of_surface_water` and no water flag on any existing surface call**, so no caller can receive a water height while believing it asked for the ground. `of_water_no_value` hands JS the exact sentinel bits rather than letting the client transcribe -1e30. Rebuilt wasm 401,301 B, `sync-wasm` to `web/public/wasm/`, `expected.json` regenerated with **exactly one line changed (`abi` 15 to 16) and zero terrain values moved**. **That fixture is a NEGATIVE CONTROL for the pond, not a confirmation of it, and the distinction is now written down**: its eight sampled directions sit 370 km to 1,141 km from the pond, so it COULD NOT have moved, and a future pond change that does move it means the containment broke. Parity: TIER 0 self 126/0, **TIER A cross-toolchain exact 108/108**, TIER B 148/3 (the pre-existing documented `tan`/`asin` 1-ULP mingw-vs-musl delta, identical in count and identity to the ABI 15 baseline). Handshake quoted live from the running client: `[of] W0 handshake abi=16 ... worker instance agrees=true (mismatches 0)`, read from `_of_abi_version()` and not from a literal. **Found and fixed on the way: `web/wasm/test/enemyloop.mjs` pinned `=== 15` and broke the instant the shim moved, a file about enemies failing for a reason with nothing to do with enemies. It now reads the number from `expected.json`, so the pin says the true thing (the wasm on disk is the wasm the fixture came from) instead of rescheduling the same break for ABI 17.** | Standing rule 9's corollary, learned twice on 2026-07-26: the fix for a handshake refusal is never to flip the constant. The `enemyloop.mjs` case is the same lesson one level out, on a hard-coded number in a test rather than in a banner. | **Accepted** (standing rule 9) | 2026-07-27 |
| WG-39 | **`WaterOracle` is a SEPARATE CLASS from `SurfaceOracle`, with no method name in common, and that is the whole design.** `SurfaceOracle.ts`'s first line is "THE surface answer"; bolting `waterHeight` onto it would have made water the sixth definition of the surface in all but name, which is exactly what DW-26 forbids. So there is no `waterHeight` on `SurfaceOracle` and no `surfaceRadius` on `WaterOracle`. `SurfaceOracle` CONSTRUCTS its `water` sibling (that is where the module handle and body already are) and gains nothing by it: there is still no way to get a water height out of a surface call. The debug API mirrors the split, `of.surface(dir)` and `of.water(dir)` and `of.submersion(point)`, so a probe cannot read one believing it asked for the other. `WaterDisc` publishes `shorelineM` AND `basinRadiusM` as different numbers, because between them is dry beach inside the bowl and a surface drawn to the wrong one is a disc of water climbing the bank. | The property that matters is not the ownership graph, it is that the two questions cannot be spelled the same way. | **Accepted** (DW-26) | 2026-07-27 |
| WG-40 | **SWIMMING, AS ONE CONTINUOUS FUNCTION OF DEPTH WITH NO `isSwimming` THRESHOLD ANYWHERE.** New `web/src/player/Swim.ts`. Net radial acceleration is `-g * (1 - buoyancy * frac)` where `frac` is the submerged fraction of the capsule, so the float line is where `buoyancy * frac == 1`. At the shipped 1.35 that is **frac 0.7407, waterline 1.333 m up a 1.80 m capsule, leaving the 1.62 m eye 0.287 m clear**. A player who stops swimming BOBS UP TO THAT LINE from above it or below it, because it is a stable fixed point of the acceleration and not a state anything switched into: nothing has to decide when swimming starts and there is no frame where the answer flickers. **Wading is the SAME equation** below the float line, so the feet stay on the bed and the walker walks, just slower, with drag scaled by the same `frac`. Buoyancy is 1.35 and not a human's 1.02 for a stated reason: 1.02 floats with the mouth at the waterline and would put the camera 1.3 cm above the water. **One real change to the capsule tick, and it is a correction rather than a special case: a floor kills DOWNWARD motion, not upward.** Out of water the two are the same statement; in water they differ, and zeroing `vUp` on every landing tick would pin a swimmer to the bottom of the pond while the acceleration said otherwise. Held jump is SWIM UP rather than an impulse, because what it is for is climbing out at the bank. **"Am I in water" is answered in ONE place, `WaterOracle.submersionM(point)`, which takes a point and nothing else: no ground query, no capsule, no grounded flag.** `Swim.ts` never imports `SurfaceOracle`, so the code that swims has no way to name the ground. **THE DROWNING DECISION: NO, and the seam is left in and MEASURED rather than stubbed.** Three reasons, in order of weight: (1) buoyancy makes it nearly unreachable by accident, so the rule would fire on a player deliberately holding a dive, i.e. it punishes experimenting with a feature 55 m from spawn; (2) wrong genre pressure, the hazards here are logistics and a hostile planet, Factorio has no swimming and KSP lets a kerbal sit in the sea, and the nearest comparable that DOES drown you does it in an ocean you cross rather than a pond you wade into; (3) nothing to trade against it yet, no suit, nothing on the bed worth having, no deep water that is a real obstacle, so it would be cost with no decision attached, which is the test DW-30 sets. **`headUnderM` and `breathSecs` are computed and published every tick and consumed by nobody**, so reversing this is wiring a consumer to a number that is already correct and already probe-readable, not adding a system. | Reid asked for swimming and did not say whether depth drowns you. An unstated rule that kills the player is the kind of thing to decide explicitly and record, not to infer. | **Accepted** (DW-12, DW-30) | 2026-07-27 |
| WG-41 | **`Capsule.ts` and `HeightfieldWalk.slopeGate` split out of `KinematicBody.ts`, which was at 399 of its 400-line cap.** The tunables table (with the measurement behind every constant) is not part of the solver that reads it, and `KinematicBody` re-exports `CAPSULE` so all six importers are unchanged. The slope rule had its SAMPLING delegated to `HeightfieldWalk.ts` and its DECISION inline, so that file owned half a rule; `slopeGate` now owns both halves and returns the uphill `cut` as a SCALAR rather than editing a vector argument, which keeps it allocation free on the tick path. Behaviour unchanged. **Water is deliberately not a slope exemption**: a swimmer is not grounded so it never runs on one, and a wader IS standing on the bed and should be refused by a bank too steep to climb exactly as on dry land. | The cap forced the question and the answer was one that improves the file regardless. Recording it because the alternative under time pressure is to shave a comment, and the comments in `Capsule.ts` are the measurements. | **Accepted** | 2026-07-27 |
| WG-42 | **`WaterSurface.ts` is the LEAST INTERESTING THING that can honestly be called a water surface, and that is the point.** World-gen owns the water's EXTENT and DEPTH; rendering owns its LOOK. What ships is only what comes out of the world data: the disc reaches the **shoreline** and not the basin rim, so dry beach is visible inside the bowl; it is a curved shell on the sphere rather than a plane; and per-vertex colour AND ALPHA are a function of the WATER DEPTH under each vertex, read from the oracle at build time, so it is translucent at the edge where the bottom is 13 cm down and dense in the middle where it is 3.4 m down. **That gradient is what makes it read as a body of water rather than a decal, and it is world-gen data, not a look.** It spends ZERO of DW-10's five custom shaders: a stock `MeshStandardMaterial` with a vec4 colour attribute. DoubleSide, so the surface is still there when the camera goes under it. Anchored per standing rule 6: float32 metres about the pond centre, engine transform re-derived from the f64 anchor on `OriginRebased`. Built inside `bootTerrain`, which cost the boot site exactly ONE line. | The brief's warning was that a water surface shipping as a flat blue plane would read as a bigger blob than the ones being removed. The answer is not to half-shade it in this lane; it is to ship the part that is genuinely world data, spend none of the shader budget, and hand rendering a clean list. See section 5 for that list. | **Accepted** (DW-10, DW-35, standing rule 6) | 2026-07-27 |
| WG-43 | **`probes/pondwade.js`: 16 checks, all green, driven on FOUR bearings with a negative control and a dive. R8 enforced.** A pond has a CIRCULAR shoreline, so every bearing is different ground and a single hand-placed wade would be the flat-clearing mistake for the third time. The probe drives the full crossing on 4 bearings and asserts on the WORST of them. **Geometry**: basin cut **4.000000 m**, worst uphill step walking inward **0** (bitwise), worst deviation outside the basin **0** (bitwise, the leak test), waterline measured independently by bisection on the sampled field at **16.623 m on all four bearings, agreeing with /core's derived value to 4.8e-9 m**. Depth ladder from the centre: 3.400 / 3.307 / 3.051 / 2.670 / 2.198 / 1.672 / 1.128 / 0.602 / 0.130 / 0 at 0 to 18 m. **Driven, per bearing**: 1,946 to 2,009 ticks, 125 to 130 m travelled, about 880 wet ticks, **300 to 338 FLOATING ticks**, passes within **0.005 m** of the centre, lands dry and grounded 96 to 101 m out the far side. **Sank below the bed: 0.000000 m on all four.** Eye clear while floating: **0.285 m**, against the 0.287 m the buoyancy constant predicts. **YAW IS CALIBRATED, NOT ASSUMED**: the probe walks briefly at two known yaws, reads the headings back off the actual displacement, and solves for scale and offset, which costs 1.2 s and removes the whole "the probe was aimed at the wrong ground" class that cost WG-33 a pass. **Swim speed is a CONVERGENCE, not a threshold**: the peak while floating is 2.448 m/s (momentum carried in off the bank, which drag is supposed to bleed rather than clamp), and the assertion is on the mean of the last quarter of the float ticks, **2.145 to 2.148 m/s against the 2.2 cap**. Asserting the peak would fail a correct implementation and pass one that clamped and then crept back. **THE DIVE, added because the wade never once put the camera under the water and "a water surface the camera crosses correctly" would have shipped on an assumption**: dropped in from above the middle, the eye goes **1.361 m** under, `breathSecs` reaches 2.083 s, and the capsule **surfaces unaided at tick 85 with no input at all**, ending floating at frac **0.741**, which is 1/1.35 to four figures. **NEGATIVE CONTROL**: the same driven walk 300 m away on the flat pad, 401 ticks, 30.6 m travelled, **0 wet ticks, 0 float ticks, maxFrac 0**. Without it a swim state that was simply always on would pass every assertion above. Pictures: `docs/screenshots/WG35_blue_yaw0.png` and `WG35_iso_{noprops,nogameplay}.png` before, `WG36_after_{shore,wide,swimming,aerial}.png` after. | The two checks that were RED on the first run were both the probe's fault and both were fixed at the property rather than the threshold: an 18 s drive was not long enough to cross and exit (so "got out" was measuring a player still standing in the shallows), and a peak-speed threshold was reading momentum as a cap failure. | **Accepted** (R8, DW-20, standing rule 11) | 2026-07-27 |

| WG-50 | **THE PER-CHUNK NOISE PHASE IS THE RIGHT SHAPE, AND IT IS NOT A CHUNK-FORMAT CHANGE. Confirmed on my own arithmetic, refuted on ownership and on cost.** Rendering's diagnosis is correct in every particular I could check. `pM` is formed at `TerrainShader.ts:185` as `(vWorld - uBodyCenter) * uMetresPerUnit`, and `uBodyCenter` is `-origin` uploaded as a GL `vec3`, so it arrives quantised at planet scale and `pM` inherits that quantum whatever `vWorld` does. **My measurement of the quantum is 0.03125 m, not 0.0625 m, and rendering's figure is the worst case rather than the typical one**: 0.0625 m is right for a component in [2^19, 2^20), but a player at a generic point on a 600 km sphere has components near 3e5 to 4e5, which sit in [2^18, 2^19) and quantise at 2^-5. The conclusion is unchanged and the ratio still bites: at the 1280x720 RN-45 camera the ground pixel is 3.56 mm at 2 m and 22.27 mm at 5 m, so the quantum is **8.77x the pixel footprint at 2 m, 3.90x at 3 m, 1.40x at 5 m and 0.55x at 8 m**, which is why the artefact is a near-field band that dies out around 6 m to 8 m rather than fading smoothly. **WHAT I REFUTE: "that is a chunk-format change, which is why it is yours."** It touches no byte of the 28 B/vertex packed layout, no `of_packed_*` export, no ABI and no parity fixture. The quantity is a pure function of `ChunkView.anchor`, which is already float64 on the main thread end to end (`ChunkView.ts:21,70` from `TerrainProtocol.ts:91-92` from `HEAPF64` from `centerUniverse.pos`, which is C++ `double`), the only narrowing happening later and only for the engine transform. **And there is a shipped precedent for exactly this shape that is not in the wire format either: `aFadeT0`** (`ChunkBatch.ts:50,122-129`), a per-chunk float allocated JS-side and `fill()`ed across the slot's vertex window, with no worker change and no `/core` change. **The refusal matters because the requested shape is the ONE version of this that endangers standing rule 4.** Putting the phase in the chunk format means changing `of_packed_*`, bumping the ABI under standing rule 9, moving the parity bytes and re-baselining 108/108 cross-toolchain. In the shape I am publishing, determinism is not protected by care, it is unreachable: the phase reads an anchor generation has already produced and produces a shading quantity generation never reads back. Terrain height, biome, solidity, deposits and the save are untouched by construction. | The brief asked me to satisfy myself on three things and all three came back clean, but only one of them the way I expected. The phase reduction IS exact (`cell + frac === anchor / L` bit-for-bit in float64 on every axis and octave). Determinism DOES survive, and the reason is stronger than "we were careful": the quantity is on the wrong side of the generation boundary to affect it. Chunk boundaries do NOT become visible, and that is the one I had to measure rather than argue. | **Accepted** | 2026-07-28 |
| WG-51 | **THE DEFECT REPRODUCED FROM ARITHMETIC ALONE, WITH NO SHADER, NO GPU AND NO THREE.JS: `web/tools/precision/artphase.ts`.** RN-45 was caught by a screenshot after every metric passed, so before spending anything on the cure I reproduced the disease independently rather than restating rendering's measurement. The tool renders one field three ways at the RN-45 camera (1280x720, fov 60, eye 1.8 m), taking screen derivatives on **2x2 quads exactly as hardware takes them**, which is the mechanism that turns a quantised position into runs of identical pixels. **It reproduces: `WG51_artphase_defect.png` has the concentric arcs centred on the eye and the quantisation lattice, and `WG51_artphase_{truth,fix}.png` are smooth and visually identical to each other.** **THE NUMBER RN-45 DID NOT HAVE: the fraction of quads whose screen derivative is EXACTLY 0.0 in both axes. truth 0.00%, defect 32.49% of 212,480 quads, fix 0.00%.** A histogram cannot see that and a band mean cannot see that; it is one cheap integer and it separates the defect from truth by two orders of magnitude. Field agreement against the ideal, which is the half the quad count cannot tell you (a mode can have a live derivative of the WRONG field): **defect max 1.341e-2 / mean 7.603e-4, fix max 2.041e-6 / mean 2.686e-7, a factor of 6,570 on the max.** **I GOT THE INSTRUMENT WRONG TWICE FIRST AND BOTH ARE RECORDED IN THE TOOL, because both are this project's own recurring failure.** (a) The first version put the ground patch at z = R with x and y small, and **the defect did not reproduce at all**: 0.00% dead quads, identical to truth. In that orientation two of three components of `pM` are small and therefore exact, so the quantisation had nowhere to land. A control that accidentally removes the thing it controls for reports success. Fixed by standing at a generic direction on the sphere, which is where a player is everywhere except on three great circles. (b) The second version reported truth and defect agreeing on **69,033 of 212,480 quads, to the digit**, because the truth path was reading the already-quantised position. Two numbers agreeing to the last digit is a wiring diagnosis, never a coincidence, and RN-45 had just published that same lesson about its own noise floor. (c) A third: truth ran the hash in float64 while the others ran it in float32, and truth and fix then drew visibly DIFFERENT terrain. That was not a precision result, it was a control changing two things at once: `hash13` is a chain of `fract` and is chaotic across precisions, so float64 gives an unrelated value for the same cell rather than a nearby one. All three modes now evaluate the identical noise function and differ ONLY in how the lattice coordinate is obtained, which is the one variable under test. | The shading gain is a declared contrast stretch applied identically to all modes and it is labelled in the tool, because at the shipped bump strength the effect is a few counts, which is exactly why RN-45's band statistics could not see it. A picture honest about amplitude would be a picture of nothing; a picture that hides its stretch is worse. | **Accepted** (DW-7, DW-20, standing rule 11) | 2026-07-28 |
| WG-52 | **THE SEAM PROOFS, THE LOD PROOF AND THE HEADROOM BOUND, AS ASSERTIONS THAT FAIL BY NAME: `web/tools/precision/phasecheck.ts`, 57/57.** Standing rule 11's corollary is that a probe reporting a number without asserting anything is a log line. Everything below is checked against **one bar, the 3.56 mm ground pixel at 2 m**, because an error under a pixel cannot become a visible artefact and any other threshold would be tuned. **Exactness**: `cell + frac === anchor / L` exactly in float64 on all 12 axis-octave pairs; `frac` in [0,1); `cell` exact in float32 with 189x to 10,131x of headroom under 2^24. **Reconstruction error over a whole depth-14 chunk: 3.3 to 12.8 micrometres, i.e. 1/279 to 1/1065 of a pixel.** **The positive control, on the same instrument: today's `pM` arithmetic reads 45.19 mm, i.e. 12.7 pixels**, so the test that passes the fix fails the shipped code by name rather than merely being satisfied by it. **Seam, neighbouring chunks at depth 14 over 2,048 samples straddling the shared plane: 2.8 to 11.1 micrometres.** **LOD cross-dissolve (DW-23 draws the outgoing parent and the incoming children in the SAME frame, so different anchors at different depths over the same ground are on screen together, and a per-chunk scheme that is merely self-consistent would shimmer): 8.0 to 11.3 micrometres.** **AND THE CONTROL THAT EXCUSED A CASE IT NEVER MET, caught in my own suite**: sections 3 and 4 both reported **0 cell-index splits over 8,192 samples**, so my claim that a split is harmless because the interpolant is C1 was reasoning and not measurement. Section 4b now lands samples on lattice planes to the ULP on purpose, **provokes 154 real splits, and asserts the reconstructed coordinate still agrees: worst 177.383 micrometres, 1/20 of a pixel.** | The bound is stated in metres of ground rather than in lattice cells throughout, because the only comparison that decides whether a screen derivative survives is against a pixel footprint, and a number in cells invites a threshold argument. | **Accepted** (standing rule 11) | 2026-07-28 |
| WG-53 | **EVERY OCTAVE THAT FEEDS THE DERIVATIVE MUST BE REDUCED, AND MY OWN HEADLINE STATISTIC FAILS TO SAY SO.** The payload size turns on whether the finest octave alone is enough, and the fine octave carries roughly 5.7x the gradient of the next one down, so it was a fair question. A fourth mode, `finefix`, reduces only the 4.2 m octave and leaves the macro ladder on `pM`. **It scores 0.00% dead quads, identical to truth and to the full fix**, because the fine octave's live derivative keeps the SUM off zero everywhere. **And `WG51_artphase_finefix.png` still has the arcs and the lattice plainly in it**, with field agreement 2.498e-3 against the full fix's 2.041e-6, a factor of **1,224**. So the dead-quad count I am handing rendering is **necessary and not sufficient**, the field agreement is the number that separates a partial fix from a real one, and the picture remains the arbiter. This is RN-45's lesson arriving from the other direction: it is not that numbers cannot see this defect, it is that each number sees only part of it, so a probe wanting to police it needs the pair. **Consequence, which is the deliverable: 6 floats per octave per chunk, 4 octaves, 24 floats.** | Recorded as a decision rather than a note because the tempting cheap version is the one that passes the test. | **Accepted** | 2026-07-28 |
| WG-54 | **THE DELIVERY MECHANISM IS RENDERING'S TO PICK, AND ONE OF THE TWO CANDIDATES IS DISQUALIFIED BY ARITHMETIC.** Terrain chunks are not meshes: the whole resident set is TWO `BatchedMesh`es (`ChunkBatch.ts:59`, `ChunkGeometryPool.ts:30-31,48-49`) sharing one `ShaderMaterial` whose own header states that per-chunk state is zero (`TerrainMaterial.ts:1-8`), with no `onBeforeRender` anywhere in the terrain path. **So a per-chunk uniform is impossible without giving back DW-11's draw-call collapse**, and that is worth saying plainly because "just pass a uniform" is the obvious suggestion. Two mechanisms remain and they are not equivalent. **(a) A vertex attribute `fill()`ed per slot, the `aFadeT0` pattern: 24 floats x 1,217 verts x about 320 near slots = roughly 37 MB, against a measured 93.7 MB frame VRAM. That is a 40% increase for six numbers a chunk and it is refused.** **(b) A `DataTexture` indexed by three's own batching id, the `MachineBatch.ts:131` pattern (`getIndirectIndex(gl_DrawID)`), already proven on r185 in this codebase: 24 floats x 320 slots = about 31 KB.** Three orders of magnitude apart, same information. The terrain vertex shader already includes `<batching_pars_vertex>` and `<batching_vertex>` (`TerrainShader.ts:30,46`), so the id is available there for free. **World-gen supplies the numbers, rendering owns the channel.** Re-stamp points: wherever the anchor changes, which is `ChunkView.refresh()` and the near/far slot swap, and NOT the rebase path, since the anchor is invariant under a rebase, which is the whole reason the field was keyed on `pM` in the first place. | I am naming (a) as refused rather than leaving both open, because a 40% VRAM increase discovered after the shader work is done is a wasted lane, and the arithmetic to rule it out is one multiplication. | **Accepted** (DW-11, DW-28) | 2026-07-28 |
| WG-55 | **THE SPAWN IS PREPARED AND NOT MOVED, and the survey found two defects in the instrument before it found a site.** Admin's item 2, deliberately stopping at a shortlist. **(1) `spawn_probe.cpp` COULD NEVER HAVE PRODUCED THE CURRENT SITE**: it hard-filters to Plains, Forest and Hills (`spawn_probe.cpp:134-136,425`), and the spawn is Mountains. It was never a probe output. Re-run at lat 2 / lon 144 it ranks the site **last of 21, score -85.84 against the winner's +32.81**, with 23.88% slope at 20 m (20x to 30x every candidate) and a 0.976 m worst step under a 4 m DW-32 module. It is also not the valley floor `Config.ts` claims: it sits **+130.7 m ABOVE the 6 km mean**, which is a tabletop, the exact failure the probe's own header warns against. **(2) `probePadHere` PREDATES WG-36's POND AND ITS PAD VERDICT IS WRONG BY 1.08 m.** It moves `homeDir` to the candidate (`spawn_probe.cpp:487-491`) and never moves `pondDir` or zeroes `pondRadiusM`, then measures with `sampleDesignedHeight`, which since WG-36 subtracts the basin. Its reported "worst 4 m step inside the flat radius 1.078601 m" at the current site is **entirely my pond**: inside the flat radius `designedHeightNoPond` is a bit-exact constant, so the basin is the only varying term, and the analytic value for a 4 m span across the basin's steepest point is 1.080 m. Candidate rows read 0.000000 m only because the pond is thousands of km away from them. Related: `printHomeDirLiteral` emits `homeDir` alone, so acting on its output as printed would orphan the pond and break `test_water_field.cpp:138-148`. **(3) THERE IS NO DESERT BIOME.** The set is Ocean / Beach / Plains / Forest / Hills / Mountains / Polar (`biome.h:53-67`). What Reid liked is partly the post grade (`PostConfig.ts:231`, `CompositeGlsl.ts:86-89`) and partly **Beach**, `0xc8b48a`, which with Mountains is one of only two biomes taking DRY_DETAIL props rather than grass (`Registry.ts:125-133`). Beach is invisible to the probe because `minAlt = 200.0` (`spawn_probe.cpp:544`) cuts the whole band. **(4) THE SNOW LINE IS THE MOUNTAINS BIOME BOUNDARY, not an elevation**: `Mtn_SnowPatch` is keyed on biome index with no elevation gate (`Registry.ts:143-144`), so the prop snow line is `kMountainsRel` 0.330 x 6000 m of raw relief, about 2,700 m designed. The shader's own snowfield is a separate thing at 5,160 m to 6,840 m (`TerrainShader.ts:263-265`, duplicated in `BiomePalette.ts:54-55`). The current spawn is **1,239 m above the prop line and 492 m below the shader one**, which is precisely why WG-35's blobs were props and not shader snow. **Shortlist in section 6.1. `homeDir` moves under 18 call sites** and I have the list; two ctests pin the current literal by name and will fail on any move, which is correct and is the reason this waits for a quiet checkout. | Not moved, as briefed. Reid picks the biome. | **Accepted** | 2026-07-28 |
| WG-56 | **THERE IS ALREADY A WELL-CONDITIONED LOCAL COORDINATE ON THE GPU, IT IS PAID FOR, AND THE TERRAIN SHADER USES IT ZERO TIMES.** Admin asked me to say plainly if the right answer were a different precision split or "not worth the chunk-format churn", so here is the third option, found while confirming the second. **Every chunk already carries a per-quad normalised UV**: `of_core_api.cpp:1445` documents it as `uv = v/65535, over the quad`, it is in the wire format's five sections (`ChunkFormat.ts`), it is in the batch's attribute list (`ChunkBatch.ts:40,47`) and it is written per slot (`:111`). **`TerrainShader.ts` contains the string `uv` exactly 0 times.** So it is uploaded, held in VRAM and interpolated every frame for nothing. **And it is well conditioned where `pM` is not, by construction, because it is local**: at a depth-14 chunk of 57.856 m one uint16 step is **0.883 mm, which is 0.248 of the 3.56 mm ground pixel at 2 m** (0.496 at depth 13), against `pM`'s 8.77 PIXELS. A tiling detail normal or albedo map sampled on `uv * N` therefore has a clean screen derivative with **no phase, no new attribute, no DataTexture, no chunk-format change and no CPU work at all**, and it is the ordinary way every other engine does this. Seams work out: `N = chunkEdge / tileM` is an integer at every depth if the base is chosen once, so neighbours at one LOD meet at a tile boundary. **The honest cost, and it is a real one: the detail's WORLD size doubles at each LOD step, because the UV is per quad rather than per metre.** That is tolerable here specifically because the bump is only well conditioned within about 20 m to 45 m of the eye by RN-45's own bound, and inside that band the player is at max depth, so the step happens outside the band. It also tiles, which is what the shipped macro colour field is already good at breaking up. **I am NOT recommending this INSTEAD of the phase, and the distinction matters.** The phase is the general fix: it repairs `pM` for the strata warp and for any future field that takes a derivative, it costs about 31 KB and 24 CPU ops a chunk, and it is now proven and asserted. The UV route is narrower, cheaper still, and may make the DETAIL BUMP SPECIFICALLY unnecessary. **Rendering should measure both before writing either**, and the reason I can hand them that choice at all is that neither is a chunk-format change. | Recorded because "the channel you need is already on the GPU and nothing reads it" is exactly the kind of fact that gets rediscovered expensively two lanes later. Note also the smell it represents: an attribute uploaded every frame that no shader samples is either a missing feature or dead weight, and nobody had asked which. **UNMEASURED, and flagged as such**: I did not build it, and it carries a DW-5 asset question the art lane owns. | **Proposed** | 2026-07-28 |
| WG-57 | **THE POND NOW MOVES WITH THE PAD, AND THE PAD METRIC MEASURES THE PAD AGAIN. `spawn_probe.cpp`, and it is the fix that makes the eventual spawn move SAFE rather than merely tidy.** Two defects, one cause: the probe had no concept of the pond at all, the word did not appear in the file. **(1) `probePadHere` moved `homeDir` to a candidate and left `pondDir` and `pondRadiusM` where they were**, then measured with `sampleDesignedHeight`, which since WG-36 subtracts the basin. So it charged my own pond to the candidate's terrain. **The proof is that the number is a CONSTANT: 1.078601 m now reads identically at sites of 436 m, 293 m, 1,643 m and 4,667 m elevation on wildly different ground, and a constant cannot be a property of terrain.** It matches the analytic 4 m span across the basin's steepest point, 1.080 m. It previously read 1.078601 m at the incumbent and 0.000000 m at every candidate purely because the pond was thousands of km from them, which is exactly why it looked like a site property. **Both numbers are now measured and BOTH printed** (DW-26: when one authority must answer in two shapes, publish both shapes): `worst 4 m step, pad alone` reads **0.000000 m at every site**, which is what the pad is FOR and was unreadable under a 4 m bowl, and `with the pond cut in` reads 1.078601 m, with the difference named as the basin. **(2) `printHomeDirLiteral` emitted `homeDir` ALONE**, so pasting its output moved the pad and left the pond at the old spawn, still referencing a water level for a pad that was no longer there, in whatever biome sits 55 m from a site nobody chose. It emits both literals now, with the separation printed against the tolerance the test pins. **THE CONSTRUCTION WAS IDENTIFIED BY BIT-IDENTITY, NOT BY TOLERANCE, AND THAT DISTINCTION IS THE WHOLE POINT.** `pondDir` is `homeDir` rotated 55 m along a heading 30 deg east of north, and I tried the obvious `offsetDir` tangent-offset-then-normalise first: it lands **154 nm** from the shipped literal with the separation at 54.999999827 m. A TRUE GREAT-CIRCLE ROTATION reproduces the shipped `pondDir` **BIT-FOR-BIT on all three components** at 54.999999981 m. **Both pass `test_water_field.cpp`'s `|sep - 55| < 1e-6`, so a tolerance check would have accepted the wrong one and quietly moved the pond 154 nm every time anyone regenerated the literal.** `pondDirSelfCheck` now reconstructs the shipped `pondDir` from the shipped `homeDir` on every run, prints BIT-IDENTICAL or a loud MISMATCH, and sets the exit code. The round trip is complete: run over the current spawn the tool re-emits both shipped literals byte for byte. | The candidates' honest defect is now visible too, and it is sharper than the score: at the current spawn **the pad ADDS 18.00 percentage points of grade** over the natural ground at a 59.70% blend annulus, while all three sampled candidates read NEGATIVE (-1.95, -0.12, -0.83), meaning the pad smooths ground that was already gentle. The incumbent is the only site where the pad makes the terrain worse, which is the cut-shelf failure the function was written to detect and could not report while the basin was drowning its other number. | **Accepted** | 2026-07-28 |
| WG-116 | **THERE ARE NO SCENERY TREES: every tree in the world is a streamed harvest node, and the canopy tier is RETIRED TO A DEFAULT rather than deleted** | Reid's ruling, and the retirement's shape is the measurable half: `?canopy=620` reproduces the WG-59 world in the SAME binary, so what the ruling cost and what it bought are one query parameter apart instead of two builds with two streamed chunk sets and two shadow states. Deleting the specs would have forfeited that, and it also belongs to the sibling lane's `Registry.ts` | **Accepted** | 2026-08-01 |
| WG-117 | **The density table is the retired tier's OWN asks, copied row for row, not re-derived** | Derived numbers were tried first and were wrong in a way only the picture caught. The canopy runs at FULL density inside 300 m and thins only beyond, so matching its ring AVERAGE (1,292/km2 at Forest) puts the near field 1.6x too thin, and the near field is what the eye reads. Copying the asks preserves the near field by construction and makes the change honest in one direction: the far field is denser, because the distance thinning is gone. Delivery read 0.9891 through the whole error (DW-7) | **Accepted** | 2026-08-01 |
| WG-118 | **The node batch loads `_LOD0`, `_LOD1` and `_LOD2` and culls per instance; the tier is a second axis on the geometry id the depletion variant already switches** | THE CRUX OF THE PASS, and an asset already paid for: every node .glb ships three tiers and `NodeBatch` matched `_LOD0` only, so a 791-triangle tree drew its near geometry at 600 m while a 16-triangle impostor sat unused in the same file. 2,226 tree nodes cost 7,558,472 triangles without it and 1,365,739 with it and per-instance culling (-81.9%). The tier keys on `distance / scale`, not distance, because WG-119 makes a tree's size mean something and one absolute distance would either pop the big ones or pay LOD0 for the small ones. `geomAt` walks DOWN to a present tier, so the three assets that author only LOD0 and LOD1 behave exactly as before rather than vanishing at 165 m | **Accepted** | 2026-08-01 |
| WG-119 | **A tree's SIZE is a linear map of its /core grade, so size IS yield**, and the band is derived from the shipped bytes | It is the answer to the question the ruling creates, which is why you would chop the big one; the outcrops have had exactly this rule since deposits.h. The band was derived from a docstring first ("a 6.5 m tree") and the measured conifer is 6.50 m while the BROADLEAF IS 5.00 m, against canopy species at 10.50 to 16.50 m, so the median tree shipped a third too short and the counters could not see it. WG-14's rule arriving from the other side: measure the asset, never the comment about it | **Accepted** | 2026-08-01 |
| WG-120 | **A streamed-out cell drops its UNTOUCHED trees from `known`**, and only a chopped one is kept for ever | `RockField` keeps everything it ever materialised, which is right at 49 rocks over a 5 km walk and unbounded at 2,226 trees per ring: the map and every autosave would grow with the walk. A full tree carries no diff and regenerates identically from its cell, so forgetting it loses nothing. Measured over the eight-site tour, `known` equals `live` at every site while `forgotten` reaches 3,465. **Owed back to RockField** | **Accepted** | 2026-08-01 |
| WG-121 | **The tree slope gate is KEPT although it is proved unable to fire on the designed surface**, and the census is written into the constant | An inert gate is a comment, and WG-63 had already read 0 refusals from the canopy's copy of it at seven sites without being able to say which kind of zero it was. A 26,120-sample hill-climb finer than the gate's own arm settles it: the steepest ground on Forge is 57.92 degrees and the steepest BELOW THE TREELINE is 29.15, against a 43.95 degree gate, so everything steep is bare because it is high. It is kept for the one case the census cannot reach, that the slope is sampled through `editsHandle` and a dug pit or levelled terrace makes steep ground under a live ring; that case is UNTESTED and owed. Lowering the constant until the counter moved was rejected: it would refuse trees on ground that is fine for trees, to flatter an instrument | **Accepted** | 2026-08-01 |
| WG-122 | **The tree ring has a HARD EDGE and no distance fade, and the edge is made ragged rather than soft** | The canopy tier faded density from 300 m out, which hid its boundary at the price of making a tree's EXISTENCE a function of where the player stood when its chunk was built. A harvest node cannot pay that: it is a thing you can chop, so it has to be there before you decide to walk to it, and the same ground must give the same trees on every approach. The edge is displaced by a smooth world-space field instead (`TREE_EDGE_WANDER_M` 70 m), which changes the geometry of the pop and not the fact of it. **A true horizon treeline still needs the far-field impostor layer WG-63 asked rendering for** | **Accepted** | 2026-08-01 |
| WG-13 | **Scenery trees are a VISUALLY DISTINCT SPECIES from harvest nodes**, not harvestable scenery and not kept beyond reach | The refusal in `build_props_forest.py:9-12` was right about the risk and wrong about the remedy. The game already announces harvestability twice BEFORE the swing (accent crosshair inside 4.0 m, `GameHud.ts:186-187`; a prompt panel naming the node and its percentage, `GameHud.ts:210-214`), and `NodeField.pick` is ray-versus-node-sphere only, so a scenery tree is invisible to the interact key by construction. The distinction is therefore learnable before the walk, not after a failed one. The project also already encodes not-harvestable in a silhouette on purpose (`Forest_DeadTree`). The two rejected options: making scatter trees harvestable crosses into factory-sim's harvest sim and needs Admin arbitration for a benefit the crosshair already delivers; keeping scenery beyond interaction range is a painted backdrop and forecloses walking into a forest, which is the entire deliverable | **Accepted** (WG-59) | 2026-07-28 |
| WG-14 | **The rule is carried by GEOMETRY and verified on the exported bytes**: harvest trees are small and branched to the ground, canopy trees are twice the height or more with a bare trunk for the lower half | A rule stated in a docstring drifts from the asset the first time either moves. Measured off the glb rather than off the source (`Parts.fit` rescales z afterwards), the lowest foliage vertex of any non-Bark primitive is at 0.550 / 0.621 / 0.632 of height for pine / fir / broadleaf, and LOD0 and LOD2 agree within 0.004 of height, so the LOD switch cannot change the read at the distance the rule is meant to work at | **Accepted** (WG-60) | 2026-07-28 |
| WG-15 | **The treeline is an ALTITUDE, not a biome id**, and Mountains carries a real canopy density so that being bare is a consequence of the altitude term rather than a second statement of it | Trees stopping at a classifier threshold is a hard line at exactly the place a real treeline never is, and the two are not even the same quantity: `biomeAt` runs on RAW relief while altitude comes from the designed height, and the survey has Hills at 861 m and at 2,077 m. Stating bareness twice is the DW-26 failure in miniature, and it was also an instrument failure: with no Mountains density, `canopyBareCells` read 0 at all seven sites because the term's refusing case was unreachable | **Accepted** (WG-61) | 2026-07-28 |
| WG-16 | **The stand field is sampled in BODY-FRAME METRES and is SMOOTH**, not a chunk-local tile hash like `ScatterLook.CLUSTER_SHIFT` | Two reasons, one visual and one numeric. `CLUSTER_SHIFT`'s lattice is chunk-local and its own comment records the restart at chunk boundaries as accepted; at 170 m that restart is another patch edge, at 620 m the chunk IS the feature size and it would tile visibly. And a cell's world position is a float64 anchor plus a float32 chunk-local offset, so the same ground arriving at two LOD depths differs in the last bits: through a smooth field that is about 1e-9 of weight, through a hashed tile boundary it is a coin flip and the stands would reshuffle every time a chunk changed depth. Same class as WG-51, and the cheap answer is to make the function continuous | **Accepted** (WG-62) | 2026-07-28 |
| WG-17 | **`CANOPY_LOD2_M` is pinned EQUAL to `DETAIL_RADIUS_M` (78 m)**, and the canopy adds no rebuild band | Measured, not chosen. A prop batch has `frustumCulled` and `perObjectFrustumCulled` both false and casts into three cascades, so every tree is drawn four times: at a 105 m LOD0 radius the canopy cost 112 triangles per tree against an authored LOD2 of 26 to 30, and 69 near trees were 85,600 of the 186,109 total. `Scatter.write` also picks LOD once at build time, so a switch distance that is not a rebuild boundary is quantised to one anyway; putting it exactly on the understorey boundary makes the quantisation exact and buys back the fourth rebuild of every chunk. **The ask that would let it go back out is a third LOD tier**, which is `PropPart`'s two geometry ids and therefore rendering's | **Accepted** (WG-63) | 2026-07-28 |
| WG-18 | **Anything that caches a chunk's ENGINE position re-derives it from `TerrainStream.afterRebase`, not from a second `OriginRebased` subscription** | A subscriber that re-composes transforms from `ChunkView.pos` is correct only if it runs AFTER the streamer's own re-place loop, and `Events` delivers to a `Set` in insertion order, so a second subscription makes correctness a function of which file `Boot` constructs first. That is a dependency nobody would preserve across a refactor. The hook makes "after the views are re-placed" a property of the code. The defect it repairs (`Scatter.replace` documented as THE rebase path and never called, measured at 4,000.089191 m of displacement across 43 of 43 chunks) was itself an ordering-free omission that survived for months | **Accepted** (WG-65) | 2026-07-28 |

| WG-141 | **The moon height field is a NINE-RUNG CRATER LADDER, not a single crater layer** (`cubed_sphere.h::sampleHeightFieldMoon`). Frequencies 9 / 27 / 81 / 243 full-neighbourhood, then 270 / 810 / 2430 / 7290 / 21870 confined; amplitudes `14800/f` and `6080/f`. Plus a 4-octave base, a mare mask gated into the basins, and an 833-frequency regolith roll. | The old stack was the pre-WG-25 original: finest content 1,111 m, so nothing on the moon was smaller than a kilometre and a player could not walk into a crater. Craters are scale-free in two laws at once (population ~D^-2, depth/diameter ~0.12); tripling frequency while thirding amplitude reproduces BOTH in one construction, so the ladder is self-similar by design and every rung has the same wall grade. Amplitudes are derived from the ratio, not tuned. **Deliberately NOT LOD-gated**, so the field stays a pure function of (seed, dir) and both bitwise crack-free proofs survive untouched. Cost 2.21x (492,507 vs 1,086,656 verts/sec), which the confined variant is what makes affordable. | **Accepted** | 2026-08-02 |
| WG-142 | **`craterField`'s 3x3x3 neighbourhood was insufficient for its own radius; the radius is capped at 0.625 so reach is exactly 1.0 cell.** | A crater reaches `cr * kCraterReach` cells from a centre jittered anywhere in its cell, so the old 0.75 maximum reached 1.2 cells and a crater two cells away could be clipped part way down its rim. Not theoretical: **346 misses in 160,000 samples, worst clipped profile 0.1906, a 470 m step out of nothing.** 3x3x3 is sufficient iff reach <= 1.0, which the cap makes exact. Moves moon bits deliberately. | **Accepted** (defect fix) | 2026-08-02 |
| WG-143 | **The crater profile is continuous: `craterProfile(t)` is one shared function for both crater fields.** Bowl `-1 + 1.5t^2` to a crest of +0.5 at t == 1, then a SQUARED fade to zero slope and zero value at t == 1.6. | The spike profile was `-(1-t*t)` inside and `(1-rim)*0.5` outside: 0 approaching t == 1 from below, 0.5 AT t == 1. **A step of half the layer amplitude at every crater rim on every body with craters**, which on the old moon was a 1,400 m vertical wall and is why a 2 m-spacing sample on a crater floor could report a 1,589 m step. Sharing one function means the confined variant cannot drift from the full one. The squared outer fade removes a circular crease a linear fade leaves in the derivative. | **Accepted** (defect fix) | 2026-08-02 |
| WG-144 | **The moon's design layer uses a CONTINUOUS gain, `designedGainForMoonRelief(rel)`; `designedReliefFactor` is retired as a shaping input on both bodies.** | WG-25 fixed exactly this on the planet and the moon was never revisited, which makes it the THIRD thing about Cinder in that category. Switching gain on the discrete biome put a **120 m wall at the 1.00->1.15 highland threshold and a 40 m one at the 1.00->1.10 crater-floor threshold**, following contour lines around the body and showing up as gaps in the biome histogram no sample could land in. Measured after: worst step at a biome crossing 173.8 m against the retired form's 287.5 m. The BIOME stays discrete, because it is a material id and should be; only the height is made continuous. | **Accepted** (defect fix) | 2026-08-02 |
| WG-145 | **`makeCinder.maxReliefM` 4000 -> 5000, and the moon biome bands re-tuned to +/-0.070.** | The two are ONE decision and separating them is how I got it wrong first. `maxReliefM` is both the declared relief bound AND the denominator `biomeAtMoonH` expresses its bands in, so raising it for headroom widened every band at once and the split went to Regolith 89.9% / MoonHighland 2.0% / CraterFloor 8.1%. A biome carrying 2% of a body will essentially never be drawn. 5000 covers the worst of five seeds (-4565 to +3985) with margin while keeping the bands tight; the split is now **53.1% / 31.7% / 15.2%**. `moon_biomes_are_all_populated` and `moon_relief_bound_holds_across_seeds` pin both halves so the next person to move one has to look at the other. | **Accepted** | 2026-08-02 |
| WG-146 | **CURVATURE, not slope, is the instrument for "does this terrain have features".** | This nearly gave me a confident wrong answer in both directions. A crater wall is a continuous 24% to 48% grade at EVERY sampling scale, so the old moon reports a perfectly healthy 0.554 m median step over a 4 m baseline and the rewrite reads as a **1.3x tweak**. It was never a plane; it was tilted without being featured. The second difference `h(a-d) - 2h(a) + h(a+d)` is zero for a straight slope of any steepness and of order its own depth for a feature of size d: on that instrument the same two fields separate by **33x at 4 m and 18x at 40 m**. The MEDIAN is the gate, not the mean, because curvature over a crater field is heavy-tailed and a mean would pass on a handful of rims. | **Accepted** (method) | 2026-08-02 |
| WG-147 | **Body plurality in the client: one `BodyId`, one `createBodyHandle`, and `?body=cinder`.** `PlanetBody.ts` owns the rule; `TerrainInitMsg` and `OracleProbeRequest` each carry a `bodyId`; the two workers create their own handle from it. **No ABI change.** | The client assumed one body in exactly three places and every other call site already threaded a handle, so this is plurality rather than un-hardcoding. Putting the rule in `PlanetBody.ts` keeps it next to the file that already calls itself "the one body-constant reader (D-006)", and after the change `_of_body_create_*` appears at exactly two call sites in `web/src`. Workers build their own handle because heaps are never shared (DW-16). **Taking no ABI number was deliberate: physics held 22 uncommitted throughout.** | **Accepted** | 2026-08-02 |
| WG-149 | **The cost meter counts crater cell evaluations as a SECOND unit, and refuses to present the valueNoise figure as the cost of a cratered body** (`OF_COUNT_CRATER_CELL`, `craterCells()`, `terrain_probe` §5). Closes R18. | The crater fields hash lattice cells directly and never call `valueNoise`, so `noiseCalls()` was blind to the dominant term. Measured on Cinder: **10 valueNoise calls against 113 crater cell evaluations per sample**, i.e. the meter was reporting about 8% of the work while looking authoritative. Admin's ruling: a cost meter blind to the most expensive thing on the body is worse than no meter. The two counts are deliberately **not summed**, because a cell evaluation and a valueNoise call are different units and the conversion is a measured property rather than a constant to hard-code; instead the probe prints both and, when the crater count is non-zero, states plainly that verts/sec is the cost authority for that body. Forge reports 0 crater cells and no note, so the refusal is body-specific rather than a blanket disclaimer. Still compiled entirely out unless `OF_NOISE_COUNT=1`. | **Accepted** | 2026-08-02 |
| WG-148 | **The airless look is ROUTED TO RENDERING, not built here, and it is not a polish item.** | Stated rather than left to be discovered, per the brief. The evidence is a controlled pair at one camera: with `forgeAtmosphere()` the moon from 12 km is a white fog bank with no visible craters; with `--atmos=0` it is a black sky, stars, a hard terminator and four scales of overlapping crater. Aerial perspective was erasing the entire rewrite. Detail and the second finding (ambient collapse) in §5. | **Accepted** (routed) | 2026-08-02 |

## 4. Architecture & approach
- **Terrain:** noise stack (continents → mountains → detail) evaluated per cubed-sphere quad at the LOD needed by camera distance; quads stream in/out. Everything derives from `(bodySeed, faceId, quadPath)` → fully regenerable, so the natural world costs ~zero to save.
- **Deformation:** when a player digs, instantiate a voxel/SDF patch seeded from the heightfield at that location; store the *delta* only. Rendering & physics query "is there a patch here?" before falling back to heightfield. (v1 may stub this; keep the seam in the API.)
- **Deposits/POIs:** a deterministic placement pass over each body (Poisson-disk / hashed-grid by seed) yields node positions + types; gameplay binds content. Depletion/looted flags are diffs.
- **Floating-origin aware:** all queries accept/return data in core-engine's coordinate/frame terms; chunk origins are 64-bit, local meshes are near-origin.

## 5. Interfaces & dependencies
**Depends on (inbound):** core-engine's **pinned** contracts (spike1-core-engine.md §5) — `FUniverseCoord` (now carries `FFrameId`), `FFloatingOrigin`/`ToEngine`, `IFrameGraph`/`FReferenceFrame` (μ/radius/SOI/spin), and the events `OnOriginRebased`/`OnSOIChange`. Chunk centers are `FUniverseCoord`, **not** `FVector`. The seed comes from [persistence](persistence.md)/new-game config.
**Provides to (outbound) — published contracts (now pinned concretely in [Spike 1 §4](../spikes/spike1-worldgen.md#4-pinned-data-contracts)):**
- `SampleTerrainHeight(FBodyId, lat, lon)` + collision queries `GetGroundContact` / `RaycastTerrain` — all `FUniverseCoord`-based, resident-free, with the `bFromVoxelPatch` voxel seam → [physics](physics.md). **`FTerrainContact` + `FTerrainHit` now also carry `SlopeRad` (terrain slope, radians, from the contact `Normal`) + `SurfaceHardness` (normalized `[0,1]` `double`, from `MaterialId`/biome) for physics tip-over + friction/restitution (RC-7, append-only).** *(Spike 1 §4.3, WG-10)*
- `FTerrainChunk` (chunk center as `FUniverseCoord`, body-center-relative verts, skirts, `MaterialId`) + `ITerrainProvider`/`OnChunkReady`/`OnChunkEvicted` streaming → [rendering](rendering.md). *(Spike 1 §4.1–4.2)*
- **`FDepositNode` deposit catalog** (C-1, **PINNED** Spike 1 §4.5) + query surface `GetDeposits`/`QueryDepositsNear`/`GetDeposit`/`ExtractFromDeposit` → [gameplay](gameplay.md) (mining UX), [factory-sim](factory-sim.md) (miner extraction), [persistence](persistence.md) (depletion). Per node: `FDepositId Id` (stable hash), `Position`(`FUniverseCoord`+`FFrameId`), `Lat`/`Lon`, `SurfaceNormal`, `FBodyId Body`, **opaque gameplay-owned `ItemId Resource`** (C-3 — referenced not defined; collapses gameplay's `DepositTypeId`), `Grade`, `InitialAmount`, mutable `RemainingAmount`. Catalog deterministically seed-generated per body (WG-6 discipline). **Cinderite `ItemId` placed only on Cinder** (WG-4 off-world hook). *(Spike 1 §4.5, §5.1–5.2, WG-11)* **Cross-domain dep: needs gameplay's C-3 `ItemId` registry (Ferrite-ore, Cinderite) to exist with stable ids — flagged to Admin.**
- **`FBodyParams`** (radius, μ, surface g, SOI, spin, atmosphere profile) → physics, rendering. **`FAtmosphereProfile` now carries Mie terms (`MieScaleHeightM`/`MieScatteringScale`/`MieAnisotropy`) alongside the Rayleigh fields for rendering's UE Sky-Atmosphere horizon haze (RC-2, append-only); Forge gets concrete Mie values, Cinder is airless → N/A.** *(Spike 1 §5, §5.1–5.2, WG-10; numeric values shared with core-engine `FReferenceFrame` — Admin to confirm single source of truth.)*
- **`WorldDiscovery` (WG-29, NEW, `core/include/of/discovery.h`)** → the map ([gameplay](gameplay.md)), [persistence](persistence.md). Two named questions, `surveyed(dir)` and `explored(dir)`, over two sparse cube-face grids fed by ONE `observe(dir, altM)`. Published across the bridge as `of_disc_*` (ABI 12) and typed in `web/src/sim/wasm/discabi.ts`; `of_disc_window` hands the map four CORNER directions per cell rather than a centre and a size, because the map's projection is orthographic and a cell near the limb foreshortens to a sliver. Serialises as a delta-varint key set at **1.04 bytes/cell** through the persistence.h cursors WITHOUT including that header (leaf, as `voxel_field.h` is). The lattice is deliberately `PollutionField`'s ([enemies](enemies.md) §4) and `discovery_tests` asserts the two agree cell-for-cell over 560,000 directions; **open cross-domain ask: enemies.h should adopt `SurfaceCellGrid` so there is one copy rather than two agreeing ones.** Consumers must ask `ModeRules.fullMapRevealed` (DW-31) rather than this field when the MODE decides visibility.
- **WHAT THE DW-37 3D MAP LANE REPLACES, AND WHAT IT INHERITS (WG-29, written FOR that lane).** DW-37 turns the map into a rotatable camera over the scene the engine already renders. Sorted so nobody has to re-derive it. **VIEW-INDEPENDENT, transfers intact, do not rewrite:** `discovery.h` entire (it answers "has this direction been seen", at two resolutions, and contains no projection); every `of_disc_*` export (`of_disc_window` takes a direction and a cosine, which is a CONE and not a projection; a camera frustum yields one too); `of_map_sample` (deliberately specified in view-REGION terms, so a far-field ground overlay reuses it; **SIGNATURE CHANGED at ABI 14, WG-33**: an `edits` DensityField handle in second position and the height is `surfaceHeight` rather than `sampleDesignedHeight`, so the player's own terrain reaches the map. `edits <= 0` is bit-identical to ABI 13 by the oracle's own undug overload. One caller, `web/src/world/MapTerrain.ts`; every other export untouched); `web/src/world/Discovery.ts` entire, including the 1 Hz interval, the generation-keyed cache and the save round trip; `MapWorld.ore()`, which is THE discovery gate and hands over plain world data; `ModeRules.fullMapRevealed`; the persistence field and `RestoreLedger.discovery`; and `MapTypes`' `MapOre` / `MapDiscovered` / `MapDiscoveryReadout`, none of which carry a screen unit. `MapFocus` splits: the ORIGIN half (`centreM`) transfers exactly, because a camera orbits a focus point, and the AXIS half becomes the camera's orientation reference rather than a projection plane; the *rule* that the axis rides on the FOCUS and never on the zoom is the part worth keeping, since it is what stops a conic foreshortening differently at different zooms. **PROJECTION-SPECIFIC, expect to replace:** `MapDraw.ts` (`Proj`, `toPx`, the body arc, the conic polyline and its Liang-Barsky clip, the apsis rings and leader labels, the scale bar); `MapPaint.ts`'s `toPx` / `discCoversCanvas` / `place` / `hits`; `MapLayers.ts`'s quad fill and glyphs; and the sampled-terrain blit, which is the half of the terrain work that a real camera makes redundant. **`MapPaint.sizeAlpha` is the interesting middle case and should be KEPT**: it is expressed in pixels per metre, which a perspective camera also has (projected size), so the legibility ramp and DW-36's "no zoom threshold anywhere" property survive a change of projection even though every call site moves. **Is `MapScene` still the seam? Yes, and more so than R17 thought.** `centreM`, `spanM`, `focusName`, `axisName`, `discovered`, `ore`, `revealAll` and both conics are all producer-side world data; only `planeU`/`planeV` are projection-specific and they become an orientation. `MapMode.readout` barely changes, and `MapView`'s READOUT COLUMN (Position / Focus / Discovery / Maneuver) is DOM over plain numbers and does not change at all.
- **`IPersistable` (C-6, world-gen side — CONFIRMED** Spike 1 §4.6**)** → [persistence](persistence.md). World-gen implements `IPersistable` (chunk-scoped, `DomainId=WorldGen`) over **deposit depletion only**: `(depositId, remaining)[]` per region, `depositId = hash64(bodySeed, region, localIndex)` (the stable id persistence §2.1 specified). Terrain + deposit *placement* regenerate from seed (PS-7/WV1) — **not serialized**; voxel-patch diffs are Phase-4 (seam empty, WG-2). *(Spike 1 §4.6, WG-12)*
- **THE WATER LEVEL (WG-36 to WG-39, NEW, `core/include/of/water_field.h`)** -> the character controller ([physics](physics.md)), [rendering](rendering.md), and eventually the pump ([factory-sim](factory-sim.md)). **A THIRD surface answer, published as its own named authority with its own bound, per DW-26.** `levelM(body)` is ONE scalar per body, metres above the datum. `levelAt(dir)` returns it or `kNoWater`. `depthAt(dir, edits)` is the only function that reads a ground authority and it reads the EDITED surface, so digging the bed deeper genuinely makes the water deeper. `submersionM(pos)` takes a POINT and nothing else and is the ONE place "am I in water" is answered. `shorelineM` is DERIVED by bisection from the basin profile and never stored, because a constant copied from the thing it describes is standing rule 11's worked example. Across the bridge as `of_water_*` at **ABI 16**; typed in `heap.ts`; on the client as `WaterOracle`, a SEPARATE class from `SurfaceOracle` with no method name in common. **There is no `of_surface_water`, no water flag on any existing surface call, and no `waterHeight` on `SurfaceOracle`. If you want water you ask for it by name, or you do not get it.**

- **WHAT THE WATER SURFACE STILL NEEDS FROM RENDERING (WG-42, written FOR that lane, and this is the honest list rather than a half-shaded handoff).** `web/src/world/WaterSurface.ts` ships the part that is world DATA and stops. **It spends ZERO of DW-10's five custom shaders**, deliberately, so the lane that gives water a look can spend one knowingly rather than inherit a compromise. What is there: a curved shell on the sphere reaching the SHORELINE (not the basin rim, so dry beach shows inside the bowl), tessellated finest at the waterline, with per-vertex RGBA where **both the colour and the ALPHA are a function of the measured water depth under that vertex** (0.14 alpha at the edge where the bottom is 13 cm down, 0.82 in the middle at 3.4 m), a stock `MeshStandardMaterial`, `DoubleSide`, `depthWrite: false`, `renderOrder` 2, anchored per standing rule 6. **WHAT IS MISSING, in the order it would most improve the read:**
  1. **AN UNDERWATER CAMERA LOOKS EXACTLY LIKE DRY LAND.** `docs/screenshots/WG36_after_aerial.png` is the camera 1.72 m below the water surface and it is indistinguishable from standing on the bed: no tint, no depth fog, no colour extinction, no surface seen from beneath. The quantity that pass needs already exists, is correct, and is published every tick: `SwimState.headUnderM` (positive = the eye is that far under), reachable from a probe as `of.swim().headUnderM`. This is the single biggest gap and it is entirely rendering's.
  2. **No surface motion at all.** No normal animation, no ripples, no flow. A perfectly still 33 m pond reads as glass or as a decal depending on the angle.
  3. **No specular sun glint and no reflection.** Water without a highlight is the main reason a translucent plane reads as a filter rather than as a liquid.
  4. **No refraction**, so anything crossing the waterline (the player's own legs, a prop, a future pipe) is straight through it.
  5. **No shoreline treatment.** No foam, no wet-sand darkening on the beach ring, no soft alpha against the terrain intersection. The 5.4 m dry beach between `shorelineM` (16.62 m) and `basinRadiusM` (22 m) is currently the same material as ground 300 m away.
  6. **Nothing in the far scene.** The pond exists only in the near pass, so it disappears rather than fades at range.
  **AND ONE THING THAT IS NOT RENDERING'S, flagged here because it is visible in every screenshot:** the prop scatter does not know the pond exists, so `Mtn_SnowPatch`, grass cards and pebbles are placed ON THE POND BED and at the waterline (see `WG36_after_swimming.png`, ice slabs floating mid-pond). The scatter needs to read `of_water_depth_at` and refuse, or bias to submerged props, wherever depth is positive. That is one query and it is now available; the file is the vegetation lane's.

- **THE PER-CHUNK NOISE PHASE (WG-50 to WG-54, NEW, `web/src/world/ChunkPhase.ts`)** -> [rendering](rendering.md). This is the answer to RN-45's open dependency, and it is **deliberately NOT in the chunk wire format**: it changes no byte of the 28 B/vertex packed layout, no `of_packed_*` export, no ABI and no parity fixture. It is a pure function of `ChunkView.anchor`, which is already float64 on the main thread.
  - `reduceOctave(anchor, wavelengthM) -> { cell, frac }` and `packPhases(anchor, wavelengthsM) -> Float32Array`, laid out `[cell.xyz, frac.xyz]` per octave, 6 floats each. `cell` is `floor(anchor / L)`, an exact integer with 189x to 10,131x of headroom under float32's 2^24; `frac` is `anchor / L - cell` in [0, 1). The division and the floor happen in float64 **here and nowhere else**, which is the entire content of the fix.
  - **THE SHADER-SIDE HALF OF THE CONTRACT, stated in one place because a phase computed one way on the CPU and consumed another way in a shader is exactly where a bit-exactness guarantee gets quietly lost.** Per axis, with `rLocal` the vertex's chunk-local offset in metres (that is the `position` attribute, already float32 relative to the anchor): `t = frac + rLocal / L`, then `i = cell + floor(t)` in exact float32 integer arithmetic and `f = fract(t)`. Hash `i` exactly as the shader hashes `floor(pM / L)` today and interpolate on `f`. **`cell` is what makes the field global rather than tiled per chunk, and `frac` is what makes it resolvable.** Dropping `cell` gives every chunk the same noise patch, which is a visible grid.
  - **Every octave that feeds a derivative must be reduced. A partial fix passes the dead-quad statistic and is still visibly wrong (WG-53).** At the shipped ladder that is 4 octaves, 24 floats per chunk.
  - **The delivery channel is rendering's to choose and one candidate is already ruled out (WG-54):** the `aFadeT0` vertex-attribute pattern costs about 37 MB against a 93.7 MB frame and is refused; the `MachineBatch.ts:131` batching-id `DataTexture` costs about 31 KB. A per-chunk uniform is not available at all, because chunks are instances in two `BatchedMesh`es (DW-11).
  - **Re-stamp wherever the anchor changes** (`ChunkView.refresh()`, the near/far slot swap) and **not on the floating-origin rebase**, because the anchor is invariant under a rebase. That invariance is the whole reason RN-45 keyed the field on `pM` in the first place, and it is preserved rather than given up.
  - **Two instruments ship with it and rendering should adopt both, because neither is sufficient alone:** `web/tools/precision/phasecheck.ts` (57/57 assertions, including a positive control that fails the shipped arithmetic by name) and `web/tools/precision/artphase.ts` (the offline reproduction, the dead-quad statistic, the field-agreement statistic, and the pictures). **The dead-quad count is necessary and not sufficient and the picture is still the arbiter (WG-53).**

- **AND BEFORE RENDERING BUILDS ANY OF THAT, read WG-56.** Every chunk already carries a per-quad normalised UV that is uploaded, held in VRAM and interpolated every frame, and `TerrainShader.ts` samples it **zero times**. Its quantum is **0.248 of a ground pixel at 2 m** where `pM`'s is **8.77 pixels**. A tiling detail map on `uv * N` may deliver sub-metre detail with no phase and no CPU work at all. The phase is still the right general fix and is the only one that repairs a derivative taken on a WORLD-space field; the UV route is narrower and may make the detail bump specifically unnecessary. Measure both.

- **WHAT AN AIRLESS BODY REQUIRES OF RENDERING (WG-148, NEW, written FOR that lane, and it is a consequence of a world-gen FACT rather than a taste).** Cinder is airless by design (D-006): `atmosphere.h::makeCinderAtmosphere()` returns an empty profile and every query returns exactly 0. The client does not read that. `web/src/app/Boot.ts:120` calls `forgeAtmosphere(body.radiusM)` unconditionally, and `forgeAtmosphere` is the ONLY producer of `AtmosphereParams` in the codebase; there is no airless counterpart. `docs/web/ARCHITECTURE.md:2553` already anticipated this ("Cinder needs an airless profile (`uAtmosOn` 0) at W9").
  - **THE MEASUREMENT, because "it would look better" is not an argument.** One camera, 12 km over the spawn meridian, everything else identical: `docs/screenshots/WG141_moon_air12k.png` (atmosphere on) is a **white fog bank in which not one crater is visible**; `docs/screenshots/WG141_moon_air12k_noatmos.png` is a black sky with stars, a hard terminator, and overlapping craters at four scales. **Aerial perspective was erasing the entire WG-141 rewrite.** That pair is the whole ask.
  - **What is needed, in order.** (1) An `airlessAtmosphere()` sibling, or a body-kind branch at the one decision site, so `uAtmosOn` is 0 on a moon: no Rayleigh, no Mie, no aerial perspective in `TerrainMaterial`. (2) **A sky that is black but not empty**: stars must be visible AT NOON, which is the single strongest "there is no air here" cue and the opposite of Forge's rule. (3) **AN AMBIENT SOURCE THAT IS NOT THE SKY INTEGRAL, and this one is load-bearing rather than cosmetic.** `SkyIbl` derives ambient from the sky, so with the sky off the shadowed side of every crater and boulder collapses to near-black: see `docs/screenshots/WG141_ground_craterfloor.png` and `WG141_ground_highland.png`, both at `sundot=0.28`. Real airless bodies genuinely do have brutal terminator contrast, so this is not simply wrong, but at these values the moon is unplayable in shadow and a player landing near the terminator sees nothing. An explicit low ambient or a bounce term is a rendering decision I am flagging, not making.
  - **NOT asked for: ejecta rays.** They are albedo, not topography, so they are deliberately absent from the height field (WG-141) rather than faked into it. If they are wanted they are a material effect and they need an "ejecta proximity" scalar this lane can publish; say so and I will add one.
  - **One thing in these screenshots that is NOT rendering's:** thin bright streaks follow the terrain contours in `WG141_ground_craterfloor.png`. Unattributed as yet, possibly the detail-card layer at grazing light. Recorded as R17 rather than blamed.

**Contracts negotiated:** chunk format (`FTerrainChunk`) + height/collision queries are **pinned in Spike 1 §4**, confirmed consistent with core-engine's `UniverseCoord`/`FFrameId`. **`FDepositNode` (C-1) + deposit-depletion `IPersistable` (C-6) pinned/confirmed in Spike 1 §4.5–§4.6 (Phase 1).** Open negotiation with physics: analytic queries vs baked per-quad collision meshes (Spike 1 WR5).

## 6. Task backlog / roadmap
- **[Phase 0 / Spike 1 — Wave-0 core BUILT + GREEN (WG-13); UE streaming pending]** Minimal cubed-sphere heightfield for one planet (atmo) + one moon (airless). **WG-Build 0 DONE in headless C++** (`cubed_sphere.h` + `world_gen_tests`): `FQuadKey` + quad math, tangent warp (WG-5), position-hashed noise stack (WG-6/WG-7), `generateQuadMesh`, `SampleTerrainHeight`; **gates WV1/WV2/WV3 pass BITWISE** (neighbour + parent/child shared edges bit-identical; deterministic regen), plus cube→sphere-on-sphere, warp-uniformity, WV7 query agreement, planet≠moon. Full design + build plan + pinned chunk/height/collision contracts + 2-body params in **[spike1-worldgen.md](../spikes/spike1-worldgen.md)**. Next: **WG-Build 1→2** — `UTerrainStreamer` (LOD/hysteresis/skirts/async/budget, §3), `FTerrainChunk` emission + floating-origin hookup (`OnOriginRebased`/`OnSOIChange`), wire the query layer's `bFromVoxelPatch` stub, then integrate at core-engine Wave-1 step 8 (replace placeholder sphere colliders) and run gates WV4–WV6/WV8 in-engine.
- **[Phase 1 — DEPOSIT GEN BUILT + GREEN (WG-14); depletion `IPersistable` pending]** Deposit catalog **`FDepositNode` pinned (C-1, Spike 1 §4.5, WG-11) and now REALIZED** — `core/include/of/deposits.h` + `deposits_tests`: `GenerateDeposits` (deterministic position-hashed Fibonacci-sphere placement, surface-snapped), the full query surface (`GetDeposits`/`QueryDepositsNear`/`GetDeposit`/`ExtractFromDeposit`), and the **Cinderite off-world hook (WG-4)** all built; determinism + Cinder-only + on-surface + extraction/depletion (seed+diff) proven bitwise. **`IPersistable` deposit-depletion confirmed (C-6, Spike 1 §4.6, WG-12)** — NEXT: wire the depletion `(depositId, remaining)[]` `IPersistable` over the catalog (the diff surface is exactly `ExtractFromDeposit`'s `RemainingAmount` decrement). Also Phase 1: biomes, full body-parameter tuning feeding physics/rendering; edge-stitch index buffers (Spike 1 WG-8 deferred polish). **Dep: gameplay C-3 `ItemId` registry — world-gen currently references Ferrite `0x0001` / Cinderite `0x0004` as bare `uint16_t` (WG-11); gameplay confirms the canonical ids.**
- [Phase 2] Diff persistence integration (deposit-depletion design done — C-6); resource identity tuning (the WG-4 rule is pinned for the 2 bodies; tune density/grades).
- [Phase 4] Voxel deformation patches (fill the `bFromVoxelPatch` seam, Spike 1 §4.4); POI/loot placement at scale.

### 6.1 WG-55 spawn shortlist, awaiting Reid's pick

Not moved, deliberately. `homeDir` re-baselines the ground under every pad,
level, build and terrain probe, and `pondDir` must move with it or the pond is
orphaned. It goes when the checkout is quiet and Reid has picked.

#### Four things that are true and were not known before this survey

Reid picks the site. The job here is to make sure he is picking from true
statements, and four of the statements we were working from were false.

**1. There is no Desert biome, and the desert he liked is Beach.** He asked for
"the desert biome" by name. The biome set is Ocean, Beach, Plains, Forest,
Hills, Mountains and Polar (`biome.h:53-67`), and that is all of it. What reads
as desert is partly the post grade and partly **Beach**, whose palette entry is
the sand colour `0xc8b48a` and which is one of only two biomes that take dry
scrub props instead of grass. `docs/screenshots/WG53_site_beach.png` is a Beach
site and it is unmistakably the look he meant: pale sand, dry tussocks, low
scrub, open sky. **So the answer to "can I start in the desert" is yes, and it
is called Beach.** Beach was invisible to the spawn probe because the probe's
`minAlt = 200.0` (`spawn_probe.cpp:544`) cuts the entire coastal band.

**2. The spawn probe could never have produced the current spawn.** It hard
filters to Plains, Forest and Hills (`spawn_probe.cpp:134-136,425`) and the
current spawn is Mountains. This is not a scoring near miss: the probe and the
world disagree about what a legal spawn even is. Re-run over the current site it
ranks it **last of 21, score -85.84 against the winner's +32.81**, with 23.88%
slope at 20 m, which is 20x to 30x every candidate. It is also not the "Hills
valley floor at 2,963 m" that `Config.ts` still documents: it is Mountains at
4,667.79 m and it stands **+130.7 m above the mean of the surrounding 6 km**,
which makes it a tabletop rather than a valley floor.

**3. The probe's pad verdict is currently wrong, and my own pond is what broke
it.** `probePadHere` moves `homeDir` to the candidate (`spawn_probe.cpp:487-491`)
but never moves `pondDir` or zeroes `pondRadiusM`, and it measures with
`sampleDesignedHeight`, which since WG-36 subtracts the pond basin. Its reported
"worst 4 m step inside the flat radius 1.078601 m" at the current site is
**entirely the pond**: inside the flat radius the un-ponded height is a bit-exact
constant, so the basin is the only varying term, and the analytic 4 m span across
the basin's steepest point is 1.080 m. Candidate rows read 0.000000 m only
because the pond is thousands of km away from them. A feature of mine is
degrading an instrument of mine, which is the shape worth remembering.

**4. The snow line is a biome boundary, not an altitude.** `Mtn_SnowPatch` is
keyed on the biome index with no elevation gate at all (`Registry.ts:143-144`),
so the prop snow line is exactly the Mountains boundary, `kMountainsRel` 0.330 of
6000 m of raw relief, about **2,700 m** of designed height. The shader's own
snowfield is a separate thing entirely, 5,160 m to 6,840 m
(`TerrainShader.ts:263-265`). The current spawn sits **1,239 m above the prop
line and 492 m below the shader one**, which is precisely why WG-35's blue blobs
were props rather than painted snow.

#### And one thing the control picture settles

`docs/screenshots/WG53_site_current.png` is today's spawn in daylight, and **the
blue blobs are plainly in it and they read as puddles of water.** They are flat,
saturated blue, and lying in the ground. They are `Mtn_SnowPatch` ice props, so
the WG-35 diagnosis is confirmed on the picture as well as in the counts, but the
useful half is the other direction: **Reid calling them water was the correct
reading of what is on screen.** An ice prop that reads as standing water is an
art defect independent of where the spawn goes, and moving the spawn out of
Mountains hides it rather than fixes it. Flagged for the art and rendering lanes;
not mine to change.

#### A selection criterion nobody knew about: this planet has no seasons and a fixed sun declination

Forge's sun direction is `normalize(cos 2*pi*t, 0.42, sin 2*pi*t)`
(`SkyPass.ts:105`). The `y` component is **constant at +0.387 for all time**, so
the sun has a permanently high northern declination and the planet has no axial
tilt and no seasons. **A far-southern spawn therefore cannot ever get a high
sun.** Measured at each candidate's own local noon:

| site | lat | best sun elevation, ever |
|---|---|---|
| hills2 | +22.29 | 89.5 deg |
| current | +2.00 | 69.2 deg |
| plains | -7.97 | 59.3 deg |
| forest | -19.85 | 47.4 deg |
| hills | -31.17 | 36.1 deg |
| beach | -35.60 | 31.6 deg |
| **beach2** | **-57.94** | **9.3 deg** |

**This is a real choice and not a rendering artefact.** If Reid picks `beach2` the
spawn is in low golden evening light permanently, at every hour of every day,
because that is the highest the sun ever gets there. Its frame reads warmer and
moodier than `beach` for exactly that reason. Some people will want that. It
should be a decision rather than a surprise, and it is the sort of thing that
would otherwise be discovered a week after the spawn moved.

`beach` at 31.6 deg is the better sand option on this criterion as well as on
the flatness numbers, which is convenient rather than a coincidence: the probe's
own latitude band stops at 60 degrees.

**Two instrument notes from the same pass**, both worth keeping:

1. **The first batch of shots came back at night, and the cause was a real
   misuse rather than a wrong flag.** `__of.setTime(t)` sets one sun vector in
   the BODY frame, so a fixed `t` is a fixed direction in space and therefore a
   *different local time at every longitude*: `t = 0.32` is mid-morning at lon
   144 and the middle of the night at lon -86. There is no `?time=` or `?sun=`
   flag; `setTime` was already the right handle, used wrongly. The probe now
   sweeps `t` and takes each site's own local noon, and solves the camera yaw so
   the sun is over the same shoulder in every frame. Comparable frames of
   different longitudes need per-site solar time, not a shared constant.
2. **`snowBatchesDrawn` is unreliable and should not be quoted.** It reads
   `OF_Ice:744` at every site including both beaches, which contradicts the
   pixels and the biome rule. Every run boots at the Mountains spawn before
   teleporting, so it is almost certainly a batch allocated at boot and never
   released rather than live geometry at the destination. **A counter that
   reports geometry which is not on screen is the DW-28 class**: it would happily
   confirm snow at a site that cannot place a single flake. The biome index and
   the pixels are the evidence; that counter is not.


#### The candidates

Every site below is under the prop snow line. `rawRel` is the number `biomeAt`
classifies on, and 0.330 is the Mountains boundary. Pictures are
`docs/screenshots/WG53_site_<name>.png`, all shot in daylight at eye height with
the same camera and settings, with the HUD left on so the biome index, latitude,
longitude and altitude can be read back off each frame.

| biome | lat | lon | alt m | rawRel | worst 4 m step | slope at 20 m | pad grade | relief in 6 km | picture | note |
|---|---|---|---|---|---|---|---|---|---|---|
| Mountains | 2.00000 | 144.00000 | 4667.8 | 0.537 | see note 3 | 23.88% | 32.37% | 1174 m | `_current` | **the CONTROL, today's spawn. Ranks last of 21.** Verified on the frame: biome 5, 4.67 km relief, and the blue blobs. |
| Hills | -31.16500 | -86.27401 | 2077.2 | 0.301 | 0.026 m | 0.75% | 12.42% | 403 m | `_hills` | **my recommendation on the numbers.** A real valley floor: 92.3% of the 6 km box stands above it, peaks 1,009 m above at 7.8 km. Mountain scenery starts 3.2 km away rather than underfoot. The pad adds 0.29 percentage points of grade over the natural ground, so it will read as a clearing and not a cut shelf. Verified on the frame: biome 4, slope 0, grounded. |
| Hills | 22.28600 | 108.84406 | 1897.2 | 0.275 | 0.049 m | 2.44% | 9.91% | 249 m | `_hills2` | quieter, no other biome within 4 km |
| Plains | -7.96750 | 116.53189 | 331.8 | 0.092 | n/a | 0.31% | 4.54% | 26 m | `_plains` | a real basin floor, 97.8% of the 1 km disc above it. **Two cautions, both visible on the frame:** 26 m of relief in 6 km is the empty green plane the probe's own header warns about, and it is the most expensive frame of the set at 2,660k triangles and 73,670 props against the Beach site's 839k and 15,135. |
| Beach | -35.60280 | 53.30131 | 12.2 | 0.007 | 0.010 m | 0.33% | 2.32% | 25 m | `_beach` | **THE DESERT OPTION, and the one to show Reid first.** Flattest ground on the planet, dry scrub instead of grass, and the cheapest frame of the set. |
| Beach | -57.93800 | -85.62600 | 8.3 | 0.004 | 0.007 m | 0.37% | 2.21% | 15 m | `_beach2` | flatter still, but **the sun never rises above 9.3 degrees here, ever** (see above), so this spawn is in permanent low golden light. Its frame also shows a translucent sheet in the foreground that reads as shallow water at 8.3 m of elevation, which is worth checking before choosing it. |
| Forest | -19.85000 | -72.78530 | 27.3 | 0.015 | 0.020 m | 0.63% | 3.13% | n/a | `_forest` | for completeness |

**Do not ship the probe's own rank 1** (lat 25.05, lon -27.17). At `rawRel`
0.3243 it sits 0.0057 under `kMountainsRel`, about 34 m of raw relief, so a
tuning nudge flips it into Mountains and it grows snow props.

**Both owed repairs are DONE (WG-57).** `probePadHere` now moves `pondDir` with
`homeDir` and reports the pad's own flatness and the ponded flatness as two
named numbers, and `printHomeDirLiteral` emits both literals with the separation
checked against the tolerance the test pins. Running the tool over the current
spawn re-emits both shipped literals byte for byte, so the output is
paste-ready.
`homeDir` is encoded in 18 places and two ctests pin the current literal by name
(`test_biome.cpp:208-216`, `test_water_field.cpp:138-148`). Both failing on a
move is correct behaviour and is the point of them, not an obstacle.


### 6.2 WG-59 to WG-63 canopy: the measured ladder, and what it does at each candidate spawn

All numbers from ONE binary at `dac779b` plus this lane's files, `?canopy=0`
against the shipping default, at a settled frame with the scatter backlog
drained at every site. `?canopy=0` reproduces the pre-change build's triangle
count to the unit at all seven sites, so the control is not merely a flag, it is
verified equal to the world before this tier existed.

| site | biome | ground m | calls | triangles | canopy trees | what the treeline does |
|---|---|---|---|---|---|---|
| rn15 (invariant camera) | Hills | 860.9 | 70 to 78 | 989,052 to **983,882** | 462 | below it: 0% of cells bare |
| current spawn | Mountains | 4,667.8 | 65 to 65 | 619,445 to **619,445** | 0 | above it: 100% bare |
| forest | Forest | 27.3 | 78 to 78 | 1,024,404 to **1,157,625** | 1,560 | below it: 0% bare |
| plains | Plains | 331.8 | 78 to 86 | 2,652,309 to **2,613,292** | 260 | below it: 0% bare |
| hills | Hills | 2,077.2 | 70 to 70 | 838,677 to **838,677** | 0 | above it: 100% bare |
| hills2 | Hills | 1,897.2 | 70 to 78 | 861,921 to **862,041** | 1 | **ON it: 79% bare** |
| beach | Beach | 12.2 | 69 to 69 | 867,387 to **867,387** | 0 | no canopy density at all |

Programs (37 or 50, order dependent within a session and identical row for row
across the pair), geometries (52) and VRAM (104.2 MB) do not move anywhere.

**What this does at whichever site Reid picks**, which is the question section
6.1 leaves open:

- **Beach** (his desert option, and the one the survey said to show him first):
  nothing changes. Zero trees, zero triangles, zero draw calls, bit-identical
  frame. The pale sand stays pale sand. If he wants a treeline BEHIND the beach
  it is one density row and it is deliberately not there today.
- **Plains**: 260 trees as isolated copses in open grass, and the frame gets
  1.47% CHEAPER, because the understorey those copses shade costs more than they
  do. This is also the site the survey flagged as the most expensive frame of
  the set at 2.66 M triangles, so the direction is the useful one.
- **Hills at 1,897 m (`hills2`)**: the spawn lands ON the treeline. 79% of
  offered cells are above it, so the picture is open ground with stragglers and
  wooded slopes falling away below. This is the most dramatic of the seven and
  it is the one to show him if he wants a view.
- **Hills at 2,077 m (`hills`, the survey's own recommendation on the numbers)**:
  bare, bit-identical frame. A spawn there has no forest in it at all and the
  nearest trees are downhill. That is correct and it is worth him knowing before
  he picks, because it is the one site where this pass is invisible from the pad.
- **Forest at 27 m**: the closed forest, and the only site that pays, at +13.0%.
- **The current Mountains spawn**: bit-identical. Nothing here moves the spawn or
  depends on where it ends up.

**Cost decomposition**, from `?canopyshade=0` (trees on, the understorey they
shade left at full density):

| site | trees alone | understorey shade | net |
|---|---|---|---|
| forest | +206,600 | -73,379 | **+133,221** |
| rn15 | +57,974 | -63,144 | **-5,170** |
| plains | +35,078 | -74,095 | **-39,017** |

**What is NOT delivered, stated plainly.** The canopy reaches 620 m. That puts a
treeline on the near ridges and fills the middle distance, and it does not reach
a horizon two kilometres away on a body where that much ground is visible. No
arithmetic gets it there: at 2 km the same density is fifteen times the
instances, and every tree is drawn four times because a prop batch has both
frustum-culling flags off and casts into three cascades. **A horizon treeline
needs a far-field impostor layer and this client has none** (there is no
billboard, impostor or card mechanism anywhere in `web/src/render`; the only
authored impostors in the repo are the harvest trees' 16-triangle `_LOD2` nodes,
which `NodeBatch.ts:190` never loads because its regex matches `_LOD0` only).
That is a rendering-lane ask and it is recorded as one rather than half built.

**Two other findings logged unfixed:**

- **`Scatter.replace` has no caller.** It is the documented rebase path
  ("re-derive every instance matrix from its chunk's anchor, THE rebase path"),
  `TerrainStream.onOriginRebased` re-places every `ChunkView`, and nothing tells
  the scatter. Chunks inside the scatter radius survive a rebase rather than
  being dropped, so their instance matrices should be stale by the rebase delta.
  `FloatingOrigin.thresholdM` is 4,000 m and every probe teleports rather than
  walking, which is exactly why nothing has seen it. **NOT confirmed by
  measurement and therefore not fixed here**; it needs a driven 4 km walk.
- **Both slope gates refuse nothing.** `CANOPY_MIN_SLOPE_COS` (44 degrees)
  refused 0 of 271,815 offered cells and `MIN_SLOPE_COS` (57 degrees, in force
  for every prop since RN-7) refused 0 as well, across all seven sites including
  the Mountains spawn. Counters for both now ship, so the next reader inherits
  the measurement rather than the comment.

### 6.3 WG-64 to WG-66: the dead rebase path, and the assumption that hid it

Section 6.2 logged this as suspected and explicitly NOT confirmed. It is now
confirmed, fixed and controlled. `probes/scatterrebase.js` is the instrument and
it is the first probe on this project that WALKS to its condition rather than
teleporting to it.

**The measurement**, one binary, driven sprint at the Forest site, input tape
held for the whole run, 88 slices of 5 sim seconds:

| t (s) | metres | rebases | staleMaxM | staleChunks |
|---|---|---|---|---|
| 5 | 44.8 | 2 | 0.000000 | 0 |
| ... 86 slices, every one 0.000000 ... | | | | |
| 430 | 3,954.8 | 2 | 0.000000 | 0 |
| **435** | **4,000.8** | **3** | **4,000.089191** | **43 of 43** |
| 440 | 4,046.8 | 3 | 4,000.089191 | 23 |

The displacement equals the rebase delta to six decimals, and it decays instead
of snapping back because a chunk is only re-placed when it is next rebuilt, at
one chunk per frame.

**The generalisable finding is not the bug.** It is that the entire probe suite
shares a locomotion assumption. A teleport moves the player 4 km in one step,
every resident chunk falls outside the scatter radius, and the stale matrices
are dropped before a frame is drawn, so the defect is unreachable by the only
motion any probe uses. `probes/walk.js` does walk, and does read `rebases`, but
reads it as streaming churn and never asks where the props went. Any subsystem
that is only ever exercised through a shortcut has this shape available to it.

**Two instrument notes worth keeping:**

- The first capture came back at NIGHT, because seven sim minutes of walking
  moves the clock, and nothing in the frame could be read. The probe now solves
  local noon before the shot using `stats()` calls that advance zero frames,
  which matters here more than usual: any frame it ran would let the scatter
  rebuild a chunk and heal the very state the picture is of.
- The correct value is a hard 0.000000 and is not a threshold anybody tuned.
  `Scatter.write` and `ChunkView.place` both go through the same f64 `toEngine`
  subtraction, so a re-placed chunk is exact; `STALE_EPS_M` is 1 mm and exists
  only so a float32 round trip cannot trip the counter, against a failure
  measured in kilometres.

**Still open, and it is now a question for other domains rather than for me:**
the scatter was one of several things that cache an engine transform. The other
`OriginRebased` subscribers (`zfight`, `voxelMesh`, `levelRing`, the water
surface, the streamer itself) are all wired; nothing here audits whether every
consumer that composes from `ChunkView.pos` is on the new hook. A driven walk is
now cheap, and the same probe shape would answer it for buildings, belts and
machines.

### 6.4 WG-67 to WG-72: the rocks are real, the tables, and what each site does

All numbers from ONE binary (HEAD `2a4d661` plus this lane's files), dev-served
scratch tree isolated from other lanes' in-flight edits, `?rocks=0` against the
shipping default, backlogs (scatter and rock) drained at every site, each site
at its own local noon. Counters are cumulative across a multi-site session;
per-site wet/slope readings are deltas between rows.

**The invariant table both ways, and the third column that matters** (HEAD's
decor measured by restoring HEAD's `Registry.ts` into the same binary):

| site | biome | tris HEAD decor | tris retired | tris rocks ON | rocks vs HEAD | calls HEAD/on |
|---|---|---|---|---|---|---|
| rn15 | Hills | 988,834 | 963,224 | 980,832 | **-8,002** | 82/82 |
| current | Mountains | 624,271 | 514,923 | 536,507 | **-87,764** | **69/61** |
| forest | Forest | 1,162,577 | 1,134,106 | 1,139,786 | **-22,791** | 82/78 |
| plains | Plains | 2,618,244 | 2,592,400 | 2,602,624 | **-15,620** | 90/90 |
| hills | Hills | 843,629 | 828,238 | 843,574 | **-55** | 74/74 |
| hills2 | Hills | 866,993 | 848,120 | 860,048 | **-6,945** | 82/82 |
| beach | Beach | 872,297 | 825,529 | 842,001 | **-30,296** | 73/73 |
| beach2 | Beach | 818,777 | 782,595 | 793,955 | **-24,822** | 73/73 |

Programs, geometries and VRAM do not move anywhere between rocks on and off
(37 or 50 order-dependent, 53, 104.2 MB, row for row). Draw calls on/off are
IDENTICAL at every site: rocks ride the node batches that already exist. The
drops against HEAD (69 to 61 at the spawn, 82 to 78 at forest) are the retired
decor's own batch draws with their shadow cascades going away.

**What Reid sees at each candidate spawn** (rocks live in the 170 m ring /
realised per km2; delivery is placed over asked, fair-draw band 4 sigma):

| site | rocks | /km2 | delivery | read |
|---|---|---|---|---|
| current (Mtn) | 38 | 419 | 1.08 | clustered scree, the WG69_rock_prompt.png frame; pond cells refused (wetCells 2) |
| hills | 27 | 297 | 1.02 | scattered singles and small groups |
| hills2 | 21 | 231 | 0.82 | same, thinner by the draw |
| rn15 (Hills) | 31 | 341 | 1.01 | the invariant camera keeps rocks in frame |
| plains | 18 | 198 | 1.19 | lone erratics in grass |
| forest | 10 | 110 | 0.87 | sparse, trees dominate, correct |
| beach | 29 | 319 | 1.23 | clustered outcrops on sand |
| beach2 | 20 | 220 | 1.11 | same, sparser |

Clustered biomes realise about two thirds of their nominal table density (the
cluster ramp's mean under the bell-shaped field), measured and recorded here
rather than hidden in the knob: the table column is the ask, this table is the
world.

**Pictures beside numbers** (RN-61: a 27-rock delta over 90,000 m2 is a few
per cent of any wide frame, so the pairs are counters' evidence and the
close-up is the eye's): `docs/screenshots/WG69_{rocks,norocks}_{hills,current,
beach}.png`, and `WG69_rock_prompt.png` is the feature in one frame: an
angular boulder under the accent crosshair, "Left click Harvest Stone, 100%
remaining", in a Mountains scree where every visible rock is a node.

**Yield and respawn:** stone flows through the existing loot path
(`resourceOf(Rock)` = stone, `baseAmountOf(Rock)` = 60 x grade, swings-to-clear
authored in gameplay.h §S.2a); no gameplay file was touched and no balance row
was needed. Rocks do not respawn, matching the trees; an emptied rock keeps its
Low stub, which is what says "done" instead of "moved".

**Known debts, stated:** the nodes-batch-no-culling debt stands and is
IMMATERIAL at these densities (zero new draw calls, pool peak 128, refused 0);
re-measure before any density increase. `NodeField.update` still pays one WASM
state read per placed node per frame (about 120 at the spawn). Moon biomes owe
their own pass. The over-cap files this pass touched (`NodeField` 464,
`Gameplay` 507+) were over the 400 cap at HEAD already and are now more so;
`check-limits` is red repo-wide and needs an Admin-routed cleanup.

### 6.5 WG-91 to WG-100: cashing in the new art, and the two rows I refused to write

All numbers from ONE binary: a `git archive HEAD web` scratch tree (`web/dist-wgwire`) carrying HEAD plus exactly nine of my files and NOTHING else, verified file by file against HEAD by sha256 before every run. That isolation was not ceremony: two sibling lanes were live in `Config.ts`, `Boot.ts`, `run.mjs` and `NodeField.ts` while this ran, and the first measurement attempt died on a `TREE_RADIUS_M` import from a `TreeTuning.ts` that does not exist at HEAD. Dev-served on port 5231 (`--strictPort`, my own process; Reid's 4200 untouched), each site at its own local noon, chunk set converged and both backlogs drained.

**The invariant table both ways, and the pass is NET CHEAPER.** Each control is a one-binary URL flag except `Plains_PebbleA`, whose control restores its row in the scratch tree (WG-67's precedent).

| site | biome | HEAD tables | shipped | delta | calls HEAD/ship |
|---|---|---|---|---|---|
| rn15 | Hills | 1,123,074 | 1,123,074 | **0** | 80/80 |
| current | Mountains | 619,981 | 627,981 | **+8,000** | 59/59 |
| forest | Forest | 1,591,650 | 1,587,471 | **-4,179** | 76/76 |
| plains | Plains | 2,701,128 | 2,673,936 | **-27,192** | 88/84 |
| hills | Hills | 922,124 | 922,124 | **0** | 72/72 |

Net **-23,371 triangles and -4 draw calls**. Programs (38 or 50, order-dependent), geometries (52), textures (99/101/105) and VRAM (104.2 MB) do not move anywhere, row for row, in any configuration measured this pass, including the one that adds a whole new 167 kB node file.

**WG-94, THE SPIRE, AND THE CONTROL IS THE PROOF.** `rock_spire.glb` is a second `NodeArt.ART[NODE_KIND.Rock]` entry with `weight` 0.5 and `biomes: [Mountains]`, both new optional fields on `NodeArt`. `?spires=0` splices it OUT of the list rather than skipping it at pick time, so the control also removes the fetch (`assets.files` 42 with, 41 without), which a pick-time skip could not. With spires off, **rn15, forest, plains and hills are bit-identical to the triangle** and Mountains reads `BoulderStone=49`; with them on the same 49 rocks read `BoulderStone=39 + RockSpire=10`, drawn 2.04 m to 3.34 m. The weighted draw is bit-identical to the uniform floor it replaces wherever every weight is absent and no biome gate is set, which is why the two trees and every ore boulder still pick the file they picked before. `hitUpM` 1.15 is derived from the player's arm and not from 0.55 of the asset: at 3.9 m placed, 0.55 of its own height is 2.15 m, over the player's head. Driven proof: `probes/wgart.js` walked to one, stood 2.88 m off, and the crosshair returned `Stone, 100% remaining` (`WG94_spire_after.png`).

**WG-98, THE RAISED ROCK BUDGETS VERIFIED IN THE CLIENT, AND THE ROCK LANE'S ARITHMETIC IS LOW BY 4x.** RN-243 priced the stone raise (142 to 426) at "about 16.5k triangles, 1.7% to 3.0% of frame" and said plainly it was unmeasured. Measured: **a harvest node's LOD0 is drawn FOUR times**, once in the near pass and once in each of three shadow cascades (`NodeBatch.makeBatch` sets `castShadow = true`). The proof is exact rather than inferred: `?rocks=0` at four sites gives **1,704 triangles per stone boulder, which is 426 x 4 to the unit**, and the spire pair moves Mountains by exactly `10 x (626 - 426) x 4 = 8,000`. The whole rock layer therefore costs 4.70% (rn15), **11.6% (Mountains)**, 1.07% (forest), 1.15% (plains) and 4.99% (hills) of frame triangles, and the boulder raise alone is about four times its estimate. Not a defect in the raise, which ART-DIRECTION.md licenses; a correction to its price. The shadow multiplier is the single biggest lever anyone has on node cost.

**WG-91, THE FOREST FLOOR, AND THE FRAME DISAGREES WITH THE PATCH ARITHMETIC IN SIGN.** `FOREST_DETAIL` gives Forest its own understorey mix at the same pre-scale sum (538,000, verified to the unit) over the same seven species. **`propsPlaced` is IDENTICAL at 25,711 both ways**, which is the instance-neutrality claim measured rather than argued. The understorey lane predicted +0.16% of patch triangles; the arithmetic on the shipped per-card counts (18/40/18/42/30/36/30) gives **+0.2022%** (+124.9 triangles on 694 m2, and no rounding convention reproduces +0.16%); the running frame gives **-4,179, i.e. -0.26%**. Both are right about different things: the patch is LOD0 cards at full density, the frame is the whole graded 78 m ring where most cards draw LOD2 and the LOD2 counts are nearly uniform, so re-weighting is almost free there while `Detail_FlowerSprig`'s drop removes real two-primitive instances (`propInstances` -229). The four unchanged sites reading bit-identical across four independent runs is what says -4,179 is signal rather than noise. Visible in the pair `WG91_forestfloor_{before,after}.png`: upright strokes with bare ground between them become low broad cover with litter.

**WG-92, THE TWO ROWS I REFUSED TO WRITE, AND THIS IS THE FINDING OF THE PASS.** RN-245's three Mountains stems (`Mtn_ScreeSheet` 0.155 m, `Mtn_TalusFan` 0.168, `Mtn_FrostShards` 0.160) get NO registry row. Wired at the retired pair's own density (1,820 pre-scale, exactly what WG-68 retired) they cost **+218,096 triangles (+34.7%) and +8 draw calls** at Mountains, and the standing frame at 1,820, at 1,050, at 520 and at ZERO is the same picture. The near-ground pair settles it: pitched 38 degrees at the dirt, **360 placed instances and +59,936 DRAWN triangles produce a frame I cannot tell from the frame without them** (`WG92_mtnclose_{none,520}.png`; the density ladder is `WG92_mtn_{after,1050,520,none}.png`). The triangle delta proves they are drawn, so this is not "they failed to place".

The mechanism is the part worth keeping: a 1.75 x 1.50 m plate that is 0.155 m tall has almost no projected area from a 1.66 m eye, and the size band it occupies is ALREADY filled by `Detail_PebbleScatter`, which Mountains runs at 64,000 pre-scale in `DRY_DETAIL`, i.e. **70x the scree ask**. **RN-247's corrected 0.174 m decoration cap and the visibility floor have collided**: under the cap a rock decoration cannot be seen, over it it lies to the crosshair, and the band between them is empty. Mountains' rock read therefore has to come from the NODES and from the terrain material, and the three stems are an asset-lane question rather than a client row. This is INSTRUMENTS.md's shape with the sign flipped: not a term measured where it cannot work, but a term ADDED where its read was already provided.

**WG-93, `Plains_PebbleA` DROPPED, and it is the cheapest win in the pass.** Authored 0.18 m and drawn to 0.279 m by `ScatterLook.scaleFor`'s 1.25 x 1.24, i.e. above the smallest harvestable silhouette, so it lies to a crosshair that cannot catch it. Dropping the row is **-27,192 triangles and -4 draw calls** at Plains, the most expensive site in the survey. The read survives on `Detail_PebbleScatter` at 26x the density. Re-authoring it under 0.174 m restores the row in one line, and that ask is Admin's to route.

**WG-97, THE SEEDED-SIBLING EVALUATION, ANSWERED WITH THE SPIRE AS THE EXPERIMENT.** Adding `rock_spire.glb` WAS the test of what a sibling file costs, because it is exactly that: a new node .glb whose materials already exist in the node batches. Measured both ways: **draw calls +0** (59/59 at Mountains; `nodeBatches` is 4 with and without), **geometries +0** (52, because a `BatchedMesh` is one geometry however many sub-geometries it holds), **VRAM +0.0 MB** on the published estimate (104.2 either way; the true figure is about 0.12 MB, under its rounding), **asset load +0 measurable** (3,105 ms against 3,140 ms, the difference in the wrong direction, which is the tell that it is noise), and **frame triangles move only by the DIFFERENCE between the two forms**, never by their sum, because a sibling is drawn INSTEAD of the original. **So the answer is yes, and the only real cost is download bytes.** One extra seed across all four ores is +470,860 B on a 743,412 B node payload; three extra seeds is +1.41 MB against 8.49 MB of total assets. **Recommendation: N=2 (one sibling per ore) unconditionally; N=4 only after the dead payload below is stripped**, because 35% of every sibling's bytes would be geometry nothing can select.

**WG-96, THE DEAD LOD PAYLOAD, MEASURED AND CLASSIFIED HONESTLY.** `NodeBatch.build` matches `_LOD0` only and admits only Full/Half/Low, so every `_LOD1`, `_LOD2` and `Stump_*` mesh in a node file is downloaded, parsed into a `BufferGeometry`, and never selected. Across the seven node files: **270,758 B of BIN, 29.73% of the shipped node payload and 35.45% of its geometry bytes**, plus about 84 kB of JSON descriptors (estimated; the BIN figure is exact, and the LOD0 and LOD1/LOD2 bufferView sets are disjoint in all seven files, so the subtraction hides nothing). For the six files actually fetched today it is 206,066 B, 27.72%. **The cost is NOT VRAM and must not be claimed as such**: three.js r0.185 creates a GL buffer in `WebGLAttributes.update`, reached only from `renderBufferDirect`, and these geometries are never submitted, so no buffer is ever allocated. It is network bytes, parse time, and JS heap retained for the session (`NodeField.templates` holds a strong reference and there is no `dispose`). **Owner note: the sibling tree lane landed `NODE_LOD1_M`/`NODE_LOD2_M` in `NodeBatch` mid-pass**, which if it selects those tiers converts dead payload into live payload and closes this item. That is theirs; it should be confirmed rather than assumed.

**WG-99, AN INSTRUMENT DEFECT FOUND BY USING IT.** `PropLibrary.stats().perMaterial[].live` is published as a per-role live count and is in fact a **monotonic high-water mark**: `release()` pushes the slot onto `free` and decrements `instancesLive` but never decrements `b.live`, and `acquire()` only increments it when it does not reuse a free slot. The tell was two different biomes reporting byte-identical per-material strings while `instances` differed by 3.3x. It is correct for the DW-28 question it was written for (what capacity does this role need) and wrong for "what is on screen here", which is INSTRUMENTS.md's "a value published for rendering is not automatically fit to measure with". The valid live counters are `props.instances` and `props.propsPlaced`, and this pass uses only those. Also noted: `stats().assets.bytes` reads 0 and is not populated.

**WG-100, `bush_scrub` IS NOT A SCATTER PROP AND MUST NOT GET A `Registry.ts` ROW.** `contracts.json` says `node_kind: "Tree"`, its `lod_nodes` are Full/Half/Low variants, its sockets are `socket_hit` and `socket_item_pop`, its clip is `Tree_Sway`, and it ships a `col_BushScrub`. Every one of those is a harvest-node fingerprint and none is a prop's. A biome-prop row would register `BushScrub_Full`, `BushScrub_Half` and `BushScrub_Low` as three unrelated stems and scatter the depleted ones as scenery. It belongs as a third `ART[NODE_KIND.Tree]` entry, which is the sibling tree lane's table, and the `weight`/`biomes` mechanism WG-94 added is exactly what stops that entry turning one tree in three into a 0.9 m bush.

**A property of the tier sampler, recorded because it cost a confused minute:** changing ANY biome prop density re-rolls which spec every draw in that biome lands on (`ScatterEmit.pickClustered` walks cumulative density), so a snow patch can move when a scree density changes. Two frames at different densities are not registered pictures of the same world.

**Known debts, stated:** the three Mountains stems and their cap collision are handed up rather than solved; the `Plains_PebbleA` re-author is an asset ask; `Registry.ts` was brought back under the 400-line cap by moving rationale into this file rather than by a file split, deliberately, because splitting the one file two world-gen lanes are both editing is the worst possible timing. Repo-wide `check-limits` is unchanged at 25 violations, none of them new.


### 6.6 WG-116 to WG-122: every tree is a node, and the LOD that made it affordable

All numbers from ONE binary (HEAD `cb160d6` plus this lane's files), a scratch
tree frozen with `git archive HEAD web` and served on a lane-private port, each
site at its own local noon with the scatter and tree backlogs drained. The
settle is 660 frames and that is a correction: `StatsProbe` keeps a 600-frame
ROLLING ring, so a 60-frame settle read a p50 that was nine tenths queue-drain
frames and made the tree world look twice as expensive as its control AT SITES
WITH NO TREES IN THEM. Frame time on this headless harness is still too noisy at
the 3 to 15 ms scale to attribute a few ms to anything, and it is reported
below only where the sign is unambiguous; the triangle and draw-call invariants
are the measurements this pass rests on.

**THE CRUX: what the LOD and the culling are worth** (Forest, 2,226 tree nodes
in a 620 m ring, one binary, four flag combinations):

| node path | triangles | vs LOD0-only | calls |
|---|---|---|---|
| `?nodelod=0 --nodecull=0` (what HEAD's node path does) | 7,558,472 | | 76 |
| `?nodecull=0` (LODs on) | 1,895,584 | **-74.9%** | 76 |
| `?nodelod=0` (culling on, one tier) | 1,871,556 | -75.2% | 71 |
| **shipping** (both on) | **1,365,739** | **-81.9%** | 71 |

Measured at the SHIPPING scale band. The same four rows at the band this pass
first shipped (WG-119, before the heights were re-derived off the bytes) read
7,574,172 / 1,718,204 / 1,747,331 / 1,313,024, i.e. taller trees cost 52,715
triangles and moved the saving by 0.8 of a point; the shape of the answer does
not depend on the correction and both sets are recorded so neither can be quoted
as the other.

The LOD does the heavy lifting and **it is an asset the project already paid
for**: every node `.glb` ships `_LOD0`, `_LOD1` and `_LOD2` meshes (the trees
ship twelve meshes each) and `NodeBatch.ts:191` matched `_LOD0` only, so a
791-triangle harvest tree drew its near geometry at 600 m while the 16-triangle
impostor in the same file was dead bytes. Loading them is a second axis on the
geometry id that the depletion variant already switches, so a tier change is one
integer write and no re-snap. Per-instance culling is the second, smaller half,
and the comment it replaces was RIGHT WHEN WRITTEN ("per-instance culling costs
more than it saves at this object count", written of the clearing's two dozen
nodes). **This closes R10 and it pays at sites with no trees at all**: the rocks,
the WG-94 spires and the spawn clearing gain 111,734 (Mountains spawn), 134,574
(hills) and 135,113 (beach) triangles from tiers they always had.

**The invariant table both ways, at all eight sites** (HEAD = `?trees=0
--canopy=620`, the WG-59 world; ship = the default):

| site | biome | ground m | trees | HEAD tris | ship tris | delta | calls HEAD/ship |
|---|---|---|---|---|---|---|---|
| rn15 | Hills | 860.9 | 799 | 980,891 | 965,364 | **-15,527** | 67/65 |
| current | Mountains | 4,667.8 | 0 | 518,847 | 518,647 | -200 | 57/57 |
| forest | Forest | 27.3 | 2,226 | 1,478,045 | 1,365,739 | **-112,306** | 63/71 |
| plains | Plains | 331.8 | 232 | 2,555,711 | 2,565,183 | +9,472 | 71/69 |
| hills | Hills | 2,077.2 | 0 | 787,550 | 787,550 | **0** | 59/59 |
| hills2 | Hills | 1,897.2 | 1 | 808,101 | 807,869 | -232 | 67/59 |
| beach | Beach | 12.2 | 0 | 780,148 | 780,148 | **0** | 59/59 |
| beach2 | Beach | 8.3 | 0 | 739,272 | 739,272 | **0** | 59/59 |

Programs, geometries and VRAM (104.2 MB) are IDENTICAL across HEAD, bare and
shipping at every site. Textures read 99/95/95 at the first two sites, which is
`props_canopy.glb` no longer being fetched, and converge once the other atlases
load. The +8 calls at Forest are node batches appearing in shadow cascades that
previously held no instance; that attribution is INFERRED from the pattern (the
same batches, more spread) and is not separately measured.

**THE NEGATIVE CONTROL, and it is the strongest one this lane has run.** A
second scratch tree frozen at pure `HEAD` was served beside the first and both
were driven by the same probe (`probes/treectl.js`, which reads nothing this
pass added). `--trees=0 --canopy=620 --nodelod=0 --nodecull=0` against pure HEAD:
**identical on every column at 8 of 8 sites** (calls, triangles, programs,
geometries, textures, VRAM, node pool count, instances, batches, refused, prop
instances, canopy props). An earlier run of the same pair differed by exactly
+2,048 at two sites, which is one terrain chunk's interior (32x32x2) and was
streaming variance between two browser sessions, not code; it disappeared on the
re-freeze and is recorded because the number looks like a bug and is not.

**What Reid sees at each candidate spawn** (trees live in the ring / realised per
km2; delivery is placed over expected over the SAME cells):

| site | trees | /km2 | delivery | read |
|---|---|---|---|---|
| forest (Forest) | 2,226 | 1,843 | 1.0000 | closed forest, the densest of the seven, `WG117_forest_trees.png` |
| rn15 (Hills, 861 m) | 799 | 662 | 0.9891 | woodland below the treeline; 13 trees inside 50 m, 16 more to 100 m |
| plains (Plains) | 232 | 192 | 0.9608 | isolated copses in open grass |
| hills2 (Hills, 1,897 m) | 1 | 0.8 | 1.61 | ON the treeline: open ground with one straggler |
| current (Mountains, 4,668 m) | 0 | 0 | n/a | bare, and bare BECAUSE the altitude term ran (4,633 cells) |
| hills (Hills, 2,077 m) | 0 | 0 | n/a | above the treeline; frame bit-identical to HEAD |
| beach / beach2 (Beach) | 0 | 0 | n/a | THE DESERT is untouched; frames bit-identical to HEAD |

Delivery at hills2 is 1.61 over an expectation of 0.62 trees, which is one tree
against a fractional ask and is quoted rather than hidden. Every other site with
trees sits inside its fair-draw 4-sigma band with both ratio sides over the same
cells (RN-15's lesson).

**THE REFUSALS, and one of them was broken until a count caught it.**

| gate | reachable? | evidence |
|---|---|---|
| treeline | YES | 4,633 cells emptied at the Mountains spawn, live 0 |
| standing water | YES | 4 cells refused at the spawn pond |
| clearing keep-out | YES | 14 refusals at a Forest spawn |
| slope | **NO, and it cannot be** | see below |

The clearing keep-out read 0 at a spawn where arithmetic said it must fire about
six times, and the cause is worth keeping: it compared a candidate expressed as
a unit direction times the body radius against a placed node's position ON THE
SURFACE, which at Forest is 27 m further out, so every comparison beat a 6 m
keep-out before the two were even in the same place. A zero from a gate that
cannot fire reads exactly like a zero from a gate with nothing to catch.

**THE SLOPE GATE CANNOT FIRE, and this time that is proved rather than
observed.** `TREE_MIN_SLOPE_COS` refused 0 at all eight sites, which is the same
reading WG-63 got from the canopy tier's copy of the same constant. Eight gentle
sites cannot tell "the gate works" from "the gate is not wired", so
`probes/treeslope.js` went looking: a coarse global pass hill-climbed to a sample
spacing finer than the gate's own 3 m arm, 26,120 oracle samples. The steepest
ground ANYWHERE on Forge is **57.92 degrees** (lat 58.0, lon 16.5, 2,865 m) and
the steepest ground BELOW THE TREELINE is **29.15 degrees** (lat -77.0, lon 2.6,
1,676 m), against a 43.95 degree gate. Everything steep is bare because it is
high. The constant is KEPT, for one case the census cannot reach: the slope is
sampled through `editsHandle`, so a dug pit or a levelled terrace makes locally
steep ground under a live tree ring. **That case is untested and is owed.**
Lowering the constant until the counter moved was rejected outright: it would
refuse trees on ground that is fine for trees, to flatter an instrument.

**SIZE IS YIELD, and the band came off the bytes on the second attempt.** A
tree's art scale is a linear map of /core's grade, and grade is the same number
`InitialAmount` is multiplied by, so the world holds trees from 20.0 to 39.98
wood and the bigger one is worth more. The first band was derived from
`NodeField`'s header calling a harvest tree "a 6.5 m tree"; measured off the
shipped glb the conifer is 6.50 m and the broadleaf is **5.00 m**, against the
retired canopy species' 12.00, 16.50 and 10.50 m, so the median drawn tree was a
third too short and `WG117_rn15_trees.png` showed a hillside of scrub while
every counter read 0.9891. Re-derived to [1.25, 2.45] the conifer spans 7.0 to
18.2 m (median 12.0) and the broadleaf 5.4 to 14.0 m. Cost of the correction:
+8,383 triangles at rn15 and +45,943 at forest, both still well under HEAD.

**PERSISTENCE, cell-keyed, proven by a real reload.** `SaveSlot.trees` is
`[latCell, lonCell, slot, remaining]`, additive and optional, and SAVE_VERSION is
deliberately unmoved (WG-29's rule: the check is `!==`, so a bump orphans every
existing world). It rides the `writeSlot` choke point that PS-13 to PS-15
established, so both save routes carry it and no second writer was introduced.
`reload.mjs --setup=probes/treereload.js` walks 70.1 m to a STREAMED tree (the
70 m floor is asserted, because chopping one of the clearing's spiral trees would
prove the OLD index-keyed path and pass while this one was broken), swings three
times for 18 wood, saves, and does a real `page.reload()`. **The tree comes back
at the same body-frame position to the bit and at `remaining`
12.358138084411621 exactly**, and it comes back through the PENDING path
(`trees: 0, treesPending: 1` in the ledger, `drainedOnRestore: 1`), which is the
harder half: the diff was parked at load and applied at the moment the tree
materialised. `--params=` was added to `reload.mjs` for this, because the default
spawn is above the treeline and a tree probe there would have had nothing to chop.

**SCALING, and the debt this pass pays and the one it leaves.** `known` is
bounded by the ring rather than by the walk: a cell that streams out drops its
UNTOUCHED trees (`forget`), because a full tree carries no diff and regenerates
identically, and only a chopped one is kept for ever. Measured over the eight-site
tour: `known` equals `live` at every site and `forgotten` climbs to 3,465, so a
long walk does not grow the map or the autosave. **`RockField` still keeps every
rock it ever materialised and should inherit this**; at 49 rocks it does not
matter and at tree densities it would. `NodeField.update` still pays one WASM
state read per PRESENTED node per frame (about 2,200 at Forest), which is WG-72's
named debt now much larger; it did not show above the harness noise and is left
measured rather than fixed.

**Pictures beside numbers** (DW-7, and this pass needed it twice):
`docs/screenshots/WG117_{forest,rn15}_{trees,head}.png` are the pairs at the two
sites that change, and `WG117_forest_trees.png` is the feature in one frame: a
closed forest with the accent prompt reading "Left click Harvest Wood, 100%
remaining" on a tree that used to be scenery. `WG117_forest_bare.png` is the
`?trees=0` control.

## 7. Open questions & risks
- **R15 (new, WG-141, 2026-08-02, ADMIN SEQUENCING, and this is the one to read first): the moon is live in `/core` and NOT YET in the browser, on purpose.** `web/wasm/dist/of-core.wasm` and `web/wasm/test/expected.json` are regenerated in the working tree (and every screenshot in this pass was taken against them) but are **deliberately NOT COMMITTED**. Two physics commits, `9e19ddc` and `ebdeed8`, landed after my build and touch `core/include/of/orbital.h` and `core/include/of/transfer.h`, which the wasm build reads; committing my binary would ship a wasm missing their Cinder-transfer work, which is a revert wearing a binary. The committed header and the committed binary therefore disagree until **someone rebuilds from a settled tree**, at which point the fixture moves by exactly 24 leaf values, all of them moon values, and Forge does not move at all. That rebuild is an Admin sequencing call, not mine to make alone.
- **R16 (new, WG-147, 2026-08-02, gameplay via Admin): 51 starter harvest nodes place on Cinder regardless of body, and 14 of them are trees.** The streaming fields are correct and prove it: `trees.live` 0 and `rocks.live` 0 at all four probed sites with 5,984 and 632 `biomeZeroCells`, so `TREE_DENSITY_KM2`'s moon zeros are working exactly as written. The nodes that DO appear are the fixed golden-angle starter spiral (`NodeField.ts`, WG-59), which is placed once at world init and consults no biome and no body: measured `TreeBroadleaf` 8, `TreeConifer` 6, plus 37 ore boulders, unchanged across four teleports. **Broadleaf trees on an airless moon.** Whether Cinder should carry a starter set at all is a gameplay decision, not a world-gen one (Reid arrives by rocket having already done the tutorial on Forge), so this is routed rather than patched.
- **R17 (new, WG-141, 2026-08-02, unattributed): thin bright streaks follow the terrain contours** in `docs/screenshots/WG141_ground_craterfloor.png` at grazing light. Could be the detail-card layer, could be chunk skirts, could be the moon palette's micro-detail row. Not diagnosed, not blamed, and recorded so it is not silently absorbed into "the moon looks odd".
- **R18 (WG-141, CLOSED 2026-08-02 by WG-149 as to the instrument; the cost itself stands as a watch item.)** The meter is fixed: crater cell evaluations are counted as their own unit and `terrain_probe` refuses to present the valueNoise number as a cratered body's cost. The measured blindness was worse than the estimate in the original text below: **10 valueNoise calls against 113 crater cell evaluations per sample**. What remains open is not the instrument but the number it now reports honestly, and it still needs measuring in the BROWSER rather than natively, because nothing has yet exercised the streaming budget on a body whose 200 km radius makes quads physically smaller at the same depth. Original text:  **the moon costs 2.21x what it did and 2.93x what Forge does** (492,507 verts/sec against Forge's 1,442,346), measured by `procgen_bench`. Nine crater rungs is the reason and the confined variant is already what keeps it from being far worse. Not yet a problem, because nothing has measured the streaming budget on a body this small, where a 200 km radius means quads are physically smaller at the same depth. It needs measuring in the browser before anyone calls it fine. Related instrument note: `OF_COUNT_NOISE` counts `valueNoise` only, and the crater fields hash cells directly, so **the noise counter understates moon cost by roughly ten valueNoise-equivalents per sample** and must not be used to budget this body.

- **R11 (new, WG-121, 2026-08-01, owed by this lane): the tree slope gate has never been seen to fire.** Proved unable to fire on the DESIGNED surface (steepest sub-treeline ground 29.15 degrees against a 43.95 degree gate, 26,120 samples). Its only reachable case is the EDITED surface, because the sample goes through `editsHandle`: dig a pit or level a terrace under a live tree ring and the ground under a candidate becomes steep. Needs a driven dig probe. Until then the gate is carried on an argument, not a measurement.
- **R12 (new, WG-122, 2026-08-01, rendering, and it is WG-63's ask restated with a second customer): the tree ring's 620 m edge pops.** Nothing in the client can draw a tree past that, and a distance fade is now structurally refused (WG-122), so the only remaining answer is the far-field impostor layer. This lane has no billboard, impostor or card mechanism to build on.
- **R13 (new, WG-119, 2026-08-01, Blender via Admin): `props_canopy.glb`'s three tall species cannot become node art.** They have no `Full`/`Half`/`Low` depletion variants, so the parasol and fir forms the RN-271 flora pass authored are currently unreachable by the harvest path and the world's big trees are scaled harvest trees instead. Three variants each (plus the existing LOD tiers) would let `NodeArt` carry them and would put those silhouettes back.
- **R14 (new, WG-120, 2026-08-01, owed by this lane): `RockField` never forgets a rock.** `TreeField.forget` drops untouched entries when a cell streams out; the rock stream keeps every rock it ever materialised, and `serialize` pays a WASM read per entry. Immaterial at 49 rocks; the same shape at any density increase is unbounded. `RockField.ts` belongs to the sibling world-gen lane this pass, so this is routed rather than done.
- **R9 (new, WG-92, 2026-08-01, needs the asset lane via Admin): the decoration cap and the visibility floor have collided in Mountains.** RN-247's corrected authored cap is 0.174 m; the measured floor for a rock decoration to be distinguishable from the `Detail_PebbleScatter` layer the biome already runs at 70x the density is ABOVE it. So no Mountains rock decoration can both be seen and be honest, and the three RN-245 stems are unwired. Resolving it needs ONE of: a Mountains-specific `DRY_DETAIL` with less gravel, a decoration form whose read is vertical relief rather than coverage, or an argued exemption. Not a client change on its own.
- **R10 (WG-98, CLOSED 2026-08-01 by WG-118): a harvest node's LOD0 is paid FOUR times.** Answered in this lane rather than routed: the node batch now loads the `_LOD1` and `_LOD2` meshes the assets always shipped and culls per instance, so the four draws are of the tier the distance deserves. Worth 111 k to 135 k triangles at sites with NO trees in them, which is the rocks and the spires and the clearing collecting on it. Original text:  `NodeBatch` sets `castShadow = true` and there are three cascades, measured exactly (1,704 = 426 x 4 per stone boulder). At the Mountains site the rock layer is 11.6% of frame triangles. Taking nodes out of the far cascades, or giving them a shadow-only proxy, is the biggest single lever on node cost and it is rendering's call, not mine.
- **R1:** Heightfield↔voxel-patch seam (rendering + collision continuity at dig boundaries). **API seam now built** (Spike 1 §4.4: `bFromVoxelPatch` in every result struct, `ApplyPatchOverride` no-op stub — callers branch on it today); implementation deferred to Phase 4 (D-005). *(Spike 1 WR4)*
- **R2:** Noise cost vs streaming budget at low LOD over large bodies. **Mitigated by design:** LOD octave-gating (Spike 1 §2) + per-tick generation budget (§3.2); validate gate WV8. Still open until measured in-engine.
- **R3 (Admin Q4):** keep voxel door open without paying for it in v1. **Settled** by R1 seam above.
- **R4 (new, Spike 1 WR3 → Admin):** numeric body constants (radius/μ/SOI/spin, Spike 1 §5) are **shared** with core-engine's `FReferenceFrame` but core-engine pinned only the *shape*, not values. World-gen proposed Forge (planet) / Cinder (moon) numbers; **Admin to confirm single source of truth** so the two domains don't diverge. No contract conflict — value agreement only.
- **R5 (new, Spike 1 WR5 → physics):** physics collision against heightfield via **analytic queries** (built) vs **baked per-quad collision meshes** — negotiate with physics. Contract supports both; collision-mesh emission can be added to `FTerrainChunk` later without breaking callers.
- **R7 (new, 2026-07-26, the FIFTH instance of the surface class — Admin asked for a position):** the ONE oracle answers with **two SHAPES**. `solidCell` is a binary 1 m occupancy grid (a cell is solid when its CENTRE is at or below the designed surface); `surfaceHeight` is a smooth continuous field. A binary lattice can only approximate a smooth surface to within half a cell diagonal, so the shell stands up to **0.87 m proud** of the walkable ground (measured: air 0.15 m above the surface reads solid on 60.6% of walking ticks — ARCHITECTURE 15.2 item 93). **Position: neither offered option is right.** Changing the quantisation in `surface_field.h` CANNOT succeed, because the mismatch is not a bad threshold, it is inherent to the representation: any rule leaves a boundary layer up to one cell thick that one shape claims and the other does not, so moving the threshold moves WHERE the disagreement is and not WHETHER it exists (and it would re-baseline every voxel pin for nothing). Per-consumer reconciliation is what produced five surfaces: `VoxelCollision.solidForWalker` is correct for the walker and is the SIXTH definition unless it is promoted. **The third option: name both shapes in the oracle and make the choice a parameter rather than a rediscovery.** `surface_field.h` should publish `solidCell` (the VOXEL shell — what the mesher draws and what a dig carves; you must draw whole cells) alongside a `solidBelowSurface` (the shell INTERSECTED with the smooth surface — what a body occupies), with a header note stating the bound explicitly, and a ctest that ASSERTS it: for a sample of directions the two answers may differ by at most half a cell diagonal. A property test on the SIZE of a known disagreement is what turns "silently 0.87 m" into "loudly, at most 0.87 m". **WG-22 hit the same class from the other side:** `levelArea` decides per cell CENTRE while `derivedRaisingAt`/`derivedLoweringAt` probe per COLUMN in 1 m steps, so a rim column is legitimately half cut and reports its original height (15.2 item 99). Same root, opposite sign. Not changed this pass (Admin did not ask for a core change); proposed for the next.
- **R6 (new, Spike 1 WR1):** cube-face-corner seam determinism under tangent-warp float error — mitigated by canonical-edge ownership (lower faceId owns a shared edge so both faces read identical doubles); validate WV2.

## 8. Subagent delegation log
| Date | Subagent task | Status | Outcome |
|------|---------------|--------|---------|
| 2026-08-02 | **WG-141, measure Cinder's height field BEFORE the rewrite: give `terrain_probe` a `--body` flag and report the wavelength sweep, slope spectrum, biome histogram and cost against Forge as the reference (general-purpose)** | Done | **The most valuable subagent result this lane has had, because three of its findings were things I had not asked about and one of them was in code I had already written.** It confirmed the headline arithmetic (finest content 1,111 m on Cinder against 60 m on Forge, and it independently derived the crater diameters and reconciled the measured knee with the predicted wavelength rather than just asserting agreement). Then it went further: it **refused to trust the RMS column**, worked out that a single sample pair straddling a jump gives `rms = J/sqrt(K)`, back-solved J = 1,596 m and went looking for the cliff, which is how the **discontinuous crater profile at t == 1** was found; it caught the **`maxReliefM` overrun as systematic across three seeds** and noted that the one existing assertion sampled too few points to see it; and it found the **moon design layer still switching gain on the discrete biome**, the WG-25 pathology one body over, quantified as a 120 m and a 40 m wall and cross-checked against gaps in its own biome histogram. It also caught that `OF_COUNT_NOISE` cannot see `craterField` at all, so the cost counter understates the moon by ~10 valueNoise-equivalents. It correctly **discarded its own first capture** on noticing it had compiled against a half-edited header, and shadowed HEAD into a scratch include dir rather than touching my working tree. |
| 2026-08-02 | **WG-147, wire body plurality into the client: one `BodyId`, one creation rule, the field on both worker protocols, a `?body=cinder` flag (general-purpose)** | Done | Nine files, 64 insertions, 7 deletions, `tsc --noEmit` clean, no new file over the line cap. It verified the claim that mattered rather than restating it: after the change `_of_body_create_*` appears at exactly two call sites in `web/src` plus the two `heap.ts` declarations, which is the real test of "one rule". It found that step 5 needed no new parameter because `probeWorkerOracle` already took `cfg`, checked my `/core` line citations against the actual file rather than copying them, and **listed every tracked file dirty from other lanes that it had not touched**, which is exactly the discipline that kept the commit clean when it turned out a physics lane was mid-flight in the same tree. |
| 2026-08-01 | **WG-118, map the canopy prop pipeline: Registry specs, Scatter's canopy branch, PropLibrary's two LOD tiers and the shipped triangle counts (Explore)** | Done | The pass rests on two facts it returned. (1) `PropPart` carries exactly TWO geometry ids and its regex folds any non-zero LOD digit into the far slot, so the prop path could not have been the model for a three-tier node LOD. (2) The node .glbs ship `_LOD0`/`_LOD1`/`_LOD2` (twelve meshes per tree) and `NodeBatch.ts:191` matches `_LOD0` only, i.e. the whole far-field saving was already authored and unread. It also returned the canopy asks per biome, which became the density table verbatim after the derived one failed the picture |
| 2026-08-01 | **WG-116, operating manual for the smoke harness, the debug handles, the Visit-site table and the save path (Explore)** | Done | The map this pass was driven from: `run.mjs`'s allow-list (a flag not registered there is silently dropped), the probe-server convention, `rocks.js`/`rocksite.js`/`rockreload.js` verbatim as the templates, `reload.mjs`'s fixed URL (which is why `--params=` had to be added), the `writeSlot` choke point and every call site of `RockField.serialize`/`restore`/`coreIndices`. Its warning that `Config.ts` was already over the 400-line cap was correct and saved a wrong baseline |
| 2026-08-01 | **WG-96 / WG-97, measure the node .glb payload, the detail-card triangle counts and the new prop stems from the SHIPPED BYTES (general-purpose, pure python, no Blender, no browser)** | Done | The numbers the whole cost half of this pass rests on, and it was right to refuse two of my framings. It established that the LOD0 and LOD1/LOD2 bufferView sets are DISJOINT in all seven node files, so the dead-payload subtraction hides nothing (270,758 B, 29.73%); it traced three.js r0.185 from `WebGLAttributes.update` back to `renderBufferDirect` and **refused to call the dead payload a VRAM cost**, classifying it as network, parse and retained JS heap instead, which is the opposite of what I had assumed; and it **falsified the understorey lane's +0.16% neutrality figure**, showing the true value is +0.2022% and that no rounding convention reproduces +0.16%. It also caught, unasked, that `Mtn_SnowPatch` is authored at 0.220 m and therefore over the 0.174 m decoration cap (it is `OF_Ice`, so outside the mineral rule, but that should be a decision rather than an omission), and that `rock_spire.glb` and `bush_scrub.glb` were shipped to `web/public` and never fetched because nothing in `ART` named them. |
| 2026-07-30 | **WG-67, survey the probe harness, reload runner, Visit-site machinery and node debug handles (Explore)** | Done | The map this pass was built on: run.mjs's arg allowlist and eval wrapping, reload.mjs's fixed phase-2 reader and the setup-gated assertion pattern (chestsave/padclear precedent, which the ROCKSAVE block copies), `VisitSites.ts` as the one teleport authority with the probe pressing the real button, `of.game().nodes` = `NodeField.stats()` and `of.nodes()` rows, and `npm run check`'s four gates. Returned every fact with file:line and flagged that probes reach scatter stats through a MERGE of two stats objects, which is why the rock stream publishes its own block instead of joining it. |
| 2026-07-28 | **WG-59, survey the harvest nodes, the picker and the refusal style (Explore)** | Done | **This is what settled the design decision.** Returned the golden-angle spiral verbatim (`NodeField.ts:135-162`, 14 nodes, 16 m to 56 m) and the two facts the brief did not have: the crosshair already goes accent-coloured on a live node inside 4.0 m and a prompt panel already names it and its percentage, and `NodeField.pick` is a ray-versus-sphere loop over the node array that touches no prop geometry. It also found that a swing at nothing is SILENT (`Interact.ts:131-136` increments a miss counter and flashes nothing), which is the one cross-domain ask this pass hands up. |
| 2026-07-28 | **WG-59, survey prop instancing, batching and LOD (Explore)** | Done | The economics of the whole pass. Batches are keyed by MATERIAL NAME alone, so trees using roles `props_forest.glb` already contributes cost zero new batches; `place` is three's `setGeometryIdAt` and LOD is chosen by the caller at build time; a float `lod2=` is a COLLAPSE decimate that destroys a conifer tier, so foliage must pass a callable. It also established there is NO impostor or billboard mechanism anywhere in the client, and that the harvest trees carry authored 16-triangle `_LOD2` impostors that `NodeBatch.ts:190` has never loaded. |
| 2026-07-28 | **WG-60, author `props_canopy.glb` (general-purpose)** | Done | Three species, 932 triangles total, four materials, validator green on the first iteration. **It verified the design rule on the EXPORTED BYTES rather than on its own source**, correctly noting that `Parts.fit` rescales z afterwards: lowest foliage vertex at 0.550 / 0.621 / 0.632 of height, LOD0 and LOD2 within 0.004. It flagged one thing honestly and it is the right flag: `Canopy_Broadleaf` separates from `TreeBroadleaf` by SCALE and crown height rather than by construction, so it is the one pair where a player at 200 m is reading a size difference. Not changed here; the crown shape is the knob if it reads wrong. |
| 2026-07-28 | **WG-50/WG-54, map the terrain chunk draw path (Explore)** - decide whether the noise phase must live in the chunk wire format or can ride a per-chunk channel | Done | **This is what turned the brief's premise over.** Established with file:line that terrain chunks are NOT meshes: the whole resident set is two `BatchedMesh`es (`ChunkBatch.ts:59`, `ChunkGeometryPool.ts:30-31,48-49`) sharing one `ShaderMaterial` whose own header states per-chunk state is zero (`TerrainMaterial.ts:1-8`), with no `onBeforeRender` in the terrain path, so **a per-chunk uniform would give back DW-11**. But it also found the two channels that need no wire-format change: **`aFadeT0`** (`ChunkBatch.ts:50,122-129`), a per-chunk float allocated JS-side and `fill()`ed across the slot and demonstrably absent from `ChunkBlobLayout`, and the **`MachineBatch.ts:131` batching-id `DataTexture`**, already proven on r185 here. And it traced the float64 anchor end to end (`ChunkView.ts:21,70` from `TerrainProtocol.ts:91-92` from `HEAPF64` from `centerUniverse.pos`, C++ `double`), confirming the only narrowing is later and only for the engine transform. |
| 2026-07-28 | **WG-55, re-run `spawn_probe.cpp` and score candidate sites below the snow line** | Done | Shortlist of seven sites in section 6.1, grouped by biome. **It found two defects in the instrument before it found a site**, both of which would have made the eventual move wrong: `spawn_probe` hard-filters to Plains/Forest/Hills (`:134-136,425`) so it **could never have produced the current Mountains site**, and `probePadHere` (`:487-491`) moves `homeDir` without moving `pondDir` or zeroing `pondRadiusM`, so since WG-36 its pad verdict carries **my own pond's 1.08 m wading slope as a defect of the terrain**. Also established that **there is no Desert biome** (the set is Ocean/Beach/Plains/Forest/Hills/Mountains/Polar, `biome.h:53-67`), that Beach is invisible to the probe because `minAlt = 200.0` cuts the band, and that **the prop snow line is the Mountains biome boundary and not an elevation**, about 2,700 m designed, with the shader's own snowfield a separate thing 2.5 km higher. Enumerated the 18 sites `homeDir` is encoded in and the two ctests that pin the current literal by name. |
| 2026-07-28 | **WG-55, photograph the shortlist** | See report | Driven captures of the seven candidate sites plus the current spawn as the control, same camera and settings, for Reid to pick from. |
| 2026-06-14 | Scoped research — cubed-sphere quadtree terrain LOD: seam/crack mitigation + KSP PQS approach + cube→sphere mapping | Done | KSP PQS = faceted cube + per-face quadtree, distance-driven subdivide/coalesce. Seam trio = **shared-edge determinism + skirts + balanced quadtree** (≤1-level edge-neighbor diff); edge-stitch index buffers are a cleaner-but-costlier alt (deferred). Cube→sphere: **tangent (equal-angle) warp** beats raw normalize for uniform tessellation (COBE/quad-sphere). → drove WG-5/6/7/8 and Spike 1 §1–§2. |
| 2026-06-14 | Reconciliation RC-2 + RC-7 (Admin) — append-only additive fields on pinned contracts | Done | **RC-2:** added `MieScaleHeightM`/`MieScatteringScale`/`MieAnisotropy` to `FAtmosphereProfile` (Spike 1 §5); Forge = `1,200 m / 1.0 / 0.80`, Cinder N/A (airless). **RC-7:** added `SlopeRad` (rad) + `SurfaceHardness` (normalized `[0,1]` `double`) to `FTerrainContact` **and** `FTerrainHit` (Spike 1 §4.3), populated from the existing `Normal` + `MaterialId`. All end-of-struct appends → no caller breaks. Logged as **WG-10**. Did NOT touch rendering's/physics's files. |
| 2026-06-14 | **Phase-1 C-1 + C-6 close (Admin brief)** — pin `FDepositNode`; confirm world-gen's `IPersistable` slice | Done | **C-1 (WG-11):** pinned `FDepositNode` in Spike 1 **§4.5** (new) — id/`Position`(`FUniverseCoord`+`FFrameId`)/lat-lon/`SurfaceNormal`/`FBodyId` + **opaque gameplay-owned `ItemId` resource** (C-3 — referenced, NOT defined; collapsed gameplay's proposed `DepositTypeId` into the shared id space) + Grade/InitialAmount/mutable `RemainingAmount`; deterministic per-body seeded placement (`GenerateDeposits`, position-hashed) + query surface (`GetDeposits`/`QueryDepositsNear`/`GetDeposit`/`ExtractFromDeposit`); **Cinderite `ItemId` placed only on Cinder** (WG-4 off-world hook, §4.5.3); deposit rows added to §5.1/§5.2 body tables. **C-6 (WG-12):** added Spike 1 **§4.6** confirming world-gen implements `IPersistable` (chunk-scoped, `WorldGen`) over `(depositId, remaining)[]` per region, `depositId = hash64(bodySeed, region, localIndex)` — exactly persistence §2.1's id; terrain/placement regenerate from seed (PS-7/WV1). **Edited ONLY world-gen files** (spike1-worldgen.md, world-gen.md). Cross-domain (C-3 `ItemId` registry, three-way `FDepositNode` confirm) routed via Completion Report, not edited. |
| 2026-06-14 | **Wave-0 headless core BUILD (Admin brief)** — implement the cubed-sphere quadtree heightfield core; crack-free property proven bitwise (WV1–WV3) | Done | Built `core/include/of/cubed_sphere.h` (tangent cube→sphere warp WG-5; integer vertex lattice → `unitDir` pure in `(faceId,ix,iy,level)`; position-hashed height `(bodySeed,dir)` WG-6; 3-layer noise WG-7 planet ridged-mtns / moon crater-field; `generateQuadMesh`; `SampleTerrainHeight`; Forge/Cinder params §5) + `core/tests/test_world_gen.cpp` (10 tests) + a parallel `world_gen_tests` target in `core/CMakeLists.txt`. **Crack-free seams asserted BITWISE** (uint64 reinterpret `==`): neighbour same-depth edges (E-W + N-S), parent-midpoint vs child-corner, same-seed regen — all bit-identical. Built clean (`-Wall -Wextra -O2`, g++ 16.1 / Ninja); **`world_gen_tests` = 10 tests / 3582 checks pass; `ctest` = 3/3 suites green** (core-engine + factory-sim unaffected). Touched ONLY world-gen's new files + added (not modified) the test target. Logged **WG-13**. |
| 2026-06-15 | **Wave-1 terrain STREAMING + LOD + procgen-opt BUILD (Admin brief)** — build the headless terrain-streaming/LOD manager the UE renderer consumes (resident-set selection around a moving observer, ready/evict under a gen budget, crack-free across LOD seams), and optimize the procgen hot path bit-identically | Done | Built `core/include/of/terrain_stream.h` (`TerrainChunk` == pinned `FTerrainChunk`; `selectResidentSet` distance-based + balanced LOD; `TerrainStreamer::updateStreaming`→ready/evict under `genBudget`; skirts for LOD-seam cracks; `makeObserverLatLonAlt`/`onOriginRebased`) + `core/tests/test_terrain_stream.cpp` (8 tests: selection determinism, LOD-deepens, balanced `<=1`-step, **crack-free across a LOD boundary proven BITWISE + skirt strictly-inward**, same-depth edge bit-identical, eviction-on-recede, **gen-budget respected + converges to the unbudgeted set**, **procgen bit-identical**) + `core/tools/procgen_bench.cpp`. Optimized `cubed_sphere.h::valueNoise` by hoisting the shared hash-chain prefixes (bit-identical, ~half the hash calls): **Forge 2.62× (909k→2.39M verts/sec), Cinder 1.47×**, checksum-verified vs a verbatim original copy. Consumed `cubed_sphere.h` mostly read-only (only `valueNoise` edited, output bits preserved). **8 tests / 52,465 checks pass; `ctest` = 17/17 suites green.** Logged **WG-16**. |
| 2026-06-15 | **Wave-2 deposit gen BUILD (Admin brief)** — implement the seeded deposit placement + mining query surface (C-1 / WG-11) on the cubed-sphere bodies, Cinderite Cinder-only (WG-4) | Done | Built `core/include/of/deposits.h` (`FDepositNode` per Spike 1 §4.5.1 with `Resource` = bare `uint16_t` `ItemId`, no gameplay.h; `GenerateDeposits(body, bodySeed, bodyFrame)` = deterministic position-hashed Fibonacci-sphere placement, surface-snapped via `sampleHeightField`, stable hashed `Id`; `DepositCatalog` with `GetDeposits`/`QueryDepositsNear`/`GetDeposit`/`ExtractFromDeposit`; `PickResource` Cinderite-only-on-Cinder) + `core/tests/test_deposits.cpp` (7 tests) + a parallel `deposits_tests` target in `core/CMakeLists.txt`. Consumed `cubed_sphere.h` **READ-ONLY** (untouched). **7 tests / 1620 checks pass; `ctest` = 11/11 suites green** (all prior suites unaffected). Determinism bitwise; Cinderite-only-on-Cinder confirmed (Forge 732 nodes / 0 Cinderite; Cinder 210 / 133 Cinderite). Logged **WG-14**. |
| 2026-06-15 | **Wave-3 BIOME + terrain-design + planet-wide RESOURCE BUILD (Admin brief)** — add a planet-scale biome + terrain-design + deterministic planet-wide resource layer so the planet has *designed*, varied terrain instead of uniform noise | Done | Built `core/include/of/biome.h` (`biomeAt`/`biomeAtLatLon` classifier {Ocean,Beach,Plains,Forest,Hills,Mountains,Polar + moon Regolith/Highland/CraterFloor}, climate = latitude→temp + position-hashed temp/moisture + elevation; `sampleDesignedHeight`/`SampleDesignedTerrainHeight` per-biome relief modulation layered ADDITIVELY on the unchanged canonical height — mountains ridge high, plains flatten, ocean carved below datum; `materialForBiome`→uint16 + `hardnessForBiome`; `GenerateBiomeDeposits` planet-wide biome-biased ore + `BiomeResourceField::QueryRegionDeposits` cube-quad region query; Cinderite moon-only WG-4) + `core/tests/test_biome.cpp` (11 tests / 8361 checks) + a parallel `biome_tests` target. Wired `terrain_stream.h` `materialId` `uint8_t`→`uint16_t` from the biome (additive). **`cubed_sphere.h` UNTOUCHED — NO canonical height bits re-baselined, NO pinned test changed.** **18/18 ctest green** (was 17). Logged **WG-17**. |
| 2026-06-16 | **TERRAIN-DEFORMATION layer BUILD (Admin brief)** — build a deterministic, persistable terrain-deformation layer (Valheim-style digging: pickaxe + automated miners lower/remove terrain up to a deep max depth; edits persist) in headless `core/` over the procedural base height | Done | Built `core/include/of/terrain_deform.h` (sparse edit store `std::map<cellId,double>` over a quantized cubed-sphere `DeformCell` at `kDeformLevel=18`; `deformedHeight` = `sampleDesignedHeight − edit` clamped to `base − maxDigDepthM` default 80 m, BIT-IDENTICAL where unedited; `digBrush` soft-falloff pickaxe holes; `drillStep` narrow shaft to bedrock; `editAt`/`depthDugAt`/`atBedrock`/`editedCellsInQuad`/`quadHasEdits` queries; `serialize`/`deserialize` templated over the persistence cursor style) + `core/tests/test_terrain_deform.cpp` (7 tests: brush-falloff + max-depth clamp, base bit-identity + clamped subtraction, drill-to-bedrock, quad-overlap query, persistence round-trip, op determinism) + a parallel `terrain_deform_tests` target. `cubed_sphere.h` + `biome.h` consumed **READ-ONLY (UNTOUCHED)** — undeformed path bit-identical, no pinned test changed. **7 tests / 251 checks pass; `ctest` = 20/20 suites green** (was 19). Logged **WG-18**. Touched ONLY world-gen's new files + the new test target + this doc. Follow-up flagged: route the `bFromVoxelPatch` collision branch through `deformedHeight` (physics/terrain_stream wiring). |
| 2026-07-05 | **RETHINK R1, SINGLE SURFACE AUTHORITY ("surface oracle") BUILD (Admin brief):** build the one `/core` surface authority (`height = designed − voxel-lowering`, single bedrock clamp; voxel solidity from the same base; deform demoted to a derived view) and wire every consumer, eliminating the five competing surface definitions. HEADLESS only. | Done | Built `core/include/of/surface_field.h` (`baseHeight` ≡ `sampleDesignedHeight`; `derivedLoweringAt` = top-anchored removed-voxel run, the UE `OpenColumn` promoted into core; `surfaceHeight = clamp(base − lowering, base − maxDig)`, the ONE 80 m clamp; `solidCell`/`solidAt`; `SurfaceField` binder + `loweringFn()`) + `core/tests/test_surface_field.cpp` (7 tests: base≡designed bit-identity, designed-based voxel solidity incl. the raw/designed gap case, dig-column lowering, single bedrock clamp, tunnel-no-lowering, pit-to-first-solid, determinism + SurfaceField view). **Switched `voxel_terrain.h` `isProcSolid`/`surfaceRadiusAt` RAW to DESIGNED** (now `sampleDesignedHeight`, +biome.h include). **Wired consumers:** `cubed_sphere.h generateQuadMesh` gained an optional leaf `HeightFieldFn baseHeight` (default null=RAW, bit-identical); `terrain_stream.h buildChunk` binds it to `sampleDesignedHeight` (mesh now draws the designed surface); `surface_walk.h` `surfaceHeightM()` now reads `surfaceHeight` via new `setVoxelEdits`; `deposits.h` `GenerateDeposits`/`LayoutTestArea`/`ForBody` gained an optional `SnapHeightFn` (default RAW; oracle path passes designed). **Deform demoted** (terrain_deform.h header note; still compiles). Fixed the two false-consistency comments (`surface_walk.h:29`, `terrain_deform.h:18`). **Re-baselined** only `test_terrain_stream`'s `deform_lowering_…` case (RAW to designed reference); **NO determinism/seam assertion deleted**, both seam bit-identity proofs pass bit-for-bit against the designed base (position-hash guarantee is base-agnostic; proven). `procgen_bench` still bit-identical. **7 new tests pass; 22/22 ctest green** from plain PowerShell (was 21). Logged **WG-21**. Touched ONLY `core/**` + this doc. **R2 hand-off (UE call sites to swap):** listed in the Completion Report. |
| 2026-07-26 | **TERRAFORMING (Admin brief): "add basic terraforming so i can flatten out an area and build a base"** — answer whether fill is representable, then ship levelling end to end | Done | **Fill was NOT representable**: `VoxelEdits` held only `removed_`, so the model was subtractive by construction. Bumped the data model rather than faking it (WG-22): `added_` set + `fill`/`fillCell`, self-describing save format that still reads old slots, `derivedRaisingAt` + a signed `surfaceOffsetAt`, `levelArea` in `/core`, ABI 3 to 4, a Q-key tool with a footprint decal, and a worker reconciled against STATE rather than an op history (`of_streamer_load_edits` + `VoxelWorld.driftedFromCore`, which closed a real defect: `VoxelSave` replays a level op as a dig and would carve a 13 m sphere out of the pad). New `web/tools/smoke/probes/level.js` (searches 610 sites for a slope first, because flat ground cannot prove a levelling tool). **22/22 ctest, parity 119/119 + 106/106, `npm run check` green (115 files), served wasm confirmed at ABI 4.** Also REFUTED an attributed `tunnelwalk.js` regression with the parity fixture (WG-22 moved two numbers, both the save header) and fixed the probe's real fault: it walked AWAY from the tunnel it dug, because the bore climbs out. Commits `56031fa`, `dd0c81d`, `e2da6ed`, `609550e`, `3b11026`. |
| 2026-07-27 | **WG-29 / DW-36 (Admin brief), five subagents.** (1) *Bridge + ore pipeline survey*: mapped the scratch-arena contract, the eleven `of_gp_patch_*` exports and the build/sync workflow before anything was written. (2) *Persistence survey*: found that `SAVE_VERSION` is checked with `!==` and therefore that an additive optional field must NOT bump it, which is what kept every existing world loadable. (3) *Probe/gate survey*: established the probe port (5199, `vite.probe.config.ts`) so nothing went near Reid's 4200, and the true gate set. (4) **ABI 12**: `of_discovery_api.inc` + `discabi.ts`, built, synced, parity re-run (Tier 0 126/126 and Tier A 108/108 UNMOVED), and proven by a node script that loads the module directly and round-trips serialize/deserialize; it also caught that `WorldDiscovery::deserialize` was not all-or-nothing and **flagged it rather than editing my header**, which is the right call and is why the fix is a decision rather than a silent patch. (5) **`discovery_tests`**: the lattice cross-check, the brute-force flood completeness, and the uint64 wraparound defect, found by writing the test against the property instead of against the code. (6) *The painter*: `centreM` through `toPx`, `sizeAlpha`, `discCoversCanvas`, and it found R17's second head (the apsis label nudging away from the canvas middle rather than from the body) which the brief had not named. | Done |
| 2026-07-26 | **LEVELLING REPAIR (Admin brief): "i tried the click q to level the ground thing and it didnt really work at all"** | Done | Three causes, and the leading hypothesis (a half-cell dead band) was only the third of them. **(1) The tool could not be reached**: the disc needed the aim ray to hit ground within 9 m, and on a 29 degree slope it found none at pitch 0, -10, -20 or -30, silently, with the ring hidden. Fixed by falling back to the feet. **(2) The pad had full-depth holes**: `levelArea` recorded only solidity CHANGES while the heightfield reads a contiguous run of edits per column, so 8.7% of columns kept their original height, worst 2.5 m out. Fixed by recording the decision over a 1.5 cell band; solidity, and therefore every voxel pin, is untouched. **(3) Nothing told the player anything**: every press now quotes the flatness it achieved and the probe asserts the quote is true to 4 mm. Built `of.meshVerts` so a probe can measure the DRAWN surface, which is what showed that levelling used to make the local step WORSE (1.883 to 2.727 m) while the spread halved; it is now 0.973 m. Rewrote `probes/level.js` around a real DOM key event, a pitch sweep and the drawn geometry. Sub-cell fill assessed and rejected. Commit `91606bd`; the /core half was swept into `906e464` by a parallel lane. |
| 2026-07-27 | **WG-30 / DW-37 (Admin brief): "cant see the terrain from the map, even in sandbox"** — add a `/core` view-region sampler, replace the discovery-quad shading with it, and extend the negative control to the ground | Done | ABI **13**, `of_map_api.inc` §19 `of_map_sample` (exact transcendental-free ray-sphere solve; biome + `sampleDesignedHeight` + the SURVEY bit per sample), `web/src/world/MapTerrain.ts` (the driver, `Discovery.ts`'s proven shape: call first, re-read pointer AND heap, copy out, cache on everything that could change it), `MapLayers.paintTerrain` (one ImageData blit replacing `strokeDiscovered`), `MapPaint.BIOME_RGB` + `terrainShade` (elevation ramp plus a slope-normalised hillshade), `MapTerrain` wired through `MapWorld.terrain` / `MapMode` / `MapBoot`, `Discovery.window()` DELETED with its dead cache. Three findings worth the entry: **the plain height ramp the brief specified draws a blank rectangle** and only looking showed it; **the obvious hillshade normalisation (by the view's relief) is also blank** because map-scale slopes are small; and **the cache key must not be a raw player position** (+7.32 ms against +0.58 ms, decided by whether the player happened to be settling). Net map cost is now BELOW the layer it replaced. 31/31 ctest, parity 126/126 + 108/108, `expected.json` moved by the abi line alone, `discovery.js` green in both modes with the terrain gate asserted at 274 vs 2,784 painted samples. |
> Note: the Agent subagent tool (`general-purpose`/`Explore`) was **unavailable this session**; research run via WebSearch/WebFetch directly per the brief. Sources cited in [spike1-worldgen.md](../spikes/spike1-worldgen.md) §9.

| 2026-07-27 | **WG-35: map how water exists today (Explore)** | Done | Returned the finding that overturned the brief: **`NodeKind::WaterPool` has no scatterer anywhere and places zero nodes**, `NodeArt.ART` has no entry for kind 5 so one would draw nothing, `water_pool.glb` ships unreachable, and the two candidates reading COULD produce were the ore-patch ground skin (0x53687d, translucent lobed disc lifted 0.25 m) and `Biome::Ocean` terrain (0x14406e, dry blue-tinted ground). Neither turned out to be it. Also surfaced that `seaLevelM` is 0.0 on both bodies, so `Biome::Ocean` degenerates to "designed height <= 0" and buys only a palette entry and a 1.15 relief gain. **Reading could not settle the question; three isolation frames and one projection measurement did.** |
| 2026-07-27 | **WG-36: map the terrain authority, the character controller and the probe harness (Explore)** | Done | The map I cut the basin from. Correctly identified that `sampleDesignedHeight` lives in `biome.h` and not `cubed_sphere.h`; that the home pad is the mechanism to mirror and is written pad-first for bit-exactness; and **flagged four landmines before I hit any of them**: the two `fieldWorldSig` memo keys that would have made the basin appear in some chunks and not others, `KinematicBody.ts` sitting at 399 of a 400-line cap so the swim state could not go inline, the pond landing inside the pad blend so composition order had to be decided explicitly, and `spawn_probe.cpp` scoring the spawn as DRY and "not a lake bed", which is a documented criterion the pond contradicts. Every one of those was real. |
| 2026-07-27 | **WG-37: build `water_field_tests`, make ctest green, verify by reversion (general-purpose)** | Done | 17 tests / 35,619 checks, **34/34 ctest**, both briefed reversions failing by name (7 tests / 8,590 checks for the decal defect; 6 tests / 8,651 checks for the circularity, which puts the water 0.600 m below its own floor). Fixed two `biome_tests` cases at the PROPERTY rather than the threshold, and strengthened one of them into an exact set-equality claim. **Contradicted three of my own header comments and was right about all three** (a 0.51 m arithmetic slip in the shoreline; an invariant block conflating "inside the pond" with "inside the rim", which makes its own left inequality false over the whole dry beach; and a justification comment arguing something `submersionM` does not do). All three now say the true thing. |
| 2026-07-27 | **WG-38: bump the bridge to ABI 16 atomically (build-tooling-controller)** | Done | Rebuilt wasm (401,301 B), regenerated `expected.json` (**one line, `abi` 15 to 16, zero terrain values moved**), synced dist, parity 126/0 self and 108/108 cross-toolchain exact, handshake quoted live at `abi=16` with `worker instance agrees=true`. **Two things I would not have got by asking for less: it proved containment SEPARATELY with a native march (last nonzero drop 21.9 m, first bitwise zero 22.0 m, 0 leaks) rather than letting an unmoved fixture stand in as evidence, and wrote down that the fixture is a NEGATIVE control for the pond because its eight directions sit 370 km to 1,141 km away and could not have moved. It also found `enemyloop.mjs` pinning `=== 15`, broken by my bump, and repointed it at the fixture rather than bumping the literal, which would have rescheduled the same break for ABI 17.** |

## 9. References
MASTER_PLAN §6 (terrain), §7 (Astroneer/Space Engineers/DSP), D-005. KSP PQS / cubed-sphere; SDF/voxel terrain (Astroneer); Poisson-disk placement.
