# Rendering & Graphics — Master Controller Context

> **Domain owner:** `rendering-controller` | **Reports to:** Admin | **Phase:** WEB (three.js, DW-1 pivot) | **Last updated:** 2026-07-28 (RN-59 to RN-61).
>
> **RN-59 to RN-61 (2026-07-28) - TREES AND ROCKS, AND THE PASS OPENED BY DISPROVING ITS OWN HYPOTHESIS.** Scope: silhouette, form and variety at walking (0-15 m), mid-field (15-60 m) and horizon (60 m+). **(1) THERE IS NO FOREST, and this is the finding of the pass.** Living trees exist ONLY as 14 harvest nodes in one spawn clearing, on a golden-angle spiral from 7 m to ~57 m (`NodeField.ts:136`), and the Forest biome's scatter atlas contains no live tree AT ALL, deliberately (`build_props_forest.py:9-12`: a scatter conifer "would read as a harvest node the player cannot chop"). So beyond ~57 m of spawn there is nothing to accumulate into a treeline: **no silhouette work can ever produce a horizon mass**, because there are no trees out there to form one. That is placement, not asset quality; Admin scoped it to world-gen as a separate lane. **(2) THE NEAR-BLACK BOULDERS ARE NOT A LIGHTING MISMATCH, and I predicted they were.** New probe `probes/proplight.js` sweeps the sun and finds prop pixels BY DIFFERENCE (capture with props shown and hidden; tiles that moved are prop tiles, tiles that moved not at all are pure ground, mixed tiles discarded), so neither band is a hand-picked rectangle. Two controls green: the visibility toggle returns the bit-identical frame, and both surfaces genuinely respond. **Two sites mattered and one would have given a confident wrong answer in either direction.** At high sun the Mountains site reads prop 96.37 against ground 96.13 (agreement to a quarter of a count) while Hills reads 60.97 against 123.41 - the Hills gap is ALBEDO, grass being brighter than rock. Every baseline frame was shot at sunT 0.30 where the two models agree to 0.02 of full range, so the dark boulders are albedo and material (`OF_RockDark` 0x57534C). R8 doing real work on a lighting claim rather than a geometric one. **(3) BUT A REAL SYSTEMIC DISAGREEMENT EXISTS AND IT IS ABOUT THE INDIRECT TERM, NOT THE SUN.** Reproduced at both sites, worst normalised gap 0.42 and 0.48, always at LOW sun; the prop band swings 71.4 counts across the sweep at Mountains where the ground swings 22.4, i.e. **3.2x**. `TerrainShader` gives itself a sky-ambient integral plus RN-30's bounce; props get three's ambient and an IBL and nothing else. **Every prop, tree, pebble and machine crushes toward black at dawn and dusk while the ground stays lit.** Bears directly on Reid's live golden-hour spawn choice: one candidate site sits at 9 degrees maximum sun elevation, which would make that defect permanent. Next commit, reachable via `SkyIbl` and `envMapIntensity` without touching `Systems.ts`. **(4) RN-61: the clone army is gone.** `NodeBatch` never called `setColorAt` at all, so 14 trees were two meshes at a literal scale of 1.0 in one colour, differing only in yaw. Added `NodeBatch.tint` (build-time only) plus one colour PER PLACEMENT applied across that placement's slots - trunk and canopy are separate material batches, so tinting them independently would drift a tree's bark from its own leaves and read as a broken asset. Deliberately NOT a call into `ScatterLook.tintFor`: the flower branch would put a 1.70/1.50/0.38 yellow on a whole conifer, and that value range (0.80-1.18) at tree size reads as a lighting bug. Per-instance tree size 0.86-1.14, bounded because **`art.radiusM` and `art.hitUpM` do not scale with it**, so a big and a small tree are chopped from the same distance; at +/-14% that is under aim tolerance, at +/-40% it would be a gameplay bug wearing an art change's clothes. Verified numerically because the eye would have said "no change": **2.48% of scene pixels (13,514 of 544,000) move by >6 counts, max channel delta 180**. Invariants: draw calls 65 -> 65, triangles 641k -> 641k, VRAM 93.7 -> 93.7 MB. **(5) THE ROOT CAUSE OF THE ROCK SAMENESS, STATED PRECISELY.** Every rock in the game is `harvest_common.lobe()`, a single-valued radial sweep about a vertical axis, **topologically incapable of an overhang, cleft or undercut**; all four ore boulders are the SAME 164 triangles with different material dressing. Canopies are closed solids with zero sky-through. Neither is reachable by a fragment shader, which is the honest converse of the RN-45 strata win (a cliff IS terrain, so one edit reached every face; a boulder is a genuine prop and what is wrong with it is its outline). **DW-10 ledger UNCHANGED at 4 ShaderMaterial + 1 live `onBeforeCompile`, and this pass is not expected to spend a slot.** **(6) THE THIRD ALREADY-UPLOADED ATTRIBUTE NOBODY WAS READING.** `PropGeometry.bakeColour` is gated on `isFoliageMaterial`, so `OF_Rock`, `OF_RockDark`, `OF_Regolith` and `OF_Bark` carry a constant-255 vertex colour with nothing writing it - after RN-50's per-quad UV and the node instance colour, that shape has now paid out three times and is worth a positive entry in `INSTRUMENTS.md`, which is currently all failures. **DEAD WEIGHT, recorded so nobody spends a day on it:** every `_LOD1`, `_LOD2` and `_Stump` in the node files is contract-required and **never rendered** (`NodeBatch` draws LOD0 at every distance), and `bush_scrub.glb`, `water_pool.glb` and `oil_seep.glb` ship, validate and are never loaded. Trees are NOT triangle-constrained: conifer 584 of a 900 budget, broadleaf 452 of 1000, 14 trees = 8,176 triangles in a ~1 M frame. Commits `53106ee`, `2e410d5` (compare-and-swap; the gameplay lane's `8ab129d` landed between them and was not reverted). Screenshots `RN59_base_{rn15,trees,tree_near,tree_far}.png`, `RN61_{before,after}.png`.
>
> **RN-50 (2026-07-28): THE DETAIL BUMP SHIPS, ON A COORDINATE THAT WAS ALREADY ON THE GPU, AND THE PER-CHUNK PHASE IS A CAPABILITY WE DID NOT NEED YET.** Admin asked me to MEASURE both the UV route and world-gen's per-chunk phase before committing to either. The UV route wins for this term. WG-56 found that every chunk already carries a per-quad normalised UV, in the wire format, in the batch attributes, written per slot, uploaded and interpolated every frame, and that `TerrainShader.ts` contained the string `uv` **exactly zero times**. It is the well-conditioned coordinate `pM` is not, because it is LOCAL: one uint16 step is 0.883 mm on a depth-14 chunk, a quarter of a ground pixel at 2 m, against `pM`'s quantum of nearly nine pixels there. **MEASURED, one binary, RN-15 camera, 1280x720, props hidden, matched pairs one settled frame apart, noise floor exactly 0.0000: the bump moves 75.4% of the near band at a mean of 6.32 counts, 67.6% of the mid at 5.35, 51.7% of the far at 3.88, and 0.0% of the horizon at 0.0007.** The horizon reading is the one that matters: the term fades itself on the PIXEL FOOTPRINT, so it is gone before a triangle is a few pixels wide, and that is now measured rather than argued. The arcs are gone (`RN50_uvbump.png` against `RN45_iso_bump.png`, same camera, same flags, bump alone). **I WALKED INTO WORLD-GEN'S TRAP AND CAUGHT IT BEFORE MEASURING.** The obvious version keeps the macro field in the bump's height and merely adds a UV octave; that scores 0.00% dead quads, identical to a correct fix, and is still visibly wrong, because a quantised octave ANYWHERE in the sum poisons the derivative of the whole sum. `hArt` now feeds the COLOUR and never the bump, and every octave the derivative sees is on the UV: two, at 14.0 and 5.3 over the quad (4.1 m and 10.9 m at depth 14), neither a power of two because a lattice aligned to the 32-cell grid would draw the grid. **THE HONEST COST, and why this does NOT replace the phase:** the UV normalises over the QUAD, so the detail's world size doubles at every LOD step. Tolerable for THIS term only because it is well conditioned solely within about 20 to 45 m, where the streamer is at max depth, so the step falls outside the band. Any future field needing a derivative at range still wants the phase. **RN-45's ARITHMETIC CORRECTED: the quantum is 0.03125 m, not 0.0625** (I quoted the worst case; a generic point on a 600 km sphere sits one binade lower). Conclusion unchanged, and the corrected figure explains the SHAPE: 8.77x the ground pixel at 2 m and 0.55x at 8 m, which is why it was a near-field band that STOPPED rather than a fade. **TWO ENVIRONMENT DEFECTS, both of which cost a measurement.** (1) `npx vite build` SKIPS `sync-wasm`, which `npm run build` runs as a prebuild step; another lane bumped the client to ABI 19 mid-session, my builds kept a wasm at 18, a binary silently failed to boot, a `--out` screenshot never wrote, and I compared two images that were THE SAME FILE and concluded an amplitude change had done nothing. **Identical output from a changed input is a wiring diagnosis, never a coincidence**, and that rule paid three times tonight. (2) The disk filled to 7.2 MB free of 931 GB from accumulated `dist-*` directories across every lane; I deleted only my own. **`Scatter.ts` is 398 lines and UNDER the 400 cap**, better than the 409 it started the night at, repaying the 3 lines RN-48/49 added. No file was split. **NOT DONE: water polish (surface motion, glint, refraction across the waterline, shoreline) is still untouched, and `WaterSurface.ts` is under `web/src/world/` which is world-gen's, so the surface half needs an ownership ruling; the post-stack half is mine and is the next thing I would take.**
>
> **RN-51 to RN-58 (2026-07-28): THE POND STOPS BEING A SHEET OF GLASS. ONE DW-10 SLOT BUYS ALL FOUR OF SURFACE MOTION, GLINT, REFRACTION AND THE SHORELINE, AND THE WET BEACH COSTS ZERO BECAUSE IT IS TERRAIN.** Reid asked for the pond and will go swimming first thing. **RN-51, THE OWNERSHIP MOVE:** `WaterSurface.ts` leaves `web/src/world/` for `web/src/render/`. World-gen keeps the LEVEL and the BASIN (`water_field.h`, `WaterOracle`, `levelAt`/`depthAt`/`shorelineM`), which is surface authority and stays in one place per DW-26; the look, the geometry and the vertex attributes are mine. **THE SLOT SPENT, AND WHICH OF THE FOUR NEEDED IT: ONE `ShaderMaterial`, and it is not four effects.** They are four questions about ONE fragment (which way is this facing, how much sun does it mirror, what is underneath it, how close is the edge), all answered from the same perturbed normal and the same water depth, so a second program would have to recompute the normal to ask its question. **The DW-10 ledger goes 3 + 3 to 4 `ShaderMaterial` plus 3 `onBeforeCompile`. REFRACTION WAS THE ONE THAT LOOKED LIKE IT NEEDED A SECOND SLOT AND DID NOT: what it needs is not a program, it is a TEXTURE**, the scene as it stood before the water drew. One `copyFramebufferToTexture` at `onBeforeRender`, which is the only instant at which the bound target holds the sky, the far scaled scene and every near opaque and does NOT yet hold the water. A pass costs a program; a grab costs a copy. This is RN-47's argument reaching the OPPOSITE answer for a good reason: the submerged view wanted a per-pixel distance the depth buffer already had, so it was post; the surface wants the normal it is already computing, so it is a material. **RN-57, AND IT IS RN-45's FINDING REPEATING: A CLIFF IS TERRAIN, AND SO IS A WET BEACH.** The waterline darkening goes into `TerrainShader`, already a custom shader, so it spends NO slot and reaches the shore, the submerged bed and any future body for no draw call, no triangle, no texture and no byte of VRAM. **THE COORDINATE IS THE CORRECTNESS ARGUMENT, TWICE, AND BOTH TIMES RN-45/RN-50 WAS APPLIED BEFORE THE ARTEFACT RATHER THAN AFTER.** (a) The ripple's gradient is ANALYTIC in `aPlane`, metres from the pond centre: **not one `dFdx` appears in the water shader.** `aPlane` spans +/-11 m so float32 resolves it to about a micron, against planet-centred metres' 0.03125 m quantum that RN-45 measured into concentric arcs. (b) The wet band measures height above the water from `vRelief` (metres above datum, a per-vertex attribute) and NOT from `length(pM) - levelRadius`: algebraically identical, and only one is computable, because a difference of two 6e5 radii resolves the 0.55 m fade into about eighteen steps and draws contour bands parallel to the shore. The LATERAL gate is allowed to use `pM`, since 0.05 m of jitter on a 5 m smoothstep is invisible. **Knowing which of two coordinates a term can afford is the skill; using the good one everywhere would have cost a varying nobody needed.** **THREE MORE PROPERTIES RATHER THAN TUNINGS.** The reflected sky is `ofAtmoScatter` along the reflected ray at `tMax` 1.0e9, so DW-22 holds (no hand-picked blue to disagree with the sky above it) and RN-30's confinement holds (an ESCAPING ray cannot pick up the boundary-layer aerosol). The refraction offset is scaled by the vertex's water depth, which is zero at the shoreline, so shoreline bleed is impossible AT the boundary by construction rather than fixed by a depth test. The fine ripple octaves fade on the PIXEL FOOTPRINT and the discarded slope variance is added back as specular roughness, because dropping a sub-pixel ripple unpaid turns a sheet of glitter into flickering dots that the bloom pyramid then amplifies. **EVERY FRAME GLOBAL IS SHARED BY REFERENCE WITH THE NEAR TERRAIN MATERIAL** (atmosphere record, planet centre, sim clock, cascade splits, ambient, sky-ambient), so there is no new per-frame push, no edit to `Systems.ts` or `Boot.ts`, and "the water agrees with the shore" is a property of the object graph. `CASCADE_GLSL` is EXPORTED from `TerrainShader` rather than copied: a second copy is a second authority on which cascade a range belongs to, and the visible failure would be a glint still lit inside a shadow the shoreline is already in. `WATER_SIGMA` and `WATER_TINT` move out of `PostConfig` into `WaterOptics.ts` and both sides read them, because looking DOWN into a pond and looking UP from inside it are one integral seen from two sides. **MEASURED, ONE BINARY, `of.framehash` (NOT screenshots: the water animates on `uTime`, so two screenshots are a tick apart and the surface has genuinely moved; `framehash` renders synchronously so two calls cannot have a tick between them). Noise floor BIT-EXACT: identical hash, 0 of 14,400 tiles, peak 0.0000. At the pond, sun 4 degrees up, 1280x720, 160x90 tiles: ripple 4,983 tiles / mean 3.10 / peak 65.6; refraction 6,820 / 5.76 / 67.0; glint 164 / 0.097 / 72.3; foam 59 / 0.013 / 11.2; wet band 4,754 / 2.45 / 25.0; all four together 6,823 / 7.19 / 120.1.** **INVARIANTS, MATCHED PAIR AT THE RN-15 CAMERA, before build archived from `3c9191c`: draw calls 70 to 70, triangles 1,001,340 to 1,001,340 (IDENTICAL, and it reproduces RN-49's published figure to the digit), geometries 52 to 52, VRAM estimate 93.7 to 93.7 MB, and programs 40 DOWN to 39**, because the custom material replaces a stock `MeshStandardMaterial` that was also compiling an environment-mapped variant. **THE GRAB IS 7,372,800 BYTES AND IS PUBLISHED SEPARATELY, because `vramEstimateMB` is built from the post targets and the chunk pool and does NOT see it**; it is allocated lazily inside `onBeforeRender`, which three calls only for objects that survive the frustum cull, so a body with no water, a camera looking away, `?post=0` and `?msaa=` all pay zero. `grabs` is a COUNTER (483 frames at the pond) and not a flag, because "it ran once at boot" and "it runs every frame" are different claims. **THE REGRESSION CLAIM IS BIT-EXACT: at the RN-15 camera, 121 km from the pond, toggling the wet band moves 0 of 14,400 tiles with an IDENTICAL hash.** The basin gate confines it exactly, and that is a stronger statement than any screenshot pair. **FOUR DEFECTS, ALL MINE, AND THREE OF THEM WERE INSTRUMENTS.** (1) The shader did not compile: `CASCADE_GLSL` was concatenated ABOVE the uniform block and GLSL requires declaration before use. (2) **The static check that should have caught it printed `(none)`**: it compared the SET of uniforms used against the set declared, and a set has no order, so it was asked a question whose answer could not express the failure. Nothing but a compiler checks a compiler. (3) **THE GLINT WAS MEASURED TWICE WHERE IT CANNOT EXIST.** Four fixed bearings gave peaks of 0.10, 0.13, 0.91 and 1.13 counts and TWO "failures"; three of those cameras had the sun behind them, where a correct specular is SUPPOSED to be invisible, which is RN-46's `wetCells: 0` error with the sign flipped. And even aimed correctly it read about 0 on a 64x36 grid, because a 20x20 px tile averages a sparkle path away; at 160x90 the same term reads a peak of 72. **The claim is now TWO-SIDED: present with the sun up, and BIT-EXACTLY absent below the horizon, which no tuned threshold can imitate.** At a HIGH sun (49 degrees) the glint reads 1.67 counts and that number is RECORDED in the probe so nobody later "fixes" what is geometry. (4) **The foam was a solid white ring 3.2 m wide.** It bounds a band of DEPTH, and depth converts to distance up the shore through the beach slope: 0.34 m at this pond's 6 degrees reached 3.2 m inland and photographed as a wash over the sand. **`?waterfoam=0` is what ATTRIBUTED it rather than a guess**, which is the whole reason standing rule 7 wants one flag per term. Shipped at 0.10 m with a much higher noise gate, which reads as a broken line of surf. **ALSO FOUND, NOT MINE, AND IT AFFECTS EVERY LANE: `__ofPost.state().sun` FREEZES BELOW THE HORIZON**, because `ShadowRig.update` does `if (!this.active) continue` before it moves the light and `Frame.publishSun` derives the vector from that light. A probe read the identical elevation 0.5486 for two different times of day, which is the tell. Harmless where the post stack uses it (the contact march is gated on the same rig), not harmless in an instrument; `of.stats().sky.elevationDot` does not freeze. **FRAME TIMINGS ARE NOT PUBLISHED. Two lanes were live and the invariant table is the whole cost claim.** New: `web/src/render/WaterSurface.ts` (moved), `web/src/render/materials/{WaterShader,WaterMaterial,WaterOptics}.ts`, `web/tools/smoke/probes/water.js`. Isolation: `?water=0 ?waterripple=0 ?waterglint=0 ?waterrefract=0 ?waterfoam=0 ?wetsand=0` plus `?rippleamp= ?glintamp= ?refractamp= ?foamamp= ?wetsandamp=`, all registered in `run.mjs`; runtime `window.__ofWater.{set,reset,tune,state,grabBytes,grabs}` and `__ofTerrainArt.{setWet,getWet}`. Screenshots `docs/screenshots/RN58_pond_{before,after}.png` (same camera, one binary, terms off against on), `RN58_glint.png` (a real sun path at a low sun), `RN58_waterline.png`, `RN58_floatline.png` (the float line, NOT a submerged camera: see the not-done below), `RN58_nofoam.png` (the attribution), `RN58_rn15cam_{before,after}.png`. **NOT DONE, AND NAMED: there is still no shot of the surface from genuinely UNDERNEATH it.** `pondshot --swim` settles the capsule at the float line with `headUnderM` -0.29, i.e. the head is ABOVE water, so that capture is a waterline shot and not a submerged one; a submerged camera needs a dive input the probe does not drive. RN-47's own list of not-dones (unattenuated first-person arms, a laterally unbounded water plane, no tint on a free camera) is unchanged. The near-field water seen at a very grazing angle from under a metre away is a flat pale wash with no ripple detail, which is partly correct (the wavelengths exceed the visible patch) and has not been separately measured.
>
> **RN-48 to RN-49 (2026-07-28): RN-46 SHIPPED DEAD IN THREE SEPARATE WAYS AND I PROVED IT WORKING WITH THE WRONG HALF OF THE EVIDENCE. THE UNDERSTOREY BECOMES SEVEN CLUSTERED SPECIES FOR ONE DRAW CALL AND NO EXTRA DENSITY.** **(1) THE POND REJECTION NEVER RAN, IN ANY BUILD.** RN-46 reported `wetCells: 0` at two DRY sites and called that a result. It is not: **zero at a dry site is equally consistent with the filter working, with it never running, with the oracle never being consulted, and with the whole feature being unreachable. A filter is proved by the case it is supposed to CATCH, never by the case it is supposed to ignore.** `probes/pondscatter.js` goes to the pond and asserts the positive half, and found THREE defects stacked: **(a) the flag was inverted** (`?scatterwet=1` means wet scattering is ALLOWED, so Boot handed the oracle over in exactly the configuration that then refuses to use it); **(b) `depthAt` takes a normalized DIRECTION and was being handed the absolute 6e5 m position**, which is why `DebugTerraform.water` divides by the length before every call and answered correctly all along; **(c) the radius guard was ineffective on an assumption I never examined, that a water level sits near the datum.** It does not: the pond is at the Mountains spawn, so its level radius is 604,667 m while the Hills site stands at 600,861 m, and every cell over most of the planet paid a WASM call to be told it was dry. Replaced by a **BASIN GATE**, one dot product per chunk against the oracle's own published 22 m disc, where the Hills site is 121,813 m away. It **fails SAFE**: a future non-disc water model leaves `disc` null, the gate is skipped, every cell is queried again, and correct-but-slow beats silently-dry (the DW-26 trap). **MEASURED at the pond, camera aimed by SEARCHING the actual aim ray rather than assuming a yaw convention: wetCells 0 to 431, foliage coverage over the water 0.3123 to 0.0716 (-77%), instances 18,789 to 18,001; the `?scatterwet=1` control restores 0 / 0.3087 / 18,789.** The probe publishes the oracle's own reading at two points so a zero can be ATTRIBUTED rather than guessed at (3.40 m at the pond centre, 0.13 m at the camera in the shallows); **my first two diagnoses of this were both guesses and both were wrong, which is why that field exists.** Every symptom read healthy throughout: `deliveredFraction` 1.0001 and the two halves of the pair identical TO THE INSTANCE. **(2) SEVEN SPECIES, AND THE DENSITY BUDGET IS UNCHANGED TO THE UNIT.** `Detail_BroadleafForb` (30 tris LOD0 / 4 LOD2, OF_Leaf), `Detail_FlowerSprig` (30 / 5, OF_Grass plus OF_LeafDry heads), `Detail_SedgeRosette` (36 / 4, OF_Grass), authored to RN-45's rule (LOD2 rebuilt at `segs=1` and widened, never decimated). Byte-identical control run first at 18,288 bytes; validator green; 48/48 assets; **`max_tris_total` went 340 DOWN to 245**, the exact built total, because 340 was slack against a real 136 and slack in this budget is slack that gets spent at one to two orders of magnitude more instances than any biome prop. **GROUND_DETAIL sums to 538,000 before and after, DRY_DETAIL to 116,000 both ways: the new species are paid for out of the old ones.** Variety must not become cost by default while DW-5 is not relaxed. **(3) THE CLUSTERING IS THE HALF THAT MATTERS, and without it the species would have been a cost with no picture.** Adding species to an unclustered scatter makes the ground MORE uniform, not less: four species salt-and-peppered and seven salt-and-peppered both average to one flat texture at any distance, and the second is merely a more expensive way to get there. Each 8x8 cell patch (about 14 m at the shipped 1.808 m near cell) draws a DOMINANT species from the SAME weighted table, so a common species dominates many patches and a rare one dominates few, and 55% of instances take it. Not 100%, because a patch that is one species reads as a planted crop, which is exactly what RN-30 spent a pass removing from the grass height. **KNOWN: the patch lattice is chunk-local and restarts at chunk boundaries. That is another patch edge rather than a seam, and the honest fix needs the same high-precision per-chunk phase the detail bump needs, so it is blocked on the same world-gen work.** **COST AT THE RN-15 CAMERA, one binary, 1280x720: triangles 972,049 to 1,001,340 (+3.0%), draw calls 69 to 70 (+1, the one new OF_Leaf batch), programs 40 to 40, geometries 51 to 52, VRAM 93.7 MB unchanged, foliage coverage 0.5438 to 0.5629 (+3.5%).** Coverage up more than triangles is the trade working: the forb and the rosette are wider than the blades they displaced. Pool refusals 0, `cellsCapped` 0, `chunksCapped` 0, backlog 0. **Across the night the frame is 1,087,719 to 1,001,340 triangles, i.e. 7.9% FEWER than the pass started with, while going from four species to seven and adding the terrain surface art.** **THE ASSET LANE'S FIRST CUT VALIDATED AND WAS VISUALLY WRONG** (sprig heads and sedge straps rendered as hairlines, the sub-pixel failure `build_props_plains` already documents); caught by rendering it, fixed before it shipped, triangle counts unaffected. **`Boot.ts` IS COMMITTED AS A FILTERED BLOB**, HEAD plus only my two hunks, built and asserted in Python (`resumeWorld` absent, both of mine present), because it still carries the vessel lane's uncommitted wiring. New: `web/tools/smoke/probes/pondscatter.js`. Screenshots `docs/screenshots/RN48_pond_after.png` (a clean bed), `RN49_species_after.png` (visible stands of forb, grass and straw). **NOT DONE: water polish (surface motion, glint, refraction across the waterline, shoreline treatment) is untouched, and `WaterSurface.ts` lives under `web/src/world/` which is now world-gen's, so the surface half needs an ownership ruling before I take it; the post-stack half is mine and is the next thing I would do.**
>
> **RN-45 to RN-47 (2026-07-28): THE LOD2 CARDS, THE TERRAIN'S FIRST SURFACE ART, THE POND, AND FOUR BROKEN INSTRUMENTS OF WHICH THREE WERE MINE. THE REORDER: ITEM 1 AND ITEM 3 OF THE BRIEF ARE THE SAME SHADER EDIT, BECAUSE IN THIS ENGINE A CLIFF IS TERRAIN AND NOT A PROP.** Admin ranked rocks and cliffs first and the terrain material third. They were merged and done as one, and the argument is structural rather than a preference: the rock props are metre-scale boulders inside a 170 m scatter ring, while every actual cliff, cut bank, ravine wall, crater rim and mountain flank on the planet is drawn by `TerrainShader`'s steep-slope branch, which was the single constant `vec3(0.30, 0.28, 0.26)` and nothing else. Bedding put into that constant reaches every cliff face on Forge; new boulder meshes reach a few hundred instances. **(1) RN-45a, THE LOD2 DETAIL CARDS, BUILT HERE RATHER THAN REQUESTED.** Blender 5.0 is on this machine and the shipped `detail_cards.glb` already carried `Khronos glTF Blender I/O v5.0.21` in its own bytes, so the exporter could not drift; the control was run first and **an unchanged rebuild came back BYTE-IDENTICAL at 13,616 bytes**, which is what makes every later diff attributable. Authored as CALLABLES and never as a decimate, because `props_common`'s own header already says a collapse decimator eats a grass blade whole and leaves a shard: blades at `segs=1` (one triangle each, a base pair and a tip), four of them instead of six or eight, and 2.4x to 2.7x wider so the covered AREA survives the dropped blades. **Shipped: 4 / 4 / 4 / 6 triangles against 18 / 40 / 18 / 42, LOD0 unchanged to the triangle, 48/48 validate, `lods 8 present`.** The pebble scatter is a single four-sided lobe rather than three flat chips, and that is measured rather than aesthetic: at 45 m with the eye 1.8 m up the ground is seen about 2 degrees off edge-on, so a horizontal facet presents 3.5% of its area and a scatter built from them would vanish at exactly the range the LOD exists to serve. **MEASURED, ONE BINARY, `?proplod2=0` as the control, the RN-15 camera at 1280x720: triangles 1,087,719 to 972,049, i.e. -115,670 and -10.6% of the whole frame, at 69 draw calls, 40 programs, 51 geometries and 93.7 MB VRAM on BOTH sides, with foliage screen coverage 0.5533 to 0.5438 (-1.7%).** The 1,087,719 reproduces RN-30's published figure to the digit, which is what says the control is the right control. **AND THE FIRST VERSION OF THAT CONTROL WAS WRONG IN THE FLATTERING DIRECTION:** `?proplod2=0` initially applied to every batch, read 2,303,735 against 972,049, and would have let this pass claim 1.33 M triangles. Most of that gap is the BIOME props' LOD2, which has been shipping since W4. A control that removes more than the change removed is not a control. **(2) RN-45b, THE TERRAIN SURFACE ART: A MACRO VARIATION FIELD AND ROCK STRATA SHIP; THE DETAIL BUMP IS A MEASURED NEGATIVE RESULT AND IS DEFAULTED TO ZERO.** New `web/src/render/materials/TerrainArt.glsl.ts`, called from the existing terrain `ShaderMaterial`. **The DW-10 ledger is UNCHANGED at 3 `ShaderMaterial` plus 3 `onBeforeCompile`**: this edits a custom shader that already existed and adds no program, no draw call, no texture and no byte of VRAM (69/40/51/93.7 identical with the terms on and off). The noise phase is PLANET-CENTRED METRES and not engine space, because engine space rebases and a field keyed on it would swim across the ground every time the floating origin moved. **TWO INDEPENDENT CONFINEMENTS, and they are not the same reason:** a compile-time `#ifndef OF_SCALED` keeps it out of the far scaled scene where a 12 m octave is far below one pixel (confinement by the call graph, RN-30's rule), and a distance fade completing at 4 km against a ~15 km handover is what prevents a SEAM, since a ridge modulated on one side of the handover and flat on the other is the failure a define cannot catch. **MEASURED, one binary, five states each one settled frame apart through `window.__ofTerrainArt`, props hidden, RN-15 camera, 1280x720, two repetitions bit-identical: the macro field moves 99.6% of the near band, 89.9% of the mid, 81.3% of the far and 54.2% of the horizon, by means of 5.86 / 4.99 / 4.65 / 3.51 counts, and ADDS 0.72 / 1.25 / 3.59 / 2.43 counts of 16x16 block-mean spread against a measured noise floor of 0.0000.** At the Mountains spawn the same term reads block-mean spread gains of x1.42 / x2.07 / x2.27 / x1.32. **THE LEVEL IS THE CONTROL AND IT DID ITS JOB TWICE.** A variation layer must raise the SPREAD and leave the LEVEL where it was, and the first tint pair (0.86,1.00,0.90) against (1.12,1.03,0.84) had a per-channel mean of (0.99, 1.015, 0.87), i.e. a 13% net blue reduction applied to the whole planet, which the probe caught as a 5.03% drop in the mid band's level while its spread rose. The pair is now reciprocal about 1.0. The residual per-band level shift is NOT a bias and that was established rather than argued: it scales LINEARLY with amplitude (mid -2.79% at 1.0 against -1.37% at 0.5, a factor of 2.04 where a tone-curve Jensen term would give 4), the field's own mean is 0.000056 over 200,000 samples computed offline in float64, and **the shift FLIPS SIGN between sites (-2.79% at Hills, +2.63% at the Mountains spawn)**, so it is which lobe of the field that patch of ground is sitting on. That is not a defect, it IS the effect. **I was wrong twice about this mechanism before measuring it, and both wrong explanations were plausible.** **(3) THE DETAIL BUMP DOES NOT WORK ON THIS PLANET, AND THE REASON IS ARITHMETIC.** A Mikkelsen surface-gradient bump from screen derivatives, needing no tangents and no UVs and costing two `dFdx` pairs on a field already evaluated. It produced a field of concentric moire arcs across the ground within about fifteen metres of the eye, **and no number saw it: it moved 35% of the near band with a healthy peak either way. A screenshot caught it (`docs/screenshots/RN45_iso_bump.png`), which is DW-7's rule and RN-15's.** A footprint fade was tried FIRST and did nothing, which is what forced the real diagnosis. `pM` is float32 and about 6e5 at Forge's surface, so one ULP is 2^(19-23) = **0.0625 m**, while at the pinned camera one pixel covers **4.3 mm of ground at 2 m and 21.5 mm at 5 m**: the quantum is 3 to 15 times the pixel footprint, whole runs of adjacent pixels sample the SAME quantised position, `dFdx` of the field is exactly zero across them and steps at the quantisation boundaries, and those boundaries are surfaces of constant range from the eye, which is why the artefact is arcs centred on the player. It is not fadeable: the term is well conditioned only once the footprint clears the quantum (about 20 m) and starts aliasing its own 4.2 m octave once the footprint passes a third of that wavelength (about 45 m), and a 25 m annulus is a band of ground rather than a surface treatment. **Left in the build behind `?bumpamp=1` because the measurement is the deliverable. THE MACRO COLOUR TERM IS UNAFFECTED and ships, because it reads the field's VALUE, where 0.0625 m against an 11.9 m finest octave is 0.5% of a wavelength; only the DERIVATIVE is destroyed.** **DEPENDENCY ON WORLD-GEN, stated rather than faked: sub-metre terrain detail of any kind needs a per-chunk phase reduced modulo the octave period on the CPU in float64 and carried with the integer cell index, so the shader adds a small local offset to a small local coordinate and never forms a 6e5 intermediate. That is a terrain-chunk format change.** **(4) ROCK STRATA, the cliff half, at essentially zero cost.** Beds are quantised from `vRelief` (metres above the datum, already a varying), WARPED by a low-frequency noise so they tilt and thicken instead of reading as a contour map, given a per-bed tone and an iron-versus-grey hue from a hash of the integer bed index, and separated by a dark parting at each contact, which is most of what says "layered" at 100 m after the tonal difference has washed out. Bed thickness 2.35 m. **SELF-CONFINING: it only multiplies the rock term, which the slope smoothstep already gates, so flat ground receives exactly nothing and needs no second guard.** Measured at 18% to 22% of the near band with peaks of 105 to 145 counts and EXACTLY ZERO in the mid, far and horizon bands at both sites. **HONEST LIMIT: I did not find a camera in this seed with a large cliff face in the mid or far field, so the term is demonstrated at close range only.** **(5) RN-46, THE SCATTER LEARNS THE POND EXISTS.** WG-35 to WG-42 cut a real pond into `sampleDesignedHeight`, so the bed is ordinary ground to the scatter: legal slope, a biome, and therefore grass cards, pebbles and snow patches four metres under water. Rejected per cell at `WET_REJECT_M` = 0.02 m through `WaterOracle.depthAt`, which is the ONE place "is there water here" is answered (a second copy of that rule in the scatter is the DW-26 failure exactly). **The affordability is a RADIUS guard, not a cache: a body has one water level, so a cell standing above it cannot be wet whatever the disc layout is, and only cells failing that pure arithmetic pay for a WASM call.** Counted as `wetCells` in `stats()` rather than silent (DW-28), and it reads 0 at both dry test sites, which is the negative half of the pair. `?scatterwet=1` restores the old behaviour. **(6) THE THREE FLAGGED FIXES, AND TWO OF THEM WERE NOT WHAT WE THOUGHT.** `?grassshort=` is wired, Boot came free and it cost the one line it was predicted to cost, closing the last unisolated claim in the ground-art programme. `ShadowRig.ts:59-73`'s false comment is corrected in the code and now says plainly that the block is a NO-OP, why (three filters shadow casters against the VIEW camera's layers at r185 line 511, never the shadow camera's), what actually puts the player in the map (`CameraRig` enabling those layers on the near camera), and why the two lines are kept anyway. **`probes/post.js` was NOT inverted, and that hypothesis was mine and Admin's and it was wrong.** Its `check` helper is correct and all three failures were genuine. **The CONTROL had gone stale, and RN-15 is what staled it:** the probe's attribution ratio compares a machine column against the SAME ROW of open ground, that threshold was calibrated on a BARE hillside at RN-10 (0.0267 edge darkening, ratio 13.2), and RN-15 then planted an understorey over it at 0.55 screen coverage. A grass card is an occluder and a 0.9 m AO radius resolves it, so the denominator became a second copy of the numerator's physics. The fix takes a second profile with `of.propsVisible(false)` and asserts attribution there, keeping the shipped frame for the checks that belong to it; **no threshold was tuned. It now PASSES for the first time: control centre 0.3335 against edges 0.0007, ratio 476.4, where the shipped frame reads 1.4658.** Two further real defects fell out: the ratio's `Infinity` on a zero denominator PASSED the attribution check on a dead effect, and the peak-band search ran over bands the file's own comments call invalid. **(7) FOUR INSTRUMENTS WERE BROKEN TONIGHT AND THREE OF THEM WERE MINE, ALL THE SAME SHAPE: A CONTROL THAT DEPENDS ON SOMETHING THAT MOVED.** (a) The macro claim was measured over a band 55% covered in grass, so a real term read as a 0.956 DECREASE. (b) The sky control was a band of the shooting frame, which is sky at Hills and is mountains at the 4,668 m spawn, so the probe reported the term had escaped into the sky when the BAND had stopped being sky; it is now taken at pitch +72 over the top 45% of the frame, which is sky at every site and every altitude, and reads **mean 0.0000 and peak 0 over 648,000 px at BOTH sites**. (c) The lower frame of that same up-look moved by a mean of 0.78 with a peak of 194: the animated first-person arms, which `probes/post.js` already documents and excludes. (d) A noise floor was added and the FIRST version of it read EXACTLY equal to the effect it was bounding, to four decimal places, on every band, because the state was not reset before the floor capture. **Two numbers agreeing to the last digit is a wiring diagnosis, never a coincidence.** With it fixed the floor is 0.0000 and every claim is asserted against it rather than against an unreachable zero. **(8) RN-47, THE UNDERWATER VIEW, AND THE APPROVED DW-10 SLOT WAS NOT SPENT.** Admin approved one scene shader slot for water. It was built as a POST pass instead, so the ledger is untouched: RN-10 established post programs cannot be infected by the depth policy (no projection, no depth test, no `gl_FragDepth`), and a post pass also reaches the terrain, the props, the machines and the scatter at once with no per-material edit, using the depth buffer a scene material would have had to recompute. Beer-Lambert per channel with red absorbed roughly 22x harder than blue (Smith and Baker 1981), the water surface treated as a PLANE because curvature over a 22 m pond is under a millimetre, and the exit distance `headUnderM / max(dot(rd, up), eps)` giving the same confined-layer structure the aerial perspective already uses. **Measured, eye 1.346 m under, 921,600 px: red over blue 1.0493 to 0.5813, mean red -44.34 against mean blue +3.36, 81.2% of pixels moved, peak 152.** **The negative controls are BIT-EXACT rather than approximate**, because screenshots cannot reach zero here (sim time advances between captures) so they were taken on `of.framehash()`, which renders and reads back synchronously: dry at 300 m gives the identical hash `3035253034` both ways, and WADING at 1.0 m depth with `headUnderM` -0.62 gives the identical hash `1014643489`, while the same instrument submerged gives two different hashes. The wading control is the sharper one: a pass keyed on "is the player wet" passes the dry control and fails that. Above water the pass is SKIPPED rather than multiplied by one. **Cost: +3 post draw calls and +3 programs when submerged, ZERO when dry, +3.5 MB allocated lazily on the first submerged frame.** It also caught a framebuffer feedback loop the same way RN-10 did: the first shape reported `ran: true`, counted its draws and moved exactly zero pixels while the driver emitted 256 `GL_INVALID_OPERATION` per frame. **NOT DONE, and named: the first-person arms are not attenuated (they are drawn after the interpose point, so it needs a second interpose); the water plane has no lateral bound, so a level ray inside a shallow puddle stays submerged past the bank; a free camera is never tinted because `swim()` needs a capsule; and there is still no surface seen from beneath.** **NOT COMMITTED, DELIBERATELY: `web/src/app/Boot.ts`.** It carries the vessel lane's uncommitted `resumeWorld` call, whose `ResumeBoot.ts` is still UNTRACKED, so a commit of Boot.ts alone would not build. My two hunks there are three lines and will ride with that lane's commit. Every other file is committed path-limited. New: `web/src/render/materials/TerrainArt.glsl.ts`, `web/src/render/post/UnderwaterGlsl.ts`, `web/src/render/post/UnderwaterPass.ts`, `web/tools/smoke/probes/{terrainart,artshot,underwater}.js`. Isolation: `?terrainart=0 ?macrovar=0 ?terrainbump=0 ?strata=0 ?macroamp= ?bumpamp= ?strataamp= ?proplod2=0 ?grassshort=0 ?scatterwet=1 ?underwater=0 ?uwext= ?uwtint= ?uwscatter= ?uwpath=`, all registered in `run.mjs`. Runtime handles `window.__ofTerrainArt.{set,get,reset}` and `window.__ofUnderwater.{set,state,setTune}`. Screenshots `docs/screenshots/RN45_world_{before,after}.png` (the RN-15 camera, so RN-15, RN-30 and this pass are all comparable), `RN45_terrain_{before,after}.png` (props hidden, which is the only way to photograph a ground material), `RN45_iso_{macro,bump}.png`, `RN_underwater_{on,off}.png`. **FRAME TIMINGS ARE NOT PUBLISHED. Four lanes were building and probing on this machine and the ring drifts by more over a minute than any of these effects costs. The invariant table is the whole cost claim and it is not noise sensitive.**
>
> **RN-30 (2026-07-27): THE GROUND STOPS BEING FLAT, AND THE AERIAL-PERSPECTIVE TERM LANDS ON THE SECOND ATTEMPT BECAUSE THE CONFINEMENT CHANGED RATHER THAN THE TUNING.** Three changes, one binary pair plus a one-binary isolation series, and the headline is that all three sky controls read **exactly 0.0000 mean and 0 peak over 276,480 samples**. RN-15 bought 3.7x the foliage by taking the understorey out of the shadow pass, and the picture paid for it: every grass shadow in the frame went with the 12.3 ms. **(1) SHADOW CONTACT COMES BACK AS TWO COMPLEMENTARY TERMS, NEITHER OF WHICH IS A CASCADE, AND THE REASON IT IS NOT A CASCADE WAS CHECKED IN THREE'S SOURCE RATHER THAN ASSUMED.** `WebGLShadowMap.renderObject` filters casters with `object.layers.test( camera.layers )` at r185 line 511, where `camera` is the VIEW camera passed to `shadowMap.render( shadowsArray, scene, camera )`, **not** the per-light shadow camera, so `light.shadow.camera.layers` is never consulted anywhere in the shadow pass and layers cannot select a cascade. `Object3D.onBeforeShadow` does fire per object per light but at lines 535/549, i.e. AFTER the `castShadow` test, so it cannot veto a draw either; the only supported per-cascade signal is `_frustum.intersectsObject`, and the understorey is ONE `BatchedMesh` whose bound covers the whole ring, so every cascade intersects it. "Understorey in cascade 0 only" needs a fork of three's shadow map. **This also convicts a live comment in my own lane's code: `ShadowRig.ts:59-73` asserts exactly the layer behaviour disproved above and enables `LAYER_PLAYER_BODY`/`LAYER_PROPS` on cascade 0's shadow camera to buy a restriction it does not buy.** The props and the player reach the shadow map because `CameraRig` enables those layers on the NEAR camera. The code is harmless, the comment is false, and the 45-to-27 draw-call measurement recorded beside it was real with another cause. So: **(1a) A BASE-CONTACT GRADIENT BAKED INTO THE FOLIAGE VERTICES, at literally zero cost.** A plant is darker where it meets the ground because the ground and its own neighbours occlude the sky there; that is an ambient-occlusion fact about the ASSET, so it belongs in the asset's vertices. `PropGeometry.bakeColour` writes 0.42 over the bottom 38% of each geometry's own height, smoothstepped, into the per-vertex colour attribute **that already existed and was a constant 255**. No draw call, no triangle, no shader, no uniform, not one extra byte. It survives per-instance tinting because three composes `vColor = 1` then `vColor.rgb *= color` then `vColor *= getBatchingColor(...)`, so the gradient and `PropLibrary.tint` MULTIPLY (`color_vertex.glsl.js`, read rather than assumed). **Measured: near band mean darkening 0.0323, p99 0.3705, 17.9% of pixels moved over 236,195 px; mid band 0.0348 / 0.3861 / 25.4%; 7 batches carry it.** **(1b) SCREEN-SPACE CONTACT SHADOWS, `post/ContactGlsl.ts` plus `post/ContactPass.ts`, two full-screen passes.** A depth-buffer march toward the sun bounded to 0.45 m of WORLD distance, which is what makes it a contact shadow rather than a shadow: it physically cannot see a hill, so it cannot disagree with the cascaded maps about one. It divides from cascade 0 by RANGE exactly as the baked ORM map divides from SSAO by spatial frequency, and cascade 0 at a +/-15.8 m box and 15.5 mm per texel cannot resolve a 3-triangle blade against the soil under it. **Measured: near band mean darkening 0.1012, p99 0.6581, 40.4% of pixels moved over 237,762 px; mid band 0.0521 / 0.3744 / 42.4%.** The sun direction is derived from `shadowCascade0`'s own position minus its target, which is not a second authority because ShadowRig already merged the sun and cascade 0 into one light; when the rig is built disabled the vector is zero and the march skips, so `?shadows=0` also removes contact shadows, which is correct rather than incidental. **(2) THE UNDERSTOREY STOPS READING AS A CROP FIELD, and the durable half of the fix is structural rather than a smaller number.** RN-15 had already retuned the height jitter once for this (1.48 down to 1.30) and it did not hold, because the jitter COMPOUNDS with the distance upscale: `Detail_GrassCardB`'s authored 0.60 m reached 0.60 x 1.3 x 1.30 x 1.32 = **1.34 m**, chest height on a 1.8 m character, and a field of 1.3 m blades has no middle distance in it. The band drops to 0.40 to 0.82 AND `DETAIL_FAR_GROW` is demoted to a HORIZONTAL upscale, so height is no longer a factor in that product at all. **Density-weighted mean understorey height 0.4262 m to 0.2811 m (-34.0%); tallest card in the world 1.34 m to 0.64 m (-52%).** Coverage is held rather than spent: the width band gains 1.18x, and measured screen coverage moves 0.5503 to 0.5456 across three repetitions each, i.e. -0.9%. Capping a product by shrinking one factor has to be repeated every time either factor moves; taking a factor out of the product holds. **(3) AERIAL PERSPECTIVE SHIPS, AND THE ONLY THING THAT CHANGED FROM THE REVERTED ATTEMPT IS THE CONFINEMENT.** The first attempt put the aerosol INSIDE `ofAtmoScatter` and relied on a 400 m scale height to keep it off the sky; that is confinement by HEIGHT, near-horizon sky rays lie inside the layer for tens of kilometres, and it failed its own control (sky saturation 0.494 to 0.410, sky red 81.9 to 97.0). **The seam that works already existed and nobody had named it: every escaping ray in this codebase passes `tMax = 1.0e9` and every terminating ray passes a real metre distance.** So `ofAtmoAerial` is a SEPARATE ENTRY POINT called from exactly one site, `TerrainShader`'s aerial-perspective line, which is the only call with a finite distance to geometry. The sky quad and the terrain's own upward sky-ambient ray both pass 1.0e9 into the other function and cannot reach it. "The sky did not move" stops being a tuning result and becomes a property of the call graph, **and a `#define` keyed on "is this the terrain material" would have got it wrong, because the sky-ambient ray IS a terrain fragment issuing an escaping ray.** Two further things had to be right. The optical depth is ANALYTIC (`L * H / (h1 - h0) * (exp(-h0/H) - exp(-h1/H))`, the closed form for a linear height profile) rather than marched, which is what makes it safe from orbit: a viewer 100 km up collects about H worth of dense air however long the ray is, where a midpoint or Simpson rule weights its samples by the FULL path length and would produce an enormous optical depth for a scaled planet. And the layer sits on the TERRAIN, heights measured from `min(h0, h1)`, not on the datum: measured from the datum the same term is 2x weaker on an 860 m hillside than at sea level and vanishes on a mountain, so one pair of constants would have to be retuned per site. Shipped at sigma 4.5e-4 per metre (Koschmieder 8.7 km), H 400 m, grey tint (0.38, 0.39, 0.43), g 0.35 with a 0.55 isotropic multiple-scattering floor. **The tint is grey because of the first attempt's finding, which is the useful one: a blue-biased coefficient does not haze, it DARKENS** (far-band red 70.2 to 57.0 while saturation moved 0.004), because it extinguishes red hard and refills with saturated blue. The g and floor pair was set to compress the phase function's dynamic range: g 0.55 spans 45:1 between the solar and anti-solar directions, which would make the distance alternately glow and darken as the player turns; g 0.35 with a 0.55 floor spans 2.8:1 and the haze is brighter than the ground it veils in every direction, which is what haze means. **(4) A DEFECT IN MY OWN INSTRUMENT, and it is the reason the first tuning read as doing nothing.** `probes/groundart.js`'s "far band" is frame rows 0.42 to 0.52, and at the pinned Hills camera the horizon sits at about 0.28, so that band is ground **30 to 80 m away**: near enough that no atmosphere on any planet would change it. The term was measured against the wrong range and read 0.3% of pixels moved. A `horizon` band at 0.29 to 0.38 was ADDED rather than the old one moved, so the RN-15 numbers stay comparable. **RESULT AT THE LOCKED RN-15 CAMERA (frozen builds `RN30-before` / `RN30-after` per BT-27, `vite preview`, 1280x720, sandbox, walk, seed 0xbf00d01, lat 12 / lon 150 / yaw 300 / pitch -10, sunT 0.30, RTX 4060 Ti, three repetitions each, interleaved BEFORE/AFTER): horizon saturation drop 0.0254/0.0322/0.0258 to 0.2148/0.2141/0.1994, i.e. mean 0.0278 to 0.2094, a 7.5x; horizon blue shift -0.0551/-0.0475/-0.0548 to +0.1199/+0.1191/+0.1019, i.e. THE SIGN FLIPS (distant ground was redder than ground underfoot and is now bluer); horizon band mean red 118.57 to 132.48 and mean blue 66.22 to 95.13; RN-15's own near-against-far saturation drop -0.185 to -0.139, still negative and correctly so, because that band is 30 to 80 m. Screen coverage 0.5580/0.5582/0.5346 to 0.5449/0.5429/0.5489. Draw calls 67 to 69 of a 150 budget, triangles 1,087,115 to 1,087,719 of a 2.7 M alert, programs 36 to 38, geometries 50 to 50, VRAM 92.8 to 93.7 MB. Props placed 45,854, instances 48,511, `deliveredFraction` 1.0018, pool refusals 0, `cellsCapped` 0, `chunksCapped` 0, all unchanged on both sides. Frame p50 11.3/10.5/12.3 to 10.6/10.9/9.9, p95 20.7/17.3/20.9 to 18.3/18.1/25.9, p99 26.9/19.5/29.8 to 28.2/20.8/42.5, worst 39.2/21.1/46.0 to 73.1/24.6/71.1.** **THE FRAME NUMBERS ARE PUBLISHED AS UNRESOLVED AND THAT IS THE HONEST READING, not a hedge.** An interleaved A/B/A/B series inside ONE binary with medians (RN-10's method) gives p50 all-on 11.4, contact-off 10.6, aerial-off 10.3, both-off 10.7, which is **non-monotonic**: both-off is slower than contact-off, and the within-label spread for "all on" alone is 10.0 / 11.4 / 11.5. Four other lanes are building and probing on this machine tonight and the frame ring drifts by more over a minute than any of these effects costs. What IS measurable and is not noise: **+2 fixed draw calls, +2 programs, +0.9 MB of R8 buffer**. An earlier quiet-machine reading had the BEFORE binary at p50 5.8 and I nearly reported a 4.8 ms regression from it; re-measuring the same binary later gave 10.2, which is the whole argument for paired repetitions rather than one A then one B. **THE ORBITAL CONSEQUENCE IS REAL, QUANTIFIED AND DELIBERATE, and it is stated rather than hidden because the same terrain program draws the scaled far planet and a ray from orbit to the ground is a TERMINATING ray.** The vertical column through the layer is exactly sigma x H = 0.18 by construction, so 16% haze. Measured at `?scenario=orbit`, 300 km, the term toggled inside one settled frame: planet disc saturation 0.4017 to 0.2789, mean red 100.23 to 120.66, 27.9% of the frame moved with a peak of 56 counts, and the disc is 34.7% of the frame. At `?scenario=ascent`, 12 km: saturation 0.4511 to 0.4266, mean red 91.68 to 99.53. The picture reads as a planet with air over its land rather than as a grey ball (`docs/screenshots/RN30_orbit_{before,after}.png`), and the scale height is the knob if that judgement changes: it does essentially nothing to a ground-level horizontal ray and is entirely what the orbital column costs. **ISOLATION, and it is weaker than this lane's standard for two of the three, for an ownership reason rather than a technical one.** `?contact=0` plus `?cslength= ?cssteps= ?csthick= ?csstrength=` are query flags (standing rule 7). The other two are RUNTIME handles instead, `window.__ofProps.setBaseShade` and `window.__ofAtmos.setAerial`, on the `Surfaces.ts` precedent, because everything that configures the scatter and the atmosphere is passed by `Boot.ts` and the VAB lane owns Boot this round. That turned out to be the STRONGER instrument for the two claims that are matched pairs (one settled frame apart, so camera, sun and streamed chunk set cannot differ), but **the grass HEIGHT has no runtime control at all and its before/after is a binary pair**; `?grassshort=` is one line in `Boot.ts` and `Config.ts` and is a coordination item, not a design gap. **NOT DONE, DELIBERATELY, AND THE LEDGER IS NOT SPENT.** Admin approved a DW-10 shader-ledger entry for an `onBeforeCompile` giving props aerial perspective, conditional on a working term shipping. The term shipped, so the approval stands available, and **I am declining to spend it on the measurement rather than on the principle**: props reach `RADIUS_M` = 170 m, and at 4.5e-4 per metre the optical depth at the ring's outermost instance is 0.0765, so a prop out there is drawn at 100% of its own radiance where the terrain at the same range is at 92.6%. A bounded 7.4% worst-case discrepancy is not worth a slot in a cap of five, and it shrinks if the scatter radius ever does. **PRE-EXISTING AND NOT MINE: `probes/post.js` returns `valid: false` on BOTH binaries with an identical three-item `fails` list** ("AO is not a global multiply", "AO darkens the machine column far more than the SAME ROW of open ground", "open ground in the peak row is essentially unchanged"), while its underlying numbers are healthy and moved the right way (centre-over-edges 1.5975 to 1.7359, p99 0.6676 to 0.7046). The strings read like the probe's PASS conditions inverted. `grass.js`, `cutbank.js`, `maps.js` and `tools/smoke/boot.mjs` are green on the after binary. New: `web/src/render/instancing/PropGeometry.ts`, `web/src/render/post/ContactGlsl.ts`, `web/src/render/post/ContactPass.ts`, `web/tools/smoke/probes/contact.js`, `web/tools/smoke/probes/aerialrange.js`. Screenshots `docs/screenshots/RN30_world_{before,after}.png` (the RN-15 camera, so all three passes are comparable), `RN30_contact_{on,off}.png` (one binary, `?contact=0`, pitch -4), `RN30_orbit_{before,after}.png`.
>
> **RN-15 (2026-07-27): THE GROUND. 3.7x THE FOLIAGE FOR 40% FEWER TRIANGLES AND A THIRD OFF THE FRAME, AND THE THING THAT PAID FOR IT WAS NOT A NEW ASSET, IT WAS TWO STALE RENDER-POLICY DECISIONS.** Reid: "the terrain, trees rocks, ground, etc look like shit still. Im talking satisfactory (the game) level graphics", while being explicitly HAPPY with the rocket and the pad, which is what said the gap was WORLD art and not the renderer. **THE BUDGET WAS FOUND BEFORE ANY OF IT WAS SPENT, and that is the whole shape of this pass.** At a fixed Hills camera (lat 12, lon 150, yaw 300, pitch -10, 1280x720, sandbox, one binary) the isolation flags said: **`?props=0` removes 1,383,872 of the frame's 1,804,969 triangles and 15.0 ms of its 18.0 ms p50; `?shadows=0` removes 1,203,460 triangles and 14.5 ms; `?detail=0` removes 1,003,112 triangles and 12.3 ms for 9,738 understorey cards.** That last is about 103 triangles for a card whose LOD0 is 18 to 42, because **every card was being drawn FOUR times**, once in the near pass and once in each of three shadow cascades. A 0.36 m card's cast shadow is a few pixels under a tuft that is already casting one. **(1) THE UNDERSTOREY LEFT THE SHADOW PASS, and it needed its own batches to do it.** `castShadow` is a property of the MESH and `Detail_GrassCardA/B` share `OF_Grass` with `Plains_GrassTuftA/B`, so there was no way to spend it per prop; `PropLibrary` now registers the shared atlas into `<material>:detail` batches (`DETAIL_SUFFIX`), read off `SHARED_ATLAS` rather than passed in, so Boot.ts kept its one-line change. Two things fell out free and both were wanted: the understorey got its OWN pool ceiling instead of sharing the one that was full at 16,384, and `stats().perMaterial` now reports the two layers separately with a `casts` flag, so a triangle count that does not say which batches were in the shadow pass can no longer be read. **(2) PER-INSTANCE FRUSTUM CULLING WENT BACK ON FOR THE UNDERSTOREY, AND THIS IS THE ONE TO REMEMBER: the comment in `PropLibrary` said culling "costs far more than it saves", it was RIGHT, it was measured at 9,340 props, and DENSITY WALKED IT OVER THE LINE without touching a line of that code.** The scatter is a RING centred on the player, so about three quarters of it is behind the camera and every one of those instances is still a `multiDrawElements` range the driver walks. Measured, same binary, `?propcull=0` as the control: **triangles 1,916,689 to 1,087,115, p50 22.8 to 11.3 ms, p99 33.6 to 20.2 ms, and screen coverage 0.5545 against 0.5563, i.e. the same picture.** This is DW-19's lesson pointed at a second constant: a measured trade is only valid at the scale it was measured, and the failure mode is silent. **WHAT THE BUDGET BOUGHT: understorey density 3.6x** (GROUND_DETAIL 906,000 to 3,228,000 instances per km2), **a graded ring instead of a hard edge** (`DETAIL_FULL_M` 30 m at full density falling linearly to 0.18 at `DETAIL_RADIUS_M`, which went 55 to 78 m, because the ring EDGE was the second thing the eye found after the bare ground and a ring that ends is a ring you can see the edge of at any radius), **per-instance colour** (`BatchedMesh.setColorAt` value jitter plus a hue drift toward dry plus 5.5% flowers, all the same card geometry so no new asset, no draw call and no triangle), **non-uniform scale** so height and width are drawn separately, and **CONTACT BLENDING**: five understorey cards tucked around the base of every prop the registry marks `collides`, which is the same set as "has a silhouette that meets the ground", so the skirt follows the asset table rather than a second list. **RESULT AT THE LOCKED CAMERA, three repetitions after against two before: props placed 12,379 to 45,854, instances 15,020 to 48,511, foliage screen coverage 0.4118/0.4178 to 0.5563/0.5568/0.5586, draw calls 72 to 67 of a 150 budget, triangles 1,804,969 to 1,087,115 of a 2.7 M alert, frame p50 17.4/18.0 to 10.8/10.9/11.1/11.3 ms, p95 23.9/25.2 to 16.1/16.5/16.6, p99 29.0/44.7 to 19.1/20.2/20.4/21.5 against a 25 ms alert the BEFORE was outside and the after is inside, worst frame 61.0/72.9 to 21.7/23.8/24.8/26.2. Pool refusals 0, `cellsCapped` 0, `chunksCapped` 0, VRAM 92.8 MB.** `MAX_CAPACITY` went 16,384 to 65,536 and that is a memory guard and nothing else (DW-28): a slot is about 100 B of typed array and 80 B of texture, a batch only reserves what it has grown into, and the batch that actually reached the new range is `OF_Grass:detail` at 32,881. **TWO DEFECTS OF MINE, both found by instruments this project already had. (a) `probes/grass.js` failed at `deliveredFraction` 1.5146**: the contact skirt PRODUCES cards it never REQUESTED, which breaks RN-7's ratio in the direction that looks harmless, and a ratio that can read 1.5 can no longer be trusted to read 0.98 when something is genuinely being dropped. A skirt card is not a density draw, its request is exactly `CONTACT_CARDS` per colliding prop, and counting it that way restored 0.9997 while still catching truncation. **(b) A PICTURE CAUGHT WHAT NO NUMBER DID**: the card height jitter topped out at 1.48 and COMPOUNDS with the distance upscale at 1.45, so a card authored at 0.58 m could reach 1.2 m, put the understorey at the same height as the tufts it is meant to sit under, and made a meadow read as a cornfield. Two multipliers that were fine apart and wrong together. **AERIAL PERSPECTIVE WAS BUILT, MEASURED AND REVERTED, and the measurement is the deliverable.** The gap is real and is quantified: near band against far band IN ONE CAPTURE (so exposure, sun elevation, tone curve and colour grade are equal by construction and range is the only free variable) reads **saturation -0.149, i.e. the DISTANT ground is MORE saturated than the ground at the player's feet.** There is no aerial perspective at playable range, and the reason is that the Rayleigh term is CORRECT and therefore useless here: Earth's molecular scattering moves a ridge by about one part in a hundred over the 200 m to 3 km a player looks across. A boundary-layer aerosol term was added to the SHARED model (DW-22, never three's `FogExp2`, which would have been free and would have been a second authority disagreeing with the terrain integral at every range). It did not work and it failed in two informative ways. Biased blue like Rayleigh it DARKENED rather than hazed (far band mean red 70.2 to 57.0, saturation 0.7072 to 0.7008, i.e. it moved by 0.004), because a blue coefficient extinguishes red hard and refills with saturated blue; grey coefficients plus an isotropic multiple-scattering floor of 0.22 on the phase improved it (red 70.2 to 60.9, blue over red 0.393 to 0.460) **and then failed its own control: the SKY band moved, saturation 0.494 to 0.410 and mean red 81.9 to 97.0.** The 400 m scale height was supposed to keep it off the sky structurally and does not, because near-horizon sky rays sit inside the layer. Shipping a half-tuned change to the one model the sky, the near terrain and the far scaled planet all read is precisely the cross-lane cost this log keeps recording, so **it is fully reverted and `Atmosphere.glsl.ts` is byte-identical to main.** The sky control is what made that call possible and it is kept in the probe. **NOT DONE, deliberately:** the colour grade (item 5) already implements cool shadows against warm sun (`shadowTint` [0.97, 0.995, 1.035], `highlightTint` [1.035, 1.005, 0.96]) and retuning another pass's measured output on the last run of a session is a poor trade; new rock and cliff meshes (item 4) are an art-lane job; **props still get NO aerial perspective at all while the terrain behind them does**, which is a real inconsistency I found and did not fix, because the honest fix is an `onBeforeCompile` on the prop materials and that bumps the DW-10 shader ledger, which is an Admin decision and not mine. New: `web/src/world/ScatterEmit.ts`, `web/src/world/ScatterLook.ts`, `web/tools/smoke/probes/groundart.js`. Isolation: `?propcull=0` (standing rule 7). Screenshots `docs/screenshots/RN15_world_{before,after}.png` (same seed, site, camera, viewport and probe; the before from a pre-change build of the same tree) and `RN15_nocull.png`.
>
> **RN-10 (2026-07-27) — THE POST-PROCESSING STACK (DW-35 item 1). THE ENTIRE POST STACK WAS ONE LINE OF ACES TONEMAPPING; IT IS NOW AMBIENT OCCLUSION, BLOOM, A COLOUR GRADE AND FXAA, FOR +15 FIXED DRAW CALLS, +27.7 MB AND +1.0 ms p50.** New `web/src/render/post/` (8 files), driven surface `web/src/app/DebugPost.ts`, probe `web/tools/smoke/probes/post.js`. **THE FOUR-PASS RIG IS NOT TOUCHED AND THE STACK IS NOT A FIFTH PASS.** It brackets the four (bind an HDR target first, resolve to the canvas last) and interposes at exactly ONE point inside them: after the NEAR pass and BEFORE its `clearDepth()`. That instant is the only one in the frame at which the depth attachment holds the near 1:1 scene and nothing else, which is what makes depth-only ambient occlusion correct here: the sky writes no depth, the far scaled pass was cleared, and the view model is not drawn yet. Moving that one call one statement later would hand AO a cleared buffer, every pixel would read as background, the effect would vanish, and every frame-time number would stay healthy. So `probes/post.js` asserts occlusion CONTRAST, never that AO ran. **REVERSED-Z IS HANDLED BY UNPROJECTION, NOT BY A HAND-ROLLED LINEARISE.** `post/DepthGlsl.ts` inverts whatever projection `DepthPolicy` built, so the reversed flip undoes itself; `DepthPolicy` gained `clearValue()` (0 reversed, 1 otherwise, and getting it backwards makes AO skip the world and process the sky) and `logFC()` for the log branch, which is a closed-form `w` recovery because an inverse projection applied to a log-encoded sample is silently wrong rather than visibly wrong. All three depth modes are COMPILED VARIANTS switched by `#define`, so a mode that is not selected cannot run. **AO IS GTAO FROM DEPTH ALONE: no normal buffer, no second geometry pass.** The alternatives were MRT out of every scene material (which means editing the terrain, atmosphere and starfield shaders DW-10 caps at five) or an override-material prepass (which doubles the near scene's 53 draw calls against a 150 budget). Normals come from four depth taps. **DW-10 LEDGER: scene custom shaders UNCHANGED at 3 `ShaderMaterial` plus 3 `onBeforeCompile` sites; post adds 9 full-screen programs (AO, its denoise, its upsample, its apply, two bloom downsample variants, the bloom upsample, the composite and FXAA) that CANNOT be infected by the depth policy, because they have no projection, no depth test and no `gl_FragDepth`.** **MEASURED, one binary, one camera, one settled frame pair via runtime toggles: AO peak row-band centre darkening 0.3528 against 0.0267 in the SAME ROW of open ground, ratio 13.2; median lit pixel 0.0000; p99 0.5974.** Same row is the control: it holds range, sun elevation and the lighting gradient equal and leaves proximity to a machine as the only free variable, which is RN-8's circular-pit argument applied to occlusion. **COST, as PAIRED repetitions rather than one A then one B: master switch +1.0 ms p50 (5.7 to 6.7), reproduced across two independent runs; the per-effect deltas fall INSIDE their own run-to-run spread and are published as unresolved rather than as numbers.** Draw calls 53 scene + 15 post = 68 of a 150 target, and the two are published SEPARATELY (`PassTimings.sceneCalls` / `.postCalls`) so a fixed full-screen cost cannot quietly eat another lane's headroom. VRAM 75.1 to 102.8 MB, and `StatsProbe` now reads it LIVE off the renderer instead of from `Boot.ts`'s boot-time snapshot, which was sized for a 300x150 canvas and read 0.9 MB for a 27.7 MB allocation. **FOUR DEFECTS FOUND BY MEASUREMENT, ALL FOUR MINE, AND THREE OF THEM LOOKED FINE.** (1) **A FRAMEBUFFER FEEDBACK LOOP**, 256 `GL_INVALID_OPERATION` per frame: the AO apply targeted the scene while sampling the depth texture that is one of that framebuffer's attachments. Fixed structurally, by resolving the depth-aware upsample into its own full-resolution buffer first, so the one pass that touches scene colour samples exactly one texture the scene framebuffer does not own. This is the only one of the four that failed loudly, which is why it is not still there. (2) **THE COLOUR GRADE CRUSHED THE SHADOWS**: contrast 1.06 about a pivot of 0.4135 applied to the ACES output, where mid-grey is 0.18 and not 0.41. Sky red **37.9 to 7.9 of 255**, invisible in any whole-frame mean because the crush is confined to the dark end. The grade now runs AFTER the sRGB encode where the pivot means what it says; sky red reads 29.7. (3) **MY FIRST BEFORE/AFTER PAIR WAS INVALID AND LOOKED LIKE A RESULT.** The build recipe places machines by trying pitches until a ghost is legal, so two runs consume different amounts of sim time, and the sun moves with sim time. The pair differed by a warm-to-cool shift across the whole hillside that read exactly like a colour grade and was entirely sun angle. `of.setTime()` is now re-pinned after every data-dependent wait and immediately before the captured frame. (4) **MY OWN PERCENTILE METRIC PASSED ON PIXELS THE EFFECT NEVER TOUCHED.** `darkening = 1 - with/without` divides by nothing on an already-black pixel and reads 1.0, so the probe reported p99 and max darkening of exactly 1.0000 and PASSED its "the 99th percentile moves a lot" check. A ratio metric needs a floor on its denominator, always. **AND ONE THAT WAS NOT A DEFECT AT ALL: I read a green wash across the ground in the after image, and I was wrong.** Measured region by region, the ground is 1.5% darker and its green bias is present IDENTICALLY in the before. It was a sequential-viewing illusion; only measuring the two captures settled it. That is WG-28's lesson pointed the other way: looking at the thing is necessary and is not sufficient. **THE TWO OCCLUSION SOURCES (the texture lane's baked ORM map and this one) ARE SEPARATED BY SPATIAL FREQUENCY, STRUCTURALLY.** `aoRadiusM` 0.9 m on a half-resolution buffer cannot resolve a panel gap, and the probe proves it by re-running the entire measurement at a 5 cm radius and requiring it to produce nothing. The first version of that control asserted "a machine face darkens less than 6%", measured 10.1%, and would have been repaired by moving the threshold; it was sampling the smelter's own recessed panels, which are decimetre-scale and are legitimately this term's business. **STANDING RULE 7 IN FULL: `?post=0` restores the pre-lane path exactly** (no render target, no composite, three's own ACES straight to the canvas, 0.9 MB), and `?ao=0`, `?bloom=0`, `?grade=0`, `?aa=0` isolate one effect each, plus `?aoradius= ?aostrength= ?aoscale= ?aoslices= ?aosteps= ?bloomstrength= ?bloomthresh= ?exposure= ?msaa= ?fxaalod=0`. **`?clear=` and `?scenario=zfight` force the stack OFF by default**, because both read RAW PIXEL VALUES: magenta through ACES, an sRGB encode, a split tone and a vignette is not magenta within `Loop.countHoles`'s 12-count tolerance, so every crack and hole probe would read ZERO holes and PASS. That is Boot.ts:114's existing argument applied to a layer that recolours every pixel. **NO TEMPORAL JITTER ANYWHERE**, because this project asserts that a settled frame equals the previous one (FrameDiff's second difference; `wires.js` requiring five identical draw-call reads): the AO rotation is a pure function of `gl_FragCoord` with a period of exactly 4 px, and the denoise kernel is exactly 4x4. Screenshots `docs/screenshots/RN9_{contact,terrain}_{before,after}.png`. **HONEST LIMIT: this is one lane of DW-35 and it is not Satisfactory yet.** No screen-space contact shadows, no depth of field, no temporal AA, and the largest remaining gap is the materials ceiling DW-35 names second, which is RN-9's lane and not this one.
>
> **RN-9 (2026-07-27) — THE SHIPPED TEXTURES REACH THE SCREEN. Three changes, all three landed, and the hand-off's one predicted failure mode turned out not to exist in this three version.** Commit `87f9846` shipped two tiling PBR surfaces and nothing changed on screen because no client path consumed them (ASSET-SPECS 2.9). **(1) `uv` now copies through all three `normalize()` functions UNCONDITIONALLY**, via one shared `Surfaces.copyUv` rather than a fourth copy-paste, because the conditional form is what reintroduces the mixed-attribute merge that returns `null` and degrades to one drawn primitive. Measured across the three paths: **294 geometries copied real UVs, 0 synthesised, 0 count mismatches** — and per consumer, because a single total would have let the machine path pass on the nodes' UVs (`machines 105 / nodes 55 / props 134`). **(2) The maps are attached** at all three sites with `RepeatWrapping`, `repeat = 1/tile_m` (0.667 for `panel` at 1.5 m, 1.333 for `coarse` at 0.75 m), `NoColorSpace`, anisotropy 16 (three clamps to the device max at upload), one ORM texture in three slots. **(3) `NodeBatch.familyOf` buckets by (family, metalness)**, so the node batches went from `matte`/`metal` to **`coarse:matte` / `flat:matte` / `coarse:metal`**: bark and rock keep the rock map, **Leaf, LeafDeep, LeafDry, LeafLight, Grass, Ice and Oil land in `flat:matte` and carry nothing**. **THE NORMAL MAP IS DEMONSTRABLY READ, NOT MERELY ATTACHED, and that is the assertion the structural half cannot make.** Toggled inside ONE settled frame (`window.__ofSurfaces.setMaps`, the `PropLibrary.setVisible` pattern, so camera, sun, streamed set and terrain cannot differ between captures) and split `normal` from `orm` so the difference is ATTRIBUTABLE: **normal map alone moves the ground box by mean 2.78 counts over 288,000 px with 18.6% of pixels moving more than 3 counts and a peak of 375; ORM alone 1.87 / 23.7%; both 3.37 / 24.7%. The negative control is a sky box in the same captures at exactly 0.000 mean and 0 peak over 72,000 px**, which is what makes the ground number the maps' and not a global exposure wobble. Ground mean luminance 92.41 -> 91.80, i.e. very slightly DOWN, which is the direction ORM-multiplies predicts (wear and grime can only take a surface below its palette constant). **`aoMap.channel = 0` is set and asserted, and the hand-off's prediction that three defaults it to a nonexistent `uv1` is OUT OF DATE for r185**: `Texture.channel` defaults to 0 and `WebGLPrograms` reads `material.aoMap.channel`, so the predicted silent-tint failure could not have occurred here — verified in `node_modules/three/src/textures/Texture.js:118` and `WebGLPrograms.js:273`, not assumed. The line is kept because the ORM image is ONE object in three slots. **THE MACHINE BATCH'S CUSTOM SHADER EDIT WAS THE ONE THING PIXELS COULD NOT REACH** (the walk scenario places no machines), so it is asserted off the REAL shader string three hands `onBeforeCompile`: `roughnessmap_fragment` 2605, `metalnessmap_fragment` 2639, `normal_fragment_maps` 2707 all resolve before the `emissivemap_fragment` hook at 2827, and `aomap_fragment` at 2996 is applied after it, on all four `MachineBatch` instances (`factoryMachines`, `beltCargo`, `structures`, `launchpads`). That is the ordering claim ASSET-SPECS 2.8 rests on when it refuses an albedo map, checked rather than remembered. **COST, from two builds of the same tree differing ONLY by this change, same seed and same input tape: draw calls 60 -> 64 of a 150 budget, and the +4 is exactly the one extra node bucket times the main pass plus three shadow cascades; triangles 849,513 -> 849,513 unchanged; geometries 45 -> 46; programs 32 -> 34; frame p50 6.60 -> 6.70 ms and p99 10.40 -> 10.20 ms. Texture VRAM +4.17 MiB measured** (3.125 MiB base plus the 4/3 mip chain), which `stats().vramEstimateMB` does NOT include because that counter is pool bytes only and stayed at 102.8 both sides — flagged, not fudged. **THE CLIENT'S ROLE TABLE IS CHECKED AGAINST THE SHIPPED MANIFEST at runtime** and disagreement is a `console.error`, because the table has to be a synchronous copy: `NodeBatch.build()` buckets a material the instant the glTF lands and a decision waiting on a fetch would be a race whose losing branch is a rock map on a leaf card. 31 roles, 0 mismatches, 0 unknown. `PropLibrary.load` awaits the manifest instead, being async and the one path that keeps a material per role name. **The probe caught its own defect first, which is the point of standing rule 11:** the first run reported `meanAbsDelta 0` on every box, and the reason was `samplePx: 0` — it read `bmp.width` after `bmp.close()`, which zeroes it, and it had never called `drawImage`. Every difference is reported next to the sample count it was computed over, and `ok` requires both a nonzero sample and a lit ground, so a measurement over nothing cannot read as a passing sky control. New: `web/src/render/instancing/Surfaces.ts` (the one place the role -> family decision lives, plus the runtime toggle and the probe report), `web/tools/smoke/probes/maps.js` (six asserted properties), screenshots `docs/screenshots/RN_maps_{before,after}.png`. **FLAGGED TO THE POST LANE: there will shortly be two occlusion sources in the frame.** The baked AO is now bound on 12 materials at `aoMapIntensity` 1.0 and their SSAO is coming; multiplying both double-darkens contact regions. The baked map was deliberately NOT weakened to pre-compensate. **NOT MINE, observed in passing:** `npm run check` is red on the current tree for other lanes' reasons (six `src/` files over the 400-line cap — `FlightMode` 554, `LaunchPad` 549, `BuildMode` 454, `FlightSession` 448, `GameplayActions` 427, `Gameplay` 418 — plus `tsc` errors in `MapBoot`/`Persist`/`MapView`/`Discovery`); all four files this lane owns are within cap and typecheck clean. An FXAA shader in `web/src/render/post` emitted `warning X4000: use of potentially uninitialized variable (f_ApplyFXAA)` during an intermediate build, which `run.mjs`'s warning filter treats as a hard failure; it did not reproduce in the final build.
>
> **RN-7 (2026-07-27) prior head, kept below.**
>
> **RN-7 (2026-07-27) - THE SHIPPED ART REACHES THE SCREEN. Six defects, and five of them reported success.** Reid's overnight art lane delivered a rebuilt player, 26 clips, clump grass, belt cargo and armour, and almost none of it was drawn. **(1) THE SCATTER PLACED NOTHING NEAR THE PLAYER, and this is the find of the night.** `Scatter.sample` quantised its per-CELL prop count with `Math.round(expected)`. DW-19 took the near terrain cell from 7.2 m to 1.808 m on 2026-07-26, so `expected` became `118920 * 1.808^2 / 1e6` = **0.389, and `Math.round(0.389)` is 0**. Every chunk under the player scattered exactly nothing while the per-chunk `want` read 399 and every instrument read healthy: a terrain LOD improvement silently deleted the ground cover, and the only surviving props were on coarse chunks far away. **Measured, same seed, same site, same camera, same binary via `?scatterfair=0`: delivered fraction 0.0182 -> 0.9971, props placed 159 -> 8,697, and the number that matters, FOLIAGE SCREEN COVERAGE 0.56% -> 28.87%** over a 600x600 px box at pitch -25 (`of.groundCover` differences the same settled frame with the layer on and off, so camera, sun, streamed set and terrain cannot differ between the two captures). Fix: spend the fractional part as a probability keyed by the SAME per-cell hash everything else uses, so determinism is untouched and realised density equals requested density at any LOD depth. **(2) `PropLibrary.ts:33 CAPACITY = 7000` per material batch, fixed, no growth, exhaustion counted into a field nothing printed: a direct DW-28 violation.** Pools now double to a 16,384 ceiling, and `registerPool` puts the foliage on the same HUD line as the machines, so a refusal shouts `POOL FULL: n NOT DRAWN`. **(3) BOTH SCATTER CAPS WERE SILENT.** They are counted now, and `chunksCapped` earned itself immediately: at the shipped understorey a depth-14 chunk wholly inside the detail ring wants 3,106 props against `MAX_PER_CHUNK` 2,600, a 16% shortfall on exactly the chunk the player stands on. **(4) `props/detail_cards.glb` was declared in `Registry.ts` and never passed to a loader** (A-2): the ground-detail understorey had never been drawn. It is now a second density tier confined to a 55 m ring, 9,503 m2 against the full ring's 90,792, so a card costs 10.5% of what a shrub costs. **(5) DW-34, THE DEAD HOLD, LANDED ATOMICALLY.** The exporter wrote the first key at Blender frame 1, so `seconds = frame / fps` put every clip's first sampler input at 1/60. Root cause was NOT the exporter's `export_anim_slide_to_zero`, which only reaches the sampled path and would have fixed the two characters while silently leaving every machine, tree and belt at 1/60; it is `of_lib.pose_clip` / `add_clip_multi`, fixed with one `clip_frame()` helper. **Browser-measured, driven, off three's own `AnimationClip`: clips opening with a dead hold 26 of 26 -> 0 of 26, worst hold 16.6667 ms -> 0.0000 ms, `Run` duration 0.416666657 s -> 0.400000006 s, hitch at 4.5 m/s 7.50 cm -> 0.00 cm per cycle.** Every published impact index moved down one and moved in the SAME commit: pickaxe 17 -> **16**, axe 18 -> **17**, dig 16 -> **15**, read out of the exported sampler, and `SWING_CLIPS` in `AnimGraph.ts` is now the one place they are stated. `validate_glb.py` gained `anim_t0`, which asserts the first sampler input is **exactly 0.0** (an equality, because `frame/fps` into float32 makes frame 0 exactly 0.0, so a tolerance is only somewhere to hide) and which failed exactly the 21 animated assets before the rebuild. Four unanimated witnesses rebuilt byte-identical, including `armour_set.glb` whose build script was edited. 48/48 validate. **(6) FOUR FP CLIPS AND THE WHOLE AXE WERE UNREACHABLE (A-6).** `FP_CLIPS` mapped `jumpStart`/`jumpLoop`/`jumpLand`/`fall` to `null` with a comment claiming that was correct; the arms went dead still in the air. `swing` mapped unconditionally to `Swing_Pickaxe` and `holdTool` had only ever been called with the pickaxe, so `Swing_Axe`, `FP_Swing_Axe` and `crude_axe.glb` shipped, validated and could not be reached. **Driven proof: chopping a tree now reads `playing Swing_Axe`, `playingFp FP_Swing_Axe`, `crude_axe.glb` in BOTH hands, sampled mid-swing (`swingLeft 0.417`), and mining ore reads `Swing_Pickaxe` on the same run; unmapped states 0 on both rigs, 26 of 26 clips reachable.** **ARMOUR IS ON THE AVATAR (A-10), and the probe caught the A-9 trap on the way.** `PlayerRig.equip/unequip` rebinds each piece to the BODY's skeleton. The first version took ONE match per slot and bound **284 of 904 triangles** while every slot still had something on it, because `Armour_Chest_LOD0` is four primitives and GLTFLoader splits it into `_0.._3`. Fixed with an anchored `_LOD0(_\d+)?` match: **904 triangles drawn, 4 slots, 44 == 44 bones, `sameSkeleton` true on all four, frame delta 3,616 = 904 x 4 passes and exactly reversible.** **CUT BANKS ARE NO LONGER BLACK (task 109).** RN-6's suspicion was right and the mechanism is narrower than "ignores three's light list": `TerrainShader.ts:211` is `albedo * (uAmbient + skyAmb + sunT * 1.45 * ndl * shadow)` and `skyAmb` is traced along `up`, not along `n`, so it is identical for every fragment. A near-vertical face at a 69 degree sun gets `ndl` = 0 and keeps **2.5 / 3.5 / 5.9 per cent** of what flat ground gets, per channel. Isolated with a CIRCULAR 10 m pit, where depth, range, albedo rule, relief band and facet steepness are equal at every bearing by construction so the only quantity that varies is `dot(N, sunDir)`: **azimuthal luminance spread 155.3 counts**. Fix is 8 ALU ops, no new shader and no new uniform: a `skyView` split plus a `ground` bounce term whose radiance is the flat-ground branch already computed at that point, so a flat fragment is algebraically unchanged (floor 154.8 -> 155.4). **Darkest wall against the sunlit floor in the SAME frame, 0.135 -> 0.313 against a derived tolerance of 0.18** (sky diffuse on a vertical face is about 13% of horizontal, halved for seeing half the dome, plus about 11% bounced). Headlamp regression zero on one identical tree: `tunnellit` lamp lift **16.99 before and after**. **COST, and it is a real trade: draw calls 45 -> 53 of a 150 budget, triangles 493k -> 1.39 M of a 2.7 M alert, frame p50 1.5 -> 5.3 ms, p99 2.6 -> 15.6 ms, worst 39.9 ms.** The worst frame is one chunk's scatter build and is the named follow-up; per-update sampling is already amortised to one chunk and pools start at 2,048 rather than 256 to stop a doubling storm (22 reallocations in one 55 m walk -> 2 per batch). Gates: **29/29 ctest**, `npm --prefix web run check` green at **180 files**, `tools/smoke/reload.mjs` PASS. New probes `tools/smoke/probes/{grass,avatar,cutbank}.js`; isolation flags `?scatterfair=0`, `?propgrow=0`, `?detail=0` (standing rule 7). Screenshots `docs/screenshots/RN_grass_{before,after}.png`, `RN_cutbank_{before,after}.png`, `RN_avatar_{before,after}.png`. **NOT DONE, and both are ownership rather than difficulty: A-3 and A-4 live in `web/src/game/NodeArt.ts` and `NodeBatch.ts`**, which a progression lane owns tonight, so `water_pool.glb` and `oil_seep.glb` are still undrawable, every harvest node's `_LOD1`/`_LOD2`/`_Stump` is still dead weight, and `NodeBatch.ts:67 CAPACITY = 128` is still a silent DW-28 ceiling. **A-5 needs Reid, not a lane**: `walkMps` 4.6 against `RUN_THRESHOLD_MPS` 3.0 means the `Walk` clip can essentially never play, and whether 4.6 m/s (16.6 km/h) is the intended default walking speed is a feel decision. **A-11 needs art**: armour is body-only because `armour_set` carries the 44-bone rig and the view model is a different 27-bone rig. | **Last updated:** 2026-07-27 (RN-7).
>
> **RN-6 (2026-07-26) prior head, kept below.**
> `rendering-controller` · **Reports to:** Admin · **Phase:** WEB (three.js, DW-1 pivot) · **RN-6 (2026-07-26) — KILLED THE FIELD OF BLACK SPIKES OVER TERRAFORMED GROUND, AND THE AIM RAY THAT STOPPED SHORT OF IT.** The reported defect (`docs/screenshots/W8_base.png`) was **the near voxel mesh**, not fill geometry, not winding, not the headlamp and not a material. **The attribution in the brief was wrong and so was every one of its four candidate causes**; the isolation that settled it is `?voxelnear=0`, which removes the mesh from the scene and takes the entire spike field with it at a fixed site and camera. **Cause:** `exposedFaces` answers "every solid-to-air face in this box", which is right for a mesher that owns the world and wrong for one that SUPPLEMENTS the heightfield. A 6 m levelling op has a 13 x 25 x 13 m dirty region, so it made the mesh draw /core's 1 m solidity shell over ~25 m of ordinary ground; that shell disagrees with the smooth `surfaceRadius` the terrain chunk draws by up to half a cell diagonal (DW-26), so its cube corners poke through, and at 2 to 3 m from the eye a 1 m face fills a fifth of the screen. Nothing about it is specific to fill: **a dig showed it too, over a couple of metres instead of twenty-five.** **Fix (`web/src/world/VoxelSkin.ts`, new):** keep a face only where the heightfield CANNOT express it, i.e. the solid cell stands above the derived surface or the air cell sits clearly below it. Measured on a driven level: **924 faces exposed, 904 dropped, 20 drawn.** Filtering to "faces of an EDIT" was tried first and is not enough, and the reason is worth keeping: `levelArea` flips only the cells on the wrong side of its target, so its edits are a SCATTER and their faces are isolated cubes standing out of a terrace the chunk had already drawn correctly. **Colour:** the mesh's per-vertex albedo now comes from `BiomePalette.terrainAlbedo`, the terrain fragment shader's own palette entry, slope-to-rock smoothstep, snow band and relief scaling. **Light stays Headlamp's:** shading it with the terrain's actual ShaderMaterial was tried, matched perfectly in daylight and BROKE the tunnel, because that program computes its light from `uSunDir` and ignores three's light list, so the lamp had nothing to act on (lift 3.46x -> 1.12x, `lampDoesTheWork` false). Lambert + imported colour keeps both; lamp lift is now **11.5x**. **Obstruction (the user's own complaint, "annoying when you dig"):** `VoxelWorld.raycast` marched `solidAt`, the quantised shell, so the aim stopped on invisible rock in front of the ground: **17 of 40 driven aims stopped early, worst 3.8 m, on a shell face up to 0.52 m above the surface.** `SurfaceOracle.solidForAim` is `solidAt` with its one quantised term swapped for the smooth surface and both edit sets untouched (`added || (r <= surfaceRadius && !removed)`), so a tunnel is still hollow and its wall is still rock. Worst driven strike error **0.20 m against a 0.25 m march step**. **Numbers:** levelled pad vs untouched terrain, same seed, same site, matched range (8.7 / 8.8 m) and matched local slope (0.573 / 0.428 m), 240 px samples: **RGB 96.5/96.6/85.8 vs 143.7/145.5/134.7 (worst channel 33.6% off) becomes 132.7/135.8/124.9 vs the same ground (7.6%), tolerance 12%.** Draw calls with a base and a levelled pad in frame **49 -> 45 of 150**; p99 unchanged. **Isolation flags shipped (rule 7): `?voxelskin=0`** (the W5 mesh exactly: whole shell, flat brown Lambert), **`?voxelnear=0`** (not in the scene at all), **`?aimshell=1`** (march the raw shell). Probes `tools/smoke/probes/{voxelskin,padshot,beltfloat}.js`; screenshots `docs/screenshots/RN_base_{before,after}.png`, `RN_pad_{before,after}.png`, `RN_pad_after_novoxel.png`, `RN_dig_{voxel,novoxel}.png`. Regression green: `tunnelwalk` (8/8 grounded, 8/8 rock overhead, 8/8 column closed), `tunnellit`, `dig`, `level` (all 7 verdicts). **Found and NOT fixed:** `tunnelpersist.stillWalkable` is red on this branch and is NOT this lane's (identical with `?aimshell=1`); three call sites in `web/src/game` pass `edits = 0` to `_of_surface_radius` while two others pass the live handle. · **Last updated:** 2026-07-26 (RN-6). Prior WEB history lives in `docs/web/STATUS.md` and `docs/web/ARCHITECTURE.md` 15.2; the UE-era status below is FROZEN (DW-6, tag `ue-frozen-2026-07-05`).
>
> **UE-era status (frozen, kept for the reference implementation):** · **R2a (2026-07-05) — WIRED THE UE TERRAIN ONTO THE /core SURFACE ORACLE (RETHINK R2a; WG-21 `core/include/of/surface_field.h`, commit `195ab63`, consumed READ-ONLY — no `/core` edit). ONE surface everywhere, voxel-only digging, deform store DEMOTED, the two root-caused hacks REMOVED.** UE-side only (`OFPlanetTerrain.cpp`, `OFAutomationManager.cpp`), commit `181ba86`; streaming/rebase, build grid, automation line, harvest, sprint, FP/TP camera, foliage NOT regressed. **The swaps:** (1) **EnsureInit** — the streamer's mesh-lowering fn is now bound to the VOXEL-DERIVED far-field lowering (`derivedLoweringAt` over `Impl->Voxels`, the top-anchored open-column depth), NOT `TerrainDeform::depthDugAt`. Core's `buildChunk` already draws the DESIGNED base (`sampleDesignedHeight`), so the visible planet gains its designed relief (mountains actually mountainous — the intended R2a look change from the old RAW mesh base). Also `SurfaceObserver::setVoxelEdits(&Voxels)` so the walker drops into the SAME dug surface the mesh shows. (2) **DigVoxelAtWorld** — the **18 m inward surface-snap probe is GONE** (root cause retired: voxel solidity now derives from the same designed base as the mesh+collision, so a ground-aimed ray hit lands in SOLID — we carve straight at the hit). (3) **DigVoxelAtWorld/DrillVoxelStepAtWorld** — stopped writing `TerrainDeform.digBrush`; **ALL digging writes VoxelEdits ONLY** (one dig system). The surface mouth is a DERIVED view: re-mesh the affected chunk → `rebuildChunk` re-derives the opening from the voxel removed set. (4) **RemeshChunksAroundDir** — selects chunks by ANGULAR overlap with the dig dir (the deform `quadHasEdits` query is gone). (5) **SaveDeform/LoadDeform** — persist the VOXEL diff as the single terrain-edit save; a pre-R2a deform+voxel save mis-parses → discarded gracefully (dev saves; noted). (6) **Automation miner** — voxel-only shaft (dropped the heightfield `DrillStepAtWorld`; bottomed = a step removes no cells). (7) Corrected the false raw-vs-designed grounding/snap comments; the node down-trace is now belt-and-suspenders (embed+orient+post-dig re-ground), no longer load-bearing for a raw/designed gap. **PIE-VERIFIED (input-driven) on `/Game/Maps/PlanetSurface`:** player GROUNDED on the designed-base mesh (`current_floor.blocking_hit=True`, walkable, `MOVE_WALKING`, `is_falling=False`, vel 0 — mesh/walker/nodes share ONE surface); **dig DOWN carves at the hit with NO snap probe** (51 cells; pit interior reads AIR); **sideways TUNNEL** carves (42 cells; interior AIR, ceiling +2 m reads SOLID → roof intact); one dig system confirmed (0→93 removed, voxel-only); **save→restart-PIE-fresh(0)→load restored EXACTLY 93** from the voxel diff (pit interior AIR again, mesh re-built). 40 harvest nodes streamed grounded, foliage populated the forest start. Screenshots `docs/screenshots/R2a_{designed_relief,tunnel_no_hacks}.png` (deep cavity renders BLACK + forest canopy hides the pit → tunnel proven FUNCTIONALLY via voxel-state queries, the M5.2 gotcha). **Residual for R2b:** the demoted `TerrainDeform Deform` member + legacy `DigAtWorld`/`DrillStepAtWorld`/`DiagDeform` heightfield entry points still compile against a now-empty store (report 0) — remove once no caller remains; the held-key WASD walk isn't sustainable through the bridge (single tap; auto-walk knob over-runs the physical walker) so long-traverse grounding-in-motion stays a harness limit (M4.3 note), grounding proven at rest. · **M5.2 (2026-06-16) — TRUE VOXEL TUNNELING (dig DOWN then SIDEWAYS, walk into the tunnel).** Bound the UE planet to the headless 1 m³ voxel core (`core/include/of/voxel_terrain.h`, `of::worldgen::VoxelEdits`/`exposedFaces`/`dig`); **no functional `/core` edit** (header consumed read-only; `voxel_terrain_tests` + the other logic suites green — the 2 ctest "failures" are a pre-existing `0xc0000139` DLL-load error, ninja "no work to do" on `/core`). UE-side only (`OFPlanetTerrain.{h,cpp}`, `OFSurvivalCharacter.cpp`, `OFAutomationManager.cpp`); foliage/build-grid/heightfield-dig/slope/movement/grounding/harvest/miners NOT regressed. **Task 1 — voxel near-field:** `AOFPlanetTerrain` owns ONE `VoxelEdits` + ONE `VoxelMesh` (ProceduralMesh under the FrameRot root); `RebuildVoxelNearField` emits a **cube-face quad per `exposedFaces()` solid→air face** over the ~24 m region around the eye cell, vertices baked engine-cm relative to `floatingOrigin().originPos()` (`(cellCenter−origin)×100`), **complex-as-simple collision on the same face mesh** (`ECC_WorldStatic`, block Pawn) so the cavity surface IS the collision; re-meshed on player cell-band change + force on dig/load; **re-baked on every floating-origin rebase** (`ReanchorVoxelMesh`). **Task 2 (THE deliverable) — pickaxe pits + tunnels:** `OnDig` → `DigVoxelAtWorld(rayHit, ~1.5 m)` carves every solid cell in radius (surface-SNAPS the brush centre inward to the first solid cell when the ground-aimed hit lands in the chunk-collision overhang the voxel proc-surface sits a few m below), re-meshes, yields stone; **aim DOWN = pit, stand in it + aim SIDEWAYS = horizontal TUNNEL** (the ray hits the solid voxel WALL). **Surface reconciliation:** lowers the heightfield (`TerrainDeform::digBrush`) per affected column ONLY by the *top-anchored contiguous removed run* (the mouth) → a sideways tunnel under solid ground opens 0 surface there, **ceiling stays intact**. **PIE-VERIFIED:** dug a vertical pit (81 cells) THEN a horizontal tunnel (91 more) at 5 m depth — interior reads AIR, ceiling +2/+3 m reads SOLID, and **C++ `ECC_WorldStatic` traces HIT the VOXEL mesh on BOTH the tunnel floor (stand in it) AND the ceiling (solid roof)**. **Task 3 — voxel miners + persistence:** the drilling miner also `DrillVoxelStepAtWorld`s a 1 m³ column toward bedrock; the voxel removed-set serialises alongside the deform map in `SaveDeform`/`LoadDeform` (one stream) + re-meshes on load (PIE: 450 voxels save→restart→load restored exactly 450; drill bored 8/9/8/7/8 cells/step). **GOTCHAS:** `dig` only removes `isProcSolid` cells and the voxel proc-surface sits a few m off the chunk collision (designed-vs-raw + `unitOf(p)` dir error at ~6e5 m → metres of high-freq height) → the inward surface-SNAP fixes ground-aimed digs; Python `line_trace` (TraceTypeQuery) does NOT hit the PMC voxel collision (verify via a C++ `ECC_WorldStatic` trace — `DiagTraceWS`); a deep voxel cavity renders BLACK + the forest canopy hides a small pit → tunnel proven FUNCTIONALLY (collision + voxel-state queries), emissive/smoother mesher = residual polish. Commits `ff3dc12` (mesh+dig) + `65158b4` (miners+persist); screenshots `docs/screenshots/M5.2_{pit,tunnel,inside_tunnel}.png`. · **M5.1 (2026-06-16) — BIOME FOLIAGE SCATTER (the planet is now ALIVE with the user's Fab/Megascans trees/shrubs/grass/boulders).** UE-side only (`OFPlanetTerrain.{h,cpp}`), no `/core` edit; digging/build-grid/slope/movement/grounding/harvest all NOT regressed (foliage is an independent HISM layer). **Fab assets found + used (disk scan of `/Game`):** Maxtree (15 species × 6 SM variants), Abelia (Nanite flowering shrub), Lolium (Nanite ryegrass), Vitex (chaste-shrub), RockSample (`SM_RockV7` boulder) — classified into a 14-entry library (trees/shrubs/grass-flowers/rock). Megaplant trees are SKELETAL → unusable in HISM, omitted; B3D + WaterMaterials added but unused (follow-on). **Biome → foliage** via a pure switch on `/core biomeAt`: Forest = dense trees+shrubs+ferns, Plains = grass+wildflowers+sparse trees, Hills = boulders+shrubs, Mountains = sparse rock, Beach = sparse grass, Polar = ~none. **Stability REUSES the M4.4 node-streaming contract** (no float/pop): same SINGLE GLOBAL angular lattice (constant `dAng=step/R`, id=`hashCombine(iLat,iLon)` → a spot maps to ONE id forever, only ring-edge churns), each instance **ground-snapped** (world down-trace onto `ECC_WorldStatic`) + **slope-oriented** (`MakeFromZX(normal)`) + per-instance yaw/scale variety, placed into **per-mesh HISMs** under the FrameRot root; trees/boulders block, grass/shrubs/flowers NoCollision (walk-through); **`ReanchorFoliage()` re-anchors every instance on the floating-origin rebase** (next to `ReanchorDeposits`). **PIE-verified:** Forest start = 2600 resident instances (trees+shrubs+grass, zero rocks); stable across a 30 m walk (ring-edge churn only); marched to Hills → trees vanish, boulders=664 appear (biome-correct). **Task 2 (per-biome ground surfaces) SKIPPED** — no dedicated soil/cliff/snow/sand SURFACE-SET pack was added (RockSample/WaterMaterials ship prop/water textures, not terrain sets; existing `M_PlanetSurface` already does the per-biome grass↔rock + soil-midpoint + snow-tint blend); wiring RockSample snow/limestone into the vertex-biome blend is a viable follow-on, left untouched to avoid risking the working ground material. **GOTCHAS:** `MakeFromZX(...)` returns an `FMatrix` → use `.ToQuat()` not `.Quaternion()` (compile error); HISM `RemoveInstance` swaps the LAST instance into the freed slot → re-point our id↔index records; HISM `AddInstance`/`UpdateInstanceTransform` are component-LOCAL space (convert the snapped world xf back via `RootXf.Inverse*`). Screenshots `docs/screenshots/M5.1_{forest_populated,plains,walk_stable}.png`. · **M4.6 (2026-06-16) — VALHEIM-STYLE DESTRUCTIVE DIGGING + DRILLING MINERS (the pickaxe digs REAL holes; automated miners bore REAL vertical shafts).** Bound the headless `core/include/of/terrain_deform.h` (`of::worldgen::TerrainDeform`, level-18 ~3.6 m cell lattice, 80 m bedrock) into the streamed planet so a dig lowers the SAME mesh+collision surface the player walks on. **`/core` change (additive, 20/20 ctest green): `cubed_sphere.h generateQuadMesh` + `terrain_stream.h buildChunk` gained an OPTIONAL `HeightLoweringFn` (default null = BIT-IDENTICAL — determinism proofs unchanged); `TerrainStreamer::setLoweringFn` + `rebuildChunk(key)` so only the dug chunk re-meshes.** UE (`OFPlanetTerrain`): `FImpl` owns ONE per-planet `TerrainDeform`; the lowering fn reads `deform.depthDugAt(dir)`; `DigAtWorld`/`DrillStepAtWorld` convert a world hit→body dir (undo FrameRot+floating origin) → `digBrush`/`drillStep` → `RemeshChunksAroundDir` re-meshes resident chunks overlapping an edit (`quadHasEdits`) + re-snaps nodes. **Task 2:** `OFSurvivalCharacter::OnDig` (LeftMouseButton) traces camera→terrain, swings the pickaxe, `DigAtWorld` (~1.5 m radius, ~0.4 m/swing) → a visible pit toward 80 m bedrock + harvests an overlapping node; build-mode LMB still places, SurvivalTest falls back to harvest. **Task 3:** `OFAutomationManager::RefreshVisuals(dt)` drills a shaft (`DrillStepAtWorld` ~0.6 s) under each working miner toward bedrock, tied to extraction, no-op off-planet. **Task 4:** `SaveDeform`/`LoadDeform` (`of::persist::SaveWriter`+`TerrainDeform::serialize`) persist the edit map to `Saved/OrbitalFoundry/` + re-mesh on load. **PIE-verified:** 80 swings dropped the `ECC_WorldStatic` collision ~22 m (matched the deform depth); a miner bored to 80 m (bottomed) while the auto-line made 134 iron; save→dig→load restored the exact saved state. **GOTCHAS:** dig radius must be ≥~1.5 cell pitches or the brush catches NO cell (floored in C++); a deep (>~30 m) pit/shaft renders BLACK in a downward PIE capture (frame a shallow wide bowl from a low angle); adding `persistence.h` shifted UE unity grouping → renamed OFSurvivalCharacter's anonymous-ns `kRawIron`/`kIron` to `kSc*` (anon namespaces merge per unity TU). Commits `55e2d7d`/`de5ce01`/`0399726`/`c0130ec`; screenshots `docs/screenshots/M4.6_{dig_hole,miner_shaft,miner_actors}.png`. · **M4.4 (2026-06-16) — HARVEST NODES GLUED TO THE GROUND + STABLE id-keyed STREAMING (fixed the FLOAT + POP bugs on the biome nodes).** On `PlanetSurface` the harvest features (trees/rocks/ore/oil) FLOATED above the ground and "popped" (despawn/respawn) every step. **Two independent bugs, both UE-side only in `OFPlanetTerrain` (no `/core` edit; collision/walking `21ac998` + slope `b9fe759` NOT regressed; harvest E untouched):** (1) **FLOAT** = the deposit position uses `/core`'s DESIGNED relief (`sampleDesignedHeight`) but the chunk COLLISION is the RAW `sampleHeightField`, so nodes floated by the relief-modulation gap → **FIX `SnapNodeToGround`: line-trace each placed node straight DOWN onto `ECC_WorldStatic` and drop its base onto the hit (+1 cm)**; the cached `NodePositions` is recovered from the snapped location so rebases stay grounded. (2) **POP** = `GenerateLocalDeposits` re-quantized every cell id each step because the lon lattice spacing `dLon = stepM/(R·cosLat)` BREATHES with latitude → **FIX: a SINGLE GLOBAL angular lattice with CONSTANT `dAng = stepM/R` in both axes; cells = global integer indices `(iLat,iLon)`, id = `hashCombine(iLat,iLon)`** (a physical spot → one id forever; only the loop EXTENT scales by `1/cosLat`, never the spacing/id). The streaming loop already kept in-range nodes, despawned only out-of-range, and `ReanchorDeposits` shifts by the origin delta on rebase. **PIE-verified CHEAPLY (no long drive) via 3 new `BlueprintCallable` diagnostics (`DiagNodeClearances`/`GetLiveNodeIds`/`StepObserverForTest`):** all 40 resident nodes clearance = 1.0 cm above the chunk collision; a continuous ~55 m / 6-step walk held id persistence 100/95/100/100/97/100 % (only 0–2 EDGE nodes churn per step at the 700 m range boundary, never the nearby set). **GOTCHA: the streamed PMC collision is NOT hit by Python `line_trace_single`/`_for_objects`/sphere traces even under the grounded player — verify PMC grounding from C++ (`LineTraceSingleByChannel(ECC_WorldStatic)`); `current_floor.blocking_hit` confirms the capsule is grounded (`FHitResult` struct fields aren't attribute-accessible in this UE Python build).** Screenshots `docs/screenshots/M4.4_{nodes_grounded,nodes_stable}.png`. · **M4.3 (2026-06-16) — SOLID COLLISION GROUND + PHYSICAL SMOOTH WALKING (fixed the FLOAT + TELEPORT bugs) + per-biome Megascans surfaces.** In real PIE gameplay the player + objects FLOATED in space (no ground) and running TELEPORTED instead of moving smoothly. **Root cause = the old "pin the player to a computed eye-height each tick" model:** the streamed chunk `UProceduralMeshComponent`s had NO collision, so the capsule had nothing to stand on (it floated, pinned to the `/core` surface height by a per-tick `SetActorLocation`), and that per-tick re-pin + the re-pin after every floating-origin rebase is what read as teleporting once you actually moved. **FIX (UE-side only, no `/core` edit, slope fix `b9fe759` NOT regressed):** (1) build chunk sections WITH collision (`bUseComplexAsSimpleCollision`, synchronous cook so the body exists frame-1, `QueryAndPhysics` + block Pawn) — solid ground; (2) put the player in `MOVE_Walking`+gravity and DROP them onto the collision (removed the per-tick pin) — physical CharacterMovement walking; (3) **invisible rebase compensation** — on `consumeRebased()` shift the player by the SAME world delta the terrain re-anchors by (`OldFrameRot.RotateVector(lastDelta×scale)`) so their position relative to the ground is unchanged (no jump, no re-pin); (4) safety net only on a >2 km catastrophic tunnel while falling. **PIE-VERIFIED IN MOTION:** standing → `clear=2.2 cm` above the mesh, `is_falling=false` (feet on solid ground, not floating); driving the real WASD path → **15 consecutive intervals all dX≈94.6 cm (rock-steady constant velocity, zero teleport spikes) + smooth dZ≈−2.7 cm** down a slope. **Task 2 — per-biome Megascans GROUND surfaces:** the user added `/Game/RockyGround` (rocky-ground scan), `/Game/Materials_TreeBark` (9 conifer-bark MIs), `/Game/WaterMaterials`, `/Game/B3D` (pine-branch atlas). `M_PlanetSurface` now BLENDS grass↔rock surfaces per biome — a per-vertex `BiomeSurfaceWeight` (0=grass…1=rock) packed into vertex ALPHA (blurred to feather boundaries), `lerp(grass,rock,A)×VertexColor.RGB` for base/normal/roughness; tree trunks use the 9 `MI_TreeBark_*` conifer instances (per-node). Grass biome PIE-verified textured (real surface relief); rock biomes wired (not yet walked-to). **Gotcha re-bit: `VertexColor` RGB is the DEFAULT output (`""`), NOT `"RGB"` — wiring `"RGB"` silently fails → black ground.** Screenshots `docs/screenshots/M4.3_{standing_on_ground,walking_smooth,planet_biomes_textured,ground_check}.png`. **Residual:** no streamed-foliage scatter on the planet yet; no dedicated snow/soil scan (Polar tints rock bright). · **M4.2 (2026-06-16) — FIXED THE PERPETUAL ~40° SLOPE BUG (level ground under the player).** On `PlanetSurface` the player stood on a constant tilted hillside everywhere (tilted horizon) instead of level ground. Root cause: `ComputeFrameRot` (the world-from-body root rotation in `OFPlanetTerrain.cpp`) had TWO compounding bugs — (1) **wrong handedness:** `East = U × North` made the `{East,North,U}` basis left-handed, and (2) **wrong matrix layout:** the body axes were the matrix ROWS, but UE's `FMatrix(rows)+ToQuat()+RotateVector()` needs them as COLUMNS to map `RotateVector(U)=+Z`. Together radial-up landed near −Y (the ~40° tilt). FIX: `East = North × U` (right-handed) + body axes as COLUMNS (transpose layout) + north pole `(0,1,0)` per `/core latLonToDir`. **Empirically verified in PIE: `R·U=(0,0,1)`, `R·North=(0,1,0)`, `R·East=(1,0,0)` → level ground, level horizon, world +X=east/+Y=north (matches the existing WASD un-rotation, no other change).** PIE-confirmed level on the forest start AND across a ~64 km auto-walk (biomes cycled, engine coords stayed bounded ~133k UE / ~1.3 km between rebases, no jitter — floating-origin rebase intact, `FrameRot` recomputed per rebase). A prior uncommitted attempt fixed only the layout (left ground inverted, U→−Z) and was never verified — reworked + verified here. UE-side only, no `/core` edit. Screenshots `docs/screenshots/M4.2_{level_ground,horizon_level}.png`. **ALSO M4.2 — REAL MEGASCANS TEXTURES WIRED IN (the M3.1 blocker is cleared — the user finally Add-to-Project'd Fab packs):** two packs imported — `/Game/BarkWillow` (willow-bark scan + 6 PBR textures) and `/Game/OWD_Flowers_Pack` (450 assets: flower + grass meshes, a `Stones/` rock surface, a `Landscape/` grass GROUND surface; ~468 MB total). Wired: **planet ground** `M_PlanetSurface` gains the grass albedo (brightness-preserving detail, normalized to mean≈1 then ×biome VertexColor so the per-biome palette still drives colour) + grass Normal; **ore boulders** `M_OreRock` (both maps) gains the `T_Stone_07` color/normal/rough/AO with the per-kind tint param preserved; **tree bark** = `M_Bark_Willow` on the trunk (one C++ line in `OFResourceNode.cpp`, both maps); **SurvivalTest meadow** dressed with 332 real `SM_Flower_*` + 464 `SM_Grass_*` meshes (replacing the basic-shape proxies). NO soil/snow/wood surface set was imported — planet biome variety tints the single grass surface; flagged as the gap. PIE-verified both maps; screenshots `docs/screenshots/M4.2_megascans_{planet,survival}.png`. · **M4.1 (2026-06-16) — POLISHED PLANET SURFACE + HARVESTABLE BIOME DEPOSITS ON THE PLANET.** The whole-planet surface now reads as a real, legible place AND the player can mine real biome-distributed resources on it (UE-side only, no `/core` edit; commit `5d8bd30`). **Task 1 (looks):** fixed the wash-out — PostProcess manual-exposure bias 10.5→8.2 (was clipping bright Mountain/Polar biomes to pure white), sun 8→5.5, skylight 1.8→1.05, bloom→0.25; **muted LINEAR biome palette** (snow caps 0.50, only on the highest peaks) with **per-vertex biome classification + a 2-pass box blur** that feathers biome boundaries into a transition band (no more hard diagonal seam); **skirtFraction 0.5→0.9** seals coarse↔fine LOD cracks; capsule already stands radial-upright in the frame-aligned root (verified); start moved to a Forest landmass for a green-coastline headline shot. **Task 2 (harvest):** `AOFPlanetTerrain::UpdateDeposits()` streams `AOFResourceNode`s around the player **parented under the rotated root** (same `(pos−origin)×scale` anchor as the chunks → they tilt + re-anchor on every floating-origin rebase, appear/disappear as you walk), wired to the existing survival harvest (E)+inventory; PIE-verified a forest start spawned 20 trees/6 rock/2 iron/2 oil (forest biome bias) and harvesting granted +1 wood. **HONEST `/core` density finding:** the planet-wide `BiomeResourceField` is too SPARSE to be walkable (~634 deposits / 600 km planet → ~14 km to the nearest node), so placement uses `GenerateLocalDeposits()` — the SAME `/core` biome rules (`biomeAt`+`biomeResource`) evaluated on a dense local grid, biome-proportional + deterministic — instead of the sparse field. Flagged to Admin (world-gen needs a density knob); no `/core` edit. Screenshots `docs/screenshots/M4.1_*`. · **M4.0 (2026-06-16) — PLAYER NOW STANDS + WALKS ON THE REAL STREAMED PROCEDURAL PLANET (the signature "seamless surface" step).** New map `/Game/Maps/PlanetSurface` + new actor `AOFPlanetTerrain` (OrbitalFoundryCore plugin, pImpl over `/core` `worldgen::SurfaceObserver` + `TerrainStreamer`) render the resident `TerrainChunk`s as biome-coloured ProcMesh sections **at real scale (1 m = 100 UE units)** with the player pinned near the engine origin. **The floating-origin crux is solved frame-aligned:** the actor root carries a `world-from-body` rotation (radial up → world +Z) so the local patch renders HORIZONTAL and plain gravity/world-Z cameras work; chunks attach with relative pos `(centerUniverse − floatingOrigin) × scale`. **All 3 milestones landed + PIE-verified:** M1 player stands on real curved biome terrain (snow mountains / green plains / ocean basins, ~200 chunks / ~240k verts resident); M2 walking drives `SurfaceObserver::move` → `updateStreaming` adds/drops chunks as biomes change; M3 `consumeRebased()` → `onOriginRebased` + re-anchor + frame-recompute + re-pin kept engine coords **bounded near 0 over a ~180 km walk (99 rebases, no jitter/blowup)**. Commit `ad472c5`; screenshots `docs/screenshots/M4.0_*`. This is the first real exercise of the `terrain_stream.h`/`surface_walk.h`/`biome.h` contracts through a renderer. **Residual:** LOD-seam cracks at coarse↔fine boundaries (skirt covers most), capsule not yet radial-up-aligned, atmosphere/exposure can blow out white biomes — all polish. · **M3.1 (Fab/Megascans) finding:** **Fab/Megascans asset acquisition is UI-ONLY — there is NO scriptable/Python/bridge path to download a Fab asset (acquisition = the Fab panel's "Add to Project" button only).** The Fab plugin ships + is enabled + the user is signed in, but the import entry point `UFabBrowserApi::AddToProject(signedUrl, meta)` is `UFUNCTION()` *without* BlueprintCallable (invisible to Python — confirmed live) and needs a short-lived signed CDN url minted by the Fab web panel's JS after a user click. Python only exposes the *post-download translator* (`InterchangeMegascansPipeline`/`MegascanImportType`), not an acquirer. So real-Megascans-materials is gated on a ~2-min user action (precise shopping list below in §6); once assets land under `/Game/…` an agent can wire them. Full evidence recorded in `ue/CLAUDE.md` ("Fab / Megascans import is UI-ONLY"). · **M3.1b (2026-06-15):** FP **held-tool view-model** landed (pickaxe/axe now visible + tracks the view in first person; fixed a pre-existing FP black-slab bug = the hidden body's self-shadow) + polish (metallic tool heads `M_Metal`, crude-oil pool `M_Oil`, minimal crosshair/interaction HUD). **The Megascans texture wire-in (M3.1) is STILL pending** — the assets were never actually Add-to-Project'd in Fab (AssetRegistry empty; launcher log shows only a search), so the slice keeps the M3.0 authored materials. Commits `30ebe1a`+`2e7082c`. · **Status:** **SURVIVAL SLICE GRAPHICS/ATMOSPHERE PASS LANDED (M3.0) — the survival slice went from flat untextured "Play-Doh plastic" to a textured, atmospherically-lit, Valheim-leaning stylized-realistic look.** The biggest single fix: **Lumen GI was OFF** (`r.DynamicGlobalIlluminationMethod=0`=None, killing all bounce light) — re-enabled in `DefaultEngine.ini` RendererSettings (+Lumen reflections). Built a full sky/lighting/post stack on `SurvivalTest`: warm low-angle key sun + real-time-capture SkyLight + Sky Atmosphere + Valheim-style Exponential Height + Volumetric Fog + a VolumetricCloud + an unbound PostProcessVolume (desaturated grade w/ cool-green shadows & warm highlights, bloom, histogram auto-exposure, AO, vignette). **StarterContent turned out NOT to be installed** (only one moss texture exists on disk — the brief's "StarterContent IS installed" premise was wrong; the prior pass was right), so authored textured PBR materials **install-free** from engine noise/detail textures: `M_Meadow_Textured` (grass+dirt ground w/ detail normal — the micro-surface normal is the key de-plastic factor), `M_GrassTuft` (two-sided foliage + subsurface) on ~1800 dressing pieces, `M_Water` (reflective + panning ripple normals), upgraded `M_FactoryColor` (tree/tools/furnace/grass) + `M_OreRock` (textured rough rock) in place. PIE-verified before→after (`docs/screenshots/M3.0_before_flat → lit_scene → textured_ground → ore_textured → character`). Commits `fb0e6e3`/`b49ff86`/`4e12d43`. Added size: +12.4 MB (the one migrated `T_ground_Moss_D` texture; all else is authored materials, trivial). Higher bar later = enabling Fab/Megascans for real photo textures. Prior: **THE FLIGHT SPINE NOW FLIES IN-ENGINE (M2.2) — `AOFVesselActor` renders the full Forge→ascent→orbit→SOI-transfer→Cinder-descent→land journey** in scaled space, owning its own `of::SimWorld` via pImpl and running the autopilot ported verbatim from `OrbitalFoundrySubsystem::AutopilotStep`, driven step-by-step from the editor (no PIE). A cyan breadcrumb trail + yellow vessel marker accumulate across the trip; the SOI handoff (Forge↔Cinder frame) is stitched into one continuous trail; screenshot-verified at three framings (`docs/screenshots/M2.2_vessel_journey.png` wide two-body, `M2.2_vessel_ascent_forge.png` launch body, `M2.2_vessel_cinder_landing.png` landed). soiSwitches=1, phase=LANDED on Cinder confirmed in-engine. Prior: **FACTORY RENDERS FROM THE §6 STREAM (M2.3) — the RC-8-validated stream is driving live pixels.** `AOFFactoryActor` renders a production line straight from `of::factory::FactorySim`'s §6 emission: machines from `EmitEntityStates`, discrete flowing items from `GetLineItems` (the lone O(items) call, at LOD-0), tinted via `M_FactoryColor` MIDs — screenshot-verified (`docs/screenshots/M2.3_demo_factory.png`). Also: **FIRST IN-ENGINE TERRAIN RENDER LANDED (M2.1).** `AOFPlanetActor` (OrbitalFoundryCore plugin) builds a `UProceduralMeshComponent` directly from `of::worldgen::generateQuadMesh` — the real crack-free position-hashed heightfield — and renders both bodies (Forge planet: fBm continents + ridged mountains; Cinder moon: crater field) in scaled space with a height-ramp vertex-colour material, **verified live via the SoftUEBridge screenshot loop** (`docs/screenshots/M2.1_forge_planet.png`, `M2.1_forge_and_cinder.png`). This is the worldgen→UE-mesh pipeline proven end to end (coarse single-mesh preview; quadtree-streamed LOD + dual-camera/floating-origin still ahead). Prior: **RC-8 / Q6 render wall NUMERICALLY VALIDATED HEADLESSLY** — a render-cost model over factory-sim's §6 stream proves the O(items)→O(lines) collapse (**100k-entity scene → 72 draw calls, 219,500 instances, 180,000 item-evals vs 12,180,000 belt items, >98% collapsed**). · **Last updated:** 2026-07-05 (R2a: wired the UE terrain onto the /core surface oracle WG-21 — voxel-only digging, deform demoted, the 18 m surface-snap + separate-heightfield-mouth hacks removed, designed relief on the mesh; PIE-verified. Prior: M5.2 TRUE VOXEL TUNNELING)
> Read alongside: [MASTER_PLAN](../MASTER_PLAN.md) · [AGENT_ARCHITECTURE](../AGENT_ARCHITECTURE.md) · [ADMIN](ADMIN.md) · **[Spike 1 rendering design](../spikes/spike1-rendering.md)** (the buildable plan)

## 1. Mission
Make surface→orbit→interplanetary→surface look seamless and run fast. Own the "rendering magic" that sells continuous traversal, plus the techniques that let a dense 3D factory render without melting the GPU.

## 2. Scope & owned subsystems
- **Scaled space** — distant celestial bodies via a second camera at huge scale; swap to real terrain on approach.
- **Multi-range depth** — logarithmic or cascaded depth buffer (draw a 1 m object and a 100,000 km planet in one frame, no z-fighting).
- **Terrain rendering** — consume world-gen's cubed-sphere quadtree; stream/blend LOD; horizon curvature.
- **Atmospheric scattering** — Rayleigh/Mie post-process; the sky-fades-to-black sell of leaving a planet.
- **Factory rendering at scale** — GPU instancing of identical parts/items; LOD that collapses distant belts to flowing textures/decals; cull/abstract un-observed item meshes.
- **Materials, lighting, VFX** — engine pipeline (Lumen/Nanite under D-001), thrust/dust/weather FX.
- **Non-goals:** what to render (world-gen, factory-sim, physics produce the data); UI/HUD (→ gameplay).

## 3. Key design decisions
| # | Decision | Rationale | Status | Date |
|---|----------|-----------|--------|------|
| RN-1 | Dual-camera scaled-space (near scene + scaled scene composited) | Proven KSP technique for seamless distance | **Accepted (Spike 1); NOT YET BUILT, scheduled Phase S** (2026-07-05, RETHINK §4-S1): M2.1 to M5.2 shipped the near/1:1 world; the scaled far-field over the 1:1 planet is the namesake seam, deferred to Phase S | 2026-06-14 |
| RN-2 | Logarithmic **or cascaded** depth buffer; near camera only | Single-frame coverage of cm→planetary scales. Spike 1: measure reversed-Z float first; cascaded passes preferred over `SV_Depth` log because Nanite can't take a custom per-pixel depth write | **Accepted** (Spike 1) | 2026-06-14 |
| RN-5 | Terrain = **classic streamed meshes (RealtimeMesh), NOT Nanite** | World-gen already does LOD (Nanite's headline feature redundant); Nanite-on-terrain conflicts with the cascaded/`SV_Depth` depth path. Nanite's payoff is later **factory** meshes, not terrain | **Accepted (Spike 1); SUPERSEDED by PMC in practice** (2026-07-05, Admin D-009): RealtimeMesh was never adopted; every shipped mesh path (chunks, voxel near-field, factory visuals) is `UProceduralMeshComponent`, which carried M2.1 to M5.2. Revisit RealtimeMesh/equivalent only if PMC becomes the perf bottleneck | 2026-06-14 |
| RN-3 | Factory items: GPU-instanced near, texture/decal flow far, none when unobserved | Only way 3D belts survive at scale | **Accepted; render-wall fit CONFIRMED vs factory-sim §6 stream (RC-8, §5) — and now NUMERICALLY VALIDATED HEADLESSLY: a render-cost model over the emitted §6 stream quantifies the O(items)→O(lines) collapse (100k entities → 72 draw calls, 180k item-evals vs 12.18M belt items)** | 2026-06-15 |
| RN-4 | Lean on Nanite for factory mesh density (D-001) | Offloads LOD/triangle budget | Provisional; MOOT under the three.js pivot (DW-1/DW-11: `BatchedMesh`) | 2026-06-14 |
| RN-7 | **A layer that draws less than it was asked for must say so, and the way to know is to assert the RATIO of delivered to requested rather than a count** | The scatter's per-cell count was `Math.round(expected)`, which is exactly zero for every expected value under 0.5, and DW-19's finer terrain LOD walked it under that line without touching a line of scatter code. No count-based check could have caught it: the chunk-level `want` was right, the density constants were right, the pool was not exhausted, the props existed in the atlas and 159 of them were genuinely on screen. What was false was the RATIO. `deliveredFraction` is placed over requested, over the ground actually drawn on, so it is invariant to terrain, to density and to LOD depth, and it reads 0.0182 against the defect and 0.9971 against the fix. The same argument gave the two truncating caps counters instead of silence, and `chunksCapped` found a 16% shortfall within one run of being added. **The general form, which is RN-6's lesson pointed at a different layer: a stage in a pipeline should publish what it was ASKED for next to what it PRODUCED, because every other instrument in the chain reports the request** | **Accepted, shipped, driven-verified** (screen coverage 0.56% -> 28.87%, same seed, same camera, one binary) | 2026-07-27 |
| RN-15 | **A performance trade is only valid at the scale it was measured, and the failure mode is that nothing announces the scale changed** | `PropLibrary` turned per-instance frustum culling OFF with a recorded measurement (9,340 props, 8.2 ms of near-pass CPU with sorting against 11.1 ms without) and a correct conclusion: for 150-triangle props the walk costs more than the redraw. Raising the understorey to about 45,000 instances inverted it completely, because the scatter is a RING and roughly three quarters of it is behind the camera, so the saving is not a fifth of the triangles, it is three quarters of them. Measured in one binary with `?propcull=0` as the control: triangles 1,916,689 to 1,087,115, p50 22.8 to 11.3 ms, and screen coverage 0.5545 against 0.5563, which is the same picture. **This is the second time a DENSITY change has silently invalidated a constant nobody edited** (RN-7: DW-19's finer terrain LOD walked `Math.round(expected)` under 0.5 and deleted the ground cover). The general form: a comment that says "measured, this is faster" is a claim indexed by a workload, and the index belongs in the comment next to the number so the next lane can see whether it still holds | **Accepted, shipped, measured both ways** | 2026-07-27 |
| RN-16 | **A layer that PRODUCES instances must also REQUEST them, or it blinds the ratio that watches for the opposite failure** | The contact skirt places five understorey cards at the foot of every colliding prop. They are not a density draw, so the first version counted them in `propsPlaced` and not in `wantedProps`, and `probes/grass.js` read `deliveredFraction` 1.5146 against a check that requires 1.00 within 5%. Over-delivery looks harmless and is not: RN-7 built that ratio specifically because every other instrument in the chain reports the REQUEST, and a ratio that can legitimately read 1.5 can no longer be trusted to read 0.98 when the pool is quietly eating a quarter of the placements. The fix is to give the skirt an honest request, exactly `CONTACT_CARDS` per colliding prop placed, which restores 0.9997 and still catches truncation because cards refused by the per-chunk allocation are counted as asked for and not produced. **Worth keeping next to RN-7 rather than inside it: the ratio caught a defect in the code that was ADDED TO SATISFY the ratio's own design** | **Accepted, fixed, probe green** | 2026-07-27 |
| RN-17 | **A shared model gets a new term only if the term can be shown NOT to move the things it was not meant to move, and the check is a control band in the same capture** | Aerial perspective at playable range is a measured gap (near band against far band in ONE frame, so exposure, sun, tone curve and grade are equal by construction: saturation -0.149, the distant ground MORE saturated than the ground underfoot). A boundary-layer aerosol term is the physically right answer and was added to the shared atmosphere rather than as three's own `FogExp2`, correctly, because fog would have been free and would have been a SECOND authority on how distance looks, disagreeing with the terrain integral at every range and seaming exactly where a rock meets the ground behind it (DW-22). It still did not ship. Blue-biased it darkened instead of hazing; grey with a multiple-scattering floor hazed, and then the SKY control moved (saturation 0.494 to 0.410, mean red 81.9 to 97.0), because a 400 m scale height does not confine the term structurally the way the design claimed: near-horizon sky rays lie inside the layer. **The control is what made a clean revert possible instead of a plausible-looking regression in the one file the sky, the near terrain and the far scaled planet all read.** The negative result is the deliverable: the gap is quantified, the mechanism is understood, and the next attempt starts from a known-bad approach rather than from scratch | **Reverted; `Atmosphere.glsl.ts` byte-identical to main. Gap remains open** | 2026-07-27 |
| RN-30 | **Shadow contact is two terms that divide by RANGE, and neither of them is a shadow map** | RN-15 removed the understorey from the shadow pass for 12.3 ms of an 18.0 ms frame and removed every grass shadow with it. Putting it back in cascade 0 alone is not expressible in three r185: `WebGLShadowMap.renderObject` tests `object.layers.test( camera.layers )` against the VIEW camera, not the shadow camera, and `onBeforeShadow` fires after the `castShadow` gate. So the contact comes from (a) a base-contact gradient baked into the foliage geometry's existing per-vertex colour attribute, which costs zero draw calls, zero triangles and zero bytes and grounds the silhouette, and (b) a 0.45 m screen-space march toward the sun, which darkens the ground the card stands on. They divide from the cascades by RANGE the way the baked ORM map divides from SSAO by spatial frequency: a term bounded to half a metre of world distance physically cannot disagree with a cascade about a hill. Measured, one settled frame apart in one binary: near-band mean darkening 0.1012 with p99 0.6581 over 40.4% of pixels for (b), 0.0323 / 0.3705 / 17.9% for (a) | Accepted | 2026-07-27 |
| RN-31 | **A product capped by shrinking one of its factors has to be re-capped every time either factor moves; take the factor out of the product instead** | The understorey's height is card height x width jitter x height jitter x distance upscale. RN-15 found it too tall, retuned the height jitter 1.48 to 1.30, and it was still 1.34 m at the worst instance, because the retune left the product intact. RN-30 lowers the band AND demotes `DETAIL_FAR_GROW` to a horizontal-only upscale, so distance is no longer a factor in height at all: mean understorey height 0.4262 to 0.2811 m, tallest card 1.34 to 0.64 m, and neither number has to be re-derived when the other one is next tuned. The general form is the one this log keeps meeting from the other side (DW-33's "a constant that encodes an assumption is a scale assumption in disguise") | Accepted | 2026-07-27 |
| RN-32 | **Confine a shared model's new term BY PATH, not by height, and the confinement must be a property of the call graph rather than a tuning** | The first aerial-perspective attempt added a boundary-layer aerosol inside `ofAtmoScatter` and relied on a 400 m scale height to keep it off the sky. Near-horizon sky rays lie inside that layer for tens of kilometres, so the ground hazed correctly and the sky moved with it (saturation 0.494 to 0.410), and it was reverted whole. The seam that works already existed and had never been named: every ESCAPING ray in this codebase passes `tMax = 1.0e9` and every TERMINATING ray passes a real metre distance. `ofAtmoAerial` is therefore a separate entry point reachable only from the one call site that has a finite distance to geometry, so "the sky did not move" is structural and the probe measures exactly 0.0000 over 276,480 samples. Note what a plausible alternative would have got wrong: a `#define` on "is this the terrain material" fails, because the terrain's own sky-ambient ray is a terrain fragment issuing an escaping ray. DW-22 is unweakened: still one model, one shared uniform record, one GLSL string | Accepted | 2026-07-27 |
| RN-33 | **An optical depth over a finite path is analytic, never marched, because a quadrature rule weights its samples by the path length and a path can be 500 km long** | The aerosol column uses `L * H / (h1 - h0) * (exp(-h0/H) - exp(-h1/H))`, the closed form for a linear height profile, with the `dh -> 0` limit taken explicitly because a horizontal ray is both the commonest case and the one that zeroes the denominator. It self-limits by construction: a viewer 100 km up collects about H worth of dense air however long the ray is, so the same term that hazes 2 km of hillside costs the scaled far planet exactly sigma x H = 0.18 of optical depth. A midpoint or Simpson rule over the same segment multiplies its samples by the full 500 km and greys the planet out, and it would have done so only in the orbital frame, which is not the frame the term was being tuned in | Accepted | 2026-07-27 |
| RN-34 | **A band named "far" that is 30 m away is an instrument measuring the wrong question, and it fails by reporting that the thing under test does nothing** | `probes/groundart.js` compared a near band at frame rows 0.80 to 0.97 against a "far" band at 0.42 to 0.52. At the pinned camera the horizon is at about 0.28, so the far band is ground 30 to 80 m out, where no atmosphere on any planet changes anything. A correctly working aerosol term measured against it moved 0.3% of pixels and read as broken. A `horizon` band at 0.29 to 0.38 was ADDED and the old one left exactly where it was, so the RN-15 numbers stay comparable rather than being improved by moving the ruler: on the same three runs the horizon saturation drop goes 0.0278 to 0.2094 while the old band's stays negative, which is the right answer for both | Accepted | 2026-07-27 |
| RN-10 | **The post stack is NOT a fifth pass. It brackets the four and interposes at exactly one point: after the NEAR pass and before its `clearDepth()`** | ARCHITECTURE 3.1 composites by CLEAR ORDER, never by depth merge, so the depth attachment holds exactly one pass's depth at any instant. A composer that assumes one scene and one depth buffer (three's own `EffectComposer` among them) would read the VIEW MODEL's depth, the last pass to write, and compute ambient occlusion for the player's forearms against a world no longer in the buffer. The near 1:1 scene is also the CORRECT subject: a planet 600 km away in the far scaled pass has no contact shadow, and the arms are a separate depth range whose occluders are not in the same buffer. Consequence, stated because it is a choice and not an accident: AO never touches the sky, the far scaled planet or the view model, while bloom and the grade see all four passes, which is what makes the sun disc and the engine plume bloom. `EffectComposer` was also unusable without breaking the DW-10 seam, since it takes a concrete `WebGLRenderer` and `Renderer.ts` is the only file allowed to name one; the stack talks to a three-method `PostHost` instead | **Accepted, shipped, driven-verified** (probe asserts occlusion CONTRAST, not that the pass ran) | 2026-07-27 |
| RN-11 | **A post pass may not sample any attachment of the framebuffer it writes to, and the rule earns its own buffer** | The obvious three-pass AO folds the upsample into the multiply: read half-resolution AO plus full-resolution depth, multiply into scene colour. The depth texture is an ATTACHMENT of the scene framebuffer, so that pass is a feedback loop and WebGL says so 256 times a frame. The structural fix, and not a workaround: the depth-aware upsample resolves to its own full-resolution R8 buffer first, and the one pass that touches scene colour samples exactly one texture the scene framebuffer does not own. Cost of the rule is 1.4 MB and one draw call; the thing it buys is that the multiply is a one-tap shader with no depth uniforms at all, which is also why it cannot get the background test wrong. Generalises to any future pass that wants depth AND wants to write colour | **Accepted, shipped** (0 GL errors after; 256/frame before) | 2026-07-27 |
| RN-12 | **A ratio metric needs a floor on its denominator, and a colour grade needs the space its pivot was chosen in** | Two instrument bugs from one night, both of which PASSED. `darkening = 1 - with/without` divides by nothing on an already-black pixel and returns exactly 1.0, so the AO probe reported p99 and max darkening of 1.0000 and passed its own "the 99th percentile moves a lot" check on pixels ambient occlusion had never touched: standing rule 11 inside the instrument rather than inside the code. Percentiles are now taken over pixels above a luminance floor of 12/255 and the lit fraction is published beside them. Separately, a contrast of 1.06 about a pivot of 0.4135 was applied to the ACES output, where mid-grey is 0.18: every value below the pivot was pushed down, hardest at the dark end, and the sky's red channel went 37.9 to 7.9 of 255 while every whole-frame mean stayed unremarkable. The grade now runs after the sRGB encode, where mid-grey really is 0.5. **General form: a metric and a curve both have a domain, and neither announces when it is being evaluated outside it** | **Accepted, both fixed and re-measured** | 2026-07-27 |
| RN-13 | **A before/after pair must re-pin every quantity that a data-dependent wait can move, and the sun is one** | The first before/after pair from this lane showed a warm-to-cool shift across an entire hillside that looked exactly like a colour grade and was entirely SUN ANGLE. The build recipe places machines by trying camera pitches until a ghost is legal, so two runs of the same recipe consume different amounts of sim time, and the sun moves with sim time. Setting the time once at the top of a probe is not enough: it has to be set again after every data-dependent wait and immediately before the frame that is captured. The pair was not wrong by a little, it was wrong in a way that invented a result for the effect under test. Related and worth keeping next to it: I then read a green wash across the ground in the after image and was ALSO wrong, and only measuring the two captures region by region settled it (ground 1.5% darker, its green bias identical in the before). **WG-28 says structural validation cannot replace looking at the thing; this is the converse, and both are true** | **Accepted** (probe re-pins; screenshots retaken) | 2026-07-27 |
| RN-14 | **A baked occlusion map and a screen-space one divide by SPATIAL FREQUENCY, and the way to prove it is to shrink the radius, not to threshold a face** | The material lane's ORM map bakes occlusion INSIDE one surface (panel gaps, bolt recesses, centimetres); this stack computes occlusion BETWEEN separate pieces of geometry (a machine's foot on ground, metres). Multiplying two terms that describe the same thing double-darkens and reads as muddy. The separation is structural rather than tuned: `aoRadiusM` 0.9 m on a half-resolution buffer is physically incapable of resolving a panel gap. **The first attempt to check this was the wrong instrument and is the lesson.** It asserted that a machine face away from contact darkens less than 6%, measured 10.1%, and would have been "fixed" by moving the threshold: the band it sampled is the smelter's own RECESSED PANELS, which are decimetre-scale and are legitimately this term's business, and "how much does this band darken" is not a statement about spatial frequency at all. The check is now: re-run the entire measurement with the radius set to 5 cm, the scale a bolt recess lives at, and require it to produce nothing. If the signal does not exist at that scale, no tuning of the shipped radius can be competing with the baked map | **Accepted, shipped** | 2026-07-27 |
| RN-8 | **A face is lit by the sky it can SEE and by the ground beside it, not by a sky term evaluated along local up** | Task 109's black cut banks. `skyAmb` was traced along `up` for every fragment, so it was a constant, and a near-vertical face with `ndl` clamped to zero kept 2.5 to 5.9 per cent of flat ground's light and rendered black. Neither replacement term is tuned: `skyView = 0.5 + 0.5 * dot(n, up)` is the share of the dome the face sees, and the bounce term's radiance is literally the flat-ground branch the shader already computes at that point, using `vBiomeColor` which IS that ground's albedo there. Two properties follow and both are checked: a flat fragment has `skyView == 1` and is algebraically unchanged (measured 154.8 -> 155.4), and every term goes to zero together at night and under shadow. Isolated with a CIRCULAR pit so that bearing is the only free variable, which is what makes the 155-count azimuthal spread attributable to `dot(N, sunDir)` and nothing else | **Accepted, shipped, driven-verified** (cut face over sunlit floor 0.135 -> 0.313, tolerance 0.18 derived from clear-sky diffuse; headlamp lift 16.99 unchanged) | 2026-07-27 |
| RN-9 | **A batch key must carry every property the batch's MATERIAL expresses, and the moment a material gains a map, the surface family becomes one of those properties** | `NodeBatch` bucketed by `metalness > 0.5`, which was free while every role was a flat colour and stopped being free the instant the bucket got a normal map: Leaf, LeafDeep, LeafDry, LeafLight, Grass, Ice and Oil shared `matte` with Bark and Rock, and the texture pass had recorded a REASON for each of those seven being left bare ("sub-pixel blades at any real viewing distance", "a double-sided card whose normal map fights the flat-shaded silhouette"). A rock normal map on a foliage card is worse than no map. The key is now `(family, metalness)` and the recorded decision survives the batching. Two properties follow and both are asserted against the LIVE materials rather than the table: every `flat` material carries zero maps, and every mapped one carries all four with `aoMap.channel === 0` and `repeat === 1/tile_m`. The cost is exactly one extra batch, measured at +4 draw calls of a 150 budget, which is the main pass plus three shadow cascades and nothing else. **The general form: three copy-pasted `normalize()` functions and two independently-derived bucketing rules is how a shared decision drifts, so the role -> family decision lives in ONE module and is checked against the shipped manifest at runtime** | **Accepted, shipped, driven-verified** (normal map alone moves the ground 2.78 counts mean over 288k px against a sky control of exactly 0.000; UVs 294 copied / 0 synthesised across all three paths) | 2026-07-27 |
| RN-6 | **The near voxel mesh draws ONLY what the heightfield cannot express, and it imports the terrain's COLOUR but not its LIGHT** | Two surfaces exist by construction and are allowed to: /core's 1 m solidity shell and the smooth `surfaceRadius` the chunk draws (DW-26). Any face the near mesh puts on the derived surface is a second drawing of ground the chunk already drew, offset by up to half a cell diagonal, so it pokes through as cube corners. The rule makes the mesh's responsibility a property of the surface rather than of the edit sets, which is what "faces of an edit" got wrong: `levelArea` edits a scatter of cells and their faces are isolated cubes. Colour comes from `BiomePalette` so cut rock and the hillside are one substance; light stays `Headlamp`'s, because the terrain program ignores three's light list and shading the mesh with it silently disabled the headlamp | **Accepted, shipped, driven-verified** (924 exposed -> 20 drawn; pad-vs-ground 33.6% -> 7.6%; lamp lift 3.46x -> 11.5x) | 2026-07-26 |

## 4. Architecture & approach
- **Scaled space transition:** maintain two representations per body — a far proxy (low-poly + baked) rendered by the scaled camera, and near streamed terrain. As the observer enters a body's approach band, cross-fade proxy→terrain; floating-origin keeps near terrain at small coords. The handoff is the "no loading screen" moment.
- **Depth:** log **or cascaded** depth in the near camera only (RN-2); the scaled camera uses plain depth at its own scale; the two are composited, not depth-merged (near always over scaled). Spike 1 measures UE's reversed-Z float first — cascaded preferred over `SV_Depth` (Nanite-compat). See [spike1-rendering §3](../spikes/spike1-rendering.md).
- **Factory LOD ladder (conceptual, not built before Phase 1):** (0) full instanced meshes + animated items near; (1) instanced machines, items as scrolling material far; (2) machines as impostors, no items; (3) not rendered — sim is on-rails anyway ([core-engine](core-engine.md) SimProxy). LOD band ties to the same active/on-rails distance bands. **Nanite's intended home** (RN-5).
- **Atmosphere:** UE5 **Sky Atmosphere** component (Rayleigh/Mie, surface→space fade-to-black out of the box) driven by camera altitude + sun direction + `FAtmosphereProfile` from [world-gen](world-gen.md) (D-006). Custom analytic LUT is a Phase-1 fallback. Forge only; Cinder airless. See [spike1-rendering §4](../spikes/spike1-rendering.md).

## 5. Interfaces & dependencies
**Depends on (inbound) — Spike-1 contracts now PINNED (code against these, not the prose):**
- **core-engine** ([spike1-core-engine.md §5](../spikes/spike1-core-engine.md)): `FUniverseCoord`+`FFrameId` & `ToEngine/FromEngine`/`FFloatingOrigin`; `FReferenceFrame`/`IFrameGraph` (`ToUniverse(SimTime)` for body/star positions); `ISimProxy.Position()/Velocity()`; `ISimClock.Alpha()/TickIndex()`. **Events: `OnOriginRebased(DeltaEngine, NewOrigin)`** (re-anchor near scene + atmosphere transform; scaled scene untouched) and **`OnSOIChange(FSOIChangeEvent)`** (re-target approach body). A `UniverseCoord` carries its frame — never a bare `FVector`.
- **world-gen** ([spike1-worldgen.md §4–§5](../spikes/spike1-worldgen.md)): `FTerrainChunk` (center as `FUniverseCoord`, chunk-local positions, skirts, `MaterialId`); `ITerrainProvider::OnChunkReady`/`OnChunkEvicted`/`UpdateStreaming`; `FBodyParams`/`FAtmosphereProfile` (we consume radius/atmosphere — D-006, hardcode nothing). Re-project chunk transforms on `OnOriginRebased` via `CenterUniverse.ToEngine`.
- **physics** (later): entity transforms (interpolated) for vehicles/parts.
- **factory-sim** ([spike3-factory-sim.md §6](../spikes/spike3-factory-sim.md)): the **factory→render stream — PINNED by factory-sim, CONFIRMED here (RC-8), now EMITTED + numerically validated headlessly (factory-sim FS-14: `EmitEntityStates`/`EmitBeltFlowStates`/`GetLineItems`)**. `FFactoryEntityState` (~32B/visible entity: `Id`,`TypeId`,`Position` as `UniverseCoord`,`FQuat16` orient, `VisualState`,`AnimPhase`,`Lod` hint,`Flags`), `FFactoryBeltFlowState` (per visible line: `ItemTypeDominant`,`FlowSpeedQuant`,`Density`,`Compressed`), and on-demand `GetLineItems(LineId)→{itemType,unitOffset}[]` — **the only O(items) call, pulled ONLY at RN-3 LOD-0**. Stream is **delta-only**, drained at render FPS, transforms interpolated via `ISimClock::Alpha()`. Belts emit `FFactoryBeltFlowState` only on flow-state *change* (a steady belt is silent). We evaluate items' world placement via the sim's baked `pathUnitToWorld` (sim never evaluates it; only we do, only for LOD-0 items). **See RC-8 resolution below + §5.4 factory-LOD-ladder mapping.**
**Provides to (outbound):** the rendered frame; camera/observer transform feedback for LOD/AOI; screenshot/observation hooks (useful to [networking](networking.md) AOI tuning).
**Open contract gaps flagged to Admin (Spike 1):**
- `FAtmosphereProfile` has Rayleigh fields but **no Mie params** — request world-gen add `MieScaleHeightM`/`MieScatteringScale`/`MieAnisotropy` (non-breaking; D-006-owned by world-gen). Placeholder Mie used meanwhile.
- UE5 Sky Atmosphere renders **only one atmosphere on screen at once** — fine now (only Forge has one), a constraint for the multi-atmospheric-body future.
**~~Still to negotiate later~~ → NEGOTIATED & CONFIRMED:** the factory→render stream schema (fields, update rate) was pinned by factory-sim Spike 3 §6 and is **confirmed sufficient for RN-3 here (RC-8, below)** — with one *non-breaking* field-add requested (`AABB`/bounds for culling; see RC-8 §4).

---

### RC-8 resolution — does the factory→render stream clear the render wall? **CONFIRMED (with one additive field request) — and now NUMERICALLY VALIDATED HEADLESSLY.**
> *Owner: rendering · Closes reconciliation item [RC-8](../spikes/PHASE0-SUMMARY.md#4-consolidated-reconciliation-register) (the #1 open Phase-0 cross-risk). Co-validation of the integrated real-hardware case stays a Phase-1 joint task with factory-sim. Date: 2026-06-14; **headless numeric validation added 2026-06-15**.*

> **HEADLESS NUMERIC VALIDATION (2026-06-15, jointly with factory-sim).** The RC-8 verdict below was reasoned *on paper* from the §6 contract. It is now **backed by integers**, with no GUI and no GPU: factory-sim emits the §6 stream (`EmitEntityStates`/`EmitBeltFlowStates`/`GetLineItems`, additive + read-only) and a numeric render-cost model (`core/include/of/render_cost.h`, tested in `core/tests/test_render_cost.cpp` — the `render_cost_tests` ctest suite, 133 checks green) scores it against the RN-3 ladder. **The numbers confirm every load-bearing claim of this resolution:**
> - **The O(items)→O(lines) collapse is real and quantified.** A **100,000-entity** stress scene with a realistic LOD spread — Near0 1,500 machines / 300 lines · Mid1 8,000 / 2,000 · Far2 30,000 / 6,000 impostors · OnRails3 60,500 / 12,000 (not rendered) — carries **12,180,000 discrete items on its belts**, yet per-frame item-render work is **180,000 item-evals = the 300 Near0 lines × 600 items/line** (the *only* band that calls `GetLineItems`). **>98% of items (12,000,000) are collapsed away** into LOD-1 flow-material / LOD-2 impostor / LOD-3 not-rendered and never hit `pathUnitToWorld`. Item-eval work scales with **Near0 LINES (hundreds), not items (millions)** — exactly the §6.2 mechanism.
> - **Draw calls scale with machine-TYPE variety (tens), not entity count.** The same 100k-entity scene issues **72 draw calls** (≈ 3 drawing bands × 20 machine TypeIds + belt-material draws), **NOT ~100,000** — instancing buckets per `TypeId`. A slice-scale scene (400 machines / 200 lines, all near) issues **16** (12 machine types + 4 item materials) vs 600 entities.
> - **Instances stay under the ceiling.** 219,500 live instances (39,500 drawn machines + 180,000 near item meshes) — comfortably below the ~2 M instance ceiling (§5.2 budget).
> - **Monotonicity + LOD-3-zero proven.** Pushing a fixed population Near0→Mid1→Far2→OnRails3 strictly reduces cost; **LOD-3 (on-rails) contributes EXACTLY ZERO** draws, instances, and item-evals (the proxy is demoted — factory-sim §5).
>
> This retires the *paper* qualifier on RC-8: the stream contract demonstrably clears the wall for both a slice factory and a 100k-entity stress factory inside one per-frame budget. **What a cost model cannot measure remains the Phase-1 integrated co-validation residual (§5.3 / R5):** the real-hardware LOD-0 `pathUnitToWorld` instance-build cost, Nanite-on-small-meshes, LOD-band hysteresis, and camera-cut burst budgeting — the *render-side* hot spots, which this model flags but only a real renderer can time. (Source: factory-sim FS-14; `render_cost_tests` green in the 14-suite headless core.)

**1. LOD-band → stream mapping (RN-3 ladder, §5.4, against factory-sim §6.2).** Each band pulls strictly less from the stream as distance grows — the mechanism that decouples sim-scale (millions of items) from render-scale:

| RN-3 band | What we draw | What it pulls from the stream | Per-item cost |
|---|---|---|---|
| **LOD-0 (near)** | Full instanced machine meshes + **discrete animated item meshes** on belts; inserter/craft anim from `AnimPhase` | `FFactoryEntityState` per machine (one instance row) **+ `GetLineItems(LineId)`** to place item instances along the sim's baked `pathUnitToWorld` | **O(visible items)** — the *only* band that pays it |
| **LOD-1 (mid)** | Instanced machines; belt items as a **scrolling-flow material** (no discrete item meshes) | `FFactoryEntityState` + `FFactoryBeltFlowState`: `FlowSpeedQuant`→material scroll rate, `Density`→fill look, `Compressed`→cheaper path. **No `GetLineItems`, no per-item data.** | **O(visible lines)** |
| **LOD-2 (far)** | Machine **impostors** (billboard/octahedral); no items, no belt material | `FFactoryEntityState`: `TypeId`,`Position`,`VisualState` only (one tiny instance row); belts collapse into the impostor field | **~0** |
| **LOD-3 (on-rails)** | **Not rendered** — chunk is demoted to `ISimProxy` rate-model (factory-sim §5) | **nothing** — proxy emits no per-entity stream when on-rails | **0** |

The load-bearing guarantee we are confirming: **`GetLineItems` (the lone O(items) call) is pulled only at LOD-0, on demand, per line we *chose* to draw discretely.** The instant the camera pushes a line past LOD-0, its item cost collapses to one `FFactoryBeltFlowState` material param set (O(lines)), then to nothing. `Lod` in `FFactoryEntityState` is the sim's *hint*; **we own the final band decision** (screen-space size / distance), and the contract explicitly lets rendering override it — correct division of authority.

**2. Rough budget (assumptions stated).**
*Assumptions:* one commodity desktop GPU + UE5 instancing (HISM/Nanite-backed machine meshes per RN-4/RN-5); ~2 M instances/frame is a sane GPU-instance ceiling; target 60 FPS / ~16.6 ms; belt physics from factory-sim §2 (**256 sub-units/tile, 64-unit saturation spacing → 4 items/tile/lane × 2 lanes = up to 8 items/tile saturated**; lines capped at 100 tiles).

- **LOD-0 item ceiling (where `GetLineItems` O(items) starts to hurt).** A fully saturated 100-tile line = ~800 items (both lanes); a realistic visible near-field rarely shows whole 100-tile lines saturated. Budget the *discrete-item* draw, not the line count: aim to keep **LOD-0 discrete belt items in view ≲ ~50–100k item instances**. At that range the per-frame work is (a) the `GetLineItems` pulls — O(items) but a flat read of the sim's gap arrays, cheap relative to (b) building/refreshing ~50–100k instance transforms via `pathUnitToWorld`. **`pathUnitToWorld` evaluation, not `GetLineItems` itself, is the LOD-0 cost** (each visible item = one arc-length→transform eval + one instance write). So LOD-0 should cover roughly **a few hundred to ~1–2k machines + the belts physically near the player** (the foreground bay), which at 8 items/tile yields tens of thousands of item instances — comfortably inside the instance ceiling. Past that, LOD-1.
- **Where LOD-1+ must engage.** LOD-0 ends at the distance where a belt item subtends **≲ ~1–2 px** (discrete items no longer legible — the scrolling material is indistinguishable and far cheaper). For ~0.5 m items at 1080p/60° FOV that is on the order of **a few tens of meters**; we drive it on **screen-space size, not a hard metric distance**, so it auto-scales with zoom/FOV. LOD-1→LOD-2 at the distance a *machine* drops below ~a few px (impostor indistinguishable from mesh). LOD-2→LOD-3 is **not ours** — it coincides with core-engine's on-rails demote band (factory-sim §5 / FS-4), so "far enough to be on-rails" and "far enough to not render" are the *same* band by construction.
- **Draw-call / instance / bandwidth ballpark for a realistic base (thousands near, 100k total, most far/on-rails):**
  - *Draw calls:* machines instanced **per `TypeId`** → draw calls = O(distinct machine types in view) ≈ **tens, not thousands** (one instanced draw per type per LOD bucket). Belt LOD-1 material = one instanced/merged draw per material. This is the whole point of RN-3 + instancing: **draw calls scale with *type variety*, not entity count.**
  - *Instances:* LOD-0 ≲ ~1–2k machine instances + tens of thousands of item instances; LOD-1 machines = instanced rows (cheap); LOD-2 impostors = up to ~tens of thousands of 2-tri billboards (trivial). Total live instances comfortably **< the ~2 M ceiling**.
  - *Stream bandwidth:* `FFactoryEntityState` ~32B, **delta-only**. Machines don't move → `Position` is silent; only `AnimPhase`/`VisualState` (≤~3B) tick for *active* machines in view. A few thousand visible active entities × a few bytes × 60 Hz = **single-digit MB/s** — negligible. Belts emit `FFactoryBeltFlowState` only on flow-state change → near-silent in steady flow. The only heavy pull is `GetLineItems` at LOD-0, and it is **read-only, bounded by visible-item count, and never per-tick-pushed.**
  - **Conclusion:** a realistic factory (thousands near, 100k total with most far/on-rails) **clears the wall**: only the small foreground set ever hits the O(items) path, draw calls stay in the tens via instancing, and stream bandwidth is trivial. The 100k entities are mostly LOD-2 impostors (≈0) or LOD-3 on-rails (0). The contract's LOD-0-only `GetLineItems` rule is exactly what makes this hold.

**3. Verdict + residual risk.** **CONFIRMED — the contract plausibly clears the render wall.** The §6 stream gives us everything RN-3 needs and, critically, *lets us stop pulling per-item data the instant we zoom out* — the precise mechanism the render wall demands. The contract is sufficient **as-is** to build LOD-0/1/2/3; the one addition below is an *optimization for culling/band-selection*, not a correctness gap. **Residual render-wall risk → must be measured in the Phase-1 integrated co-validation (jointly with factory-sim):**
  - (a) **Instance build/refresh cost at LOD-0** — whether ~50–100k `pathUnitToWorld` evals + instance-transform writes/frame fit the frame budget on real hardware (this, not `GetLineItems` itself, is the suspect hot spot). Measure with a saturated near-field bay.
  - (b) **Nanite on thousands of small factory meshes** (RN-4/RN-5, rendering.md R1) — Nanite's cost on many small static-ish machine meshes is still unmeasured; this is *the* open Nanite question (terrain already settled to classic).
  - (c) **LOD-band hysteresis / popping** at the LOD-0↔1 boundary (discrete-items → scrolling-material swap) — must cross-fade or it pops, the factory analogue of R2.
  - (d) **Stream drain vs. camera-cut spikes** — a fast pan that promotes many lines to LOD-0 in one frame causes a burst of `GetLineItems` + instance builds; need amortized/budgeted promotion (spread builds across frames, like world-gen's per-tick mesh budget).

**4. Field requests to factory-sim (non-breaking; routed to Admin).** `FFactoryEntityState`/`FFactoryBeltFlowState` are **sufficient as-is for correctness**. One *additive* request would materially help our band-selection/culling and is cheap for the sim (it already stores `StaticDef.AABB`, §1.3 / `bounds`):
  - **`AABB Bounds` (or a packed bound-radius) on `FFactoryEntityState`** — lets us frustum-cull and compute screen-space size for LOD-band selection **without** a per-type asset-bounds lookup on the render side, and without evaluating geometry. Sim already carries `StaticDef.bounds`; surfacing it (or a 2-byte bound radius) in the stream row is non-breaking and removes a render-side indirection. *Nice-to-have, not a blocker.*
  - **(Already in contract, just affirming we rely on it):** the `Lod` hint field and rendering's right to override it; `Compressed` on the flow state (cheaper LOD-1 path); `FlowSpeedQuant`/`Density` for the scrolling material. **No other additions needed.** If the `AABB` add is declined, we fall back to a render-side per-`TypeId` bounds table — slightly more render bookkeeping, zero contract change.

---

## 6. Task backlog / roadmap
- **[RN-45, DONE 2026-07-28, built in this lane rather than requested] Authored `_LOD2` for the four `detail_cards.glb` props.** Blender was available here and the shipped glb already carried the same exporter version, so an unchanged rebuild was proved byte-identical first and the request was withdrawn. Shipped 4 / 4 / 4 / 6 triangles against 18 / 40 / 18 / 42, rebuilt rather than decimated. Measured one-binary against `?proplod2=0`: 1,087,719 to 972,049 triangles, -10.6% of the frame, coverage 0.5533 to 0.5438.
- **[RN-45, OPEN, needs world-gen] A per-chunk noise PHASE in the terrain chunk format.** Sub-metre terrain surface detail of any kind is unreachable from planet-centred metres: float32 at Forge's 6e5 m radius has a 0.0625 m ULP, which is 3 to 15 times the pixel footprint of the ground under the player, so any SCREEN DERIVATIVE of a field keyed on it quantises into concentric arcs (measured, `docs/screenshots/RN45_iso_bump.png`). The fix is a phase reduced modulo the octave period on the CPU in float64, carried alongside the integer cell index, so the shader adds a small local offset to a small local coordinate and never forms a 6e5 intermediate. That is a chunk-format change and therefore world-gen's. The macro COLOUR term does not need it and has shipped.
- **[RN-47, OPEN, this lane] Underwater polish beyond the correctness fix.** The arms are not attenuated (they are drawn after the post interpose point, so it needs a second interpose); the water plane has no lateral bound; a free camera is never tinted; there is no surface seen from beneath; no ripples, glint, refraction, foam or wet-sand darkening. Admin ruled items 2 to 5 of the water lane's list are polish and must not each buy a DW-10 slot; none was spent on the correctness fix either, so the approval is still available.
- **[Phase 1 / M3.1 — BLOCKED ON USER (UI-only), 2026-06-15]** Get real **Megascans/Fab nature materials** into the survival slice (Valheim-grade photo grass/rock/dirt/bark/foliage), replacing the M3.0 authored install-free PBR materials. **Finding: Fab import is NOT scriptable** (see status header + `ue/CLAUDE.md`). **User action needed — Window → Fab → search term → "Add to Project" for each (~2 min total):** ① **Grass surface** — search *"Wild Grass"* (Surface) for the meadow ground · ② **Soil/dirt surface** — *"Forest Floor"* or *"Dry Dirt"* (Surface) to blend bare patches · ③ **Rock/cliff surface** — *"Granite Cliff"* or *"Rock Cliff"* (Surface) for the ore boulders · ④ **Gravel/ground** — *"Forest Ground"* or *"Gravel"* (Surface, optional) · ⑤ **Bark** — *"Pine Bark"* or *"Birch Bark"* (Surface) for the tree trunk · ⑥ **3D plant / grass clump** — *"Wild Grass"* (3D Plant) to replace the flat cone tufts · ⑦ **Wildflowers** — *"Wildflowers"* or *"Meadow Flowers"* (3D Plant) · ⑧ **Wood planks** — *"Wood Planks"* (Surface, optional) for tools/furnace. Filter to **Free** (Fab has a price=Free filter) + prefer **2K** quality to keep size down (each 4K Megascans surface ≈ 50–150 MB — flag size before committing). Fab imports under the folder chosen in the Add-to-Project dialog (commonly `/Game/Megascans` or `/Game/Fab`). **Once added, Admin runs a follow-up agent to wire them** into `M_Meadow_Textured` (ground grass/dirt blend), `M_OreRock` (rock surface, keep per-kind tint + crystal emissive), the tree bark, and replace the ~1800 flat dressing pieces with the 3D-plant clumps.
- **[Phase 1 / M2.2 — LANDED 2026-06-15]** Vessel flies the physics-sim journey in-engine: `AOFVesselActor` (OrbitalFoundryCore plugin, self-contained pImpl over `of::SimWorld`) runs the scripted flight spine ported verbatim from `OrbitalFoundrySubsystem::AutopilotStep` — ascent (ACTIVE thrust) → circularize+park to rails → on-rails coast → transfer across Cinder's SOI (frame switch) → descent → land — driven one fixed tick at a time from the editor (no PIE). Renders in scaled space (MetreToUE=0.001, matching the planet): Forge at origin, Cinder at its 1.2e7 m Forge-frame offset (12000 UE), a cyan breadcrumb trail (`UInstancedStaticMeshComponent`) + yellow cone marker. **Frame handling:** the trail adds Cinder's Forge-frame centre when the vessel is in Cinder's frame, so the SOI handoff is one continuous path. Screenshot-verified (`M2.2_vessel_journey.png` + ascent/landing close-ups); soiSwitches=1, LANDED confirmed. **Known-coarse / next:** at this scale low orbit is surface-hugging and the autopilot teleports across the transfer (`setVesselState`), so the trail is two clusters not a smooth Hohmann arc — a *continuous* powered transfer arc + a true follow-camera/floating-origin rig are the M2.4 / scaled-space-rig follow-ons; the body proxies are the M2.1 static ProcMesh (not yet the streamed-LOD terrain).
- **[Phase 1 / M4.0 — LANDED 2026-06-16]** **Player on the REAL streamed procedural planet (Forge surface walk).** `AOFPlanetTerrain` (OrbitalFoundryCore plugin, pImpl over `/core` `SurfaceObserver`+`TerrainStreamer`) renders the resident `TerrainChunk`s as biome-coloured ProcMesh sections at **real scale (MetreToUE=100)** under the player on new map `/Game/Maps/PlanetSurface`. Floating origin solved **frame-aligned** (actor root rotates radial-up→world-+Z so the patch is horizontal + plain gravity works; chunks relative-pos `(centerUniverse−origin)×scale`). M1 stand / M2 walk-streams / M3 rebase (99× over ~180 km, coords bounded, no jitter) all PIE-verified. **Implements the §6 "swap the static ProcMesh for the quadtree-streamed `ITerrainProvider` LOD path" task from the M2.1 follow-ons** — this IS the streaming-LOD terrain (the `TerrainStreamer` is the provider). **Next:** seal LOD-seam cracks (skirt sizing at coarse↔fine boundaries), radial-up capsule alignment, atmosphere/exposure tuning for the surface, the dual-camera scaled-space handoff (RN-1) wiring the orbital preview to this surface, and carrying harvest nodes/automation onto the planet. Commit `ad472c5`; `docs/screenshots/M4.0_*`.
- **[Phase 1 / M2.1 — LANDED 2026-06-15]** First in-engine terrain render: `AOFPlanetActor` builds a `UProceduralMeshComponent` from `of::worldgen::generateQuadMesh` (6 faces × 4^depth quads × 33² grid), scaled-space (`MetreToUE`), height-ramp vertex colours, two-sided `M_PlanetSurface`. Both bodies render (Forge + Cinder), screenshot-verified. **Known-coarse / next:** swap the single static ProcMesh for the quadtree-streamed `ITerrainProvider` LOD path (RN-5 RealtimeMesh) so the surface holds up on approach; wire the dual-camera scaled-space rig (RN-1) + floating-origin rebase (currently a fixed preview scale); fix per-face winding so single-sided is possible (two-sided is the interim); add Sky-Atmosphere driven by altitude. The static preview is the scaffold, not the streaming terrain.
- **[Phase 0 / Spike 1 — DESIGNED, ready to build]** Scaled-space dual-camera + depth + terrain LOD + atmosphere so walk→orbit→land→walk is *visually* seamless. Full buildable plan + validation in [spike1-rendering.md](../spikes/spike1-rendering.md). Build order: rig → depth → scaled proxies → terrain → transition → atmosphere → integrate. Blocked-on: core-engine/world-gen Wave-1 build landing the demo to hook into (contracts already pinned).
- [Phase 0] Atmospheric scattering: folded into Spike 1 (§4) via UE Sky Atmosphere; custom analytic LUT is a Phase-1 fallback if the built-in is too inflexible.
- [Phase 1] Factory LOD ladder v1 (instancing + far texture flow) — conceptual sketch in spike1-rendering §5.4; **stream contract confirmed sufficient (RC-8, §5) AND numerically validated headlessly (factory-sim FS-14 render-cost model: 100k entities → 72 draw calls, 180k item-evals vs 12.18M items)**; **Nanite's intended home** (RN-5). Phase-1 build + the integrated 100k-rendered co-validation with factory-sim (the RC-8 residual measurements, now the *only* render-wall unknowns: LOD-0 instance-build cost, Nanite-on-small-meshes, LOD-band hysteresis, camera-cut burst budgeting).
- [Phase 4] Visual polish, weather, advanced VFX; multi-atmosphere path (Sky-Atmosphere single-atmo limit).

## 7. Open questions & risks
- **R1 (terrain Nanite — RESOLVED for Spike 1):** terrain uses **classic streamed meshes, not Nanite** (RN-5) — world-gen already LODs and Nanite blocks the depth path. **Nanite cost on thousands of small *factory* meshes is the surviving open Nanite question** → now scoped as a named RC-8 Phase-1 residual (§5, item b): measure in the integrated factory-render co-validation, not Spike-3 (which is sim-only).
- **OWED DEBT (RN-61, 2026-07-28) - node scale does not carry the harvest reach.** `NodeField.build` now gives trees a per-instance size of 0.86 to 1.14, but `art.radiusM` (harvest reach) and `art.hitUpM` (hit point) in `NodeArt.ts` are per-KIND constants and do not scale with `pl.scale`. So a big tree and a small tree are chopped from the same distance and struck at the same height. The range was capped at +/-14% specifically to keep this under the aim tolerance rather than to hide it. **Any lane widening node scale must scale those two with it**, or the art change becomes a gameplay bug. Same applies to outcrops, whose `pl.scale` comes from deposit richness and can already exceed this range.
- **R5 (render-wall LOD-0 hot spot, from RC-8 §5) — now the SOLE surviving render-wall residual; the *contract* half is closed headlessly.** The numeric render-cost model (factory-sim FS-14, `render_cost_tests`) confirms the suspect cost is **not** factory-sim's `GetLineItems` (a cheap flat read whose work is bounded to the Near0 lines — 180k item-evals for 300 near lines in the 100k stress scene) but **our** per-frame `pathUnitToWorld` evals + instance-transform writes for those ~50–180k discrete LOD-0 belt items, plus burst spikes when a camera cut promotes many lines to LOD-0 at once. The model proves the *count* that reaches us is bounded (Near0 lines × items/line, O(lines)) — what it can't time is the **per-item build cost on real hardware**. Mitigation direction unchanged: screen-space-driven LOD-0 band kept tight (foreground bay only), amortized/budgeted line-promotion (spread instance builds across frames), LOD-0↔1 cross-fade to kill the discrete-items→scrolling-material pop. **Measured in the Phase-1 integrated co-validation (jointly owned with factory-sim).**
- **R2 (cross-fade — mitigation DESIGNED, pass/fail set):** scaled↔near pop + lighting mismatch on fast approach. Addressed structurally in spike1-rendering §2 (proxy-as-backdrop + occlusion-first cross-fade + single shared sun + depth-aware horizon haze). Retired by validation **RV3/RV8** (frame-stepped orbital-speed descent: no silhouette jump, no terminator discontinuity, no band-edge flicker). **Primary residual visual risk until the build runs.**
- **R4 (new — depth × Nanite):** Nanite can't take a custom per-pixel `SV_Depth` write → log-depth and Nanite-on-terrain partly incompatible. Resolved by RN-5 (classic terrain) + measuring reversed-Z-float-first (RN-2). Flagged to Admin as a standing UE5 constraint.
- **R3 (Admin Q3):** 1st vs 3rd person affects camera rig and near-field detail budget. Rig (spike1-rendering §1) is perspective-agnostic — NearCam parents the pawn either way; decision still pending gameplay.

## 8. Subagent delegation log
| Date | Subagent task | Status | Outcome |
- **2026-07-28 (RN-59)** - *Tree and rock asset survey.* Read the headless-Blender builders, `contracts.json` and the scatter/node paths for geometry, LOD budgets, materials and existing per-instance variety. Returned the three findings the pass turned on: there is no forest (14 nodes, no scatter tree); every rock is one vertical radial sweep (`harvest_common.lobe`) and all four ore boulders are the same 164 triangles; `NodeBatch` never calls `setColorAt`. Also mapped the dead weight (`_LOD1`/`_LOD2`/`_Stump` never drawn, three .glb files never loaded). Folded into the status block above; no report file kept.
|------|---------------|--------|---------|
| 2026-07-27 | RN-30 shadow contact, grass height, aerial perspective take two. **Two Explore subagents, both read-only, both narrow, run in parallel before any edit.** (1) *Shadow rig and post stack*: is a per-cascade caster set expressible in three r185, where does a full-screen pass hook, does a geometry vertex-colour attribute survive `BatchedMesh.setColorAt`, and where do query flags get parsed. Returned the finding the whole shadow-contact design turns on: `WebGLShadowMap.renderObject` tests `object.layers.test( camera.layers )` against the VIEW camera at r185:511, not the shadow camera, so `ShadowRig`'s own comment is false and layers cannot select a cascade; and confirmed from `color_vertex.glsl.js` that a per-vertex gradient MULTIPLIES the per-instance tint rather than being erased by it, which is what made the free option free. | Done | Folded into `ContactGlsl.ts`'s header and RN-30/RN-31. |
| 2026-07-27 | RN-30, subagent 2, *the shared atmosphere*: map every exported GLSL string, every function signature, every consumer and every call site, and say per call site whether it knows a FINITE ray length. Returned the seam the second aerial-perspective attempt is built on and that I had not seen in two passes over the file: `ofAtmoScatter` is the ONE top-level entry point, sky passes `1.0e9`, terrain aerial perspective passes a real distance, **and the terrain's own sky-ambient ray also passes `1.0e9`**, which is precisely the case that kills the obvious "switch on the material" alternative. | Done | Folded into `Atmosphere.glsl.ts`'s `ofAtmoAerial` note and RN-32. |
| 2026-06-14 | Research thread (run inline via WebSearch/WebFetch — Agent-spawn tool unavailable in this session): scaled-space dual-camera + log-depth techniques for planetary renderers; Rayleigh/Mie scattering in UE5 | Done | Outerra log-depth formula + fragment-shader-depth requirement + 24-bit→cosmic-range precision; UE5 Sky Atmosphere does surface→space fade-to-black out of the box but renders **only one atmosphere on screen at once**; UE default **reversed-Z float** depth may suffice (measure first); **Nanite blocks custom `SV_Depth`** → drove RN-5 (classic terrain). Folded into [spike1-rendering.md](../spikes/spike1-rendering.md) §3–§5. |
| 2026-06-14 | RC-8 render-wall reconciliation (run inline): evaluate factory-sim Spike-3 §6 stream (`FFactoryEntityState`/`FFactoryBeltFlowState`/`GetLineItems`) against RN-3 LOD ladder; map each band→stream, budget LOD-0 item ceiling + band-engage distances + draw-call/bandwidth, render verdict | Done | **CONFIRMED**: stream sufficient as-is for RN-3; `GetLineItems`-at-LOD-0-only is exactly the O(items)→O(lines) collapse the wall needs. Draw calls scale with type-variety (tens), not entity count; stream is delta-only/near-silent; realistic factory clears the wall. **One non-breaking field requested** (`AABB`/bound-radius on `FFactoryEntityState` for cull/band-select). Real hot spot is **our** LOD-0 `pathUnitToWorld` instance builds (new R5), not the sim's call. Folded into §5 (RC-8 resolution + budget) + R1/R5. |
| 2026-06-15 | RC-8 / Q6 render wall HEADLESS NUMERIC VALIDATION (joint with factory-sim; cost model + tests run by factory-sim, verdict consumed here): factory-sim emitted the §6 stream additively and built a numeric render-cost model (`render_cost.h`) + `render_cost_tests`; rendering consumes the numbers to retire the "on paper" qualifier on RC-8 | Done | **VALIDATED — the paper verdict now holds in integers.** 100k-entity stress scene → **72 draw calls (not 100k), 219,500 instances, 180,000 item-evals vs 12,180,000 belt items (>98% collapsed)**; item-eval work scales with Near0 LINES (300), not items (12M); slice scene → 16 draw calls vs 600 entities; monotonic cost outward; **LOD-3 on-rails = exactly zero**. Sole surviving render-wall residual is render-side R5 (real-hardware LOD-0 `pathUnitToWorld` build cost / Nanite-on-small-meshes / LOD hysteresis) — the Phase-1 integrated co-validation, jointly owned. Folded into the RC-8 resolution (headless-validation note), RN-3 row, status, R1/R5. 14/14 headless ctest suites green. |
| 2026-06-15 | M2.1 first in-engine terrain render (Admin, via the SoftUEBridge screenshot loop): wrote `AOFPlanetActor` to build a cubed-sphere `UProceduralMeshComponent` from `of::worldgen::generateQuadMesh`; authored `M_PlanetSurface` (two-sided vertex-colour lit); spawned/framed/captured both bodies | Done | **Worldgen→UE-mesh pipeline proven end to end.** Forge + Cinder render from the real heightfield, screenshot-verified. Gotchas logged: cubed-sphere faces have mixed basis handedness → single winding culls ~half (interim fix two-sided material; proper per-face winding TODO); UE `MaterialExpressionVertexColor` connects via its DEFAULT output, not `"RGB"`; `build-and-relaunch`'s relaunch step hung (editor stuck pre-log on a native modal) — direct `UnrealEditor.exe <uproject>` launch is reliable. Coarse static single-mesh preview; quadtree-streamed LOD + dual-camera/floating-origin are the next rendering tasks (§6). |
| 2026-06-15 | M2.3 factory render from the §6/G7 stream (Admin): wrote `AOFFactoryActor` (self-contained, owns a `FactorySim` via pImpl); machines from `EmitEntityStates`, belts as static surfaces, discrete items from `GetLineItems` mapped `UnitOffset/capacityUnits`→path lerp; `M_FactoryColor` MIDs; `BuildFactory()`/`StepFactory(n)` drive it without PIE | Done | **The RC-8-validated stream now drives live pixels** — a production line of 5 crafting machines + belts of flowing items, screenshot-verified (`docs/screenshots/M2.3_demo_factory.png`), `producedCount` climbing. This is the first real exercise of the §6 contract through an actual renderer (the RC-8 residual co-validation is now partially under way in-engine, not just the cost model). v1 is LOD-0 only (discrete item meshes); LOD-1 scrolling-flow material / LOD-2 impostors / per-`TypeId` instancing buckets are the next factory-render tasks. New gotcha: `trigger-live-coding` stalled 15 min+ (cold console full recompile) — direct `Build.bat` (~10 s) is the dependable path for `.cpp`-only edits. |
| 2026-06-15 | M3.0 survival-slice graphics/atmosphere pass (Admin, via the SoftUEBridge screenshot loop): lift the survival slice from flat untextured "Play-Doh plastic" to a textured, atmospherically-lit Valheim-leaning look — lighting/sky/fog/post stack + textured PBR materials on ground/foliage/tree/water/ore/tools/furnace, install-free | Done | **The slice is de-plasticked.** Root cause of the flat look found: **Lumen GI was disabled** (`r.DynamicGlobalIlluminationMethod=0`) — re-enabled (+Lumen reflections) in `DefaultEngine.ini`; that plus the warm-key-sun / real-time-SkyLight / Valheim-fog / unbound-PostProcessVolume stack gave an instant before→after jump even pre-texturing. **Corrected a wrong premise in the brief: full StarterContent is NOT installed** (only `T_ground_Moss_D` exists on disk; the prior pass was right that it's absent) → authored textured PBR materials install-free from engine noise/detail textures (`T_Default_MacroVariation`, `RandomNormal2`, `Good64x64TilingNoiseHighFreq`) — the **detail normal + roughness variation is what kills the plastic read**. `M_Meadow_Textured` grass/dirt ground, `M_GrassTuft` two-sided foliage on ~1800 dressing pieces, `M_Water` reflective ripple water, upgraded `M_FactoryColor`/`M_OreRock` in place. One material-path-only C++ line (water node loads `M_Water`), no gameplay/sim change. 5 PIE screenshots (`M3.0_*`). Commits `fb0e6e3`/`b49ff86`/`4e12d43`. New material-authoring gotchas logged in `ue/CLAUDE.md` (protected expression list, add-don't-recreate, normal sampler type, FlattenNormal path). **Residual:** flat-shaded low-poly meshes still read stylized (acceptable, Valheim-ish); enabling Fab/Megascans later would allow real photo-textured ground/bark/rock for a higher bar. |
| 2026-06-15 | M3.1b FP held-tool view-model + polish (subagent): wire the (assumed) newly-added Megascans nature assets, fix the first-person held-tool feel, polish | Done (Task 1 BLOCKED — Megascans absent; Tasks 2+3 landed) | **Task 1 (Megascans textures) BLOCKED: the assets were NOT actually imported.** AssetRegistry confirms `/Game/Megascans|Fab|Quixel` all empty + zero Megascans content on disk; the Epic launcher log shows the user opened the Fab panel + searched "megascans" but **no Add-to-Project/download/import completed** — so the ground/ore/tree/foliage stay the M3.0 authored materials. **Task 2 (FP held-tool) LANDED:** new FP view-model (`FPArmMesh`+`FPToolMesh` `UProceduralMeshComponent`s parented to the FP camera, `bOnlyOwnerSee`, shown FP-only) so the pickaxe/axe is visible + tracks pitch+yaw like a real held tool, with a procedural Tick swing; the TP hand tool + montage are kept. **Found+fixed a pre-existing FP bug:** the hidden player body still CAST A SHADOW at the camera → a black slab over the FP view; `ApplyBodyVisibility` now drops the body shadow in FP. **Task 3 (polish) LANDED:** `M_Metal` (real metallic head material — tool heads read as metal not plastic), `M_Oil` (dark glossy crude-oil pool), and `AOFSurvivalHUD` (minimal crosshair + "E Harvest <res>" prompt, replacing the always-on debug text). PIE-verified (`M3.1_{fp_tool,fp_swing,thirdperson,ore_metaltool,oil_water}`). Commits `30ebe1a`+`2e7082c`. Gotchas logged in `ue/CLAUDE.md` (FP black-slab=body-shadow; FP view-model must be small @90° FOV; C++ `FRotator(Pitch,Yaw,Roll)` vs Python `Rotator(roll,pitch,yaw)`). **Residual TODO = the original M3.1 Megascans wire-in, still gated on the user actually clicking Add-to-Project in Fab (shopping list in §6 below).** |
| 2026-06-15 | M3.1 get real Megascans/Fab nature materials into the survival slice (subagent): verify Fab sign-in + plugin enabled; find an automatable Fab/Megascans import path; if scriptable, pull+wire a free nature set, else produce a precise shopping list + the definitive UI-only finding | Done (UI-only branch) | **Fab import is UI-ONLY — NO scriptable path exists, by architecture.** Fab plugin ships + enabled + **user signed in** (fresh EOS persist `.dat`); Fab module loads clean in-editor. The acquire call `UFabBrowserApi::AddToProject(signedUrl, meta)` is `UFUNCTION()` w/o BlueprintCallable → **not Python-exposed** (verified live: default object has no `add_to_project`/login/token) + needs a signed CDN url minted by the Fab web panel's JS after a user click → unmintable headlessly. Python only exposes the *post-download translator* (`InterchangeMegascansPipeline`/`MegascanImportType`/`MegascanMaterialPair`), not an acquirer; `BridgePythonCallHelper` is generic stubs; the legacy Quixel Bridge :31337 socket needs the dead standalone Bridge app. No `/Game/Megascans|Fab|Quixel` content yet. **Deliverable = the 8-asset free shopping list + click-path** (§6 M3.1 row) for the user's ~2-min Fab-panel pass; Admin wires them after. Editor left clean on SurvivalTest. Full evidence → `ue/CLAUDE.md` "Fab / Megascans import is UI-ONLY". No code/scene/material change this pass (UI-only branch); nothing committed except docs. |
| 2026-06-16 | M4.0 put the player on the REAL procedural planet (subagent, via the SoftUEBridge screenshot loop): new `AOFPlanetTerrain` actor (pImpl over `/core` `SurfaceObserver`+`TerrainStreamer`) renders resident `TerrainChunk`s as biome-coloured ProcMesh sections at real scale on new map `/Game/Maps/PlanetSurface`; drove M1 static / M2 walk-stream / M3 floating-origin rebase | Done — all 3 milestones landed + PIE-verified | **The seamless surface works.** Player stands + walks on Forge's streamed cubed-sphere terrain (biomes + LOD + floating origin). Frame-aligned floating origin (root rotates radial-up→world-+Z so the patch is horizontal) was the key insight that turned a tilted-slab render into "standing on the ground". M3 verified over a ~180 km auto-walk: 99 origin rebases, engine coords bounded near 0 (no jitter/blowup), biomes cycled mountains→plains→ocean. ~200 chunks / ~240k verts resident. No `/core` edit (the `terrain_stream`/`surface_walk`/`biome` contracts were sufficient as-is). Commit `ad472c5`; `docs/screenshots/M4.0_{planet_surface,biome_terrain,walking,horizon_curve,fp_look}`. New gotchas in `ue/CLAUDE.md` (tilted-slab→frame-align fix; flying-mode pin beats gravity-vs-snap fight; pin to `/core` surface height not ProcMesh collision; new_level dup-suns; PowerShell-only map args). Residual: LOD-seam cracks, radial-up capsule alignment, biome exposure blowout — all polish. |
| 2026-06-16 | M4.3 collision ground + physical smooth walking + per-biome Megascans surfaces (subagent, via the SoftUEBridge PIE loop): the player FLOATED (no ground) and running TELEPORTED. Gave the streamed chunks real collision, switched the player from per-tick eye-pin to physical `MOVE_Walking` on that collision, made the floating-origin rebase shift player+terrain together (invisible), wired `M_PlanetSurface` to blend grass↔rock Megascans surfaces per biome + conifer bark | Done — Task 1 + Task 2 landed + PIE-verified IN MOTION | **Both gameplay bugs fixed.** Root cause = the old eye-pin model (chunks had `NoCollision`, player snapped to `/core` height each tick → floated; the per-tick + per-rebase `SetActorLocation` = the teleport). Fix: chunks `QueryAndPhysics`+sync-cook+block-Pawn; player `MOVE_Walking`+gravity dropped onto the collision (pin removed); rebase shifts the player by the SAME `OldFrameRot.RotateVector(lastDelta×scale)` the terrain re-anchors by → no visible jump; safety net only on a >2 km tunnel-while-falling. **PIE-verified in motion:** standing `clear=2.2 cm`/`is_falling=false` (solid, not floating); WASD walk = 15 consecutive intervals all dX≈94.6 cm (constant velocity, no teleport) + smooth dZ≈−2.7 cm downslope. Task 2: `M_PlanetSurface` blends grass↔rock per biome via a per-vertex `BiomeSurfaceWeight` packed in vertex ALPHA (`lerp(grass,rock,A)×VertexColor.RGB` base/normal/rough), tree bark = 9 `MI_TreeBark_*` conifer instances; grass biome PIE-verified textured, rock biomes wired. Gotcha re-bit: `VertexColor` RGB = DEFAULT output `""` not `"RGB"` (black ground). New `/Game` packs committed (`RockyGround`, `Materials_TreeBark`; `WaterMaterials`+`B3D` left untracked — not yet wired). Commit `21ac998`; `docs/screenshots/M4.3_*`. **Residual / honest limit:** the observer-FOLLOW (streaming centre tracking the player over long walks) could NOT be cleanly PIE-verified through the bridge — its ~1 Hz bursty input + the editor's ~3 fps PIE tick alias the per-frame player delta/velocity to ~0 at the actor's tick samples, so the observer barely advanced in the harness (lat/lon stuck) even though the float/teleport fixes are rock-solid. The drive is implemented robustly (max of position-delta and velocity×dt, `TG_PostPhysics`) and is correct for continuous held-key gameplay; needs confirming with a real held-W session. The large resident patch (300 chunks/depth 13) covered a 7.8 km walk grounded regardless. No streamed-foliage scatter on the planet; no snow/soil scan (Polar tints rock). |
| 2026-06-16 | M4.4 fix the floating + popping harvest nodes on `PlanetSurface` (subagent, recovering a prior agent's uncommitted half-fix, via the SoftUEBridge PIE loop): nodes floated above the ground and despawn/respawn'd every step. Snap each node onto the real chunk collision + make the deposit streaming id-stable so in-range nodes persist | Done — both bugs fixed + PIE-verified cheaply + committed | **FLOAT + POP both fixed, UE-side only (no `/core` edit; `21ac998`/`b9fe759` not regressed; harvest E untouched).** (1) FLOAT: deposit pos uses `/core` DESIGNED relief but the chunk collision is the RAW heightfield → `SnapNodeToGround` line-traces each placed node down onto `ECC_WorldStatic` and drops its base on the hit (cached `NodePositions` recovered from the snap so rebases stay grounded). (2) POP: the prior agent's "snap grid origin to dLat/dLon multiples" attempt FAILED because `dLon=stepM/(R·cosLat)` breathes with latitude → re-quantized every id each step (measured 0% persistence) → REWROTE `GenerateLocalDeposits` onto a SINGLE GLOBAL angular lattice (constant `dAng=stepM/R`, cell = integer `(iLat,iLon)`, id=`hashCombine(iLat,iLon)`; only the loop extent scales by 1/cosLat). Streaming loop already kept in-range / despawned out-of-range / rebase-shifted via `ReanchorDeposits`. **Verified CHEAPLY** (no long PIE drive) with 3 new `BlueprintCallable` diagnostics: all 40 nodes clearance=1.0 cm above collision; ~55 m/6-step walk → id persistence 100/95/100/100/97/100 % (only 0–2 range-edge nodes churn/step). **Gotcha:** streamed PMC collision is NOT hit by Python trace-type/object-type/sphere traces even under the grounded player — verify from C++ `LineTraceSingleByChannel(ECC_WorldStatic)`; `current_floor.blocking_hit` confirms grounding. Commit `<this>`; `docs/screenshots/M4.4_{nodes_grounded,nodes_stable}.png`. Diagnostics left in for regression. |
| 2026-07-26 | RN-6 (Explore subagent, read-only): map the terrain and terraform render path end to end — `surface_field.h` raising/lowering/`levelArea`, the WASM exports and current ABI, every JS consumer, `exposedFaces`' face record and `VoxelGreedy`'s winding, the terrain material and biome palette against the near voxel mesh's, every URL flag, and how a probe is written and run | Done | Handed back the facts the fix was built on: `exposedFaces` **collapses provenance** (`isSolid` is evaluated before the face test, so a cut wall and a placed cell are indistinguishable and no material id is transmitted); `of_edits_is_added_cell` / `of_edits_is_removed_cell` already exist at ABI 5, so the filter needed **no ABI bump**; `VoxelGreedy.emitRect`'s winding and per-vertex normals are CORRECT (which retired the brief's winding hypothesis before a line was written); the terrain is a `ShaderMaterial` with a per-vertex biome palette and its own atmosphere integral against the near mesh's flat `MeshLambertMaterial(0x8a7a63)`; and `?lamp=` is forwarded by the smoke runner but **parsed by nothing** (a dead flag, reported). Folded into RN-6 and into `VoxelSkin.ts`'s header. |
| 2026-07-27 | RN-9 consume the shipped tiling surfaces (ASSET-SPECS 2.9 / build-tooling R8). **No subagent was spawned:** the hand-off named the three call sites and their line numbers, so the work was three edits and the whole cost was in the verification, which is not delegable when the acceptance is "prove the shader READS the map" | Done | See the RN-9 head. Landed all three changes; six asserted properties green; before/after captured from one settled frame. Two findings worth keeping beyond this pass: **three r185 already defaults `Texture.channel` to 0**, so the hand-off's predicted `aoMap`-on-`uv1` silent-tint failure does not exist in this version (checked in `node_modules`, not assumed) — the explicit set is kept only because the ORM image is one object in three slots; and **an intermediate `vite build` captured another lane's mid-edit source and produced a bundle that booted into a per-frame `this.d.player is not a function` with `__of.ready` never resolving**, which presents as `run.mjs` hanging with zero output. With three lanes live in one tree, `npm run build` is a snapshot of a moving target: rebuild immediately before measuring, and confirm boot with a 10-second driver before spending five minutes on a probe. |
| 2026-07-27 | RN-15 world art. **Two Explore subagents, both read-only and both narrow**, run in parallel while I read the shaders myself: (1) the ground-detail card assets, their triangle counts, LODs, dimensions and material roles, and whether per-instance colour exists anywhere in the client; (2) the smoke runner's exact command line, `--out` conventions, the probe interface, the page's global API and where draw calls, triangles and frame percentiles are exposed | Done | Their answers set the whole budget argument: the cards are 18 to 42 triangles with **no authored LOD2** (so `pair.lod2 ?? near` means a card at 70 m costs what a card at 2 m costs, which is why the fix had to be shadows and culling rather than a geometry ladder), and per-instance colour existed nowhere. **The one thing I would not delegate again is the same thing RN-9 would not: verification.** The subagent report was the input to the plan; every number in the head above came from running the thing. Also worth recording because it nearly cost the pass: I checked three's own source rather than trusting the obvious reading of `setColorAt`, and the obvious reading is wrong. `USE_BATCHING_COLOR` declares and writes `vColor` in `color_vertex.glsl.js:24`, but `color_fragment.glsl.js` multiplies `diffuseColor` by it only `#if defined( USE_COLOR ) || defined( USE_COLOR_ALPHA )`, so per-instance colour on a `BatchedMesh` is silently DISCARDED unless the material also sets `vertexColors`, and setting `vertexColors` without a `color` attribute binds the default (0,0,0,1) and renders every prop BLACK. Both halves are required and neither fails loudly on its own |
| 2026-06-15 | M2.2 render the vessel flying the physics-sim journey (Admin, via the SoftUEBridge screenshot loop): wrote `AOFVesselActor` (self-contained, owns an `of::SimWorld` via pImpl); ported `OrbitalFoundrySubsystem::AutopilotStep` into a one-step-at-a-time state machine; `BuildJourney()`/`StepJourney(n)` drive it without PIE; spawns two `AOFPlanetActor` bodies (Forge origin, Cinder @1.2e7 m offset); cyan breadcrumb-trail ISM + yellow marker; `M_VesselColor` emissive MIDs; SOI-frame-continuous trail | Done | **The flight spine flies in-engine.** Ascent→orbit→SOI-transfer→Cinder-descent→land renders in scaled space (MetreToUE=0.001), soiSwitches=1 + phase=LANDED confirmed live, 1626 trail breadcrumbs across the trip; screenshot-verified at 3 framings (`M2.2_vessel_journey.png` wide two-body + ascent/landing close-ups). The composed-core `of::SimWorld` now drives pixels end to end (the M2.1 worldgen + M2.3 factory + the physics flight loop, one actor each). Honest limits: scaled low orbit hugs the surface and the autopilot teleports across the transfer, so the trail is two clusters not a continuous Hohmann arc (a true continuous transfer arc + follow-camera/floating-origin rig are M2.4/scaled-rig follow-ons). New gotchas logged in `ue/CLAUDE.md`: "Restore Packages" startup modal blocks the bridge after a hard editor kill (delete `Saved/Autosaves/PackageRestoreData.json`); the default World-Partition landscape buries scaled scenes and resists hide → destroy it for screenshots + pitch up past the SkyAtmosphere horizon; `connect_material_expressions` is plural; `get_instance_transform` returns a bare `Transform`. |

## 9. References
MASTER_PLAN §6 (seamless magic), §7 (No Man's Sky/Astroneer references). KSP dual-camera scaled space; log-depth-buffer techniques; UE5 Nanite/Lumen.
