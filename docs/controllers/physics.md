# Physics & Orbital Mechanics — Master Controller Context

> **Domain owner:** `physics-controller` · **Reports to:** Admin · **Phase:** R/S · **Status:** Wave-0 orbital core GREEN; **the VESSEL programme's headless foundation is BUILT + GREEN** (`vessel.h`, `atmosphere.h`, `flight.h`, 24/24 ctest, a reference rocket flies pad-to-orbit in `flight_tests`); the BROWSER walker is a hand-rolled kinematic capsule (DW-12) and owns `web/src/player/` · **THE ROCKET FLIES IN THE BROWSER (PH-20 to PH-27), 2026-07-27: a player builds a vessel in the bay, walks to it, boards it, launches it, stages it and reaches an 85.9 x 79.7 km orbit on a navball, with the factory still producing on the ground below and no loading screen anywhere in that sentence** (`probes/ascent.js`, green) · **THEN IT WAS DRIVEN ADVERSARIALLY UNTIL IT STOPPED BREAKING (W11 lane G, PH-28 to PH-36): nine defects, every one of which read healthy on every instrument, including a rocket that was not being DRAWN for the whole walk over to it, a stage key that dismantled the vehicle on the pad with no way back, an upper stage that burned inside the interstage, an ALT AGL that read 19.20 m with the rocket standing still on the ground, and a message line on which no message had ever been visible** · **THEN THE FLIGHT BECAME NAVIGABLE (W12, PH-37 to PH-43): M opens an orbital map, a maneuver node on it publishes the delta-v, the direction, the countdown to LIGHTING THE ENGINE and the burn's length plus the orbit it produces, the node draws on the navball, seven SAS mode keys and buttons point at the orbital triad, and the flight camera finally reads the mouse** (`orbital::look()` had been published, clamped, correct and CALLERLESS since the day it was written) · **THEN THE REPEAT SINKING DEFECT WAS HUNTED UNDER A REAL MOUNTAIN AND NOT REPRODUCED (PH-45), which is the finding: the walker's feet are bit-identical for 3000 ticks under 19 m of rock, before and after a real browser reload, and the heightfield never gets a say. What the hunt DID find is a second authority gap, where a buried capsule owns nothing and falls 29 km through bedrock (R18)** **AND THEN THE STRUCTURAL PATH CAME BACK FLAT TOO (PH-47): with 90 player-built parts under the walker, enclosed by a foundation laid at the feet, on a deck, beside the base and in a tunnel next to it, driven AND on the real rAF loop at 100 to 121 Hz, the spread is 0.000000 m everywhere. Three of the four candidate mechanisms are now closed by measurement and the fourth by reading** **AND THEN THE ONE DEFECT THAT WAS REPRODUCIBLE GOT FIXED (PH-60 to PH-63, R18 CLOSED): a capsule buried in rock owned nothing and fell 29 km out of the world, because one null meant both "there is nothing under me" and "I am inside rock" and the caller assumed the first. It is now two answers and two rules: a tick that would end buried is REFUSED, and a capsule the world buries is EJECTED up the local radial, which is the only direction with a termination proof. Found on one seed, then reproduced on FLAT GROUND with the levelling key, where the pre-fix walker falls 508 m in ten seconds. R18b was driven and REFUTED: you walk out of a wall** · **AND THEN A VESSEL STOPPED NEEDING TO BE LOOKED AT (PH-64 to PH-70, 2026-07-28): a rocket left on a pad, one abandoned mid-ascent and one in orbit all survive a REAL browser reload with their design, their staging state, their fuel and their place, the fuel differing by EXACTLY 0 kg and the place by 0.000e+00 m; an unattended vessel in orbit is nine numbers and a clock rather than a simulation, advanced by one Kepler solve WHEN SOMEBODY ASKS and by nothing at all when nobody does; asking where it is at tick N+3600 in one jump and in six jumps is BIT-IDENTICAL; and that answer agrees with 60 s of genuinely flown integration to 6.5e-5 m. The player's body now parks coherently and its position is written down, which closes R13 as a side effect. This is the FOUNDATION for remote control and deliberately not the feature: the seam is published, every name in it is driven by a probe, and nothing puts a player back inside a vessel** · **Last updated:** 2026-07-28 (PH-64 to PH-81, ABI 20; R11, R13, R24, R25, R27, R28, R31 and R32 closed; R22, R23, R26, R29, R30, R33 to R37 open, R38 to R40 opened for the gameplay lane)
> Read alongside: [MASTER_PLAN](../MASTER_PLAN.md) · [AGENT_ARCHITECTURE](../AGENT_ARCHITECTURE.md) · [ADMIN](ADMIN.md)

## 1. Mission
Deliver believable, stable, time-warpable orbital flight and rigid-body vehicle/structure physics — KSP-grade credibility — within the active/on-rails framework, while staying cheap enough to coexist with the factory sim.

## 2. Scope & owned subsystems
- **Orbital mechanics** — patched conics (two-body + SOI switching), analytic propagation for on-rails bodies/vessels.
- **Rigid-body vehicle physics** — multi-part craft, joints, thrust, mass/CoM, aerodynamics & drag in atmospheres.
- **Surface & character physics** — walking, vehicles on terrain, collision against world-gen terrain.
- **SOI transitions** — handing a vessel between reference frames as it crosses sphere-of-influence boundaries (with [core-engine](core-engine.md)).
- **Active↔on-rails conversion** — produce/consume orbital element sets so a vessel can "park" on rails and resume under physics.
- **Non-goals:** the coordinate/frame substrate (→ core-engine); rendering of trajectories (→ rendering/gameplay map view).

## 3. Key design decisions
| # | Decision | Rationale | Status | Date |
|---|----------|-----------|--------|------|
| PH-1 | Patched conics, **not** n-body (mirrors D-002) | Time-warp + stable orbits + cheap + deterministic | Accepted | 2026-06-14 |
| PH-2 | Full rigid-body physics only for active vessel + nearby; rest on rails | Core scalability (D-003) | Accepted | 2026-06-14 |
| PH-3 | On-rails state = classical Keplerian element set + epoch; advance via **universal-variable** Kepler solve (one path for elliptic/parabolic/hyperbolic) | Analytic propagation; warp-free; trivial save/restore; hyperbolic transfers need no separate code path | **Accepted** (Spike 2) | 2026-06-14 |
| PH-4 | ~~Use Chaos for active rigid bodies; custom integrator for orbits~~ **REVISED → HYBRID (RC-4 call): custom fixed-step symplectic integrator owns the active vessel's flight dynamics (gravity/thrust/drag/joints); Chaos retained ONLY for collision detection + contact response.** Fallback: fully-custom analytic ground contact (Chaos-free) if touchdown contact is unstable. | CE-6 removes the velocity problem but not the integration problem; UE5 LWC jitters ≥3 km so Chaos is only trustworthy near origin and would accumulate error on 600 km gravity; closed-form-consistent integrator preserves the on-rails no-drift guarantee + Phase-3 determinism; matches core-engine's predicted "partial-pass" path. | **Accepted** (Spike 2, RC-4) | 2026-06-14 |
| PH-5 | Terrain collision = world-gen's **analytic** queries (`GetGroundContact`/`RaycastTerrain`/`SampleTerrainHeight`), NOT baked per-quad meshes; baked-mesh emission deferred (Phase-1 perf option, non-breaking). | Resident-free (need ground before LOD streams), floating-origin-clean, deterministic, point-local contact. | **Accepted (Spike 2, RC-3); SUPERSEDED for the character near-field** (2026-07-05, Admin D-010): M4.3/M5.2 cook complex-as-simple PMC collision on the chunk + voxel meshes and the character physically stands on it (the float/teleport bugs died the day the capsule got real geometry). The **vessel path remains analytic** as designed; this reversal is the character near-field only. | 2026-06-14 |
| PH-7 | **The walker requires BOTH oracle answers to agree before it treats a point as rock.** `solidAt` is quantised to the 1 m cell (a cell is solid when its CENTRE is at or below the designed surface, `surface_field.h` §5) while the walkable ground is the smooth `surfaceRadius`. A point ABOVE its own column's `surfaceRadius` is therefore air for the capsule, whatever the cell says. | The two answers are the same authority (R1 holds, nothing new is derived) but they are not the same SHAPE: the solid shell is a staircase that stands up to ~0.87 m proud of the ground being walked on. Measured on ordinary terrain: the air 0.15 m above the walkable surface reads solid on **60.6% of ticks**. `resolveEmbedded` ejected the capsule along the minimum translation, which out of a just-entered cell is back through the face just crossed, i.e. exactly the 7.7 cm the player had walked. Walking returned **0.44 m per commanded metre**. Fix restores 1.00. Below the surface nothing changes, so the dig mouth (15.2 item 48) and tunnel interiors are untouched. | **Accepted** | 2026-07-26 |
| PH-9 | **`floorBelow` may never return a radius ABOVE its query.** If the query point is itself solid the capsule is embedded, which is `resolveEmbedded`'s problem, not the floor march's. | The march returned `r + 0.1` at d = 0, which is not a floor but one march step of pure lift, and the caller reads any ground above the feet as a landing. Held against a vertical face it ratchets **0.1 m every tick, 6 m/s straight up**: that is how the walker climbed 12 m out of a 10.4 m shaft at a dead-constant 0.097 m/tick (0.1 less one tick of gravity). It reads as a walkable ramp in a screenshot and as a hard-coded constant in a trace. The capsule's `free` test samples 0.15/0.9/1.65 m and nothing at the feet, so a position with the feet a few cm inside rock passes and feeds the march. | **Accepted** | 2026-07-26 |
| PH-8 | **`CAPSULE.stepUpM` = 1.1 m is the heightfield's step-up AND its wall**: a horizontal move onto ground more than that above where the feet started the tick is refused, sliding along one tangent axis first. The radial half of the move is exempt, so a jump still reaches higher ledges. 1.1 m is sized to the 1 m quantum the derived lowering moves in, so a scrape you dig is not a trap, and it matches the ledge the voxel resolver already climbs. It never fires on ordinary ground: real relief there is 0.034 m per tick. 0.6 m was tried first and cost `tunnelwalk.js` 12 of its 22 dig strikes, because one strike digs a crater deeper than 0.6 m and the player could no longer step out of their own. | The heightfield path had NO wall: `gap <= 0` meant "landing", so one tick's 7.7 cm of travel into a cliff snapped the capsule to the top of it. Measured: the walker climbed **12 m out of a 10.4 m shaft it had just dug**. `slopeLimitCos` cannot catch this and never could, for two reasons: it is sampled AFTER the snap (so it reads the flat ground on top of the cliff) and its rejection projects the tangential velocity onto the RADIAL up, which is orthogonal to it by construction, so it removed ~6e-7 of a 4.6 m/s velocity. It has been a no-op since it was written. Left in place as the jump gate and as the reported `slopeCos`; the wall is now `climbGate`. | **Accepted** | 2026-07-26 |
| PH-10 | **The atmosphere is ONE exponential, `rho = rho0 * exp(-h/H)`, with Forge at rho0 = 1.225 kg/m^3 and H = 5600 m, ceiling 60 km.** Isothermal, so `p/p0 == rho/rho0` and one curve serves both drag and engine lapse. Cinder is airless. | H is not invented: D-006 fixes Forge's scale height at ~5.6 km and the rendering lane's scattering model already ships `rayleighScaleM: 5.6e3` / `thicknessM: 6.0e4`. Choosing a second scale height would have made the air you fly through disagree with the air you look at, which is the five-surfaces failure wearing a different hat. This is also DW-30's "kept real" list item: density genuinely falls off, so drag and Isp genuinely change as you climb. | **Accepted** | 2026-07-26 |
| PH-11 | **Density FADES to exactly zero over the top 10 km (50 to 60 km) rather than being truncated at the ceiling.** | The raw exponential at 60 km is **2.723e-5 kg/m^3**, which is **0.072 m/s^2** on a 2 t vessel at orbital speed. A hard cut therefore puts a step in the force at exactly the altitude every player circularises at: an orbit 100 m below the line decays and one 100 m above it is eternal. The fade buys three things and costs one: density is continuous everywhere (worst measured step **3.7e-8 kg/m^3 per metre**), it is EXACTLY 0.0 above the ceiling so "am I on rails" is a boolean rather than a threshold, and the raw exponential is bit-identical below 50 km where the whole ascent drag integral lives. The cost is slightly less drag in a band the vehicle crosses in about 12 seconds. | **Accepted** | 2026-07-26 |
| PH-12 | **DW-30 item 1 implemented as: DESTABILISING pitching moment is multiplied by `AeroTuning::unstableAeroGain`, which ships at 0.0. RESTORING moment is untouched.** The branch is the sign of the static margin `(CoP - CoM) . forward`. | Three properties, all measured. **(1) It is continuous**: the moment is proportional to the static margin, so it is exactly zero where the gain switches (swept the fins through the CoM in 400 steps, worst torque jump under 6 kN·m, torque at the crossing under 6 kN·m from both sides). **(2) Fins still do real work**, because a finned vehicle is on the restoring branch and gets the full physical moment. **(3) At 0.0 the claim is a theorem, not a race between two coefficients**: rate damping alone CANNOT hold the ANGLE of a statically unstable body, it bounds the rate and lets the angle grow linearly, so the only honest ways to a bounded angle are to remove the divergence or to fly a controller, and DW-30 asks for the former. Wind tunnel, 10 deg AoA at 36.7 kPa, unstable airframe (margin +3.188 m): **gain 1.0 tumbles past 90 deg in 1.34 s and reaches 180; gain 0.0 peaks at 21.66 deg, settles there with 0.0000 deg/s left; gain 0.0 FINNED peaks at 10.00 and weathercocks back to 0.00.** | **Accepted** | 2026-07-26 |
| PH-13 | **Aerodynamic rate damping is a real derivative, generously sized**: `M = -k * 0.5 * rho * v * CdA_normal * L^2 * omega_perp`, k = 0.25 pitch / 0.05 roll. | Real vehicles have pitch damping (a rotating body meets its own rotation as a distributed angle of attack along its length) and KSP models it thinly. It scales with rho so it does nothing in space, which is correct and is what makes a coast feel different from an ascent. It is what settles a gust rather than letting the vehicle ring. | **Accepted** | 2026-07-26 |
| PH-14 | **The centre of pressure is weighted by NORMAL-FORCE SLOPE (Barrowman), not by broadside drag area.** A nose gets `2 x` its frontal area, a constant-diameter body tube gets `0.15 x` its side profile, an aerodynamic surface gets `2.5 x` its planform. | Broadside area was tried first and is wrong in a way that matters: a 12 m tube of 16 m^2 of flank drowns out 3.7 m^2 of fin, so **four fins moved the CoP 0.87 m and left the reference rocket statically unstable**. With the correct coefficient the same four fins move it **4.117 m** and the margin flips from **+3.188 m to -0.884 m**. Broadside area answers "how hard does the air push a vehicle flying sideways"; normal-force slope answers "where does the pushing start when it is barely off axis". They are different distributions and a rocket needs both. | **Accepted** | 2026-07-26 |
| PH-15 | **Axial drag is NOT a sum over parts.** `CdA_axial = Cd(nose) * A_max + 0.004 * wetted area + unshaded radial parts`. The BROADSIDE sum stays a sum. | Parts in a stack SHADE one another: air meets the nose and then flows along the tube. Summed, the 12 m reference rocket had `Cd*A = 3.274 m^2` on a 1.227 m^2 disc, an **effective Cd of 2.67, roughly a parachute**, spending **64 kN of drag at max-q against 180 kN of thrust**. It still reached orbit, which is exactly why it needed a number rather than a look. Now **0.887 m^2, effective Cd 0.72**. Broadside is left as a sum and that is not inconsistent: flying sideways, every part is in the flow. Consequence worth keeping: a nose cone (Cd 0.12) against a bare capsule (0.55) cuts total drag by about 60%, so the part earns its place. | **Accepted** | 2026-07-26 |
| PH-16 | **The `flight.h` / `orbital.h` boundary is a BIT-EXACT reduction, not a tolerance.** Above the atmosphere ceiling with the engine shut, thrust and drag are exactly zero and `FlightSim::step` is written in the same term order as `orbital::Integrator::step`, so the two produce identical bits. `onRailsEligible()` is the published predicate. | A tolerance would let the two paths drift apart silently and would make "which one is right" unanswerable. They really are the same arithmetic, so the test asserts that. Measured: 50,000 steps of 20 ms, all six components identical. Separately, one full 2042.6 s revolution integrated at 20 ms lands within **0.62 m and 0.0003 m/s** of the conic `orbital::resume` predicts, which is the integrator's own truncation error and not a disagreement about physics. | **Accepted** | 2026-07-26 |
| PH-17 | **Staging: ONE integer per part (`PartInstance::stage`, the stage GROUP it burns in), and the KSP press sequence** (stage k drops the hardware of burn k-1 and lights burn k). `fireStage` severs the tree at each named decoupler; the decoupler and its whole subtree leave, and the decoupler's mass goes with them. | "Which stage decouples me" was tried first and is off by one from "which stage do I burn in", which broke the delta-v filter. A stage that ignited and decoupled in the same press can never be burned. One integer with one meaning makes a per-stage delta-v sum a filter over it, and stating the split rule in terms of the TREE rather than in terms of up and down means a radial decoupler holding a strap-on booster needs no special case. Mass conservation is asserted for both the stack and the two-booster case. | **Accepted** | 2026-07-26 |
| PH-19 | **The part catalogue's names and dimensions are the ART LANE's, not physics'.** `PartDef::asset` is the exact glb node name and every geometric figure is read off `tools/blender/build_rocket_parts.py` / `rocket_common.py`. Two stack classes: S = 1.25 m, L = 2.50 m, exactly 2x, `StackAdapter` between them. | Written mid-task after the art lane landed an expanded `rocket_parts.glb` under different names while this work was in flight. Publishing a taxonomy that disagrees with the shipped meshes is the two-authority failure this project has paid for five times, and a renderer looking a part up by name would simply not find it. Their reading of DW-29's "engines in two sizes" (two DIAMETERS plus an adapter) is also better than the draft's (two performance tiers on one diameter), so this is not merely a rename. A ctest asserts every stack part's diameter is EXACTLY one of the two classes (14 class S, 4 class L), because 1.24 would mate visually and leave a seam. | **Accepted** | 2026-07-26 |
| PH-18 | **`atmo::kG0 = 9.80665` is a UNIT CONVERSION and is not a gravity.** DW-18's `BodyParams::muM3S2` remains the one gravity authority; nothing in `vessel.h`/`flight.h` derives a g from anything else. | Forge's surface gravity is 9.81 m/s^2 and the Isp reference constant is 9.80665, so the two look almost identical and would be easy to conflate: that is precisely why it is written down. A rocket flown to Cinder must burn at the same Isp it burned at on the pad. The reference vehicle's pad TWR (1.6567) uses `mu/(R+h)^2` for weight and never touches kG0, and the two constants differ by 0.03% in that number, which makes it a real discriminator in a test rather than a nicety. | **Accepted** | 2026-07-26 |
| PH-20 | **Ground contact is solved at the PAD end only, with a LAUNCH CLAMP, and the landing end is DEFERRED and named.** The clamp is client-side (`web/src/sim/FlightSession.ts`), needs no `/core` change and no ABI bump: it STEPS the sim and writes the pad pose back, releasing when the throttle is up AND the measured TWR is at or above 1. Coming down, `DOWN` is an ARREST: the state freezes at the surface with no gear compression, no friction, no tip-over and no impact tolerance. | R5 said the vessel core cannot sit on anything, and the honest split is that a hold-down clamp is a fully solvable problem and a touchdown is not. **The step-and-restore shape is not a detail, it is the bug**: `FlightSim::telemetry` is written by `step` and by nothing else, so the first version, which simply did not step while clamped, read zero thrust and zero mass, hence a TWR of zero, hence refused to release, forever, while every number on the HUD looked healthy. Stepping and restoring is also what a hold-down physically does, and with the throttle shut it is free because a step with no thrust and no motion burns no propellant. Landing legs stay data; half-building contact here would have produced a landing that looks right and is not, which is this project's most expensive failure class. | **Accepted** | 2026-07-27 |
| PH-21 | **Flight uses the FOUR-PASS scaled-space rig unchanged. There is no third render mode and `Frame.vabActive` stays a boolean.** The vessel draws into `scenes.near` at floating-origin engine metres. | The bay REPLACES the four passes because a rocket on a stand and a planet at 600 km share no depth range. Flight is the opposite case and the whole point of the milestone: the vessel has to be in the same 1:1 near scene the walker is, so the rig carries it from the pad to orbit continuously. A flight pass would have been a second seam to maintain and would have deleted the feature it was meant to serve. **Measured: 37 draw calls, 107,680 triangles, frame p50 2.6 ms / p95 4.0 ms from orbit, with 0 near chunks and 102 far ones.** | **Accepted** | 2026-07-27 |
| PH-22 | **The VESSEL BECOMES THE STREAMING OBSERVER, through a `ViewSource` router (`web/src/player/ViewRouter.ts`), not through a branch in the loop.** `VesselObserver` is a chase camera implementing the same contract the walking capsule does; boarding swaps the one object `Loop` already talks to. | `Loop` owns ORDER and must never branch on player mode. Making the rocket the observer is what makes the near-to-far handoff need NO flight-specific code at all: the origin rebases around the vessel, chunks stream to it, the regime crosses off its altitude, the sky and the shadow fit follow. Anything else would have been a second definition of "where the player is", which is the failure this project has paid for five times. | **Accepted** | 2026-07-27 |
| PH-23 | **The player commands an ATTITUDE in the LOCAL HORIZON FRAME, at 20 deg/s, and stability assist holds it.** `FlightAttitude.slewCommand`: pitch and yaw move a commanded direction in the same heading/pitch frame the navball draws in, and `/core`'s `SasMode::Command` tracks it. | Two measured reasons. **(1) Frame:** with the keys moving the nose in the vessel's own BODY frame, the ascent tracked the guidance ribbon's PITCH to within three degrees the whole way up while flying to heading 270 against a ribbon asking for 90, because pitching about the vessel's right axis moved the nose south. In the horizon frame the two keys ARE the two numbers on the ball. **(2) Rate:** 45 deg/s overshot the ribbon by 20 degrees a side, because a slew rate the torque cannot follow is not generous authority, it is a marker that lies about where the nose is going. 20 deg/s tracks it. | **Accepted** | 2026-07-27 |
| PH-24 | **The local horizon frame's `east` is `player/ViewSource.tangentFrame`'s (`Y x up`), copied and not re-derived, and stability assist holds ROLL as well as pitch and yaw.** | Two sign bugs of the same shape, both of which read perfectly healthy. `up x Y` makes the triad (east, north, up) LEFT-handed; every angle READOUT survives that unharmed because heading and pitch are computed in the same broken frame, and only a ROTATION exposes it, which is why the ascent flew 180 degrees off its own ribbon while every number agreed. Roll is the same lesson: `/core`'s SAS points the nose and has no opinion about roll, and the only thing damping roll in `flight.h` is an aerodynamic derivative that does nothing in vacuum, so the first orbit arrived at **ROL -93 degrees with no roll input ever given** and the navball horizon was vertical. The damper's first sign converged just as smoothly on the OTHER fixed point, **ROL 179.998**. | **Accepted** | 2026-07-27 |
| PH-25 | **The renderer draws the vessel at the INTERPOLATED instant the camera was placed for (`VesselObserver.renderPos`), from `onPreRender` and not from `onDrain`.** | The camera aims at the lerp between the last two fixed ticks; the sim's own position is the END of that lerp. A renderer reading the raw position puts the model up to a whole tick of travel from where the camera is looking, which is **38 m at 2.3 km/s against a 21 m chase distance**, i.e. the rocket entirely out of frame. On the pad the two agree exactly, so it looks perfect until it moves, and it looks like a camera bug rather than a timing one. | **Accepted** | 2026-07-27 |
| PH-26 | **PHYSICS warp, not on-rails warp: `of_fl_step_n(f, dt, n)` takes n steps of the SAME dt.** Capped at 10x in the air and 1000x in vacuum. | Warping therefore costs the integrator exactly nothing, which is why the in-air cap is the LOWER one: what it costs is CONTROL LATENCY, the stick being sampled once per block. This is not KSP's time warp, which teleports along a conic and cannot be used under thrust, so it does not touch DW-30's concession list. | **Accepted** | 2026-07-27 |
| PH-27 | **`of_vs_*` and `of_fl_*` are SEPARATE handle registries that both number from 1**, so a flight handle passed to a vessel call silently answers about the DESIGN. The per-stage delta-v table is read from the design handle by name (`FlightReport.readStagePerformance`); propellant aboard is summed off the live craft's own parts. | Found because the stage table agreed perfectly on the pad: the bay's design and the flight both held handle 1. It would have diverged silently at the first stage separation, which is the worst possible moment for a delta-v readout to start describing a vehicle that no longer exists. | **Accepted** | 2026-07-27 |
| PH-28 | **`FlightSession.altitudeAglM` SUBTRACTS the base offset. It was adding it.** | `rollOut` puts the vessel's ORIGIN at `surfaceRadius + baseOffsetM`, because the origin is the TOP of the stack and the base has to land on the ground; the base is therefore `baseOffsetM` BELOW the origin and the offset subtracts. Adding it made a rocket **standing still on the pad read ALT AGL 19.20 m, exactly twice its own 9.6 m base offset**, and armed the touchdown arrest only once the hull was **buried by the same 19.2 m**. It is the signature failure of this codebase in one operator: the SAME wrong number fed the navball, the guidance ribbon's altitude input, the regime band and the arrest trigger, so every instrument agreed with every other. Nothing caught it because every existing assertion is RELATIVE (`liftoffAltM` is seeded from the same function; `ascent.js` asserted `aglAfter > aglOnPad + 5`) and **no test ever asserted that a rocket on the ground is at zero.** `flightabuse.js` now does. | **Accepted** | 2026-07-27 |
| PH-29 | **On the pad, ONE stage press. A second while the clamp still holds is REFUSED and says so.** | The first press lights the engine, exactly as in KSP. The second throws away the booster, and what is left cannot reach TWR 1, so **the clamp never releases and there is no route back to a whole rocket from inside the cockpit**: the player is bolted to the ground in a vehicle that cannot fly and can only escape by climbing out, walking 200 m and pressing G. Measured before the guard: four presses took the reference vehicle from **11 parts to 4**. This is the single most likely wrong thing a player does in the first thirty seconds, because Space is the stage key and Space is also the jump key they have been holding all game. | **Accepted** | 2026-07-27 |
| PH-30 | **A SAVE THE WORLD CANNOT DESCRIBE IS NOT WRITTEN.** While the player is aboard, `Persist.saveSlot` refuses, counts the refusal, and the navball carries a standing amber chip saying "flight is not saved: reloading returns you to the last ground save". The latch is `web/src/sim/SaveInhibit.ts`. | R11. The slot has **no field for a vessel** and the autosave fires every 20 s from above `Gameplay.fixedStep`'s `uiOpen \|\| suspended` return, so it fires while strapped in. A slot written in orbit is a **perfectly valid GROUND state**: reload it and the base, the pack and the tunnels all come back, the player is at the scenario spawn, and the rocket they were flying never existed. Every number in that save is correct and nothing says the flight was dropped, which is the same instrument-clean wrongness as PH-28. Refusing leaves the last GROUND save on disk, which is exactly where a reload should put somebody whose flight was not saved, and the chip tells them BEFORE they reload rather than after. **Not inhibited: a vessel PARKED on the ground.** That save describes the player and the base correctly and loses only a hull that costs one key press to put back, because the DESIGN is persisted separately (`VabStore`, localStorage); inhibiting there would mean a player who rolls out and then lays track for ten minutes never autosaves again. **This is the honest fallback, not the fix**: see R13 for what real persistence needs. | **Accepted** | 2026-07-27 |
| PH-31 | **The vessel's render position is seeded from the sim on ROLL-OUT and kept synced every frame it is parked (`VesselObserver.syncToVessel`).** | `renderPos` was written only by `interpolate()`, which is reached only through `ViewRouter`, which routes to the vessel only while somebody is ABOARD. So between roll-out and boarding it was still the `{0,0,0}` it was constructed with and `FlightMode.frame` drew the entire rocket at the **BODY CENTRE, 600 km under the pad and outside the near camera's far plane**. The vessel is planted at 26 m and boarding reaches 18 m, so **there is ALWAYS a walk during which the rocket the game has just told you to walk to is not on screen**, and it appears out of nowhere at the instant you board. Invisible to every instrument because `distanceToVessel` reads `session.state.pos`, which was right the whole time: the walk, the range refusal and the boarding all measured correctly against a rocket nothing was drawing. `VesselView.report().drawnEngineM` now publishes where the meshes actually are, so this is a number and not a screenshot somebody has to notice. Same call is the reset a SECOND flight needed: without it the first tick aboard lerps camera and model from the previous landing site to the new pad in one frame. | **Accepted** | 2026-07-27 |
| PH-32 | **Boarding range is measured to the vessel's BASE, not to its origin.** | The origin is the top of the stack, so measuring to it adds the whole rocket's height to the distance of a player standing at its feet. With an 18 m range that means **any vehicle taller than 18 m can never be boarded**: G answers "vessel is 24 m away" from the base, walking closer cannot help, and past 200 m it rolls out a second one on top of the first. The reference fixture has 9.6 m of offset so every probe passes; **the first player who adds a tank hits a wall with no diagnostic.** `frameFor`/`midOffsetM` already knew about this offset in the camera and the range check did not, which is the two-authority shape again. | **Accepted** | 2026-07-27 |
| PH-33 | **Only the ACTIVE stage's engines draw a plume (`nextStageIndex - 1`), and the plume is NOT gated on the clamp.** | The filter was the catalogue's thrust figure alone, i.e. "is this part an engine", so **every engine on the stack burned and the upper stage flamed inside the interstage from the moment the clamp released.** It reads healthy (the plume count matches the engine count) and it becomes accidentally correct at separation when the spent parts leave the tree, so it is wrong only in the window between liftoff and staging, which is the window every launch screenshot is taken in. Dropping the clamp gate is the other half: an engine lit under a hold-down IS burning propellant, and drawing no flame there was the one moment the picture disagreed with the propellant gauge. | **Accepted** | 2026-07-27 |
| PH-34 | **A warped block may not cover more than a TENTH of the height the vehicle has left when it is closing on the ground (`web/src/sim/FlightWarp.ts`).** | The in-air cap of 10x was gated on `this.tm.inSpace`, which is the PREVIOUS tick's telemetry. Climbing, the lag is harmless. Descending it is not: the block that crosses the atmosphere ceiling still runs at up to 1000x on the strength of a sample taken in space, which at 60 km and 2 km/s is **16.7 seconds and about 33 km of re-entry taken in one blind step**, with no telemetry read and no control sample inside it. The vehicle jumps from space to deep atmosphere or straight through the ground (the arrest then snaps it back up), and `maxQPa` misses the real peak entirely because it is sampled once per block rather than once per step. Bounding by the height REMAINING fixes it **without keeping a second copy of the ceiling altitude in the client**, which would be one more place for the air the vehicle flies through to disagree with the air it is drawn in. Costs nothing climbing, because a climbing vehicle is not closing on anything. | **Accepted** | 2026-07-27 |
| PH-35 | **Both flight message clocks are the LOOP's clock, and both expire.** | Two opposite bugs in one line of readout, which is why neither showed. `FlightSession.flash` set its deadline on the MISSION clock (`metS`, -1 on the pad) and `tick` compared it against the LOOP clock (seconds since boot, in the hundreds by the time a rocket is built), so **every message the session ever raised was cleared in the same frame it was raised**: "clamp holding: TWR 0.00, throttle up", "clamp released", "staged, 2400 kg away", "no stage left" have never once been on screen. `FlightMode.message` had no expiry at all, so the last thing it said stayed on the navball forever, which is what made the pair look like a working message line. PH-29's whole recovery affordance is a message, so this had to be fixed with it. | **Accepted** | 2026-07-27 |
| PH-36 | **The flight entrance NAMES ITSELF as a stand-in**: roll-out flashes "ROLL-OUT (stand-in for DW-29's launch pad): rocket 26 m ahead, walk to it and press G". | R12. A stand-in that announces itself is a stand-in; one that does not is a broken feature, and the entire difference is whether it is named. DW-29's real entrance is a pad gated behind ground progression which does not exist at any layer, and the sequencing is still Admin's call, so the honest move tonight is to be loud rather than to fake a gate. | **Accepted** | 2026-07-27 |
| PH-37 | **A MANEUVER NODE IS A PLAN AND NOT AN AUTOPILOT, and the line is drawn at ARITHMETIC vs DECISIONS.** `maneuver.h` computes what a burn costs, which way to point, when to light it, how long for, and the orbit it produces. Nothing in it, or in `MapMode.ts`, sets a throttle, starts a burn, warps to a node or cuts an engine. | DW-29 gates autopilot behind a research unlock earned by reaching orbit MANUALLY, and DW-30's enumerated greasing list keeps auto-circularising on the far side of that gate because it removes a DECISION. A node is on the near side and the test DW-30 states says why: it removes arithmetic a human should not be doing by hand, which is a frustration, and leaves every decision intact. The player still points the ship, still starts the burn at the right moment, and still stops it. **Planning a burn and executing it are different skills and only the second one is the game.** The one thing the map commands is hold-node, which is the SAS the player already had (DW-30 item 2) aimed at a direction they chose. | **Accepted** | 2026-07-27 |
| PH-38 | **The burn's DURATION is published and the burn is LED BY HALF OF IT**, as `Plan::burn.leadS` and `timeToBurnStartS`. The map's most prominent number is "light it in", not "node in". | An impulsive plan executed as a finite burn spends its second half somewhere the plan never was. R6 already recorded what that costs: starting a circularisation burn AT apoapsis instead of leading it by half caps the vehicle at **121 x 33 km**. Half the duration is the first-order correction, and "burn for 42 s" and "start burning 21 s early" are the same fact where only the second is actionable. **Measured, driven in `/core` at dt 0.002 with everything else identical**: an 800 m/s / 44.2 s burn from an 80 km circular orbit, LED, lands its periapsis **0.8 m** from the prediction; the SAME burn started at the node lands it **302 m** low, 377x worse, and its apoapsis 1.95% low against the led case's 0.61%. `test_maneuver.cpp` asserts the comparison, not a threshold. **`burnFractionOfPeriod` is published with it**, because the impulsive approximation is only as good as that is small (0.0026 of a period lands 19 m out; 0.023 lands 39.5 km out) and a caller that shows a predicted orbit without showing its own error is choosing not to say. | **Accepted** | 2026-07-27 |
| PH-39 | **ONE derivation of the prograde/normal/radial triad, `orbital::basisAt`, and it lives in `orbital.h` because TWO things need it.** SAS points at those directions (`sasTarget`) and a node's handles are expressed in them (`maneuver::deltaVVector`). `SasMode` gains `Normal(5)`, `Antinormal(6)`, `RadialIn(7)`, `RadialOut(8)`, **appended**, so 0..4 still mean what the bridge, `FlightAbi.ts` and every shipped probe think. **There is deliberately no `Node` mode.** | Two constructions of "which way is normal" is the two-authority failure this project has paid for six times, and here it would show up as a node marker that is not quite where the SAS holding it points, which nothing would catch because both look plausible. A `Node` mode is unnecessary rather than merely redundant: a node's burn direction is FIXED in inertial space once the node is placed, so `Command` plus `FlightSession.commandDirection` already IS hold-node, and adding a fifth way to say "point there" would put a maneuver concept inside the attitude controller. The node's basis is evaluated **at the node**, never at the ship: on an ellipse, prograde half an orbit away is very nearly reversed, and `test_maneuver.cpp` asserts that burning prograde at apoapsis raises the PERIAPSIS, which is an assertion with a wrong answer to fail against. | **Accepted** | 2026-07-27 |
| PH-40 | **Inclination is computed against the WORLD's +Y pole in `maneuver::samplePath`, and is deliberately NOT `orbital::Elements::i`.** | `orbital.h` takes the inclination from `h.z` and the node line from `cross({0,0,1}, h)`, i.e. **+Z is its pole**, while the planet's pole is **+Y**: `cubed_sphere.h`'s `dirToLatLon` reads `dir.y` for latitude and `flight.h` spins the atmosphere about +Y. The element set is internally consistent, so `state -> elements -> state` round-trips exactly and nothing that USES it is wrong; but its `i` is an angle to a plane this planet does not have. A test pins the discriminator: the demo orbit lies in the XZ plane, so its inclination against the real pole is 0 and `Elements::i` reads **90 degrees**. Printing that beside a latitude would have been a number that looks right and is not. | **Accepted** | 2026-07-27 |
| PH-41 | **The map is a 2D ORTHOGRAPHIC plan view on its own canvas, looking down the orbit normal. It is a MODE, not a fifth render pass, so `Frame.vabActive` stays a boolean.** A 3D orbital camera is DEFERRED and named. | The projection is exact rather than a simplification: patched conics (D-002) put the whole trajectory in one plane and a sphere projects to a circle, so nothing about the body or the current orbit is distorted, and a trajectory in a DIFFERENT plane (what a normal burn makes) is honestly foreshortened. It also answers `FlightMode.ts:7`'s standing "there is no third render mode" argument by not raising it: `vabActive` would have had to become an enum, touching `Frame.ts`, `Scenes.ts` and `CameraRig.ts` for a picture that a canvas draws better. **The atmosphere annulus is drawn because "is my periapsis inside the air" is the question the player is actually asking**, and `atmosphereCeilingM` is read from `of_atmo_space_altitude` so the line the map draws the air at is the air the vessel flies through. | **Accepted** | 2026-07-27 |
| PH-42 | **The flight camera: the mouse orbits, the wheel zooms between bounds derived from the LIVE vessel's length, and `V` is a pod-mounted COCKPIT whose horizon rolls with the hull. In the cockpit the hull is NOT DRAWN.** | `VesselObserver.look()` was published, clamped, correct and **had no caller for the whole milestone**: `step(_inp, dt)` took the input frame and ignored it, underscore and all. That is the fourth built-but-unreachable capability found in a day and it is the same shape as `board` not being in `UI_ALLOWED`. The cockpit being hull-locked is NOT a reversal of the chase camera's decision: a CHASE view welded to a rolling hull leaves a player unable to tell which way is up, and sitting inside one the hull IS the frame of reference. KSP draws the same distinction. **Not drawing the hull was found by screenshotting**: the eye sits 1.2 m below the top of the stack and looking down at the planet put a black wedge of the vessel's own interior across two thirds of the frame. There is no IVA geometry (art lane A-11), so an empty cockpit is honest and a hull seen from the inside out is not. Zoom bounds are multiples of the live length re-measured on every staging, because a 12 m rocket and a 40 m one want different close-ups. | **Accepted** | 2026-07-27 |
| PH-43 | **`mute` moved off `KeyM`; the map has it.** `mute: ['Backslash']` is a holding pen and says so in the file. | M is the map in KSP and Reid asked for it by name. `Bindings.ts` permits a code firing two actions **only when one of the two consumers is ever live**, and `mute` breaks that precondition: it is read in `GameplayInput.chrome`, which runs ABOVE `Gameplay.fixedStep`'s `suspended` return and is therefore live in a rocket. Left alone, one press of M would open the map AND silence the game, which is the silent-second-effect class this project keeps paying for. Three other repairs were tried on paper and rejected: gating `mute` needs `web/src/game/`, which a live lane owns; holding the map's UI capture through the key release fails because `setUiCapture` calls `down.clear()` and keyboard auto-repeat re-arms the key; and leaving the collision in place is the defect. **Follow-up owed by whoever owns `GameplayInput.ts`: the HUD still flashes "sound off  (M)" at `GameplayInput.ts:63`.** | **Accepted** | 2026-07-27 |
| PH-44 | **A NODE IS ANCHORED TO AN ABSOLUTE MISSION TIME, never to "seconds from now."** Plus two readout defects the same driven probe found: the navball's CMD marker drew `state.forward` (the nose) rather than where SAS is aiming, and a pure ROLL input dropped the vessel out of PRO into CMD. | **The node one is fatal and it looked perfect on every instrument.** `MapMode` stored `tFromNowS` and handed that same number to `/core` every frame, so the node slid along the orbit WITH the vessel and stayed a fixed distance ahead for ever: measured, **40.0 s of mission time elapsed with the countdown bit-identical at 1004.2540207730244 s and the panel's "light it in" frozen at 16:44.** Every number was self-consistent, the conic was right, the burn was right, and the burn could never be flown because the countdown could not reach zero. It is now `atS`, an absolute point on the vessel's own clock, and `tFromNowS` is derived; it is allowed to go NEGATIVE, because "you missed it" is a true readout and a clamp at zero would be a second lie. **The CMD marker one is the same shape in miniature:** a marker pinned to the reticle centre cannot disagree with the nose, so it can never show a command the vessel has not reached, and it was measured **133.7 degrees off** the instant hold-node engaged while still drawing on the crosshair. `FlightSession.commandDir` publishes the real one. **The roll one:** `slew` routed any non-zero input through `commandDirection`, so pressing roll while in PRO silently changed the mode. All three were found by `probes/maneuver.js` and none by reading. | **Accepted** | 2026-07-27 |
| PH-45 | **THE REPEAT SINKING DEFECT DOES NOT REPRODUCE IN THE WALKER, AND THE HEIGHTFIELD-RE-ASSERT HYPOTHESIS IS REFUTED BY MEASUREMENT.** Admin's model was that something periodically re-asserts the HEIGHTFIELD as the ground authority under a mountain, snapping the player up toward the outer surface. Driven at a measured 42.7 to 45.0 degree hillside on the shipped seed, with a 26-strike bore giving **17.2 to 19.0 m of rock overhead**: **standing spread 0.000000 m over 2400 ticks; 0.000000 m at all 33 stops walked along the whole bore, from 19 m of overhead out through the mouth and back; 0.000000 m over 3000 ticks after a REAL browser reload with 267 removed cells restored from IndexedDB; 0.000000 m at 60 Hz and at 144.3 Hz render; 0 snap-ups, 0 pushes, 0 ratifications and 0 underRock flips everywhere.** Beyond the feet: 480 per-frame samples of the eye, the floating origin, the DRAWN chunk-sheet height under the feet, chunksBuilt, poolInUse, restitched, near/far/pending/fading/hidden and the voxel mesh triangle count were **flat, zero changes, over 24 s**, so the map lane's 60 Hz rebuild storm is not present here; and an 8x6 frame hash held the **40 ground tiles pixel-identical for 20 s** while only the top row (sky and view model) moved. Third person adds nothing: `springArm` marches against `surfaceRadius` alone, so 19 m under it the arm is pinned at 0 and the TP camera IS the FP eye, measured at 0.000000 m of spread with the mouse sweeping. **The mechanism reason the hypothesis cannot be right as stated:** `editsHandle` is written once in `VoxelWorld`'s constructor and cleared only in `dispose`, so the `deep` gate cannot flicker; and under a mountain `qr` sits 15 to 19 m below its own 1.5 m threshold, so the heightfield branch is not merely losing, it is never entered. | I have been wrong about causes on this project repeatedly and measurement has corrected me every time; this is that, applied to somebody else's hypothesis and then to my own. The value is the negative result plus **where it says not to look**: this rules out the capsule, the first-person eye, the third-person camera, the drawn sheet, the chunk stream and the reload path, at a site with ten times the spawn shaft's overhead rock. It does NOT rule out a session with player-built structures (`solids.count` was 0 in every run, so `KinematicBody.step`'s structural block never executed) or the real rAF loop at a real framerate. See R19 for the ordered list of what is left. | **Accepted** | 2026-07-27 |
| PH-46 | **`probes/tunnelsink.js` NOW RUNS ITS WHOLE SCENE TWICE, at the spawn and on a measured mountainside, and THE SITE ITSELF IS AN ASSERTION.** The mountain is found by sweeping `of.surface` for the greatest rise 30 m away on the best of eight bearings, the facing is read off the engine's own aim ray, and `overheadRockM` is reported at both sites. The 21st check is that the second site really does put more rock over the player's head than the first, bounded by **the spawn's own number rather than a typed one**. | R8: every geometric probe in this repo only ever ran on the flat spawn clearing, and the belt lane hit the same class on the same day (a 10.1 degree corner error that was EXACTLY ZERO on flat ground across 39 driven keypresses and only appeared on a 14.82 degree hillside). Without the site assertion the second run can silently be the first run again, and the probe would report two greens for one measurement. **The negative control is what makes the new site mean something (standing rule 11):** re-run with the discarded WG-31 floor query (`oldFloor` in the eval args) and the mountain site fails five checks with **spread 0.249142 m, 7 snap-ups at a mean period of 93 ticks, sinking 0.002685 m every tick against one tick of gravity of 0.002685 m, ratified on 593 of 600 ticks**, which is Reid's sentence with a period on it; while the spawn site on the same build produces **zero snap-ups** and only a pure 0.26 m sink. So the mountain site is a strictly harder case than the spawn and the assertions bite there first. On current main both sites read 0.000000 and all 21 checks pass. | **Accepted** | 2026-07-27 |
| PH-47 | **R19 LEAD 1 MEASURED: THE STRUCTURAL PATH IS FLAT TOO.** Reid's HUD reads `structures 140/512`, and every run behind PH-45 had `solids.count == 0`, so `KinematicBody.step`'s structural block (lines 278 to 300) had never executed once. It now has, with **90 player-built parts** placed by the probe (0 refusals, 0 uneven-ground refusals), at every place a player in that world stands: **enclosed by a foundation laid on the cell under the feet** (the exact state GP-53 measured its 0.198989 m sawtooth at a 74.3 tick period in), **on a deck**, **on open terrain beside the base**, and **in a tunnel dug next to it**. Every one: **0.000000 m of spread, 0 snap-ups, 0 pushes, 0 deck ratifications, 0 terrain ratifications.** In the enclosed case the trace shows the deck top exactly **0.499993 m above the terrain** with the feet seated on it to 0.000000 m, so GP-53's fix holds where it was made. **Lead 2, the real loop, is closed with it:** the same standing measurement was repeated with `of.run` NOT driving and the page's own rAF loop left running, which is the only way to get a jittering dt and a sweeping interpolation alpha. **1440 ticks in 24.0159 s at 59.96 Hz sim and 121.5 Hz render on a deck, and 1436 ticks in 24.0122 s at 59.80 Hz sim and 100.8 Hz render in a tunnel; spread 0.000000 m in both**, with the tick and frame counters read either side so DW-20 is satisfied rather than assumed. **Lead 3, the 2.0 s `Idle` clip, is refuted by frame hash:** standing still in third person with the arm fully out at 3.5 m, only **4 of 48 tiles** change and they change on **essentially every 0.1 s sample** (mean gap 1 sample against the 20 a 2.0 s period would give), which is a character breathing continuously and not a step; the other 44 tiles are pixel-identical for 20 s. | **The two deck gaps do not survive re-derivation as periodic mechanisms, and I should have re-derived them before relaying them.** (1) "Sunk more than 0.55 m into a deck can never recover" cannot happen to a deck at all: `deckUnder`'s window is `[rFrom - 2.0, rFrom + 0.55]` and the shipped deck is `deckH` 0.50 m, so a capsule anywhere inside the slab has its top face at most 0.50 m up, inside the 0.55 m rung. It is real only for the 4.00 m WALL, where being embedded means being STUCK because there is no structural `resolveEmbedded`, and stuck is not periodic. (2) "A 0.35 to 0.55 m sink leaves the player floating for 0.29 s" is not a sink: `groundSnapM` is 0.35 and `deckUnder` reaches 2.0 m below the feet, so a deck in that band is FOUND but not snapped to and the player falls the gap. That is stepping off a ledge, it is correct, and it is one-shot. **The one structural quantity that does vary with TIME was chased and closed by reading:** `Solid.shut` is written by `Structures.step` (door swings) and by `LaunchPad.step` (clamp release, and Reid has 9 launchpads), but both advance only while `swing != want` / `clampT != want`, so both settle and stop. There is no auto-close and no proximity trigger anywhere in `web/src/game`, so neither can cycle. | **Accepted** | 2026-07-27 |
| PH-60 | **`VoxelCollider.floorBelow` NO LONGER ANSWERS ONE NULL FOR TWO DIFFERENT WORLDS.** It returns a discriminated `FloorAnswer`: `floor` (a radius), `shaft` (the feet are in AIR with no rock within `searchM` below, so fall) or `buried` (the feet are IN ROCK past the step rung with rock all the way up, so NEVER fall). `KinematicBody` mapped both nulls to `groundR = -Infinity` and fell on both. | **This is the whole of R18 stated at the interface.** Neither function was wrong: `floorBelow` is right to refuse to answer a radius for a buried capsule (answering one is what ratified the sink twice already, GP-53 and WG-31), and `resolveEmbedded` is right to decline when no one-cell axis push frees the capsule, because its answer IS the minimum translation vector and its whole value is that it can never exceed one cell. The defect is that the caller could not tell "there is nothing under me" from "I am inside rock", and only one of those is a fall. **DW-26's rule applied to an ANSWER rather than to a predicate: when one authority must say two things, it says which.** Measured before anything was changed, at the entry tick on seed 991733: all three capsule samples solid on both solidity predicates, rock 30.14 m up to the surface with no air pocket at all, no air within 40 m below, all six one-cell exits between 0.714 and 0.989 m and **not one of them freeing the capsule**, and the nearest whole-capsule-free position **3.5 m away along +Y**; 437 ticks later, 284 m down, **nothing free within 40 m on any of the six axes**. So the one-cell bound did not want widening. The case wanted an owner. | **Accepted** | 2026-07-27 |
| PH-61 | **TWO RULES OWN THE BURIED CAPSULE, and the second is why the first terminates** ([`web/src/player/RockEscape.ts`](../../web/src/player/RockEscape.ts)). **(1) A capsule may not BECOME buried:** a tick that would end with the feet in rock, no floor and no push out is REFUSED, and the capsule keeps the position it started the tick at, losing the velocity that drove it there. **(2) A capsule that is ALREADY buried is EJECTED UP THE LOCAL RADIAL**, to the first free radius within one capsule plus one cell, and otherwise straight to its own column's `surfaceRadius`. | **Rule 1's argument is an induction and it names no rung, no cell and no seed, which is exactly why it holds at any burial depth**: the capsule starts on the surface, and every tick after that either ends legal or ends where it started, which was legal. Rule 2 exists because the WORLD can take the fallback away (the levelling tool raises terrain, PH-62; a save written before tonight can hold a player 29 km down), and **the radial is the only direction with a termination proof**: `solidForWalker` is false for every point above its own column's `surfaceRadius` (PH-7), so an upward escape is bounded by `surfaceRadius - r` from any depth, while down and sideways are bounded by nothing. That asymmetry is precisely why `resolveEmbedded`'s six axes cannot own this and this can. The one constant, capsule height plus one cell, is the distinction the answer turns on rather than a tuned window: **rock thinner than the body standing in it is a FLOOR you are sunk into and its top is where you belong; rock thicker than that is a MOUNTAIN you are inside, and the only place a mountain lets you stand is on it.** It also bounds the deep case at twelve grid steps instead of the 116,000 a 29 km legacy save would march. **The two rules separated cleanly under measurement, which is the evidence they are two rules and not one with a patch:** the seed-991733 cave drive enters the buried state on **126 of 5976 ticks and rule 1 owns every one of them (0 ejects, 0 unrescued)**, while every levelled burial is owned by rule 2 with **exactly one eject of exactly the raise**. | **Accepted** | 2026-07-27 |
| PH-62 | **R18 IS NOT A CURIOSITY OF ONE DIG ON ONE SEED. IT IS ONE PRESS OF THE LEVELLING KEY ON FLAT GROUND.** `probes/burylevel.js` levels a pad to `ground + N` with the player standing in it, which buries them by N, and sweeps N. | R18 was found by a jittered dig that happened to bury the walker, on 1 of 3 seeds, and a fix held only to that run is a fix fitted to a sample. The levelling tool is allowed to RAISE terrain and `LevelAction.ts:180` already says out loud that levelling raises the ground under your feet; nothing anywhere said what happens to the person standing there. **Measured on the pre-fix build, flat ground, no seed in the probe: a 3 m raise and a 12 m raise each drop the player 508.052 m through the world in ten seconds, 0 of 600 ticks grounded, 0 pushes**, and at ten seconds they are still accelerating; the same probe run twelve seconds longer reports 728 to 738 m. **After PH-60/PH-61, at raises of 2, 3, 5, 8 and 12 m: one eject each, of 2.214 / 3.004 / 5.004 / 8.004 / 12.004 m, the player grounded on the new pad, and 0.000000 m of spread over the following 600 ticks with 600/600 grounded.** A raise of 1 m buries nobody and fires nothing, because it is inside the walker's own 1.5 m deep threshold and the heightfield still owns the ground there. The 20 m row is refused by the level tool itself and is excluded by the probe rather than counted as a pass. | **Accepted** | 2026-07-27 |
| PH-63 | **R18b IS REFUTED: a capsule embedded in a structural wall is NOT stuck, it walks out.** The recovery is `StructureBody.resolveStep`'s escape hatch, which accepts any destination when the capsule is already not free at its start. | Driven rather than read (`probes/wallstuck.js`), because the previous audit reasoned from `deckUnder` alone and never exercised `resolveStep`, and I relayed it without re-deriving it. **Placement genuinely will bury you**: `StructurePlacement.resolveTarget` refuses on five grounds (built, too high, unsupported, uneven, cost) and consults the capsule on none of them, so free placement (the shipped B key) aimed at your own feet puts a wall centre **0.062819 m** from your axis with the capsule column **1.75 of 1.75 m** inside solid. **What happens next is nothing bad:** 600 stationary ticks at **0.000000 m** of spread and 600/600 grounded, on a deck and on bare terrain both, and walking out covers **8.926664 m in 2 s in all four directions with blockedByBuild 0/480**, which is unobstructed walking at the full 4.6 m/s. Note the shipped wall is **3.50 m**, not the 4.00 m storey the audit quoted; the premise survives that correction because 3.50 m is just as far outside `deckUnder`'s 0.55 m rise window. **The real defect the claim was masking is the converse one, and it is worth more than the claim was:** while ANY capsule sample is inside ANY structural solid, ALL structural collision is off, not just for the part you are in, so an embedded player walks through every other wall and every closed door at full speed. See R20. | **Accepted** | 2026-07-27 |
| PH-64 | **THE VESSEL REGISTRY IS THE ONE ANSWER TO "WHERE IS THIS VESSEL", and the live `/core` flight handle is a CACHE of the promoted record rather than a second copy** ([`web/src/sim/VesselRegistry.ts`](../../web/src/sim/VesselRegistry.ts)). A vessel IS a record with a stable, never-reused `id`. At most one record is PROMOTED, meaning it is the one the client's single `FlightSession` is carrying. `saveVessels` calls the sync hook as its FIRST STATEMENT, so the record is brought up to date from the live sim before it is serialised, with no caller able to forget. | **DW-26 applied to an object instead of to a query.** Before this there were two answers and neither was written down: a rolled-out rocket existed as a live flight handle and NOWHERE ELSE, not in the slot, not in a list, not under an id, so closing the tab lost it in silence (R12), and `rollOut` destroyed the previous one as its opening statement. The reason the id is monotonic and never reused, unlike a `/core` handle: `Registry::add` in `of_core_api.cpp` scans for the first freed slot and hands the same integer back, so a reused id would silently alias one vessel onto another **the first time a save file named one**, which is PH-27's handle trap with a disk behind it. Sync-before-serialise is the mechanism half: it makes "you cannot save a vessel in a place it is not" a property of the code rather than of somebody's memory. | **Accepted** | 2026-07-28 |
| PH-65 | **A RECORD IS IN EXACTLY ONE OF THREE MODES AND `/core`'S OWN PREDICATE DECIDES WHICH. PARKED** (on the ground or clamped: its place is a position and it does not change). **RAILS** (on a conic, nothing simulating it: its place is NINE NUMBERS AND A CLOCK, and there is **no stored position at all**). **FROZEN** (in flight but not on-rails eligible, i.e. in the air or under thrust: held exactly at the state vector it had). The discriminator is `of_fl_on_rails_eligible` and never a client-side altitude test. **A rails record NEVER RE-FITS its elements**; advancing changes only the time asked for. | Three separate arguments. **(1) The discriminator.** `onRailsEligible` is true exactly where `flight.h` and `orbital.h` produce identical bits (PH-16), so the boundary at which a vessel becomes propagatable IS the boundary at which the two integrators become the same arithmetic. A client-side ceiling test would have been a second copy of the atmosphere. **(2) No stored position on a rails record**, because a stored one is the second authority this whole design exists to delete, and it would go stale the moment the clock moved. **(3) Never re-fitting is what makes the answer deterministic**, and it is measured rather than asserted: re-parking at every one of 3000 steps lands **8.98e-10 m and 1.53e-11 m/s** away from the single jump, so the two paths are NOT bit-identical and the choice is load-bearing rather than stylistic. Fit once and only the time moves, and one jump of an hour equals 216,000 jumps of a tick **bit for bit** (asserted with `==`, not a tolerance, in `core/tests/test_physics.cpp` and again in the browser). **FROZEN is the honest one and it is not a cheat:** a vessel under drag or under thrust is not a conic, so the only two available answers are "keep integrating it", which is the cost that leaving it was meant to avoid, and "hold it", which is what a closed tab does anyway. Holding it is therefore the truthful reading of a browser reload. | **Accepted** | 2026-07-28 |
| PH-66 | **ABI 18: THREE EXPORTS, `of_fl_set_propellant`, `of_orb_park`, `of_orb_resume`. The conic pair is PURE AND HANDLE-FREE, and there is deliberately no `of_fl_park`/`of_fl_resume`.** The propellant setter CLAMPS to the part's own `propellantCapacityKg`. | **The setter is why R11 refused to ship real persistence and it is not a convenience.** `FlightCheats.refillTanks` says out loud what its absence cost: it had to rebuild the whole craft from its design and replay every staging just to refill, "because /core publishes no propellant setter". Restoring a save the same way gets the right HARDWARE and **FULL TANKS**, i.e. free delta-v on every reload, and every instrument afterwards reads healthy, which is this project's most expensive defect class. The clamp is the safety property and it exists because **a save file is untrusted input**: without it a hand-edited slot puts 10^9 kg in a 2150 kg tank and the integrator flies that mass. The worst a corrupt slot can now do is a full tank, which one key press in the bay already produces. **The conic pair takes no handle** because a vessel on rails is not a `FlightSim`, it is nine numbers, and requiring a live flight handle to advance one would mean keeping alive exactly the object being retired; this is the shape `of_mn_*` already takes (PH-37). **`FlightSim::park`/`resume` are NOT exported**, because a flight handle that could park itself would be a second answer to "is this vessel on rails" living inside the object whose retirement is the question. Purely additive: `expected.json` moved by exactly one field, `"abi": 16 -> 18`, and no pinned value moved. | **Accepted** | 2026-07-28 |
| PH-67 | **THE SAVED FORM IS THE REGISTRY RECORD, not a second schema, and it is stamped at the `SaveGame.writeSlot` CHOKE POINT rather than in `Persist.snapshot`. `SAVE_VERSION` does NOT move. PH-30's save refusal is RETIRED.** The design travels as `VesselDesign`'s own `DesignJson`; the staging state travels as `fired` and is restored by REPLAYING `stage` that many times; the fuel travels per part, keyed by design-part INDEX and not by handle. | **The record already had to decide what a vessel is in order to answer "where is it", so a save format that re-decided would be a second answer to the same question in a file nobody reads until a reload goes wrong.** The only field dropped is `stampedTick`, a reference into a loop clock that restarts at zero on every page load and is therefore meaningless on disk; restoring it as -1 reads as "the world was not running, so it did not move", which is exactly right. **The choke point is GP-102's precedent and its argument transfers verbatim**: every write funnels through `writeSlot`, so a snapshot path written next month carries the vessel without knowing it exists, whereas a field filled in by `Persist.snapshot` is a field the second snapshot path (`pagehide`, the debug save) forgets. `Persist.ts` is untouched by this lane, which also keeps it out of a file another lane had already pushed over its line cap. **Replaying `stage` rather than adding a `nextStageIndex` setter is deliberate**: `fireStage` is a pure function of the tree (PH-17), so k presses reproduce exactly the hardware k presses produced, and a setter would have let a save claim a stage index the tree does not agree with. **Keying fuel by design index rather than by handle** is what survives a save at all, because handles are `/core`'s and are reallocated on rebuild. **PH-30's refusal is retired rather than reworded**: it existed because the slot had no field for a vessel, and it now has one. Retiring it is also load-bearing rather than tidy, because with the refusal in place no autosave can fire while the player is in orbit and the orbital reload could never have been proven. | **Accepted** | 2026-07-28 |
| PH-68 | **THE PLAYER'S BODY PARKS COHERENTLY WHEN CONTROL IS ELSEWHERE. It is not stepped, and its position is RECORDED as an anchor that survives a reload** ([`web/src/app/PlayerAnchor.ts`](../../web/src/app/PlayerAnchor.ts), `SaveSlot.player`). The anchor is read off `ViewRouter.walker`, never off the active view source. **This closes R13.** | The brief asked for a decision either way and said that leaving it unstated is what is not defensible. **Keep-simulating was rejected on three counts.** (1) It is a second answer to "where is the player": a stepped body can fall, slide, be ejected up the radial (PH-61), drown or be moved by a structure **while nobody is looking at it**, and the first anyone learns of it is on coming back. (2) Nothing observes it, so nothing can catch it going wrong; R20 is already exactly this shape, a player inside a wall reading healthy on every instrument, and an unwatched simulated body is that with no camera on it. (3) It costs a full collision step per tick for a body whose only job is to be somewhere when you get back. **The client was already doing the parking half by accident and that is the state the brief said not to leave it in:** `ViewRouter.step` delegates only to the active source, so the walking `Controller` is simply never stepped while aboard, and `Systems.ts` gates the dig and level rays off `aboard` because its aim ray is stale. That behaviour was inherited, never decided. What is added is the half that was missing: the parked position is WRITTEN DOWN. **Reading the anchor off the WALKER and not off the router is the one subtle line**: `ViewRouter.state()` reports whichever source is live, so calling it while strapped in returns the ROCKET's latitude and altitude and a save in orbit would record a body 80 km up. **R13 closes as a side effect**, and with a vessel now persisting it stops being an annoyance and becomes an incoherence: a rocket in orbit above a body teleported half a planet away. Measured before: in at lat 1.613 lon 107.822, back at 2.000 / 144.000. | **Accepted** | 2026-07-28 |
| PH-69 | **THE CONTROL HANDOFF IS PUBLISHED, NOT BUILT (`ResumeBoot.ts` §5), and `releaseControl` IS NOT `FlightMode.disembark`.** The seam: a vessel is named by `VesselRecord.id`, selected by `promoteVessel(flight, id, tick)`, enumerated by `registry.list()`, located without being promoted by `stateOf(...)`, and `mayLeave(rec)` refuses to abandon a FROZEN one. | **The two verbs are genuinely different and conflating them was a real defect, found by driving and not by reading.** `disembark` is CLIMBING OUT: it teleports the body to the vessel's own lat and lon, and it is refused above 2 m/s because a walker cannot survive stepping out at 2.3 km/s. Both are correct for a key press and both are wrong for a handoff, where the body is at the pad two hundred kilometres below and where **the only vessel you may walk away from is one moving fast enough to stay up**, i.e. the refusal is exactly backwards. Measured before the fix: `demote` called `disembark`, the speed guard refused, **`aboard` stayed true, `refusals` went 0 to 1, and the session was destroyed underneath it**, leaving the player strapped into a vessel that no longer existed. `reload.mjs` already carries a standing assertion against precisely that state, which is how well known this failure is here. **`mayLeave` is the other half and it is a design statement:** leaving a FROZEN vessel would produce a rocket hanging motionless mid-ascent for as long as you are away, which is neither a simulation nor a story, so the refusal is "cut the engine and coast above 60 km first", which is also exactly the skill DW-29 says reaching orbit is meant to teach. FROZEN still EXISTS because closing a tab is not a choice the game gets to refuse; `mayLeave` governs only the voluntary handoff. | **Accepted** | 2026-07-28 |
| PH-70 | **THE DESIGN SNAPSHOT IS VERIFIED, NOT TIMED.** Before any staging, the flight's own part list is a verbatim copy of the design's (`of_fl_create` deep-copies), so the part ids in order must match exactly or the snapshot is REFUSED and counted in `refusedSnapshots`. | The obvious implementation is "snapshot at roll-out", which is a claim about WHEN and therefore a claim nothing checks; the bay's design handle keeps being edited afterwards, so a late snapshot records a rocket the player is flying a different version of, and nothing downstream could ever tell. Checking the snapshot against the craft it claims to describe turns a timing hope into an assertion. **Refusing is the right failure**: a vessel with no design cannot be restored, and a counter a probe can read is strictly better than saving a plausible wrong rocket. It also bought the per-tick hook a home with no shared file touched: the check rides `FlightPad.stepPadClamps`, the one flight call the loop already makes whether or not anybody is aboard, and it is one integer compare on the common path. Measured `refusedSnapshots: 0` in every driven run of both probes and all three reload phases. | **Accepted** | 2026-07-28 |
| PH-71 | **A PROMOTED VESSEL WITH NOBODY ABOARD IS NEVER STEPPED, SO IT IS PRIMED WITH ONE `stepClamped` AT PROMOTE AND AT ROLL-OUT.** Parked vessels only (CLAMPED and DOWN), because only they are genuinely stationary. Guarded by [`probes/clamprestore.js`](../../web/tools/smoke/probes/clamprestore.js). | **This is PH-20's launch-clamp bug with a new cause, and it was found by Admin ASKING rather than by a probe failing**, which is worth recording on its own. `FlightSim::telemetry` is written by `step` and by nothing else; `FlightSession.step` is reached only through `VesselObserver.step`; `ViewRouter` drives that only while `aboard`. So a rocket restored onto its pad at boot could have published `massKg 0`, hence `currentTwr()` 0, hence a clamp that can never release, with every HUD number reading healthy. **Reid spent a large part of a day behind exactly that screen** and GP-73 to GP-76 fixed it; handing it back with a different cause would have been worse than not restoring the vessel at all. **Driven, not reasoned about, and the answer was no**: a restored vessel reads **massKg 9845** and the clamp releases at **TWR 1.9457**. **The obvious repair does not work and the reason is worth knowing**: `of_fl_step` guards `!(dt > 0.0)` and returns 0, so a zero-length step is silently a no-op and the first version of this fix did nothing while looking correct. What works is the clamp's OWN mechanism (PH-20): step, then write the pad pose back. It is applied to parked vessels only, because zeroing 2.3 km/s to make an instrument read would be destroying the orbit to fix the gauge. **And the probe found the same defect where nobody had looked: a rocket that has just been ROLLED OUT and never boarded read `massKg 0` for the whole walk over to it.** It never blocked anything, because boarding steps the vessel long before anyone throttles up, which is precisely why it had never been seen. One integer says it: the fix is the same call. | **Accepted** | 2026-07-28 |
| PH-72 | **THE WIRED VESSEL-RAILS IMPLEMENTATION WINS; `sim_world.h`'s IS MARKED SUPERSEDED AT ITS SYMBOLS, NOT DELETED.** Admin's R27 ruling. Markers on the `VesselMode` enum, on the active/on-rails section and inside the on-rails branch of `step()`, each naming the client file that replaces it. | **Kept rather than deleted because the FILE is not dead, only the API is duplicated.** `SimWorld` is reachable from two green suites and `journey_dump`, and those cover the SOI crossing, the star to Forge to Cinder frame graph, the floating origin and a factory ticking under flight on one clock, none of which the client has. Deleting the header to remove a duplicate vessel API would have taken that coverage with it. **The markers are at the SYMBOLS and not only in the file banner, because the reader this protects arrives by grep and never sees a banner.** The section marker also records why this one could not be promoted even if someone wanted to: it holds a single `state_` and a single `elements_`, so it cannot represent many vessels with at most one active, which is the whole problem. **One line for the corroboration, per the ruling:** its on-rails branch and PH-65 independently reached the same rule, elements held fixed and only the time moving. That is a reason to trust the RULE and not a reason to keep two copies of the code. **R27 CLOSED.** | **Accepted** | 2026-07-28 |
| PH-73 | **FOUR BOUNDS IN `test_flight.cpp` NOW ASSERT THEIR OWN MEASURED RESIDUE TIMES A MARGIN OF 2.0, AND THE MARGIN IS DERIVED.** Was `posErrM < 5.0` against a residue of **0.00570342259015 m**. | **A control that cannot fail is not a control, and this was the dominant failure catalogued in [`INSTRUMENTS.md`](../web/INSTRUMENTS.md).** The integrator could have degraded by two orders of magnitude and the test would still have passed. The three siblings were worse: the apoapsis bound was **6e8** times its own **8.03265720606e-09 m** deviation and the eccentricity bound **1e8** times its **7.02847016676e-14**. **The margin is derived rather than picked for looking safe.** The residue is DETERMINISTIC (repeated runs agree on every printed digit), so the margin covers only the same source built by a different compiler: instruction selection and libm differences, about 1 ulp per operation over 102132 steps, so below **2.3e-11** relative even if it accumulated linearly. The leading truncation term goes as dt squared, so the cheapest real regression (a doubled step) moves the residue by **4x**. The usable window is therefore [1+2.3e-11, 4) and **2.0 is its log-scale middle**. **Proved it has teeth rather than asserting it**: doubling the step to 0.04 fails three of the four, the apoapsis at 2.49e-08 against a bound of 1.61e-08. Reverted, 843 checks green, full ctest **35/35**. If a compiler change ever trips these, re-measure and restate the residue; do NOT widen the margin back into decoration. **R24 CLOSED.** | **Accepted** | 2026-07-28 |
| PH-74 | **`reload.mjs`'s THREE PAD-OCCUPANCY ROWS ARE SPLIT BY MODE; `recover` KEEPS THEM CHARACTER FOR CHARACTER.** The `leave` side asserts the mirror image against the BEFORE measurement rather than a constant. `LaunchPadSave`'s stale clamp rationale corrected in place. | **Both modes stopped having the same right answer the moment vessels persisted.** "Nothing came back" is still the pass condition for `recover` and is now the FAILURE condition for `leave`. Split rather than deleted per Admin: the recover rows are the only thing between us and a pad that resurrects a recovered rocket, and deleting them to turn `leave` green would have thrown away the control the runner is built around. **Two things are deliberately NOT asserted for `leave`: `flightLive` and the pad's `holding`.** Neither has been measured across a `leave` reload and asserting an unmeasured expectation is how a control encodes a bug as the spec, which is PH-73's defect class exactly. The clamp row survives for BOTH modes in its mode-independent half: a reloaded pad is never caught mid-swing. **`LaunchPadSave`'s header said the clamp state is not saved BECAUSE the flight it was released for is not in the save either; both halves of that premise are now false.** The behaviour is right and stays, on a narrower reason: `clampT` lives on the pad and not on the vessel, so shut is the only value that cannot contradict the vessel row beside it. The rotted reasoning is kept VISIBLE rather than deleted, because a correct behaviour whose justification has failed is what the next reader removes. **UNVERIFIED and deliberately so** (see R28). **R25 CLOSED.** | **Accepted** | 2026-07-28 |
| PH-75 | **R28 CLOSED: the `leave` branch is DRIVEN, and the two assertions held out of it are now asserted FROM MEASUREMENT.** Both modes green against HEAD. One of my own guesses was wrong and the measurement corrected it. | **Run against a HEAD ARCHIVE and not the working tree, which mattered:** the working tree carried another lane's uncommitted Water material whose fragment shader fails to compile, so the first run exited 1 on 512 WebGL errors that had nothing to do with these rows. Against HEAD both modes exit **0**. Measured for `leave`: `flightLive true`, `aboard false`, `parts 7` against 7 before, pad `holding true, clampT 0, releasing false`. **The correction: `holding` was never mode-dependent.** I had put it in the recover-only set on the guess that a pad with a vessel back on it might report differently; it does not, both modes come back identically, so the row moved BACK to being shared and is stronger there than it was as two copies. That is the value of refusing to assert it the first time: had I guessed it into the `leave` branch as `false` it would have passed nothing and hidden the fact that the pad half is mode-independent. **The `leave` rows are provably not vacuous**: the recover run's own measured values (`live false`, `parts 0`) fail both of them, so the pair discriminates the two modes rather than merely passing. | **Accepted** | 2026-07-28 |
| PH-76 | **THE CONTROL HANDOFF: `leaveVessel` / `resumeControl` / `takeControlRemote`, and the gap was NOT persistence.** New in [`FlightMode.ts`](../../web/src/app/FlightMode.ts) (`takeControlRemote`), [`FlightVessels.ts`](../../web/src/app/FlightVessels.ts) (`leaveVessel`, `resumeControl`), driven by [`probes/handoff.js`](../../web/tools/smoke/probes/handoff.js) and `vesselreload.mjs --phase=handoff`. | **`board()` routes to `climbIn()` only inside `BOARD_RANGE_M` (18 m), so a vessel in orbit could NEVER be re-entered however well it was saved.** That was the whole gap; the persistence work (PH-64 to PH-75) had already done the rest. `board()` and the range gate are untouched, because walking up to a rocket on the ground must keep its check: what exists now is a SECOND explicit door for the case where distance is not the question. **`resumeControl` refuses to half-seat** (if `promoteVessel` returns false it seats nobody). **`leaveVessel` calls `syncPromoted` BEFORE consulting `mayLeave`, and that order is load-bearing rather than tidy**: `makeRecord` stamps `mode 'parked'` at roll-out and only `syncPromoted` ever updates it, so an unsynced guard would have waved a rocket under full thrust out of the world. **Driven on a clean HEAD archive with only these files overlaid**, so it depends on no other lane's tree. Across a real `page.reload()`: fuel **882.7804658633409 kg** before and after, **difference 0**; pose `fwd`/`right` bit-identical to the last digit; conic **a 724925.1064716058 m, e 0.06228720022942358**. **The number that proves the mechanism is `distanceToVesselM 417995.3` against `boardRangeM 18`**: the old path could not have produced that seat. **The refusal discriminates rather than decorates**: a frozen vessel refuses with `whyNotLeave`'s published sentence and `aboard`, `live`, `records`, `promotedId` and `demotions` are identical before and after the refused call, and cutting the throttle at the same altitude flips the record to `rails` and the same call then succeeds. `--phase=all` (pad, ascent, orbit, handoff) passes with zero page failures. `MapView.ts` deliberately untouched. | **Accepted** | 2026-07-28 |
| PH-77 | **THE DIG SCENE GETS A THIRD SITE AT 68.441 m OF OVERHEAD ROCK, AND THE SINKING STILL DOES NOT REPRODUCE.** Sites now measure 7.189 m (spawn), 14.722 m (mountainside), **68.441 m** (deep), each required to beat the one before by 5 m or the run fails as a duplicate. | **The brief's premise was half wrong and the obvious repair does nothing, which is the part worth keeping.** `tunnelsink` already ran two sites and the second was not a duplicate. The suspected hole was `findMountain`'s 0.30 deg sweep (about 3 km); **widening it to 5 deg, 550 km, 3721 points raises the best 40 m rise only from 29.7 m to 37.4 m.** The terrain is fractal, so the steepest slope within 3 km is about as steep as the steepest within 550 km, and **two of world-gen's three shortlisted coordinates are WORSE than spawn** (0.66 m and 0.74 m of rise over 40 m): they are named Hills and they are hills. **Bore LENGTH is the lever, not sweep radius**, because a level bore sits under exactly as much rock as the hill climbs over the distance bored. The new site scores by rise SUSTAINED over the whole bore (minimum at four points, so a slope that flattens loses) and drives until the overhead PEAKS and begins to fall. **Still 0.000000 m on every axis**, 0 snaps, 0 ratifying ticks, 0 pushes: 68.4 m of rock over a 160 m bore; 59.1 m overhead walked end to end six times then stood in for 7200 ticks; save and load with the tunnel present at all three sites; the shaft bottom on the HEIGHTFIELD branch, which the still phase structurally could not reach before because it requires `underRock`; and **the rendered eye at 144.3 Hz over 1000 samples, never sampled before because every probe runs `of.run` at a multiple of 60 and `Loop.ts` bypasses the interpolator there**. | **Accepted** | 2026-07-28 |
| PH-78 | **THE LAST UNCOVERED PATH IS NOT CLEAN, IT IS UNREACHABLE, AND THAT CLOSES THE SINKING HUNT.** A fourth `tunnelsink` site bores, cuts a room, lays foundations, presses walls into the capsule's own space, places a machine, saves, reloads and stands. | **4800 ticks at spread 0.000000 m is the weaker half of this result.** The number that ends the hunt is `structureTests` = **0 on all 4800 ticks**. A structural part pressed while standing in a bore does not land in the bore: it lands OVERHEAD at the heightfield surface, measured over four presses at 9.2 / 12.0 / 13.2 / 17.3 m above the feet against overheads of 9.16 / 11.93 / 13.196 / 17.21 m, **a miss of 0.004 to 0.09 m**, with nothing inside the capsule at all. `StructurePlacement` addresses the grid off `Structures.groundRadius`, which calls `_of_surface_radius` (the heightfield), and **a bore with an intact roof leaves the heightfield untouched**. So structures in a tunnel cannot be a source of the sinking BECAUSE STRUCTURES CANNOT BE IN A TUNNEL, and the walker's structural port is never consulted down there. **Closed by geometry rather than by a clean measurement, which is the stronger form.** The probe carries two CHARACTERISATION checks that fail the day placement changes, rather than asserting the current behaviour is correct: it is not (R34). The built site requires `--sandbox=1` and **says so rather than going green without it**. Default run unchanged: 32 checks, 0 failures, verified on a clean HEAD archive at `abi=19`. **With this the sinking hunt is COMPLETE: every path is clean or unreachable, and PH-60 is the fix.** | **Accepted** | 2026-07-28 |
| PH-79 | **VESSELS AND THE FLIGHT PATH SURVIVE A WALKED REBASE: 0.000000 m ON ALL FOUR SUBJECTS, AND THE INSTRUMENT WAS PROVED CAPABLE OF SAYING OTHERWISE.** New: [`probes/vesselrebase.js`](../../web/tools/smoke/probes/vesselrebase.js), plus staleness report surfaces on `LaunchPadView` and `Avatar`. | **Nothing in scope caches an engine-space transform, and it is structural rather than luck**: all 33 `toEngine` call sites were enumerated and every subject in scope re-derives from its f64 anchor EVERY FRAME, which is what `FloatingOrigin`'s own header prescribes. The plume is parented to the vessel root and correct by construction; the records, the clamp and the pad's collision are body-frame f64 and never engine-space at all. **Driven the way the defect requires**: every probe on this project TELEPORTS, and a teleport drops and rebuilds every resident chunk before a frame is drawn, releasing the stale state, so none of them could ever have seen this. This one **walks 11,894 m continuously on the movement keys**, 1550 sim-seconds, 114 s wall clock, no teleport after setup, crossing **three rebases** in one unbroken tape, demoting after the first and promoting after the second. Displacement **0.000000 m**: rocket over 206 slices, pad over 310, body over 310, demoted record over 104. **The green means something because the instrument was made to FAIL first**: in a sandbox copy only, placing each pad once, computing the vessel's engine position once and placing the avatar once turns the same probe red at **120.279 m** on the first rebase (exactly the delta), accumulating to **360.62 m** over three, with the record correctly staying at 0 because it was not among the things broken. **One reading was not zero and the tempting fix was wrong**: the pad read 1e-6 m at 137 m from the origin, 4e-6 at 244 m, 6e-6 at 367 m, which is **2^-23 times the distance**, float32's relative epsilon, because a `BatchedMesh` keeps per-instance matrices in a float32 DataTexture and `matrixAt` returns the f64 value rounded. A tolerance would have buried that; the expectation is now rounded the same way the GPU rounded the reality, through `Math.fround`, and the zero is hard again. | **Accepted** | 2026-07-28 |
| PH-80 | **R32 ANSWERED: THE 1.5 m GATE CANNOT BE STOOD ON, AND THE REASON IS THE DIG BRUSH RATHER THAN THE HARNESS.** `DEEP_UNDERGROUND_M` is left at 1.5 because **any threshold in (0, 3.1) gives a bit-identical game**. New: [`probes/deepgate.js`](../../web/tools/smoke/probes/deepgate.js). | **My gentle-slope hypothesis was wrong and it failed twice, which is the useful half.** A level bore into a gentle rise **cannot be started at all**: on a 3.68 deg site, 44 strikes removed exactly ZERO cells, because a level aim ray leaves an eye 1.62 m up and ground rising 0.064 m/m never reaches it inside the 4.5 m dig reach. And with the shaft-then-bore that does work, the RATE was never the problem: 2.912 m of bore per metre of depth at spawn is gentle. **The problem is range.** `DIG.radiusM` is 1.5 with the brush centred on the ray hit, so the smallest cavity a player can cut is **3 m tall**, a column has a roof only once the brush top is under the surface, and the feet are then a brush DIAMETER below that roof. Sampled at 0.02 m through the walker's own free-space predicate, **10,656 standable columns across 104 m at two sites contain not one with a depth in (0.25, 3.0) m**: the spectrum is {0} together with [4.108314, 9.594922] at spawn and {0} together with [3.149978, 16.819797] on the hillside. **1.5 m sits in the middle of a hole nobody can dig**, so the constant is not a tuned value but one of a wide band of equivalent ones, and no test can distinguish them. **Crossing is CONTINUOUS at spawn**: the tick authority changes hands moves the feet **0.038926 m** out and **0.039115 m** in, grounded, no push, no eject, against the 1.1 m bound, while the gate quantity itself jumps **4.450269 m** between the two adjacent positions. The switch absorbs a 4.45 m discontinuity in 3.9 cm. **On a 45 deg face it CHATTERS (R36).** | **Accepted** | 2026-07-28 |
| PH-81 | **ABI 19 to 20. A RADIAL PART'S ORIGIN IS A DECLARED PROPERTY, NOT AN INFERENCE, AND THE STRAP-ON BOOSTER STOPS DRAWING INSIDE THE TANK.** `PartDef` gains a REQUIRED `RadialOrigin` (`MountPlane` or `Axis`); `originFrom` AND `centroidOf` branch on it. `of_vs_transforms` also gains `radialOffsetM`. | **Neither the contract nor the asset was wrong, which is exactly why this passed review and shipped.** `/core` documents a radial origin as the inboard MOUNT PLANE, correct for fins, panels, RCS, legs and verniers; the booster's mesh is authored around its OWN AXIS because the same mesh must serve stack mounting. Two correct conventions and no way to tell them apart by looking, so the convention became a declaration. **Required means required**: no default constructor and no in-class initialiser, so all 24 authored rows had to answer and a new part cannot be added without answering. **Audited against `ASSET-SPECS.md` §3.3 rather than guessed**: the 6 parts publishing `socket_radial_mount` at their own origin are `MountPlane`, the 18 authored around their centreline are `Axis`. Only `SolidBooster` is read today (the only `Axis` part with a radial mount); the other 17 are declared anyway because the day one of them grows a radial mount is the day nobody remembers to come back. **Measured: a flush strap-on goes from -0.6250 m of interpenetration to exactly 0.0000 m, and on a radial decoupler from -0.4750 m to +0.1500 m. The fin is bit-identical.** Two independent negative controls, one native and one **a second wasm built from the reverted header and swapped into the served archive so the layout rule was the only variable**; the same probe goes red on four checks by name under both. **No pre-existing expectation moved, 35/35 green, and the reason is the finding**: `centroidOf` gave 0.775 + 0.625 = 1.40 before and 1.40 + 0 = 1.40 after, so the centre of mass, inertia, centre of pressure and every delta-v number were ALREADY RIGHT. **The old code was accounting the mass in the correct place and drawing the mesh in the wrong one.** Item 2 in the same bump: the flight part row and `of_vs_transforms` share ONE client constant, so both widened to nine words together; widening one alone would have been a misaligned read, not a smaller change. Handshake verified in the browser at **`abi=20`** rather than assumed from the rebuild. | **Accepted** | 2026-07-28 |
| PH-6 | **Wave-0 headless orbital core BUILT + GREEN.** `core/include/of/orbital.h`: classical Keplerian elements + state↔elements; **universal-variable Kepler propagation** (Stumpff C/S, Newton on χ — one path for elliptic AND hyperbolic, PH-3 realized); **custom fixed-step symplectic integrator** (velocity-Verlet) owning flight dynamics (PH-4 realized); naive forward-Euler kept only as the cautionary contrast; park/resume conic fit. 71 checks pass in `core/tests/test_physics.cpp`; all 4 core suites green via ctest. | **Headline NO-DRIFT proven at the math level (G3/G6/G8):** symplectic max \|dE/E\| = **2.4e-11** over 30 orbits (final 1.6e-14), position vs analytic = **434 m** on a 700 km-radius orbit; naive Euler gains energy to \|dE/E\| = **0.40** (~2.5e13× worse — the "kraken"). Park/resume round-trip is identity on (r,v) for elliptic AND hyperbolic; energy + angular momentum conserved by analytic propagation. Retires the Chaos-at-orbital-speed risk at the math level. | **Accepted** (Spike 2 build) | 2026-06-14 |

## 4. Architecture & approach
- **Two regimes:** (a) **On-rails** — vessel = Keplerian elements in its dominant frame; advance analytically, including under time-warp; (b) **Active** — full rigid-body sim near the player. core-engine's `SimProxy` triggers promotion/demotion; physics fills the content of each side and must make the handoff smooth (KSP's load-in wobble is the cautionary tale).
- **Patched conics:** vessel feels gravity from exactly one body (its SOI). Crossing an SOI boundary = a frame switch event (core-engine) + re-derivation of elements relative to the new body. Predicted trajectories (for the map/maneuver UI) computed by propagating conics forward and intersecting SOIs.
- **Vehicle structure:** parts as rigid bodies joined by constraints; thrust applied at engine parts; aero forces from atmosphere density (world-gen) × velocity. Joint stability/wobble is a known KSP pain — consider rigidified part-trees or substepping.
- **Surface:** character controller + vehicle contact against world-gen collision (heightfield, or voxel patch where dug). Gravity from the local body.

## 5. Interfaces & dependencies
**Depends on (inbound) — Spike-1 contracts CONSUMED verbatim:**
- core-engine: `FUniverseCoord`(+`FFrameId`), `FReferenceFrame`/`IFrameGraph` (μ/SOI/spin, D-006), `ISimProxy`/`ISimRegistry` (we implement the four hooks; core-engine owns promotion/demotion + hysteresis), `ISimClock` (warp, `TickIndex`), `OnOriginRebased`, **`OnSOIChange` (carries pos+vel re-expressed in new frame — we consume both)**, **CE-6 frame-velocity subtraction** (we read TRUE velocity, integrate the slow residual). All honored; no requested change.
- world-gen: `SampleTerrainHeight`/`GetGroundContact`/`RaycastTerrain` (analytic, resident-free), `FBodyParams`/`FAtmosphereProfile`. Adopted as the collision source (PH-5).

**Provides to (outbound) — the VESSEL programme's published C++ surface (new, 2026-07-26):**

| Header | What it publishes |
|---|---|
| [`core/include/of/vessel.h`](../../core/include/of/vessel.h) | `PartId` / `parts::` (20 authored parts, §5.1 below), `PartDef`, `PartCatalogue` + `catalogue()`, `PartInstance`, `Stage`, `Vessel` (tree + `addRoot`/`attach`/`layout`/`assignSubtreeToStage`), `MassProperties` + `massProperties()`, `staticMarginM()`, `activeEngines()`, `fireStage()` + `StageResult`, `StagePerformance` + `stagePerformance()`/`allStagePerformance()`, `tsiolkovsky()`, `totalDeltaVVacuumMS()`, `remainingDeltaVVacuumMS()`, `thrustToWeight()` |
| [`core/include/of/atmosphere.h`](../../core/include/of/atmosphere.h) | `kG0`, `AtmosphereProfile`, `makeForgeAtmosphere()`/`makeCinderAtmosphere()`/`atmosphereForBody()`, `density()`/`densityRaw()`, `pressureRatio()`, `dynamicPressure()`, `spaceAltitudeM()`/`inSpace()`, `lapse()` |
| [`core/include/of/flight.h`](../../core/include/of/flight.h) | `FlightState`, `FlightEnvironment`, `PropulsionOutput` + `evaluatePropulsion()`, `feedGroup()`/`drawPropellant()`, `AeroTuning`/`AeroForces`/`aerodynamics()`, `SasMode` (**9 values at W12: Off/Hold/Prograde/Retrograde/Command/Normal/Antinormal/RadialIn/RadialOut, appended**), `ControlAuthority`/`controlAuthority()`/`SasTuning`/`stabilityAssist()`, `FlightSim` (`step`/`stage`/`park`/`resume`/`onRailsEligible`/`telemetry`), `OrbitSummary` + `summarize()`, `GravityTurnProgram`, `clampToMaxAoA()` |
| [`core/include/of/orbital.h`](../../core/include/of/orbital.h) | **W12 addition: `Basis` + `basisAt(StateVector)`, the ONE prograde/normal/radial-out triad (PH-39).** Everything else unchanged. |
| [`core/include/of/maneuver.h`](../../core/include/of/maneuver.h) | **NEW, W12.** `Node` (the KSP handle decomposition + `tFromNowS`), `Basis`/`basisAt` (aliased from orbital), `deltaVVector()`, `BurnEstimate` + `estimateBurn()` (duration across the REMAINING stages, lead, feasibility against `remainingDeltaVVacuumMS`, `burnFractionOfPeriod`), `Plan` + `plan()`, `Path` + `samplePath()` (the conic as a polyline for the map, E-uniform, plus apsis positions/times and the orbit pole). Pure functions; no state, no FlightSim, no execution. |

Consumers this is aimed at: **gameplay** (assembly UI, navball, per-stage delta-v readout, DW-30 item 4), **rendering** (part transforms via the tree layout, plume from `PropulsionOutput`), **persistence** (a vessel is its tree plus per-part propellant plus `nextStageIndex`), **networking** (fixed-step + pure functions, determinism asserted).

### 5.1 The published part taxonomy (the art lane and the assembly UI bind to this)

Ids are a separate opaque space starting at `0x0100` so a `PartId` and a gameplay `ItemId` can never be confused. **`asset` is the exact glb node name and every dimension is read off what `tools/blender/build_rocket_parts.py` actually builds**, not invented here.

**Reconciliation note, 2026-07-26:** the first draft of this table used its own names (`TankSmall`, `EngineMain`, ...) and its own guesses at the Tier-2 heights. The art lane landed an expanded `rocket_parts.glb` mid-task with **two diameter classes** (S = 1.25 m, L = 2.50 m, exactly 2x, with a `StackAdapter` between them), 17 stack parts and 6 radial ones, under different names. The catalogue was **realigned to theirs**, because a taxonomy that disagrees with the shipped meshes is the two-authority failure this project keeps paying for, and because their reading of DW-29's "engines in two sizes" (two diameters plus an adapter) is better than the draft's (two performance tiers on one diameter). Heights corrected: monoprop tank 0.60 -> 1.00, reaction wheel 0.30 -> 0.40, battery 0.30 -> 0.60, solid booster 1.00 x 5.00 -> 1.25 x 6.00 with no bottom node.

| PartId | `parts::` | asset | diam x height (m) | dry kg | propellant | engine (T_sl/T_vac kN, Isp_sl/Isp_vac s) | notes |
|---|---|---|---|---|---|---|---|
| 0x0100 | `CommandPod` | `CommandPod` | 1.25 x 2.50 | 800 | 40 monoprop | | crew 1, 5 kN·m torque, 100 charge |
| 0x0101 | `TankLiquidSmall` | `LiquidTankSmall` | 1.25 x 2.00 | 215 | 2150 LF | | 10:1 wet/dry |
| 0x0102 | `TankLiquidSmallLong` | `LiquidTankSmallLong` | 1.25 x 4.00 | 430 | 4300 LF | | exactly 2x the short one |
| 0x0103 | `EngineLiquidSmall` | `LiquidEngineSmall` | 1.25 x 1.60 | 1200 | | 160/200, 264/330 | gimbal 5 deg, mdot 61.8010, no bottom node |
| 0x0104 | `EngineVacuumSmall` | `LiquidEngineVacuumSmall` | 1.25 x 1.00 | 400 | | 30/60, 180/360 | gimbal 5 deg, mdot 16.9953. **NO MESH** |
| 0x0105 | `SolidBooster` | `SolidBooster` | 1.25 x 6.00 | 1300 | 9000 solid | 600/672, 190/212.8 | no throttle, no restart, no gimbal, no bottom node; 27.95 s burn |
| 0x0106 | `DecouplerStackSmall` | `StackDecouplerSmall` | 1.25 x 0.25 | 50 | | | |
| 0x0107 | `DecouplerRadial` | `RadialDecoupler` | 0.30 x 0.40 | 25 | | | radial, 0.30 m standoff |
| 0x0108 | `NoseCone` | `NoseCone` | 1.25 x 1.20 | 30 | | | Cd_axial 0.12 against a pod's 0.55, no top node |
| 0x0109 | `LandingLeg` | `LandingLeg` | 0.48 x 0.42 | 50 | | | radial, reach 2.13 m |
| 0x010A | `Parachute` | `Parachute` | 1.25 x 0.75 | 100 | | | 50 m^2 deployed |
| 0x010B | `Fin` | `Fin` | 0.10 x 1.10 | 40 | | | radial, planform 0.85 x 1.10 |
| 0x010C | `CargoBay` | `CargoBay` | 1.25 x 1.60 | 150 | | | |
| 0x0110 | `RcsBlock` | `RcsBlock` | 0.50 x 0.50 | 20 | | 1 kN total, Isp 240 | radial, burns monoprop |
| 0x0111 | `TankMonoprop` | `MonopropTank` | 1.25 x 1.00 | 40 | 200 monoprop | | |
| 0x0112 | `ReactionWheel` | `ReactionWheel` | 1.25 x 0.40 | 50 | | | 15 kN·m |
| 0x0113 | `Battery` | `Battery` | 1.25 x 0.60 | 20 | 1000 charge | | |
| 0x0114 | `SolarPanel` | `SolarPanel` | 0.44 x 0.30 | 20 | | | radial, 2.0 charge/s |
| 0x0115 | `DockingPort` | `DockingPort` | 1.25 x 0.30 | 50 | | | capture radius 0.60 m, cone 30 deg (DW-30 item 5) |
| 0x0116 | `EngineVernier` | `EngineVernier` | 0.28 x 0.43 | 80 | | 16/20, 250/312.5 | radial, gimbal 8 deg |
| 0x0117 | `TankLiquidLarge` | `LiquidTankLarge` | **2.50** x 4.00 | 1720 | 17200 LF | | class L, 4x the cross-section |
| 0x0118 | `EngineLiquidLarge` | `LiquidEngineLarge` | **2.50** x 2.60 | 4800 | | 640/800, 264/330 | class L, mdot 247.20, no bottom node |
| 0x0119 | `DecouplerStackLarge` | `StackDecouplerLarge` | **2.50** x 0.35 | 150 | | | class L |
| 0x011A | `StackAdapter` | `StackAdapter` | **2.50** x 1.00 | 300 | | | L below, S above; the only part with two classes |

**One part still has no mesh: `EngineVacuumSmall` (asset name reserved as `LiquidEngineVacuumSmall`).** A 1.25 m vacuum bell, shorter than `LiquidEngineSmall` at 1.00 m, is what an upper stage wants and it is what the reference vehicle's 3065 m/s second stage is built on. Substituting a second `LiquidEngineSmall` costs that stage about 970 m/s. It is a bell on the existing class-S contract, so it is an addition to the file rather than a new convention.

**Reference vehicle "Ascender I"** (the test fixture, and the obvious first tutorial rocket): `CommandPod` + `Parachute` + `TankLiquidSmall` + `EngineVacuumSmall` + `DecouplerStackSmall` + `TankLiquidSmallLong` + `EngineLiquidSmall` + 4 `Fin`. 12.10 m, 9845 kg on the pad, pad TWR 1.6567, per-stage vacuum delta-v **1857.79 + 3065.12 = 4922.91 m/s**, static margin -0.884 m (stable), axial Cd*A 0.887 m^2.

**Provides to (outbound):**
- Interpolated transforms of active physical entities → [rendering](rendering.md).
- **`FVesselOrbitalState`** (mode, dominant frame, elements, true pos+vel, predicted path + next SOI crossing) → [gameplay](gameplay.md) (map/maneuver UI — we compute, they draw), [persistence](persistence.md) (save = ~10 doubles), [networking](networking.md) (replication; pure time-addressable conic + fixed-step integrator = determinism-friendly per D-004). *New outbound contract — flagged to Admin (spike2-physics §8.2).*
- Contact/landing events, structural state → gameplay.

**Negotiated with world-gen (RC-3, settled PH-5):** analytic queries adopted; **one non-breaking additive request** — add `SlopeRad` + `SurfaceHardness` to `FTerrainContact`/`FTerrainHit` (spike2-physics §5.2). Append-only, no caller breaks. Baked collision meshes deferred (Phase-1 perf option, contract already supports it).

## 6. Task backlog / roadmap
- **[Phase 0 / Spike 2]** ✅ **Designed** — [spike2-physics.md](../spikes/spike2-physics.md): patched-conics propagator (universal-variable Kepler), on-rails↔active conversion (the four `ISimProxy` hooks, no-drift guarantee), one rigid-body craft (hybrid integrator), RC-4 hybrid resolution, RC-3 analytic-query resolution, full launch→orbit→coast→SOI→land build plan + gates G1–G11.
- **[Phase 0 / Spike 2 · PH-Build 0]** ✅ **BUILT + GREEN (pure-CPU headless core)** — `core/include/of/orbital.h` + `core/tests/test_physics.cpp` + `physics_tests` CMake target (PH-6). Covers PH-Build 0 steps 1 (`FConics` round-trip, all conic types solve, 1-period return), 2 (analytic advance), 3 (custom symplectic integrator + RC4-A energy-bounded), and 5 (G6 park/resume identity). Gates retired at the math level: **G1** (conic correctness), **G3** (integrator vs analytic, energy bounded), **G6** (park/resume no-drift, elliptic + hyperbolic). Steps 4 (predictive SOI root-find) and PH-Build 1/2 (G4/G5/G7–G11) remain — they need the UE5 project / core-engine Wave-1 skeleton.
- **[Phase S / vessel programme]** ✅ **BUILT + GREEN (headless, `core/` only)** — `vessel.h` + `atmosphere.h` + `flight.h` + `test_vessel.cpp` (338 checks) + `test_flight.cpp` (843 checks). 24/24 ctest, parity 106/106 + 119/119 unchanged (no wasm touched). Decisions PH-10 to PH-19. **Acceptance: the reference rocket flies the pad to an 86.9 x 75.5 km orbit, e 0.0083, period 1880 s, with 1200 m/s and 630 kg of propellant left**; staged at 69.58 s, the burn time the hand computation predicted; max q 27.3 kPa at 7.8 km; max AoA 4.43 deg under load; SAS tracking error 4.35 deg under load.
- **[Phase S / browser flight] BUILT + GREEN (`probes/ascent.js`, PASS)** - the client half of the vessel programme. New: `web/src/sim/` (`FlightAbi`, `FlightSession`, `FlightReport`, `FlightAttitude`), `web/src/player/` (`ViewRouter`, `VesselObserver`, `FlightControls`, flight bindings), `web/src/render/` (`VesselView`, `VesselPlume`), `web/src/ui/` (`Navball`, `NavballDraw`, `navball.css`), `web/src/app/` (`FlightMode`, `DebugFlight`). **NO `/core` change and NO ABI bump: the bridge was already complete at ABI 6 and every `of_fl_*` export was used as shipped.** Decisions PH-20 to PH-27. **Acceptance, driven, sandbox: the reference vehicle is assembled in the bay (4922.91 m/s, 9845 kg, statically stable), a drill and a smelter are built on an ore patch, the player WALKS 27.9 m to the rocket and boards it with a REAL DOM keyboard event, the clamp refuses to release an unlit rocket at full throttle, staging lights the engine at pad TWR 1.88 (the fixture's 1.6567 lapsed for a pad 3 km up), it leaves the ground, stages at 69.7 s (against /core's 69.58 s), max q 17.8 kPa, cutoff at 107.2 s and 1548 m/s, circularises leading apoapsis by half a burn, and reaches 85.9 x 79.7 km, e 0.0045, period 1886.5 s, with 1504 m/s left.** Against `/core`'s own reference ascent of 86.9 x 75.5 km, e 0.0083, period 1880 s, 1200 m/s left. **The factory produced throughout: mined 40 -> 525 -> 1031, ingots 13 -> 186 -> 365.** Then it deorbits, arrests at the surface, the player gets out and the world is still coherent.
- **[Phase S / W11 lane G] ADVERSARIAL VALIDATION, DONE.** New: [`web/src/sim/SaveInhibit.ts`](../../web/src/sim/SaveInhibit.ts) (PH-30), [`web/src/sim/FlightWarp.ts`](../../web/src/sim/FlightWarp.ts) (PH-34), [`web/tools/smoke/probes/flightabuse.js`](../../web/tools/smoke/probes/flightabuse.js) (do the wrong things), [`web/tools/smoke/probes/flyto.js`](../../web/tools/smoke/probes/flyto.js) (fly to a named phase and stop), [`web/tools/smoke/reload.mjs`](../../web/tools/smoke/reload.mjs) (**a second runner: it reloads the BROWSER at a phase, in the same context, so IndexedDB survives exactly as it does for a person pressing F5** — `run.mjs` drives one page load and structurally cannot ask what a player gets when they come back). Decisions PH-28 to PH-36.
  **The headline is the SPREAD, not the best run. Ten cold pad-to-orbit-to-ground ascents, 10/10 pass, and NINE OF THE TEN ARE BIT-IDENTICAL:** apoapsis min 92.589 / p50 92.589 / max 92.918 km, periapsis 79.693 / 79.693 / 79.812 km, e 0.00940 / 0.00940 / 0.00955, delta-v left 1587.6 / 1588.5 / 1588.5 m/s, max q 14421.5 / 14421.5 / 14423.1 Pa, staged 69.7 s, cutoff 112.3 s at 1712 m/s, factory mined 35 -> 367 and ingots 12 -> 132 in every run. **Draw calls were 36 in all ten**; frame p50 1.60 to 2.10 ms, p95 2.00 to 4.10 ms; wall clock 76.4 to 81.7 s. The single deviation is one run's circularisation cut landing one probe step later on the eccentricity minimum, which is **0.36% of apoapsis and 0.9 m/s**. Determinism is not a bonus here, it is the demo risk retired: a flight that varies run to run is a flight that will fail in front of somebody.
  **Per-phase render cost, which the W9 acceptance only measured at two points:** pad 70 draw calls / 455k triangles (the near scene is full of terrain, props and the base), mid-ascent at 20 km 58 / 131k, just after staging at 29 km 40 / 128k, orbit 36 / 116k. The budget is 150 and the alert is 300, so the whole flight sits between a quarter and a half of it.
  **`flightabuse.js` is the deliverable that matters more than the fixes.** It mashes the stage key on the pad with the throttle shut, throttles up under the clamp, presses Escape, Tab, V and the left mouse button in flight, opens the ASSEMBLY BAY at 20 km and closes it again, presses the board key while the bay is open, tries to climb out at altitude, points retrograde under power, mashes the warp ladder to its top and back, aborts the ascent, stages a coasting vehicle, slams the throttle, rides the whole re-entry at 1000x without touching a key, and then checks the player can get out, walk and dig. **Every row records what the state was before and after, so a run reports the whole table instead of stopping at the first defect, and a row that breaks nothing but leaves the player STUCK still fails.**
- **[Phase S / W12] THE MAP, THE MANEUVER NODE, THE SAS MODES AND THE FLIGHT CAMERA. BUILT + GREEN.** New in `/core`: [`maneuver.h`](../../core/include/of/maneuver.h) + [`test_maneuver.cpp`](../../core/tests/test_maneuver.cpp) (**242 checks, 9 tests**) + a `maneuver_tests` target; `orbital.h` gained `Basis`/`basisAt`; `flight.h` gained four `SasMode` values. New on the bridge at **ABI 11** (atomic, and the commit boots): [`web/wasm/of_maneuver_api.inc`](../../web/wasm/of_maneuver_api.inc) with `of_mn_plan` / `of_mn_path` / `of_mn_orbit_meta`, and `of_fl_set_sas` widened from 0..4 to 0..8. New in the client: `web/src/sim/ManeuverAbi.ts`, `web/src/ui/{MapTypes,MapView,MapDraw}.ts` + `styles/map.css`, `web/src/app/{MapMode,MapBoot,DebugMap}.ts`, `web/tools/smoke/probes/maneuver.js`. Decisions PH-37 to PH-43.
  **The node maths was pinned to a HAND COMPUTATION done on paper before any of it ran**, the way the vessel lane pinned Tsiolkovsky: `v_circ` at r = 680 km is **2278.931638 m/s**, `v_peri` of the 680 -> 800 km transfer is **2369.520287**, so the burn is **90.588649 m/s** and it must produce an **80 x 200 km** orbit; on the Ascender's upper stage (m0 **3705 kg**, Isp 360 s, mdot **16.9953 kg/s**) that is **5.5227 s** of burn and a **2.7614 s** lead. `test_maneuver.cpp` asserts all six, then re-derives the propellant independently and checks that spending a whole stage's delta-v takes exactly `StagePerformance::burnTimeS`, which vessel.h computed by a different route.
  **THEN IT WAS FLOWN, which is the assertion that matters, because a node that predicts an orbit you cannot fly to is worse than no node.** The plan is handed to `FlightSim`, SAS holds the node's own published direction, the throttle opens at `timeToBurnStartS` and shuts `burnDurationS` later, and the orbit that comes out is compared with the orbit that was predicted: **apoapsis 199,980.6 m against a predicted 200,000 (19.4 m, 0.0097%), periapsis exact to 0.0 m, period within 1 s**, with the delivered impulse 90.577 against 90.589 m/s. The tolerance is the measured residue plus headroom and is stated as a fraction, never tuned.
  **Driven in the browser, at orbit, on a freshly built bundle:** M opens the map (a real key press through `Input.act`), the panel draws **192 current points and 192 planned points** with markers `body, air, ap, pe, ap-plan, pe-plan, node, ship, scale`, and a node placed at apoapsis with 90 m/s prograde reads **dV 90.0 m/s (green), have 1588 m/s, burn 3.6 s, LIGHT IT IN 16:35, node in 16:37, result AP 199.5 km, result PE 92.4 km**. The node marker appears on the navball (`markersDrawn` gains `node`, and loses it again on clear) and the hold-node KEY (Digit 8) puts SAS in CMD on the node. Camera: `looks` 0 -> 8 on a driven mouse move (yaw 0 -> 45.8 deg), `zooms` 0 -> 6 with the distance clamping at both published bounds (6 m and 150 m), and `V` flips to a cockpit with the hull hidden. Screenshots `docs/screenshots/W12_map_node.png`, `W12_cockpit.png`.
  **Gates: 30/30 ctest** (was 29/29, +`maneuver_tests`), self-determinism **126/126**, cross-toolchain exact **108/108**, `npm --prefix web run check` green at **190 files**.
- **[Phase R / R18] THE WALKER CAN NO LONGER FALL OUT OF THE WORLD. BUILT + GREEN (PH-60 to PH-63).** New: [`web/src/player/RockEscape.ts`](../../web/src/player/RockEscape.ts) (the two rules), [`web/src/player/StructurePort.ts`](../../web/src/player/StructurePort.ts) (the structural port lifted out of `VoxelCollision.ts`, which was over its cap: boxes on a 1 m LATTICE and boxes in ROTATED frames are different geometry and were sharing a file), [`probes/burylevel.js`](../../web/tools/smoke/probes/burylevel.js) (the depth sweep with no seed in it), [`probes/burieddiag.js`](../../web/tools/smoke/probes/burieddiag.js) (the entry-tick measurement), [`probes/wallstuck.js`](../../web/tools/smoke/probes/wallstuck.js) (the wall-embed drive). Changed: `VoxelCollision.floorBelow` returns a `FloorAnswer`, `KinematicBody` reads it, `StandTrace` gains `buried` and `ejectM`, `mtnfall.js` gains a fourth check that does not depend on the floor ever being lost.
  **The headline is that the same defect has TWO routes and only one of them needed a seed.** `mtnfall.js` seed 991733 goes from three named failures (**4624 ticks with 0 grounded, 0 pushes, 29,208 m down**) to **4/4 green with `lostFloor: false`, the last grounded tick 5975 of 5976, and the player still in their cave 27.625 m under the surface**. The trace shows the buried state is entered **126 times in that one drive** and every one is back on footing within a second, with **0 ejects**: rule 1 owns all of them. `burylevel.js` reaches the identical state on flat ground with the levelling key, and there rule 2 owns it: **one eject per burial, of 2.214 / 3.004 / 5.004 / 8.004 / 12.004 m against raises of 2 / 3 / 5 / 8 / 12 m, then 600 of 600 grounded ticks at 0.000000 m of spread.** The same probe on the pre-fix build fails six checks with the player **728 to 738 m** below the surface and a **701.016924 m** spread in the ten-second window, at every one of those depths.
  **Both builds stamp `3b486f9+dirty`** and BT-27 cannot tell them apart, because five other lanes have uncommitted work in this checkout; the discriminator for every number above is the port, `:5457` before and `:5459` after, and that is stated rather than papered over.
  **REGRESSION SWEEP, eleven walker and terrain probes, before and after, with every failure re-run on the BEFORE build before being called a regression. NOTHING GREEN ON MAIN IS RED AFTER.** `tunnelsink` **21/21 at both sites, spread 0 m, 0 snap-ups, 0 of 600 ratifying ticks**, and its negative control (`oldFloor`) still fails **9 of 21** with a 0.2615 m sink and 600/600 ratified, so the probe still tests something; `mtnstand` after a REAL browser reload with 267 cells restored from IndexedDB, **spread 0 m over 3000 ticks, 3000/3000 grounded and under rock**; `mtncave` 442 cells over 44 jittered strikes, **18 stand points and not one of them moved the feet** under 33.3 m of rock; `decksink` 300/300 ticks with the deck winning and the feet 0.000002 m off its top face; `jump` apex **0.862 m** / airtime **0.833 s**, bit-identical before and after; `walkfeel` ratio 0.998 against 1.000 with the stall count unchanged at 18 (different leg lengths, so it is not a like-for-like); `tunnelwalk`, `mtnbore`, `dig`, `walk` (376.5 m, 100% grounded) all green. **Two probes are red on main and were red before this work**: `tunnelmouth`'s `sheetTracksFieldOverDugGround` (the chunk sheet stands 4.04 m above the field over dug ground, byte-identical on both builds, and it is a rendering question), and `basereal`'s phase 2 "never got onto a deck", which I re-ran on the before build myself and which fails there identically (90 parts, phases 0 and 3 flat at spread 0 on both). `basereal` was green at PH-47 four hours ago, so something in the live build or placement lanes broke it since; **it is not this lane's and it is flagged rather than chased.**
- **[Phase S / vessel persistence] A VESSEL SURVIVES NOT BEING THE FOCUS, AND AN UNATTENDED ONE MOVES BY ARITHMETIC. BUILT + GREEN (PH-64 to PH-70).** New in `/core`: `vessel_on_rails_determinism` in [`test_physics.cpp`](../../core/tests/test_physics.cpp). New on the bridge at **ABI 18** (§13.3 of [`of_flight_api.inc`](../../web/wasm/of_flight_api.inc)): `of_fl_set_propellant`, `of_orb_park`, `of_orb_resume`. New in the client: [`web/src/sim/VesselRegistry.ts`](../../web/src/sim/VesselRegistry.ts) (the authority), [`web/src/app/FlightVessels.ts`](../../web/src/app/FlightVessels.ts) (demote / promote / sync / `releaseControl`), [`web/src/game/VesselSave.ts`](../../web/src/game/VesselSave.ts), [`web/src/app/PlayerAnchor.ts`](../../web/src/app/PlayerAnchor.ts), [`web/src/app/ResumeBoot.ts`](../../web/src/app/ResumeBoot.ts) (boot order, and §5 is the published handoff seam). New probes: `probes/vesselrails.js` and the `vesselreload.mjs` runner. Decisions PH-64 to PH-74. **Regression sweep on this build: `ascent` PASS, `flightabuse` 14/14 (its two save rows INVERTED rather than deleted: they asserted PH-30's refusal and now assert that the autosave HAPPENS while strapped in and that what it wrote is a real record with a design and its fuel, which is strictly stronger and fails against the old build), `maneuver` 64/64, `clamprestore` 6/6.**
  **THIS IS THE FOUNDATION AND DELIBERATELY NOT THE FEATURE.** There is no control handoff and no re-entry UI; the seam they need is published in `ResumeBoot.ts` §5 and every name in it is exercised by a probe, which is the difference between a seam and a hope.
  **The measured properties are recorded in the R-items and in PH-65; the headline is that the analytic path is EXACT where it claims to be and small where it can only be close.** Path independence is asserted with `==` rather than a tolerance, in `/core` and again in the browser; the rails answer against a genuinely flown 60 s of integration is **1.0e-4 m** in `/core` and **6.5e-5 m** in the browser; and a reload round-trips a vessel's position to **0.000e+00 m** in all three states, because the clock is stamped and the world was not running.
- **[Phase S] DEFERRED, and the next lanes need to know** (see §7 R5 to R9): no WASM bridge for any of this (out of scope by instruction); no vessel↔terrain contact, so landing legs, touchdown and the launch clamp are unbuilt; no docking logic (only the capture cone as data); no electric-charge budget (solar/battery are data, nothing consumes them); no parachute deploy dynamics; no multi-part structural flex (a vessel is a compound rigid body, R1); no reaction-wheel electricity cost; body rotation is carried as `FlightEnvironment::bodySpinRadS` and is 0 because `BodyParams` has no rotation rate yet.
- [Phase 1] Full per-part aerodynamics + atmospheric entry (Spike 2 ships single-`Cd·A` only); surface vehicle physics; baked-mesh collision option if contact-query volume bottlenecks (PH-5 deferral).
- [Phase 1] Multi-part flex joints beyond the spike's compound-rigid default (KSP wobble mitigations 1/3/4 are built+stubbed in the spike).
- [Phase 3] Cross-platform determinism verification of the custom integrator with networking (D-004).

## 7. Open questions & risks
- **R1 (mitigated, design-level):** Joint/wobble for multi-part craft. Spike 2 default = **compound rigid body** (sidesteps it entirely for the spike flow); flex joints use stiff 6-DOF constraints + fixed sub-stepping + strain-free promotion (spike2-physics §3.3). Validated by gate G10. Residual risk moves to Phase-1 multi-part craft.
- **R2 (resolved — math-level no-drift now BUILD-VALIDATED, PH-6):** on-rails↔active handoff smoothness — **no-drift guarantee** via lossless conic fit on demote + strain-free re-seed on promote (spike2-physics §2.2, gate G6). The *math-level* half is now demonstrated in `test_physics.cpp`: park→resume is identity on (r,v) for elliptic + hyperbolic, and the custom symplectic integrator tracks the analytic conic with bounded energy (max \|dE/E\|=2.4e-11 over 30 orbits) — secular energy-drift retired (G3/G6). The *UE5-integration* half (strain-free joint re-seed, the load-in-wobble seam) still pends the UE5 project (gate G6 in-engine).
- **R3 (Admin Q1):** time-warp semantics when other players have active vessels (MP) — deferred, networking-coupled. Unchanged by Spike 2.
- **R5 (PARTLY CLOSED, 2026-07-27, PH-20):** the PAD end is solved with a client-side launch clamp and needs no `/core` change. **The LANDING end is not, and is deliberately not half-built:** `DOWN` freezes the state at the surface, which is an arrest and not a touchdown. Landing legs, gear compression, friction, tip-over and impact tolerance remain unbuilt, and `LandingLeg::legReachM` and the `Leg_Deploy` clip remain data. A vehicle that comes down survives, which is generous, and nothing about the way it comes down is modelled. That is the next physics milestone and it is where the world-gen contact query (PH-5's `GetGroundContact`) finally has to be wired to a vessel. Original text: the vessel core has **no contact with the ground**. It flies and it orbits; it cannot sit on a launch pad, cannot be held by a clamp, and cannot land. `LandingLeg::legReachM` and the shipped `Leg_Deploy` clip exist as data. This needs the world-gen contact query (PH-5's `GetGroundContact`) wired to the vessel, which is a real piece of work and is the gap between "reaches orbit" and "completes a mission".
- **R6 (NEW, for gameplay):** the guidance that flies the acceptance ascent lives in the TEST, deliberately, because DW-29 makes reaching orbit a manual skill and autopilot a research unlock. The core publishes the ribbon (`GravityTurnProgram`) and the angle-of-attack clamp; the pilot is somebody else's. Three guidance defects found by the integrated ascent are recorded in the test where they bit (clamp-from-the-pad lock, the periapsis-catches-apoapsis cut that can never fire, and starting the circularisation burn AT apoapsis instead of leading it by half a burn). **Whoever builds the autopilot should read that driver before writing another one.**
- **R7 (NEW):** the residual eccentricity of the acceptance ascent (0.0083) is the GUIDANCE, not the physics: a pure prograde burn centred on apoapsis leaves a few percent, and real ascent guidance pitches slightly below prograde near the end to take the rest out. That belongs behind the autopilot unlock.
- **R8 (NEW, for the art lane):** one of the 24 authored parts has no mesh, `EngineVacuumSmall` (§5.1), a 1.25 x 1.00 m vacuum bell. It obeys the existing class-S contract. **The rest of the catalogue was realigned onto the art lane's names and dimensions mid-task** (see the reconciliation note in §5.1); if `build_rocket_parts.py` moves again, `vessel.h`'s `asset` strings and heights move with it, and that reconciliation should be a named job rather than something two lanes each half-do.
- **R9 (NEW, for Admin):** the ITEM form of a part (what a recipe costs, what sits in the pack) is an `ItemId` and belongs to `gameplay.h`'s pinned table. `vessel.h` deliberately has no opinion about what a part costs. Somebody has to allocate that block.
- **R18 (CLOSED, 2026-07-27, PH-60 to PH-62).** The mechanism the earlier pass diagnosed was confirmed by driven measurement at the entry tick before anything was changed, and then found to be reachable on flat ground with the levelling key, which is what made the fix an authority statement rather than a widened rung. `mtnfall.js` is green on all four checks (the fourth is new and does not depend on the floor ever being lost); `burylevel.js` sweeps the burial depth and is green at 2, 3, 5, 8 and 12 m and red on the pre-fix build at every one of them. **The one open piece is R20.** Original text: **A CAPSULE BURIED IN BEDROCK OWNS NOTHING AND FALLS OUT OF THE WORLD, PERMANENTLY.** `VoxelCollider.floorBelow` deliberately returns null when the feet are buried past one rung with rock all the way up, on the grounds (WG-31) that this is not a floor question but an embedded capsule, and that `resolveEmbedded` owns it. **`resolveEmbedded` does not own it in general**: it returns null whenever no single-axis push of at most one cell leaves the whole capsule free, which is exactly the case for a capsule inside solid rock. Neither function is wrong on its own terms; together they own nothing, and the walker then integrates gravity into bedrock for ever. **Measured, deterministic, `probes/mtnfall.js` seed 991733:** the player is grounded on a dug floor at tick 1351, `groundR` goes to -Infinity at tick 1352, and over the following **4624 ticks there are 0 grounded ticks, 0 floors and 0 pushes**, ending **29,208 m below the surface** at 12.83 m per tick and still accelerating. `of.solidAt` reads true continuously from 0.5 m to 100 km deep, so this is a fall THROUGH rock and not down a shaft. Found by `probes/mtncave.js`, which digs with a jittered aim the way a person does; 1 of 3 seeds hit it, and the clean single-yaw bore every other probe cuts never can. **NOT FIXED, deliberately:** the obvious repair is to make an unfreeable capsule freeze rather than fall (rock you cannot be pushed out of is rock you cannot fall through), which is one line and is an authority statement rather than a damping term, but it is a walker behaviour change in a state reached once on one seed, and this defect class has already been closed twice on a hypothesis. It wants its own brief. `mtnfall.js` ships reporting `valid: true, pass: false` with three named failing assertions, which is `tunnelmouth.js`'s split and is how the fix gets held to account.
- **R19 (NEW, for whoever picks up the sinking complaint next): WHERE THE SINKING DEFECT IS NOT.** PH-45 rules out, by measurement at a real mountain site: the kinematic capsule, `floorBelow`, `resolveEmbedded`, the `deep` gate, `editsHandle` flicker, the first-person eye, the third-person spring arm, the drawn chunk sheet under the feet, the chunk stream, the stitch pass, the floating origin, the render rate and the reload path. **ALL THREE OF ITS LEADS ARE NOW CLOSED BY PH-47**: player-built structures (90 parts, four standing states, 0.000000 m), the real rAF loop (1440 and 1436 ticks at 121.5 and 100.8 Hz render, 0.000000 m) and the 2.0 s `Idle` clip (continuous, no period, 44 of 48 tiles pixel-identical). The two deck gaps this risk originally carried were re-derived and do not hold as periodic mechanisms; what survives of them is a single real defect of the SAME SHAPE as R18, recorded here so it is not lost: **a capsule embedded in a 4.00 m structural WALL is stuck**, because `deckUnder` correctly refuses to lift it a whole storey and there is no structural counterpart to `VoxelCollider.resolveEmbedded`. **THAT CLAIM IS WRONG AND WAS RETRACTED BY DRIVING IT (PH-63): the player walks out, 8.926664 m in 2 s in all four directions, because `StructureBody.resolveStep` has an escape hatch the audit never exercised.** I relayed it without re-deriving it, which is the same mistake the audit made one level down, and it is the second claim from that audit to fail when somebody finally drove it. What the driving found instead is R20. **The honest state of the hunt: there is no longer a next place to look that this lane can name.** Everything on the walker's own side is measured flat under conditions strictly harder than Reid's. What is needed now is not another hypothesis but Reid's actual coordinates and view mode, because the defect is either in a state the probes cannot construct blind or in a quantity nobody has thought to instrument, and the cheapest way to find out which is to stand where he was standing.
- **R20 (NEW, and it is R18's shape in the structural port): THE EMBEDDED ESCAPE HATCH IS GLOBAL WHEN IT SHOULD BE PER-SOLID.** Both `VoxelCollider.resolveStep` and `StructureBodies.resolveStep` accept ANY destination once the capsule is not free at its start position, on the stated grounds that "the destination cannot be worse than here". **It can be worse.** Structurally, measured: a player inside one wall walks through every OTHER wall and every closed door at the full 4.6 m/s, `blockedByBuild` 0 on all 480 driven ticks, so one bad free-placement turns collision off for the whole base until they leave the box. In the voxel path the same hatch is what carried the R18 capsule sideways deep into the mountain while it fell: between the entry tick and 34 ticks later the feet dropped 1.506 m while the air above went from inside the 0.55 m rung to 30.14 m, which is how a recoverable 3.5 m burial became one with nothing free within 40 m. **PH-61 rule 1 closes the voxel consequence** (a tick that ends buried is refused, so the hatch can no longer carry anybody into bedrock) **and does NOT close the structural one**, which needs the hatch narrowed to "ignore only the solids you are currently inside". That is safe by construction (a solid you are not inside cannot be one you need to pass through to get out) but it is a behaviour change in `web/src/game/StructureBody.ts` during a live gameplay week, and it needs its own driven case: embedded in wall A, blocked by wall B, still able to leave. **Also unfixed and worth its own line: the state is SILENT.** `onDeck`, `grounded`, `blockedByBuild` and the whole stand trace read exactly as they do for somebody standing on open deck, so nothing in the client can tell that a player is inside a wall, and there is no signal to hang a warning or an auto-eject on.
- **R21 (NEW, for whoever touches the walker next): the DEEP branch has no `climbGate`.** Above ground a horizontal move onto ground more than `CAPSULE.stepUpM` above where the feet started is refused (PH-8). Underground there is no such wall: `floorBelow`'s "feet in rock, seat on the top of the slab within `riseM`" branch will lift the capsule by up to one rung EVERY TICK with no bound over time, which is 33 m/s of vertical ratchet. Measured on seed 991733, the four ticks before the R18 fall: the feet rise 0.084, 0.085, 0.127 and 0.317 m per tick while `fallM` stays a constant -0.002685, i.e. **the state that produced the double null was manufactured by `floorBelow` walking the capsule up into the hillside**, not walked into. PH-61 rule 1 makes the outcome safe (the tick it can no longer find a slab top is refused rather than fatal), so this is now a feel defect rather than a fall, but a walker that can climb a vertical rock face at a rung a tick inside a tunnel is not what PH-8 decided anywhere else.
- **R4 (RC-4, RESOLVED → PH-4 hybrid):** Chaos-vs-custom for the active vessel. Resolved: custom fixed-step integrator for flight dynamics, Chaos for contact only (fallback: fully-custom analytic contact). Confirmation gate = G8 (= Spike-1 V4, no kraken at 3 km/s). Only escalates to D-001/Admin if near-origin low-speed contact itself fails — which the hybrid is designed to avoid.

- **R10 (STILL OPEN, for rendering/world-gen):** `Regime`'s `ORBIT_START` is **100 km**, but a stable orbit on Forge starts just above the 60 km atmosphere ceiling, so the whole demo orbit (92.6 x 79.7 km) sits in the **ASCENT** band. Nothing is broken by it: the near scene empties anyway (0 near chunks, 102 far) and `probes/ascent.js` asserts the HANDOFF rather than the band NAME. But a band called ASCENT while the player coasts in a closed conic is a label that will mislead the first gameplay gate that reads it, and it is not cosmetic in one place already: `Systems.ts:230` switches the shadow pass off on `band !== 'ORBIT'`, so from an 80 km orbit the client is still fitting and rendering cascades for a surface that is 80 km away. **Recommended value: `ORBIT_START = 62_000`, just above the atmosphere ceiling, because that is the altitude at which the physics stops acting on the vessel (`atmosphere.h`'s ceiling, PH-11) and it is therefore the same boundary `onRailsEligible` already uses.** `Regime.ts` is world-gen's file and that lane is live in `web/src/world`, so this is raised for Admin to route rather than changed here.
- **R11 (CLOSED, 2026-07-28, PH-64 to PH-67).** The ABI dependency it named was the whole blocker and it was exactly right about what it would cost to skip: "restoring by replaying `stage` calls gets the right hardware and **full tanks**, i.e. free delta-v on every reload". ABI 18's `of_fl_set_propellant` is the one export it asked for, and the restore path is `refillTanks`'s own skeleton with the fuel written back. Measured across a REAL browser reload in all three vessel states, fuel round-tripping with a difference of **exactly 0** at 6490.0 kg on the pad, 3056.955416307 kg mid-ascent and 923.002605375252 kg in orbit, the last two strictly below the full load so "the fuel came back" cannot be satisfied by a rocket that never burned any. PH-30's save refusal is retired with it, which was also load-bearing rather than tidy: with the refusal in place no autosave can fire in orbit and the orbital reload could never have been proven. Original text: flight is still **not persisted**, but it is no longer LOST SILENTLY. The world save is refused while aboard, the refusal is counted, and the navball carries a standing amber chip. **What real persistence needs, and why it was not done tonight:** a vessel is its tree plus per-part propellant plus `nextStageIndex`, and **the bridge has no propellant setter**.
- **R11 original detail (kept for the argument, not for the status):** flight is still **not persisted**, but it is no longer LOST SILENTLY. While the player is aboard the world save is refused, the refusal is counted (`__of.game().persist.saveInhibit`), and the navball carries a standing amber chip. Measured end to end by `tools/smoke/reload.mjs --phase=orbit`: at 92.6 x 79.5 km, **8 saves refused against 1 allowed**, and the reload comes back with 2 buildings, 1 link, the hotbar restored, no phantom vessel and no NaN. **What real persistence needs, and why it was not done tonight:** a vessel is its tree plus per-part propellant plus `nextStageIndex`, and **the bridge has no propellant setter**. `of_fl_*` publishes `set_pos_vel`, `set_attitude`, `set_ang_vel`, `set_throttle`, `set_sas`, `set_sas_command` and `stage`, and nothing that can put a half-empty tank back. Restoring by replaying `stage` calls gets the right hardware and **full tanks**, i.e. free delta-v on every reload, which is exactly the class of "looks right and is not" this project keeps paying for. So it needs an ABI bump (rule 9, atomic, ABI is at 8 tonight and another lane moved it twice), which is not a 3 a.m. move with five lanes live. **The field is additive and optional, so `SAVE_VERSION` does NOT need to go to 6 for it and existing worlds are not invalidated.**
- **R27 (NEW, and it is a CORROBORATION plus a warning): `/core` ALREADY HAD THIS DESIGN, in [`sim_world.h`](../../core/include/of/sim_world.h), and it has never been wired to the browser.** `SimWorld` carries `VesselMode::{Active,OnRails}`, `parkToRails()`, `promoteToActive()`, `makeActiveFromCurrentState()` and a `step()` whose on-rails branch is `state_ = orbital::resume(elements_, nextTime)`. **It keeps `elements_` fixed and moves only the time asked for, which is PH-65's rule arrived at independently in Wave 1**, and that agreement is worth more than either statement alone. Two differences, both deliberate on this lane's side. **(1) It advances per TICK; this lane PULLS.** `SimWorld` calls `resume` every tick whether anyone is looking or not, which is a Kepler solve per vessel per tick for a fleet nobody can see; the client evaluates on demand and costs exactly zero when nothing asks. **(2) It re-parks on an SOI change** (`switchVesselToFrame`), and that is the ONE legitimate re-fit and the named exception to PH-65: mu changes, so the old elements describe a gravity the vessel no longer feels. When SOI transitions reach the browser, that is the shape to copy and it must be the only re-fit anywhere. **The warning:** two implementations of "a vessel on rails" now exist in this repo, one headless and unwired and one in the client, and a third would be the failure this project has paid for five times. The right eventual move is one of them, and it is not obvious which: `sim_world.h` is the fuller model (it owns the frame graph, the SOI crossing and the factory in the same tick) and it is also 100% unreachable from the browser, with no bridge export and no caller outside `core/tools/journey_dump.cpp` and two tests. **RESOLVED 2026-07-28 by Admin: the wired one wins.** `sim_world.h`'s vessel-rails API is now marked SUPERSEDED at its symbols (PH-72); the header is kept for the coverage only it has. Build on the client's. Note also what the brief for this lane assumed and what is actually there: FS-4's `Demote`/`AdvanceOnRails`/`Promote` is not a framework, it is three concrete methods on `factory::FactorySim` with no interface, no base class and no concept; `ISimProxy` exists only in the spike documents. So "compose the two halves" meant adopting the CONTRACT and the vocabulary, not calling shared code, and no such shared code exists to call.
- **R28 (CLOSED 2026-07-28 by PH-75, driven against a HEAD archive; both modes exit 0 and the two held-back assertions are now asserted from measurement. Kept for the record): `reload.mjs`'s new `leave` branch had never been RUN.** PH-74 split the three pad-occupancy rows by mode and the `recover` side is unchanged and still covered, but the `leave` side was written and syntax-checked only. Two reasons it could not be driven: the working tree does not build (`TerrainBoot.ts` imports a `WaterSurface.js` that does not exist, another lane's in-flight edit) and the disk was at **267 MB**, which is not the headroom to start a browser in after this lane already took the machine to zero once. The command is `node tools/smoke/reload.mjs --url=http://127.0.0.1:<port>/ --setup=probes/padclear.js --setupargs='{"mode":"leave"}'` and it wants a run as soon as the tree builds. **Two assertions were deliberately left OUT of that branch and should be added by whoever runs it first, from the measurement rather than from a guess: `flightLive` and the pad's `holding` across a `leave` reload.** Both are unmeasured, and the reason they are absent instead of guessed is PH-73's lesson: a bound written from an assumption asserts the assumption, not the behaviour.
- **R29 (NEW, LOW severity, and it is PH-71's shape in a place PH-71 deliberately does not reach): a vessel resumed in ORBIT reads `massKg 0` and `altitudeDatumM 0` for one frame.** Measured at the instant of `resumeControl`: `steps 0, massKg 0, altitudeDatumM 0`, while `speedMS` is already a correct 2349.22. It self-heals on the first tick because the player is now aboard and `ViewRouter` steps it (measured `steps 1 -> 121` over 2 s, `massKg 2398.3`). **PH-71's fix cannot be reused here**: it primes with `stepClamped`, which writes the pad pose back, and doing that to an orbiting vessel would zero 2.3 km/s to make a gauge read. The honest options are a one-tick genuine `of_fl_step` at promote (legitimate, since the vessel IS active again) or having the HUD suppress a zero-mass frame. Not fixed this pass because it is one frame, self-healing, and the handoff was the scope.
- **R30 (NEW, LOW, and it is a NAMING trap rather than a bug): `report().boardings` now counts REMOTE seats.** `takeControlRemote` reuses `climbIn`, deliberately, so there is one code path for "the player is flying this". The consequence is that `boardings` goes 1 to 2 across a resume, so anything downstream reading it as "times the player walked up to a rocket" is now wrong. Nothing does today. Either rename it or split the counter before something starts.
- **R31 (CLOSED 2026-07-28: Admin rebuilt `web/dist` with a full `prebuild` so `sync-wasm` ran, and confirmed `escapeRock` is in the new bundle. Kept for the record): `web/dist` WAS a PRE-FIX bundle sitting on disk.** Built 2026-07-27 05:27, it contains the old clamp `Math.min(r, rr + 0.1)` and **no `escapeRock` at all**, so it predates GP-53 (09:19), WG-31 (15:24) and PH-60 (22:05) of the same day. Against that bundle the sink looks exactly as Reid describes it and never self-corrects. **The demo build on 4200 is NOT affected: it is `1b1d739` (2026-07-28 03:17) and `git cat-file` confirms `RockEscape.ts` and `escapeRock` are both in it.** But `web/tools/smoke/survival.mjs` still says Reid plays "from `dist-play`", a directory that no longer exists, so the harness's own account of what he runs is stale. **The cheap next step is to establish WHEN Reid saw this and against which bundle**, before any more hunting: if his report predates 2026-07-27 22:05 it is already fixed. Deleting or rebuilding `web/dist` would remove the trap entirely.
- **R32 (CLOSED 2026-07-28 by PH-80, and the answer is that it cannot be stood on BY CONSTRUCTION, not by accident: `DIG.radiusM` 1.5 makes the smallest cuttable cavity 3 m tall, so the reachable depth spectrum is {0} together with [3.1, 16.8] and any threshold in (0, 3.1) is equivalent. Kept for the record): the 1.5 m deep/shallow gate CANNOT BE STOOD ON.** Measured at the bore mouth: the roof goes from **3.972 m to 0 in a single 0.46 m step**, so there is nowhere in a bored tunnel where the roof is stably around 2 m. A probe hook aimed at that gate never landed and is reported as `landed: false` rather than quietly dropped. If that threshold matters, it needs a scene built to hold the roof at the gate height deliberately, which a bore will not do by accident.
- **R33 (NEW, and it is NOT mine, but it was measured here so it should be written down once: A MACHINE IS NOT A COLLIDER AT ALL.)** `Machines`/`Factory` never call `Structures.adopt`, so a smelter, assembler or belt never enters `StructureBodies`. Measured: the solid set went **5 to 5** across a placed machine. **A player can walk straight through every factory building in the game.** Nothing in this lane depends on that and the walker is behaving correctly given what it is handed; the question is whether factory buildings are supposed to be solid, which is factory-sim's and gameplay's call. Flagged rather than fixed.
- **R34 (NEW, and it is placement's line, not mine): A STRUCTURAL PART PRESSED INSIDE A TUNNEL APPEARS ON THE MOUNTAIN ABOVE YOU.** `StructurePlacement` addresses the build grid off `Structures.groundRadius` (`web/src/game/Structures.ts:242`), which is `_of_surface_radius`, the HEIGHTFIELD. A bore with an intact roof does not move the heightfield, so the part is placed at the surface overhead: measured 0.004 to 0.09 m from the overhead surface across four presses. **`probes/tunnelsink.js` now carries two CHARACTERISATION checks for this**, which assert the current behaviour so they fail the day it changes, deliberately WITHOUT claiming it is correct. Related and measured at the deep site: presses are refused about half the time with "ground too uneven here, the ground stands 1.95 m into it" rising to 5.55 m, which is the mountainside's own slope across a 4.00 m cell; cutting a 249-cell room around the player moved that by **0.01 m**, confirming the cause is the cell footprint and not the room.
- **R35 (NEW, and it is a GIFT FOR THE FACTORY LANE, measured here while proving my own instrument): [`FactoryStale.ts:66`](../../web/src/game/FactoryStale.ts) differences f64 against a float32 readback with no `Math.fround`, so its "hard zero" will read about 1e-5 m for any building far from the origin and will look like a small real defect.** A `BatchedMesh` keeps per-instance matrices in a float32 DataTexture, so `matrixAt` returns the f64 value ROUNDED: the error is **2^-23 times the distance from the origin** and is a property of the readback, not of the placement. This lane hit the identical thing on the pad (1e-6 m at 137 m, 4e-6 at 244 m, 6e-6 at 367 m) and the fix is to round the EXPECTATION the same way rather than to widen a tolerance. **RETRACTED 2026-07-28, and the retraction is the useful part.** This entry originally also claimed that `probes/factoryrebase.js:66` calls `mustNum(of.world(), 'rebaseM', ...)` against a field that lives only on `Config`, and therefore could not run. **That is FALSE.** Checked at HEAD `c5c680c`: line 87 reads `OF_ARGS.rebaseM ?? null`, line 61 is a comment correctly naming `Config.rebaseM`, and `mustNum(of.world(), 'rebaseM'` has **zero** matches in the file. The working tree is identical to HEAD there. The claim came from a stale read and I relayed it without checking it at the source, which Admin caught before it reached the factory lane. **The `FactoryStale.ts` half above stands and was verified in source.** The lesson, and it generalises: a subagent of mine verified that every file in its subjects' path was byte-identical to HEAD before claiming its clean-archive run was not a weaker result, unasked, and that is exactly the check that would have caught this. **Cross-lane findings must name the sha they were read at.** An instrument's stated blind spot is the list of claims that cannot have come from it, and a claim read from a stale file is the same failure with a different cause. Neither factory file was touched by this lane.
- **R36 (NEW, and it is a REAL defect found by PH-80's probe): THE DEEP/SHALLOW GATE CHATTERS ON A STEEP FACE, AND THE WALKER GOES BALLISTIC WHILE IT DOES.** On a 45 deg face: **8 flips in 152 ticks**, 19 of them airborne, worst flip-tick step **0.190239 m**, and two flip ticks ungrounded with the feet moving **0.002686 m, exactly one tick of free fall**. Bit-for-bit reproducible. **The cause is NOT the threshold and moving it would not help.** At a steep mouth `surfaceR` and the voxel floor differ by more than `CAPSULE.groundSnapM` (0.35 m), and `deep` is evaluated at the STEP TARGET, so sub-centimetre horizontal jitter flips it; on the shallow ticks nothing is within the walking snap so the walker is ballistic, and on the deep ticks `floorBelow` catches it again. **The real gap is that the shallow branch has no fallback when the heightfield is further away than its own snap.** In `KinematicBody.step`, untouched. Reproduce with `probes/deepgate.js --evalargs='{"sites":["hill"]}'`, which is an opt-in precisely because it is red.
- **R37 (NEW, and it is WORLD-GEN's line, measured here while building PH-80's scene): THE 80 m BEDROCK CLAMP IS NOT AN INSTRUMENT, AND A PLAYER CAN STAND ON CLAMPED GROUND OVER OPEN AIR.** Eight `level` presses took a pit floor to `base - 80`. The ninth dug **2,753 more cells 10 m lower** and the walker **did not move by one bit**: 599334.952412 before and after, grounded, depth 0. It is standing on the clamped heightfield with air beneath it. That is `surface_field.h`'s `surfaceHeight` clamp (`kSurfaceMaxFillM` / the -80 m bound), so the decision is world-gen's, not this lane's. Flagged, not touched.
- **R38 (NEW, URGENT, and it is a defect PH-81 INTRODUCED into the gameplay lane's file, so it needs routing rather than reaching): THE BOOSTER'S PLACEMENT GHOST IS NOW DRAWN 0.625 m INBOARD OF WHERE THE PART COMMITS.** `ghostOrigin` in [`VesselNodes.ts:258`](../../web/src/game/VesselNodes.ts) returns `node.posM` unchanged for a side node, which was a CORRECT restatement of `originFrom` before PH-81 and is now the mount-plane answer for an axis part. Measured and driven in the shipped bay; fin and solar panel are 0.000 m, so it is exactly the `Axis` parts. `attachNodes`'s pylon node computes its position with the same `pd.diameterM * 0.5`. **Both are about three lines once `radialOrigin` is published to the client, which R39 blocks.** Not touched: the gameplay lane is live in this file.
- **R39 (NEW, and it is the ENABLER R38 needs): `radialOrigin` IS NOT PUBLISHED TO THE CLIENT, because widening `of_vs_part_info` would silently empty the catalogue.** [`VesselCatalogue.ts`](../../web/src/game/VesselCatalogue.ts) reads it with `if (got !== PART_INFO_WORDS) continue`, an EXACT-STRIDE check, so going from 34 to 35 words without changing that constant kills every part in the bay rather than failing loudly. A two-line coordinated change, deliberately left to whoever fixes R38 so the two land together.
- **R40 (NEW, and it is the gameplay lane's evidence file, so it is theirs to invert): [`probes/radialsink.js`](../../web/tools/smoke/probes/radialsink.js) (GP-150) NOW FAILS BY NAME, EXACTLY AS DESIGNED.** Its three checks assert the defect EXISTS (`axis at 1.4 not 0.775`, `overlap -0.15 not > 0.4`, `touching would need the axis further out: 1.25 against 1.4`), and its own header says to invert section 2 rather than delete it. Left alone rather than edited mid-flight: a lane's evidence file is not something another lane should rewrite while they are in it.
- **R22 (NEW, for the map lane, and it is the natural next consumer): A DEMOTED VESSEL IS NOT DRAWN AND IS NOT ON THE MAP.** The registry publishes exactly what a map needs and nothing reads it yet: `registry.list()` enumerates every vessel, and `stateOf(M, registry, rec, tick)` gives one a position **without promoting it and without advancing anything**, which is precisely the property a map marker wants (it costs one Kepler solve per marker per draw, and zero when the map is shut). DW-36/DW-37's map already switches focus to orbital objects, so the marker and the selection gesture are the same work. Flagged rather than started because `MapDraw`/`MapView` are mid-migration to a 3D camera (R17) and a half-migrated projection would break the conic that works.
- **R23 (NEW, and it is a TESTABILITY gap rather than a defect): `flight('report')` publishes no state vector.** The probe lane had to read the live integrator's position off `map().focus.centreM`, which is gated on `aboard`, so **a promoted-but-unboarded vessel has no exact published position anywhere in the client**. That is why `probes/vesselrails.js` checks radius and speed plus a conic re-fit rather than a raw vector, which is a weaker assertion than the one it wanted to make. Three numbers on the flight report would fix it. Related and worth the same line: `flight('report').propellantKg` does not move while an engine burns, because it sums the cached `partRows` which `refreshParts` only rewrites on a roll-out or a staging; `FlightCheats.ts` documents this and the record's own `fuelKg` is a live read, so the save is right and the report is stale.
- **R24 (NEW, for whoever owns `test_flight.cpp` next): PH-16's recorded 0.62 m is STALE BY TWO ORDERS OF MAGNITUDE, and the bound that should have caught the drift is 900x too loose.** Reproducing PH-16's own scenario today (120 km circular, same vehicle, 20 ms, 102,132 steps) measures **0.00570342 m and 1.75437e-5 m/s** against the recorded 0.62 m and 0.0003 m/s, and `test_flight.cpp:641` still asserts `posErrM < 5.0`. The millimetre figure is what theory gives, which is why it is the one to believe: velocity-Verlet's relative frequency error here is `(omega*dt)^2/24 = 1.9e-10`, so one revolution slips 1.2e-9 rad and 680 km of radius turns that into about a millimetre. Either the integrator improved since PH-16 or that number was taken at a coarser step, and **nobody can now tell which**, which is the actual cost. A bound at 900x its own residue cannot fail against any regression a player would notice. Not changed by this lane, because tightening another suite's bound on a number this lane did not originally take is how a bound gets tuned until it passes.
- **R25 (NEW, and it is this lane's own blast radius): `reload.mjs`'s PADCLEAR block now asserts the OLD behaviour.** Three of its rows say a reloaded pad carries no vessel, a reloaded session carries zero parts, and the pad comes back HOLDING. All three were true and are now false BY DESIGN for `--mode=leave`, because a vessel left on a pad survives a reload. They must be split by mode rather than deleted: the `recover` run should still assert all three (a recovered vessel really is gone) and the `leave` run should assert the opposite, which makes the pair a stronger control than it was. `reload.mjs` is a shared runner with another lane's uncommitted GP-103 work in it, so this is flagged for whoever holds it rather than edited underneath them. `LaunchPadSave.ts:8-19`'s "THE CLAMP STATE IS DELIBERATELY NOT SAVED, because the flight the clamps were released for is itself not in the save" is stale for the same reason and should be re-argued rather than left contradicting the code.
- **R26 (NEW, for the control-handoff lane): the client can hold exactly ONE live vessel, structurally, and that is fine until docking.** `FlightMode.session` is `readonly`, `VesselObserver.flight` is `private readonly`, and `FlightSession.rollOut` destroys before it creates. The registry removes the constraint on how many vessels EXIST but not on how many are PROMOTED. Nothing in the handoff needs more than one promoted at a time (you fly one rocket), so this is not a blocker for it. It becomes one at DOCKING, where two vessels are in the same near scene and at least one of them has to be drawn and collided against while the other is flown. Naming it now because the fix is a constructor signature, and a lane that discovers it mid-feature will be tempted to reach for a second global instead.
- **R12 (CLOSED as far as physics can close it, PH-36):** the flight entrance is a **roll-out stand-in**, not DW-29's launch pad, and it now says so on screen in those words. DW-29's real entrance is a pad gated behind ground progression, which does not exist at any layer (gameplay R9); **the gate remains an Admin sequencing call and physics should not invent one.**
- **R13 (CLOSED, 2026-07-28, PH-68).** It said this was "a two-field addition to `SaveSlot` and it is not physics' file", and both halves turned out to be wrong in a useful way: it is four fields (`lat`, `lon`, `alt`, plus `aboard`/`vesselId` so a handoff can tell a flying player from a walking one), and it became physics' file the moment a vessel persisted, because a rocket in orbit above a body teleported to the spawn is not a save, it is two half-saves of different worlds. Measured after: lat and lon round-trip to **5e-15 degrees** across a real reload in all three phases, against R13's own measurement of going in at lat 1.613 lon 107.822 and coming back at 2.000 / 144.000. The one subtlety, recorded because it is the sort of thing that reads healthy: the anchor is taken off `ViewRouter.walker` and never off the router, because the router reports whichever source is LIVE and a save taken while strapped in would have recorded a body 80 km up. Original text: **the player's own POSITION is not in the save either.** `SaveSlot` has no player key and `Boot.ts:166` teleports to `cfg.scenario` on every boot, so a reload returns you to the spawn point wherever you were and whatever you were doing, flight or no flight. Measured: `reload.mjs` went in at lat 1.613 lon 107.822 and came back at lat 2.000 lon 144.000. The base, the tunnels, the pack and the hotbar all come back correctly; only the person does not. That is a two-field addition to `SaveSlot` and it is not physics' file.
- **R15 (NEW, and it is the finding the coordinator asked for): a REACHABILITY SWEEP of this domain's own published surface, because `look()` was the fourth built-and-unreachable capability found in a day.** Method: for every public method and every bridge export this domain owns, find the input path that reaches it. **Fixed this lane:** `VesselObserver.look()` (no caller at all), camera zoom and first person (never existed), the four new SAS modes and hold-node (bound the same commit that added them, and `sasNode` on Digit 8 was very nearly the fifth instance: it was in `BINDINGS` and in `MAP_ALLOWED` with nothing reading it until `Systems.ts` was wired). **Still unreachable and NOT fixed, stated plainly:** (1) **the `Parachute` is on the reference rocket, is paid for, and nothing deploys it** - there is no action, no `of_fl_*` export and no deploy dynamics, so the part a player buys for the way down does nothing on the way down; (2) **`LandingLeg` is data** (R5, the landing end is deliberately unbuilt); (3) `Battery`, `SolarPanel` and `DockingPort` are inert by design until there is an electric budget and docking logic; (4) ~~`FlightSim::park`/`resume` exist in `/core` and have **no bridge export**, so on-rails parking is unreachable from the browser~~ **CLOSED 2026-07-28 by PH-66, and note the shape of the fix: the exports added are `of_orb_park`/`of_orb_resume`, the PURE `orbital::` pair, and NOT the `FlightSim` members the risk named.** Exporting the member form would have given a flight handle the ability to park itself, i.e. a second answer to "is this vessel on rails" held inside the object whose retirement is the question. The risk was right that the capability was unreachable and wrong about which capability to reach for, which is worth recording: a reachability sweep finds the gap, it does not design the door. Warp remains physics warp only (PH-26, by design). **Bridge exports with zero client callers**, which are API surface rather than features and are listed so nobody mistakes them for either: `of_atmo_density`, `_density_raw`, `_dynamic_pressure`, `_g0`, `_in_space`, `_lapse`, `_pressure_ratio` (all computed inside `of_fl_step` and reported through telemetry), `of_fl_capture_hold`, and the `of_vs_*` duplicates of `of_fl_*` reads (`_assign_stage`, `_count`, `_fire_stage`, `_next_stage_index`, `_part_index_of`, `_propellant_aboard`, `_remaining_dv_vacuum`, `_stage_clear`). **The general form, and it is cheap: a capability is not shipped until a KEY OR A BUTTON reaches it, and the check is one grep per public method.**
- **R16 (NEW, for whoever owns `web/src/game/GameplayInput.ts`): one word.** PH-43 moved `mute` off `KeyM` so the map could have it, and `GameplayInput.ts:63` still flashes `'sound off  (M)'` / `'sound on  (M)'`. The key is now Backslash. The physics lane may not touch that file. **Backslash is a holding pen and not a design**: mute deserves a real home, and the right one may well be a settings panel rather than a key at all.
- **R17 (NEW, for Admin, and it lands ON this work): DW-36 expands the map from an orbital view into ONE continuous player-centred map with discovery, ore counts and focus switching.** Read as a delta against what shipped: the MODE, the modal registration, the per-panel allow list, the readout column and the node UI all survive; what does not is `MapDraw`'s assumption that the projection is centred on the BODY and framed by the conic. A player-centred, continuously zooming map wants the projection origin to be a parameter and the drawn layer set to be a function of scale. `MapTypes.MapScene` is where that lands and it is already the seam: it carries `planeU`/`planeV`/`spanM` and would gain a centre. **Discovery and ore counts are not this domain's**: a visited grid is world-gen/persistence and deposit pools are `deposits.h`, so the map becomes a consumer of two more publishers rather than a new authority. Flagged rather than started, because the coordinator asked for this lane to land first and because a half-migrated projection would break the conic that works.
- **R14 (NEW, cosmetic, for whoever owns the readout next):** on the pad and for the first seconds of a vertical climb the trajectory is very nearly RADIAL, so `e` is 1 to within rounding and `/core` classifies it as unbound: **AP and PE read `---` for about the first ten seconds of every launch**, which KSP fills in immediately. It is correct arithmetic on a degenerate conic and it is not what a player expects. A radial ellipse has a perfectly good apoapsis and computing it is a special case in `summarize()`, in `/core`, not in the client.

## 8. Subagent delegation log
| Date | Subagent task | Status | Outcome |
|------|---------------|--------|---------|
| 2026-07-28 | **Audit vessels and the flight path across a WALKED floating-origin rebase**, briefed with the scatter defect as the known instance, told that every probe on this project teleports and therefore cannot see it, told the instrument must be a SUBTRACTION with a hard 0.000000 and must refuse to pass a run that never crossed the threshold, and told that if a fix were needed it goes at the END of the existing rebase handler and NOT as a second `events.on` subscriber (because `Events` delivers to a `Set` in insertion order). | Done | **It proved the instrument could fail before reporting that it passed**, which is the only thing that makes a green here worth anything: it broke three placements in a SANDBOX COPY ONLY and got 120.279 m at the first rebase and 360.62 m over three, with the record correctly staying at 0 because it was not among the things broken. Those mutations never entered the repo. **It also refused to accept its own first non-zero reading as either a defect or a tolerance case**: 1e-6 m at 137 m from the origin is `2^-23` times the distance, float32's relative epsilon from a `BatchedMesh`'s DataTexture readback, so it rounded the EXPECTATION through `Math.fround` rather than widening a bar. It then carried that finding across the line and reported the identical un-`fround`ed comparison in the factory lane's `FactoryStale.ts:66` and a hard throw in their `factoryrebase.js:66`, **without touching either file** (R35). It verified that every file in the subjects' path was byte-identical to HEAD in the live tree before claiming its clean-archive run was not a weaker claim. |
| 2026-07-28 | **Hunt the sinking with STRUCTURES in the tunnel** (the last uncovered path), briefed with the five clean axes already driven, told the `solids` port is this lane's and terrain is not, told that "does not reproduce" was the LIKELY answer and must not be dressed up as a find, and forbidden from weakening any existing assertion. | Done | **It found that the scene it was asked to build is not reachable by a player, which is a better answer than the one I asked for.** A part pressed in a bore lands OVERHEAD at the heightfield surface, within 0.004 to 0.09 m of it, because placement addresses the grid off the heightfield and a bore does not move the heightfield. `structureTests` was **0 on all 4800 ticks**, so the port cannot be brought under a player in a tunnel by any amount of digging. It also measured that **a machine is not a collider at all** (R33), left both findings alone as other lanes' lines, and wrote CHARACTERISATION checks that fail the day placement changes rather than asserting the behaviour is right. **It fixed six defects in its own harness rather than shipping them**, including a `hold()` that laid a tape longer than it ran (so the first cut measured a moving player), a walk that left the bore entirely (so a "tunnel" scene ran on open hillside at 0/600 under-rock ticks), and a column sweep that stopped at +40 m and reported a part at exactly 40.0 m, which is a sweep bound dressed as a measurement. It demoted its own levelling check from an assertion to a logged number because it measures the levelling tool and not the walker. |
| 2026-07-28 | **Build the control handoff** (`takeControlRemote`, `leaveVessel`, `resumeControl`, `probes/handoff.js`, `--phase=handoff`), briefed with the published `ResumeBoot.ts` §5 contract, told the 18 m board range was the suspected gap, **forbidden from touching `MapView.ts`** (the gameplay lane is live in it), told to make the frozen refusal a REQUIRED negative control, and told a truthful "this refuses and here is why" beats a green probe that tests nothing. | Done | **It found the ordering bug that would have shipped.** `leaveVessel` must call `syncPromoted` BEFORE consulting `mayLeave`, because `makeRecord` stamps `mode 'parked'` at roll-out and only `syncPromoted` ever updates it, so an unsynced guard would wave a rocket under full thrust out of the world **and would have looked correct in every test run on a parked vessel**. Credit for that is entirely the subagent's; I briefed the guard, not the ordering. It also reported that its own first negative control asserted the wrong thing (`thrustN > 0`) after `flyto` circularises to a dry stage, and rewrote it to assert commanded throttle with the altitude pair as the discriminator, rather than deleting the row. And it volunteered that `boardings` now counts remote seats (R30). |
| 2026-07-28 | **Fix the dig probe's coverage, then hunt the tunnel sinking**, briefed with my own belief that `findMountain`'s 0.30 deg sweep was the coverage hole, given world-gen's surveyed coordinates, told terrain deformation is world-gen's and to STOP and report rather than reach across the line, and told explicitly that "driven hard, does not reproduce" is an acceptable and valuable answer it must not dress up as a find. | Done | **It refuted my premise with a measurement instead of implementing it.** Widening the sweep to 5 deg / 550 km / 3721 points raises the best rise only 29.7 m to 37.4 m, because the terrain is fractal, and two of the three surveyed coordinates I handed it are worse than spawn. It identified **bore LENGTH** as the actual lever and got overhead rock from 14.7 m to **68.441 m**. It then failed to reproduce the sinking on six independent axes and said so plainly, including sampling the rendered eye at 144.3 Hz after noticing every probe in the harness runs `of.run` at a multiple of 60, which `Loop.ts` says bypasses the interpolator. It **fixed two defects in its own stress hooks rather than shipping them** (one was measuring PH-60's eject rather than a sink) and reported one hook as `landed: false` rather than quietly dropping it (R32). |
| 2026-07-28 | **Prove the on-rails property in `/core`** (one new test in `test_physics.cpp`), briefed with the exact properties and told to REPORT rather than widen any bound it could not meet, and told that a bit-identical re-fitting control would be a real finding it must not force to pass. Forbidden from touching any other file. | Done | Delivered every number, and **it corrected the brief on a figure I supplied**. It measured one revolution at **6.39459e-3 m**, not the 0.62 m PH-16 records, and rather than widening or narrowing a bound on a guess it built a throwaway probe that reproduces PH-16's own scenario verbatim and measured **0.00570342 m** there too, then checked the figure against theory (velocity-Verlet's frequency error gives about a millimetre at this radius) before saying which number to believe. That is R24, and it also found that `test_flight.cpp:641` asserts a bound **900x its own residue** and therefore cannot fail against any regression. It declined to touch that file. It also strengthened the fixture unprompted, lighting the first stage so `nextStageIndex` is 1 rather than a trivial 0, which is what makes the "the craft is untouched by resume" assertion mean something. |
| 2026-07-28 | **Write and drive the browser proofs** (`probes/vesselrails.js`, `tools/smoke/vesselreload.mjs`), briefed with the six on-rails properties and the nine reload assertions, told exactly which bars are BIT-EXACT and which are measured-plus-headroom, told to hunt `refusedSnapshots` above all, and **forbidden from editing anything under `web/src`, `web/wasm` or `core/`: report defects, do not fix them.** | Done | **It found three defects and obeyed the instruction not to fix any of them, which is why all three got the right fix rather than the quick one.** (1) The lane did not compile: `DebugFlight.ts` called `syncPromoted` without importing it, so the whole `sync` op was dead. Rather than patch the shared tree it copied the tree to a scratchpad, changed the one word THERE, and stated that the repo copy was byte-identical to how it found it. (2) **`demote` left the player strapped into a destroyed vessel**, because it called `disembark`, whose 2 m/s guard correctly refuses at orbital speed, and the session was destroyed anyway: `aboard true -> true, refusals 0 -> 1, flight.live false`. That is PH-69's whole argument and I had not seen it. (3) A promoted vessel published `altitudeDatumM 0, massKg 0` for one tick, because `/core` writes telemetry only inside `of_fl_step`, which is PH-20's launch-clamp bug in a new place. It also volunteered three testability gaps that are now R23, and it reported its own probe as **564 lines against the 400 cap** rather than trimming the reasoning out of it to fit. |
| 2026-07-27 | **Measure the buried capsule at the entry tick** (`probes/burieddiag.js`), briefed with the hypothesis to CONFIRM OR REFUTE rather than to assume, told to replicate `solidForWalker`, `free`, `floorBelow` and `resolveEmbedded` in probe JS rather than approximate them, and forbidden from touching `web/src/`. | Done | **It is why PH-60 is a proof and not a retelling.** It replayed `mtnfall.js`'s drive verbatim, landed on the identical site and the identical loss tick, caught the loss live in 6-tick slices, and reported the per-axis exit distances and the nearest whole-capsule-free distance at TWO burial depths. Both halves confirmed; the numbers are in PH-60. It also volunteered the three findings that shaped the fix and two open risks: that the entry is a LIFT manufactured by `floorBelow`'s own up-branch over the preceding four ticks (R21), that `resolveStep`'s embedded hatch carries a falling capsule sideways into worse rock (R20), and that the two nulls are trivially distinguishable at the call site and the caller simply throws the distinction away. It stated plainly that it was not writing the fix, and did not. |
| 2026-07-27 | **Drive the wall-embed claim** (`probes/wallstuck.js`): can a player get inside a 4 m structural wall, and if so are they stuck. Told that "you cannot get in there at all" would be a perfectly good finding, and to search the hotbar for the part rather than recite a slot. | Done | **It refuted the claim I had relayed (PH-63) and found a bigger defect in its place (R20).** It established that placement consults the capsule on none of its five refusal grounds, got the player inside by both routes, measured the solid band along the feet radial to prove the capsule really was inside, and then measured the walk-out in all four directions with the distances identical to six decimals, which is what proves the wall offers literally zero resistance rather than merely being escapable. It corrected the brief's own figure (the shipped wall is 3.50 m, not 4.00 m, which is the storey) and said why the premise survives it. It also ran the whole scene a second time on bare terrain with no foundation, so a deck could not be what was holding the player up. |
| 2026-07-27 | **Run the walker and terrain regression sweep** on the before and after builds, with an explicit instruction to re-run any failure on the BEFORE build before reporting it as a regression. | Done | Eleven probes, no regression; numbers in the roadmap entry above. **The re-run instruction earned its place twice**: `tunnelmouth` is red on both builds with the identical named check, and it would have read as a regression without it. It also ran the `tunnelsink` negative control unprompted-by-me-twice and confirmed it still fails 9 of 21, which is the thing that makes the 21 green ones mean anything. **And it volunteered the observation that mattered most: the sweep proves ABSENCE OF HARM and nothing whatever about the fix**, because none of the eleven probes reproduces the defect. That is exactly right, and it is why `burylevel.js` exists. |
| 2026-07-27 | (no subagent) **R19 lead 1, run directly:** `probes/basesink.js` and `probes/basereal.js`, which build a real base and then measure the per-tick standing trace with `solids.count` above zero for the first time. | Done | 90 parts, four standing states, the real rAF loop and a third-person frame hash. All flat. **Two probe defects found and fixed mid-run, both of which would have reported a clean zero for the wrong reason:** the base builder walked 90 and 270 degrees alternately and re-covered ground whose cells were already taken, building **8 parts where the same sweep on fresh ground built 52**; and the enclosed-deck phase ran AFTER the base was up, so the player was already standing on a deck and nothing would place at their feet, which the probe reported as "nothing would place" rather than silently skipping. The foundation's hotbar slot is now SEARCHED rather than recited as `decksink.js` recites 4, because the combat and pad lanes are both live in the gameplay files this week and a recited slot index can quietly become a different item that places nothing. |
| 2026-07-27 | **Adversarial READ-ONLY audit of everything that can move a STATIONARY player's rendered position**, briefed with the GP-53 / WG-31 general form as the failure signature to hunt, with my own measurements stated up front so it could not re-derive them, and forbidden from editing, building or running anything. | Done | **It is why PH-45 can say where NOT to look.** It re-derived `floorBelow` and `deckUnder` clean line by line; established that `editsHandle` is written once and cleared only in `dispose`, which kills the heightfield-flicker hypothesis at the mechanism level rather than only at the measurement level; established that the FP eye is a pure function of the feet with no smoothing anywhere and that `socket_head_cam` is in the asset and referenced nowhere in `web/src`; and reported that a grep for the GP-53 form across all of `web/src` finds **no third instance**. It swept every timer in the client and found the cadences are 9, 20, 30 to 35 and 1200 ticks plus a few sub-second eases, so **nothing at all sits in the 1 to 5 second band** except the 2.0 s looping `Idle` clip. It found the two deck gaps now in R19, and it correctly flagged that a 60 Hz driven run pins the interpolation alpha to a single value, which is why the 144.3 Hz rerun happened. Two of its own candidates were then checked and refuted by measurement: the TP spring arm is pinned at 0 under a mountain, and the chunk re-stitch loop is converged and flat here. |
| 2026-07-27 | **Write the driven browser probe** (`web/tools/smoke/probes/maneuver.js`), briefed with the six things to assert, standing rule 11 and DW-20, and forbidden from fixing anything it found. | Done | **1068 lines, 61 rows, and it found the defect that would have shipped a feature that cannot be used.** It caught PH-44's node countdown by asserting a PROPERTY nobody had thought to state ("40 s of mission time passes, the countdown moves 40 s") rather than reading the number and moving on; it caught the CMD marker by reasoning from the near-hemisphere filter that a marker at 133.7 degrees off the nose could not have been drawn and yet was; and it caught the roll hijack and declined to assert it, on the grounds that asserting today's behaviour would bake the bug in. **It also refused a green line I had asked for**: "assert the predicted apoapsis is higher" passes on 0.34 m of floating-point noise for a node placed AT apoapsis, so it replaced it with two regime-independent invariants and a 100 m/s case that genuinely moves the apoapsis 95 to 215 km. Standing rule 11 doing its job through a subagent that had been told the rule. Final: **61/61, `fails: []`**, on a 90.0 x 80.3 km orbit with 1589.7 m/s left. |
| 2026-07-27 | **Build the orbital map's canvas PAINTER** (`web/src/ui/MapDraw.ts` + `styles/map.css`), briefed with the `MapTypes.ts` data contract, the 400-line cap, the zero-three.js rule in `src/ui/`, and an explicit instruction to RENDER its work rather than read it. | Done | 378 + 245 lines. **It could not use the Browser pane and said so instead of claiming it had looked**, then drove real headless Chrome through the repo's own `playwright-core` against a throwaway server on a port nobody was using, over six synthetic scenes with a 4x magnifier. **Five defects, none of which it found by reading**: the atmosphere ceiling label was gated on the annulus being 13 px thick and at any span that frames a low orbit 70 km of air is about ten pixels, so it had NEVER once appeared; every apsis ring was stamped through the middle of its own label (offset by half the label's HEIGHT, but a two-line label is six times wider than tall); the current AP's label landed on the planned AP's ring in the exact two-conic scene the map exists for, fixed by reserving every ring before ANY label lays out, because a label moves and a ring cannot; the dashed planned conic rendered SOLID, because a canvas dash phase restarts at every `moveTo` and each 7 px segment is shorter than one dash; and leader lines re-entered their own rings, reading as struck-out markers. It also flagged, unprompted and correctly, that gating the PERIAPSIS on `bound` costs a hyperbola its periapsis marker, which is exactly the number a re-entry needs. Taken. |
| 2026-07-27 | **Read-only recon of the modal, binding and mode machinery** before a new full-screen mode was added, asked for the `UI_ALLOWED` comment block verbatim, the whole Escape call chain, the VAB's end-to-end mode transition, and per-file line counts against the cap. | Done | **It changed three decisions.** It found that `Input.setUiCapture` had grown a SECOND ARGUMENT, `alsoAllow`, since the brief was written, which is exactly the per-panel allowance the map needed and meant the map did not have to widen the global list. It found `mute` already bound to `KeyM` (PH-43), which the brief had not noticed and which would have made the map key silence the game. And its line-count table is why `MapMode` was split from `MapBoot` and why the map's construction in `Boot.ts` is one line: Boot had two lines of headroom and three other lanes in it. |
| 2026-07-27 | **Recon of `orbital.h` / `flight.h` / `vessel.h` / the bridge**, asked for verbatim signatures rather than prose, before any maneuver code was written. | Done | Delivered the whole surface signature by signature. Two findings shaped the design: `orbital::Elements::i` is measured against **+Z** while this world's pole is **+Y** (PH-40), and `OrbitSummary` publishes altitudes and nothing else, so time-to-apoapsis, the apsis POSITIONS and the orbit pole all had to be added rather than read. It also stated plainly that nothing named maneuver existed anywhere in the repo outside docs, which is the negative result that makes "extend, do not duplicate" a decision rather than a hope. |
| 2026-07-27 | **Adversarial READ-ONLY audit of the ten flight files**, briefed with the four already-fixed defects as the FAILURE SIGNATURE to hunt (a left-handed frame both readouts agreed on, a damper converging on the wrong fixed point, a renderer at the raw position while the camera aimed at the interpolated one, two handle registries that both number from 1) and told to find more of that shape. Forbidden from editing, building or running anything. | Done | **The single highest-yield thing lane G did.** It independently found PH-28 (the AGL sign) from the arithmetic while the probe was finding it from the data, which is the cross-check that made it certain rather than plausible. It also found PH-31 (the vessel drawn at the body centre for the whole walk, with the reason nothing caught it: `distanceToVessel` reads the sim and was right the whole time), PH-32 (board range to the origin, so any rocket over 18 m is unboardable), PH-33 (every engine plumes), PH-34 (the warp cap on last tick's `inSpace`), PH-35 (both message clocks, opposite bugs, which is why the pair looked like it worked), the `scratchF64` view held across a call into WASM in `readStagePerformance`, and `flightReport` reading telemetry after an arrest where the navball reads state. **It also published a "verified clean" list**, re-deriving `horizonFrame`'s handedness, `slewCommand`'s two signs, `holdRoll`'s branch and `place`'s determinant against second sources and saying so, which is worth as much as the findings: it says where NOT to look next time. Two of its findings were judged and NOT taken: the `VesselView` scale asymmetry (real, needs the art contract, no part ships a non-unit scale today) and `surfaceSpeedMS == orbitalSpeedMS` (honest while `bodySpinRadS` is 0, R5). |
| 2026-07-28 | **Three read-only recons run in parallel before a line was written**: the save/persist path and its schema; `/core`'s on-rails framework, `orbital.h`, `flight.h` and the whole bridge with verbatim signatures; and this lane's own client files. All three forbidden from editing, building or running anything. | Done | Between them they changed the shape of the work three times. The save recon found that `SaveSlot` lives in `web/src/game/SaveGame.ts` and not where the brief assumed, that `Persist.ts` was already **over** its line cap, and that GP-102 had just established a **CHOKE-POINT stamping** precedent in `writeSlot` whose own comment argues for exactly this use; that is why `Persist.ts` is untouched by this lane. The `/core` recon returned the finding that **FS-4's on-rails framework is not a framework**: `Demote`/`AdvanceOnRails`/`Promote` are three concrete methods on `factory::FactorySim`, `ISimProxy` exists only in the spike documents, and `park`/`resume` had no bridge export at all, which is what made the ABI bump unavoidable rather than optional. The client recon found the two live constraints that shaped every file placement: `FlightSession.ts` and `FlightMode.ts` were both at **399 of 400 lines**, and `FlightSession.step` is reachable ONLY through `VesselObserver.step`, so **an unattended vessel has no tick path at all today**, which is the sentence the whole lane is an answer to. |
| 2026-07-27 | **Read-only recon of the save/autosave/load path**, asked for exact file:line for the schema version, the timer and what gates it, the storage key, the boot order, any before-save hook, and the per-file line counts against the 400 cap. | Done | Decisive for PH-30 and it changed the design twice. It established that the autosave sits ABOVE `Gameplay.fixedStep`'s `uiOpen \|\| suspended` return (so it fires while strapped in), that `pagehide` is a SECOND write path through the same `save()` funnel, that **there is no before-save hook of any kind**, and that `Gameplay.ts`, `Vab.ts`, `Debug.ts` and `FlightSession.ts` were all within three lines of the cap. That last fact is why the guard went into `Persist.saveSlot` (120 lines of headroom, one funnel, both writers) instead of into `Gameplay`, and why the warp policy was extracted to its own file. It also found the thing nobody asked about: **the player's own position is not in `SaveSlot` at all** (R13). |
| 2026-07-27 | **Build the navball** (`web/src/ui/Navball.ts` + `NavballDraw.ts` + `navball.css`). Briefed with the exact `NavballReadout` interface and forbidden to touch anything else. | Done | Delivered to the interface field for field, 325 + 385 + 223 lines, check green. Sky and ground hemispheres split by the true projected horizon, pitch ladder, compass, four distinct markers (prograde, retrograde, SAS command, guidance ribbon), the full telemetry block and the per-stage delta-v table. **It found three defects by SCREENSHOTTING at 4x rather than by reading its own code**, which is standing rule 3 doing its job: a stale fill left every compass tick after the first painted black, the reticle's tail scribbled through the "N" in the single most common attitude, and ladder numbers near the rim were being clipped. `report().markersDrawn` is the array actually handed to the paint pass, not a second opinion, so a navball that is present but never fed is distinguishable from a live one. |
| 2026-07-27 | **Build the flight vessel renderer** (`web/src/render/VesselView.ts` + `VesselPlume.ts`). Briefed with the glTF duplicate-node-name trap in full. | Done | 399 + 142 lines, check green. It **verified the basis handedness numerically against the project's own three r185 rather than from memory**, on three cases including an off-axis pair: `makeBasis(right, forward, right x forward)` has determinant +1 while the other order gives -1, a reflection that draws a mirrored rocket looking correct from most angles. It also Gram-Schmidts `right` against `forward` before building the basis, so a flight state whose rows have drifted a milliradian rotates the model instead of shearing it. Socket lookups are on the DETACHED clone, so the twenty-parts-called-`socket_stack_top` trap is avoided by construction. **One debt flagged honestly:** `ASSET_RENAMES` is mirrored from `VabView`, which does not export it, bounded by a `renameCount` a probe can assert against. |
| 2026-07-27 | **Recon of the gameplay tick** (read-only). Asked for the exact seams a flight mode must not break, above all where the factory is ticked and what proves production advanced. | Done | Decisive, and it changed the design. It traced the factory to `Gameplay.fixedStep` -> `Factory.tick` -> `AutoLine.step` -> `_of_net_step_n` and established that it is gated on **nothing**: not proximity, not residency, not the VAB being open. So flight needed no plumbing to keep the base alive, only a gate that does not reach the block above the `uiOpen` return. It also enumerated every site that assumes `services.observer === services.player` (the dig ray, the level ray, the whole avatar block, the headlamp, `__of.teleport`), which is what made the router a two-line change per site instead of a hunt. **And it said out loud that the counter I meant to assert on could not move in an empty world**, which is why `probes/ascent.js` builds a drill and a smelter before it flies. |
| 2026-06-14 | Scoped web research (run directly, no subagent — 5 targeted searches) for Spike 2: (1) UE5 Chaos under origin shifting / LWC jitter; (2) KSP Krakensbane / why analytic orbits; (3) Kepler universal-variable + patched-conic SOI; (4) UE5 Chaos fixed-substep / Async Physics Tick; (5) KSP joint-wobble mitigation (KJR/autostrut/JOINT_RIGIDITY). | Done | **Decisive for RC-4.** Findings: LWC meshes jitter ≥3 km, peak 10 km (validates REBASE_RADIUS=4 km; Chaos only trustworthy near origin); Krakensbane is a *coordinate workaround* not a physics fix (CE-6 handles velocity, not integration); universal-variable Kepler = one path for all conic types; Chaos Async Physics Tick gives fixed substep but per-substep force callbacks changed/awkward; joint stiffness is a Chaos solver-iteration problem too. → **PH-4 HYBRID** (custom integrator + Chaos-for-contact). Folded into spike2-physics §4. |
| 2026-07-26 | **Playtest complaint "moving across the ground feels like you are always getting stuck unless you jump"** (run directly, no subagent). Wrote [`web/tools/smoke/probes/walkfeel.js`](../../web/tools/smoke/probes/walkfeel.js) FIRST, took a baseline, then fixed [`web/src/player/VoxelCollision.ts`](../../web/src/player/VoxelCollision.ts) + [`KinematicBody.ts`](../../web/src/player/KinematicBody.ts). Commits `5d68727` (probe), `e90064d` (fix). | Done | **Cause was none of the four suspected.** Not the step-up, not the ground snap (0 airborne transitions at baseline), not the slope limit (minSlopeCos 0.98 against a 0.643 limit), not the resolver's axis choice. The walker was resolving against rock that is not there: PH-7. **0.442 -> 1.000 m travelled per commanded metre, 527/917 -> 1/902 stalled ticks, 688 -> 0 embedded pushes.** Negative control (walk into the wall of a shaft you dug) **12.00 m -> 0.23 m climbed**, and it exposed PH-8 and PH-9, both of which had been in the walker since it was written. **Caveat on the numbers:** WG-22 (`levelArea`, ABI 4, deposits-as-patches) landed mid-task and changed the terrain; on the post-WG-22 world the resolver no longer trips at any of the 8 sites sampled, so the after-numbers are "does no harm" there rather than a second before/after. The defect itself is still measurable at every site: `proudHalfMetrePct` 7.5, `phantomPct` 27 to 41. |
| 2026-07-26 | **Independent hand-verification of the reference vehicle's delta-v budget.** Briefed with the authored masses, Isps and the staging convention ONLY, and explicitly forbidden from reading any repository code, so that `stagePerformance` could be pinned to an outside answer rather than to itself. | Done | Confirmed every figure to 2 dp: launch mass **9845 kg**; stage 0 m0 9845 / m1 5545, ln ratio 0.5740670913, **1857.79 m/s** vacuum (1486.23 sea level); stage 1 m0 3705 / m1 1555, ln ratio 0.8682077131, **3065.12 m/s** vacuum; total **4922.91 m/s**; pad TWR **1.6567**; mdot 61.8010 and 16.9953 kg/s, **bit-identical** at sea level and vacuum (`160x330 == 200x264` and `30x360 == 60x180` as exact rationals); burn times 69.58 s and 126.51 s; circular speed at 60 km 2313.20 m/s. It also flagged the two failure modes those numbers discriminate, unprompted: dropping the stage-1 wet mass out of m0(stage 0) shows up as a pad TWR of 2.66, and counting the 40 kg of monopropellant as burnable is worth **+95 m/s** of optimism. Both are now assertions. |
| 2026-06-14 | Build PH-Build 0 — pure-CPU headless orbital core (run directly, no subagent): `core/include/of/orbital.h` (Stumpff C/S, universal-variable `propagate`, `stateToElements`/`elementsToState`, velocity-Verlet `Integrator`, contrast `EulerIntegrator`, `park`/`resume`) + `core/tests/test_physics.cpp` (9 tests / 71 checks) + `physics_tests` CMake target. Mirrored the 3 existing Wave-0 cores' pattern; D-006 μ derived in-header (Forge 3.5316e12, Cinder 6.52e10). | Done | **GREEN.** Built clean (g++/Ninja, `-Wall -Wextra -O2`, no warnings); `physics_tests` 71/71 checks pass; ctest 4/4 suites pass (didn't break the others). **No-drift demonstrated:** symplectic max \|dE/E\|=2.4e-11 over 30 orbits vs Euler 0.40. One bug found+fixed during build: a `cross()` z-component typo in the test-oracle helper (caught precisely *because* the independent angular-momentum oracle disagreed with the propagator). → **PH-6 logged.** |

### 8.1 The demo script, and what a person can safely do to it

Written for whoever is standing behind the keyboard. Every line below was
driven, not reasoned about, by `probes/flightabuse.js` (14/14 rows) and
`tools/smoke/reload.mjs` (pad, orbit and ground).

**The path:** C builds a rocket in the bay. G rolls it out 26 m ahead and says
it is a stand-in for a launch pad. Walk to it, G again to board. Shift or Z
opens the throttle, Space lights the first stage and releases the clamp, W and S
fly the nose onto the guidance marker, Space again when the first stage runs
dry, X cuts at an 80 km apoapsis, Y cycles SAS, P and O warp, G gets out once
it is down.

**Verified safe to do wrong:** mash Space on the pad (refused, and it says
throttle up first); throttle up with no engine lit (clamp holds, TWR shown);
Escape, Tab, V and left-click in flight (nothing, and the click cannot dig a
crater under the parked avatar below); open the assembly bay at 20 km and close
it again (the flight continues, and G is deliberately inert while the bay owns
the screen, so C then G is the way out); try to climb out at altitude (refused);
fly retrograde under power (no tumble, no NaN); mash the warp ladder to its top
and back; abort the ascent and ride the whole re-entry at 1000x without touching
a key (48 telemetry samples inside the atmosphere, a real 139 kPa peak, deepest
AGL 0.0 m); stage and throttle a spent vehicle. **After all of it the player
gets out, walks and digs.**

**Known rough edges a viewer may notice, none of them faults:** AP and PE read
`---` for about the first ten seconds of a launch (R14, the trajectory is
radial and `/core` calls a radial conic unbound); the HUD says regime ASCENT at
80 km (R10, `ORBIT_START` is 100 km); and the standing amber chip says the
flight is not saved, which is true and is the point (R11).

## 9. References
**Vessel programme (PH-10 to PH-18):** DW-29 (the programme), DW-30 (real but greased, the enumerated concessions), DW-18 (`BodyParams::muM3S2`, the one gravity authority), D-006 (body constants, Forge's 5.6 km scale height, Cinder airless), [ASSET-SPECS §3.3](../web/ASSET-SPECS.md) (the 1.25 m stack contract, which every dimension in the catalogue is read off). Built: [`vessel.h`](../../core/include/of/vessel.h), [`atmosphere.h`](../../core/include/of/atmosphere.h), [`flight.h`](../../core/include/of/flight.h), [`test_vessel.cpp`](../../core/tests/test_vessel.cpp), [`test_flight.cpp`](../../core/tests/test_flight.cpp), `vessel_tests` + `flight_tests` targets in [`core/CMakeLists.txt`](../../core/CMakeLists.txt). Method: Tsiolkovsky; Barrowman's normal-force coefficients (PH-14); turbulent flat-plate skin friction (PH-15); velocity-Verlet with a velocity predictor for the drag term, reducing bit-exactly to `orbital::Integrator` in vacuum (PH-16).

MASTER_PLAN §6 (orbital), D-002, D-006. Spike-2 design + full citations: [spike2-physics.md](../spikes/spike2-physics.md) §9. **Built core (PH-6):** [`core/include/of/orbital.h`](../../core/include/of/orbital.h) (orbital math + symplectic integrator), [`core/tests/test_physics.cpp`](../../core/tests/test_physics.cpp) (9 tests / 71 checks), `physics_tests` target in [`core/CMakeLists.txt`](../../core/CMakeLists.txt). Key: universal-variable Kepler propagation (Stumpff C/S, Curtis Algorithm 3.4); patched-conic SOI intersection (poliastro); KSP Krakensbane (coordinate workaround); UE5 LWC jitter ≥3 km + Chaos Async Physics Tick; Kerbal Joint Reinforcement / autostrut (wobble mitigation). Principia (n-body, cautionary — not adopted, D-002).
