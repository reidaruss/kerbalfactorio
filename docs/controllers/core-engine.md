# Core Engine & Simulation Framework — Master Controller Context

> **Domain owner:** `core-engine-controller` · **Reports to:** Admin · **Phase:** 0→1 · **Status:** **THE STATION HAS ONE ATTITUDE (CE-115 to CE-117, 2026-08-15).** The shot-baseline lane handed over "the drawn hull has two pose authorities"; it has one, and the SECOND authority was the station's PUBLISHED attitude. `of.station().axes` was `stationQuat(pos)`, a position-only nadir lock whose roll is THREE's shortest arc, while the solid, both gravity volumes and the hull are posed by `CarrierMount` at `poseAt(t) . local`, whose roll rides LVLH. **Measured 0.230 deg apart at tick 121 growing LINEARLY to 1.373 deg at tick 721, 0.00190 deg/tick and unbounded over an orbit, with the POSITIONS agreeing to 0 m exactly; after the fix 0 to 2.7e-6 deg, FLAT, which is `acos`-near-1 rounding.** `stationAxes` reads the mounted solid now and `stationQuat` is demoted to the authored-frame constructor plus the no-solid fallback; `installAndMountStation`'s redundant `view.place` is deleted so `CarrierMount` is the hull's only writer. **The driven-clock hypothesis is FALSIFIED rather than left open**: `applied` +60 and `drawn` +30 over `of.run(1.0, 30)`, and `drawn` +15 while PAUSED. `artframe.js`'s station shot, marked UNMEASURED for irreproducibility, no longer swaps scene: box luma **14.84 identical to every published digit on three consecutive captures at base `8b324ee`**, and **15.52 / 13.82 / 15.89 (ratio 1.15) once REBASED onto `ac6c38f`**, against a pre-fix 2.46 / 40.29 / 30.98 / 2.89 (ratio 16.4). The identical triple did not survive the rebase and is reported rather than re-run until it agreed; the residual moves with the whole frame and is terrain streaming reaching a sealed hull's IBL fill, not pose. Handed to rendering, not fixed here: the corrected yaw solve moves 244.15 -> 255.35 deg, so the shot's `yawOff: 45` framing constant is stale by exactly the bug. `probes/stationpose.js` 16/16. See §5n. **Prior status: `zerog.js` Z4 IS GREEN ON A DECK THAT IS REALLY MOVING (CE-100 to CE-104, 2026-08-15, CE-54's open question closed).** The gap was never a missing mechanism: `of.standAboard()` already existed and Z4 was still asking `of.standAt(hub, { frame: 'body' })`, which seats a walker the deck leaves 4,698 m behind over the leg's own 2.5 s settle. **THE CONTRACT: `standAt` names a point in the BODY frame and NEVER boards, in either frame** , the refusal is NOT extended to `{ frame: 'body' }` and `{ frame: 'body' }` does not quietly board, because that reading is the one honest way to ask for the defect and Z4neg is now exactly that negative control (`carrier: station:anchorage`, `boarded: false`, `riding: null`, `inVolumes: []`, left behind **4698.120753 m** against a predicted **4698.145411 m**). Boarded, the same leg reads **5.49e-10 m of local drift against 1879.254895 m of body-frame drift over the same 60 ticks** (predicted exactly), grounded and onDeck in both volumes with `restoredExactly: true`. **Four findings came out of it:** `{ frame: 'body' }` was CE-54's one SILENT branch and now reports `carrier`/`depthM`/`boarded` (CE-101); **`mounts.boarding.boarded` is the RULE's census, not the boarding census** , it reads `tested: 2747, boarded: 0` with a rider perfectly aboard, which is the same shape GP-805 diagnosed the gap from, so `rider` is published beside it (CE-102); the probe's station snapshot was read once before Z4 and spent on Z5b/Z7/Z8 tens of kilometres later, CE-46 again (CE-103); and **the generator's magnitude is installed ONCE while only its pose follows the frame**, so GP-805's unreached `carrierG === of.gravity(deckR)` fails on correct code by 9e-16 and is now the conic's own radius band, recorded and not fixed (CE-104). GP-400 closed as a side effect: `at(0.5)` was inside `col_HallCore` and the leg is now at the asset's `socket_hall`. **No `/core` change, no ABI move.** `carrier.js` 43/43, `stationwalk.js`/`stationboard`/`stationride`/`stationreboot`/`stationdraw`/`stationvisit`/`docking` green, `check:pose` 14/14. See §5m. **Prior status: A CHOPPED TREE STAYS CHOPPED (CE-70 to CE-74, 2026-08-14, R-BODY-2 playability half).** **The brief said an in-page body round trip regrows the world's rocks and trees and the trigger is not the body switch:** three trees chopped, a **3 km walk out and back on ONE body with no `reboot` of any kind**, and they came back **`24.91/24.91`, `28.89/28.89`, `39.77/39.77`** at fresh /core indices. `TREE_RADIUS_M` is **620 m**, so the case a player actually hits needs no debug verb. Cause: `of_gp_node_add` always appends a node at full `RemainingAmount`, and `buildRock`/`buildTree` then OVERWROTE `known[cellKey]` , the map that remembered the harvest , with the fresh index, while the only re-drain path (`pending`) is filled solely by a save on disk. **And it cost the save too:** `serialize()` walks `known`, so after a regrow the autosave agreed the tree had never been chopped (**`trees 0`** on HEAD against **`trees 3`** fixed). Fixed by one shared authority, `web/src/game/NodeMemory.ts`'s `inheritDepletion`, replacing the three-line `pending` block both fields carried; live memory outranks saved; re-add-and-re-drain rather than re-present, so a rock still seats on dug ground. `probes/noderegrow.js` **fails: []** with the refusing case proven on a separately built HEAD dist (**4 reds**, worst carried-vs-left **21** against a one-float32-ulp bound of **1.19e-6**). Spawn-clearing nodes are the negative control and were in the reading all along. **No /core change, no ABI move**; `RockField.ts` 407 -> 403. See §5l for what it does NOT fix (`known` is not body-keyed; the persistence freeze is untouched and cannot be relaxed by this). **Prior status: THE HARNESS IS HONEST ABOUT FRAMES NOW (CE-54, 2026-08-14, RN-1412).** `of.standAt` seated probes at rest in the BODY frame on a deck travelling at 1879.2552 m/s, so `stationdraw.js` shot the wrong part of space while reporting `visible: true`, `drawnParts: 2`. **The bug report named one cause and there were two, and it got the rate wrong:** the aim was ALREADY **5,352.394 m** stale (`install.standPos` is a boot record) — 187 bound-radii outside the **28.644895616162408 m** bound, so the per-tick rule boarded nobody (**`boarded: 0`**) and the walker free-fell at exactly `g*t` (**26.371 m/s at 7.467 s**) while the deck left at 1879.24 m/s. RN-1412's "7.67 km/s" is the stale figure §5d already flagged as 4.05x out. **Nor was the frame black:** 13.79 mean luma of correctly drawn planet and stars, so a black-frame gate would have passed the defect. `standAt` now **REFUSES inside a mounted carrier's bound** and names `of.standAboard()` / `of.standAboard(lx, ly, lz)` (the station's AUTHORED LOCAL frame, resolved live, through the shipped `seatOnStationDeck`), with `{ frame: 'body' }` keeping the old behaviour by name for body-frame instruments like `zerog.js`. Auto-seating was rejected because `zerog.js` measures speed as a body-frame position difference and could not tell. `stationdraw.js` **fails: []**, eye **4.316 m** from the station after the settle (defect: 6,215.659 m), litFrac **0.991** (defect: 0.183); `stationboard.js` **29/29**, `stationframe.js` 12 checks valid. See §5k. **Prior status: THE ROTOR ROW WAS AN INSTRUMENT DEFECT, NOT A SIM DEFECT (CE-53, 2026-08-13).** `probes/carrier.js` C1 "the rotor's turn and its travel agree through its own radius" failed deterministically at **1.7240873e-6 relative against a 1e-6 gate** after ph357 stamped the station. The sim was right to 4e-11: `turnBetween` extracted the angle as `2*acos(|dot|)`, whose conditioning multiplies a **4.228e-16** dot error by **1.2772e5** and hands back a w that is **5.4e-11 rad** light. Swept over 20,000 bases the acos form spans **-2.350e-6 to +1.724e-6**, breaches 1e-6 on **1.6%** of them, and lands on a **quantised 1,055-value lattice**, which is why it reproduced bit-stably on two machines. It also **invents 4.21e-8 rad of turn for a frame that did not turn**. Now `2*atan2(|vec|, |w|)` of the relative quaternion, residual **-5.28e-11 to -2.65e-11**, and the probe row compares the **chord `2r sin(w/2)`** the discrete pose actually sweeps at a **1e-9** gate (70x headroom). `carrier.js` **43/43 fails: []**, `stationride` 22/22, `stationboard` 29/29, `check:pose` 14/14. **Prior status: THE DECK NO LONGER STUTTERS UNDER A PLAYER STANDING ON IT (CE-51, CE-52, 2026-08-11).** Reid filmed it from a real GPU. The camera was interpolated between fixed ticks and the station's hull was posed at the last integer one, so at 1879.26 m/s a motionless player saw the deck sweep **27.04 m peak to peak, 13.52 m within one tick, correlated with alpha at 0.9999999860**. The drawn geometry is now posed at the fractional render tick and the collider stays on the integer tick: **0.00243 m / 0.000165 m**, with the collider channel deliberately unchanged at 27.05 m as the control that the sim path is untouched. **Prior status: THE STATION ARRIVAL NO LONGER SEATS THE PLAYER INSIDE A PILLAR (CE-49, CE-50, 2026-08-11).** Reid reported it from a real GPU: grounded, carried, and inside `col_HallCore`, which the press had been aiming at since GP-234 described a placeholder hub that no longer exists. The arrival is the asset's own `socket_hall` now, on the live pose, verified against the walker's own collision query: **0 of 15 capsule samples solid at the seat point, 3.999998 m off the hub centre, 8e-06 m onto the deck's top face, and 9.053095 m of deck-relative walking**, against a negative control in which the OLD point still overlaps. **The assertion that would have caught it did not exist**: `grounded`, `onDeck` and 0 m off centre are all TRUE inside a pillar. **Prior status: R17 IS CLOSED (CE-47, CE-48, 2026-08-11): THE STATION SURVIVES A REBOOT, ON ITS CONIC.** `of.reboot()` emptied the mounts and the carrier registry and nothing put them back, so the deck stayed solid and standable while it silently stopped following its own orbit and CE-40's membership rule declined for ever. The station's install wiring is now ONE function called at boot (tick 0) and on every rebuild (the live tick): measured **1 mount back, the frame re-registered, the deck 0 m off its conic, the press boarding again at 1879.25517152564 m/s, and 1.3941873453354372e-10 m of deck-relative drift over the 601 ticks after**, against a control (`unmount`) in which the rule boards nobody for ever and the deck sits **1,879.25 m** off its conic while still being solid to stand on. `probes/stationreboot.js` 18/18; stationboard 28/28, stationride 22/22, stationvisit green, check:pose 14/14 all unregressed. **Prior status: ALL THREE LINKS OF THE FIRST STATION MISSION ARE IN ONE BUILD (CE-39 to CE-45 + PH-357, 2026-08-11, todo #1).** `ph357-station-stamp` is merged, so **Anchorage actually travels at 31.32091952549093 m per tick**, and the `visit:station` press boards its frame and seats the player AT REST IN IT: **seat 1879.2551715278214 m/s where a `standAt`-only arrival reads ~0, feet 0 m from the LIVE hub while the boot position is already 6,232.85 m behind, deck-relative drift 1.5729040161257287e-9 m over 601 ticks against an un-boarded 3,747.31 m with no floor**, walk-off releases once keeping 1879.2552 m/s, walk-back-on boards with no press, 5 origin rebases against a predicted 4.706, and **CE-6's Krakensbane trigger is ABSENT from `FloatingOrigin` entirely**. `stationboard.js` 28/28 with NO instrument frame in it, `stationride.js` 22/22 with its frozen-fixture assertion inverted, `stationvisit.js` green with the off-centre projection re-derived off the live deck (the install pos was 8,926 m stale). See §5g. **Prior status: THE RIDER IS ON THE CARRIER (CE-39 to CE-44, 2026-08-11, todo #1).** The `visit:station` press boards the station's frame and seats the player AT REST IN IT, and a per-tick membership decision immediately after `mounts.syncAt` boards and releases with 1.0 m / 5.0 m hysteresis. Measured on a rotor derived from Anchorage's own `r x v`: **deck 18792.275184861268 m, rider 18792.275184859634 m, deck-relative drift 1.6545725990496454e-9 m over 600 ticks at 31.320919525472796 m per tick**, against an un-boarded control of **3,747.31 m with no floor** in the same run; walk-off releases exactly once keeping 1879.2552 m/s, walk-back-on boards with no press, **4 origin rebases over 600 ticks against a predicted 4.698**, and **CE-6's Krakensbane trigger is ABSENT from `FloatingOrigin` entirely**. (that block's closing claim, **"Anchorage still does not move"**, was true when written and stopped being true the same day: the branch was pushed and merged) · **THE CARRIER FRAME HAS A CONSUMER (CE-80 to CE-86, 2026-08-03).** Anchorage's collision solid, both its gravity volumes and its drawn hull are re-posed every fixed tick from the same `poseAt` the rider uses (Admin ruling R13, carrier-local geometry). Measured on a moving fixture: **deck 18,792.275184861268 m, rider 18,792.275184859576 m, local drift 1.7e-9 m over 600 ticks**, with `solidBuild` (the walker's own predicate) saying the deck is no longer where it was AND is under the player; negative control in the same loop reads **deck 0 m against a rider that moved 18,791.4 m and no floor beneath it**. 21/21 driven, 43/43 on the pre-existing carrier gate, `check:pose` 9 -> 14 checks · **THE CARRIER FRAME IS BUILT AND MEASURED (CE-30 to CE-38)** , a rider on a moving frame stays put in that frame to **exactly 0.000000 m** over 600 ticks while the same rider unboarded is left behind at **9.041571 m per tick**, which is physics R79's independently measured number; eight-way negative control, `posecheck.mjs` gate, Forge bit-identical at hash `461637469` after a Forge→Cinder→Forge round trip · **THE WORLD LIFECYCLE SEAM IS BUILT AND MEASURED (CE-19 to CE-22)** · WASM pivot spike COMPLETE (CE-11, CE-12); Spike 1 designed; Phase-1 contracts C-6/C-7/C-8 closed (core-side) · **Last updated:** 2026-08-15 (CE-100 to CE-104: `zerog.js` Z4 green on a moving deck; the boarding contract decided (`standAt` never boards in either frame, `standAboard` is the way aboard, `{ frame: 'body' }` kept as the negative control and now reporting the carrier it landed inside); `mounts.boarding` named as the RULE's census with `rider` published beside it; the probe's measurement frame follows the rider; and the generator's magnitude installed once while only its pose follows the frame, recorded not fixed. Prior: 2026-08-14, CE-70 to CE-74: a harvested rock or tree stays harvested when its cell streams back in; the defect was a streaming one, not a body-switch one, and it reached the save as well as the screen. Prior same day, CE-54: `standAt` refuses inside a carrier bound and `standAboard` takes the station's own local frame; RN-1412 diagnosed as TWO defects, a stale boot-record aim plus the zeroed absolute velocity, with the report's 7.67 km/s corrected to 1879.2552 m/s and its "black frame" corrected to 13.79 mean luma. Prior: 2026-08-13, CE-53: the rotor row's 1.7e-6 is `acos`-near-1 conditioning inside the debug instrument, not the sim; `turnBetween` becomes the stable atan2 form and the probe compares the chord the discrete pose actually sweeps. Prior: 2026-08-11, CE-51/CE-52: the deck stops stuttering, the hull and the camera share one clock, and the first per-rendered-frame instrument in the project. Prior same day: CE-49/CE-50: the station arrival stops seating the player inside `col_HallCore`, and the occupancy assertion that would have caught it. Prior same day: CE-47/CE-48: R17 closed, the station survives a reboot on its conic, and the station's install wiring becomes one function called by boot and by every rebuild. Prior same day: CE-45 + the ph357-station-stamp merge: Anchorage TRAVELS and the whole of todo #1 is measured on it, with the frozen-fixture numbers kept beside it as the control in §5f. Prior same day: CE-39 to CE-44, the RIDER is boarded onto the carrier, and three findings: ph357-station-stamp is on no reachable ref or remote, FloatingOrigin has no Krakensbane trigger, R97's warp guard is unreachable in this build. Prior: the carrier frame's FIRST CONSUMER: the station's geometry rides its own conic. Prior: the carrier frame, opened once for the station, the moon and docking. Prior: the runtime body-switch seam escalated from world-gen WG-150; 2026-07-27 enemies sub-domain; 2026-07-25 WASM bridge shim + parity + benchmarks)
> Read alongside: [MASTER_PLAN](../MASTER_PLAN.md) · [AGENT_ARCHITECTURE](../AGENT_ARCHITECTURE.md) · [ADMIN](ADMIN.md) · **[WASM-BRIDGE](../web/WASM-BRIDGE.md)** · **[Spike 1 design](../spikes/spike1-core-engine.md)**
> **Sub-domain spun out 2026-07-27:** the enemies / pollution / evolution loop (W11 lane E) has its own context file, **[enemies.md](enemies.md)** — decisions EN-1..EN-10, `core/include/of/enemies.h`, `enemies_tests` 248 checks. It is a new simulation subsystem rather than part of the foundational layer, so it does not live here; nothing in §5 changed for it.

## 1. Mission
Own the foundational layer every other domain stands on: the 64-bit coordinate system, floating origin, reference-frame hierarchy, the **active/on-rails** simulation framework, the tick/time model (including time-warp), and the data-oriented ECS foundation. If this layer is wrong, everything above it is wrong.

## 2. Scope & owned subsystems
- **Universe coordinate system** — 64-bit (double or int64 fixed-point) authoritative positions.
- **Reference-frame hierarchy** — star → planet → vessel; frame switching at SOI boundaries (coordinates with [physics](physics.md)).
- **Floating origin** — rebasing the world around the active observer so GPU/physics see near-origin floats.
- **Active/on-rails framework** — the generalized [MASTER_PLAN §3](../MASTER_PLAN.md#3-the-unifying-principle-active-vs-on-rails) mechanism: registration, promotion/demotion of entities between simulated and abstracted states.
- **Tick & time** — fixed-step sim tick (decoupled from render FPS), time-warp orchestration, sim/render interpolation.
- **ECS foundation** — entity/component layout conventions, system scheduling, the boundary between engine Actors and the data-oriented sim.
- **Floating origin, BY FILE (added explicitly 2026-08-03, CE-21).** `web/src/world/FloatingOrigin.ts` and the `OriginRebased` contract around it. This was always inside "floating origin" in §1, and the file nevertheless carried **zero decision tags and appeared in no controller document for a year**, which is how the *lifecycle* around it (who unsubscribes, what the origin means when the body changes) came to be owned by nobody. Naming the subsystem is not the same as naming the file, and a load-bearing file that no document claims is a defect in the agent architecture before it is a defect in the code. **Any future subsystem this domain owns gets its files listed here, not just its concept.**
- **The carrier frame, BY FILE (added 2026-08-03, CE-31).** `web/src/world/FramePose.ts` (the rigid-pose value type and its algebra), `web/src/world/CarrierFrame.ts` (the `CarrierFrame` interface, `CarrierRegistry`, and the two instrument frames), `web/src/world/CarrierRide.ts` (a body riding one), `web/src/world/CarrierSources.ts` (the concrete frames over `VesselRegistry` and `of_body_state`), `web/src/app/DebugCarrier.ts` (the driven surface) and `web/tools/smoke/posecheck.mjs` (the headless algebra gate). Listed by file per CE-21's rule, in the same pass that created them rather than a year later.
- **Carrier-local geometry, BY FILE (added 2026-08-03, CE-80).** `web/src/world/CarrierGeometry.ts` (`PosedInFrame`, `CarrierMount`, `CarrierMounts`), `web/src/app/StationMount.ts` (the composition-root binding: `STATION_CARRIER_ID`, `mountStation`, `mountStationOn`) and `web/tools/smoke/probes/stationride.js` (the driven gate). `web/src/world/FramePose.ts` gains `composePose` and `invertPose`. Listed in the same pass that created them, per CE-21's rule.
- **Carrier MEMBERSHIP and the arrival, BY FILE (added 2026-08-11, CE-39 to CE-43).** `web/src/world/CarrierBoarding.ts` (`BOARD_MARGIN_M`, `RELEASE_HYSTERESIS_M`, `RideSeat`, `MountSet`, `RideDecision`, `BoardingRule`) is new: the per-tick board/release decision, split off `CarrierGeometry.ts` on the same seam `CarrierFrame.ts` / `CarrierRide.ts` are split on, and for the same reason (where the deck is, versus who is standing on it). `web/src/world/CarrierGeometry.ts` gains `CarrierMount.containsPoint` / `depthAt` / `carries`, `CarrierMounts.mountOf` / `mountCarrying` / `mountContaining` / `decideAt`, and a `bounds` option on `attach`. `web/src/app/StationMount.ts` gains `seatOnStationDeck` (plus `DeckSeat`, `DeckWalker`, `StationSeat`). `web/src/app/Loop.ts` gains the ONE call site. `web/src/app/VisitSites.ts` gains the optional `rideStation` port and `web/src/app/MenuBoot.ts` supplies it. `web/tools/smoke/probes/stationboard.js` is the driven gate. Listed in the same pass that created them, per CE-21's rule.
- **CE-51 / CE-52 (2026-08-11), BY FILE.** `web/src/app/FrameTrace.ts` is new: `FrameTrace`, `FrameSample`, `TracedPose`, `carrierFrameTrace`, the first per-RENDERED-FRAME instrument in the project. `web/src/world/CarrierGeometry.ts` gains `CarrierMount.syncWatchersAt`, `drawnPos`, `drawn`, `lastDrawnTick` and `CarrierMounts.syncWatchersAt`. `web/src/app/Loop.ts` gains `alpha`, `renderTick` and the one call site. `web/src/app/DebugCarrier.ts` gains the `frames` op. `web/tools/smoke/probes/stationframe.js` is the driven gate. Listed in the same pass, per CE-21's rule.\n- **CE-53 (2026-08-13), BY FILE.** No new files. `web/src/app/DebugCarrier.ts`'s private `turnBetween` changes formulation (both call sites, `turnPerTickRad` and `turnRad`, and every consumer gets a strictly more accurate number); `web/tools/smoke/probes/carrier.js` C1's rotor row changes what it compares against and tightens 1e-6 -> 1e-9. The other two consumers of the field, `probes/stationboard.js` and `probes/stationride.js`, only test it `> 1e-9` and are unchanged. Listed per CE-21's rule even though nothing was created, because the instrument is the thing that moved.
- **CE-49 / CE-50 (2026-08-11), BY FILE.** `web/src/app/StationMount.ts` gains `stationArrivalBody`, `StationArrival`, `SolidQuery` and the private `capsuleClearAt`, and `seatOnStationDeck` gains an optional `SolidQuery`. `web/src/app/VisitSites.ts` gains the `stationArrival` port so the fallback door lands on the same point. `web/src/app/MenuBoot.ts` supplies both from `gameplay.structures.bodies`. `web/tools/smoke/probes/stationvisit.js` gains the capsule-clearance check, its negative control and the deck-relative walk. Listed in the same pass, per CE-21's rule.
- **CE-47 / CE-48 (2026-08-11), BY FILE.** `web/src/app/StationMount.ts` gains `installAndMountStation` and `StationInstallDeps`, which is where `Boot.ts`'s inline station install now lives, and `mountStation` gains an `at` tick (defaulted, so no caller changed). `web/src/world/CarrierGeometry.ts` gains `CarrierMounts.lastTick`, published in the census. `web/src/app/Boot.ts` gains the `stationRebuild` holder and the one call at the end of `buildBodyScope`. `web/tools/smoke/probes/stationreboot.js` is the driven gate. Listed in the same pass, per CE-21's rule.
- **CE-45 (2026-08-11): no new files, and the fixture problem is over.** `ph357-station-stamp` merged, so `game/SpaceStation.ts` gains `installStation`'s `registry.stamp(rec, tick)` and `stationBodyPosNow()` (physics', listed here because this domain's probes and `app/VisitSites.ts` bind to both). `web/tools/smoke/probes/stationboard.js` no longer registers an instrument frame at all.
- **The world/session lifecycle, BY FILE (added 2026-08-03, CE-20).** `web/src/world/WorldSession.ts`, `web/src/app/Lifetime.ts`, `web/src/sim/wasm/HandleLedger.ts`, `web/src/app/DebugLifecycle.ts`, and the **`Services` record's own lifetime** (`web/src/app/Services.ts`: what is fixed at boot, what follows the session, and who may tear anything down). The teardown *contract* is this domain's even where the object being torn down is another domain's.
- **Non-goals:** rendering (→ rendering), the physics math itself (→ physics), factory rules (→ factory-sim). This domain provides the *substrate* they run on. Note the boundary this pass had to hold: core-engine owns *what dispose means and who calls it*; a domain still owns *what its own dispose does*.

## 3. Key design decisions
| # | Decision | Rationale | Status | Date |
|---|----------|-----------|--------|------|
| CE-1 | Engine = UE5 (mirrors global D-001) | Native double-precision world coords + Mass Entity | Provisional — Spike 1 is the validation gate (R1 pass/fail decides this) | 2026-06-14 |
| CE-2 | Active/on-rails generalized to all entity types (vessels *and* factories) | The core scalability lever (D-003) | Accepted | 2026-06-14 |
| CE-3 | Fixed sim tick decoupled from render; render interpolates | Determinism-friendly, stable physics, time-warp support | **Accepted** (settled in Spike 1 §4: accumulator loop, 60 UPS, `Alpha` interpolation, `TickIndex`) | 2026-06-14 |
| CE-4 | Coordinate authority = 64-bit; engine/GPU layer = 32-bit near floating origin | Solves planetary-scale precision loss | Accepted | 2026-06-14 |
| CE-5 | **Drive our own explicit floating-origin rebase; do NOT rely on UE's automatic World-Composition origin shifting** | Research: UE auto origin-shift is single-player-only, snaps in integer-meter quanta, jitters world-space materials/Niagara/Chaos; LWC widens *authority* but the GPU/Chaos hot loop is still 32-bit near the tile. We need tight control to stress-test R1. (Spike 1 §1.1) | **Accepted** | 2026-06-14 |
| CE-6 | **Floating-origin rebase is dual-trigger: position (4 km) AND Krakensbane-style frame-velocity subtraction (>800 m/s)** | A position-only threshold is unstable at orbital speed; subtracting a frame velocity keeps Chaos sub-orbital regardless of true speed. (Spike 1 §1.3–1.5) | **Accepted** | 2026-06-14 |
| CE-7 | **Registry supports MIXED proxy granularity: a proxy is an opaque `ISimProxy`; promotion/demotion/hysteresis run per-proxy regardless of what it represents (point vessel vs. coarse factory chunk). The bubble test uses the proxy's near edge via a defaulted `PromotionRadius()` accessor.** | RC-10 reconciliation. physics registers one proxy per vessel (point); factory-sim (FS-7) registers one per factory *chunk* (coarse, has spatial EXTENT). A centroid-only distance test under-promotes a large chunk (near edge close, centroid far) → fails "promote anything the player can touch." `gap = max(0, ‖Pos−observer‖ − PromotionRadius())` inflates a chunk's band by its extent; points default to radius 0 → unchanged. Additive, non-breaking. (Spike 1 §3.2.1, §5.3) | **Accepted** | 2026-06-14 |
| CE-8 | **C-7 — save-quiesce handle: `ISimClock::QuiesceAtTickBoundary(body)` (additive method).** Parks sim advance at end-of-tick N, invokes `body(N)` once while NO domain advances, then resumes (no time make-up — a save is not a warp). | Persistence (persistence-phase1 §4.2/§7) needs all domains to snapshot at the SAME `TickIndex` → no torn cross-domain read. Only core-engine's fixed-tick loop (§4) can enforce this, so the fence lives on `ISimClock`. Pure addition — existing `ISimClock` callers unaffected. Determinism preserved (no sim state/time consumed). (Spike 1 §5.4, §5.4.1) | **Accepted** | 2026-06-14 |
| CE-9 | **C-8 — `FChunkKey.RegionDepth` (per-body): Forge depth 9 (≈1841 m region), Cinder depth 8 (≈1227 m region), under factory-sim's confirmed ~1 km factory-chunk extent** (locked per PHASE1-PLAN §10, 2026-06-14, reconciled per the 2026-06-14 review; was 11/9 under a 256 m placeholder). `FChunkKey` = coarse PREFIX of world-gen `FQuadKey` truncated to `RegionDepth`; region edge = `R·(π/2)/2^d`. | Persistence asks "1 save region ≈ 1 factory chunk proxy" (persistence-phase1 §3.2). Picked smallest depth with region edge ≥ chunk extent (region ⊇ chunk + margin). Per-body because Cinder (R=200 km) is 3× smaller than Forge (R=600 km) → needs depth 1 shallower for the same span. **Corrects persistence's placeholder depth 5–6** (that swallowed the whole factory; the ask is chunk-granule). **Reconciled: factory-sim confirmed extent E ≈ 1 km; depth = `floor(log2(R·(π/2)/E))` per body.** No core contract change — only the integer moved. (Spike 1 §5.5) | **Accepted (locked per PHASE1-PLAN §10, 2026-06-14)** | 2026-06-14 |
| CE-12 | **`persistence_file.h` is excluded from the browser build; `persistence.h` is not.** The byte serializer compiles and runs in WASM unchanged (proven by a `VoxelEdits` serialize round-trip whose bytes are bit-identical to the native build). Only the `<filesystem>` container is dropped; the JS side owns IndexedDB/OPFS storage of those byte streams. | `std::filesystem` has no browser implementation. Splitting the format from the container was already the design, so the pivot costs one adapter, not a format change. **Hands PS-6 (atomic single-slot save file) to the persistence domain as a JS-side task.** | **Accepted** | 2026-07-25 |
| CE-11 | **The WASM build is the CANONICAL target for world generation; the ~1 ULP `libm` delta vs a native build is accepted, not fixed.** WASM↔native terrain is NOT bit-identical: `std::tan` in `cubed_sphere.h::warp()` (plus `asin`/`atan2`/`cos` in `biome.h`) differs by 1 ULP between mingw-w64 libm and emscripten/musl libm at ~2.8% of cube-sphere lattice arguments. Because height is position-hashed from the direction's raw bits (WG-6, by design), 1 ULP in yields an *unrelated* height at ~1.7% of vertices. | Both builds are internally perfect — WASM self-determinism, crack-free shared edges, tunnel semantics, exact factory arithmetic and persistence bytes are all bit-for-bit (parity Tier 0 19/19, Tier A 94/94). The game is browser-only: one binary, so every client agrees with every other client and seed+diff saves stay portable. The fix (vendor fixed `tan`/`asin`/`atan2`/`cos` into `/core` — the ONLY four transcendentals in the determinism path; everything else is floor/fabs/sqrt/mul/add and already exact) shifts the seed→world mapping once and is not worth paying until a NATIVE peer exists. **Revisit if a native dedicated server or a native content-baking tool is ever planned.** Full diagnosis: [WASM-BRIDGE §6.3](../web/WASM-BRIDGE.md#63-tier-b--the-honest-caveat-fully-diagnosed). | **Accepted (Admin-notified)** | 2026-07-25 |
| CE-13 | **The WASM bridge reads the surface oracle, and an executable guard enforces it (ABI 2).** Audit of every export in `of_core_api.cpp` found **three** authority defects. (1) `of_observer_latlon_alt` derived positions from `terrain_stream.h::makeObserverLatLonAlt`, i.e. the RAW heightfield: at lat 48 / lon 18 on Forge that is 4,075.51 m where the designed surface is 6,520.81 m, so an "altitude 60 m" observer spawned **2,445.30 m underground** and every chunk rendered behind the camera. It now returns `dir * (surfaceRadius + alt)` bit-exactly and takes an `edits` handle. (2) `of_quadmesh_generate`'s flag was `designedBase`, so `0` (the value a forgetful caller passes) selected RAW; inverted to `rawBase` so **zero means correct**, and raw-base combined with voxel edits is now refused because a designed-base lowering applied to a raw base is the original WG-21 bug rebuilt by hand. (3) `of_chunk_max_offset` returned the largest single-AXIS offset over interior vertices only (52,639 m where the furthest emitted vertex was 108,403 m), unusable as a bounding radius; it is now the true Euclidean max over interior **and** skirt. | The bridge was written one commit after [DECISIONS.md](../web/DECISIONS.md) standing rule 1 ("one surface authority; no module re-derives terrain height") and broke it anyway, in the exact way [core-docs-audit Task B](../review-2026-06-16/core-docs-audit.md) predicted. **A rule in a document is not a control.** The guard is now executable and gates the build: `dump_expected.cpp` CASE 7b refuses to emit a fixture unless the observer equals the oracle position, and `parity.mjs` CASE 7b/7c assert the positive identity **and** the negative one (raw and designed must still differ by >100 m, or the test is vacuous). Parity Tier 0 19/19 → **29/29**, Tier A 94/94, fixture re-baselined by 3 lines with every streaming/chunk/LOD hash unchanged. Detail: [WASM-BRIDGE §4.0](../web/WASM-BRIDGE.md#40-surface-authority-which-export-reads-what). | **Accepted** | 2026-07-25 |
| CE-19 | **The teardown contract: `dispose()` has five clauses, its caller is the `Lifetime` the object was registered with, and its order is the exact REVERSE OF REGISTRATION.** Clauses (`web/src/app/Lifetime.ts`): release what the GC cannot see; release only what you constructed (ownership is by construction, not reachability); be idempotent; leave the object unusable and loud; never throw past the scope. **A step is registered where its subject is CONSTRUCTED**, so registration order is construction order and its reverse is a correct teardown order for free. | Reverse-registration is the only ordering that cannot drift, because every alternative (an ordered list, a priority number, a declared graph) is a second copy of the wiring. The clauses are written down because five `dispose` methods existed with no caller and each was missing a different one: `TerrainStream.dispose` left its `OriginRebased` subscription attached, left the worker listener and the `afterRebase` hook holding the whole scatter graph, was not idempotent, and answered afterwards as if nothing had happened. **A dispose with no caller has no way to be wrong.** | **Accepted** | 2026-08-03 |
| CE-20 | **`WorldSession`: the body-scoped lifetime, and the RE-SEAT / REBUILD rule.** Three categories, and telling them apart is the design. **Process-scoped** (WASM module, GL context, `Scenes`, `Events`, `PropLibrary`, `Loop`): never rebuilt. **Body-scoped** (terrain worker, stream, geometry pool, terrain materials, water surface, scatter): torn down and rebuilt. **RE-SEATED** (`SurfaceOracle`, `WaterOracle`, `FloatingOrigin`): neither. **The rule: re-seat an object whose IDENTITY outlives the body; rebuild an object whose STATE is a cache of the body.** `Services.body/.terrain/.materials/.scatter` became **getters onto the session**, so the published record is structurally unchanged and none of its ~90 readers moved. `session.epoch` is published so any subsystem that wants to detect its own staleness can, in one comparison, without this file knowing it exists. | `SurfaceOracle.body` going from a `readonly` constructor property to a getter is the whole fix for about a dozen collaborators that were holding a boot snapshot without anyone having decided they should. Rebuilding the oracle would have invalidated a dozen references for nothing; leaving it would have answered questions about a world that is gone. **A `readonly` field promises the holder may not write it; it silently also meant "this value is the same for the life of the process", and that was never a decision anybody made.** | **Accepted** | 2026-08-03 |
| CE-21 | **`FloatingOrigin` is core-engine's, by file, and `reseat(observer)` is SILENT.** Ownership claimed in the file header. `reseat` forgets the previous body's origin without emitting `OriginRebased`. | A `UniverseCoord` is meaningless without its frame: `{0, 600000, 0}` is a point on Forge's surface and three body-radii above Cinder, so after a switch the stored origin is not stale, it is nonsense, and the next `step()` would emit a rebase whose delta is the distance between two planets. **`OriginRebased` means "the same world moved under you, re-derive from your 64-bit anchor"**, and after a body switch the anchors are in a frame that no longer exists, so a dutiful subscriber would re-derive from a coordinate in the wrong frame. Emitting would give subscribers a third, wrong option. | **Accepted** | 2026-08-03 |
| CE-22 | **ONE body identity: `cfg.bodyId` is read at exactly one site in the client (`PlanetBody.create`); everything downstream reads `body.bodyId`.** Closed three violations: `bootTerrain` read the config (so the worker would keep generating Forge after the main thread moved to Cinder, same seed, two planets, no error), `MapBoot` derived `kind === 'moon' ? 1 : 0` for the discovery driver, and `WorkerProbe` asked the config which body the worker should build while comparing the reply against the body. | Four sources for one identity existed (`cfg.bodyId`, `body.bodyId`, `body.kind`, the literal `'Forge'`). `body.kind` cannot express two moons and `of_atmo_*`/`of_disc_*` are indexed by `BodyParams::bodyId`, so passing /core's own key is the difference between one convention and two. Note the shape: GP-271 had already fixed the atmosphere line **eight lines below** the discovery line in the same function and had not looked at its sibling. WG-144's rule, one directory over. | **Accepted** | 2026-08-03 |
| CE-30 | **`FramePose`: a reference frame answers with a rigid pose (translation + unit quaternion), local -> parent, and THE OPERATION THAT MATTERS IS THE DELTA `B . A^-1`, not the pose.** Seven numbers, not a `Matrix4`: a 4x4 can hold a scale and a shear and a frame may have neither, and a type that can express a forbidden state will one day hold one. `setPoseFromBasis` REFUSES a basis whose determinant is not +1. | The delta is INDEPENDENT of whatever basis convention a source picked for its own local axes: compose a constant re-basis `C` into both and `(BC)(AC)^-1 = BA^-1`, unchanged. That is what lets a body ephemeris (no orientation at all) and a station's LVLH deck (a rotating one) satisfy ONE interface, and it is why a consumer that wants the station's authored attitude composes its own constant offset rather than this file guessing. The determinant check is nine multiplies and it exists because it was needed: a left-handed basis produced a **unit-looking quaternion of magnitude 0.790** that silently scaled every vector it rotated, and no magnitude check anywhere could see it. | **Accepted** | 2026-08-03 |
| CE-31 | **A CARRIER IS A SECOND, NON-EXCLUSIVE FRAME ON A MOVING THING, and it is a PURE FUNCTION OF THE TICK.** `CarrierFrame.poseAt(tick, out)`; nothing is cached between calls, so an unridden carrier costs exactly zero and nobody can read a stale pose. `CarrierRegistry` is a **process-scoped object with body-scoped contents**: `Services.carriers` hands out one reference for the life of the process, and `bindTo(lt)` registers its clear with the body `Lifetime`. | "Non-exclusive" is the word that does the design work: the body frame stays what it is and a carrier is an ADDITIONAL frame, several can exist at once, a rider is on at most one, and a carrier with no rider still answers `poseAt` for a renderer or a docking test. The pure-function rule is `VesselRegistry.stateOf`'s ("nothing is advanced per tick, so an unattended fleet costs zero until somebody asks", PH-3) applied one layer up. The body-scoped contents are CE-21 a second time: Anchorage's 1,000,000 m orbit about Forge is five body-radii outside Cinder, so a carrier that survived a switch is not stale, it is nonsense. **Measured with the binding removed: the ride still held `moon` on Cinder and reported a local coordinate of -12,510,361 m.** | **Accepted** | 2026-08-03 |
| CE-32 | **The concrete carriers DERIVE from an authority that already exists; not one of them holds a number of its own.** `OrbitCarrier` reads a `VesselRecord` through `VesselRegistry.stateOf` (one Kepler solve per `poseAt`); `EphemerisCarrier` reads `of_body_state` through `CelestialEphemeris`; `RotorCarrier.fromOrbit` takes its axis and rate from a record's own `r x v`. A frozen record therefore produces a stationary carrier **with no branch anywhere**. | D-014's whole ruling is that an orbiting thing does not exist twice. A cached pose here would have been the second copy, immediately. The no-branch property is the tell that it is right: `mintStation` leaves Anchorage with `stampedTick = -1`, so `clockAt` returns the same clock for every tick and `poseAt` is constant, without this file knowing what a station is. **That is also the trap, and the probe asserts it out loud: Anchorage as it ships has `perTickM === 0`, so it is the IDENTITY ELEMENT of the operation under test and boarding it proves nothing (GP-142).** | **Accepted** | 2026-08-03 |
| CE-33 | **`CarrierRide.tick(tick, dt, step)` is ONE call that takes the step as a callback, and `KinematicBody.step` never learns that frames exist.** The sandwich: remove the frame's velocity at the rider, run the walker's own tick, transport the result rigidly, give the frame's velocity back. With nothing boarded the first line is an early return, so a world with no carrier runs the instruction sequence it ran before this file existed and `applied` stays 0 as the evidence. | Two reasons it is one call rather than a `before()`/`after()` pair. **Ownership:** `KinematicBody` is physics's file by charter, its signature is unchanged, and `CarriedBody` is a STRUCTURAL type (`{feet, vel}`) so nothing here imports `player/`; a vessel, a dropped item or a second player satisfies it without knowing this exists. **Correctness:** a caller that CAN do half of it eventually will, and half of it is a rider that gains the frame's velocity without losing it, i.e. a player accelerating off a station at its full orbital speed every tick. `KinematicBody.step` already carries the same argument about water winning over freefall. | **Accepted** | 2026-08-03 |
| CE-34 | **A RIDER IS CARRIED, NOT FREE, and the whole of that is which interval the outgoing velocity is sampled over.** The velocity is removed over `[t, t+1]` and given back over `[t+1, t+2]`, so a rider with zero relative velocity and zero apparent gravity stays at exactly zero relative velocity **in any frame , translating, turning, or accelerating**. | This was wrong first and it was a ROTATING frame that hid it: there the outgoing point velocity happens to be the incoming one rotated, so both intervals agree and only a TRANSLATING accelerating frame can exhibit the defect. Measured on Cinder's frame with the same interval on both sides: **1.2283 m of drift in ten seconds against a predicted `1/2 g T^2` of 1.2260**, i.e. a rider that stops being carried the instant the carrier accelerates. "Carried" is also the convention the project already had: `GravityVolumes` subtracts `carrierG` so that apparent gravity inside a station's freefall region is ~0, which only means anything if the occupant is moving with the station. | **Accepted** | 2026-08-03 |
| CE-35 | **THE POSITION TERM AND THE ACCELERATION TERM ARE TWO HALVES OF ONE TERM, and neither is a test of the other.** `apparent g = g(here) - a(the frame I ride in)` (GravityPort.ts, verified before being relied on) is the acceleration half and it already existed **as a scalar magnitude only** (`GravityVolumes.ts:110`, `carrierG`); there was no frame velocity and no frame position anywhere in the client. CE-33 is the other half. | The measurement is the argument and it cost a wasted pass to learn. A freely falling frame is NOT an inertial one, so "zero the gravity to isolate the transport" measures **1.2487 m of drift on a perfect transport and 0.0001 m on no transport at all**, and both readings are correct physics. With the acceleration half right (apparent g 0 in a falling carrier) the same transport gives **exactly 0**; with it wrong (full local gravity) it gives **1.228089 m against a predicted 1.226046**. The probe therefore isolates the transport on a LINEAR carrier seeded from the moon's own speed, which differs from the moon ONLY by not curving. | **Accepted** | 2026-08-03 |
| CE-36 | **`tools/smoke/posecheck.mjs`: the pose algebra has a headless gate, and it asserts DIRECTIONS rather than sizes.** Nine checks over 4,000 random bases plus a left-handed refusal and its right-handed mirror. It type-strips the SHIPPING source with `typescript` (vite 8 ships rolldown, there is no esbuild in the tree and a check may not reach the network) rather than testing a transcription. | It was written because the bug it catches is invisible to every magnitude check there is: the first `setPoseFromBasis` returned a perfectly unit quaternion that was the CONJUGATE, so `|q| - 1` read 8.9e-16, `transportPoint(A, A, p)` was still exactly the identity, and the only symptom was a rider drifting. The check that finds it is one line and it is a direction: rotating the local X axis must give back the parent X axis, and conjugated it reads **1.9957 out of a possible 2**. **And the first version of the check could not exhibit the second bug**, because all 4,000 of its fixtures built `Z = X x Y` and were right-handed by construction while the one real caller wrote `Z = Y x X`; GP-142 inside the instrument rather than the subject. | **Accepted** | 2026-08-03 |
| CE-37 | **`__of.carrier()` is the driven surface and it is DEBUG-ONLY, for the same reason `reboot` is.** The frame term is correct and general; nothing in the game boards a carrier. `standLocal` is an instrument on the same terms as `Controller.standAt` (PH-90) and exists because the state it produces , at rest in the CARRIER's frame , is one the client cannot otherwise reach, and it is one of the two readings the whole claim is a comparison between. | Making Anchorage genuinely travel is a ruling with rendering, gameplay and physics in it (the interior's 57 collision proxies would have to be re-posed per tick, and the floating origin would rebase every 2.1 s at its measured 1879.26 m/s). None of that is this domain's to decide, and shipping the term without it would be R7's residue problem a second time. **The door drives the SHIPPING path**: `board` puts a frame on the same `CarrierRide` the fixed tick calls, so there is no "simulate a carrier" entry point to keep honest. | **Accepted** | 2026-08-03 |
| CE-38 | **The carrier sandwich lives in `Loop.fixedTick`, immediately before `origin.step`, and NOT in `onFixedStep`.** | `origin.step(observer.position)` is the next line, and `onFixedStep` runs after it. A carried walker whose transport ran in a hook would be rebasing the floating origin against LAST tick's position, every tick, silently. The ordering ARCHITECTURE.md section 2.3 fixes is `input -> observer -> FloatingOrigin`, and the carrier term is part of "observer" rather than a system that runs beside it. | **Accepted** | 2026-08-03 |
| CE-80 | **A CARRIER MOUNT IS NOT A SECOND FRAME: it writes the five fields the existing registries already read, on the objects they already hold.** `CarrierMount.syncAt(tick)` asks `poseAt` ONCE and writes `pos`, `quat` and `cx/cy/cz` onto every attached body. `PosedInFrame` is STRUCTURAL, so `Solid` (game/StructureBody.ts) and `GravityVolume` (game/GravityVolumes.ts) satisfy it already and `world/` imports neither. Nothing is cached between calls. | `StructureBodies.blocks/deckUnder/resolveStep/rayHit` and `GravityVolumes.weightOf` re-read those fields on EVERY call (StructureBody.ts:187,196,269; GravityVolumes.ts:101,125), so writing them IS the whole consumer, and a parallel "posed things" registry beside them would be D-014's second authority rebuilt three days after D-014 removed the first. Mutation rather than rebuild is also what keeps `bodies.remove((s) => s === installed)` working, which is how the station's own teardown finds its solid, and avoids 57 boxes of allocation at 60 Hz. | **Accepted** | 2026-08-03 |
| CE-81 | **`CarrierMounts` is a process-scoped object with body-scoped contents, and its clear is registered LAST so teardown drops it FIRST.** Registration order in `Boot` is now `carriers.bindTo` then `ride.bindTo` then `mounts.bindTo`; reverse-of-registration teardown is therefore mounts, rider, frames. | CE-31 a third time: a mount holds body-frame geometry and a frame expressed in this body, so one that survived a switch would pose Forge's station against Cinder. And a mount holding a frame the registry has already forgotten is the dead-handle state clause 4 of the teardown contract exists to make impossible, so it must go first. `probes/carrier.js` asserts the three-step scope by name; the first version of that assertion read the list as teardown order when `of.life().scope` publishes REGISTRATION order, and the run said so. | **Accepted** | 2026-08-03 |
| CE-82 | **`composePose(a, b, out)` and `invertPose(a, out)` added to `FramePose` (ADDITIVE, no existing signature touched), gated on their DEFINING IDENTITY rather than on their arithmetic.** `posecheck.mjs` asserts `apply(compose(a,b),x) === apply(a,apply(b,x))` over 4,000 random poses, `a . a^-1 === identity`, `invert(a) applied === applyInv(a, .)`, aliasing safety, **and that composition does NOT commute** (worst disagreement asserted `> 1e3` m). Gate is 9 checks -> 14. | The identity is the only property any caller wants and the only one that cannot be satisfied by transcribing the same sign error into both functions. The non-commutativity check is the refusing case: a `composePose` that ignored an operand, or multiplied the quaternions the other way round, satisfies `compose(a, identity) === a` and `|q| = 1` and would pass everything else. **Proven red:** reversing the quaternion product makes the identity check read **32,356.35 m** and the gate exit 1; reverted and re-proven green (byte-identical to the backup AND the gate back to 14/14). | **Accepted** | 2026-08-03 |
| CE-83 | **`lastStationSolid()` and `lastStationVolumes()` are published from `game/` and return THE OBJECTS, not copies.** Two one-line getters, in `SpaceStation.ts` and `StationGravity.ts`. | A copy would be a second authority for where the station is, immediately, which is the defect D-014 settled. These are the smallest possible reach into another domain's files: nothing in `game/` decides when the station moves, and with no mount wired the station sits exactly where it always did, so every existing assertion about it is unchanged. **Flagged to Admin as the only two lines this lane wrote in `web/src/game/`.** | **Accepted** | 2026-08-03 |
| CE-84 | **THE STATION'S ATTITUDE IS A MEASURED CONSTANT INSIDE ITS CARRIER, NOT AN ASSUMPTION THAT THE TWO CONVENTIONS AGREE.** `local = poseAt(installTick)^-1 . authored`, where `authored` is read back off the pose `installStation` actually wrote rather than re-derived. | `OrbitCarrier.poseAt` publishes an LVLH basis off the record's `r x v`; `stationSolid` poses the interior nadir-from-+Y with `stationQuat`. Neither is wrong and they are **0.8648 apart as a quaternion dot, measured**. Writing the carrier's own quaternion straight onto the solid would have rotated an interior Reid has walked around inside, **and every assertion in the client would still have passed, because the hull, the collider and the gravity boxes would all have rotated together**: `orbitdeck.js`'s upside-down corridor exactly, one wrong shared pose being self-consistent. CE-30 predicted this in as many words. The probe asserts BOTH halves: the translation offset is exactly 0 m, and the rotation offset is NOT the identity. | **Accepted** | 2026-08-03 |
| CE-85 | **The mount sync lives in `Loop.fixedTick` AFTER `this.tickIndex++`, and the position is the whole decision.** | `CarrierRide.tick(t)` runs the walker's step "against geometry at pose A" where A is `poseAt(t)`, and leaves the rider at pose B = `poseAt(t+1)`. Syncing after the increment satisfies both ends with ONE call: the render between ticks sees the deck and the rider at the same instant, and the next `fixedTick` finds the geometry already where its own step needs it. Syncing at the TOP instead would draw the deck one tick behind the person standing on it, i.e. **31.32 m at Anchorage's real orbital speed**, which is a player floating outside their own station. CE-38's ordering argument, one line further on. | **Accepted** | 2026-08-03 |
| CE-86 | **`of.carrier('mounts' / 'remount' / 'unmount')` exists ONLY because the shipping station is the identity element of the mount's own operation, and `remount` goes through `mountStationOn`, the same function `Boot` calls.** | `mintStation` ships Anchorage with `stampedTick = -1`, so the shipping mount writes identical numbers 60 times a second and a probe that drove only it would be GP-142 wearing a real feature's name. `remount` puts the geometry on a `rotor` frame derived from the station's own `r x v` (**31.320919525472796 m per tick, a third independent derivation of R12's 31.320920**), and `unmount` is the reachable refusing case in the same loop. There is deliberately no second "simulate a moving station" path to keep honest. **`mountStationOn` holds the geometry WHERE IT IS at the tick it is called**, so a re-mount is continuous (measured: the swap moves the station by exactly 0 m) and does NOT heal a separation the player has already opened. The first version of the probe's closing check assumed it would; asserting that would have been asserting a teleport nothing performs. | **Accepted** | 2026-08-03 |
| CE-39 | **MEMBERSHIP IS A DISTANCE AGAINST THE BOUND THE GEOMETRY ALREADY PUBLISHES, AT TWO RADII, AND THE GRAVITY VOLUMES ARE EXCLUDED FROM IT.** `CarrierMount.containsPoint(x,y,z,margin)` unions the bounding spheres of the attachments marked `bounds`, read where `syncAt` last put them. Board at `cr + BOARD_MARGIN_M` (1.0 m), release only past `+ RELEASE_HYSTERESIS_M` (4.0 m). | `StructureBodies.blocks` answers "is this point inside a wall" and is FALSE for the air a person standing on a deck occupies, so it cannot answer "am I on the station"; the O(1) bounding-sphere reject both registries already hold is the right question and invents no second authority. **The gravity volumes are attachments too and the freefall one is 207.85 m against the interior's 28.64 m**, so a union over everything would board a player 179 m outside anything they could stand on AND would do it by reading the gravity model, which Admin ruled against (resize a field for a rendering reason, change who is aboard). A hand-authored radius was ruled against for the same reason from the other side. **The hysteresis is mandatory, not a nicety:** R36 measured 8 mode flips in 152 ticks elsewhere in this client, and here one un-boarded tick is 31.32 m of Anchorage's orbital speed applied to a person standing still. | **Accepted** | 2026-08-11 |
| CE-40 | **THE PER-TICK DECISION IS SITED IMMEDIATELY AFTER `mounts.syncAt(this.tickIndex)` IN `Loop.fixedTick`, AND THE SITE IS THE WHOLE CORRECTNESS ARGUMENT.** Also: a carrier with NO MOUNT gets no opinion. | That line is the only instant in the tick at which the deck and the rider are both described at `tickIndex`: `syncAt` has just posed the geometry at `poseAt(t)` and `ride.tick` left the rider at `poseAt(t)` too. In `onFixedStep` the rider is at `poseAt(t+1)` against geometry at `poseAt(t)`, **31.32 m apart**, and the predicate would read healthy on every frozen fixture and be wrong the day the station moves. **No-mount-no-opinion is load-bearing:** `of.carrier('board')` puts instrument frames under the rider deliberately with no geometry on them, so a release rule that fired on "not inside anything" would un-board every measurement in `probes/carrier.js` one tick after it started, and `stationride.js`'s `unmount` control IS the state "still riding, deliberately, with the mount gone". Consequence, named rather than fixed: after a `remount` a rider holding the OLD frame keeps holding it until they leave and re-enter. | **Accepted** | 2026-08-11 |
| CE-41 | **THE `visit:station` ARRIVAL SEATS THE PLAYER AT REST IN THE FRAME, THROUGH THE SEQUENCE `of.carrier('standLocal')` HAS BEEN MEASURING SINCE CE-37, PROMOTED RATHER THAN COPIED.** `seatOnStationDeck`: board, `restAt`, `standAt`, then put back the velocity `standAt` zeroed. The destination is the LIVE `lastStationSolid().pos`. | `Controller.standAt` zeroes the ABSOLUTE velocity, which on a moving carrier means "at rest in the BODY frame", i.e. a player put on the deck and left behind at full orbital speed. The two readings are the feature and the defect and they differ only in this number. **The live solid pos is not a second derivation of GP-234's authored hub centre, it IS that point carried by the frame** (the mount writes it every tick and it is the object `StructureBodies` queries); on the frozen station the two are bitwise identical, which the probe asserts as a positive control. `Controller.ts` and `KinematicBody.ts` are untouched: CE-33's boundary holds, `step()` still never learns frames exist. The debug op and the shipped press call ONE function. | **Accepted** | 2026-08-11 |
| CE-42 | **THE CENSUS PUBLISHES WHAT THE PREDICATE WOULD SAY RIGHT NOW, not only what it once did.** `of.carrier('census').aboard` carries the signed depth to the nearest bounding attachment and both radii; `mounts.boarding` carries `tested` / `boarded` / `released`. | `boards: 1` says a decision fired at some point. It cannot distinguish "declined, the player is 40 m out" from "the decision is not running at all", which are F1 and a pass that looks identical to it. `tested` growing while `boarded` does not is the evidence that the rule is running and declining. Signed depth lets a probe watch a walk-off cross the two radii rather than only see the flip afterwards. | **Accepted** | 2026-08-11 |
| CE-44 | **R97's "refuse warp while boarded" GUARD WAS NOT BUILT, BECAUSE THE STATE IS UNREACHABLE IN THIS BUILD.** Recorded as a comment at the seam in `app/StationMount.ts` instead. | Verified rather than assumed: warp is `player/FlightControls.ts` calling `FlightSession.setWarp`, and `FlightControls` is a field of `VesselObserver`, so it samples input only while the active view source is a vessel. A boarded rider is a walker: no warp key, no warp cheat (the warp-to-orbit cheat needs a live flight session). `sim/DayCycle.ts` states the rule outright: "warp is flight-local by design". **A guard on an unreachable path is dead code wearing a safety feature's name, and this project's own rule is that a fixture which cannot exhibit the defect proves nothing.** The requirement is written down where R93's dock-then-EVA will land. Reid can overrule; the guard is four lines the day the path exists. | **Accepted, flagged to Admin** | 2026-08-11 |
| CE-45 | **THE INSTRUMENT FRAME IS RETIRED FROM `probes/stationboard.js` AND KEPT IN `probes/stationride.js`, AND THE TWO REASONS ARE DIFFERENT.** The board/release/arrival measurement is now taken entirely on the shipped station; the geometry measurement keeps the rotor. | A fixture exists to make a defect exhibitable, so the moment the real thing can exhibit it the instrument is a SECOND THING TO KEEP HONEST and stationboard drops it: what ships is now what is measured, end to end. stationride keeps it for a claim the real station cannot make, that the mount follows **any** frame and not only its own conic, which is what a player-built moving platform needs; and it is a second independent derivation of the same rate (**31.320919525442836 off the record's r x v against 31.320919525483315 off the stamped conic**), which is worth more than either alone. **The refusal moved rather than went away:** section 2 now reads the SHIPPED rate before the press and stops the run if it is not 31.320919525472796, because `stashVessels` drops `stampedTick` on every save and any world between a load and its first stamp is frozen again. | **Accepted** | 2026-08-11 |
| CE-46 | **A PROBE'S OWN SNAPSHOT AGES NOW, AND THAT IS A NEW CLASS OF DEFECT IN THIS SUITE.** `stationride.js` compared a 60-tick-old `mounts` snapshot with a fresh survey. | It read identical numbers for as long as the station was frozen and reads **1,879 m** apart now, so the check would have measured the probe's own `sleep` and reported it as a translation offset in the mount. **Every probe in this suite that holds a station-derived value across an `await` is now suspect in the same way**, and the fix is to read the pair adjacently rather than to widen the bound: measured at **0 m exactly** after the fix. This is R14's family (an `await` is an unmeasured number of ticks) with a consequence that only appears once something moves. | **Accepted, and it generalises** | 2026-08-11 |
| CE-47 | **R17 IS FIXED WITH A REBUILD HOOK, NOT WITH FULL BODY-SCOPE PARTICIPATION, AND THE STATION'S INSTALL WIRING BECOMES ONE FUNCTION.** `StationMount.installAndMountStation` holds the install, the gravity, the hull's pose and the mount; `Boot` calls it at tick 0 and every rebuild calls it at the live tick. | Admin offered both shapes. Full scope participation means making the station's `Solid` body-scoped, and that `Solid` lives in `gameplay.structures.bodies` while the hull is a `StationView` in rendering's near scene: a cross-domain change with two other domains in it. What IS this domain's is that the FRAME and the MOUNT are body-scoped and must come back with the scope. **The extraction is the part that makes it honest**: `Boot` held ten lines of install-and-mount wiring inline, and a copy of them in a reboot handler would have been a second authority for where the station is, agreeing right up until one was edited. **THE TICK IS THE WHOLE DIFFERENCE BETWEEN THE TWO CALLS**, and both halves take the same one, so the install and the mount are self-consistent by construction rather than by agreement. Re-mounting the geometry WHERE IT LAY instead would leave the deck permanently behind its own orbit by the rebuild's travel, measured at **2,317.7 m**, and it reads healthy from inside the station. Guarded on the body (CE-31), so a switch to Cinder leaves it unmounted, which is correct and leaves a residue this does not fix: the interior's `Solid` is process-scoped, so its boxes are in `StructureBodies` while the player is on Cinder. Gameplay's scoping, and it predates this. | **Accepted** | 2026-08-11 |
| CE-48 | **`CarrierMounts.lastTick` IS PUBLISHED, AND IT IS A REPORT FIELD RATHER THAN A SECOND CLOCK.** | `Loop` is constructed in `main.ts` AFTER `boot()` resolves, so the composition root has no `tickIndex` to hand a rebuild hook, and a rebuild that re-poses the station at tick 0 puts the deck where the conic was at boot, which one minute of world time in is 112 km from the player. `syncAt` runs unconditionally every fixed tick even with an empty list, so this is live throughout a rebuild, which is exactly the state that reads it. It is the same shape as `CarrierMount.lastTick`, which has always existed, and `clear()` deliberately does not reset it for the same reason `added` and `removed` are not reset: it is a fact about the loop, not about the contents. | **Accepted** | 2026-08-11 |
| CE-49 | **THE ARRIVAL POINT IS THE ASSET'S OWN SPAWN SOCKET, CARRIED ON THE LIVE POSE AND VERIFIED AGAINST THE WALKER'S OWN COLLISION QUERY BEFORE THE FEET GO THERE.** `StationMount.stationArrivalBody`, behind both the carrier door and the bare-teleport fallback. | The press was seating players INSIDE `col_HallCore`, measured and photographed by Reid on a real GPU. **This is not a new authority: `stationStandLocal()` and `StationReport.standPos` both already held the right point**, and the press took the hub centre instead, through three successive passes that each made the wrong point more accurate. What is added is that the socket is applied to the LIVE solid pose rather than the boot pose (the station has moved 6,264 m by press time), and that the result is CHECKED: five columns at `CAPSULE.radiusM` by three heights at `CAPSULE_SAMPLES_M`, all through `StructureBodies.blocks`, the exact call `KinematicBody` resolves the walker against. **The scan is a guard, not the mechanism**: deterministic (8 directions by 0.5 m rings out to 12 m, fixed order) and on the shipping asset it never runs, measured `scannedM` 0. It exists so a re-authored asset that puts furniture on the spawn degrades to a metre away rather than into a wall. | **Accepted** | 2026-08-11 |
| CE-50 | **A CHECK THAT ASKS WHETHER THE ANSWER IS WHERE YOU ASKED FOR IT CANNOT NOTICE THAT THE PLACE YOU ASKED FOR IS NOT A PLACE.** | `stationvisit.js` asserted `grounded`, `onDeck` and distance-from-hub-centre, and a player standing inside a structural pillar satisfies all three EXACTLY: grounded true, onDeck true, 0 m off centre. Every assertion in the file agreed with the defect for eight months. **Same shape as R17**, which `probes/carrier.js` walked through by never asserting the mount after a reboot. The generalisation worth carrying: **a position check needs an OCCUPANCY check beside it**, and the occupancy check has to use the consumer's own predicate at the consumer's own dimensions. The negative control is what makes it a check rather than a hope: the OLD point is computed every run and asserted to overlap. | **Accepted, and it generalises** | 2026-08-11 |
| CE-51 | **THE COLLIDER AND THE HULL ARE ON DIFFERENT CLOCKS, DELIBERATELY.** `CarrierMount.syncAt` poses collision geometry at INTEGER ticks; `syncWatchersAt` poses DRAWN geometry at the FRACTIONAL `Loop.renderTick`. | The camera is interpolated between fixed ticks and the hull was not, so at 1879.26 m/s a player standing perfectly still saw the deck sweep **27.04 m peak to peak, 13.52 m within a single tick, correlated with alpha at 0.9999999860**. Giving both halves the integer tick is the stutter; giving both the fractional one would move the floor out from under a step that was already resolved against it. **`poseAt` takes a fractional tick by contract, so this is not a new authority**: it is the same function the collider, the rider and the census ask, asked at the instant the frame draws. Post-fix **0.00243 m / 0.000165 m**, with the collider channel deliberately unchanged at **27.05 m** as the discriminating control. | **Accepted** | 2026-08-11 |\n| CE-52 | **A MEASUREMENT WHOSE SAMPLE RATE EQUALS THE RATE THE SYSTEM IS CORRECTED AT CANNOT SEE THE ERROR IT IS CORRECTED FROM.** | Four station probes, 80-plus checks, all green, all sampling PER FIXED TICK, while a player watched the deck shake at 60 Hz. The fixed tick is the exact instant at which the camera and the hull are corrected into agreement, so every one of them sampled the one moment the defect is absent. `FrameTrace` samples per RENDERED FRAME and needs **more than one frame per tick** to see anything at all: at the 15 Hz every other station probe uses to survive this VM, alpha is pinned near 1 and the defect is invisible, which is why `stationframe.js` drives 120 Hz and says so. **The general rule: when a system is corrected at rate R, an instrument at rate R measures the correction, not the system.** | **Accepted, and it generalises** | 2026-08-11 |\n| CE-53 | **`turnBetween` IS `2*atan2(\|vec\|, \|w\|)` OF THE RELATIVE QUATERNION, NEVER `2*acos(\|dot\|)`, AND THE ROTOR'S PROBE ROW COMPARES AGAINST THE CHORD `2r sin(w/2)` RATHER THAN THE ARC `r*w`.** `app/DebugCarrier.ts` builds `r = b (x) conj(a)` and takes its angle; `probes/carrier.js` C1 tightens from 1e-6 to **1e-9** on the chord form. | `acos` near 1 is the worst-conditioned call in the library: `d(acos)/dx = -1/sin(t/2)`, so the returned ANGLE carries **2/sin(t/2) = 1.2772e5** times the dot's absolute error. The dot is four products and three sums of unit-quaternion components, so a few ulps of 1.0 is all it takes: **4.228e-16 becomes 5.4e-11 rad, which against Anchorage's own w = 3.132092e-5 rad/tick is 1.72e-6 RELATIVE.** Swept over 20,000 LVLH bases on a 1e6 m conic, the acos form spans **-2.350e-6 to +1.724e-6 and breaches a 1e-6 gate on 1.6% of them**; the failing run read **+1.7240873e-6, the top of that range to five significant figures**. **THE ERROR LIVES ON A QUANTISED ULP LATTICE (1,055 distinct values over 20,000 samples), which is why it reproduced bit-stably on two machines instead of jittering like the rounding noise it is** , and is the reason it read as a sim defect. **`ph357` did not break anything**: `RotorCarrier.poseAt` rotates a `base` copied ONCE in its constructor and never re-reads the record, so no station motion can enter the rotor's travel. Stamping only made the registration-tick LVLH basis vary, which redrew the lattice onto a rung outside the gate. **Second defect the same fix removes: the acos form invents a turn for a frame that did not turn** , two poses with IDENTICAL quaternions read **4.21e-8 rad**, because `\|q\|^2` lands a hair under 1 and `min(1, .)` only clamps the other side. The `turnPerTickRad === 0` rows passed only because every translating carrier here holds the exact identity quaternion; atan2 gives exactly 0 for any pose, identity or not. **Ruled out numerically before touching code:** chord-vs-arc is `w^2/24` = **4.0875e-11**, four orders too small AND the wrong sign; float32 (6e-8 relative) is 30x too small and nothing on this path is f32; frame mixing is refused by the constructor copy. Post-fix residual is **-5.28e-11 to -2.65e-11**, which is no longer error but the arc-vs-chord term itself; against the chord it is **1.44e-11 worst case**, so the 1e-9 gate has 70x headroom and discriminates again instead of sitting at 0.6x. | **Accepted** | 2026-08-13 |
| CE-70 | **A NODE THAT MATERIALISES INTO A CELL THIS SESSION HAS ALREADY HARVESTED IS RE-DRAINED TO WHAT THE PLAYER LEFT, AND THE RULE HAS ONE HOME (`web/src/game/NodeMemory.ts`, `inheritDepletion`).** `RockField.buildRock` and `TreeField.buildTree` call it in place of the three-line `pending` block they each carried. | `of_gp_node_add` unconditionally APPENDS a node at full `RemainingAmount`, and both fields then OVERWROTE `known[cellKey]` with the fresh index. `known` was the memory of the harvest and nothing ever read it back: the only re-drain path, `pending`, is filled solely by `restore()` from disk. **The rule is written once because two copies of it is PS-13's defect pre-armed** , the two fields already carry near-identical `known`/`pending`/`serialize`/`restore` and TreeField's own header says its pruning rule is "owed back to RockField". Extraction is also what keeps both files at or under the 400-line cap; `RockField.ts` goes 407 -> 403, and `TreeField.ts` stays under. | **Accepted** | 2026-08-14 |
| CE-71 | **THE DEFECT IS NOT BODY-SCOPED, AND THE BRIEF THAT SENT THIS LANE SAID IT WAS.** The trigger is a cell leaving and re-entering the streaming ring. A body switch is only the largest possible stream-out. | Measured before a line was written, one body, NO `reboot` of any kind: three trees chopped to `idx 412 9.91/24.91, idx 411 13.89/28.89, idx 394 18.77/39.77`, a 3 km walk out (all three ABSENT), and back on the same ground: **`idx 1840 24.91/24.91, idx 1839 28.89/28.89, idx 1822 39.77/39.77`**. **`TREE_RADIUS_M` is 620 m, so the case a player actually hits is walking away from a tree they chopped**, and it needs no debug verb at all. PS-49..52 inferred "body round trip" from a count of drained PRESENTED nodes falling 3 -> 1 across a switch; two of that three were streamed trees the fixture never returned to, so the regrow itself was never measured. **The transferable rule: a count that falls across an event has not shown you the mechanism, only that the population changed , and the two survivors were spawn-clearing nodes that never stream, i.e. the control was in the reading all along and unread.** | **Accepted, and it generalises** | 2026-08-14 |
| CE-72 | **RE-ADD AND RE-DRAIN, NOT RE-PRESENT THE OLD /core INDEX.** `inheritDepletion` lets `of_gp_node_add` make a fresh node and then lowers it through `_of_gp_node_drain`, the same verb a harvest and a save restore use. | Re-presenting the surviving old entry would carry the depletion for free and would freeze the node at the surface height it was first snapped to. Both fields pass a LIVE edits handle precisely so "a rock streaming in over a dug pit seats on the EDITED surface" (RockField's header); re-adding keeps that. It also costs no `g_gpNodes` growth that today's code does not already pay , every stream-in already appends , and it means there is still exactly ONE verb that can lower a node, so nothing here can reach a state a harvest could not. **`initial` is not carried, only `remaining`:** `InitialAmount` is `baseAmountOf(kind) * Grade` with `Grade` hashed from the node's DIRECTION, which is a pure function of (seed, cell, k), so the fresh node is born with the same `initial`. Measured, not assumed: **24.909327030181885 before and after, on every mark, exactly.** | **Accepted** | 2026-08-14 |
| CE-73 | **THE LIVE MEMORY OUTRANKS THE SAVED ONE.** When `known` and `pending` both know a cell, `inheritDepletion` replays the live reading. | `restore()` drains the STANDING node in place at load, so a live entry below full already carries everything the save said plus every swing since; a `pending` entry is only reachable for a cell this session has never materialised, because `restore` files a row there only when `known` lacks the key and the first materialisation deletes it. Preferring the saved value would replay an older number over a newer one. | **Accepted** | 2026-08-14 |
| CE-74 | **`regrowsPrevented` IS PUBLISHED ON BOTH FIELDS' `stats()` BECAUSE THE DEFECT IS SILENT IN EVERY COUNTER THAT ALREADY EXISTED.** | `live`, `known`, `cells`, `delivered` and `deliveredFraction` all read healthy while a chopped tree comes back whole , that is exactly how this survived from WG-67/WG-116 to now. A counter that fires once per re-drain is the cheapest thing that can go to zero if the fix is ever reverted or bypassed, and `probes/noderegrow.js` asserts it fires at least once per streamed mark **and asserts it is 0 before anything has streamed back in**, so a build that re-drained indiscriminately fails too. | **Accepted** | 2026-08-14 |
| CE-100 | **`standAt` NAMES A POINT IN THE BODY FRAME AND NEVER BOARDS, IN EITHER FRAME; `standAboard` IS THE ONLY WAY ABOARD.** The refusal is NOT extended to `{ frame: 'body' }`, and `{ frame: 'body' }` does NOT quietly board. | `standAt`'s argument is an absolute coordinate and boarding is a velocity match against a LIVE pose, so this verb has nothing to board WITH: whatever it did would be a guess. Extending the refusal would delete the only honest way to ask for the defect, and a defect nobody can request is a defect nobody can build a negative control from , `zerog.js` Z4neg is now that control and would not exist. Auto-boarding is CE-54's own argument with the sign flipped. See §5m. | **Accepted** | 2026-08-15 |
| CE-101 | **THE BODY-FRAME PATH REPORTS THE CARRIER IT LANDED INSIDE (`carrier`, `depthM`, `boarded: false`).** The membership test runs on both paths; only the ACTION differs. | CE-54 made `frame: 'body'` the one SILENT branch, so a seat at a point the deck was about to abandon returned `carrier: null` , indistinguishable from a seat in empty space. An escape hatch that cannot be observed cannot be asserted on, which is why GP-805 had to go to `of.carrier('mounts')` to diagnose Z4 at all. Measured on the control: `depthM: -28.144896`. | **Accepted** | 2026-08-15 |
| CE-102 | **`mounts.boarding` IS THE RULE'S CENSUS AND `rider` IS THE BOARDING CENSUS; THE RULE'S COUNTERS ARE NOT WIDENED.** | `boarded: 0` reads identically in a broken world and a perfectly healthy one, because `standAboard` boards through `CarrierRide.board` and `decide` then declines. Measured aboard: **`tested: 2747, boarded: 0`** beside `rider.boards: 1`. GP-805 read exactly this shape and concluded "boards nobody" , correctly for what it was looking at. Widening `boarded` would destroy the distinction the counters exist for ("running and declining" against "not running"), so the second question gets a second number. | **Accepted** | 2026-08-15 |
| CE-103 | **A PROBE ON A CARRIER MEASURES IN THE CARRIER'S FRAME, AND REPORTS THE BODY-FRAME COLUMN BESIDE IT.** | A rider standing still on Anchorage moves 31.32 m per tick in the body frame; reported as a speed that is the orbit, not the player. `of.carrier('local')` is a rigid transform so the distances stay physical, and the RATIO of the two columns (5.49e-10 m against 1879.254895 m over the same 60 ticks) is what proves the rider is carried , neither number alone can, since a frozen station passes the first and an un-boarded walker fails it. | **Accepted** | 2026-08-15 |
| CE-104 | **THE GENERATOR'S MAGNITUDE IS INSTALLED ONCE AND ONLY ITS POSE FOLLOWS THE FRAME.** `carrierG` is `g` at the INSTALL tick's radius, so it is compared against a live `of.gravity(deckR)` within the conic's own radius band, not with `===`; conversely the tidal predictions use the INSTALLED value and stay bit-exact. | Measured: **3.5315999999999974** installed against **3.5315999999999983** fetched. GP-805's `===` was written but never reached, and would have failed on correct code. On Anchorage the whole effect is `a*e` = 5.27e-10 m times `dg/dr`; on an eccentric orbit it is the full periapsis-apoapsis swing and the deck weighs the wrong amount. **Recorded, not fixed** , per-tick re-derivation is a change to the carrier term's design and is physics's. | **Accepted (finding recorded, not fixed)** | 2026-08-15 |
| CE-115 | **THE CARRIER'S LVLH POSE IS THE STATION'S ONE ATTITUDE, AND `stationQuat` IS DEMOTED TO THE AUTHORED-FRAME CONSTRUCTOR.** `SpaceStation.stationAxes` reads the mounted solid's live quaternion and falls back to `stationQuat(pos)` only when no solid is installed (`stateOfDocked`'s existing pattern, same case, no other). | Two derivations, and only one posed anything: `CarrierMount` writes `poseAt(t) . local` onto the collision solid, both gravity volumes and the drawn hull, while `stationAxes` recomputed a POSITION-ONLY nadir lock whose roll is THREE's shortest arc. They agree at the install tick and nowhere else. **Measured (`probes/stationpose.js`, one build, six samples): 0.230 deg apart at tick 121 growing LINEARLY to 1.373 deg at tick 721, 0.00190 deg/tick, unbounded over an orbit, while the POSITIONS agreed to 0 m exactly.** LVLH wins because everything that rides the station already uses it (collider, volumes, rider transport, docking port, boarding test) and its roll is tied to prograde, which is a physical convention; the holdout was the description handed to callers, and `artframe.js`'s station shot aimed its camera with it. After: 0 to 2.7e-6 deg, FLAT, which is `acos`-near-1 rounding. See §5n. | **Accepted** | 2026-08-15 |
| CE-116 | **THE DRAWN HULL HAS EXACTLY ONE WRITER, AND IT IS `CarrierMount`.** `installAndMountStation`'s `view.place(st.pos, stationQuat(st.pos))` is deleted; `mountStationOn` poses the hull itself at the install tick with `syncWatchersAt(at)`, through the same watcher every later tick goes through. | The two writes agreed BITWISE at the install tick (the solid is built with the same `stationQuat` of the same `pos`, so `local` absorbed exactly nothing), and that is precisely why the redundancy survived a year of edits with only a comment ("the mount re-poses it every tick after this") holding them together. A redundant writer that is correct on the day it is written is the shape PH-357 and CE-115 have now both paid for. `syncWatchersAt` and not `syncAt`: the collider is already exactly where `installStation` put it and re-posing it would be a second write of the pose `local` was just measured against. | **Accepted** | 2026-08-15 |
| CE-117 | **THE DRIVEN CLOCK RE-POSES THE HULL, SO "THE CAPTURE CAUGHT THE INSTALL POSE" IS FALSIFIED AND NOT MERELY UNLIKELY.** `Loop.run` calls `frame()` directly, so `syncWatchersAt` runs on every scripted frame, paused or not. | Asserted with the mount's own two counters rather than with the call graph: over `of.run(1.0, 30)`, `applied` +60 (the fixed-tick half) and `drawn` +30 (the rendered-frame half), `lastDrawnTick` 907.0000000000224 against `lastTick` 908 (fractional, CE-51, within one tick); and with `of.pause(true)`, `of.run(0.5, 30)` still advances `drawn` by 15. `probes/stationpose.js` §2. | **Accepted** | 2026-08-15 |
| CE-10 | **C-6 (core-engine side) — `IPersistable` confirmed.** Core-engine implements persistence's `IPersistable` (DomainId `CoreEngine`, GLOBAL) over the **regeneration anchors only** (not user diffs): `worldSeed` + observer `FUniverseCoord`/`FFrameId` + `SimTime`/`TickIndex` at save + floating-origin `UniverseOrigin`/`Frame`. | persistence-phase1 §2.1 / §4.5: core-engine is read FIRST and is the LAST baseline established on load (seed+time+frame must exist before any domain regenerates/patches). All fields already exist on the §5 contracts → no contract change, just an impl over them. | **Accepted** | 2026-06-14 |

## 4. Architecture & approach
- **Floating origin:** maintain a `UniverseOrigin` (64-bit) per active observer. When the observer drifts beyond a threshold (e.g. 4 km), rebase: subtract the delta from all rendered/physics transforms, add it to `UniverseOrigin`. Physics & GPU never see large coords.
- **Reference frames:** each celestial body defines a frame (position + rotation in the universe). The active observer is parented to the dominant frame (its current SOI). Frame switches (SOI crossings) are events emitted to [physics](physics.md) and [rendering](rendering.md).
- **Active/on-rails registry:** every simulatable thing registers with a `SimProxy` exposing two modes — `Active` (full sim, owned by the relevant domain) and `OnRails` (analytic state: an orbit element set for vessels; a production-rate model for factories). Core-engine owns *promotion/demotion* triggers (distance, load state, time-warp); domains own the *content* of each mode.
- **Time-warp:** in on-rails mode, advance analytic state by Δt directly (no per-tick sim). Active entities cannot be warped past physics stability limits → warp is gated by what's currently active. (MP interaction is Admin Q1.)
- **ECS boundary:** gameplay-significant but low-count things (player, active vessel) may be engine Actors; high-count things (factory entities, debris, items) live in the data-oriented sim. Define the bridge once, here.

## 5. Interfaces & dependencies
**Depends on (inbound):** the engine (D-001). Nothing else , this is the base layer.
**Provides to (outbound) , published contracts:** *(now pinned with concrete signatures in [Spike 1 §5](../spikes/spike1-core-engine.md#5-pinned-api-contracts-rendering--world-gen-code-against-these); deltas flagged below , additive, no breaking change vs. prior prose)*
- `UniverseCoord` (64-bit position) + `ToEngine/FromEngine`. **Now carries its `FFrameId`** — a coord is meaningless without its frame; `ToEngine/FromEngine` take an explicit `FFloatingOrigin`. **Used by: all.**
- `ReferenceFrame` / `IFrameGraph` + `FSOIChangeEvent`. **SOI event now carries position AND velocity re-expressed in the new frame** (physics needs both at a crossing). **Used by: physics, rendering, world-gen.**
- `SimProxy` (`ISimProxy`/`ISimRegistry`) + Active/OnRails promotion/demotion hooks. Core-engine owns promotion/demotion + hysteresis; domains own each mode's *content* (`EvalOnRails/StepActive/OnPromote/OnDemote`). **Used by: physics, factory-sim, networking.** **RC-10/CE-7 delta (additive, non-breaking):** `ISimProxy` now carries a **defaulted `PromotionRadius()`** (bounding-sphere radius about `Position()`, default `0`) so the bubble test treats **mixed granularity** uniformly — a point vessel (radius 0, unchanged) and a coarse factory chunk (radius = chunk half-extent) share one extent-aware `gap()` test. **Affects: factory-sim** (overrides `PromotionRadius()` to return chunk half-extent); **physics** unaffected (inherits 0, no code change). Spike 1 §3.2.1, §5.3.
- `SimClock` (`ISimClock`): fixed tick, warp, interpolation `Alpha`, **+ `TickIndex()`** (determinism/replication, D-004), **+ `QuiesceAtTickBoundary(body)`** (CE-8 / Phase-1 C-7: save-quiesce fence — parks advance at end-of-tick N, runs `body(N)` with no domain advancing, resumes; guarantees a no-torn-read cross-domain snapshot at one `TickIndex`). **Used by: all sim domains; `QuiesceAtTickBoundary` used by persistence.** Both additions additive, non-breaking (Spike 1 §5.4/§5.4.1).
- Events: `OnOriginRebased(DeltaEngine, NewOrigin)` and `OnSOIChange(FSOIChangeEvent)` — the rebase/frame-switch hook points for rendering, world-gen, audio.
- **`IPersistable` (CoreEngine, GLOBAL) impl + `FChunkKey.RegionDepth` value (Phase-1 C-6/C-8 — core-engine *consumes* persistence's `IPersistable`/`FChunkKey`, doesn't publish them):** core-engine serializes the regeneration anchors (`worldSeed` + observer `FUniverseCoord`/`FFrameId` + `SimTime`/`TickIndex` + floating-origin `UniverseOrigin`/`Frame` — CE-10); and pins the `RegionDepth` granule **Forge 9 / Cinder 8** (≈1841/1227 m regions, factory-sim's confirmed ~1 km chunk — CE-9; locked per PHASE1-PLAN §10, 2026-06-14, was 11/9 under a 256 m placeholder), `FChunkKey` being a coarse prefix of world-gen's `FQuadKey`. **No core contract change** — both are values/impls over already-pinned §5 types.

**5a. The browser world lifecycle (NEW 2026-08-03, additive; nothing in §5 above changed).**
- **`Lifetime`** (`web/src/app/Lifetime.ts`) — a scope with an end. `add(label, fn)` / `own(label, obj)` / `addUnsubscribe(label, off)` / `end(): TeardownReport`. `end()` never throws; each step gets its own try/catch and failures come back in the report, so **the caller asserts the failure list is empty**. `add` after `end` THROWS (registering into a dead scope is a leak with no symptom). `DisposeGate` is the idempotence + loudness pair in three lines.
- **`WorldSession`** (`web/src/world/WorldSession.ts`) — `body` / `terrain` / `scatter` / `lifetime` / `epoch` / `isRebooting` / `reboot(bodyId?)` / `audit()`. The composition root supplies a `BuildBodyScope` thunk; this class must never learn the wiring or it becomes a second `Boot.ts`.
- **`Events.census()`** (additive on the existing bus) — per-key subscriber AND emit counts. `Events.on` has always returned an unsubscribe; the defect was four call sites discarding it.
- **`HandleLedger`** (`web/src/sim/wasm/HandleLedger.ts`) — installed inside `loadOfCore`, which is the only module-construction site on the main thread and in every worker, so the census is total by construction rather than opt-in. It **refuses to load** if any `*_destroy` export has a kind whose creator it cannot see, which is the reachable refusing case for the instrument itself.
- **Debug surface:** `of.life()` and `of.reboot(bodyId?)` (`web/src/app/DebugLifecycle.ts`), plus `staleHolders()`. **`reboot` is debug-only on purpose** and must not be wired to a door a player can walk through until the residue in §7 R7 is zero.
- **Worker protocol (additive):** `TerrainInitedMsg.handles` carries the worker's own handle census, which is the only evidence the main thread can get that a rebuilt scope got a NEW heap.

**5a-2. The carrier frame (NEW 2026-08-03, additive; nothing in §5 above changed).**
- **`FramePose`** (`web/src/world/FramePose.ts`) — `{px,py,pz,qx,qy,qz,qw}`, local -> parent. `apply` / `applyInv` / `rotate` / `rotateInv` / `transportPoint(a,b,..)` / `transportDir` / `pointVelocity(a,b,dt,..)` / `rotatePoseAboutOrigin` / `setPoseFromBasis` (throws on a determinant that is not +1). No allocation: every function writes an `out` the caller owns.
- **`CarrierFrame`** (`web/src/world/CarrierFrame.ts`) — `{ id, what, poseAt(tick, out): FramePose }`. **Takes a TICK, fractional allowed**, because that is the client's one clock (`Loop.tickIndex`, and `VesselRegistry.clockAt` is the only thing that maps it to mission seconds). Must be a pure function of the tick: the ride asks for `t+2` before `t` has happened.
- **`CarrierRegistry`** — `add` (throws on a duplicate id) / `remove` / `get` / `clear` / `bindTo(lt)` / `census()`. On `Services.carriers`.
- **`CarrierRide`** (`web/src/world/CarrierRide.ts`) — `board(f)` / `release()` / `tick(tick, dt, step)` / `localAt` / `parentAt` / `restAt` / `report(tick)` / `bindTo(lt)`. Carries a **`CarriedBody`**, which is the structural type `{ readonly feet: V3; readonly vel: V3 }` and deliberately not an import of `KinematicBody`. On `Services.ride` (non-null exactly when `Services.player` is).
- **Concrete frames** (`web/src/world/CarrierSources.ts`) — `OrbitCarrier(id, M, rec)`, `EphemerisCarrier(id, M, bodyId, secsPerTick)` (with `.available`, which refuses a wasm predating PH-161 rather than shipping a carrier that silently never moves), `RotorCarrier.fromOrbit(id, orbit, tick0, secsPerTick)`.
- **Debug surface:** `of.carrier(op, arg)` with ops `census register survey remove board release local standLocal` (`web/src/app/DebugCarrier.ts`). **Debug-only on purpose (CE-37).**
- **Gate:** `node tools/smoke/posecheck.mjs` (14 checks, exit 1 on failure) and `probes/carrier.js` (43 checks, `--scenario=walk --settle=25`). CE-53: C1's rotor row now gates at 1e-9 on the chord, not 1e-6 on the arc. **CE-85 (2026-08-14): the "new" 1.7e-6 rotor red re-filed against that row IS CE-53's red, reported off a fork predating `a4f396d`; 43/43 twice on real D3D11, live residual 1.97e-12, and THE 1e-9 GATE STANDS UNWIDENED.** **CE-86: `carrier.js` documented no invocation until 2026-08-14, so `probeall.mjs` never ran it in the documented sweep , the gate existed but the census could not see it.**

**5a-3. Carrier-local geometry (NEW 2026-08-03, additive; nothing in §5 above changed).**
- **`PosedInFrame`** (`web/src/world/CarrierGeometry.ts`) , the structural type `{ pos: {x,y,z}; quat: { set(x,y,z,w) }; cx; cy; cz }`. **Any domain's object satisfies it without importing anything**, which is `CarriedBody`'s decision (CE-33) applied to geometry. `cr` is deliberately NOT written: a rigid motion does not change a radius.
- **`CarrierMount`** , `attach(body, what, local?)` / `watch(fn, what, local?)` / `syncAt(tick)` / `report()`. `local` is a FIXED pose inside the carrier and is **copied**, never held. One `poseAt` call per sync, whatever is attached.
- **`CarrierMounts`** , `mount(frame)` / `syncAt(tick)` / `clear()` / `bindTo(lt)` / `census()`. On **`Services.mounts`**, never null.
- **`FramePose.composePose(a, b, out)` / `FramePose.invertPose(a, out)`** , additive; `out` may alias `a` or `b`, and the gate asserts it.
- **`STATION_CARRIER_ID = 'station:anchorage'`** and **`mountStationOn(mounts, frame, view, at)`** (`web/src/app/StationMount.ts`).
- **THE BINDING FOR ANY OTHER DOMAIN IS STILL TWO CALLS AND THERE IS STILL NO THIRD.** `Services.carriers.get(id).poseAt(tick, out)` for the pose, `FramePose.apply` (or now `composePose` for a fixed sub-frame) to place a local point. A mount is the *shipped* consumer of exactly that pair, not a new way to ask.
- **Debug surface:** `of.carrier('mounts' | 'remount' | 'unmount')`. **Gate:** `node tools/smoke/posecheck.mjs` (now **14** checks) and `probes/stationride.js` (21 checks).

**Contract stability:** these four (+ two events) are the most depended-upon interfaces in the project. Spike 1 made them concrete additively (no breaks); Phase-1 C-7 added `ISimClock::QuiesceAtTickBoundary` (additive). Future breaking changes escalate to Admin (AGENT_ARCHITECTURE §6).

## 5b. Browser (WASM) surface — the pivot's new outbound contract
The nine `/core` domains now also ship to JS through **one flat C ABI**:
[`web/wasm/of_core_api.cpp`](../../web/wasm/of_core_api.cpp), documented signature
by signature in **[docs/web/WASM-BRIDGE.md §4](../web/WASM-BRIDGE.md#4-the-exported-c-api)**.
Two conventions bind every consumer and are as load-bearing as the §5 contracts:
- **Handles, not pointers.** JS holds an `int`; every `*_create` needs its `*_destroy`. Handle id spaces are **per module instance** (each instance has its own heap — see the op-log ownership model in [§5.2](../web/WASM-BRIDGE.md#52-the-ownership-corollary--plan-for-it)).
- **Never cache a heap view or a scratch pointer across a call into WASM.** `-sALLOW_MEMORY_GROWTH` detaches every existing `ArrayBuffer`; re-read `M.HEAPxx` **and** the scratch pointer every time. This is the single most likely integration bug and is spelled out in [§3.2](../web/WASM-BRIDGE.md#32-the-scratch-arena-and-the-allow_memory_growth-footgun).
- **One surface authority, at the ABI boundary too (CE-13).** Every export that answers "where is the ground" reads [`surface_field.h`](../../core/include/of/surface_field.h); RAW is exposed only where RAW is in the name and the doc says diagnostic. Where a flag picks an authority, **`0` picks the oracle**. The per-export table is [WASM-BRIDGE §4.0](../web/WASM-BRIDGE.md#40-surface-authority-which-export-reads-what) and it is enforced by parity CASE 7b/7c, not by good intentions.
The renderer's two hard requirements are met: the surface oracle is a set of plain
scalar-in/double-out exports callable **synchronously on the main thread**
(1.4–3.8 µs), and a chunk crosses as **one pre-interleaved 28 B/vertex buffer**
(`of_chunk_packed`, 1217 verts, 34,076 B) built in 3.39 ms against a 12 ms gate.
**ABI is now 2** (2026-07-25, CE-13): `of_observer_latlon_alt(body, edits, lat,
lon, altM)`, `of_quadmesh_generate(..., rawBase)`, and `of_chunk_max_offset` as a
true bounding radius. `OfCore.ts` checks the version and throws on mismatch, so
the client fails loudly rather than drifting. Note also that the renderer
**de-interleaves** the packed buffer into five sections in the worker: three.js
cannot bind mixed attribute types from one `InterleavedBuffer`, so the "bind it
directly" snippet that used to be in §4.8 was wrong and has been corrected.

## 5c. The measured numbers behind CE-19/CE-20 (so nobody has to re-derive them)
Driven, `probes/worldreboot.js`, `--scenario=walk`, port 5311.
| Claim | Number | Reachable refusing case, run in the same loop |
|---|---|---|
| Same-body reboot is exact | 309 resident chunks, 10,170 props, 10,170 live prop slots, 1,167 drawn vertices hashing `461637469`, all identical before and after | (this IS the negative control for the mechanism) |
| Cost | teardown **2 ms**, rebuild **71 ms** (dominated by one worker module load) | — |
| No subscriber leaked | per-key counts equal across a same-body reboot; back to the phase-1 values after the round trip | drop the unsubscribe in `TerrainStream.dispose` → `OriginRebased` **3 → 4** |
| No main-thread handle leaked | handle delta `{}` across every switch | skip `old.dispose()` → **`{body: 1}`** |
| The worker heap is NEW, not re-inited | `{body: 1, streamer: 1}` on every scope | a re-inited worker would report 2, then 3 (ids are per instance) |
| No prop slot leaked | `PropLibrary.instances` 10,170 → 10,170 | drop the release step → **10,170 → 20,340** (only after R8's frame gate; before it the control could not fire) |
| Forge is identical after a round trip out to Cinder and back | 309 / 10,170 / 1,167 verts / `461637469`, three times | — |
| Cinder is really Cinder | radius **200,000 m**, 96 resident, no pond, its own ground | — |

Two instrument corrections, both of which produced a confident wrong answer first: the ground hash was taken at 120 m where `meshVertsNear` **truncates at 6,000 rows and walks the resident map in chunk ARRIVAL order**, so two rebuilds of a provably identical world hashed differently (now 30 m, sorted, with `capped === false` asserted as a fixture); and the round trip failed until the observer was pinned, because **a reboot does not relocate the player**, so on Cinder the walker sits at Forge's body-frame position, 400 km above a 200 km moon with no near terrain under it, and falls.

## 5d. The measured numbers behind CE-30 to CE-38 (so nobody has to re-derive them)
Driven, `probes/carrier.js`, `--scenario=walk`, **43 checks, 43/43 green**, on an isolated `git archive HEAD web` tree on port 5317. Every leg is 600 fixed ticks (10 s) and every expected displacement is `perTickM * MEASURED ticks`, never `speed * seconds`: `of.run`'s accumulator carries across calls and two legs of the same wall-clock length differ by an integer number of ticks.

| Claim | Number | Reachable refusing case, run in the same loop |
|---|---|---|
| A rider carried by a moving frame stays put IN THAT FRAME | local drift **0.000000 m** over 600 ticks at 542.494 m/s | the SAME rider unboarded: **5424.942396 m**, i.e. **9.041571 m per tick** |
| ...and that per-tick number is physics R79's, reached through different code | **9.041571** against R79's 9.04 | — |
| ...and the unboarded rider did not move in the body frame AT ALL, which is the defect exactly | absolute displacement **0.000000 m** | (this IS the refusing case) |
| The carried rider went exactly as far as the frame did | moved **5424.942396 m**, frame **5424.942396 m** | — |
| On a REAL orbiting frame with the acceleration half right | drift **0.000000 m** | with that half wrong: **1.228089 m**, against a predicted `1/2 g T^2` of **1.226046** |
| On a ROTATING frame (the only gate that goes through the quaternion) | drift **0.000000 m** | it is neither of the two ways of being wrong: **176 m** if carried but not turned, **18,824 m** if not carried |
| The carrier ADDS motion and does not CHANGE it | same thrust, relative speed **4.025 m/s** ridden and unridden; absolute **542.514**; the walker's OWN `speedMps` **0.000846** | difference bounded by one tick of travel over the window (0.033 m/s), predicted rather than tolerated |
| Boarding and releasing move nothing | position and velocity **bitwise identical** across both verbs | a released rider keeps the frame's velocity to **1e-6 m/s**, which is what letting go of a moving thing does |
| The ride is inert with nothing boarded | `applied` **0** after boot and a run | — |
| Nothing leaks across a body scope | registry **5 -> 0**, ride released, handle delta **{}**, per-key subscribers equal | drop `carriers.bindTo(lt)` / `ride.bindTo(lt)` and **eight checks go red by name**: registry `size 5, removed 0`, the ride still holding `moon` ON CINDER at local **-12,510,361 m**, and the ground hash falling to `489443965`/64 verts |
| ...and the clear is proven twice, the second way a lying counter cannot satisfy | re-registering `moon` after the reboot **succeeds** | with the control applied it is **refused** (`already registered`), and a genuine duplicate is still refused in the green run |
| Forge is identical after a Forge -> Cinder -> Forge round trip | **1,167 drawn vertices hashing `461637469`**, before, after a same-body reboot, and after the round trip | — |
| Teardown cost | teardown **2.6 ms**, rebuild **78.3 ms**, teardown failures **[]** | — |
| Scope order | `['carriers:clear', 'carrier:release', 'terrain.stream', ...]`, so teardown releases the rider BEFORE dropping the frames | with the control applied both are absent from the list |

**The teardown control was run TWICE, by accident, and the accident is worth more than the second run.** The first was deliberate; the second happened because the `finally` that was supposed to revert it resolved its paths in the wrong directory (see §7 R15), so a run intended as the green confirmation reproduced the control instead. Both fired the same eight checks by name, with the same shape of collapse: the ride still holding `moon` after a Forge->Cinder switch at a local coordinate of **-12,510,359 m**, and Forge's ground hash falling from `461637469`/1167 verts to `916522506`/66 because the walker had been carried somewhere no terrain exists. **A control that reproduces exactly is a control; the interesting part is that a green run and a control run are told apart only by reading the numbers, which is why the check count and the named failures both matter.**

**Anchorage's real orbital speed is 1879.26 m/s, i.e. 31.32 m per tick, not the 7.5 km/s and 125 m per tick that `SpaceStation.ts` and physics R67 both state.** Forge's mu is 3.5316e12 (read off the walker's own published `trueG` at a measured radius), so `sqrt(mu/r)` at the 1,000,000 m orbit radius is 1879.2 m/s. The conclusion those documents draw is unchanged and the figure is **4.05x** out; it is Earth's low orbit. Routed in §7 R12.

Two instrument corrections, both of which produced a confident wrong answer first, and both are in [INSTRUMENTS.md](../web/INSTRUMENTS.md)'s existing families:
- **The first version of this probe read BACKWARDS**, reporting 1.2487 m of drift on a perfect transport and 0.0001 m on no transport at all, because it zeroed gravity to "isolate the term" and **a freely falling frame is not an inertial one**. Both readings were correct physics. That is what produced CE-35.
- **The seat must not be followed by an `await`.** `of.run` pauses the rAF loop and restarts it when it returns, so between two `of.run` calls the loop IS ticking; a `setTimeout(0)` after `standLocal` lets a variable number of fixed ticks pass, and on an accelerating frame a seat velocity `k` ticks stale is a relative velocity of `k*a*dt`. One run read 0.25 m where two others read exactly 0, and the only difference was how many frames the scheduler fitted into the yield.

## 5e. The measured numbers behind CE-80 to CE-86 (so nobody has to re-derive them)
Driven, `probes/stationride.js`, `--scenario=walk`, **21 checks, 21/21 green**, on an isolated `git archive HEAD web` tree (HEAD **`29d1935`**; the doc said `870f3e4`, which the 2026-08-03 history rewrite destroyed, CE-44) on port 5327. The station is standing still because its conic is frozen, so **every moving measurement is taken on a `rotor` frame derived from the station's own `r x v`**, remounted through `mountStationOn`, the function `Boot` calls.

| Claim | Number | Reachable refusing case, run in the same loop |
|---|---|---|
| The boot mount exists and is applied every tick | one mount, items `station:solid` + both gravity volumes, one watcher `station:view`; `applied` **137 -> 197** over one second | a mount that never ran writes nothing and every geometric number below still reads correctly, which is why `applied` is asserted first |
| The measured attitude offset is a real rotation, not the identity | deck-to-frame-origin **exactly 0 m**; hull-vs-carrier quaternion `|dot|` **0.8648017903054996** | `|dot| = 1` would mean the two conventions already agreed and CE-84 was unnecessary |
| The shipped station is the identity element, said out loud | `station:anchorage` **perTickM 0** | (this IS the fixture assertion; the probe FAILS here the day physics unfreezes the record, deliberately) |
| The moving fixture moves, and turns | **31.320919525472796 m per tick**, turn **3.132e-5 rad/tick** | a frozen frame reads 0 and would prove nothing |
| Re-mounting does not teleport the station | the swap moves it by **exactly 0 m** | , |
| **The deck went exactly as far as the rider** | deck **18792.275184861268 m**, rider **18792.275184859576 m** over 600 ticks | control: deck **0 m** against a rider that moved **18791.445789125573 m** |
| **And the rider did not drift inside it** | **1.72901676080656e-9 m** local drift over 600 ticks | , |
| Both gravity volumes and the O(1) bounding centre followed | volume-to-solid separation **< 1e-9 m**; `c` to `pos` **< 1e-9 m** | a bound left at the boot pose rejects every query once the thing moves further than its own radius, i.e. a deck that stops existing rather than one in the wrong place |
| **The floor is under the player, in the walker's OWN predicate** | `solidBuild(feet - 0.2 m up)` **true** after 10 s on a moving station, and **false** at the point the deck used to occupy | control: **no floor under the player at all**, and separation **1.65e-9 m -> 18791.45 m** |
| The control is the mount and nothing else | re-mounting, the deck moves again **3758.5 m in 2 s** | , |
| The pre-existing carrier gate is unregressed | `probes/carrier.js` **43/43**, `leftBehindPerTickM` 9.041571, carried/orbiting/rotating drift all **0** | , |

**The deck depth is MEASURED, not typed.** The first version probed a hard-coded 0.35 m under the feet, read `false`, and would have made every later check a claim about a point in empty space while the world's own report said `onDeck: true`. It now scans 0.05 to 2.0 m, asserts that a solid depth exists at all, and uses the middle of the range (0.2 m, 6 solid samples). **A fixture that cannot exhibit the defect proves nothing, and this one could not have.**

**`461637469` is no longer Forge's walk-spawn ground hash.** `probes/carrier.js` reads **661948540** at HEAD. Not attributable to this lane: **`29d1935`** (WG-214, written here as `870f3e4` before the history rewrite) moved the spawn off the mountain. Recorded because §7b item 6 tells the next reader that a change in that number means something real moved, and it did , upstream.

## 5g. CE-45. THE SAME MEASUREMENT AGAINST A STATION THAT ACTUALLY MOVES, with the instrument-frame numbers beside it
`ph357-station-stamp` was pushed after §5f was written and is merged. `installStation` stamps the record, `VesselRegistry.clockAt` advances it, and Anchorage travels. **Every number in this table is measured on the station the player visits, and `probes/stationboard.js` no longer contains an instrument frame at all.** Driven, `--scenario=walk --settle=25 --width=320 --height=180`, port 4301, VM `claude-dev`. **28 checks, 28/28.**

| Claim | ON THE ROTOR INSTRUMENT (§5f, frozen station) | ON THE SHIPPED STATION (stamped) |
|---|---|---|
| The carrier's rate | **31.320919525472796 m/tick**, derived from the record's own `r x v` while the record was frozen | **31.32091952549093 m/tick**, from the stamped conic. **The two agree to 1.8e-11 m**, which is two independent routes to one number and is worth more than either |
| The press boards the player | `boards` **1**, carrier `station:anchorage` | **unchanged**: `boards` **1**, carrier `station:anchorage` |
| Seat velocity (F2) | **1879.2551715283678 m/s** on the instrument; **0 m/s exactly** on the frozen shipped station, which is why the frozen run could not tell the feature from the defect | **1879.2551715278214 m/s**, read synchronously off `row.click()`. **A `standAt`-only arrival reads ~0 here, on the station Reid will fly to** |
| Where the feet land | 1.166e-10 m from the hub (frozen: live pos and install pos were the same point) | **0 m exactly** from the LIVE hub, and the install report's `pos` is by then **6,232.85 m** behind. PH-357's regression, asserted as a positive fact |
| Deck and rider over the window | deck 18792.275184861268 m, rider 18792.275184859634 m, 600 ticks | deck **18823.59471943044 m**, rider **18823.59471942887 m**, 601 ticks |
| **Deck-relative drift** | **1.6545725990496454e-9 m** (2.76e-12 m/tick) | **1.5729040161257287e-9 m** (**2.62e-12 m/tick**) |
| Un-boarded control, same run | 3747.311721457493 m, no floor | **3747.311721483005 m**, no floor. Same to 2.6e-8 m, because the control never depended on which frame the geometry was on |
| Rebases | 4 over 600 ticks vs predicted 4.698 | **5 over 601 ticks** vs predicted **4.706** |
| Membership counters | tested 1604, boarded 1, released 1 | tested **1536**, boarded **1** (walked back on, no press), released **1** (walked off past `cr + 5 m` keeping 1879.2552 m/s) |

**NOTHING SHIFTED THAT WAS NOT PREDICTED TO.** The drift moved from 1.65e-9 m to 1.57e-9 m, i.e. it stayed at the same 1e-9 order on a different frame, which is the claim the rotor was standing in for. The window is 601 ticks rather than 600 because the tick count is read back off the census rather than assumed from the seconds argument, which is the point of reading it back.

**THE TWO NEIGHBOURING FIXTURES, REWRITTEN AS THE MERGE MADE NECESSARY.**

| Probe | Result | What the stamp broke, and what it did not |
|---|---|---|
| `probes/stationride.js` | **22/22** (was 21; the inverted fixture assertion became two checks) | Section 3 said "Anchorage is frozen as it ships" and existed to be the handover signal on this exact day. It is now "Anchorage moves as it ships" with the rate PINNED at 31.320919525472796 (measured **31.320919525483315**), because a station drifting off that rate has had its orbit changed, which is a different event from having been unfrozen. **One real defect the stamp exposed:** `solid0` was a 60-tick-old snapshot compared against a fresh survey. Identical numbers while frozen; **1,879 m of travel now**, so the check would have measured the probe's own `sleep` and called it a translation offset. Read adjacently: **0 m exactly**. **The rotor is kept**, with its job changed: it is the evidence the mount is GENERAL (any frame, not only the station's own conic) and it is the second derivation above |
| `probes/stationvisit.js` | **fails []** | `P`, the hub centre the off-centre projection is taken about, was `st.install.pos`, **8,926.43 m behind by press time**: the projection would have read 8,926 m about a player standing dead centre in the hub. It reads the live solid now, the SAME authority `pressStation` teleports to. Measured: offCentre **0 m**, `feetMinusDeckM` **0**, grounded and onDeck **true**, **0 m** of drift 1.5 s later. **The check flagged as the one that would need re-reading, `grounded` after the press, did not need it**: CE-41 seats the rider at rest in the frame, so the deck is not travelling under the feet, it is travelling WITH them, and the first tick lands as it always did |
| `npm run check:pose` | **14/14** | , |
| `tsc --noEmit`, `check:limits` | clean; `check:limits` has two NEW over-cap files and **neither is this lane's**: `MapPlanner.ts` 490 -> 495 and `SpaceStation.ts` 549 -> 607, both already over the 400 cap at the merge base and both grown by PH-357's own comments | , |

**THE ONE CONFLICT IN THE MERGE WAS TWO PEOPLE FIXING THE SAME DEFECT.** PH-357 found that stamping the record makes `lastStationInstall().pos` stale and added `stationBodyPosNow()`; CE-41 found the same staleness from the rider's side and read the live solid inside `seatOnStationDeck`. Both read `lastStationSolid()`, neither re-solves the conic, so the resolution is one authority and not two: the carrier door first, PH-357's live-solid teleport as the fallback. They land on the same point and differ only in the velocity, which is the whole of CE-41.

**AND THE BRANCH'S OWN SUBJECT WAS RIGHT AND THIS DOCUMENT WAS WRONG.** It says "UNMEASURED, PARKED ON A BRANCH" and its body says "nothing here has been driven in a browser". The state of the union called it MEASURED. It is measured now, above.

## 5f. The measured numbers behind CE-39 to CE-44, ON THE FROZEN STATION (superseded by §5g, kept because it is the control)
> **READ §5g FIRST.** Everything below was measured before `ph357-station-stamp` was reachable, when Anchorage's conic was frozen and the only moving carrier was a `rotor` instrument. It is kept rather than rewritten because it is now the CONTROL: the same claims on a different frame, agreeing to the 1e-9 order. Where this section says "Anchorage is still frozen", that stopped being true on 2026-08-11.

Driven, `probes/stationboard.js`, `--scenario=walk --settle=25 --width=320 --height=180`, on `lane/carrier-rider` served from the lane's own `vite preview` on port 4301, VM `claude-dev`. **27 checks.** Anchorage is STILL FROZEN (`ph357-station-stamp` is on no reachable ref, see §7b item 1b), so **every moving measurement is taken on a `rotor` frame derived from the station's own `r x v`**, re-mounted through `mountStationOn`, the function `Boot` calls. The probe REFUSES to run the rest if that frame's `perTickM` is not > 1.

| Claim | Number | Reachable refusing case, run in the same loop |
|---|---|---|
| F1 control: nobody is aboard on the ground, **and the rule is running and declining** | `boards 0`, `applied 0`, `tested` **> 50** and rising, `boarded 0`, depth to the deck **399,201 m** | `boards: 0` alone is also what a rule that never runs looks like, which is why `tested` is asserted beside it |
| **THE SHIPPED `visit:station` PRESS BOARDS THE PLAYER.** This read `boards: 0` at HEAD and is todo #1 | `boards` **1**, carrier `station:anchorage`, `applied` **> 50** | the per-tick rule's own `boarded` stays **0**, so the press is provably the author and not a coincidence |
| Positive control: on the frozen station the LIVE deck pos and GP-234's tick-0 install pos are the same point | feet to hub **1.166424738903981e-10 m** | a second derivation of the hub centre would show up here first |
| ...and the seat velocity on a frozen carrier is zero, not 1879 | **0 m/s exactly** | (this is the identity element, asserted as such) |
| The fixture moves at Anchorage's own measured rate, and turns | **31.320919525472796 m per tick** (wanted 31.320919525472796), **3.132095e-5 rad/tick** | the shipped station reads **perTickM 0** and the probe refuses to continue on it |
| **F2 CONTROL: the arrival is at rest IN THE FRAME** | seat speed **1879.2551715283678 m/s** | a `standAt`-only arrival reads **~0 m/s** here, which is the defect: it is the same feet and a different velocity |
| **The deck went exactly as far as the rider, over 600 ticks** | deck **18792.275184861268 m**, rider **18792.275184859634 m** | , |
| **AND THE RIDER DID NOT DRIFT INSIDE IT** | **1.6545725990496454e-9 m** over 600 ticks, i.e. **2.76e-12 m per tick** | control, same run: un-boarded, **3747.311721457493 m** over 600 ticks (**6.2455 m per tick**), and **no floor under the player at all** |
| The floor is under the feet in the walker's OWN predicate | `solidBuild(feet - 0.2 m up)` **true** after 600 ticks on a moving station, and **false** at the point the deck used to occupy | control: after the un-mount and release, **false** |
| **F3: no chatter** across the 600-tick window | `boards` and `releases` both **unchanged**, `tested` **+600**, rule `released` **0** | , |
| **F4: stepping past `cr + 5 m` LETS GO, exactly once** | rule `released` **0 -> 1**, `boards` unchanged, and the released rider keeps **1879.2552 m/s** to within 1 m/s | , |
| **F1 the other way: walking back on BOARDS AGAIN with no press** | rule `boarded` **0 -> 1**, floor back under the feet | , |
| **F6: the rebase rate is the predicted one** | **4 rebases** over 600 ticks against a predicted **4.698** (4000 m / 31.320920 m per tick = 127.7 ticks) | , |
| The station's own bound, which is what membership is measured against | `cr` **28.644895616162408 m**; board at **29.64 m**, release at **33.64 m** | the freefall gravity volume's `cr` is **207.84609690826525 m**, which is why it is excluded (CE-39) |
| Deck depth, SCANNED and never typed | **0.2 m**, 6 solid samples in 0.05..2.0 m | a typed depth read `false` once in `stationride.js` and would have made every later check a claim about empty space |
| Unregressed gates | `npm run check:pose` **14/14**; `probes/stationride.js` **21/21** with deck **18792.275184861268 m**, rider **18792.275184859576 m** and local drift **1.72901676080656e-9 m**, all three bit-identical to the numbers recorded for it at CE-80; `probes/stationvisit.js` **fails []** with `grounded`/`onDeck` true, `feetMinusDeckM` **0**, `offCentreM` **0** and **0 m** of drift 1.5 s later, i.e. the CE-41 door produces the SAME arrival on a frozen carrier; `check:limits` clean of every file this lane touched (`CarrierGeometry.ts` 345, `CarrierBoarding.ts` 155, `DebugCarrier.ts` 395, under the 400 cap); `check:roles`, `check:proxies` and `tsc --noEmit` clean | stationride's OWN fixture assertion ("Anchorage is frozen as it ships") is still GREEN, which is the independent evidence that `ph357-station-stamp` really did not land |
| **`probes/carrier.js` COULD NOT BE RUN TO COMPLETION ON THIS BOX** | timed out twice under SwiftShader, at **1200 s** and again at **2700 s**, with no `eval` block returned either time; `page.evaluate: Target page ... has been closed` | NOT a claim that it is red, and NOT a claim that it is green. It drives `atHome()` three times, which teleports 12,000 km out and settles the streamer back in, and this VM cannot afford that in software rasterisation. **It is owed on a real GPU before CE-39 to CE-43 is called unregressed on that gate.** What IS measured here in its place: `applied === 0` and `boards === 0` with nobody aboard, asserted in section 1 of `stationboard.js`, which is the specific thing this lane could have broken |


**F6 IS THE ONE REAL COST AND IT IS NOW A NUMBER: a boarded player rebases the floating origin every 128 ticks, forever, and nothing in the client knows they are going fast.** `world/FloatingOrigin.ts` is a single `d2 < t*t` against `thresholdM = 4000` with **no velocity term anywhere in the file**, so **CE-6's Krakensbane trigger (>800 m/s) is ABSENT**: a rider at 1879 m/s gets exactly the rule a walker at 5 m/s gets. Reported and deliberately not built, per the brief. Frame times in this run are **p50 8.7 ms / p99 37.8 ms / worst 1886.3 ms**, and that worst is boot, on SwiftShader, at 4 sim ticks per rendered frame: **an order of magnitude, never a budget.** A real-GPU reading is owed before anyone calls the rebase cheap.

**THE HARNESS COST OF THIS MEASUREMENT, because it is a lesson and not an anecdote.** The first two attempts of this probe never finished: at the default 1600x900 and `of.run`'s default 144.3 Hz, two 600-tick windows are **2,886 rendered frames**, one Chrome sat at **733% CPU for 25 minutes** under SwiftShader, and nothing came back. Two changes fixed it and **neither touches a single asserted number**: `of.run(s, 15)` renders 9.6x fewer frames while the accumulator inside `Loop.frame` produces the identical 600 fixed ticks (15 Hz is 4 ticks per frame against a `MAX_CATCHUP` of 5, so nothing is dumped), and `--width=320 --height=180` cuts rasterisation by 25x. The run then took **9 minutes**. **A probe that measures geometry should not be paying for pixels**, and on a software rasteriser that difference is the difference between a measurement and a timeout.

**AND ONE CHECK IN A NEIGHBOUR HAS BEEN RED SINCE `29d1935` WITHOUT ANYBODY KNOWING.** `probes/stationvisit.js` pins `view.visits.length === 7`; WG-214 added an eighth site row on 2026-08-03. It has printed `fails: [...]` for eight days while `run.mjs` printed `smoke: PASS` and exited 0, which is the state of the union's §4b met in the wild rather than in a negative control. Fixed here, split into the claim it was actually about (the station row is not one of the site rows, true throughout) and the count (pinned to 8, with a message saying what to do when it moves). **Found by running a neighbouring probe as a regression, not by looking for it**, which is an argument for running neighbours rather than only the file you wrote.

**ONE CHECK WENT RED AND IT WAS THE PROBE.** `F3 CONTROL: NO CHATTER` first read `releases === 0` absolutely and failed at `releases: 1`, which was the deliberate `release()` four lines before the window opened. **A cumulative counter cannot answer a question about an interval.** It is now a delta across the window. Recorded because it is the same shape as NUMBERS.md's standing complaint: the instrument was wrong, not the thing under test, and it took a minute to tell them apart only because both counters were published.

## 5h. CE-47. R17 CLOSED: the measured numbers for the reboot
Driven, `probes/stationreboot.js`, `--scenario=walk --settle=25 --width=320 --height=180`, port 4301, VM `claude-dev`. **18 checks, 18/18.** A SIBLING rather than a section of `stationboard.js`, because that file already drives two 600-tick windows and takes nine minutes here, and a reboot is the most expensive single operation the client has.

| Claim | Number | Reachable refusing case, in the same run |
|---|---|---|
| The station moves, so a mount that stopped following it is distinguishable from one following a constant (G6) | **31.320919525449114 m/tick** | the probe REFUSES the whole run if this is not > 1 |
| The reboot really tore down and rebuilt | same body, `epoch` **1**, teardown **9 ms**, rebuild **8,897 ms**, spanning **74 ticks**, during which the station travelled **2,317.7 m** | , |
| **G1: THE MOUNT IS BACK** | **1** mount, `station:anchorage`, carrying all three items and the hull watcher, `applied` **59** and rising | control: `of.carrier('unmount')` gives **0** mounts and the rule then boards **0 -> 0** for ever |
| **G2: the carrier frame is registered again** | `registry.ids` contains `station:anchorage` | , |
| **G3: THE DECK IS ON ITS CONIC, not where it lay when the scope ended** | **0 m** off | **this is the number that separates the two possible fixes.** A hold-in-place re-mount reads **2,317.7 m** here, i.e. the rebuild's own travel baked in as a permanent offset, and it looks perfectly healthy from inside the station. The control measures exactly that: after `unmount`, the deck is **1879.2548950715407 m** off its conic |
| **G4: the press boards the player again** | `boards` **0 -> 1**, carrier `station:anchorage` | control: with no mount the rule boards nobody, for ever |
| **G5: seated at the station's own speed** | **1879.25517152564 m/s** against a wanted 1879.2551715283678 | a `standAt`-only arrival reads ~0 |
| **And it still carries correctly afterwards** | 601 ticks: deck **18823.594719430483 m**, deck-relative drift **1.3941873453354372e-10 m** (**2.32e-13 m/tick**) | , |
| WHAT MADE R17 SILENT, asserted rather than described | after `unmount` the deck is **still solid** (true at the instant, and true under the deck's own position afterwards) while the player has come off it | a control in which the floor also vanished would not be R17's shape, and the probe says so |

**THE ONE CLAIM THIS DOES NOT COVER, NAMED RATHER THAN SKIPPED: a rider who is ABOARD when the scope ends.** `of.reboot()` from the ground is **8.9 s** here; from the station's deck, 400 km up, it did not return in **two runs of over ten minutes each**. That is the terrain streamer being asked to cover a viewpoint 400 km above the surface, it is pre-existing (nothing in CE-47 touches `bootTerrain`), and `probes/stationvisit.js` names the same shape in its own header ("a walker parked 400 km up with the streamer chasing him never converges"). **It is a real gap in coverage and it is also a real defect worth someone's time**: a reboot that never returns is worse for a player than one that returns wrong. Routed, not fixed here.

**WHAT THE FIX DOES NOT DO, and it is deliberate.** It does not re-seat the rider. `ride.release()` runs in the same teardown, so a player who was aboard comes out un-boarded KEEPING the station's absolute velocity (CE-33): they coast alongside it and fall behind only by the orbit's curvature, `1/2 a t^2` at Forge's 3.5316 m/s^2, which is about **7 m over a two-second rebuild**, well inside the 33.64 m release radius, so CE-40's rule re-boards them on its own. Inventing a teleport-the-rider policy would be a second answer to "where is the player" for a case the existing rule already handles.

**UNREGRESSED, same box, same served build:** `probes/stationboard.js` **28/28** (drift 1.6240214993411655e-9 m over 601 ticks, press seat 1879.255171524751 m/s, install pos 6,264.17 m stale); `probes/stationride.js` **22/22** (deck 18792.275184861326 m, rider 18792.27518486012 m, drift 1.20971534540029e-9 m, `deckToFrameOriginM` 0); `probes/stationvisit.js` **fails []** (offCentre 0 m, grounded and onDeck true, install pos 8,769.83 m stale); `npm run check:pose` **14/14**; `tsc --noEmit` clean.

## 5i. CE-49. THE SEAT POINT: a player bug, and the assertion that would have caught it
Reid, real GPU, build `cfeffad`: the `visit:station` press seated him INSIDE A WALL. Grounded, 0.00 m/s, 398.11 km, carried correctly, and half the frame solid black because the camera was inside interior geometry. Driven, `probes/stationvisit.js`, `--width=320 --height=180`, **fails []**.

| Claim | Number | Reachable refusing case, same run |
|---|---|---|
| **The old seat point IS inside geometry** | `solidBuild` TRUE at the feet AND at **all three** walker sample heights at the live hub centre | this IS the control: computed every run, never seated at, asserted to overlap. If it ever reads clear, the probe can no longer tell the fix from the defect |
| **The new seat point is clear** | **0 of 15** capsule samples solid (5 columns at `CAPSULE.radiusM`, 3 heights at `CAPSULE_SAMPLES_M`) | , |
| Where it is | **3.999998 m** off the hub centre, which is `socket_hall` exactly | 0 m would be back inside `col_HallCore` |
| On the deck, not above or below it | `feetMinusDeckM` **8e-06 m** after settling; **0.6000080000376329 m** above the hub radius at the instant of the press, which is `stationStandLocal`'s own documented feet clearance | , |
| **And the player can WALK** | **9.053095 m across the deck** in 2.5 s, deck-relative | the first version of this check differenced the BODY-FRAME feet and read **4,666.83 m**, which is the station carrying him at 1879.26 m/s, not walking. It would have gone green for a capsule welded inside a pillar, for ever |
| The scan never runs on the shipping asset | `scannedM` **0** | , |
| Still grounded and carried | grounded and onDeck true, **2.3e-05 m** of drift 1.5 s later | , |

**THE CAUSE IS ONE SENTENCE IN GP-234 THAT STOPPED BEING TRUE, AND THREE LANES MADE IT MORE ACCURATE INSTEAD OF CORRECT.** It said "the station's local origin is both the hub centre and the deck's top face, so `pos` IS the spot to stand on". That described the PLACEHOLDER hub, which was empty in the middle. `SpaceStation.ts` records what the shipped asset did to it: "THE STATION'S ORIGIN IS NO LONGER EMPTY ... `col_HallCore` is a solid column from y = 0.000 to 5.400 spanning +/- 1.548 m in both horizontal axes, and the origin is inside it." GP-234 aimed at the origin; PH-357 made that live because the station had started moving; CE-41 read it off the live solid. **Each pass made the WRONG POINT more accurate.**

**AND THE RIGHT ANSWER WAS ALREADY PUBLISHED, TWICE.** `stationStandLocal()` reads the asset's own `socket_hall`, and `StationReport.standPos` carries it in the body frame under the comment "Body-frame point a player arriving at the station should be placed at". **The press had a correct point and a wrong point on the same object and took the wrong one.** The fix is not a new authority; it is the existing one made live and then CHECKED.

**WHY NO PROBE CAUGHT IT, WHICH IS WORSE THAN THE BUG.** `stationvisit.js` asserted `grounded`, `onDeck` and the distance from the hub CENTRE. Standing inside `col_HallCore` satisfies all three: he IS grounded, he IS on the deck, and he is ZERO metres off centre. **Every assertion in the file agreed with the defect.** Same shape as R17, which `probes/carrier.js` walked through by never asserting the mount after a reboot.

**UNREGRESSED:** `probes/stationboard.js` **29/29** (drift 1.928165763043945e-9 m over 601 ticks), `probes/stationreboot.js` **18/18** (deck 0 m off its conic, drift 4.3003365969989704e-10 m), `probes/stationride.js` **22/22**, `npm run check:pose` **14/14**, `tsc --noEmit` clean. Two assertions in the neighbours were mis-calibrated for a seat AT the frame origin and both differences are geometry: the seat speed reads 1879.2562990909673 against 1879.2551715283678 because `pointVelocity` is evaluated AT THE SEAT POINT and a rotating frame adds up to `omega x r` = 7.6e-3 m/s at 4.045 m of arm, and the feet sit 0.6 m above the hub radius because that is the socket's own clearance.

## 5j. CE-51. THE STUTTER: measured per rendered frame, pre-fix beside post-fix
Reid, real GPU, build `cfeffad`, with video: standing STILL on the station's deck stutters rapidly. ~300 fps, GROUNDED, 0.00 m/s, ORBIT, ~399 km, the rebase counter stepping 25 to 26 across two seconds, one frame catching the station double-imaged mid-snap. Driven, `probes/stationframe.js`, `--width=160 --height=90 --atmos=0 --stars=0 --shadows=0 --props=0`, **12 checks, 12/12**.

**THE MEASUREMENT WAS TAKEN BEFORE THE FIX AND IS THE NEGATIVE CONTROL.**

| Channel, per RENDERED frame | PRE-FIX | POST-FIX | bound asserted |
|---|---|---|---|
| **DRAWN hull, peak to peak** | **27.04063896095613 m** | **0.0024335834896191955 m** | < 0.01 m |
| **DRAWN hull, within ONE tick** | **13.524666351033375 m** | **0.00016539497300982475 m** | < 0.001 m |
| correlation with the interpolation alpha | **0.9999999860** | -0.436 | , |
| COLLIDER, peak to peak (the control) | 27.04 m | **27.048541665659286 m** | > 1 m within a tick, and > 1000x the drawn channel |
| COLLIDER, within one tick | 13.52 m | **13.528614421200473 m** | unchanged BY DESIGN |
| sim deck-relative drift | 1.9e-9 m / 601 ticks | **7.882773980177518e-11 m / 60 ticks** | < 1e-6 m |

**THREE HYPOTHESES, NAMED BEFORE MEASURING, ONE CONVICTED.** (a) TWO CLOCKS: the camera is interpolated between fixed ticks and the hull was posed at the last INTEGER tick. **Convicted: 27.04 m of sawtooth correlated with alpha at 0.9999999860.** (b) f32 render-space wobble: the quantum at the measured 2,537 m from the origin is 1.52e-4 m, **1.8e5 times too small**. (c) the rebase snap: **rebaseSteps 0 across the whole window**, and the sawtooth was on every frame regardless. The counter Reid saw stepping is real and is CE-43's known 2.13 s cadence; **it is not the stutter, and CE-6's Krakensbane trigger was NOT built** because the evidence does not ask for it.

**THE NUMBER THAT SAID WHICH FIX WAS CORRECT: at alpha = 1, where the two clocks coincide, the offset was ALREADY EXACTLY RIGHT** (4.3156 m, the seat), and at alpha = 0.5 it read 12.4527 m. That is a clock disagreement and not precision loss, so the fix is to give both halves one clock rather than to make either half more precise.

**THE FIX IS A SPLIT, NOT A NEW AUTHORITY.** `syncAt` keeps posing the COLLISION geometry at integer ticks, because that is what the walker's step was resolved against; `syncWatchersAt` poses the DRAWN geometry at `Loop.renderTick` = `tickIndex - 1 + alpha`. `CarrierFrame.poseAt` takes a fractional tick BY CONTRACT, so this asks the same function the collider, the rider and the census ask, at the instant the frame is actually drawn. One call site, in `Loop.frame`, before `onPreRender` because `StationView.sync` runs there and recomposes from the f64 pose this writes.

**THE COLLIDER CHANNEL IS THE DISCRIMINATING CONTROL AND IT IS MEANT TO STAY LOUD.** It is simultaneously the proof that the instrument still works and the proof that the sim path was untouched: a collider that went quiet would mean the fix had reached into the walker, which is CE-33 broken. `KinematicBody` and `Controller` are not in the diff.

**THE RESIDUAL 2.43 mm IS THE WALKER'S OWN RENDER LERP**, which interpolates a CHORD while the carrier pose follows the ARC. The sagitta over one tick at r = 1.0e6 m is 1.2e-4 m, which is what the within-tick figure reads. Sub-centimetre and no longer alpha-correlated.

**AN INSTRUMENT BUG, CAUGHT BY THE FIX APPEARING TO DO NOTHING.** The first `FrameTrace` sampled `lastStationSolid().pos`, the COLLIDER, and reported the post-fix run as byte-identical to the pre-fix one. It was right, and it was measuring the wrong object: the collider had not changed and must not. The player sees the HULL. `CarrierMount.drawnPos` publishes what the watchers were actually posed at, and carrying BOTH channels is what makes the control possible at all.

**UNREGRESSED, same box, same build:** `stationboard.js` **29/29** (drift 1.974741881832284e-9 m over 600 ticks), `stationreboot.js` **18/18** (deck 0 m off its conic), `stationride.js` **22/22**, `stationvisit.js` **fails []** (offCentre 3.999998 m, grounded and onDeck true), `check:pose` **14/14**, `tsc --noEmit` clean, every touched file under the 400-line cap. The UNBOARDED render path is asserted in `stationframe.js` section 1: two renders of the still ground scene hash identically and the ride has `applied: 0, boards: 0`.

## 5k. CE-54. `standAt` CANNOT BOARD AN ORBITING DECK, AND THE BUG REPORT WAS WRONG ABOUT WHY
RN-1412, recorded by the art-foundation lane and seconded by station-reveal: `stationdraw.js` fails unmodified with a **black frame and a perfect report**. Diagnosed, reproduced and fixed on `lane/standat-orbit`, real GPU (`ANGLE (NVIDIA, RTX 4060 Ti, D3D11)`), port 4421.

**THE REPORT NAMED ONE CAUSE AND THERE ARE TWO, AND THE ONE IT NAMED IS THE SMALLER.** Reproduced with the probe's own arrival, sampled every half second:

| Reading | Measured | What it says |
|---|---|---|
| feet-to-live-deck at the instant `standAt` returned | **5,352.394 m** | **THE AIM WAS ALREADY STALE.** `install.standPos` is written once at install; the probe aimed at where the station was at boot |
| station bound radius (CE-39's, the one membership uses) | **28.644895616162408 m** | the stale aim is **187 bound-radii** outside it |
| `mounts.boarding.boarded` over the whole run | **0** | so the per-tick rule never got the chance to catch it, and `insideBoard` read false at every sample |
| deck travel per sample second | **1879.24 m/s** | Anchorage's own rate, and the second cause: `standAt` zeroes the ABSOLUTE velocity |
| walker speed at t = 7.467 s | **26.371 m/s** | **exactly `g*t`** at Forge's 3.5316 m/s². The walker was in FREE FALL, not being carried, not boarded |
| `stationDraw` at 19,182.746 m out | **`visible: true`, `drawnParts: 2`** | every column true, the station a speck inside the 100 km far plane |

**RN-1412'S RATE IS 4.08x TOO HIGH AND THE ERROR HAS A KNOWN SOURCE.** The report derived "7.67 km/s times its own 1.52 s settle" by dividing one measured miss by one assumed speed. Anchorage travels at **1879.2551715283678 m/s**; the 7.5 km/s in `SpaceStation.ts` and physics R67 is Earth's low orbit and **§5d already flagged it as 4.05x out**. The honest decomposition is **two terms, not one**: a ~5.35 km stale aim plus 1879.2552 m/s times the settle. A single-term fit could only close by inflating the speed, which is exactly what happened, and it read plausible because the wrong figure was already written down in two places.

**AND IT IS NOT A BLACK FRAME.** Measured at 1600x900 with `readPixels` and Rec.709 luma (`airless.js`'s method):

| Frame | meanLuma | litFrac (luma > 8) | brightFrac (luma > 40) |
|---|---|---|---|
| pre-CE-54 arrival, reproduced whole | **13.79** | **0.183** | **0.12** |
| aboard, after CE-54 | **58.155** | **0.991** | **0.404** |

13.79 is the planet and the starfield, correctly drawn, with the station out of shot. **A "is the frame black" gate would have passed the defect**; what discriminates is how much of the frame is near geometry, so `stationdraw.js` gates on `litFrac > 0.6` and `meanLuma > 30`, each quoted beside its own failing value.

**THE DECISION: A REFUSAL PLUS A NEW EXPLICIT VERB, NOT A SILENT SEAT.** `of.standAt` refuses when the target is inside a mounted carrier's bound, by `BoardingRule`'s own predicate at `BOARD_MARGIN_M`, and names the alternative in the returned sentence. **The case that decided it against auto-seating is `zerog.js`**, which measures every speed it reports as a finite difference of BODY-FRAME position: a `standAt` that quietly boarded would hand it 1879 m/s where it expects a walking pace, and nothing in the probe could tell. This verb cannot know which frame the caller means and **guessing wrong is invisible in both directions**, which is the same argument its own header already makes about `grounded`. The debug API's idiom is explicit frame verbs (`carrier('board')`, `carrier('standLocal')`, `carrier('seat')`), not context-sensitive ones.

**THREE ANSWERS, ALL REACHABLE, NONE GUESSED:**
- `of.standAboard()` — the asset's own live spawn socket, through the SHIPPED `seatOnStationDeck` the `visit:station` row presses.
- `of.standAboard(lx, ly, lz)` — a point in the **station's authored local frame** (+X spine, +Y radial, +Z across), resolved against the LIVE pose. **Local and not body-frame coordinates is the half of RN-1412 the velocity does not cover**: every published body-frame aim at the station is a boot-time record, so a local point is the only kind that cannot go stale.
- `of.standAt(x, y, z, { frame: 'body' })` — the pre-CE-54 behaviour, unchanged, for a body-frame instrument or for the defect as a control.

**GROUND BEHAVIOUR IS UNTOUCHED.** `mountContaining` returns null 400 km below the deck, so the ground path runs the instruction sequence it always ran; the cost is one bounding-sphere reject against one mount, in a debug-only verb.

**ONE SEATING AUTHORITY, NOT TWO.** `seatOnStationDeck` gained an optional `localAt` and `stationSolidPose` was extracted from `stationArrivalBody`, so the press, `carrier('seat')` and `standAboard` are one function. The CE-49 clearance scan is deliberately NOT run for a named local point (the caller named a coordinate; relocating them would be the silent move the scan's own comment refuses), but clearance is still measured and reported.

**THE FIX, MEASURED.** `stationdraw.js` `fails: []`: seated on `station:anchorage` at **1879.2562990895392 m/s** (1.13e-3 above the origin's rate, the seat's own lever arm, `stationboard.js`'s documented figure), **24.600145932933913 m inside** the bound, and after the settle the eye is **4.316 m** from the station against its 28.644895616162408 m bound, where the reproduced defect reads **6,215.659 m**.

**UNREGRESSED, same box, same build:** `stationboard.js` **29/29 fails: []**, `stationframe.js` **valid, 12 checks**, drawn amplitude **0.002404522616416216 m** against its 0.01 m bound, with its known display-cadence-sensitive CONTROL row reading **13.546598130778875 m against ~31.320919525472796 m** (42 frames over 21 ticks, alpha span 0.5, so the sampler sees half the sawtooth). `tsc --noEmit` clean, `check:roles`/`check:proxies`/`check:pose` (14/14)/`check:boot` all pass; `check:limits` FAILs on ~40 files and **did so on `origin/main` before this lane** (`DebugGameplay.ts` 642 and `StationMount.ts` 550 lines were both already over the 400 cap).

**CLOSED 2026-08-15 BY CE-100 TO CE-104, SEE §5m.** The paragraph below stood as written and its own prescription is what was taken: a boarded rider with a frame-relative instrument. Z4 is green, Z4neg is the defect kept as a named control, and the "may be true because the volume has GONE" risk is answered by the deck being under the walker's feet for the whole leg. Original text follows.

**A FINDING ROUTED TO ADMIN, NOT FIXED HERE: `zerog.js` Z4 IS RED ON `origin/main` AND STILL RED HERE, IDENTICALLY.** Verified by stashing this lane's diff, rebuilding and re-running: `"Z4: the player is not standing on the powered deck"`, the same string before and after. Its two hub sites now say `{ frame: 'body' }` out loud and behave byte for byte as they did. **What that costs is that Z4 samples gravity at a point the station is receding from at 31.32 m per tick, so `restoredExactly` may be true because the volume has GONE rather than because it cancelled** — a GP-142 vacuous pass wearing a red row. It belongs to whoever owns zerog's zero-g model, and the honest fix is a frozen station fixture or a boarded rider with a frame-relative instrument, not a frame argument.

**AND THE REFUSAL CANNOT CATCH A STALE AIM, SAID OUT LOUD.** A body-frame point 5 km off the deck is inside no bound, so `standAt` accepts it silently and always will. That is why `standAboard` takes LOCAL coordinates: the refusal closes the "wrong frame" hole and the local coordinate closes the "stale aim" hole, and only the two together make the harness honest. Any probe still composing a station aim from `install.pos` or `install.standPos` has the second defect and no instrument will report it — **`stationwalk.js` (`install.standPos`, line 136) is one, named here rather than discovered later.**

## 5n. CE-115 to CE-117. THE STATION HAD TWO ATTITUDES AND NOW HAS ONE

Handed over by the shot-baseline lane (RN-1800 to RN-1810) as "the drawn hull
has two pose authorities". It does not, quite, and the correction is worth more
than the confirmation would have been. **No `/core` change, no ABI move.**

### WHAT THE TWO AUTHORITIES ACTUALLY WERE

- **A. `stationQuat(pos)`** (`SpaceStation.ts`): `setFromUnitVectors(+Y,
  pos/|pos|)`. Position-only nadir lock; the roll is THREE's shortest-arc
  convention, i.e. whatever falls out of the position. Recomputed on every call.
- **B. `CarrierMount.syncAt` / `syncWatchersAt`**: `poseAt(t) . local`, with
  `local = poseAt(install)^-1 . authored` measured once at install. Roll rides
  the LVLH basis and is therefore tied to prograde.

**B poses everything that is drawn or collided with** , the solid, both gravity
volumes and the hull. A posed only one thing, `installAndMountStation`'s
`view.place`, and that write is overwritten by the watcher on the first frame.
So the DRAWN HULL was not the thing with two live authorities. **What had two
was the station's PUBLISHED ATTITUDE**: `of.station().axes` was `stationAxes
(st.pos)`, which is A, and `artframe.js`'s station shot aims its camera with it
while standing on geometry posed by B. The brief's instinct was right and its
target was one hop off.

### THE MEASUREMENT (`probes/stationpose.js`, this build, real D3D11)

Angle between the published axes and the mounted solid's own quaternion, at the
same instant, six samples 120 ticks apart on the shipped station:

| tick | before (deg) | after (deg) |
|---|---|---|
| 121 / 128 | 0.230481687848029 | 2.41e-6 |
| 241 / 247 | 0.459058361894267 | 2.09e-6 |
| 361 / 367 | 0.687634625299605 | 2.70e-6 |
| 481 / 487 | 0.916210273611989 | 2.41e-6 |
| 601 / 608 | 1.144785102386016 | 1.71e-6 |
| 721 / 728 | 1.373358907186798 | 0 |

**Before is LINEAR in orbital phase at 0.00190 deg/tick with no bound on it over
an orbit; after is FLAT.** The residual is not a tolerance, it is `acos` near 1
amplifying float64 rounding by its own square root (a 1e-16 dot error becomes
1.4e-8 rad = 8e-7 deg), which is why the gate is 1e-4 deg and not 1e-15.
**`posGapM` was 0 on both**: the whole disagreement was attitude.

The comparator is proven live in the same run rather than assumed: fed the first
sample's axes and the last sample's quaternion it reports **1.07 deg**, the turn
the station actually performed over the span.

### CE-117. THE DRIVEN-CLOCK QUESTION, SETTLED

The handover asked whether the per-tick re-pose fires under `of.run`, because a
re-pose that only ran under real rAF would explain a scripted capture catching
the install pose. **It fires.** `Loop.run` calls `frame()` directly, and
`frame()` calls `syncWatchersAt(renderTick)` unconditionally. Asserted with the
mount's own counters: `applied` +60 and `drawn` +30 over `of.run(1.0, 30)`,
`lastDrawnTick` 907.0000000000224 against `lastTick` 908, and `drawn` +15 over
`of.run(0.5, 30)` with the sim PAUSED. That hypothesis is closed.

### WHAT IT BOUGHT, AND THE ONE THING IT DID NOT

`artframe.js`'s station shot, whose target grade the shot-baseline lane had to
mark UNMEASURED because three captures read 21.78 / 3.73 / 5.69 and four more
read 2.46 / 40.29 / 30.98 / 2.89: on the fixed build at base `8b324ee`, **three
consecutive captures read box luma 14.84, rgb 14.68/15.21/11.69, p50 7.64,
loFrac 0.883, identical to every published digit**, and nine across the session
span 13.81 to 15.47 (ratio 1.12 against the pre-fix 16.4).

**REBASED ONTO `ac6c38f` THE IDENTICAL TRIPLE DID NOT SURVIVE: 15.52 / 13.82 /
15.89, ratio 1.15**, and it is reported rather than re-run until it agreed
again, because re-running until it agrees is exactly what RN-1800 had to
retract. The residual moves with the WHOLE frame (`world.luma` 31.43 to 47.55,
`world.loFrac` 0.511 to 0.615) on captures whose camera eye, look direction,
elapsed-since-stamp (272), `eyeDistM` (4.32), `yawDeg` (255.35) and
`sunBearingDeg` (49.09) are identical to full float precision. The planet is
most of the IBL a sealed hull 400 km up receives, so how much terrain has
streamed by capture time reaches its fill light. **A 15 per cent lighting band,
not a 16.4x scene swap; a terrain-streaming determinism question rather than a
pose one, and named here as the next thing between this shot and a grade.**

**NOT FIXED AND HANDED TO RENDERING:** the shot's yaw solve moves from 244.15 to
255.35 deg, because it is now solved against the attitude the hull is really
drawn with. `yawOff: 45` and `pitch: 3` were CHOSEN against the pre-fix bearing,
so the reproducible frame is now the station's INTERIOR while the shot's own
`why` says exterior. That is a framing constant to re-choose, not a pose bug,
and it is rendering's call. See rendering.md R5.

## 5m. CE-100 to CE-104. THE BOARDING CONTRACT, AND `zerog.js` Z4 GREEN ON A DECK THAT IS REALLY MOVING

**CE-54 left one question open and §5k stated the answer before it was taken: "the honest fix is a frozen station fixture or a boarded rider with a frame-relative instrument, not a frame argument."** This is the boarded rider with the frame-relative instrument. No `/core` change, no ABI move, no change to what `standAt` refuses.

### CE-100. THE CONTRACT

**`standAt` NAMES A POINT IN THE BODY FRAME AND NEVER BOARDS, IN EITHER FRAME.** Its argument is an absolute coordinate; boarding is a velocity match against a LIVE pose, and this verb has no live pose. So the two candidate designs the brief offered are both declined, and for the same reason in opposite directions:

* **the refusal is NOT extended to `{ frame: 'body' }`.** That would delete the only honest way to ask for the defect, and a defect nobody can ask for is a defect nobody can build a negative control out of. `zerog.js` Z4neg is now exactly that control and it would not exist.
* **`{ frame: 'body' }` does NOT quietly board through the membership rule.** A verb whose name says "body frame" returning a rider at 1879 m/s is CE-54's own argument with the sign flipped: guessing wrong is invisible in both directions, so the caller says which, and `frame: 'body'` means what it says.

**`standAboard()` / `standAboard(lx, ly, lz)` IS THE WAY ABOARD**, and Z4 uses the no-argument form, which drives the shipped `seatOnStationDeck` , the same function the `visit:station` row presses, scanning the asset's own `socket_hall` for clearance. So the leg measures the deck a player actually arrives on.

**AND `at(0.5)` WAS THE WRONG POINT EVEN WITH THE RIGHT VERB, which closes GP-400 as a side effect.** Station-local `(0, 0.5, 0)` is inside `col_HallCore` (x and z in [-1.548, 1.548], 5.4 m tall) and `resolveStep` lets a body already inside a solid move freely, so Z6's push-off had been running with no collision at all. `socket_hall` is `(0, 0, 4)`, clear of the column by 2.45 m, and it is the asset's answer rather than the probe's.

### CE-101. `standAt`'s BODY-FRAME PATH STOPS BEING THE SILENT ONE

CE-54 skipped the membership test entirely for `frame: 'body'`, so a seat at a point the deck was about to abandon came back indistinguishable from a seat in empty space. The test now runs on **both** paths and only the ACTION differs: the body-frame request is still granted in full, and the return carries `carrier`, `depthM` and `boarded: false`. Measured on the control: `carrier: "station:anchorage"`, `depthM: -28.144896`, `boarded: false`. Cost off a carrier is unchanged (one bounding-sphere reject, one mount).

### CE-102. `boarding.boarded` IS THE RULE'S CENSUS, NOT THE BOARDING CENSUS

**GP-805 read `tested: 137, boarded: 0` and concluded "boards nobody", which was true of what it was looking at and is ALSO what a perfectly healthy world reports.** `standAboard` boards through `CarrierRide.board` directly, so `BoardingRule.decide` finds `seat.carrier` already set and returns null without incrementing. Measured on the fixed run: **`tested: 2747, boarded: 0`** beside **`rider: { carrier: "station:anchorage", boards: 1, applied: 210 }`** and 5.49e-10 m of local drift. The rule's counters are deliberately NOT widened , separating "running and declining" from "not running" is their whole value , and `of.carrier('mounts')` now publishes `rider` beside them so the two questions have two numbers.

### CE-103. THE MEASUREMENT FRAME FOLLOWS THE RIDER

Every speed in `zerog.js` was a finite difference of BODY-FRAME position, which on Anchorage's deck is 1879 m/s for a rider standing still. Z0 to Z3 are unboarded and unchanged byte for byte; Z4 onward differences `of.carrier('local')`, a rigid transform of the body frame, so the distances stay physical. Its +Y is the radial, verified in the run (a 0.19375 m rise in local y arrived as 0.193758 m in `|feet|`; Z6 asserts the two columns agree). The body-frame column is kept BESIDE the local one, because the ratio is the evidence.

**AND THE PROBE'S OWN SNAPSHOT WAS THE OTHER HALF (CE-46 again).** `st = of.station()` was read once before Z4 and its `pos`/`axes`/`deckR` then built Z5b's sample point, Z7's aim and both of Z8's EVA sites, tens of kilometres of orbit later. Every one refetches now, with no `await` between the fetch and the last use. `back()` also releases the frame before returning to the ground, because `standAt` does not release and one more `CarrierRide.tick` 400 km off the frame origin is a 31 m teleport in the middle of a measurement.

### CE-104. THE GENERATOR'S MAGNITUDE IS INSTALLED ONCE AND ONLY ITS POSE FOLLOWS THE FRAME

GP-805's `carrierG !== of.gravity(deckR)` check was written but never reached, because Z4 died four lines above it. Reached, **it fails**: measured **3.5315999999999974** installed against **3.5315999999999983** fetched. That is not a wiring defect, it is `installAndMountStation` calling `installStationGravity(volumes, st.pos, gravityAccel(st.deckR))` **once**, so the magnitude is frozen at the install tick while the mount re-poses the volumes every tick. On Anchorage (authored circular, `e` = 5.274224570254155e-16) the whole effect is the conic's own radius band, **5.27e-10 m**, times `dg/dr = -2g/r`; on an eccentric orbit it would be the full periapsis-to-apoapsis swing and the deck would weigh the wrong amount. **Recorded, not fixed:** re-deriving `carrierG` per tick is a change to the carrier term's own design and belongs with physics, not inside a probe fix.

The assertion is now that band rather than an `===`: `2*g/r * a*e + 8 ulps of g`, i.e. **9.9987e-15**, against a measured error that ranges from **0** to **8.9e-16** depending on the tick. It still catches a zero, a stale kilometre or a hand-tuned magnitude by eleven orders of magnitude. Z5 and Z5b, conversely, predict the tidal residual from the **installed** `carrierG` and stay bit-exact `===`: predicting them from `of.gravity(deckR)` reads -1.8e-15 of error that belongs entirely to the instrument asking a different question from the one the field answered.

### THE MEASURED RUN (locally built, `vite preview`, real D3D11 headless Chrome, `ok: true`, reproduced four times)

| leg | reading |
|---|---|
| **Z4neg**, the control | `carrier: station:anchorage`, `boarded: false`, `riding: null`, `onDeck: false`, `inVolumes: []`; left behind **4698.120753 m** over 150 ticks against a predicted chord-plus-fall of **4698.145411 m** (err **-0.024658 m**), having fallen **10.531061 m** |
| **Z4**, aboard | `riding: station:anchorage`, `scannedM: 0`, `clear: true`, **24.600146 m** inside the bound; grounded, onDeck, both volumes, `restoredExactly: true`; **localDrift 5.49e-10 m** against **bodyDrift 1879.254895 m** over the same 60 ticks, predicted **1879.254895 m**, err **0**; `carrierGErr 0` against a band of **9.9987e-15** |
| **Z5** | `tidalErr 0`, apparentG **-5.650813150737122e-11**, feet **8e-06 m** above the centre radius, floating, not grounded, still onDeck, still riding |
| **Z5b** | 100 m up: `errG 0`, apparentG **-0.0007062141226161778**, outward, still inside the freefall box |
| **Z6** | push-off **2.65 m**, plateau **0**, the carrier-local and body-radial columns agreeing to **0.000000 m**. (The old `authoredCeilingPredictionM: 0.85` is retired: it was the headroom over a point inside `col_HallCore`.) |
| **Z7** | peak **3.979167 m/s** local against **1879.256 m/s** body, end **0**, travelled **7.054323 m**, first solid **0.05 m** ahead and none within 1.2 m behind, **7.732852 m** off centre against the hull's own **28.644896 m** |
| **Z8** | near EVA aboard at 30 m: floating, local drift **0**, body drift **3758.508132 m**; far control released and body-seated at 200 m: `riding: null`, fell **15.291906 m** against a predicted **15.8922 m**, not floating |

**Z7's old `acrossM >= -(6 + 0.31)` is retired and replaced rather than re-tuned.** It was calibrated against a start point inside a pillar; from `socket_hall` the same walk legitimately ends at -6.59 m and the constant fires on correct behaviour. The claim is made directly instead, out of the walker's own predicate: `of.solidBuild` finds a solid ahead of the stop and open space behind it. **It is a SCAN and the first draft's failure is why:** a single sample at 0.6 m read FALSE against a wall that is really there, because `col_JambMouthAftR` is 0.30 m thick and 0.6 m ahead is already through it.

**UNREGRESSED, same box, same build:** `carrier.js` **43/43 fails: []**, `stationwalk.js` **ok: true**, `stationboard.js` / `stationride.js` / `stationreboot.js` / `stationdraw.js` / `stationreveal.js` / `padgate.js` / `research.js` all `fails: []`, `pickaxegate.js` valid, `stationvisit.js` `fails: []` (with its documented `--scenario=walk --sandbox=1`), `orbitdeck.js` `ok: true` (`--sandbox=1`), `docking.js` `fails: []` (`--sandbox=1 --settle=25`), `check:pose` 14/14, `tsc --noEmit` clean, `vite build` clean. **Three probes are red and all three are red identically on a stashed, rebuilt baseline of this same checkout:** `airlock.js` ("the ramp is not the length the fringe predicts"), `eva.js` via `evaorbit.mjs` ("E4: the RECORD advanced while the sim it describes sat still"), and `discovery.js` (the limb-fill rows).

## 5l. CE-70 to CE-74. THE REGROW: measured on one body, then across a body switch, with the refusing case beside it

**THE BRIEF WAS WRONG ABOUT THE TRIGGER AND THAT IS THE ENTRY.** Persistence's
PS-49..52 row routed this here as "an in-page body round trip regrows the
world's rocks and trees". The MECHANISM it named is right; the trigger is a cell
leaving and re-entering the streaming ring, and a body switch is only the
largest possible stream-out.

| Claim | Measured, unfixed build | Measured, fixed build |
|---|---|---|
| Three trees chopped at 19 to 25 m | `idx 412 9.909327030181885/24.909327030181885`, `idx 411 13.89078140258789/28.89078140258789`, `idx 394 18.765465259552002/39.765465259552` | same, bit for bit |
| 3 km out, **NO reboot** | all three **ABSENT** (cells left the ring) | same |
| Back on the same ground, **NO reboot** | **`idx 1840 24.91/24.91`, `idx 1839 28.89/28.89`, `idx 1822 39.77/39.77`** , full health at fresh indices | **`idx 1840 9.909326553344727/...`, `idx 1839 13.89078140258789/...`, `idx 1822 18.765464782714844/...`** |
| After `of.reboot(1)` -> moon -> `of.reboot(0)` -> home | full again, `idx 2265 / 2264 / 2247` | left amounts, `idx 2255 / 2254 / 2237` |
| The two SPAWN-CLEARING nodes (`idx 0`, `idx 1`), which never stream | drained, index unchanged | identical , **the control was in the same reading all along** |
| `regrowsPrevented` (rocks + trees) | `0` (the counter does not exist) | `3` after the walk, `6` after the switch |
| **The SAVE's depletion diff**, `of.save()` | **`rocks 0 + trees 0`** , the autosave agreed the trees had never been chopped | **`rocks 0 + trees 3`** |

**THE SAVE ROW IS THE HALF A LIVE NODE COUNT CANNOT SHOW, and it is the reason
this was worth a lane.** `serialize()` walks `known` and emits a row per node
below full. After a regrow `known[key]` points at the fresh FULL node, so no row
is emitted: a player who chopped a tree, walked 700 m and let the 20 s autosave
fire had lost the chop on disk as well as on screen.

**THE ONE BOUND IN `probes/noderegrow.js` THAT IS NOT AN EXACT EQUALITY, AND THE
FINDING UNDER IT.** The carried amount comes back a constant `4.76837158203125e-7`
(2^-21) low on two of three marks and EXACT on the third. A constant absolute
crumb is a quantisation, not drift, and it is one line in the WASM shim:
`of_gp_node_drain` ends `n.RemainingAmount = static_cast<float>(...)` on a
comment that says "`RemainingAmount` is a float". **It is a `double`
(`core/include/of/deposits.h:70`).** The cast is a stale belief costing single
precision on every drain. **PRE-EXISTING and deliberately not touched here:**
`restore()` has always re-drained through the same call, so a reloaded save has
carried exactly this fidelity since it shipped, and moving the numeric behaviour
of a shipped path is not a playability lane's call. Routed, not fixed. The probe
therefore bounds the amount at ONE FLOAT32 ULP of itself , a bound taken from
the cause, not from the observed error , with six orders of magnitude of
headroom against the defect (a regrow moves this node by **15.0**; the bound is
**1.19e-6**).

**WHAT THIS DOES NOT FIX, STATED SO NOBODY READS IT AS MORE THAN IT IS.**
1. **`known` IS NOT BODY-KEYED.** `RockField`/`TreeField` still hold the BOOT
   body's handle and radius (R-BODY-2 proper, untouched), and one map with no
   body in it means the same cell key on two bodies is one entry. **Unreachable
   today and measured rather than argued:** on Cinder the fields place nothing
   at all , `rocks live=0` out of `offeredCells 791` and `trees known=3 live=0`
   , because an airless body's biome density is 0. The day a moon grows
   anything, this becomes a real cross-body leak and `inheritDepletion` needs a
   body in its key.
2. **THE PERSISTENCE FREEZE CANNOT BE RELAXED BY THIS, and the brief invited the
   opposite conclusion.** `WorldScope`'s freeze is about the SAVE writing one
   body's world under another body's name after a switch. This fix is about node
   depletion WITHIN one body and gives no cross-body attribution to anything.
   Nothing is owed to persistence here except the note that its `probes/persist.js`
   "inherited red" is **stale**: it reads `valid: true` on the fixed build AND
   `valid: true` on a separately built HEAD dist, same numbers, so it was already
   green before this lane and is not this lane's doing.
3. **`Gameplay`'s constructor contract is untouched.** No population was
   registered with a body `Lifetime`, and doing so would not have fixed this
   defect: it fires with no body switch, and rebuilding the fields per body would
   WIPE `known` outright, making the same-body case no better and the cross-body
   case strictly worse.

## 6. Task backlog / roadmap
- **[Phase 0 / Spike 1 — DESIGNED, ready to build]** Floating-origin + frame hierarchy + active/on-rails skeleton sufficient to walk a planet, fly to orbit, land on a moon. *Highest-risk item in the whole project.* → full design + build plan + pinned contracts in **[spike1-core-engine.md](../spikes/spike1-core-engine.md)**. Next: execute Wave 0→2 build plan (§6) and run validation gates V1–V7 to retire R1.
- [Phase 0] Define `SimClock` + fixed-tick loop + render interpolation.
- [Phase 0] Define `SimProxy` Active/OnRails interface (stub OnRails content with domains).
- [Phase 1] **Persistence contracts (core-side) — DONE:** C-7 `QuiesceAtTickBoundary` defined (CE-8); C-8 `RegionDepth` Forge 9 / Cinder 8 (CE-9; locked per PHASE1-PLAN §10, 2026-06-14, was 11/9 — factory-sim's chunk extent now confirmed ~1 km); C-6 `IPersistable` core-side confirmed (CE-10). All additive. See Spike 1 §5.4.1, §5.5, §8.
- **[NEXT, CE-20 continuation] Close R7's seven stale holders**, then and only then wire `reboot` to something a player can reach. Three of the seven are rendering's (`PlanetProxy` geometry, the sky's baked radius) and four are gameplay/physics `bodyHandle`/`bodyRadiusM` snapshots. `session.epoch` exists so each can detect its own staleness in one comparison.
- **[DONE 2026-08-03, CE-30 to CE-38] THE CARRIER FRAME IS BUILT AND MEASURED.** The door below was opened deliberately by CE-20/CE-21 and this is it walked through. Original text kept below the fold. **What is built:** the frame abstraction, the registry, the ride, two real sources and three instrument ones, a headless algebra gate and a 43-check driven probe. **What is NOT built, stated so nobody reads the term as the feature:** no consumer boards a carrier, and making one do so needs the CONSUMER HALF , the carrier's own geometry has to move with it. See §7 R13.
- **[DONE 2026-08-03, CE-80 to CE-86] THE CONSUMER HALF IS BUILT: a carrier's GEOMETRY moves with it.** Admin ruling R13 implemented and measured (§5e). The station's collision solid, both gravity volumes and the drawn hull are re-posed every fixed tick from the one `poseAt`. **What is still NOT built, stated so nobody reads this as the mission:** nothing UNFREEZES Anchorage's conic, so the station still does not travel; that is physics' half of D-014 and it is now unblocked, because the interior no longer breaks when it happens. Original text below the fold.
- **[DONE 2026-08-11, CE-39 to CE-44] THE RIDER IS BOARDED ONTO THE CARRIER (todo #1).** Membership (`containsPoint`, two radii, gravity volumes excluded), the per-tick decision sited immediately after `mounts.syncAt`, and a `visit:station` arrival that seats the player AT REST IN THE FRAME instead of at rest in the body frame. Measured in §5f: **1.65e-9 m of deck-relative drift over 600 ticks at 31.320919525472796 m per tick, against 3,747.31 m un-boarded in the same run.** **What is still NOT true, stated so nobody reads this as the mission: Anchorage does not move.** `ph357-station-stamp` is on no reachable ref and on no remote, so the fixture is still an instrument frame.
- **[DONE 2026-08-11, CE-45 + PH-357 merged] ANCHORAGE TRAVELS, AND ALL THREE LINKS OF THE FIRST STATION MISSION ARE IN ONE BUILD.** The branch was pushed after the logistics item below was written. Measured in §5g: the press seats at **1879.2551715278214 m/s** on the real station with **1.5729040161257287e-9 m** of deck-relative drift over 601 ticks, against **3,747.31 m and no floor** un-boarded in the same run.
- **[SUPERSEDED, kept because it was true for a day] GET `ph357-station-stamp` ONTO A MACHINE THAT CAN SEE IT.** `git ls-remote --heads origin` returns `main` and nothing else; the tip `1e9a899` is not a valid object in this clone. Every other link in §4a of the state of the union is now done, so this branch is the whole of what stands between the client and a station that travels. If it is unrecoverable, the change it contains is one line (`stampedTick`) plus the fixture rewrites that follow it.
- **[DONE 2026-08-11, CE-47/CE-48] R17 IS CLOSED.** The station's install and mount are one function called at boot and on every rebuild; measured in §5h: mount back, frame back, **deck 0 m off its conic**, press boards again, **1.394e-10 m** of deck-relative drift over the 601 ticks after. **What is still open on this path and is now the next thing:** `of.reboot()` while the player is 400 km up does not return (two runs over ten minutes; 8.9 s from the ground). Pre-existing, `bootTerrain`'s, and worth someone's time because a reboot that never returns is worse than one that returns wrong.
- **[SUPERSEDED, kept because it names the defect] R17: `mountStation` runs outside `buildBodyScope`.** `Boot.ts` installs the station and calls `mountStation` in a block outside the body scope, while `mounts.bindTo(lt)` registers `clear()` INSIDE it. So `of.reboot()` empties the mounts and nothing re-mounts, and now that membership exists the symptom is worse than a static deck: the rule finds no mount, declines forever, and a player standing in the hub is **silently never carried again**. Named in `probes/stationboard.js`; it is a Boot ordering fix and Boot is this seat's file.
- **[NEXT, physics', and now genuinely unblocked] UNFREEZE ANCHORAGE'S RECORD.** `mintStation` sets `stampedTick = -1`. Stamping it is the whole of the remaining change on the station's side: `OrbitCarrier` already answers a moving pose from the same record, the geometry already follows it, and the rider already stays on the deck. **The one thing to check on the day it lands: `probes/stationride.js` FAILS BY DESIGN at its fixture assertion** ("Anchorage is frozen as it ships"), which is the signal to re-read the probe rather than a regression.
- **[NEXT, small, physics's to take, and now a THREE-line problem] `of_dk_*` and the docking port pose per tick (physics R77).** `composePose(carrierPose, stationSocketFrame('socket_dock'), out)` is the whole query, and CE-84's measured hull offset composes into it the same way. Nothing new is owed on this side.
- **[SUPERSEDED, original text kept] The consumer half: a carrier's GEOMETRY.** `installStation` puts 57 `col_*` proxies into `StructureBodies` at the record's frozen pose, and the walker resolves against them in the body frame. A carrier that moves needs those proxies re-posed from the SAME `poseAt` the ride uses, or the deck stays behind while the player is carried. That is `game/SpaceStation.ts` and `game/StructureBody.ts`, which are not this domain's, and the decision (does an orbiting structure's interior live in the body frame or in its carrier's?) is Admin's with rendering and gameplay in it. **Published so the other side can bind: `Services.carriers.get(id).poseAt(tick, out)` is the one authority, and `FramePose.apply` puts a local box in the body frame.**
- **[NEXT, small, physics's to take] `of_dk_*` and the docking port pose per tick (physics R77).** R77's open question was "where does the station's port pose come from per tick, and that is a core-engine frame question". The answer is now concrete: an `OrbitCarrier` over the station record, composed with the constant local offset `stationSocketFrame('socket_dock')` already publishes. `transportPoint`/`apply` are the two calls.
- **[SUPERSEDED, original text kept] The CARRIER FRAME, open door, not built.** Physics needs "the frame this thing rides in" so a player standing on an orbiting station is not left behind 125 m per tick, and the concept already exists here for *acceleration* (apparent gravity is `g(here) − a(the frame I ride in)`) but not for *position*. This is the same gap as CE-21 seen from the other side: a `UniverseCoord` is meaningless without its `FFrameId`, and the client currently has exactly one frame (the body) held implicitly by `FloatingOrigin`. **`WorldSession` is the natural owner** — it already holds "which frame is current" and rebuilds the scope when that changes; a carrier is a second, non-exclusive frame attached to a moving thing rather than to the world. Nothing built this pass forecloses it: `FloatingOrigin.reseat` takes an observer position rather than assuming one, `origin` is per-session rather than global, and no new code assumes the body frame is the only frame.
- [Phase 1] Harden frame switching during continuous flight; rebase stress tests.
- [Phase 1] Build `QuiesceAtTickBoundary` + unit-test the consistency fence (assert `TickIndex` frozen across the call) alongside the Wave-0 `SimClock` build.
- [Phase 2] Time-warp orchestration tied to on-rails factories.

## 7. Open questions & risks
- **R17 (CLOSED 2026-08-11, CE-47, measured by `probes/stationreboot.js` 18/18).** The fix is a rebuild hook calling the one install path at the live tick; §5h has the numbers and the two-shapes argument. The original entry below was right about the cause and about the fix being "the station install belongs in the body scope"; what it could not know is that the `Solid` staying game-scoped makes the hook the honest half of that, and that PH-357 would turn the symptom from "the geometry sits frozen where it last was, which is exactly HEAD's behaviour" into a player left behind at 1879.26 m/s. **Its last sentence was also exactly right and is worth keeping as a lesson: `probes/carrier.js`'s C7 round trip passed because it never asserted the mount after a reboot.** Original text follows.
- **R17 (NEW, 2026-08-03, CE-80 block, KNOWN GAP, not a regression): A REBOOT CLEARS THE STATION MOUNT AND NOTHING RE-MOUNTS IT.** `installStation`, `installStationGravity` and `mountStation` all run in `boot()` **outside** `buildBodyScope`, so they happen once per page load; `mounts.bindTo(lt)` is inside the scope, so `of.reboot()` empties the mount set and nothing puts it back. The station's geometry then sits frozen where it last was, **which is exactly HEAD's behaviour**, so nothing regresses , but the mount silently stops. Same family as R7's residue and gated by the same thing: `of.reboot` is debug-only and must not reach a player until R7 is zero. The fix, when someone takes it, is that the station install belongs in the body scope, which is a bigger ruling (its `Solid` lives in `StructureBodies`, which is game-scoped, not body-scoped). **Measured only by inspection of the call sites; `probes/carrier.js`'s C7 round trip passes because it never asserts the mount after a reboot.**
- **R18 (NEW, 2026-08-03, ROUTED TO ADMIN, and it is a HALF-BRIDGE ON `main` RIGHT NOW): HEAD's committed wasm does not export `of_fl_rcs_translate`, and HEAD's client calls it unconditionally.** `web/src/sim/FlightRcs.ts:83` and `:95` call `s.V._of_fl_rcs_translate(...)`; loading HEAD's committed `of-core.mjs` + `of-core.wasm` pair in node gives `_of_fl_rcs_translate = undefined`. A driven run at HEAD fails with `TypeError: s.V._of_fl_rcs_translate is not a function` **the moment a player flies with RCS**. The working tree's uncommitted binary HAS it. **`abi=22` passes on both**, which is NUMBERS.md's own entry ("`abi=N` is a consistency check, not a freshness check") pointed at a live case: client and wasm agree with each other about the version number and disagree about the export table. Not this lane's to fix under the standing rule that no lane commits the binary; **Admin's settled rebuild closes it.** This lane measured against the working-tree pair and says so.
- **R19 (NEW, 2026-08-03, THE MID-FLIGHT RELOAD, and the stated repro DOES NOT REPRODUCE): the game did not reload itself in four measured attempts, and a source edit in the served tree reproduces the symptom exactly.** A playability lane reported "3 of 3 with the map open, `b:cinder` selected, then the `Anchorage` row, then 25 s idle", pinned to MenuBoot's `restart`/`goTo` port because `norestart` suppressed it. Measured on an isolated tree at HEAD, port 5327: (a) an in-page probe that **swallows the 400 ms timer both exits schedule through, and captures the caller's stack** , chosen over pressing `norestart` because that flag stops the port BEFORE the timer and so says nothing about who asked , ran the full repro (aboard, in orbit, map open, `b:cinder` then `v:1` Anchorage) with **0 timers caught, 0 `beforeunload`, 0 navigations**, in sim-tick idle (1,919 ticks) and again in **30 s of wall-clock idle**; (b) a driver-side watcher (`framenavigated`, which the page cannot misreport) saw **0 navigations during a 45 s idle**. **What DOES reproduce it: appending one line to a served source file mid-run navigates the main frame at that instant, with no `[vite] page reload` line, no console error and an unchanged URL.** Vite's dev-server full reload is, from a player's chair and from a probe's, indistinguishable from the game reloading itself. **This is not proof of what the lane saw** , `norestart` would not suppress an HMR reload, and their five clean runs may simply have been a quiet editing window , but this checkout had **16 modified and 2 new source files from a live gameplay lane** while I measured. **Recommendation to Admin: before treating this as a game defect, have the reporting lane re-run against `git archive HEAD` on its own port with nothing editing the tree.** The durable fix, if it is wanted, is one this lane did not build for budget: stamp a reason into `sessionStorage` before either exit navigates and print it at boot, so a reload with no reason recorded is provably not the game's.
- **R12 CORRECTED UPSTREAM 2026-08-03: Admin has fixed D-014 in [MASTER_PLAN](../MASTER_PLAN.md) with the real figure and the reason it survived.** The 7.6 km/s came from a physics report, was propagated into the authoritative decision log, and was carried by three places that all drew conclusions unchanged by it. **Admin's framing is the part to keep: this is NUMBERS.md's own implausible-magnitude rule pointed at a number that PASSES it.** `SpaceStation.ts` line 23 and physics R67 still carry the old figure in prose; neither is this lane's file and neither changes what those files do. Original text below.
- **R12 (ORIGINAL TEXT, kept. NEW, 2026-08-03, CE-30 block): ANCHORAGE'S ORBITAL SPEED IS 1879.26 m/s AND 31.32 m PER TICK, NOT 7.5 km/s AND 125 m PER TICK.** `game/SpaceStation.ts` line 23 says "7500 m/s / 60 Hz = 125 m PER TICK", physics R67 says "passes a stationary station at 7.6 km/s", and D-014's discussion inherits it. **Forge's mu is 3.5316e12** (derived from the walker's own published `trueG` = 0.0245209 at a measured radius of 1.2001e7 m), so at the 1,000,000 m orbit radius `sqrt(mu/r)` is 1879.2 m/s. Measured independently off the station record itself through `OrbitCarrier`: **31.320920 m per tick**, and its turn rate and travel agree through its own radius to 1e-6. **Every conclusion those documents draw is unchanged** (a station that travels still leaves a walker behind, and 31 m per tick is still catastrophic), so this is a correction rather than an escalation. **It is worth naming because of HOW it survived: 7.6 km/s is Earth's low orbit, so it is entirely plausible as a magnitude and INSTRUMENTS.md's "an implausible number is caught by size" cannot see it. What catches it is that it is a number nobody derived from this world's own mu.**
- **R13 RULED 2026-08-03 by Admin: CARRIER-LOCAL GEOMETRY. The station's collision proxies do NOT go into the body frame.** The three reasons, recorded because the next lane will be tempted by the other option since it is fewer lines: **the floating origin exists specifically to avoid that churn**, and reintroducing a rebase every 2.1 s for one object inverts the thing it was built for; **carrier-local geometry is the SAME construction as the player riding the carrier**, so the deck and the person standing on it are one concept with two consumers rather than two mechanisms that have to be kept in agreement; and **it generalises for free** to a player-built station and to the docking port's per-tick pose, which is the same query. Admin routes the work; it touches `game/SpaceStation.ts` and `game/StructureBody.ts` and needs a rendering decision, and this lane does not reach into either. **What is owed on this side is nothing: `Services.carriers.get(id).poseAt(tick, out)` plus `FramePose.apply` is the whole binding, and `CarrierRide.parentAt` / `localAt` are the two directions already published.** Original text below.
- **R13 (ORIGINAL TEXT, kept. NEW, 2026-08-03, CE-30 block, THE ONE THING BETWEEN THE TERM AND THE FEATURE): A CARRIER'S GEOMETRY DOES NOT MOVE WITH IT.** The frame term is built, general and measured, and **no consumer boards a carrier**, which is why `of.carrier` is debug-only exactly as `of.reboot` is. The blocker is not this domain's: `installStation` puts 57 `col_*` proxies into `StructureBodies` at the record's frozen pose and the walker resolves against them in the body frame, so a moving carrier would carry the player and leave the deck behind. **Three things are owed and none is core-engine's to decide.** (1) The proxies re-posed per tick from the SAME `poseAt` the ride uses, or the two authorities D-014 just removed come straight back. (2) A ruling on whether an orbiting structure's interior lives in the BODY frame (everything moves at 1879 m/s, the floating origin rebases every 2.1 s) or in its CARRIER's frame (geometry static, only the render transform moves) , that is a rendering and gameplay question with a real performance side. (3) `StationView`'s draw and `StationGravity`'s volume centres follow whichever answer wins. **The abstraction expresses both**, which is why it was built as `poseAt` rather than as a mutation.
- **R14 (NEW, 2026-08-03, CE-30 block, PROCESS, and it is a new member of INSTRUMENTS.md's family rather than a defect): A PROBE'S `await` IS A VARIABLE NUMBER OF SIM TICKS, AND ON AN ACCELERATING FIXTURE THAT IS A MEASUREMENT ERROR.** `Loop.run` PAUSES the rAF loop and restarts it when it returns, so between two `of.run` calls the loop is ticking on its own. A `setTimeout(0)` between seating a fixture and starting a leg therefore lets an unpredictable number of fixed ticks pass. On a frame accelerating at `a`, a seat velocity `k` ticks stale is a relative velocity of `k*a*dt`, and one run read **0.25 m of drift where two others read exactly 0**. Nothing about the run reports how many ticks the yield cost. **The general form, which every driven probe on this project is exposed to: `of.run(secs)` is not the only thing that advances the sim, and any quantity seeded between two `of.run` calls has an unmeasured age.** The cheap discipline is to seat and measure with no `await` in between; the better one, if anyone wants it, is for `of.run` to leave the loop paused and make resumption explicit, which is a build-tooling call.
- **R15 (NEW, 2026-08-03, CE-30 block, PROCESS, and it nearly shipped a deliberate defect): A NEGATIVE CONTROL'S `finally` IS ONLY AS GOOD AS THE DIRECTORY IT RUNS IN.** [NUMBERS.md](../web/NUMBERS.md) says to put the re-apply in a `finally` so no interruption leaves the reverted state behind, and this lane did exactly that: `trap 'cp $BACKUP src/app/Boot.ts; cmp -s ...' EXIT`. **The trap fired, the `cmp` printed "byte-identical to pre-control", and the repository still held the defect**, because the shell's cwd had moved to the isolated measurement tree between arming the trap and running it, so **every path in the trap resolved somewhere else** and the verification checked the file it had just restored rather than the one it was supposed to. It was caught only because the next full run reproduced the control's failures exactly, and the tell was that a run made after the revert failed the same eight checks by name. **The rule: a trap must use ABSOLUTE paths, and the proof of a revert is `git status --porcelain <path>` printing nothing, not a `cmp` against the backup you just copied from.** This is the same family as everything else in INSTRUMENTS.md, arriving through the one door that exists specifically to close it.
- **R7 (NEW, CE-20, 2026-08-03, ROUTED — Admin to allocate): seven collaborators still hold the previous body after a switch, and they are MEASURED, not guessed.** `of.life().stale` compares what each holder currently answers against the live body, which a grep cannot do (it cannot tell a copy that is re-read per frame from one that is frozen). Measured on a Forge→Cinder switch, exactly seven rows are stale and three are clean. **Stale:** `proxy.bodyName` = `ForgeProxy` (the far-scene planet geometry is baked from radius and relief at construction → **rendering**); `sky.uPlanetR` = 600000 against a live 200000 (baked into a `ShaderMaterial` → **rendering**, and RN-840 has just made the sky body-aware at BOOT, which is the same seam one step earlier); `voxelMesh.bodyHandle`, `gameplay.bodyHandle`, `gameplay.bodyRadiusM`, `flight.bodyHandle`, `flight.bodyRadiusM` (**gameplay / physics**). **Clean, and they are the positive controls that stop the census reporting everything as broken:** `oracle.body.radiusM` and `oracle.water.body.radiusM` (re-seated) and `scatter.bodyRadiusM` (rebuilt). The census cannot see anything `Services` cannot reach; what it misses is named here rather than pretended away.
- **R8 (NEW, CE-19, 2026-08-03, the finding worth more than the pass): a GPU instance pool was being freed by an accident of frame timing, and the negative control REFUSED TO GO RED TWICE.** First refusal: `propsPlaced` is the scatter's own counter, so a rebuilt scatter counts back up to the same 10,170 while the old scope's slots sit orphaned in batches that outlive it. **An instrument that cannot see the failure mode it was chosen for.** Second refusal, with the right instrument (`PropLibrary.instances`, decremented by `release`): it still passed, because the loop ticks **nine animation frames** during a rebuild, `TerrainStream.dispose` has already cleared the resident set, and the outgoing scatter's ordinary per-frame reclaim releases all 10,170 slots as a SIDE EFFECT of the world being gone. So the explicit teardown step was deletable with no measurable consequence. Fixed by gating all three `Systems` hooks on `session.isRebooting`, after which the control reads **10,170 → 20,340**. **The general risk this leaves open: an incidental cleanup path makes an explicit one untestable, and there is no way to find the next one except by trying to delete each teardown step and watching what happens.**
- **R9 (NEW, 2026-08-03, other lanes' to triage): four of the five keys on the event bus have never had a subscriber, and two of them are emitted per chunk.** Measured on one boot: `ChunkReady` 309 emits, `StreamUpdate` 20, `ChunkEvicted` 0, `RegimeChanged` 0, all into an empty `Set`. Cost is trivial today and the point is not cost: an event with no subscriber is an interface nobody is honouring, and `ChunkReady`/`ChunkEvicted` are exactly the hooks a future scatter, audio or physics consumer would reach for, so they will be *presumed working* by whoever wires the first one. Either give them a subscriber or delete them.
- **R16 CLOSED 2026-08-03 on Admin's allocation, and the one-line change turned out to have a POSITION as well as a content.** `"check:pose": "node tools/smoke/posecheck.mjs"` is in `web/package.json` and folded into `check`. **It is placed BEFORE `check:limits`, not after, and that is the whole of the care this needed: `check` is `&&`-chained and `check:limits` is red for everybody (R10 below), so a gate added at the end of that chain would never execute a single line.** It would have read as "added to the aggregate check" in the commit message and been INSTRUMENTS.md's "a tool that reports nothing may not be running" on the day it landed. Verified rather than reasoned: `npm run check` prints `posecheck: PASS (9 checks)` and then goes red on `check:limits`, in that order. **The general form for whoever adds the next gate: in an `&&` chain, where you put a check decides whether it is a check.**
- **R10 (NEW, 2026-08-03, build-tooling): `npm run check` is red for everybody and has been for some time.** `check:limits` reports **29 files over the 400-line cap** at HEAD, including `Boot.ts` at 527 and `Scatter.ts` at 683. This lane did not create the condition and did not fix it, but a gate that everyone has learned to ignore is INSTRUMENTS.md's central failure and it is currently the state of the project's own aggregate check. `typecheck`, `check:proxies` and `check:boot` are all green.
- **R11 (NEW, 2026-08-03, observed, not attributable to this lane): `probes/discovery.js` is red on its limb-projection claim** ("the out/back fill flip is the LIMB entering the frame ... on-body 3600 -> 3600"). Verified pre-existing by a discriminating run: red **identically** with and without CE-22's one-line change, which is in any case a numeric no-op on Forge (`kind === 'moon' ? 1 : 0` and `bodyId` are both literally 0). Belongs to whoever owns the map's limb test.
- **R6 (NEW, process finding, 2026-07-25):** **A standing rule with no executable guard does not hold.** WG-21 made `surface_field.h` the single surface authority and [DECISIONS.md](../web/DECISIONS.md) standing rule 1 wrote it down; the very next component built against that rule (the WASM bridge) violated it in three places, one of which put the player 2.4 km underground and shipped through a green test suite because nothing asserted the rule. Fixed for this contract by CE-13's CASE 7b/7c. **The generalisation, and the open item: every authority-style invariant in the project needs a test that fails the build, and the test must assert the negative half too** (that the wrong source is still measurably different), or it will one day pass vacuously. Candidates to audit next on the same criterion: the UE surface layer (R2b), deposit/foliage snapping, and anything that will consume `surfaceHeight` across the /core-to-JS boundary.
- **R5 (NEW, characterised and accepted — CE-11):** WASM↔native world-gen is **not** bit-identical (~1.7% of terrain vertices), because the four `libm` transcendentals in the determinism path (`tan` in `warp`, `asin`/`atan2` in `dirToLatLon`, `cos` in `temperatureAt`) are not bit-specified by IEEE-754. **Not a risk while the game is browser-only** (one binary). **Becomes a blocker the moment a native peer is proposed** — a native dedicated server, or a native tool that pre-bakes world content, would generate a different planet from the same seed. Mitigation is known and bounded (vendor those four functions into `/core`); cost is one re-baseline of the seed→world mapping. Escalate to Admin *before* any native-peer work is scheduled, not after. Measured by `node web/wasm/test/diag.mjs`.
- **R1 (critical) — test designed, not yet run:** Does UE5's LWC + Chaos behave well under aggressive floating-origin rebasing at speed? Spike 1 §7 defines explicit **pass/fail criteria** (V2 no-hitch, V3 no-wobble at 10⁹ m, V4 no-kraken at 3 km/s, V5 seamless SOI). **Likely outcome flagged:** *partial pass* — coords/render fine, **Chaos may need replacing for the active vessel** (a physics-domain/Spike-2 escalation). Full fail → revisit D-001 (Unity/DOTS or custom). Still OPEN until the build runs.
- **R2:** double vs int64 fixed-point for the authoritative coord (determinism vs ergonomics). Lean double for v1 (Spike 1 `UniverseCoord` uses double; int64 path deferred); revisit if networking needs determinism.
- **R3 — design settled, validation pending:** Active/OnRails promotion hysteresis. Spike 1 §3.2 pins it: activate 2 km / deactivate 3 km gap + 2 s demote-dwell; promotion immediate, demotion lazy. **RC-10/CE-7 extends the `gap` to be extent-aware (§3.2.1) so coarse chunk proxies and point vessel proxies hysteresis-uniformly — same 2/3 km band + 2 s dwell on the same metric, so no added thrash risk.** Validated by gate V7 (recommend adding a coarse-proxy case: jitter a *chunk* proxy at its extent-inflated boundary, assert no mode oscillation).
- **R4 (Phase-1, DEPENDENCY — RECONCILED, locked per PHASE1-PLAN §10, 2026-06-14):** CE-9's `RegionDepth` (Forge 9 / Cinder 8) uses factory-sim's **confirmed ~1 km factory-chunk extent** (was a ~256 m placeholder → 11/9). Per the formula `RegionDepth(body)=floor(log2(R·(π/2)/E))` with E ≈ 1 km, Admin reconciled the values in the §10 register (no core contract change — only the integer moved). CE-9 is now "Accepted (locked)."

## 7b. HANDBACK: what the next lane in this seat should know (written 2026-08-03)

Two general capabilities landed tonight, the world lifecycle (CE-19 to CE-22) and the carrier frame (CE-30 to CE-38). They are one seam seen from two directions and the second was built inside the door the first deliberately left open. This is what a fresh context would otherwise have to rediscover.

**1c. UPDATE 2026-08-11, LATER THE SAME DAY (CE-45): THE FIXTURE IS REAL NOW, AND ITEM 1b IS SUPERSEDED ON EXACTLY THAT POINT.** `ph357-station-stamp` was pushed and is merged. Anchorage travels at **31.32091952549093 m per tick**, `stationride.js`'s frozen assertion has fired and been inverted, and `probes/stationboard.js` measures the shipped station end to end with **no instrument frame in it**. Three things for the next lane in this seat. **(i) The refusal did not go away, it moved:** `stashVessels` drops `stampedTick` on every save, so a world between a load and its first stamp is frozen again and every carrier measurement on it is the identity element; both probes now check the SHIPPED rate before they measure anything. **(ii) A probe's own snapshot ages now (CE-46).** Anything holding a station-derived value across an `await` was exact while the station was frozen and is 31.32 m per tick wrong now; one such check was found and fixed, and it is worth grepping for others rather than waiting for them. **(iii) R17 WAS the sharpest edge in this domain and is CLOSED (CE-47, §5h); what replaces it is that `of.reboot()` while the player is 400 km up does not return at all**, which is `bootTerrain`'s and is measured only as two timeouts against an 8.9 s ground reboot. Item 1b below, on everything except the fixture, still holds.

**1b. UPDATE 2026-08-11 (CE-39 to CE-44): THE RIDER IS ON IT, AND THE THING TO KNOW IS THAT THE FIXTURE STILL IS NOT REAL.** todo #1 is done: membership, the per-tick decision, and an arrival that does not zero the velocity. **What the advice below got wrong is only its assumption that `ph357-station-stamp` was one merge away.** That branch is on no ref in this clone, in no worktree and on no remote (`git ls-remote --heads origin` returns `main` and nothing else); its tip `1e9a899` is not a valid object here. So Anchorage still ships with `stampedTick = -1`, every moving measurement in this domain is still taken on an instrument frame derived from the station's own `r x v`, and **the handover signal item 8a describes (`stationride.js` failing at its fixture assertion) has still not fired.** Do not read `boards: 1` as "the station moves". It does not. Two other things worth carrying: `FloatingOrigin` has **no CE-6 Krakensbane trigger at all**, just a 4 km distance test, so a boarded rider at 1879 m/s rebases the origin every 128 ticks forever; and **R17 is now load-bearing rather than theoretical**, because `mountStation` runs outside `buildBodyScope` while `mounts.clear()` is registered inside it, so `of.reboot()` leaves the station on no frame and the membership rule then finds nothing to board, silently and forever.

**1a. UPDATE 2026-08-03 (CE-80 block): R13 IS BUILT. The carrier frame now has ONE consumer and it is the station's geometry.** The advice below was followed exactly and it was right. What is left between this and a station Reid can dock with is **physics stamping the record**, and nothing else on this side. The next lane in this seat should NOT bind a second consumer before that lands: the docking port (R77) is a `composePose` away and costs nothing to wait for, and a second consumer bound while the only fixture is still an instrument frame is how you get two things that agree with each other and not with the game. Original item below.

**1. The carrier frame is FINISHED and it is not a FEATURE, and the gap between those two words is R13.** The term is general, measured and leak-free. Nothing boards a carrier. The single thing standing between it and a station a player can stand on while it orbits is the consumer half, now ruled carrier-local, plus physics unfreezing Anchorage's record (D-014's other half). **Do not "finish" the carrier frame. It is finished. Bind a consumer to it.**

**2. The binding is two calls and there is no third.** `Services.carriers.get(id).poseAt(tick, out)` gives the frame's pose at a tick; `FramePose.apply(pose, lx, ly, lz, out)` puts a carrier-local point in the body frame. `CarrierRide.localAt` / `parentAt` are the same pair for the rider. **Everything else is a mistake in progress.** Anything that caches a pose is D-014's defect rebuilt, and anything that stores a local coordinate for the rider is a second authority for where the rider is (the ride deliberately keeps the rider in the parent frame and applies the frame's contribution to it).

**3. The two halves rule is the thing to carry, not the code.** `apparent g = g(here) - a(the frame I ride in)` and the position transport are ONE term. A rider with the transport and the wrong acceleration half drifts by `1/2 a T^2` and looks exactly like a broken transport; a rider with neither looks like it is working if the frame happens to be inertial. **Before believing any carrier measurement, ask what the frame's own acceleration is and whether the rider is getting it.** A freely falling frame is not an inertial one and that cost this lane a whole measurement pass.

**4. `Boot.ts` / `Services.ts` / `Loop.ts` / `main.ts` are this seat's files and other lanes publish their needs instead.** That arrangement worked: four wiring files were edited tonight with zero cross-lane collisions, against NUMBERS.md's record of it going wrong four times when it was not arranged. **Keep it, and keep the diffs small enough to read in one screen**, which is what makes "is any of this someone else's?" answerable in seconds at commit time.

**5. The residue in R7 is still seven holders and it is still routed, not fixed.** `of.life().stale` measures it. Nothing tonight moved it, and `reboot` still must not be wired to a door a player can walk through until it is zero. The carrier work adds no new holders: the registry is emptied by the body `Lifetime` and the ride releases before it, proven both ways.

**6. Three instruments now exist in this domain and they are cheap. Use them before writing a new one.** `npm run check:pose` (9 checks, one second, no browser); `probes/worldreboot.js` (the lifecycle, 309 chunks / 10,170 props / hash `461637469`); `probes/carrier.js` (43 checks, the frame term). **`461637469` at 1167 vertices is Forge's ground at the walk spawn and it has now been reproduced by two independent probes across five reboots.** If it ever differs, something real moved.

**7. The harness lessons are worth more than the code and three of them are new tonight.** A probe's `await` is an unmeasured number of sim ticks, because `of.run` pauses and restarts the rAF loop (R14). A `finally`'s cwd is part of its revert (R15). And in an `&&`-chained aggregate check, where you put a gate decides whether it is a gate (R16). All three were found the same way: a measurement contradicted something that was obviously true.

**8a. UPDATE 2026-08-03: item 8's first step is done and the order still holds.** Bind ONE consumer: done (the station's geometry, CE-80 to CE-86). **Do not open a fourth general capability**, and now also: do not add a second consumer until Anchorage's conic is stamped, because until then every carrier fixture in this project is an instrument. The two things this seat should do next, in order: **(i) nothing, until physics unfreezes the record** , the probe is written to fail loudly on the day it does, which is the handover signal; **(ii) R7's seven stale holders**, which is still what stands between `reboot` and a door a player can walk through, and is now also what stands between a body switch and a station mount that survives it (R17).

**8. What this lane would do next, in order.** Bind ONE consumer to the carrier frame (the docking port's per-tick pose is the smallest, and physics R77 already names the shape); then the station interior under R13's ruling; then R7's residue, because that is what unlocks `reboot` for a player. **Do not open a fourth general capability before one of the three has a consumer.** Three abstractions with no caller is how `of_atmo_*` became API surface that read as a feature.

**9. THREE HARNESS LESSONS FROM THIS PASS, all found the same way as the last three: a measurement contradicted something obviously true.**
- **A probe's idle in SIM TICKS is not a player's idle in WALL CLOCK, and the two measure different bugs.** `of.run` pauses the rAF loop and steps the sim itself, so eight `of.run(4)` calls buy 1,919 fixed ticks in about two wall seconds. Anything driven by a real timer, a rAF-driven interval, or a pending promise cannot see them. R14 said `of.run` is an unmeasured number of ticks; this is its complement, **`of.run` is an unmeasured number of SECONDS**, and a bug reported as "after 25 s idle" needs the second kind of wait.
- **A negative control's revert cannot be proven with `git status` when the file is legitimately dirty.** R15 says the proof of a revert is `git status --porcelain <path>` printing nothing. That is right for a file you did not otherwise touch and **wrong for one you are actively editing**: this pass sabotaged `FramePose.ts`, which already carried 70 new lines, so `git status` printed `M` in both the sabotaged and the reverted state. The proof that works in both cases is **`cmp` against a backup taken in the same procedure AND re-running the gate green**, which is two independent facts rather than one ambiguous one.
- **Arming has to be asserted by the thing that does the arming, not by a diff that would be non-empty anyway.** The same control printed `git diff --numstat` as an "arming verified" line; it read `69 0` because the file already differed from HEAD by 69 lines, so it would have printed exactly that with no sabotage applied at all. **The check that actually worked was the `assert old in s` inside the Python that wrote the file**, which fails loudly when the target text has moved. NUMBERS.md's newest entry, met on the day it was written.

## 8. Subagent delegation log
| Date | Subagent task | Status | Outcome |
|------|---------------|--------|---------|
| 2026-08-14 | **RE-DIAGNOSE the carrier.js rotor C1 red that Admin re-filed as a NEW genuine red** (`perTickM 31.32092 vs r*w 31.320866`, ~1.7e-6, deterministic across two runs, blamed on the stamped station perturbing the rotor's seed). Opus lane on `lane/rotor-c1`, Windows host, **real D3D11 (`ANGLE (NVIDIA, NVIDIA GeForce RTX 4060 Ti (0x00002803) Direct3D11 vs_5_0 ps_5_0, D3D11)`)**, locally built (`npm run build`) and served from `dist`, server killed by PID. **NO SUBAGENTS SPAWNED**: the question was whether one number was reachable from live code, which is arithmetic, not search. | **Done, NOT REPRODUCED, nothing widened** | **CE-85, CE-86.** `carrier.js` **43/43 `fails: []` twice independently**; the red is CE-53's, re-reported off a fork predating `a4f396d` (which is an ancestor of HEAD, and whose own comment already predicts the exact figure: "It would have read 1.7240873e-6 against the old instrument"). **Decomposition, all four brief hypotheses killed by number:** `31.320866` implies `w = 3.1320866e-5` against the live atan2 `w = 3.132091952687908e-5`, a gap of **5.3527e-11 rad = 1.7090e-6 relative**, which over the acos conditioning `2/sin(w/2) = 127,710` is **1.89 ulps of the quaternion dot**; the lattice steps **9.05e-7 relative per ulp**, which is why a quantised artefact read as determinism; the only real term is arc-vs-chord `w^2/24 = 4.0875e-11`, **24x inside the 1e-9 gate**; and `r`, `w`, `perTickM` all come from ONE `survey()` call on the SAME pose pair, so there is no stale seed radius and no settle frame , **the stamped station is exonerated**. Live chord residual **1.97e-12**. **Tolerance NOT widened and widening REFUSED** (500x headroom already). **The one real defect is the harness: `carrier.js` documented no invocation, so `probeall.mjs:extractCmd` returned `null` and the documented sweep never ran it , which is how a fixed red stayed on the backlog. Fixed; an audit of every other `null`-extracting probe is owed to build-tooling.** |
| 2026-08-13 | **DIAGNOSE `probes/carrier.js` C1's deterministic 1.7e-6 rotor failure.** Opus lane on `lane/rotor-tolerance`, Windows host, real GPU. **NO SUBAGENTS SPAWNED**: the mechanism was reachable by reading two functions (`RotorCarrier.poseAt` and `DebugCarrier.turnBetween`) and then measuring, so fanning out would have cost context for nothing. Diagnosis was done OFFLINE FIRST, against the shipping `FramePose.ts` type-stripped the way `posecheck.mjs` does it, over 20,000 LVLH bases: that is what produced the -2.350e-6/+1.724e-6 range, the 1.6% breach rate, the 1,055-value lattice and the identification of the failing +1.7240873e-6 as the top rung, all before a line of code changed. Each candidate mechanism was killed by a number rather than by inspection. | **Done** | CE-53. `carrier.js` 43/43 `fails: []`; `stationride` 22/22, `stationboard` 29/29, `check:pose` 14/14 unregressed |
| 2026-08-11 | **A SECOND PLAYER BUG REPORT: standing on the station deck STUTTERS.** Opus lane on `lane/frame-render`, off `lane/station-arrival`. Measure first, land the measurement as the control, then fix. | **Done, 12/12 + four regressions green** | CE-51, CE-52. Three hypotheses named before measuring; the pre-fix run convicted (a) TWO CLOCKS at **27.04 m peak to peak, 13.52 m within one tick, corrAlpha 0.9999999860**, and acquitted (b) f32 wobble (1.8e5x too small) and (c) the rebase snap (**zero rebases in the window**), so **CE-6's Krakensbane trigger was deliberately NOT built**. Fixed by splitting the clocks: collider at the integer tick, hull at the fractional render tick, through the same `poseAt` everything else asks. **27.04 m to 0.00243 m**, with the collider channel held at 27.05 m as the discriminating control that also proves CE-33 intact. **CE-52 is worth more than the fix**: four probes and 80-plus checks were green throughout because they all sample per FIXED TICK, which is the one instant the defect is corrected away. One instrument bug caught by the fix appearing to do nothing (the trace was sampling the collider, not the hull). |\n| 2026-08-11 | **A PLAYER BUG REPORT: the `visit:station` press seats Reid inside a wall.** Opus lane on `lane/station-arrival`, off `lane/r17-reboot`. | **Done, fails [] + three regressions green** | CE-49, CE-50. Diagnosed in one driven run: the seat point read `solidBuild` TRUE at the feet and at all three walker sample heights, because it was the station's local ORIGIN and `col_HallCore` is a solid column through it. **The right point was already published twice** (`stationStandLocal()`, `StationReport.standPos`) and the press took the wrong one through three successive passes that each made it more accurate. Fixed by applying the asset's own socket to the live solid pose and CHECKING it with the walker's own predicate; the deterministic outward scan is a guard that never runs on the shipping asset (`scannedM` 0). Measured: **0 of 15 samples solid, 3.999998 m off centre, 8e-06 m onto the deck, 9.053095 m of deck-relative walking**, with the OLD point computed every run as the negative control. **The lesson is bigger than the bug (CE-50): grounded, onDeck and 0 m off centre are all TRUE inside a pillar**, so every assertion in `stationvisit.js` agreed with the defect for eight months, exactly as `probes/carrier.js` agreed with R17 by never asserting the mount. One of the new checks was itself a vacuous pass on first writing (it measured 4,666.83 m of walking that was the station carrying him) and is now deck-relative. |
| 2026-08-11 | **CLOSE R17**, the reboot defect this seat named as the sharpest edge in the domain. Opus lane on `lane/r17-reboot`, off `origin/main` after the carrier-rider work merged at `cfeffad`. | **Done, 18/18 + four regressions green** | CE-47, CE-48. The rebuild-hook shape rather than full body-scope participation, because the station's `Solid` is gameplay's and its hull is rendering's; the honest half of that is that `Boot`'s ten inline lines of install wiring became ONE function both callers use, so there is no second authority for where the station is. **The tick is the whole difference between the two calls**, and getting it wrong is not subtle: a hold-in-place re-mount leaves the deck **2,317.7 m** behind its own orbit and reads perfectly healthy from inside the station. Measured: mount back, frame back, **deck 0 m off its conic**, press boards again at **1879.25517152564 m/s**, **1.394e-10 m** of drift over the 601 ticks after, against an `unmount` control in which the rule boards **0 -> 0** for ever while the deck is still solid to stand on. One gap named rather than skipped: **`of.reboot()` while 400 km up does not return** (two runs over ten minutes, against 8.9 s from the ground), which is `bootTerrain`'s and pre-existing. One process slip, self-caught and reverted in its own commit: `git lfs pull` left four screenshots staged and they rode into the fix commit as 2.9 MB of blobs. |
| 2026-08-11 | **MERGE `ph357-station-stamp` AND RE-MEASURE EVERYTHING AGAINST A STATION THAT MOVES.** Same Opus lane, second pass, after Admin pushed the branch that this lane had reported as unreachable. | **Done, 28/28 + 22/22 + green** | CE-45, CE-46. The premise correction from the first pass was right and is now moot: the branch existed only on Reid's Windows machine and was never pushed. Merged with one conflict, in `pressStation`, which turned out to be **two lanes fixing the same defect from opposite sides** (PH-357's `stationBodyPosNow` and CE-41's live-solid read inside `seatOnStationDeck`, both reading `lastStationSolid()`, so the resolution is one authority and not two). Re-measured on the real station: **seat 1879.2551715278214 m/s, feet 0 m from the live hub with the boot position 6,232.85 m stale, drift 1.5729040161257287e-9 m over 601 ticks against 3,747.31 m un-boarded.** The rotor instrument's rate and the stamped conic's agree to **1.8e-11 m**, which retro-validates every §5f number. Two fixture rewrites, both of which the stamp broke in the same way (a boot-time fact read as a live one), plus CE-46: a probe's own snapshot ages now, and that is a new class of defect across this suite. |
| 2026-08-11 | **BOARD THE RIDER ONTO THE CARRIER (todo #1)**: the membership predicate, the per-tick board/release decision, the `visit:station` arrival, the driven gate, and the doc corrections. Opus implementation lane on `lane/carrier-rider`, VM `claude-dev`. | **Done, 27/27 driven** | CE-39 to CE-44. `of.carrier('census').ride` read `boards: 0` at HEAD on a world with a player standing inside Anchorage; it now reads `boards: 1` after the shipped press, and the rule boards and releases on its own as a walker crosses 29.64 m / 33.64 m from the deck's own measured 28.6449 m bound. **Deck-relative drift 1.6545725990496454e-9 m over 600 ticks at 31.320919525472796 m per tick, against 3,747.31 m and no floor un-boarded in the same run.** Four findings, none of them the code: **`ph357-station-stamp` is on no reachable ref and on no remote**, so the brief's step 1 (merge it) was impossible and Anchorage still ships frozen; **three SHAs in the docs died in the 2026-08-03 history rewrite** and are replaced; **`FloatingOrigin` has no CE-6 Krakensbane trigger at all**, so a boarded rider at 1879 m/s rebases every 128 ticks under the same 4 km rule a walker gets; and **R97's warp-while-boarded guard is unreachable in this build**, so it was recorded at the seam rather than built as dead code. Neither named fixture rewrite was needed, because neither probe went red: both were predicated on the merge that could not happen. |
| 2026-06-14 | Scoped web research — UE5 Large World Coordinates + origin-shifting/rebasing specifics & known pitfalls (Chaos, world-space materials, auto world-composition shifting) | Done | UE auto origin-shift is single-player, integer-meter-quantized, jittery; LWC = 64-bit *authority* but 32-bit GPU/Chaos hot loop near tile. → drove **CE-5** (roll our own rebase) and Spike 1 §1.1. |
| 2026-06-14 | Scoped web research — KSP Krakensbane / dual-frame velocity-based origin shifting | Done | Krakensbane shifts origin **position AND frame velocity** so the active vessel stays near origin *and slow* in physics space. → drove **CE-6** (dual-trigger rebase) and Spike 1 §1.5 (the core R1 mechanism). |
| 2026-06-15 | Headless **surface-walk data layer** — `core/include/of/surface_walk.h` + `test_surface_walk.cpp` (the math/queries the UE whole-planet PLAYER controller binds to: player ON a body surface, a character mode distinct from the orbital vessel path) | Done | `SurfaceObserver` (geodetic lat/lon/heading/eyeHeight ↔ body-frame `UniverseCoord`): radial `localUp` / centre-pointing `gravityDir`+`gravityAccel`; snaps to the **designed** terrain (`biome::SampleDesignedTerrainHeight`); great-circle `move(forwardM,rightM)`; drives a `FloatingOrigin` rebase as the walker travels with a one-shot `consumeRebased()` event (→ `TerrainStreamer::onOriginRebased`); `makeStreamObserver()` for `TerrainStreamer::updateStreaming`; local `biomeHere`/`hardnessHere`/`nearbyDeposits` (via `BiomeResourceField::QueryRegionDeposits`). ADDITIVE — no published interface changed. **19/19 ctest suites green** (was 18; +`surface_walk_tests`). Rebase threshold reuses the SimWorld 4 km (CE-6 position trigger); movement is closed-form great-circle (arc = dist/R) re-snapped to the designed surface each step → deterministic. |
| 2026-07-25 | **WASM PIVOT SPIKE (the crux de-risking task for the Three.js move)** — compile the headless `/core` sim to WebAssembly and prove it runs correctly from JS. Deliverables: `web/wasm/` (flat C ABI shim `of_core_api.cpp`, `build.ps1`, Node parity test, determinism diagnostic, native ground-truth generator) + [docs/web/WASM-BRIDGE.md](../web/WASM-BRIDGE.md). | **Done — VIABLE** | **`/core` runs in the browser unmodified.** 127 KB `.wasm` + 19 KB glue, emscripten 6.0.4, no embind (flat C + a scratch arena so bulk arrays cross zero-copy). Exposes the surface oracle, world gen, terrain streaming, voxels, and the factory; stateful objects are opaque int handles. **Parity vs a native g++ -O2 build of the same shim: Tier 0 self-determinism 19/19, Tier A cross-toolchain transcendental-free 94/94, Tier B terrain field 138/141.** The 3 Tier-B deltas have ONE diagnosed root cause → **CE-11**. Crack-free shared edges, tunnel-leaves-ceiling, exact miner arithmetic and persistence bytes all bit-identical. **Perf: quad mesh 94% of native, factory 82%, voxel 62%. R1 chunk gate: 3.39 ms/chunk vs a 12 ms budget (3.5× headroom); packed buffer 1217 verts × 28 B = 34,076 B, constant → poolable. Main-thread surface oracle 1.4–3.8 µs/call, synchronous, zero alloc.** Threading recommendation: single-threaded instance per thread, **no SharedArrayBuffer → no COOP/COEP headers**; main thread owns the authoritative `VoxelEdits`, workers replay an edit op-log. `float64` crossing to JS is exact (same IEEE-754 double); the only precision reduction is the packed f32 vertex buffer, made safe by the per-chunk 64-bit anchor (0.9 mm at walking LOD vs 71.5 mm for absolute f32). |
| 2026-07-25 | **WASM bridge surface-authority audit** (raised by the W1 renderer agent: the observer spawned 2.4 km underground). Audit every export in `of_core_api.cpp` for raw-vs-oracle drift, fix, add a regression guard, correct WASM-BRIDGE §4.8. | **Done** | **Three violations, not one.** `of_observer_latlon_alt` (raw-derived positions, the reported bug), `of_quadmesh_generate` (`designedBase` flag meant `0` = RAW, so the default value was the wrong authority, and raw base + voxel edits silently mixed two bases), `of_chunk_max_offset` (largest single-axis offset over interior vertices only, so it under-reported the bounding radius by ~2x and frustum-culled visible chunks). All fixed under **ABI 2**; see **CE-13**. New guards: `dump_expected.cpp` CASE 7b (native, gates fixture emission) and `parity.mjs` CASE 7b/7c (WASM, asserts the oracle identity **and** the raw inequality so the test cannot pass vacuously). Parity Tier 0 **29/29**, Tier A 94/94, Tier B 147/150 (same 3 pre-existing libm deltas). Fixture re-baselined by 3 lines only; every streaming, chunk-content, LOD-key and quad-mesh hash unchanged, which is what proves the flag inversion was applied consistently on both sides. Also corrected WASM-BRIDGE §4.8, which showed a `THREE.InterleavedBuffer` binding that cannot work (three.js takes the GL type from the shared buffer's array, so one buffer cannot carry f32 positions plus i8 normals plus u16 UVs); documented the shipped de-interleave-into-one-transferable path instead. **W2 action: bump `OF_ABI_VERSION` to 2 in `web/src/sim/wasm/OfCore.ts` and update the `_of_observer_latlon_alt` declaration in `heap.ts`.** |
| 2026-08-03 | **CE-19/20, map every `Events.on` call site, the whole `Boot.ts` construction order, the teardown inventory, and the terrain worker's protocol lifecycle (Explore)** | Done | The map the seam was cut from, and it **corrected the escalation it was checking**. World-gen's WG-150 reported "seven objects subscribe to `OriginRebased`" and "`Events` has no unsubscribe"; the truth is **four** subscribers and `Events.on` has always RETURNED an unsubscribe closure that all four discarded. Both halves matter: the fix was smaller than briefed and in a different place. It also returned the finding this pass turned on, unasked, that **four of the five event keys have no subscriber at all**, and an exhaustive dead-teardown inventory (15 `dispose`/`destroy` methods with no caller anywhere, plus 9 more reachable only from a dead parent) that made "who calls it" answerable rather than rhetorical. Its `Boot.ts` ordering table is what showed the props load could move above the terrain, which is the single edit that let boot and reboot share ONE construction path instead of two. |
| 2026-08-03 | **CE-22, census every body-identity convention, every cached `bodyHandle`/`bodyRadiusM` snapshot, and every chunk/lattice key in the client (Explore)** | Done | Produced the routed list in §7 R7 and the evidence for CE-22. It **distinguished a held OBJECT from a copied SCALAR**, which is the distinction the whole re-seat rule rests on and which the brief had not drawn: everything reaching through `oracle.body` is automatically live, everything holding a number is a separate snapshot, and that is why one getter fixed a dozen call sites. Findings not asked for: `Enemies.ts` initialises `private bodyRadiusM = 600000` and falls back to the same literal (a hard-coded Forge radius, `game/`); `MapMode` passes `bodyName: 'Forge'` as a literal; `NodeField.placeStarter` takes `bodyId = 0` as a DEFAULT parameter, which is GP-142's identity-element trap in a signature; and `VabDest` memoises a `home` body it never invalidates. None are this domain's and all are now written down. |
| 2026-08-03 | **CE-30 block, map the carrier-frame code surface: `KinematicBody` and its exact integration expression, `FloatingOrigin` and every `OriginRebased` consumer, `WorldSession`, the apparent-gravity model, Anchorage's record, Cinder's ephemeris on both sides of the ABI, the per-frame ordering, and the probe harness's exact contract (Explore)** | Done | The map the term was cut from, and it **settled the question the brief said to verify before relying on it**: `GravityPort.ts:21` really does state `apparent g = g(here) - a(the frame I ride in)` and `GravityVolumes.ts:110` really does implement it, but **`carrierG` is a SCALAR MAGNITUDE ("radially inward") subtracted from the magnitude of `trueG`** , it is not a vector, it never enters the position integration, and there is no frame velocity or frame position anywhere in the client. So the sentence was written down and only half of it was built, which is exactly what CE-35 turned on. Three other things it produced that shaped the design: `KinematicBody.step` has **exactly one call site and one constructor site** (`Controller.ts:134` and `:38`), so the ownership seam is cheap; the walker runs at `Loop.ts:222`, **one line before `FloatingOrigin.step`**, which is CE-38; and there is **no parenting, attachment or relative-frame concept anywhere in `web/src`** (`SolidBodies.deckUnder` returns a radius, not a body, so a walker standing on the station does not know what it is standing on). It also found that `stationSocketFrame` now publishes a full pose, which closes physics R76 and is what makes R77 a two-call problem. |
| 2026-08-03 | **CE-80 block, map how Anchorage's geometry, collision, gravity, render pose and record are wired, and enumerate every consumer and every cached station-derived pose (Explore)** | Done | The map the consumer half was cut from, and its most useful product was a **complete enumeration of the boot-frozen poses: exactly three objects** (the collision `Solid`, the two `GravityVolume`s, `StationView.pos/quat`) plus two cached scalar reports. That is what made the mount a three-attachment problem rather than an open-ended one. It also established the two facts the design turns on: **`StructureBodies` and `GravityVolumes` have no "update pose" method and need none**, because every query reads `pos`/`quat`/`cx/cy/cz` off the stored object per call; and **`OrbitCarrier` is already the only per-tick station-frame source in the client**, so nothing had to be invented. Findings not asked for and routed rather than fixed: `DebugGameplay.ts:268` runs an **independent second `stateOf` solve hard-coded to tick 0** for the debug surface, which is a second authority for the station's position that will disagree with the first the day the record is stamped; and `Structures.reset()` calls `bodies.clear()` while `SpaceStation.installed` still points at the dropped solid, leaving a dangling reference only `resetStationInstall()` clears. |
| 2026-07-27 | **W11 lane E — the enemies / pollution / evolution / base-defense loop**, built as a new headless simulation core. Deliverables: `core/include/of/enemies.h`, `core/tests/test_enemies.cpp`, a `core/CMakeLists.txt` target, and a new controller file. | **Done — green** | New sub-domain, documented in **[enemies.md](enemies.md)** (EN-1..EN-10). `enemies_tests` **248 checks**, ctest **28/28**. Headline numbers: **1,200 machines cost 10.06 us/sim-tick, and doubling to 2,400 in the same footprint cost 1.28x rather than 2x** (pollution lives on 5,500 sparse 146 m cells, not on machines); diffusion pinned to six hand-derived values to 1e-12; mass conserved to `1000*0.99^30` including across a cube-face seam; the negative control (six nests, thirty simulated minutes, no emitters) gives **0 waves while evolution provably rises to 0.3017**. **Two findings relevant to THIS domain.** (1) The module is deliberately **transcendental-free** (only `+ - * / sqrt fabs floor`), so it does not extend the CE-11 / R5 list of four `libm` functions in the determinism path and should be bit-identical cross-toolchain; the cost is a measured 2.1209x cell-size variation between a cube-face centre and corner, against the 3/sqrt2 analytic limit. **R5's mitigation list is unchanged: still exactly `tan`, `asin`, `atan2`, `cos`.** (2) It serialises through a **templated byte cursor** (the `voxel_terrain.h` idiom) rather than taking a `persistence.h` `DomainId`, so a whole new domain now persists inside DW-17's single atomic save **without a `persistence.h` schema bump**. That is a reusable pattern for any future domain and is worth knowing before someone widens `DomainId` for the next one. No §5 contract touched; `UniverseCoord`, `ReferenceFrame`, `SimProxy` and `SimClock` are unchanged. |
> Note: research run via WebSearch/WebFetch directly (Agent subagent tool unavailable this session); findings folded in above, raw sources cited in Spike 1.

## 9. References
MASTER_PLAN §3 (active/on-rails), §4 (engine), §5.1 (layers). KSP floating origin & Krakensbane; UE5 Large World Coordinates docs.
