# Build, Tooling & Test Infrastructure — Master Controller Context

> **2026-08-03, OPEN AND PAUSED:** the gate audit (BT-40) is **18 of 189 probes measured** and was stopped mid-sweep so Reid could game. Nothing is half-applied: `run.mjs`, `web/package.json` and `web/scripts/` are byte-identical to HEAD, and the isolated measurement tree is scratch-only. **Resume by rebuilding the isolated tree and re-running `probeall.mjs`; the recorded results are resumable, not restart-only.**
>
> **Domain owner:** `build-tooling-controller` · **Reports to:** Admin · **Phase:** build · **Status:** Both builds GREEN · headless harness (g++/CMake/Ninja, **41 suites, 41/41 green, shell-independent** since BT-8, re-counted BT-34 2026-08-10 — the "21 suites" figure below was stale) + UE 5.7 project (MSVC 14.44) · asset pipeline **48/48 green, byte-identical rebuild INCLUDING the generated textures, 3.97 MB of models + 0.52 MB of maps**, all joints exact and 0 coaxial conflicts under `check_mating.py`, belt cargo asserted by `check_belt_cargo.py`, surface maps asserted on the shipped bytes by `texgen.py check` · repo history rewritten onto **git LFS** (BT-7); **remote is `https://github.com/reidaruss/kerbalfactorio.git` (public), branch `main`** · **as of BT-30 to BT-39 (2026-08-10) development also runs on Proxmox VM `claude-dev` (10.10.10.36), 41/41 ctest green there too, web build green, served LAN-bound at `http://10.10.10.36:4200`; VM push access to GitHub is UNRESOLVED, see BT-31** · **Last updated:** 2026-08-19 (`lane/limits-batch2`, BT-285, line-cap BATCH 2, executing the BT-275 pilot template rather than inventing a method. **`check:limits` 51 -> 46**, `npm run check` 6/7 with every other link green including `check:boot`. **Five files split, worst-splittable-first, skipping the six recorded resisters** (`TerrainMaterial.ts`, `DebugGameplay.ts`, `TerrainShader.ts`, `Boot.ts`, `Gameplay.ts`, `Scatter.ts`): `StructurePlacement.ts` **755 -> 89** into `StructureAim.ts`/`StructureResolve.ts` (the raycast march and the target-resolution-plus-commit machinery, split along which functions call `aimHit` vs. which `resolveTarget` calls; zero `export` prefixes needed because every symbol crossing the new boundary was already `export function`), `Navball.ts` **665 -> 367** into `NavballTypes.ts`/`NavballFormat.ts` (the readout shapes and the HTML skeleton plus 21 text-formatting helpers; 22 `export` prefixes added, all previously module-private), `StationMount.ts` **644 -> 197** into `StationInstall.ts`/`StationArrival.ts`/`StationSeat.ts` (install-at-a-tick, the deck-plane clearance scan, boarding the walker, each calling into at most the group listed after it; one `export` prefix added, `capsuleClearAt`), `Autopilot.ts` **581 -> 102** into `AutopilotBridge.ts`/`AutopilotCurve.ts`/`AutopilotFit.ts`/`AutopilotPlan.ts` (the of_ap_* ABI detection, the departure curve, module-fitted, the plan and scheduling rule; three `export` prefixes added: `apModule`/`pending`/`readReach`), `Config.ts` **580 -> 256** into `ConfigTypes.ts` (every type/data declaration, including the 268-line `Config` interface, moved wholesale since none of it is behaviour; the barrel keeps `parseConfig` and its helpers; zero `export` prefixes needed). **Every original file stays the barrel**, re-exporting every symbol checked against every real external import site (not assumed) before landing. **Three new resisters found and characterised, same shape as the six already recorded**: `FlightMode.ts` (644 lines, one 549-line class body, 96 to 644), `NodeBatch.ts` (626 lines, one 390-line class, 237 to 626, whose ~195 movable header lines of helpers/consts/interface, if moved out, still leave the barrel at an estimated ~436 lines because the class alone is 390), `Loop.ts` (604 lines, one 562-line class body, 43 to 604, with almost no movable header at all). Together with the pilot's six, **nine of the worst offenders are now characterised as resisting a behaviour-preserving cut**, all for the identical reason (one function's or one class's body is essentially the whole file), which sharpens the pilot's finding: this is not a long tail of one-off holdouts, it is a recurring SHAPE, and the remaining ~37 splittable-but-not-yet-split reds plus these nine are two different kinds of debt needing two different remedies (more of this template vs. an Admin-level decision on class-shaped files). **Proof per file**: `npx tsc --noEmit` and `npx vite build` clean after each; moved text verified verbatim against `git show HEAD:<path>` by substring match (not by inspection) for every range, with every `export` prefix addition individually counted and named above; probes at documented invocations run before AND after each split (`wallstuck.js`/`qolbuild2.js`/`basesnap.js`/`build.js`/`ghostsize.js` for the structure split; `navdraw.js`/`phnav.js`/`stagedv.js` for the navball split; `stationpose.js`/`stationride.js`/`stationboard.js`/`stationwalk.js` plus the canonical `artframe.js` `station` shot for the station split, since it touches the drawn hull's pose, cross-build pngdiff 0.64% against a 0.35% same-build control, both in the pilot's own noise band; `vabdest.js`/`apshot.js`/`apexec.js` (the last a ~4 minute rendezvous-executor probe, run twice each way) for the autopilot split; `boot.mjs`/`buildtol.js`/`vabdest.js` for the config split); PASS -> PASS on every one, no NO_OUTPUT. **No BT-278-shaped repoint needed**: grepped `web/scripts`, `web/tools` and the probe corpus for all five filenames before touching any of them, and found only prose line-number references in probe comments (`wallstuck.js` cites `StructurePlacement.ts 138 to 153`, now stale by line number but not a programmatic dependency; `buildtol.js` cites `Config.ts HOME`), never a script that text-scrapes a symbol out of a hardcoded path the way `check-roles.mjs` did. **A hazard for the next lane found and worked around, not caused by this one**: this worktree carried two PRE-EXISTING, unrelated stash entries left over from other sessions reusing it (`WIP on lane/body-playability` and an uncommitted `TerrainMaterial.ts` split, both untouched, still present, see `git stash list`); a bare `git stash pop` after this lane's own `git stash -u` briefly surfaced the wrong entry by index collision, and after that near-miss every further before/after comparison in this batch used a direct file swap (`git show HEAD:<path> > <path>`, move the new siblings aside, rebuild, probe, restore from an out-of-repo backup copy) rather than stash at all. **Standing rule for a reused worktree: check `git stash list` before your first `git stash`, and if anything is already on the stack, never use a bare `pop` — apply by explicit index (`git stash apply "stash@{N}"`) and drop only the one you applied.** Full account: NUMBERS.md's BT-285 to BT-294 row; earlier same day `lane/limits-pilot`, BT-275 to BT-278, the BT-42 line-cap pilot. **`check:limits` 54 -> 51**, `npm run check` 6/7 with every other link green. Three behaviour-preserving splits: `TerrainArt.glsl.ts` 1117 -> 156, `Surfaces.ts` 1047 -> 365, `SpaceStation.ts` 776 -> 235, twelve new sibling modules, no call-site churn anywhere because each original file stays the barrel. **The finding that matters is not the three files: five of the eight worst offenders CANNOT be split behaviour-preservingly**, because essentially the whole file is one function's or one class's body (`TerrainMaterial.ts`, `DebugGameplay.ts`, `TerrainShader.ts`, `Boot.ts`, and by the same shape `Gameplay.ts` and `Scatter.ts`), so the remaining 51 reds are not 51 more files of the kind this lane retired and the debt needs an Admin decision rather than more of this. **`check-roles.mjs` broke and broke correctly** -- it text-scrapes `ROLE_FAMILY` out of a hardcoded path and was the ONLY thing in the repo that noticed a file had moved; repointed, self-test still 7/7, and the standing warning is now written down: grep the build scripts and the probe corpus for a file's PATH before splitting it. Full account: BT-275 to BT-278 below and NUMBERS.md's BT-275 to BT-284 row; earlier same day `lane/verdict-integrity`, BT-265 to BT-274, two related tasks closing the two gaps BT-260 flagged and routed rather than fixed. **BT-265 to BT-269, the well-formedness gate**: `check-invocations.mjs` gained `wellFormednessDefects()`, reusing `probeall.mjs`'s own exported `flagsOf()`/`FLAG_TOKEN_RE` (not re-derived) rather than reimplementing a second tokeniser. Every documented command's flag-section tokens must be a real `--flag`/`--flag=value` or a benign, corpus-confirmed marker (a trailing `#` shell comment -- `deepgate.js`/`goalmoot.js`/`lifeless.js`; a `|` pipe into a second command -- `artframe.js`/`stationdraw.js`/`sunshot.js`; a `[--flag=value]` bracket-wrapped optional -- `map3d.js`), and every `--evalargs` value is `JSON.parse`d. Proven against the REAL file: `shadowk.js` was temporarily reverted to its literal BT-260 pre-fix header (uncommitted, restored), and the gate caught it by name (`MALFORMED_INVOCATION`); restored, clean again. Building the check found a NINTH invocation-header instance, `clamprestore.js` (a stray literal `//` mid-line, a merge artifact), fixed. Corpus: 326 files, 314 documented, 12 exempt, 0 offenders. **BT-270 to BT-274, the throw-on-fail audit**: all 41 `throw`-bearing probe files audited and classified; 6 more (`keycollide.js`, `navdraw.js`, `qolbuild1.js`, `qolbuild3.js`, `qolhandsafe.js`, `qolsandbox.js`) carried the exact GP-401/GP-609 `bail()`/`finish()`-throws pattern qolbuild2.js was fixed at BT-260, converted identically; the other ~24 throwing probes are a different, older, single-linear-assert architecture with no `fails[]` accumulation and were deliberately NOT converted (out of this brief's "small mechanical diff, one pattern" scope). All 6 proven through the REAL `probeall.mjs` sweep: GREEN today, then each temporarily forced RED (uncommitted, reverted) with the injected failure named in the report, zero NO_OUTPUT either way, re-swept clean a third time. **run.mjs decision**: no partial `report` object exists at a probe-throw's point in the code to preserve (confirmed by running a synthetic always-throwing probe through the real runner: empty stdout, as the code's own ordering predicts); what already reaches `probeall.mjs`'s `runnerFails`/`stderrTail` from stderr is real and sufficient now that the only multi-line-throw probes are converted. One separately-committed, minimal fix: the caught error now carries `[stage=...]` (already tracked for the heartbeat, never surfaced at the terminal failure) after the unchanged `runner: ` prefix, verified not to break `probeall.mjs`'s extraction regex. `cd web && npm run check` 6/7 throughout, only the pre-existing `check:limits` red. Full account: NUMBERS.md's BT-265 to BT-274 rows; earlier same day `lane/nooutput-trio`, BT-260 to BT-264, AMENDED after a fresh-context verifier caught a wrong claim in this lane's first pass (see the shadowk.js entry below): the three fast NO_OUTPUT probes found by BT-210's sweep were THREE DIFFERENT CAUSES, not one, and none was the probe's subject going stale in the world-state sense (the GP-506 pickaxe-gate class) -- two were the harness eating a real verdict, one was a broken invocation header this lane initially missed by testing off the real sweep path. **ripplewalk.js**: a genuinely stale fixture, `sunDot:0.64`, unreachable at its own site (`setSunElev` ceiling measured 0.5242) and thrown on every run; RN-1002, written the SAME NIGHT this exact site was chosen, already fixed this project-wide to `0.45` and every sibling probe at this site (`etchsplit.js`) already uses it, but this one file's default and header never got the memo. Fixed to `0.45`; GREEN 3/3 (`gotDot 0.4501, err 0.0001`). **qolbuild2.js**: a genuine, reproducible content finding (the 'place' stage's "press aimed at the SKY places nothing" check fails: `structGhost.snapped` still resolves via the reach-clamped `FALLBACK_M` point even when `aim.found` is false, so a press places a foundation the player cannot see a preview for -- GP-289 fixed the SILENT half of this bug, drawing, in 2026-08-03 but never gated actual placement on `aim.found`/`overhead`, so the underlying defect it was named for is still live) that the OLD `throw`-based `finish()` was turning into NO_OUTPUT: run.mjs's try/catch drops the WHOLE report on a `page.evaluate` throw, not just `eval`, so a correctly-diagnosed RED read as indistinguishable from a crash. `finish()` now RETURNS `{fails, valid, log}` (verdictOf() convention 1) instead of throwing, keeping the per-failure `console.error` lines so a standalone run is still honest about its own exit code; real RED verdict 3/3, `fails: ["a press aimed at the SKY places nothing :: 1 parts placed at pitch +45"]` every time. **Routed to gameplay/factory-sim, not fixed here**: the sky-placement content bug itself (`StructurePlacement.ts` `resolveTarget`/`groundOrCarried` never checks `aim.found`/`overhead` before computing a placeable address). **shadowk.js, CORRECTED**: this lane's first pass reported "did not reproduce here, no code change" and left the file untouched. That was WRONG. The mistake was methodological: every verification in the first pass hand-typed the probe's invocation, copying its multi-line `--evalargs` JSON across two comment lines by eye -- something a human does correctly without thinking about it and a parser does not. The REAL cause, found by a fresh-context verifier and confirmed here directly (calling `extractCmd()` on the file and running the probe through the actual `probeall.mjs` sweep path, not a hand-typed stand-in): shadowk.js was the ONLY probe in the whole 314-probe corpus wrapping `--evalargs='{...}'` onto a second comment line, and the wrap was broken TWO WAYS AT ONCE. (1) That continuation line never carried the trailing `\` every other wrapped line in this header has, so `extractCmd()` (which stops collecting the moment a comment line does not end in `\`) truncated the command after `"lon":-86.27401,` -- unterminated JSON. (2) Restoring only the backslash does NOT fix it: `extractCmd()`'s own `parts.join(' ')` inserts a further bare space at the join regardless of each line's indentation, and `probeall.mjs`'s `flagsOf()` tokenises the extracted command on bare whitespace with no quote-awareness, so that inserted space still splits the quoted JSON and drops the tail token silently (it fails `flagsOf()`'s `--key=value` regex and is dropped, not erred on) -- same truncation, a second, independent mechanism. Fixed by collapsing the whole `--evalargs='{...}'` back onto ONE line, matching the convention every other multi-flag probe header already uses. **Proof, both ways, as required**: `extractCmd()` called directly on the fixed file returns the complete, JSON-valid command (`--evalargs='{"site":{"name":"mountains","lat":-31.165,"lon":-86.27401,"yaw":300,"pitch":-6},"sunDot":0.10}'`); the PRE-fix file, run through the real `probeall.mjs --tree=<worktree>` path (not run.mjs by hand), reproduces the exact originally-reported defect on demand (`ms:15795, verdict:NO_OUTPUT, runnerFails:["runner: page.evaluate: SyntaxError: Invalid or unexpected token"], parseErr:"Unexpected end of JSON input"`); the POST-fix file, run the same real way twice, returns real verdicts both times (`ms:46246 RED` with `fails:["valid: false"]`, the probe's own honest signal-vs-floor noise check, then `ms:32732 GREEN`) -- zero NO_OUTPUT. Three-probe subset through the real sweep path, post-fix: `qolbuild2.js RED, ripplewalk.js GREEN, shadowk.js RED then GREEN across two runs`, 0 NO_OUTPUT in the table. **`check-invocations.mjs` gap, found alongside this and NOT fixed here per the brief** (do not rebuild that gate in this lane): it only checks that `extractCmd()` returns something at all (a real command vs. `null`/`PROSE_ONLY_INVOCATION`), never that the returned command is well-formed, so a header whose extraction silently truncates or drops a token passes the gate exactly as this one did. Eighth instance of the invocation-header class (BT-190's twenty, orbitdeck.js, airlock.js, the three vm*.js probes, now this); routed to Admin, with the concrete shape it would take (attempt `flagsOf()` plus a `JSON.parse` of any `--evalargs` value, fail loudly on error, BT-225 to BT-227's precedent) recorded in NUMBERS.md's BT-260 row. **Harness-level pattern flagged for Admin routing, not fixed here per the brief**: ANY probe using the `throw`-on-fail convention (documented and intentional, per GP-401, for exactly this reason: "a returned `fails:[...]` exits 0 and prints PASS" for a standalone run) loses its whole report to NO_OUTPUT under the sweep the moment it has a real failure to report, because run.mjs's catch block discards the report unconditionally rather than only the `eval` field. qolbuild2.js is one instance; an unknown number of the corpus's other throw-convention probes are presumably in the same state, silently misclassified as instrument failures rather than content failures, until someone either audits how many probes use this pattern or fixes run.mjs to build a partial report before rethrowing. **Secondary, minor generator finding**: `build-probe-budgets.mjs`'s `buildBudgets()` excludes `timedOut` (HUNG) samples from budget derivation but not NO_OUTPUT samples, so a probe that crashes FAST has its crash duration counted as if it were a real measurement -- harmless when, as here, the real duration turns out longer, but capable of deriving a budget that's too TIGHT if a crash happens to be slower than the real thing. Not fixed, flagged. **probe-budgets.json updated surgically** (not a full 314-probe re-baseline, out of this lane's scope; used the generator's own `deriveBudget()` function, not hand-arithmetic, on top of the existing committed file), with samples from BOTH the hand-typed runs and the real sweep-path runs: `shadowk.js` 30000/120000 -> **60000/150000** (material: real completed runs measure 32732 to 46246 ms against the old crash-measured "max" of 13534); `qolbuild2.js` 30000/120000 -> **40000/120000** (material: its real sweep-path run measured 28082 ms, enough over the 30000 floor once the 1.25x margin applies); `ripplewalk.js` refreshed with a new sample (22503 ms) but stayed floor-bound at 30000/120000, unchanged. `cd web && npm run check` 6/7 (`check:limits` red, pre-existing, unchanged). NUMBERS.md BT-260 to BT-264 row updated. **Working-tree note**: this lane discovered mid-session it had been running commands against the SHARED main checkout (`D:\karbalfactorio\web`) instead of its own worktree copy for its first several diagnostic runs; corrected by installing `node_modules` fresh in the worktree, syncing assets there, and redoing every verification run (all 3-run proofs above) against the worktree's own dev server; a stray file and two stray dev-server processes left on the shared checkout during the mistake were found and killed by PID before landing. Full account in NUMBERS.md's BT-260 to BT-264 row; earlier same day `lane/probe-budgets-2`, BT-210 to BT-215: every one of the 314 documented sweepable probes now has a MEASURED, per-probe sweep budget in a single committed file, `web/tools/smoke/probe-budgets.json` (generated by new `web/tools/smoke/build-probe-budgets.mjs`), replacing the shared 240 s default plus three ad hoc `PROBEALL-TIMEOUT` header markers (`cantilever.js`, `machineports.js`, `padgate.js`, now deleted). One stated margin rule, not per-probe guesswork: soft `budgetMs` = 1.25x the probe's own measured max (exceeding it flags `overBudget: true`, never a content fail); hard `hardCapMs` = 3x measured max, floor 120 s (a kill here is a new, distinct, fatal `HUNG` verdict, no longer collapsed into `NO_OUTPUT` the way `assembler.js`'s false read was). Distribution across all 314: min 5.6 s, median 25.7 s, p90 89.1 s, max 710.8 s (`autoapproach.js`); `assembler.js` itself measured 686.9 s and now clears its own 860 s budget clean, proven live (696.7 s, GREEN, no NO_OUTPUT). Two **uncommitted** local-override cases proved the OVER_BUDGET and HUNG paths fire correctly and are restored before anything else ran. This was the SECOND attempt at BT-210: the first attempt's own isolation worktree lost its `.git` link mid-sweep and died at 52/325 (see the WORKTREE CORRUPTION INCIDENT row in ADMIN.md); this lane measured from a plain non-worktree scratch tree with all output in the session scratchpad throughout, and its own sweep process was independently killed by the environment three times mid-run, resuming cleanly each time with zero lost data via `probeall.mjs`'s existing resume-by-results-file logic. `cd web && npm run check` 6/7 (`check:limits` red, unchanged at 54 files). **Landed on `lane/probe-budgets-2`, not `lane/probe-budgets`: that name is still held by the first attempt's dead-but-still-registered worktree** (`agent-ae63b32323d392697`), which this lane's own worktree sandbox has no authority to remove; needs a rename or a worktree prune at or before merge. Full account: NUMBERS.md's BT-210 to BT-224 row; earlier same day `lane/artframe-out`, BT-255: `run.mjs --out` on `artframe.js`'s `station` shot was photographing the RN-352 forest at 1.62 m, not the station, because the shot teleported the walker back to the ground before resolving and `--out` screenshots after resolution; the teleport was removed, tested (not assumed) not to reintroduce the PH-89 settle hang it existed to dodge, and verified before/after (altitude 1.62 m -> 399,868 m, pngdiff against the same run's own `eval.png` 93.21% -> 5.21%, `forestfloor` unaffected at 0.21%); the other eight canonical poses audited clean; no previously published station PNG was found to actually be the forest, and a flag was added to rendering.md section 7 regardless; earlier 2026-08-18 `lane/check-chain`, BT-250: `npm run check` is no longer an `&&` chain, see the BT-250 decision row for the runner shape and both proofs; earlier same day `lane/probe-authoring-gate`, BT-225 to BT-227: a probe with no documented invocation now FAILS `npm run check:probes` (wired as the second link of `npm run check`, right after `check:roles`) instead of being silently skipped, the eighth instance of that class closed with a gate instead of another census; `vmlight.js`/`vmshade.js`/`vmcost.js` given real, live-verified invocations, one of which (`vmshade.js`) turned out flaky at its own correct flags and is routed to rendering, not fixed here; the cascade-0 shot-set gap moved from `rendering.md` section 2.9a prose to a proper `RN-1993` decision row in section 3; earlier same day `lane/bt-smalls`, two small fixes: **BT-240** `boot.mjs`'s ephemeral-port picker now re-rolls Chrome's unsafe ports, root cause was this box's Windows dynamic port range (1024-65534) overlapping 17 of Chromium's restricted ports, not the port list itself, proven with 300 real binds plus a forced-6667-collision case in `--selftest`; **BT-245** `docs/phase1/OVERNIGHT-LOG.md`'s 31-line mojibake cluster (67b4857) repaired by a new scripted transform, `tools/repair_mojibake.py`, verified character-identical to the pre-move original except for one unrelated intentional path rewrite; earlier 2026-08-16 BT-155 to BT-160, the harness gate census: all 316 probes swept in one pass, 217 GREEN/35 RED/30 NO_VERDICT/22 NO_OUTPUT/12 EXCLUDED, every BT-116 red now closed, and a flip-blocking harness gap found -- `probeall.mjs` does not recognise a singular `fail` string as a verdict, so 6 live probes that are actually failing sit as NO_VERDICT; gate not flipped, that is Admin's call; full account `docs/web/HARNESS-CENSUS-2026-08-16.md`; earlier 2026-08-14 BT-100/BT-101, `web/wasm/test/expected.json` cross-toolchain reproducibility: root-caused to a genuine 1-ULP libm rounding difference amplified by the noise pipeline to ~1e-12 m, bounded and correctly left unfixed, Tier B same as the existing native-vs-WASM class; a real, separate CRLF-vs-LF defect in the tool's own raw stdout found and fixed alongside it; `core/` re-confirmed 42/42 ctest green on this box, one suite more than the 41 below, not re-audited further; earlier 2026-08-11 BT-60 to BT-66, the SwiftShader diagnosis: BT-38 was Git LFS pointer files, not the GPU; `boot.mjs` GREEN on the VM; earlier 2026-08-10 BT-30 to BT-39, VM provisioning; earlier: 2026-07-27 W12 lane: BT-21 the procedural PBR surface pass, BT-22 UVs in metres on every render primitive, BT-23 the 24 m launch pad. **The maps and the UVs are INERT until the client lands the R8 hand-off**; earlier same day W11 lane A: BT-16 contract-first across four parallel build agents, BT-17 the player and the first-person hands, BT-18 the gait measured rather than eyeballed, BT-19 belt cargo, BT-20 armour, BT-14 Blender stays at 5.0.1, BT-15 `contact_sheet.py`)
> Read alongside: [MASTER_PLAN](../MASTER_PLAN.md) · [AGENT_ARCHITECTURE](../AGENT_ARCHITECTURE.md) · [ADMIN](ADMIN.md) · **[EXECUTION-PLAN](../EXECUTION-PLAN.md)** (the plan that created this domain)
> Created 2026-06-14 to own the execution layer the [review](../REVIEW-2026-06-14.md) found unowned (Q7).

## 1. Mission
Make the project buildable, testable, and reproducible. Own version control, the toolchain, the headless test harness that the entire "prove it Wave-0 first" thesis depends on, CI, and the asset pipeline. If a domain can't build or test its core, that's this controller's problem.

## 2. Scope & owned subsystems
- **Version control** — git repo, `.gitignore` (UE5 + build artifacts), branch/commit conventions, Git LFS for binary assets.
- **Toolchain** — UE5 version pinning, Visual Studio 2022 / MSVC, project generation, dependency management.
- **Headless test harness** — the standalone C++ test project (no UE5 dependency) + framework (Catch2/GoogleTest) for the Wave-0 pure-CPU cores; the bench harness for the 100k @ 60 UPS claim.
- **CI** — automated headless tests per commit; later, UE5 build CI.
- **Asset pipeline** — placeholder-art workflow for the slice; LFS + import conventions; cooking/packaging later.
- **Non-goals:** the gameplay/sim code itself (each domain owns its code + its tests); art *content* (Reid's hiring/asset call); the design (the controllers).

## 3. Key design decisions
| # | Decision | Rationale | Status | Date |
|---|----------|-----------|--------|------|
| BT-1 | Standalone **headless C++ test harness** built BEFORE the UE5 project | Wave-0 cores are engine-independent; retire the biggest claims cheaply first | **Accepted** — dependency-free harness; 7 suites green under g++/CMake/Ninja | 2026-06-14 |
| BT-2 | **git from day one** | docs+code were un-versioned (real risk) | **Accepted** — `git init` done; 8 commits on `main`; `.gitignore`/`.gitattributes` set (Git LFS deferred until binary assets exist) | 2026-06-14 |
| BT-3 | **UE 5.7** + VS2022/MSVC on Windows | Mirrors D-001; LWC is load-bearing | **Accepted** — UE 5.7.4 at `D:\UnrealEngine\UE_5.7`; editor target builds green | 2026-06-15 |
| BT-4 | **UE 5.7 Windows build prerequisites** — install via the **"Game development with C++" workload** to get them all: MSVC **≥14.44.35207** (UBT **BANS 14.40–14.43**); **Windows 11 SDK 10.0.22621**; **.NET Framework SDK 4.6.2+** (SwarmInterface needs the NetFxSDK). VS **17.13**'s channel offered only banned toolchains → had to update VS to **17.14** first. | The first UE build hit each of these one at a time; documenting so it is never re-debugged | **Accepted** | 2026-06-15 |
| BT-5 | **Install VS components via the GUI (self-elevating), NOT CLI `setup.exe modify --quiet`** | `--quiet` refuses to self-elevate from a non-elevated shell (exit **5007**, no UAC); `--wait` is also rejected by this installer version (exit 87) | **Accepted** | 2026-06-15 |
| BT-6 | **UE build command (reproducible):** `D:\UnrealEngine\UE_5.7\Engine\Build\BatchFiles\Build.bat OrbitalFoundryEditor Win64 Development -Project="<repo>\ue\OrbitalFoundry.uproject" -WaitMutex` | The headless cores build separately via `cmake -S core -B core/build -G Ninja -DCMAKE_CXX_COMPILER=g++` | **Accepted** | 2026-06-15 |
| BT-7 | **Repo surgery (RETHINK R3): one-time history rewrite onto git LFS, done PRE-remote.** `git lfs migrate import --include="*.uasset,*.umap,*.png,*.jpg,*.zip,*.fbx,*.exr,*.wav" --everything` over all 96 commits; 10 vendor demo maps (+BuiltData) untracked and gitignored first; the 3 unwired Fab packs (B3D, Megaplant_Library, WaterMaterials) gitignored; reflog expired + `gc --aggressive --prune=now`. Result: 1,060 LFS objects; git object store 2.43 GiB loose / 0 packs → **1.14 MiB packed** (binaries live in `.git/lfs`, ~2.7 GB). Rollback bundle: `C:\Users\reida\of-backup-pre-lfs-2026-06-16.bundle` (2.47 GB, verified). **No remote exists yet; adding one (LFS-capable host) is the next repo task, and is now safe.** All pre-rewrite hashes are dead; do not cite them. | 2.55 GB of binary blobs in plain git history could never be pushed to a normal host (GitHub 100 MB/file cap); rewrite was cheap only while no remote/collaborators existed. Reid approved the one-time rewrite. | **Done** | 2026-07-05 |
| BT-9 | **Base-building art set: one MODULE, owned in one file.** `tools/blender/structure_common.py` holds `CELL 1.00`, `DECK_H 0.50`, `WALL_H 2.50`, `WALL_T 0.25`, `STOREY 3.00` and asserts `DECK_H + WALL_H == STOREY` at import; the four build scripts (`build_foundation/floor/wall/door.py`) import it and declare nothing dimensional of their own. Two anchors, published as sockets rather than as constants a caller re-derives: a **deck** snaps to a cell CENTRE, a **wall** to a cell EDGE MIDPOINT and straddles it. `floor.glb` IS the ceiling (same part, placed at `y = 3(N+1)`); there is no `ceiling.glb`. Door collision is **three** proxies, not one, because a convex hull of a doorway is a sealed wall. Verified: 46/46 `validate_glb.py` green, full 46-script rebuild byte-identical, tiling measured off the shipped GLBs (collinear gap `+0.000000000 m`, storey pitch 3.000 exact). New tool `render_structures.py` assembles the shipped files into a room, because every part passes in isolation and the interesting failure is BETWEEN parts. | Reid asked for free-place/grid-snap base building; the art half had to land before the placement lane could start. Building four assets independently would have left the storey pitch unowned, which is exactly how a tiling set drifts. | **Done** (art only; **no sim referent exists**, see risk R4) | 2026-07-26 |
| BT-30 | **Proxmox VM `claude-dev` (10.10.10.36) provisioned as the implementation node (D-017).** apt toolchain (build-essential, cmake, ninja-build, git, git-lfs 3.4.1, python3, unzip, curl); Node.js **22.23.2** via NodeSource (matches `web/package.json`'s `^20.19.0 \|\| >=22.12.0`); Google Chrome **151.0.7922.108** via Google's own apt repo (matches what `run.mjs`/`boot.mjs` need, see BT-33); Blender **4.0.2** via apt (see BT-36, WRONG version for this pipeline); emsdk cloned to `~/emsdk`, **6.0.4** installed and activated to match the exact release the Windows machine's `C:\Users\reida\emsdk` resolves to (git hash `b23272fac5d05617cd36dc451356dca0f79bf22d`). Sudo is passwordless for user `reid`. | Reid's D-017, 2026-08-10: heavy work moves off his desktop and onto the VM; his browser plays the served build over the LAN. | **Done** | 2026-08-10 |
| BT-31 | **P1 was WRONG: the repo is PUBLIC, not private.** `curl https://api.github.com/repos/reidaruss/kerbalfactorio` returns `"private": false`. Anonymous HTTPS clone onto the VM (`~/kerbalfactorio`) needed no auth at all and its HEAD matched local `main` and `origin/main` exactly (`1cd0e47a...`) with LFS materialising all 30 HEAD objects (`auth=none` on the LFS endpoint too, because the repo is public). **PUSH access is a separate, unresolved problem**: an ed25519 deploy keypair was generated on the VM (`~/.ssh/of_deploy_key[.pub]`, fingerprint `SHA256:GcQ2d4BXPZsnFN4ze/vrgt2oeetWbH2AxC5rqWcN4wg`), but registering it (`gh repo deploy-key add --allow-write`) was refused by the local session's own permission classifier as a repo-config change needing explicit user go-ahead, and separately `gh auth status` shows the local `gh` CLI is not logged into any GitHub host at all. **Public key that needs adding, by Reid or an authenticated `gh` session, via `gh repo deploy-key add --allow-write -R reidaruss/kerbalfactorio <file>` or the GitHub UI (Settings → Deploy keys):** `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICGPqIl9hunXmBYp0x1e9j4QvR+YB5R9gVilcf4Wijeb claude-dev-vm-orbitalfoundry`. Until that lands, **no lane running on the VM can push**; every VM-side commit sits local-only. | The brief's fallback (bundle+scp) turned out to be unnecessary because of P1, which unblocked read immediately; the write side still needed an account-level action outside this session's authority. | **Half-done — push access BLOCKED, needs Reid or an authenticated `gh`** | 2026-08-10 |
| BT-32 | **P2 confirmed: `web/wasm/build.ps1` cannot run on Linux (it's PowerShell), so it was ported to `web/wasm/build.sh`**, flag-for-flag identical (same emcc flags, same `-std=c++17 -I core/include`, same native-ground-truth-then-wasm two-step), committed at `web/wasm/build.sh`. One deliberate divergence: the Windows script static-links the GNU runtime into `dump_expected.exe` (BT-8, dodging a stale system `libstdc++-6.dll`); the Linux port does not, because that problem is specific to Git-for-Windows' bundled DLL and does not exist on a box with one glibc/libstdc++ on PATH. Verified: `bash build.sh` on the VM produced `of-core.mjs` (43,230 B) and `of-core.wasm` (464,431 B), then `npm run sync-assets && npm run sync-wasm && npm run build` all green (`tsc --noEmit` clean, vite build in 1.0 s). **pwsh was never installed and is not needed**; do not add it on this evidence. | Brief's instruction: port if straightforward and clearly equivalent. It was. | **Done** | 2026-08-10 |
| BT-33 | **P5 was WRONG: Chrome discovery in BOTH `web/tools/smoke/run.mjs` and `web/tools/smoke/boot.mjs` was hardcoded to Windows-only paths** (`C:/Program Files/Google/Chrome/...`, `%LOCALAPPDATA%\...`), with no Linux entry and no env-var override, so the probe harness could not launch on Linux at all before this fix (`smoke: no Chrome or Edge found`, exit 2). Fixed **both** files identically (a comment in `boot.mjs` already states the invariant: "The exact candidate list run.mjs uses. One list, or the two tools drift" — honoured it rather than fixing one and leaving the other to drift): prepend an optional `process.env.CHROME_PATH` override, then append `/usr/bin/google-chrome-stable`, `/usr/bin/google-chrome`, `/usr/bin/chromium-browser`, `/usr/bin/chromium` after the existing Windows/Edge candidates. Verified on the VM: `node tools/smoke/boot.mjs` now finds and launches Chrome (previously immediate exit 2). | Every previous probe/CI assumption baked in "runs on Reid's Windows desktop"; the VM move makes this the first time it needed to run anywhere else, and it silently couldn't. | **Done** | 2026-08-10 |
| BT-34 | **P4 was WRONG, and the controller's own header is stale: `core/` has 41 ctest suites at HEAD, not 21, and all 41 are green on the VM** (GNU 13.3.0, `cmake -G Ninja -DCMAKE_BUILD_TYPE=Release`, 140/140 build targets, `ctest`: 100% passed, 41/41, 51.90 s total). The 21-suite figure in this file's own top banner and in BT-8/BT-7's entries is from 2026-07-05/07-27 and the suite count grew with every domain's later work (physics/vessel/flight/maneuver/docking/approach/descent/ascent, enemies, gameplay+survival+research+progression, poi/spawn/discovery/water_field/surface_field/voxel_field/terrain_deform/voxel_terrain/biome/surface_walk, persistence+persistence_file, slice_e2e, etc.); nobody had re-counted it since. One harmless build warning (`-Wformat-truncation` in `spawn_probe.cpp:899`, a `%2d` into an 8-byte buffer that in practice never gets a value wide enough to truncate). | Verifying rather than assuming, per the brief. The stale number matters beyond cosmetics: anyone reading the banner would under-report this domain's own test coverage by half. | **Done — banner needs a follow-up edit to say 41** | 2026-08-10 |
| BT-35 | **The native ground-truth generator (`web/wasm/test/dump_expected.cpp`) is NOT toolchain-portable at the hash level, though the underlying doubles are.** Running the (uncommitted, `--skip-native` avoided by default) native step of `build.sh` on the VM's GNU 13.3.0 g++ regenerated `expected.json` with **different `contentHash`/`rawContentHash`/`heightHash`/`chunkHashes` values than the committed (WinLibs g++) fixture, while `h0`/`h544`/`h1088`/`posHash`/`nrmHash` and every other bit-exact IEEE-754 double stayed byte-identical.** That split (heights identical, hashes different) points at container **iteration order** feeding a hash rather than at floating-point drift, most likely an `unordered_map`/`unordered_set` whose libstdc++ ABI or bucket layout differs enough between the two toolchains to reorder what gets hashed, though this was not root-caused. **Not committed**: `git checkout --` reverted `web/wasm/test/expected.json` and `web/wasm/dist/of-core.wasm` on the VM clone before this report, so nothing regenerated by this run left local-only diffs behind. Escalated as a cross-domain flag (world-gen/core-engine): if any hash in that fixture is meant to be a determinism proof across machines and not just across runs on one machine, it currently is not one. | Found by running the full (non-skip-native) build path once to prove it end-to-end, per the brief's step 4; not something the brief asked to be root-caused. | **Flagged, not investigated further — cross-domain** | 2026-08-10 |
| BT-36 | **P3 confirmed WRONG: apt's Blender (4.0.2) does not match the pinned pipeline version (5.0.1, BT-14) and must not be used to run the asset build scripts on this VM as installed.** BT-14 pinned Blender at 5.0.1 specifically because the exporter version string (`"generator": "Khronos glTF Blender I/O v5.0.21"`) is written into every shipped `.glb`, so running `tools/blender/*.py` against 4.0.2 would rewrite the exporter identity, and likely bytes, of all 48 shipped assets — exactly the "expensive rebaseline" BT-14 explicitly declined to do even for the *newer* 5.2 LTS. Blender 4.0.2 is installed and present (`blender --version` confirms) for anything that does not touch the committed asset pipeline (e.g. ad hoc CPU Cycles renders), but **no lane should run `build_*.py`, `texgen.py`, `validate_glb.py`'s rebuild path, or `render_check.py`'s asset-affecting modes on this VM until either Blender 5.0.1 is sourced for Linux or Reid approves upgrading the pin.** | The brief asked this to be checked, not assumed; it would have been an easy, silent way to corrupt the byte-identical-rebuild guarantee BT-9/BT-12/BT-13/BT-21/BT-22 all depend on. | **Flagged, blocking asset-pipeline work on this VM** | 2026-08-10 |
| BT-37 | **The served build is LAN-bound and survives logout/reboot.** `loginctl enable-linger reid` (confirmed `Linger=yes`) plus a systemd **user** unit `~/.config/systemd/user/orbital-preview.service` (`ExecStart=/usr/bin/npx vite preview --port 4200 --strictPort --host 0.0.0.0`, `Restart=on-failure`), enabled and started. Verified **from the local Windows machine**: `curl -s -o /dev/null -w "%{http_code}" http://10.10.10.36:4200` → **200**, body is the real `index.html` (`<title>Orbital Foundry</title>`, the built asset bundle). This is the loopback-binding mistake every previous freeze made (state-of-the-union §1), fixed at the source this time (`--host 0.0.0.0` in the unit file itself, not a flag someone has to remember). | Brief step 5, and the one concrete thing the state-of-the-union file names as the next session's required fix. | **Done** | 2026-08-10 |
| BT-38 | **One smoke probe run on the VM (`node tools/smoke/boot.mjs`, no sweep): the probe STACK works; the probe's own verdict is a real FAIL, not an infra failure.** Wall clock 55.2 s total (sync 0.2 s, vite build 0.7 s, headless boot 54.2 s). After BT-33's Chrome fix, Chrome launched under `--enable-unsafe-swiftshader`, the page loaded, the WASM module booted (`W0 handshake abi=22 wasm 56 ms`, oracle timings in the 2 to 3 us range), a terrain worker built a real chunk (`W2 terrain chunk 1217 verts / 73940 B`), and the harness captured console output correctly. **The FAIL itself is real content, not a harness defect**: `of_ground.png`/`of_ground_relief.png` and the shared surface maps failed to decode (`InvalidStateError: The source image could not be decoded`) and WebGL reported `CONTEXT_LOST_WEBGL` followed by a restore, with GPU-stall driver messages around `ReadPixels`. `/dev/shm` is 24 GiB (ruled out as the classic headless-Chrome-on-Linux cause). **Not root-caused** — flagged for rendering/world-gen as an open question whether this is Linux/SwiftShader-specific, an artifact of this exact Chrome build (151.0.7922.108), or something that also happens on Windows and has just never been looked at with console capture this strict. | Brief step 6: prove the stack runs, not sweep. It ran to a real, informative verdict in under a minute, which is the answer to "does this box work for probes" even though the specific probe failed. | **Done (stack proven); the FAIL is a cross-domain open question, not a build-tooling defect** | 2026-08-10 |
| BT-39 | **Claude Code on the VM works non-interactively and is NOT on PATH for non-interactive ssh shells.** `~/.local/bin/claude -p "reply with exactly: OK"` returned `OK` in 4.1 s. `ssh reid@10.10.10.36 'echo $PATH'` does not include `~/.local/bin` (a login-shell-only PATH addition, most likely in `.bashrc`'s interactive-only guard or `.profile`, not sourced by a non-interactive `ssh host cmd` invocation). **Every future lane must use the full path** `~/.local/bin/claude` (or `/home/reid/.local/bin/claude`), never bare `claude`, when driving the VM over `ssh user@host 'claude ...'`. | Brief step 7. A lane that assumed `claude` was on PATH would get a confusing "command not found" on its very first remote invocation. | **Done** | 2026-08-10 |
| BT-60 | **BT-38's texture decode failure was NOT SwiftShader and NOT a renderer defect: the 25 shipped texture PNGs were Git LFS POINTER FILES on this VM, 130 bytes of ASCII each.** `assets/textures/dist/of_ground.png` is 1,436,496 B of real PNG; on the VM it was 132 B reading `version https://git-lfs.github.com/spec/v1`. `git lfs ls-files` showed **0 of 30 objects downloaded**, in the lane worktrees *and* in the main clone `~/kerbalfactorio`. Cause: **BT-7's blanket `*.png filter=lfs` rule was retired on 2026-08-03, which also removed the SMUDGE filter from the .png files ALREADY in LFS**, so a checkout writes the pointer text verbatim and no tool complains. Reid's Windows tree still holds the real bytes because it was populated while the filter was live, which is exactly why this is invisible there. Fixed as `.gitattributes` itself prescribes ("add an explicit rule for it by path"): explicit `filter=lfs` rules for `assets/textures/dist/*.png` and for the one LFS probe fixture `web/tools/smoke/fixtures/RN958_ripple_frames.png`, which was also stranded as a pointer and would have failed any probe that diffs it. | BT-38 was flagged as a rendering/world-gen open question about SwiftShader. It was a repo/checkout defect in this domain, and the sweep would have produced garbage reds all night on it. | **Done** | 2026-08-11 |
| BT-61 | **`sync-assets.mjs` now REFUSES to copy a Git LFS pointer, naming every offender and the `git lfs pull` fix.** The instrument had printed the answer and nobody read it: the passing line was `25 .png, **0.00 MB**` and the healthy line is `25 .png, **7.29 MB**`. It copied 130-byte text files into `public/`, the static servers served them as HTTP **200** with `content-type: image/png`, and the only symptom appeared three layers away as a decode error, which read like a GPU fault. **Negative control (scratch copy at `/tmp/bt60scratch`, never the repo tree):** with real textures it exits 0 (`25 .png, 7.29 MB`); with two pointers reintroduced it exits **1** and names both files. | NUMBERS.md's whole thesis: the harness defect outnumbers the system defect, and a number that is printed but unread is not an instrument. | **Done** | 2026-08-11 |
| BT-62 | **`GPU stall due to ReadPixels` is allowlisted in `run.mjs` and `boot.mjs` (BT-33 invariant: edited identically in both).** A KHR_debug **Performance** message, severity High, emitted only by the SwiftShader/Vulkan backend, 3 to 4 times per run on **100% of runs** on the VM and never on Windows, where ANGLE runs on D3D. It fires on the synchronous readback the `ItemIcons` baker deliberately performs (`canvas.toDataURL` per icon). It is not a fallback and not an app defect, so it is named exactly, not wildcarded, alongside the two existing X4122/X4000 entries. **This, not the context loss, is what deterministically failed the gate.** | The console rule exists to catch a silent shader FALLBACK. A driver saying a readback is slow on a software rasteriser is the rasteriser working as designed. | **Done** | 2026-08-11 |
| BT-63 | **GL context loss is now judged as an EPISODE with a narrow tolerance, not blanket-allowlisted, in both tools.** Measured on the VM: the context is lost at ~0.9 s and restored at ~2.6 s on **3 of 4** runs, always inside the `ItemIcons` bake, where the app holds a **SECOND** WebGL context. **It is benign for measurement, and that was measured rather than assumed:** a lost-and-restored frame differs from a clean one by **0.49%** of pixels (3,373 of 684,800), against **0.31%** (2,096) between two clean runs and **0.51%** between two lost runs, same `maxDelta` 227 to 237. Same tick counts either way. So the rule is: a loss is tolerated **only** when a restore answers it **and** the pair completes before `__of.ready`; a loss after ready fails ("everything measured past that point was drawn on a dead context") and an unanswered loss fails ("it never came back"). A tolerated loss is **printed on the PASS line**, never silent. Two traps found while building it, both of which would have caused false verdicts: one real loss emits **TWO** console lines (Chrome's `CONTEXT_LOST_WEBGL` and three's `Context Lost.`), so message-counting scores one loss as two, exceeds the restore count and fails every SwiftShader run for the wrong reason (hence the `down` state machine); and Chrome's warning is printed **only when something afterwards touches the dead context**, so it cannot be the key at all. | Blanket-allowlisting `CONTEXT_LOST_WEBGL` would have blinded the harness to the losses that DO matter, which is precisely the "probe that cannot fail" the brief forbids. | **Done** | 2026-08-11 |
| BT-64 | **`boot.mjs --selftest` grew from 4 cases to 7 and is 7/7 green**, adding the three branches of the BT-63 rule, driven by a **real** `WEBGL_lose_context` call and the same `webglcontextlost`/`webglcontextrestored` DOM events three.js reports from: a loss after ready must FAIL, a loss before ready **with** a restore must PASS, and the same page one line different **without** the restore must FAIL. Two of the three are must-fail cases, so the tolerance cannot quietly widen into "context loss is fine". The after-ready case builds its context **inside** `settle()` and not at load, because a context that exists while the page is still coming up can be taken by the browser first, which lands the loss before ready and silently tests the wrong branch (this happened, and the case failed until it was fixed). | Standing rule 11: a gate that has never been seen to fail is a log line. | **Done** | 2026-08-11 |
| BT-65 | **End-to-end negative control: BT-38's exact symptom reproduced on demand, which is what makes BT-60 a cause and not a correlation.** Serving 132-byte pointers in place of the textures (corrupting `dist/` **after** the build, since `prebuild` re-runs `sync-assets` and silently repaired the first attempt, which produced a misleading PASS) turns `run.mjs` red with **`InvalidStateError: The source image could not be decoded`** for `of_ground.png` and `of_ground_relief.png` plus `[of] surfaces: the shared maps did NOT load`. Restoring real bytes returns it to green. Note the app's own error text already said `run \`npm run sync-assets\``, and it was right. | The first control run PASSED and would have let a wrong root cause ship; the build re-syncing assets under the experiment is itself a NUMBERS.md-class trap worth naming. | **Done** | 2026-08-11 |
| BT-66 | **`boot.mjs` is GREEN on the VM, 4 runs of 4, ~60 to 71 s each** (was: FAIL at 55.2 s). Example: `boot: PASS tick 141, 74 frames, gpu ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero) (0x0000C0DE)), SwiftShader driver), 1 pre-ready context loss(es) restored 1x`. **No assertion was weakened to get there**: the decode errors were a real defect and are fixed, the ReadPixels entry is a named driver performance note, and the context-loss rule is strictly narrower than "allowlist it". | The gate is the precondition for the 284-probe sweep. | **Done** | 2026-08-11 |
| BT-8 | **Static-link the GNU runtime in all headless test/tool exes** (`add_link_options(-static-libstdc++ -static-libgcc -static)` for MinGW in `core/CMakeLists.txt`) **+ ctest `TIMEOUT 120` on all 21 suites.** | Exes dynamically linked `libstdc++-6.dll`; Git for Windows ships an older DLL on the system PATH missing the `<filesystem>` symbols, so the two `<filesystem>` suites died at load (0xc0000139) or passed depending on the shell, and each loader death stalled ~1000 s in WER (the "hung overnight run"). Root cause proven in [core-docs-audit Task A](../review-2026-06-16/core-docs-audit.md). Verified after fix: fresh configure+build, **21/21 green from plain PowerShell AND Git Bash**, no PATH setup needed. | **Done** | 2026-07-05 |

| BT-10 | **DW-27 packaging spike: the Steam desktop shell lives in `electron/`, its OWN npm package, and the scale test found the wall.** Electron 40.10.6 serves the unmodified `web/dist` over a privileged `of://` scheme (`standard/secure/supportFetchAPI/corsEnabled/stream`). **`web/`'s dependency tree and dev loop are untouched**, which was a hard constraint because every lane depends on them. Measured, all with REAL GPU-composited windows parked at (-3200,-3200) rather than headless, and a `gpuguard.mjs` that refuses to report a number from a software rasteriser: **cold start 7047 ms median against Chrome's 7032 ms** (Electron's own share is 381 ms to `app.whenReady`; the other 6 s is our client and shows identically in the browser); **frame time, draw calls and triangles identical to Chrome** (41 / 45 / 49 draws, p50 2.0 to 2.2 ms, p99 3.0 to 3.7 ms); **installed 337.8 MB of which 7.5 MB is ours**; **604.7 MB process working set, flat over 2 minutes**; JS heap 60 MB above Chrome, WASM heap 64 MB with zero growth. **DW-17 survives the origin change**: `persist.js` and `tunnelpersist.js` both `valid: true` on `of://`, and `file://` turned out to support IndexedDB, module workers and wasm streaming too (Electron does not give `file://` the opaque-origin treatment a browser does). The custom scheme is kept for a different reason: vite's absolute `/assets/` base means the real app never boots from `file://`, and fixing that would mean changing `web/vite.config.ts`, i.e. the shared dev loop. **THE FINDING: the factory stops drawing at 150 machines** because `MachineBatch.ts:26` is `const CAPACITY = 256` with no grow path, shared by machines, belt tiles and DW-9 inserters. Past it, `acquire()` returns -1, `FactoryView.sync` skips, and the machines exist in the plan and in `/core` and tick and are never drawn: triangles freeze at exactly 602,994, draw calls at 45, frame time does not move, and every budget indicator reads `ok`. **It fails as a pass.** `StructureView` is the same shape at 512. Separately, one adopted build site bounded the layout at 323 machines (`SITE_REACH_M 32`). **DW-11's model held and beat itself**: whole factory = 1 batch = 4 draw calls, whole base = 1 batch = 4 more, 740 live instances and 764,690 triangles for 49 draw calls of a 150 budget. **Steamworks verified**: `steamworks.js@0.4.0`, prebuilt win32-x64 Node-API binary so no `electron-rebuild`, `init(480)` succeeds against the running client, cloud flags true, and binary save bytes round-trip losslessly through its `string`-typed cloud API at ~1.5x inflation. No partner account created, nothing spent, no credential written. | DW-27 asked for the two unproven things to be spiked EARLY, while the client is ~123 files. Every performance number this project had was taken on six-building lines. Finding a silent 150-machine render ceiling today rather than in a playtest in two months is the entire return on the spike. | **Done** | 2026-07-26 |

| BT-11 | **DW-32: the structural module goes 1 m to 4 m, and ONLY THE PLAN MODULE SCALES.** `CELL 1.00 -> 4.00`, `WALL_H 2.50 -> 3.50`, `STOREY 3.00 -> 4.00`; **`DECK_H` stays 0.50 and `WALL_T` stays 0.25**. The split is the decision: a deck thickness and a wall thickness are set by what a deck and a wall physically ARE, not by how wide the bay is, so scaling them x4 would have produced a 2 m slab and a 1 m partition. The new identity is **`STOREY == CELL == 4.00`**, asserted at import next to `DECK_H + WALL_H == STOREY`, so the plan lattice and the vertical lattice are one number and both are whole multiples of the 1 m voxel grid. **The door stays ONE FULL CELL and its OPENING does not scale** (0.76 x 2.10 -> 1.20 x 2.40): a 4 m opening is a garage, and a sub-cell door would need a second finer lattice for wall furniture that the placement system does not have, which would cost the property that makes the set usable, that a wall run is a list of cells and any cell can become a door. **New `structures/pillar.glb`**: four ground-pivoted parts (foot, shaft, collar, head) assembled by a published recipe, because the gap an overhanging deck must span is a CONTINUOUS number. The shaft is **prismatic**, constant cross section with no axial feature, so scaling it in Y is exact rather than approximate, and it is authored at exactly 1.00 m so `scale.y` IS metres; rhythm comes from unscaled collars at a 2 m pitch. Verified: 5/5 validator green, byte-identical rebuild, tiling measured off the shipped GLBs (collinear gap `+0.000000000 m`, storey pitch 4.000 exact, clear interior 3.750, pillar residual `+0.000000000` at four gap heights), six renders under `docs/screenshots/structures_*.png`. | Reid: foundations and walls are "wayyy too small" (a modest base was hundreds of pieces; a 20 x 20 m platform goes from 400 foundations to 25) and he needs to build out from a slope to make a flat surface. The rescale was cheap only because BT-9 put every dimension in ONE file; four independently-typed scripts would have made this a re-authoring job. | **Done** (art only; the client pass is section 7 R6) | 2026-07-26 |
| BT-12 | **DW-29: the vessel catalogue, in TWO diameter classes and no more.** Class **S 1.25 m** (R 0.625, 16 segments) and class **L 2.50 m** (R 1.250, 24 segments), exactly 2x. Two is the smallest number that lets a first stage be wider than its payload, which is what makes staging read as staging; a third would double the catalogue for a size nobody asked for. 13 parts became **24**, with the DW-29 names (`LiquidTankSmall/Large`, `LiquidEngineSmall/Large`, `StackDecouplerSmall/Large`, `SolidBooster`, `RadialDecoupler`, `MonopropTank`, `ReactionWheel`, `Battery`, `DockingPort`, ...). **`StackAdapter` is an addition to the DW-29 list and is the consequence of it**: the moment "two sizes" means two DIAMETERS, a wide stage under a narrow payload has no legal joint and the classes are two disjoint games. Class is published per socket in `of_role` (`stack_top_s`, `stack_bottom_l`, ...), because with two classes the anti-parallel dot product is necessary but no longer sufficient and a 2.50 m decoupler will otherwise sit happily on a 1.25 m tank. **Lengths did not scale with the class**: `StackDecouplerLarge` is 0.35 m, not 0.50, because a joint gets wider when the vessel does, it does not get taller. Verified: validator 3/3 (including `lander_landed` and `launch_pad`, rebuilt to prove the shared `rocket_common` refactor changed nothing, byte-identical), `check_mating.py` reports `+0.000000000 m` at every joint in both class chains, 7132 render tris, 315,028 bytes, three renders. | DW-29 is the project's premise being built, and the assembly UI needs an interface, not a pile of meshes. **The process point worth keeping: `contracts.json` was written FIRST, before any geometry, and the build agents were forbidden from editing it.** ASSET-SPECS 7.1 already said a checker derived from the builder only proves the builder agrees with itself; writing the contract first makes that structural rather than aspirational. | **Done** | 2026-07-26 |
| BT-13 | **A defect class that only a human eye could catch is now arithmetic: the COAXIAL SCALLOP check in `check_mating.py`.** Two coaxial round surfaces with different segment counts do not share a surface at the same radius: a 16-gon and a 24-gon of circumradius 0.625 have inradii 0.6011 and 0.6146, so each pokes through the other at alternating azimuths and the joint renders as a sawtooth ring. **It passes every other check**: the AABB is set by the circumradius so `scale` is exact, and the mating PLANE is untouched so `part_sockets` and every gap measurement read `+0.000000000`. Detection works on the shipped bytes with no help from the builder: dedupe positions (flat shading splits a vertex per face, so raw indices do not describe adjacency), union triangles into connected components, test each for being a solid of revolution about a principal axis (rings of >= 6 vertices, one radius, uniform azimuth), then require that any two same-axis components with different segment counts NEST, one inside the other's inradius, checked at every ring plane in the overlap. **No false positives by construction**: a slab, a fin and a stringer are not rings and are never compared. Found 3 defects in the new catalogue and **a fourth in an asset that had already shipped**: `player_body`'s 8-gon elbow band (inradius 0.0628) let the 10-gon arm inside it poke through by 0.6 mm. Fixed by matching the segment count; the rig re-validated clean (44 joints, every vertex weighted, rest pose 2.7e-07, 14 clips live). Now 47 assets, 255 render meshes, **0 conflicts, and 4 round components reported as NOT EXAMINED**. That last number is the part worth keeping: two shells and two mixed-segment components cannot meet the method's precondition, and printing only "0 conflicts" would have read as full coverage. Each plane is now classified by a POSITIVE test rather than by inferring a reason from a failed one, because the first version inferred "shell" from the single-ring test failing and reported **203** skips instead of the true 4, which is the same uselessness in the other direction. Per DW-20 the check has also **demonstrated it can fail**: run against the pre-fix `player_body.glb` out of git it reports the elbow conflict, against the shipped one it reports nothing, and a `selftest` pass pins the nesting arithmetic on the three real bad geometries plus two legal ones that must not fire. | Admin's call, and the right one. This is DW-28's lesson in a different costume: **a failure that reports success is the expensive kind**, and three silent scallops that passed a clean mating measurement are exactly that. The generalisation for future art work: when a render catches something twice, the third time should be a check, and the right place to put it is wherever the information actually exists (here the exported mesh, not the builder, because the shipped bytes are what the game loads). | **Done** | 2026-07-26 |

| BT-16 | **W11 lane A ran contract-first across FOUR parallel build agents, and the tree was deliberately RED for most of the night.** Every `contracts.json` change was written by this controller BEFORE any geometry existed, and every agent was explicitly forbidden from editing it, on the BT-12 principle that a checker derived from the builder only proves the builder agrees with itself. The measurable consequence: `validate_glb.py --all` went from 47/47 to **38/47 the moment the contracts landed**, and stayed red until the art caught up. That is the intended state and it has to be said out loud, because I first briefed all four agents with "47/47 must pass" as the acceptance and **that was unreachable by construction**; I corrected all three running agents in writing within the hour. The lesson to keep: **when contracts lead the geometry, the acceptance criterion is "every asset I own passes individually, plus the named list of other lanes' assets that are expected to fail", not a global green.** A global green is only meaningful at the end. Final state: **48/48 pass, one new asset (`armour_set`), `ALL MATES AND TILES EXACT` with 0 coaxial conflicts, dist 2.69 MB to 3.25 MB across 48 files.** | Four agents editing one asset pipeline overnight, with five OTHER lanes live in `core/` and `web/`. Contract-first is what let them run in parallel without negotiating with each other, because the interface was already written down. | **Done** | 2026-07-27 |
| BT-17 | **The player character: the first-person hands were the worst asset in the game and the fix was three separate causes, not one.** Reid: the on-screen hands "read as large white mitts". Tiling the eight before-renders into one sheet (BT-15) is what showed the actual defect, and no single render says it: **the model was ONE UNBROKEN WHITE SHAPE.** Every piece of colour separation the asset already had (a `SteelDark` elbow band, a `SuitAccent` deltoid, a `Skin` wrist band) was up the sleeve and therefore out of frame; from the eye point the entire model was `OF_Suit` `D8D3C6`. Cause two: **the hand was a round tube with three round prongs on the end**, and a circular cross section cannot read as a palm from any angle. Cause three: framing, the hands sat 0.435 m from a roughly 70 degree camera. Fixes, in that order: colour moved to where the camera can see it (`SuitDark` glove, `SuitAccent` cuff, `Plate` knuckle plate, a wider bare `Skin` band); the palm rebuilt as an **elliptical** section widening to a knuckle line, with **five** finger tubes carried on the three frozen bone chains and a thumb springing from the side of the palm on a genuinely different axis; hands moved to `(+/-0.200, -0.302, +0.620)` and elbows in to +/-0.367, which took visible frame height at the hand from 0.61 m to 0.87 m. **`player_fp_arms` 964 to 1740 tris, `player_body` 1244 to 2320.** | This is the single most visible thing in the game, on screen every frame. It is also the clearest case yet for BT-15: the defect was a property of the SET of renders, and it had survived every previous per-render review. | **Done** | 2026-07-27 |
| BT-18 | **The gait was wrong in three ways and all three were found by MEASURING the shipped file, not by looking at it.** `Walk` and `Run` are now solved from an authored ankle path (`rig_common.leg_ik`) instead of sampled sines, because a pure sine has no foot plant. The three defects, each of which had survived every render: **(1)** the run lean was keyed on `Hips`, which `UpLeg` inherits, so an 11 degree lean pitched the sole 11 degrees toe-down and drove the toe **30 mm through the ground**; the lean moved to the spine chain, above the legs. **(2)** `rig_common.keys` rounds the FRAME but not the sampled parameter, so at 16 samples over 25 frames a key for phase 0.0625 was written to frame 3, which is phase 0.0833; contact-phase foot velocity swung between 1.7 and 6.6 m/s around a 4.5 target. Gait clips now key one sample per frame. **(3)** pelvis yaw moves the hip socket: the `UpLeg` head is 0.10 m off the midline, so +/-5 degrees of yaw carries it +/-8.7 mm fore and aft, a **2.3% skate that survived everything else because the foot path was right and the HIP was moving.** Measured result on the shipped bytes: contact-phase net world slip **+0.0005 m over a 0.5250 m root travel**, implied ground speed **4.496 m/s against 4.50 authored**, worst single-frame slip 0.9 mm, **ground penetration 0.0000 m** (was -0.0296). Left and right measure identically. `ToeBase` is now keyed in every locomotion clip; it was keyed in **zero** of them before. | DW-7's lesson generalised one step further. DW-7 says structural validation cannot replace looking at the thing. These three say **looking at the thing cannot replace measuring it either**: a 2.3% skate and a 30 mm toe penetration are invisible in a still and obvious in a number. The pipeline now has both halves. | **Done** | 2026-07-27 |
| BT-19 | **Belt cargo: the published carrying height was not on any surface, and the fix was to make the number true rather than to move the number.** Items now ride belts. The convention is **one rule for both tile shapes: an item's position across a tile is the circular arc through `socket_item_a`, `socket_item`, `socket_item_b`, parameterised by ARC LENGTH**, which degenerates to the chord on a straight tile so a consumer has no per-shape branch. The midpoint is load-bearing: from two endpoints alone a quarter turn and a three-quarter turn are the same two points on the same circle. **The finding: `socket_item` at Y = 0.280 floated 8 mm above the belt.** Measured off the shipped bytes, the rubber deck top was 0.250 and the slat top 0.272. Rather than move a socket other code would come to depend on, the slats went 22 mm to 30 mm so that 0.250 + 0.030 = 0.280 **is** the carrying surface. A second defect fell out of it: the end rollers top out at 0.275 and had been standing **3 mm proud of the old ride plane at every tile seam**, so items would have ticked over a bump at each cell boundary. **Spacing is not an art choice**: `factory_sim.h` has `kItemSpacing / kUnitsPerTile` = 64/256, so saturated items are 0.250 m apart, and therefore an item's extent along the flow axis must be <= 0.250 m. Twelve of fourteen already complied; `Item_FerritePlate` and `Item_FramePart` were 0.28 and nobody had noticed **because nothing had ever put an item on a belt**. New `tools/blender/check_belt_cargo.py` asserts all of it on the shipped bytes, with a `selftest` of **19 cases, 11 that must fire and 8 that must not**. New `Item_Crate` carries all twelve `ItemCategory::Buildable` items, because a 4 m foundation scaled to 0.24 m is a smudge. | DW-8 is binding and was honoured: belt motion stays the shader band, the items are the only per-item geometry, and there is no `AnimationMixer` anywhere near a belt. Also worth keeping: **the art lane checked that its arc and the client's existing belt SHADER describe the same curve** (fitted centre and r = 0.500000 to 2.9e-8 m against `MachineBatch.ts:191-205`) rather than assuming two curves that look alike are one curve. | **Done** | 2026-07-27 |
| BT-20 | **Armour, four slots, and the two decisions that make it an interface rather than four meshes.** `player/armour_set.glb`, 904 tris, 90,840 bytes, 5 materials. **Decision one: the node names are SLOT names, not SET names** (`Armour_Head_LOD0`, `Armour_Chest_LOD0`, `Armour_Legs_LOD0`, `Armour_Feet_LOD0`). A second armour set is a second `.glb` carrying the same four names, so slot lookup is set-independent, swapping a set is swapping a file, and nothing ever keys off a filename. **Decision two: each slot is a SKINNED mesh sharing the body's own 44-bone rig**, built from `rig_common.BODY_BONES` so there is one declaration and the rigs structurally cannot drift, rather than a pile of rigid props bone-parented one per bone. A leg plate has to bend at the knee and a rigid part cannot; the exporter emits one shared skin, so the client's equip is `mesh.bind(bodyRig.skeleton, mesh.bindMatrix)` and unequip is `mesh.removeFromParent()`. `contracts.json` lists all 44 bone names and `rest_pose: true`, so the two rigs agreeing is a checked property (measured 2.69e-07), and `render_armour.py` measures **rig drift 0.00e+00** at all nine tested poses. **Four defects, every one visible only in MOTION**, which is the whole reason the acceptance was "render it on the body, animated": the waist opened 57 mm in `Crouch_Idle` because the two sides of a seam did not share the bones they straddle; the knee cop floated at `Run:19` because the front of a bending knee is the OUTSIDE of the bend and no weighting can invent material, so the fix is slack and not tuning; thin 58 mm plates showed daylight along both long edges the moment a limb swung; and the nape lifted off the helmet because its centroid gave it a 55/45 Head/Neck split. **NO REFERENT IN `/core`**: `grep -riE 'armor|armour|equip' core/include` returns nothing, so this is BT-9's situation again and is escalated to Admin with the concrete header shape gameplay needs. | Reid asked for "armor and armor slots for head chest legs feet". Also worth keeping: **the subagent pushed back on an acceptance rule I wrote and was right.** I said a negative clearance minimum was fatal. It is not, for this asset: every negative vertex is the INNER face of a strapped-on plate deliberately buried in the limb, and a plate with zero penetration is a plate floating off the body. The number that matters is the mean standoff (22 to 36 mm) and the identity of the deepest vertex. A brief that states a bound should expect to be corrected by whoever has the geometry in front of them. | **Done** (art only; no sim referent, risk R7) | 2026-07-27 |
| BT-14 | **Blender stays at 5.0.1. Reid offered an upgrade and the right answer is no, tonight.** Reid gave explicit permission to "install it, upgrade it in any way you see fit". Blender **5.2 LTS shipped 2026-07-14**; this machine has **5.0.1** (build 2025-12-16) at `C:\Program Files\Blender Foundation\Blender 5.0\blender.exe`. Not upgrading, and the evidence is in the shipped bytes: every one of the 47 files carries `"generator": "Khronos glTF Blender I/O v5.0.21"` in its glTF asset block, so **the exporter version is written into the file and an upgrade rewrites every byte of every asset before a single vertex moves.** That turns the byte-identical rebuild proof from a property into a rebaseline, and it would do it while four build lanes are mid-flight against 5.0.1. Nothing is blocked by 5.0.1: the pipeline uses armatures, scripted skinning, `export_rest_position_armature`, `export_extras` and `ACTIONS` mode, all stable since 4.x, and the one historical failure (DW-7's bone-heat solver) is inherent to non-manifold input and no release fixes it. **What would justify the upgrade later:** a needed exporter feature, a 5.0 exporter bug, or a quiet week with no lanes running, and it should be done as its own commit that changes only bytes, with the 48-file rebuild diff as the evidence, so the version bump and an art change are never in one diff. | An upgrade is cheap to do and expensive to do at the wrong moment. The generator string makes "expensive" measurable rather than a feeling. | **Decided** | 2026-07-27 |
| BT-15 | **`tools/blender/contact_sheet.py`: a stdlib-only PNG tiler, so renders get looked at.** `render_check.py` writes one 420 x 540 PNG per shot and a character pass is thirty of them; nobody opens thirty files, which quietly turns a review into an artefact. This tiles them into one labelled sheet, taking each tile's caption from the input FILENAME because `render_check.py` already encodes clip and frame there, so the sheet carries which clip and which frame without anyone retyping it. **Stdlib only, and that is the point:** Pillow is installed in neither the system python nor Blender's, and `validate_glb.py` and `check_mating.py` already set the precedent that a gate runs as a plain `python3` step with no environment. So the PNG codec is written out: `zlib` does the hard part and the rest is the five scanline filters and a CRC. Scope is stated rather than discovered (8-bit non-interlaced grey/RGB/RGBA, which is everything Cycles emits) and anything else **raises rather than guessing**. Per DW-20 it has a `selftest` that demonstrates it can fail: a round trip, all five scanline filters decoding to identical pixels, and a 16-bit PNG and a non-PNG both REFUSED rather than silently misread. | Every art review in this project has ended with "look at the renders", and the friction of doing that has been growing with the asset count. This is the cheap fix, and it belongs next to the other two checkers rather than in a notebook. | **Done** | 2026-07-27 |

| BT-21 | **DW-35, the procedural PBR surface pass: TWO SHARED TILING SURFACES, NO ALBEDO, AND BLENDER IS NOT IN THE TEXTURE PATH AT ALL.** 37 of 48 shipped assets were flat colour on a PBR material with no maps. `tools/blender/texgen.py` now generates `panel` (512 px, 1 m tile: plate seams, rivets, bolt heads, a weld bead, scratches) and `coarse` (256 px, 0.5 m tile: chipped facets and grit), each as a tangent-space normal map plus an ORM pack, into `assets/textures/dist/` with a `surfaces.json` manifest. Both land at **512 px/m**, which is ASSET-SPECS 2.8's first-person target and twice its machine target. **Payload 0.59 MB, VRAM +3.33 MiB.** Three decisions carry the weight. **(1) No albedo map, for three independent reasons**: DW-35 ranks it last behind roughness; it is the only map that can silently move the palette, and the ask was polish and not restyle; and it would not survive the client anyway, because `MachineBatch` sets `vertexColors: true` and its `onBeforeCompile` writes `diffuseColor.rgb` AFTER `<map_fragment>`, so a base-colour map would be tinted by the baked vertex colour and then partly overwritten for four of five roles. Normal, roughness, metalness and AO all land earlier and are safe. Finding that out from the client BEFORE authoring is the whole return on surveying it first. **(2) ORM multiplies rather than replaces**, so the palette's thirty per-role constants survive and a map can only take a surface DOWN from them, which is the physically right direction (wear polishes metal, grime buries it). **(3) One heightfield per family and everything derived from it** - the normal is its gradient, the AO its local relief, the roughness and metalness masks functions of it - so the AO structurally cannot darken a seam the normal map did not dent. That is standing rule 1 applied to a texture. **Determinism is owned rather than hoped**: no RNG (a 32-bit integer hash), no transcendentals in the field synthesis (only `+ - * /` and `sqrt`, which is correctly rounded and bit-portable; DW-14 is this project's scar from a 1 ULP `std::tan`), and a PNG encoder that writes IHDR + IDAT + IEND and nothing else, so there is no `tIME`, no `tEXt` and no generator string to differ. zlib is pinned to explicit parameters and its version is recorded in the manifest as the one remaining external in the byte stream. **Two consecutive full rebuilds are byte-identical across all 48 GLBs, all 4 PNGs and the manifest.** `texgen.py check` asserts the shipped bytes: sha256 against the manifest, normal maps unit-length within the derived 8-bit quantisation bound of sqrt(3)/255, zero texels facing away, and **every channel's spread, with a CONSTANT channel required to be exactly 255 and to be declared in `ALLOWED_CONSTANT` with a reason** - because a constant metalness map at anything but identity silently rescales every material that wears it. 655,360 texels examined, 0 skipped. | Reid: "ultimately i want this to approach satisfactory level graphics... polished like that." DW-35 ordered post-processing first and textures second, and named this the long pole. The thing worth keeping: **the client survey came before the first pixel**, and it changed two decisions (no albedo, uniform UVs) that would each have been a full rebuild to undo. | **Done** | 2026-07-27 |
| BT-22 | **UVs are in METRES, they are on EVERY render primitive, and the uniformity is a correctness requirement rather than tidiness.** `of_lib.MeshBuilder.build()` box-projects each face along its dominant axis automatically; no build script authors a UV and `col_*` proxies are skipped because the client never renders them. **Why uniform and not "textured assets only":** the client merges an asset's primitives with `mergeGeometries(list, false)`, three returns `null` on a mismatched attribute set, and BOTH call sites swallow that with `?? list[0]` - so an asset with mixed UV coverage draws its FIRST primitive and silently discards the rest, with one console line as the only evidence. A partial rollout is far more dangerous than none, so untextured roles still carry UVs and the selectivity lives entirely in the role -> family table. **Why metres and not tile units:** a UV of 2.5 means 2.5 m, the consumer applies `texture.repeat = 1 / tile_m` from the manifest, and retuning texel density is therefore a JSON edit instead of a rebuild and rebaseline of 48 binaries. **Cost, measured and not estimated: +744,420 B, +22.9% on the models.** Skipping `_LOD1`/`_LOD2` recovers most of it and is explicitly NOT taken, because which LOD bands enter a `BatchedMesh` is client knowledge that is changing right now (RN-7 A-3/A-4) and a saving that turns into a silent one-primitive draw later is not a saving. No `TANGENT` is exported: three derives a tangent frame from screen-space derivatives when it is absent, which is correct for flat-shaded geometry and saves a further 16 B/vertex. **The validator gained `uv_present` and `uv_metres`, and the second one took three attempts, all three worth recording.** v1 asserted a band on `uv_area / world_area` derived from the dominant-axis bound `cos(theta) >= 1/sqrt(3)`: the derivation is right and it failed 20 assets, because a COLLAPSE decimator interpolates UVs and a non-planar quad triangulates away from its Newell normal. v2 measured the NORMAL MAP's two edge columns - and could not have failed, because `_normal_rgb` differences with wrapped indices, so the normal map is continuous across the wrap BY CONSTRUCTION whatever the height does. **The negative control caught it**, which is the only reason it is not still there. v3 asserts the exact property: a box-projected UV is a COPY of two position components, so after the Y-up conversion and the exporter's V flip every vertex UV must equal exactly one of three expressions. No tolerance, nothing to tune. Decimated LODs cannot satisfy an equality and are **reported by name and count as NOT EXAMINED**, still bounded by a convex-combination argument on the projected hull - and even that bound is stated honestly, since Blender's quadric-optimal vertex placement can leave the collapsed edge, measured worst overshoot **8.3e-5 UV metres on `Forest_FallenLog_LOD2`** against a 1e-3 slack and a defect scale of order 1. The worst overshoot prints on every run so it cannot drift into the slack unseen. Also added: `build()` now RAISES if `mesh.validate()` changed the face count, an assumption every per-face zip in the library has silently relied on since BT-9. | Standing rule 11, three times in one file, by the person who was quoting it. The general form: **a check derived from the right property can still be the wrong instrument, and the way to tell is a negative control.** | **Done** | 2026-07-27 |

| BT-23 | **The launch pad, rebuilt 8 x 8 m -> 24 x 24 m as the hero asset of the material pass.** Reid: "add a launch pad object where rockets are launched from. Make it big and make it look good", and he said big twice. Extended rather than replaced, because the original's shape (deck, clamps, tower, `socket_vessel`) was right and only its scale and its surface were wrong. 24 m is 6 cells of the DW-32 4 m module and **9x the ground area**. The decision that makes it read as launch infrastructure rather than as a large foundation is that **the deck is RAISED 2.00 m so the flame trench has somewhere to go**; a pad flush with the ground has nowhere to send the exhaust and looks like a car park. Four hold-down clamps on a 1.90 m circle sized for the DW-29a class L 2.50 m stack, a lattice service tower to 28 m with a swing arm, hazard striping, handrails, floodlight masts, a propellant tank and a blockhouse. **`socket_vessel` is the rollout anchor that replaces the flight lane's admitted stand-in of planting a rocket 26 m in front of the player**, and it MOVED from [0, 1.60, 0] to [0, 2.00, 0], measured off the shipped bytes along with `socket_clamp` [1.90, 2.00, 0] and the new `socket_umbilical`. Contract-first per BT-12: the whole `contracts.json` entry was rewritten by this controller BEFORE any geometry existed and the build agent was forbidden from editing it, so the tree sat at 47/48 with `launch_pad` red until the art caught up, exactly as BT-16 describes. Final: 2,576 of 2,600 LOD0 tris, 7 of 7 materials, 24.00 x 28.00 x 24.00 m to 0.005, four collision proxies including a `col_LaunchTrench` that does NOT bridge the trench, byte-identical over two consecutive builds. **The contact sheet earned itself again**: one of the nine renders, the human-scale deck shot, came back at a mean luminance of 8.1/255 against 54 to 87 for the others, i.e. a black frame from a camera inside geometry. It is invisible if you open renders one at a time and obvious in a tiled sheet, which is the whole BT-15 argument repeating. | The pad is where the two halves of this game meet, and it is the best possible showcase for the surface pass: a large industrial concrete-and-steel structure seen from the ground and from orbit, using both material families at once (`coarse` on the deck, `panel` on the tower and rails). Also a landmark for the map lane to mark. | **Done** (art + anchor only; placement, the `/core` buildable, the research gate and the save entry are the gameplay lane's, scoped in gameplay.md section 6) | 2026-07-27 |

| BT-26 | **Three probes were asserting on `voxels().mesh.faces`, a field the client stopped publishing at WG-24, and the interesting part is WHICH WAY it broke.** The greedy cube mesher became surface nets and `faces`, `quads` and `mergeRatio` went away; `VoxelMeshStats` publishes `triangles`, `vertices` and `bricks`. `tunnelpersist.js` read the dead name three times (dug, forgotten, restored) and compared them with `<` and `>=`. **Both comparisons on `undefined` are FALSE**, so this was not a silent pass, it was a **stuck red**: `tunnelSurvivesReload` could not go green however well the tunnel survived, and it printed `meshFaces: {}` because `JSON.stringify` drops undefined values, so the report showed no missing field either. Measured against the pre-fix file out of git: `tunnelSurvivesReload false`, `meshFaces {}`. **A stuck verdict is worth as little as a vacuous one and is worse to live with**, because a check that is always red trains everyone to explain it away, and this one had cover: the probe was already known red for `stillWalkable` (RN-6 logged it and disowned it), so a second red term beside it read as the same known problem. The check that proves a dug tunnel survives a save and a reload sat dead for weeks in plain sight. `voxelskin.js` and `digquality.js` had the same dead reads with the other failure mode, report-only, printing `drawn faces undefined, quads undefined` into a log line and dropping the keys out of the JSON report entirely. Repointed and given assertions with measured teeth: **tunnelpersist 774 triangles / 540 vertices / 8 bricks dug, 0 / 0 / 0 once the rock is back, 778 / 544 / 8 restored**; `goneTris === 0` is now EXACT and stricter than the pre-WG-24 `goneFaces < dugFaces` (1016 to 745), because /core's edit filter drops a brick holding no edit so putting the rock back leaves no near mesh at all. **The 4-triangle restored-minus-live drift is real and is the LIVE mesh being slightly stale**, not the restore inventing rock: a strike re-meshes /core's dirty region grown by one cell, a surface-nets cell reads density at corners it shares with the neighbouring brick, and a restore re-meshes over the wider brush boxes and picks those up. It is reported as `meshDrift` on every run rather than absorbed into a tolerance. **The negative controls are the deliverable, not a nicety**, and one of them found a second defect: voxelskin's control (the Q key never pressed, nothing levelled at all) reports `valid true`, `padMatchesGround true`, `skinIsEditsOnly true`, `aimReachesTheGround true`. **The probe could not tell a levelled pad from untouched ground**, and its colour check passes BETTER when nothing was built. Only the new `padHasADrawnSkin` catches it. Also new: `craterHasANearSkin` in digquality, which had `valid` and no other verdict at all. **General guard (`tools/smoke/run.mjs`): a one-line prelude wrapped around every probe defining `mustNum(obj, key, where)` and `mustHave`**, which throw on a field the client does not publish and name the keys it does; a throw rejects `page.evaluate`, which the runner already reports as a FAILURE with a non-zero exit. It lives in the runner because that is the one place all ninety probes go through (survival.mjs, lodsweep.mjs and writeshot.mjs all shell out to it), and it is kept to one line so probe line numbers in a stack trace do not move. Demonstrated: a copy of tunnelpersist asking for `faces` now dies with `probe: voxels().mesh.faces is undefined, not a finite number. Published keys: rebuilds, vertices, triangles, lastMs, boxes, bricks`. | Standing rule 11, and specifically its corollary that a probe reporting a number without asserting anything about it is a log line and not a test. The sharper generalisation this one adds: **the failure mode of a dead field read depends on the OPERATOR, so grepping for vacuous passes misses half the class.** `===` between two dead reads passes forever, `<` and `>=` fail forever, and both are equally dead. The only fix that covers both is to make reading the dead name itself an error, which is why the guard is a read-time throw and not an assertion helper. | **Done** | 2026-07-27 |

| BT-40 | **`web/tools/smoke/run.mjs` PRINTS every probe's verdict and never READS it, so no probe in this project has ever been able to fail the harness.** `run.mjs:436` evaluates the probe, `:445` copies the result into `report.eval`, and `:473-479` decides the exit code from console errors, failed requests and runner exceptions only. **Negative control, run against the isolated HEAD build and not argued from the source:** a probe whose entire body is `({ valid: false, fails: ['DELIBERATE FAILURE'] })` produced `smoke: PASS (no console errors, no failed requests)` and `EXIT=0`, with `{"valid":false,"fails":["DELIBERATE FAILURE"]}` sitting in the printed report. **Static census at HEAD: 284 probes, and ZERO of them convert a failed check into anything the runner can see** (none throws on failure; the `mustNum`/`mustHave` prelude from BT-26 throws only on a dead FIELD READ, which is why that one class became loud and the whole verdict class did not). **The verdict must key on both fields and that is measured, not assumed:** 35 probes carry a `fails[]` array and return a `valid` not derived from it (29 of those return a literal `valid: true`), and 8 more carry `fails[]` with no `valid` at all, so a `valid`-only gate stays green with failures in the report on 43 probes. **And the convention already exists everywhere except the chokepoint**: `survival.mjs`, `reload.mjs`, `powerreload.mjs`, `stationreload.mjs`, `twobody.mjs` and `worldjump.mjs` all exit non-zero on their own `fails`, and `survival.mjs` even states the distinction this needs in prose ("the run itself failed, which is a defect and not a verdict"). `run.mjs` is the outlier, and it is the one file all 284 probes go through. **THE SWEEP IS INCOMPLETE: 18 of 189 runnable probes measured before Reid needed the machine.** Of those 18: **4 RED, every one exiting 0 and printing `smoke: PASS`** (`apexec.js` 6 failures including an autopilot that missed an 800 km target by 50 km and spent half its planned delta-v; `post.js` 4; `assembler.js` `valid:false` "no belt off the smelter outlet"; `animgate.js` `ok:false`), 11 GREEN, and 2 that broke the run (`ascent.js`, `research.js`, both timing out past 420 s). **The sample is alphabetically biased (mostly `a*`) and is not a rate.** Method, so it can be resumed rather than redone: `git archive HEAD web assets/models/dist assets/textures/dist` into scratch (never the whole tree, `ue/` is 6.8 GB), `npm run build`, `vite preview --port 4262 --strictPort`, and `probeall.mjs` reading each probe's own header command. Nothing in `web/` was modified; `run.mjs`, `web/package.json` and `web/scripts/` are byte-identical to HEAD. | Admin's brief. The reason it had to be measured before being gated: turning the exit code on would take every green run in the repo red at once, and nobody knew by how much. | **Measured, 18 of 189; NOT LANDED. Resume the sweep when the machine is free.** | 2026-08-03 |
| BT-41 | **Proposed shape for the gate, held for Admin.** (1) `run.mjs` exit codes go four-valued: `0` clean and green, `1` THE RUN broke (unchanged meaning, so nothing that reads 1 today changes), `2` caller error (unchanged), **`3` the run was clean and THE PROBE FAILED**, with `fails[]` printed by name. The split matters because "the game is wrong" and "the instrument is wrong" have different owners. (2) Verdict rule `fails.length > 0 || valid !== true`, extended to `ok` and `pass` (see BT-43). (3) A probe with no verdict at all is NAMED and counted in the summary line, not silently passed. (4) **A named, dated allowlist that is TWO-SIDED or it will not shrink**: `known-red.json` records probe, failure count, base commit, owning domain and one line of reason; MORE failures than recorded fails, exactly the recorded count passes while printing `KNOWN RED (n of n, since <date>, owner <domain>)`, and **ZERO failures on a listed probe also fails**, "expected red and came back green, delist it in the commit that fixed it". An allowlist that can only suppress is a list nobody removes from. (5) `run.mjs --selftest` proving the gate can return 0, 3 and the no-verdict warning, on `boot.mjs`'s precedent, because otherwise this is a control whose arming step can fail silently. (6) `npm run check` stops being an `&&` chain and becomes `scripts/check-all.mjs`: every link runs unconditionally, a per-link PASS/FAIL table prints, exit 1 if any failed. (7) `check:limits` gets the same two-sided dated baseline as (4), so the cap is enforceable again today instead of after a 40-file refactor. **Known blast radius of adding exit 3:** `lodsweep.mjs` calls `run.mjs` through `execFileSync`, which THROWS on any non-zero status, and `survival.mjs`'s "BROKEN" message would become wrong for a probe-red; both need updating in the same commit. `airlesspair.mjs` ignores `r.status` entirely today, which is its own small finding. | Admin asked for the shape to be decided together rather than landed. The through-line of (4) and (7) is one idea applied to two instruments: a suppression list that fails when the suppression stops being needed. | **Proposed, awaiting Admin** | 2026-08-03 |
| BT-42 | **`npm run check` is an `&&` chain and its fifth link has been red for 407 commits, so its SIXTH LINK HAS NEVER RUN FOR ANYBODY.** The chain is `check:roles && check:proxies && typecheck && check:pose && check:limits && check:boot`. Measured at HEAD: roles PASS, proxies PASS (52 assets, 166 proxies, 190 sockets), typecheck PASS, pose PASS (14 checks), **limits FAIL with 40 files over the 400-line cap**, and `check:boot` therefore unreachable. Dated by walking history rather than asserted: `check:boot` was wired in at `929c2ef` (2026-07-27, BT-24), which is **415 commits** back; `check:limits` went red at `d0091c5` **407 commits** back, eleven commits later. **So the one gate in this project that proves the app STARTS ran for eleven commits and has been decoration ever since**, while the file-count backlog it hides behind grew 3 -> 12 -> 25 -> 30 -> 35 -> 39 -> 40. NUMBERS.md's own entry on this describes it at **29** files; it is 40 now, which is the decay rate. Also measured while in there: **only 2 of the 6 links can demonstrate they are able to fail.** `check:roles` has a 7-fixture self-test (6 must be refused) and `check:boot` has `--selftest` (3 servers that must fail, 1 that must pass). `check:proxies`, `check:limits` and `check:pose` have no arming check, and `typecheck` is the one INSTRUMENTS.md already records as silently degrading to no-information when any file fails to parse. | Admin routed this as the same class as BT-40, and it is: a verdict nothing acts on. The `&&` chain makes it worse than a dead gate, because the position of a link is a hidden dependency on every link before it, and each new gate's author verifies their gate in isolation. | **Measured; fix proposed in BT-41, not landed** | 2026-08-03 |
| BT-43 | **`tools/smoke` has 284 probes and no suite: there is no runner, no manifest, no CI, and every probe is invoked by hand from a command written in its own header comment.** 190 of the 284 carry that command; **94 do not**, and only 16 of those 94 are named by any driver `.mjs`, so 78 probes have no recorded way to run them at all. 79 of the 94 nevertheless RETURN a verdict, which nothing anywhere reads. The audit harness written for BT-40 (`probeall.mjs`, currently in scratch) is the first thing in this project that runs the probe set as a set: it extracts each header command, re-points `--url` at an isolated build, runs them, and applies the verdict rule `fails.length > 0 || valid !== true`. **Two harness defects were caught by its own output before any conclusion was drawn from it**, both of the class it exists to find. (1) `spawn(..., {shell:true})` split `C:\Program Files\nodejs\node.exe` at the space, so the first four probes recorded `NO_OUTPUT` for runs that had never started; a uniform verdict across four unrelated probes is the tell. (2) The verdict rule knew `valid` and `fails` and **not `ok`**, which seven probes (`animgate`, `padflat`, `zerog`, `machinesolid`, `orbitdeck`, `qolbuild3`, `qolplay`) use as their only verdict field, so they were being filed as NO_VERDICT and any red among them would have been dropped. The harness now records each probe's actual returned KEYS, so the NO_VERDICT bucket is checkable instead of asserted. **Also: probe header commands are written for a SHELL**, so `--evalargs='{"a":1}'` carries the shell's quotes; passed through `spawn` without a shell, `OF_ARGS` arrives as a string and every `OF_ARGS.foo` reads undefined, i.e. the probe measures the default while the record says it measured the request. That is run.mjs's own dropped-flag failure (RN-698) one layer out. 62 probes take `--evalargs`. | Admin asked for the list. There was no way to produce it, which is itself the finding: **a set of 284 instruments with no aggregate runner cannot have a state, only anecdotes.** | **Done** (harness); adoption is BT-41 | 2026-08-03 |
| BT-44 | **Process, paid for: `taskkill /F /IM chrome.exe /T` on a shared machine kills the USER'S browser, not just yours.** Cleaning up orphaned headless browsers between audit passes, this controller killed every `chrome.exe` on the box at 21:16. Every Chrome process alive afterwards has a start time later than that kill, so whatever Reid had open went with it. **Kill by property, never by image name:** `Get-CimInstance Win32_Process -Filter "Name='chrome.exe'" | Where-Object { $_.CommandLine -like '*--headless*' }` selects exactly the instances this domain launched, and the second pass killed 4 and left 8 untouched. | The isolated-tree discipline in NUMBERS.md protects the user's FILES from a measurement. It says nothing about the user's SESSION, and a measurement that needs a browser has to share one machine with a person using a browser. | **Done** | 2026-08-03 |
| BT-100 | **`dump_expected.cpp`'s raw stdout was never actually LF on Windows: fixed at the source, `_setmode(_fileno(stdout), _O_BINARY)`.** The Windows CRT opens stdout in TEXT mode by default, silently rewriting every `\n` this tool prints to `\r\n`. `build.ps1` has always papered over this downstream (it rewrites the fixture with an explicit LF join via `.NET`'s `UTF8Encoding` before writing `test/expected.json`), which is why the bug was invisible: the one path that ever produced the committed fixture already dodged it. Verified before the fix: `./dump_expected.exe > out.json` on this box produced CRLF on all 43 lines (`grep -c $'\r'` = 43), a byte-for-byte divergence from a Linux run that exists **before a single float is compared**. After the fix (`web/wasm/test/dump_expected.cpp`, guarded `#ifdef _WIN32`, no-op on Linux/mac where text mode already is binary mode): 0 CRLFs, and a direct run's substantive JSON now diffs against the committed fixture on exactly the same 4 fields BT-101 characterizes, nothing else. Two independent clean rebuilds on this box (`rm` the exe, reconfigure, rebuild) produced byte-identical output to each other. | The brief named "binary-mode writes" as one of the candidate causes and this was a real one, not a red herring: it is a second, independent, purely mechanical axis of non-reproducibility riding alongside BT-101's genuine float divergence, and it is the one actually fixable at this layer. | **Done** | 2026-08-14 |
| BT-101 | **The remaining `expected.json` divergence (contentHash/rawContentHash/heightHash/chunkHashes, first flagged BT-35 and independently reconfirmed by the `lane/disc-body` GP-541 finding in NUMBERS.md) is a genuine cross-toolchain 1-ULP libm rounding difference, chaotically amplified by multi-octave noise synthesis, and it is NOT fixable at this layer.** Root-caused rather than left as a hypothesis: built `dump_expected.exe` natively on this Windows box (WinLibs g++ 16.1.0, `-O2 -static`) and, over SSH, natively on Proxmox VM `claude-dev` (Ubuntu GNU g++ 13.3.0) from the identical commit (`d58733e5`, the ABI-26 settled rebuild that generated the committed fixture) — the VM build reproduces the committed `expected.json` byte-for-byte (`diff` exit 0), confirming the fixture's provenance is that VM/Linux/glibc build exactly as ADMIN.md's backlog item said, and isolating the whole divergence to Windows-vs-Linux rather than any run-to-run nondeterminism. Used the project's own `--diag` instrumentation (`of_diag_libm`, `diag_tan.bin`, `diag_scan.bin`, the same probes `web/wasm/test/diag.mjs` already uses for native-vs-WASM) to run the identical comparison native-vs-native for the first time: **`sin`/`cos`/`tan`/`exp` each differ by exactly 1 ULP on 1 of ~20-30 probed arguments** (`asin`/`acos`/`atan2`/`sqrt`/`floor`/`fabs`/`log`/`pow` all 0 ULP, all match); the real cube-sphere lattice `tan()` sweep (`cubed_sphere.h`'s `warp()`) differs on **74 of 3,598 lattice points (2.06%), worst case exactly 1 ULP**; and walking the streamer's real 48-quad terrain pipeline stage by stage shows the 1-ULP `tan` noise **amplifying through each further transcendental stage** (dir components picking up 3 ULP, latitude 3 ULP, temperature up to 26 ULP, moisture up to 14 ULP) until `rawHeight`/`designedHeight` differ by up to **29,750 / 47,813 ULP** at 4.2%/4.16% of the 52,272 sampled vertices — which is exactly why `posHash`/`nrmHash` (float32) and `h0`/`h544`/`h1088`/`seamOk`/`keyHash`/`chunkBiomes` all MATCH while `heightHash` (raw float64 bytes) does not: **the worst-case absolute magnitude, converted back from the two diverging bit patterns, is ~1.3e-12 to ~3.3e-12 METRES** — far below single-precision (float32) quantization and unmeasurable by anything downstream that isn't hashing raw double bits on purpose. This is the exact class `docs/web/WASM-BRIDGE.md` already names and accepts as **Tier B — cross-toolchain, transcendental-dependent (informational)** for native-vs-WASM (mingw-w64 vs emscripten/musl); this finding extends the same classification to native-vs-native (mingw-w64 vs glibc), because the mechanism is identical: `sin`/`cos`/`tan`/`exp` are not required to be correctly rounded by IEEE-754, and this codebase's own noise synthesis stacks enough of them (per the `latticeDir`/`warp` chain, one `tan` seeds every sampled direction) that a 1-ULP seed is not damped, it is amplified, the same lesson `texgen.py` (BT-21) already learned and fixed by banning transcendentals from ITS field synthesis. **Not fixed, on purpose**: making `generateQuadMesh`'s height synthesis transcendental-free the way `texgen.py`'s is would be a world-gen/core-engine noise-function rewrite, not a build-tooling fix, and would change the actual terrain, not just its reproducibility. **The honest conclusion, not a workaround**: `expected.json` cannot be byte-identical across WinLibs-Windows and glibc-Linux while `cubed_sphere.h::warp()` calls `std::tan`; the fixture's provenance is and must stay single-host (the VM/Linux `d58733e5` build already recorded as ground truth), the divergence is bounded at ~1e-11 m (physically and visually inert), and `parity.mjs`/anyone reading `expected.json` on Windows should expect exactly these 4 field families to differ and nothing else. | The brief's own instruction: isolate the term, measure its magnitude, and record the honest conclusion rather than paper over it. BT-35 flagged this as unrooted for four days; it is rooted now, with a number instead of a hunch. | **Done — root-caused, bounded, correctly left unfixed** | 2026-08-14 |

| BT-240 | **`tools/smoke/boot.mjs` drew port 6667 (ERR_UNSAFE_PORT) not because of anything in the port LIST, but because this box's Windows dynamic port range is wider than the ephemeral range other platforms restrict binds to.** `netsh int ipv4 show dynamicport tcp` reports start 1024, 64511 ports, i.e. **1024-65534**, not the IANA 49152-65535 range. `startStaticServer`/`tinyServer` both called `server.listen(0, '127.0.0.1', ...)` and trusted whatever the OS handed back; the OS does not consult Chrome's `kRestrictedPorts` (`net/base/port_util.cc`, fetched and confirmed verbatim), so nothing stopped it handing back one of the **17 restricted ports that fall inside this box's range**: 1719, 1720, 1723, 2049, 3659, 4045, 5060, 5061, 6000, 6566, 6665, 6666, **6667**, 6668, 6669, 6697, 10080. Fixed with `CHROME_UNSAFE_PORTS` (the full 78-entry list, port 0 included), `isUnsafePort`, `pickSafePort` (a bare retry loop over an injectable `bind()`, kept separate from any real socket so it is provable without one) and `listenEphemeral` (closes and re-listens on the same `http.Server` until a safe port comes back). `startStaticServer` and the selftest's `tinyServer` both route through it now: one picker, both call sites, BT-33's "one list, or the two tools drift" invariant applied to a second thing that used to be duplicated by copy-paste. **Proof, in `boot.mjs --selftest`, both PASS**: 300 real OS-assigned ephemeral binds asserted none unsafe; a forced-collision case scripts a fake `bind()` returning 6667 then 54321 and asserts the picker rejects 6667 (recorded in `rejectedPorts`), calls `bind()` exactly twice, and returns 54321. | A run dying on ERR_UNSAFE_PORT reads exactly like a real boot failure and burns an investigation on the wrong layer, precisely the class NUMBERS.md exists to catalogue. | **Done** | 2026-08-18 |

| BT-245 | **`docs/phase1/OVERNIGHT-LOG.md` carried a 31-line mojibake cluster from the 2026-08-03 repo-move commit (67b4857): its real UTF-8 bytes were decoded as cp1252 and the resulting wrong string re-encoded as UTF-8, a double encode that shows up as sequences like the file's own em dash rendering as four garbage characters.** Repaired with a new script, `tools/repair_mojibake.py`: scan every line for a fixed set of twelve non-ASCII characters that only occur in this file because of the double encode (`Â â Î £ ¦ § œ ’ “ ” † €`), and for any flagged line, `line.encode('cp1252').decode('utf-8')`, which reverses exactly the two steps that broke it. Lines with no marker pass through untouched, so the fix cannot reach content the corruption never touched. **Measured 31 affected lines, not the ~29 estimated at allocation.** `git diff --stat` shows one file, 31 insertions and 31 deletions, i.e. exactly the flagged lines and nothing beyond them. **Verified against the true original**: `git show 67b4857^:docs/phase1/OVERNIGHT-LOG.md` is the pre-move file; diffed against the repaired one, the whole 60-line file differs in exactly one line, and that line is not mojibake at all, it is the move commit's own deliberate path rewrite (`C:\Users\reida\Nextcloud\Kerbal Factorio\ue\...` to `D:\karbalfactorio\ue\...`) on line 8, correctly untouched by a transform keyed on encoding rather than content. Every one of the 31 repaired lines is byte-identical to the pre-move original. | A scripted, markers-only transform is the only way to prove a text repair changed exactly what it claims to and nothing else; hand retyping a corrupted line has no such proof. | **Done** | 2026-08-18 |
| BT-225 | **A probe with no documented invocation now FAILS A GATE at authoring time, `web/tools/smoke/check-invocations.mjs`, instead of being filed as `NO_DOCUMENTED_CMD` and quietly skipped forever.** This is the seventh instance of the class (BT-190's twenty prose-matched invocations, `orbitdeck.js`, `airlock.js`, now `vmlight.js`/`vmshade.js`/`vmcost.js` from the RN-1990 view-model light lane, RN-1990 to RN-1997), and another census would only have found an eighth later. The new check is a pure static scan (no browser, no build, under a second), reusing `probeall.mjs`'s own `extractCmd`/`PROSE_ONLY_INVOCATION` (already exported for `verify-extractcmd.mjs`) plus `excludedReason` (newly exported for this), not reimplemented. **Wired as `npm run check:probes` and inserted as the SECOND link of `npm run check`**, right after `check:roles`, not appended at the end: `check:limits` has been red for hundreds of commits (BT-42, still 54 files over cap today) and the `&&` chain never reaches anything after it, so a link appended at the end would have been exactly as decorative as `check:boot` was found to be. Own 4-fixture selftest, exits 2 if it fails, on `check-roles.mjs`'s precedent. The only exemption honoured is the census's own existing `PROBEALL-EXCLUDE`; no second, silent skip class was added, and exemptions print by name on every run rather than folding into a silent pass. **Proven both ways**: before the three probes' headers, exit 1 naming exactly `vmcost.js`, `vmlight.js`, `vmshade.js`; after, exit 0 across all 325 probe files (313 documented, 12 exempt). | Detail down, summary up: a census finds a defect once and a gate finds every future instance of it for free; BT-190/BT-155/BT-175 each spent a lane re-discovering this same class by hand. | **Done** | 2026-08-18 |
| BT-226 | **`vmlight.js`, `vmshade.js` and `vmcost.js` given real invocations chosen from each probe's own content (BT-192's method) and verified live, real D3D11 headless Chrome, a locally built/served tree.** `vmlight.js` and `vmshade.js` -> `--scenario=walk --lat=-19.85 --lon=-72.7853` (the RN-352 forest site RN-1993 measured cascade 0 actually carrying casters at; the default HOME site also passes today but with a third the margin, `casters:8` against `casters:24`). `vmshade.js` additionally needs `--evalargs='{"dot":0.7}'` (RN-1994's own sun dot) and, measured rather than assumed, needs the site AT BOOT: letting the probe's own `of.teleport` carry it there from the default site FAILED (`valid:false`, 0 of 81 scanned stations wrote a caster), most likely scatter density not yet built up around a teleported-to site inside the probe's settle budget. `vmcost.js` -> `--scenario=walk` alone, site-independent (`ViewModelLight.installVmLightDiag`'s own comment: `__ofVmLight` publishes unconditionally). **A finding surfaced while verifying, not fixed here and not this domain's to fix: `vmshade.js` is FLAKY at its own correct invocation.** Three isolated runs, identical flags, identical station counts (8/71, 9/70, 8/71 shaded/lit): `valid` true, false, true. Its own `> 2.5`-count drop threshold sits close to the wind/canopy noise its own header already names, the same badly-conditioned-threshold shape `terrainart.js` was found to have (`HARNESS-CENSUS-2026-08-16.md`'s amendment). Routed to rendering via `spawn_task` rather than fixed. | A probe that documents itself falsely (or not at all) has never actually been proven at the flags its own author believed it ran at, per every prior instance of this class. | **Done, one finding routed not fixed** | 2026-08-18 |
| BT-227 | **The cascade-0 shot-set gap moved from section 2.9a prose to a proper decision row, `RN-1993` in `rendering.md` section 3, matching the RN-1859 form.** Cascade 0 only ever receives props, never terrain, and none of the six canonical shots stands in a cast shadow, so a change to any cast-shadow-reception term can move a real in-game signal (`vmshade.js`: -9.87 counts, 19.9%, n=6 at a covered station) while every published shot pair reads unmoved. Section 2.9a's prose is trimmed to a four-line pointer at the new row; one source of truth. | A doc move, not new analysis, per the brief; the finding itself (RN-1993) was already landed by `lane/vm-light`, just never given a row of its own. | **Done** | 2026-08-18 |
| BT-255 | **`run.mjs --out` on `artframe.js`'s `station` shot was photographing the RN-352 forest at 1.62 m altitude, never the station, and the fix is a removal, verified rather than assumed safe.** RN-2030's verifier found it; this lane fixed it. The shot's own end-of-file comment said the walker was teleported back to the ground "for the same PH-89 reason: `run.mjs` settles AFTER the eval returns" (`window.__of.settle(n)`, gated on `terrain.report().converged`, hangs for a walker with no nearby terrain, which is why the shot's OWN pre-capture settle already uses a fixed `of.run` window instead of a gate). **That reasoning does not survive contact with the actual timing**: `run.mjs`'s post-eval `settle()` runs after boarding is long done, and riding the station carrier is a physics integration, not a re-teleport, so nothing re-targets the terrain streamer and its `converged` flag is still the one true from before boarding. Tested directly rather than trusted from the comment: with the teleport removed, `run.mjs --out station` still prints `smoke: PASS`, same order of wall-clock as before, no hang, no timeout. **Measured, real Chrome/D3D11 on this box, before -> after:** `--out` altitude 1.62 m (the forest) -> 399,868 m (the station, matching `eval.png`'s own 399,870 m to five figures); `pngdiff` between `--out` and the SAME run's `eval.png` 93.21% moved -> 5.21% moved, landing in the same modal band an untouched shot shows on its own (`forestfloor` eval-vs-out 6.10%, the ordinary settle-window drift NUMBERS.md's own trap catalogue already documents and accepts as inherent). **Unaffected-pose control**: `forestfloor` re-run before and after the fix (its code path was never touched) is unchanged, altM 1.62 m both times, before-vs-after pngdiff 0.21%, inside this project's own accepted run-to-run noise floor. **Audit of the other eight canonical poses (forestfloor, midfield, voxelface, machine, smelterhero, ruin, ruinwall, basedusk) found the defect nowhere else**: every per-shot `if (name === ...)` block in `artframe.js` sets the camera/scene once during the setup phase (before line ~1435, "the capture"), and the shared capture/settle/screenshot pipeline after that point never branches on shot name except for `station`'s (now read-only) diagnostics. `station` was the only shot with post-measurement scene mutation. **Searched docs/ for previously published station `--out` artifacts that might actually be the forest and found none**: the RN-2030..2038 census (rendering.md, NUMBERS.md, ADMIN.md) explicitly reads only `eval.png`, never `--out`; the artframe-tied PNGs referenced by name in rendering.md (`CE115_station_repro.png`, `RN1935_station_yo105.png`, `RN1409_shot5_station_before/after.png`) were opened and each shows genuine station content (a riveted interior corridor or the exterior hull against stars), none the forest. A one-line flag was still added to rendering.md section 7 for any `--out` capture dated before this fix that was not checked here. `npm run check` re-run after landing: 6 of 7, only the pre-existing `check:limits` red, unchanged. | RN-2030's verifier found the mechanism; Admin routed the fix here as BT-255 to BT-259 (build-tooling owns `run.mjs`/`artframe.js`). The comment's own stated reason for the workaround turned out to be checked against the wrong moment in the timeline (pre-boarding settle, not post-boarding settle), which is why it is worth recording that the fix was TESTED for the hang it was nominally protecting against, not just removed on the strength of "nothing downstream reads this". | **Done. BT-256 to BT-259 unused, abandoned per NUMBERS.md rule 4.** | 2026-08-19 |
| BT-250 | **`npm run check` is no longer an `&&` chain: `web/tools/check-all.mjs` runs all seven links (`check:roles`, `check:probes`, `check:proxies`, `typecheck`, `check:pose`, `check:limits`, `check:boot`) unconditionally, in the same order the chain used, and prints one PASS/FAIL summary table with a nonzero exit if any link failed.** This is BT-42's fix, landed: the fifth-of-old-six link, `check:limits`, has been red since BT-42 (still 54 files over the 400-line cap today, unrepaired on purpose, that debt is BT-42's own and not this lane's) and no longer blocks the links after it, which is exactly the problem `check:probes` (BT-225) had to fight position for. Each `check:*` npm script is untouched, byte-for-byte, and stays individually runnable; the runner only calls `npm run <name>` per link via node's `child_process.spawnSync(..., {shell:true})`, chosen because a bare `spawnSync('npm.cmd', ...)` throws `EINVAL` on Windows without a shell (a .cmd batch file needs an interpreter; `shell:true` supplies node's own cross-platform one, cmd.exe or /bin/sh, not a bash script of ours). Also added, as real functionality rather than a demo hack: `--only=name,name` to run a named subset, used to prove the green path without touching the known-red `check:limits`. **Proof, both directions, real runs on this box:** full `npm run check` -> `FAIL check:limits (0.7s, exit 1)` sandwiched between six PASS lines including `check:boot` running AFTER it (previously unreachable), overall `7 checks: 6 passed, 1 failed`, process exit **1**; `node tools/check-all.mjs --only=check:roles,check:probes,check:proxies,typecheck,check:pose,check:boot` (the six non-`check:limits` links) -> `6 checks: 6 passed, 0 failed`, process exit **0**. **Consumers checked**: no CI config exists anywhere in the repo (`.github/workflows` absent, and a repo-wide `*.yml`/`*.yaml` sweep found none outside build-tool cache dirs in other worktrees); every `npm run check` reference outside `web/package.json` itself is prose in a doc or a comment (STATUS.md, ROADMAP-W11.md, ASSET-SPECS.md, controller files, `check-roles.mjs`/`check-proxies.mjs`/`boot.mjs`/`check-invocations.mjs` comments) recording a past green/red state, not a script or tool that invokes the chain programmatically or depends on it stopping early. Nothing depended on the short-circuit. | Admin's brief, BT-250 to BT-254 allocated. BT-41's proposed shape (item 6) named this exact fix, `scripts/check-all.mjs`, an unconditional per-link table and exit 1 on any failure; built at `web/tools/check-all.mjs` instead (the brief's own suggested location) since `web/scripts/` is reserved for the individual `check:*` implementations and this is the orchestrator, one directory level up. | **Done. Used BT-250 only; BT-251 to BT-254 unused, abandoned per NUMBERS.md rule 4.** | 2026-08-18 |
| BT-265 | **`check-invocations.mjs` now gates header WELL-FORMEDNESS, not just presence, closing the exact gap BT-260 found and routed rather than fixed.** New `wellFormednessDefects(cmd)` re-tokenises the extracted command's flag section and demands every token be a real `--flag`/`--flag=value` (`probeall.mjs`'s own `FLAG_TOKEN_RE`, exported rather than re-derived, per the standing "one parser, or two drift" rule) or a documented benign marker this corpus already uses on purpose: a bare `#` (a trailing shell comment naming which site/case a flag combo covers, confirmed against `deepgate.js`/`goalmoot.js`/`lifeless.js`), a bare `\|` (a pipe into a second command, confirmed against `artframe.js`/`stationdraw.js`/`sunshot.js`), or a `[--flag=value]` bracket wrap (a documented-optional flag, confirmed against `map3d.js`). Every `--evalargs=` value additionally goes through `JSON.parse`. Each exemption was found by first running the check TOO strictly against the real corpus and reading what it flagged, not invented speculatively. **Proven both ways against the REAL file**: `shadowk.js` was temporarily edited back to its literal BT-260 pre-fix header (uncommitted, restored after) and the gate caught it, `MALFORMED_INVOCATION`, `"--evalargs value is not valid JSON"`; restored, clean again. Building the check found a NINTH invocation-header instance (BT-190's twenty, `orbitdeck.js`, `airlock.js`, the three `vm*.js` probes, `shadowk.js`, now this): `clamprestore.js` carried a stray literal `//` embedded mid-invocation (`--debug=1 //     --evalfile=...`), a merge artifact from collapsing two comment lines into one without stripping the second line's `//` prefix; fixed by restoring the missing continuation backslash. Corpus-wide: 326 probe files, 314 documented, 12 exempt, **0 offenders**. | BT-260's own report named the concrete shape ("attempt `flagsOf()` plus a `JSON.parse` of any `--evalargs` value, fail loudly on error") and routed it to Admin rather than building it inline, on the BT-225 precedent of finishing the diagnosis lane before opening a new one. | **Done** | 2026-08-19 |
| BT-270 | **Six more probes carried the exact GP-401/GP-609 throw-on-fail pattern qolbuild2.js was fixed at BT-260, converted identically; ~24 others that also `throw` were deliberately left alone because they are a different architecture.** Audit: every `throw`-bearing file in `probes/` (41) classified as either a genuine precondition guard (`window.__of`/a runtime handle missing -- correctly stays a throw, there is no verdict yet) or the deliberate `bail()`/`finish()` pattern (accumulate `fails[]`/`log[]`, then throw the whole thing). `keycollide.js`, `navdraw.js`, `qolbuild1.js`, `qolbuild3.js`, `qolhandsafe.js`, `qolsandbox.js` had the latter; converted to `finish()` returning `{fails, valid: fails.length===0, log}` (still `console.error`-ing every failure, so a standalone run keeps its own honest non-zero exit), and `bail()` recording its reason as a failure and returning through `finish()` instead of throwing it away. The other ~24 (`aniso.js`, `bandsplit.js`, `etchsplit.js`, `nightexp.js`, `ripplewalk.js`, ...) are a single linear `if (cond) throw` sequence with no accumulation at all; converting those to the returned-verdict convention is a materially larger, riskier rewrite per probe and was explicitly out of this brief's "small mechanical diffs, one pattern" scope. | The audit itself is the deliverable as much as the fix: a scope boundary stated and justified is worth more here than a larger diff that blurred two different probe architectures into one conversion. | **Done** | 2026-08-19 |
| BT-271 | **Every converted probe proven through the REAL `probeall.mjs` sweep path, both green and forced-red, against a real locally built and served tree** (not hand-typed, not asserted from reading the diff). All 6: real GREEN today, zero NO_OUTPUT. Each then temporarily forced to fail (an uncommitted `fails.push('DELIBERATE TEST FAILURE...')` right after `finish()`, reverted before commit) and re-swept: all 6 real RED with the injected failure named verbatim in the report, still zero NO_OUTPUT -- the report now survives a failure instead of collapsing into a crash-shaped verdict. Re-swept a third time after reverting: clean GREEN again, confirming no residue (`git diff` on the 6 files shows only the intended structural conversion). `probe-budgets.json` checked against the generator's own committed samples, not updated: every run across all three sweeps finished well inside its existing budget (0 OVER_BUDGET), so nothing materially changed. | DW-20's "a check that has never been seen to fail is a log line" standing rule, applied to a PROBE'S OWN conversion rather than to a gate: proving the GREEN path alone would have left the actual bug (a real failure silently eaten) unverified. | **Done** | 2026-08-19 |
| BT-272 | **`run.mjs`'s catch: decided NOT to synthesize a partial report, because none exists to synthesize; DID add the failing `stage` to the terminal error line, since that was a real and separately-justified gap.** `report` is built in one atomic `page.evaluate` call strictly AFTER the probe eval and settle stages, confirmed directly (not just read from the code) by running a synthetic always-throwing probe through the real `run.mjs`: stdout is empty, exactly as the ordering predicts. What already survives a throw was checked before deciding anything: `probeall.mjs`'s `runnerFails` (stderr, regex-extracted) and `stderrTail` (last 600 chars) already carried the thrown message before this change -- that mechanism is literally how BT-260 diagnosed both `shadowk.js` and `qolbuild2.js` despite the report being dropped. The one real, previously-missing piece: `stage` (already tracked for the alive-heartbeat, `smoke: alive Ns stage=...`) never reached the terminal failure record, so a reader could not tell "the app never booted" from "the probe's own check failed" from "the screenshot write crashed" -- all three read as the same bare message. Fixed with one line, additive: `runner: [stage=${stage}] ${e?.message ?? e}` (the unbroken `runner: ` prefix preserved on purpose -- `probeall.mjs`'s own `runnerFails` regex is anchored on that literal string, verified both by inspection and by grepping a synthetic probe's real stderr with the exact production regex). Committed separately from the probe conversions, per the brief's own instruction that a harness change carries NUMBERS.md's discipline on its own. | The brief asked for a decision, not an assumed fix; the "obvious" structural change (build a synthetic partial-report object in the catch) turned out to have no data to build FROM, which only became visible by tracing where `report` is actually assembled rather than by pattern-matching the bug description. | **Done** | 2026-08-19 |
| BT-275 | **THE LINE-CAP PILOT'S HEADLINE IS NOT THE THREE FILES, IT IS THAT THE TOP OF THE LIST DOES NOT SPLIT.** `check:limits` **54 -> 51**. Of the eight worst offenders, **five resist a behaviour-preserving cut** because essentially the whole file is the body of ONE thing and a function may not be carved mid-body: `TerrainMaterial.ts` 989 (`createTerrainMaterials` alone is 565; the ~363 lines of `*FromQuery` helpers above it ARE movable and still leave 615), `DebugGameplay.ts` 895 (`gameplayApi` is 850 of them, one `return {~50 methods}`), `TerrainShader.ts` 884 (134 lines of GLSL declarations plus ONE 690-line GLSL `main()`; the file's own documented extract-to-a-sibling mechanism, used four times already for `ATMOSPHERE_PARS`/`TERRAIN_ART_PARS`/`BAYER`/`CASCADE_GLSL`, reaches only ~750 and every further cut goes through `main()`'s body), `Boot.ts` 822 (`boot()` is 713, one order-dependent async construction sequence), and by the same shape `Gameplay.ts` 736 and `Scatter.ts` 715, one class each. **So the 51 remaining reds are not 51 more files of the kind this lane retired, and a plan that extrapolates the pilot's rate is wrong.** The ones that split cleanly have many top-level declarations; what is left needs a cap exemption, a rule that measures something other than raw lines, or a real decomposition that is not a refactor. That is an Admin call, and it is the pilot's actual finding. | The brief said to record why rather than force an arbitrary cut, and the reasons turned out to be one reason five times. | **Done** | 2026-08-19 |
| BT-276 | **THE SPLIT TEMPLATE, in four rules, from `TerrainArt.glsl.ts` 1117->156, `Surfaces.ts` 1047->365 and `SpaceStation.ts` 776->235.** (1) **Split along the STATE, not along reading order.** An ES module binding is read-only for an importer, so a `let` cannot move away from the code that reassigns it, while a `const` array/Map/object moves anywhere because callers only MUTATE it and the identity is one object. Every group here is drawn so each `let` travels with its single writer (`uvCopied` with `copyUv`, `manifest` with `ready`, `learned`/`spawns` with `learnStationProxies`), and the one cycle that would have formed (`stateOfDocked` needs `lastStationSolid` while `installStation` needs `findStation`) was avoided by leaving `stateOfDocked` at the top of the DAG rather than importing both ways. The original file stays the BARREL and re-exports every public symbol, so no import site outside the directory changes. (2) **Do the move with a SCRIPT over line ranges, never by hand**, then verify independently by re-extracting the same ranges from the pre-split file OUT OF GIT and requiring them verbatim: 1691 moved lines across the three, all verbatim, the only edits being 30 `export ` prefixes on declarations that now cross a boundary, each asserted by the script to match exactly once. (3) **Prefer a proof the pixels cannot give you.** For `TerrainArt.glsl.ts`, compiling the module before and after and hashing every export: 41 exports, surface sha256 `7afb40f1...` identical, `TERRAIN_ART_PARS` identical at 28894 characters. For a module that is GLSL text and constants that is stronger than any frame. (4) **Every pngdiff needs a same-build control taken in the SAME session.** `forestfloor` measured 0.67% cross-build against 0.64% same-build (BT-275) and **0.94% against 0.94%** (BT-276) -- that second one would have read as alarming alone, and the two diffs differ only in which run happened to be lighter; the `station` shot was 0.23%. In all three the shot's own numeric box decode was identical to the digit. | Reid's pilot brief. The template is the deliverable; the three files are the worked example. | **Done** | 2026-08-19 |
| BT-277 | **Probes, before and after, at their documented invocations**, against a dev server on 127.0.0.1:4291 owned by this worktree and sentinel-checked (BT-260's lesson, applied from the first command this time): `terrainspec.js`/`reliefgrad.js`/`reliefsweep.js` PASS->PASS with `survivedRatio 0.804` and `bestTexels 64` unmoved; `maps.js`/`machinefam.js`/`platetile.js` PASS->PASS with the surface report identical field for field (`uv copied 600 / synthesised 0 / countMismatch 0`, `vramMB 48`, `tableAgreesWithManifest true`, `mismatches []`, the same 23 material families in the same order, `suitplate tileM 0.4` with `tileOverridden false`); `stationpose.js`/`stationride.js`/`docking.js` PASS->PASS, `fails []`, every `angleDeg` still at the 1e-6 degree floor. **`terrainart.js` was deliberately NOT used as a gate**: the harness census records it as nondeterministic in isolation (GREEN/RED/GREEN/RED/RED over five runs), so a verdict from it would have been uninterpretable in either direction. | Rule 3 of the brief. Choosing which probes to trust is part of the proof, not a detail. | **Done** | 2026-08-19 |
| BT-278 | **THE ONE THING THAT BROKE, AND IT BROKE CORRECTLY: `check-roles.mjs` reads `ROLE_FAMILY` by TEXT-SCRAPING a hardcoded path**, so BT-276 moving that table to `SurfaceRoles.ts` turned the gate red with `Surfaces.ts: no const ROLE_FAMILY found`. Its own header names exactly this case -- "a regex that stopped matching, a path that moved, or a glob that found nothing now reads as a failure instead of as a clean sweep over an empty set" -- and it was **the only thing in the repo that noticed a file had moved**; `tsc`, the bundler and every probe were blind to it. Repointed at `SurfaceRoles.ts` (the path plus the 16 message and comment sites that all name where `ROLE_FAMILY` lives), the one sentence about `verifyAgainstManifest` de-named rather than repointed since that function is now in a third file and naming it would be a third place to keep in step; the stale prose pointer in `tools/blender/build_player_body.py` corrected alongside. Self-test still 7/7 (1 clean fixture passes, 6 must-refuse fixtures refused), positive control reports 56 .glb -> 54 shipped roles / 54 in surfaces.json / 54 in `ROLE_FAMILY`, so it is not passing vacuously. **STANDING WARNING FOR ANY FUTURE SPLIT: grep `web/scripts`, `web/tools` and the probe corpus for the file's PATH before moving anything out of it.** A text-scraping gate is invisible to the type checker, and this project has more than one. | The pilot's most transferable lesson, and an unplanned refusing-case for a gate that had never been seen to fail on the real repo. | **Done** | 2026-08-19 |

## 4. Architecture & approach
- **Two build targets:** (1) the **headless core lib + tests** — pure C++, CMake or minimal MSVC solution, no UE; each domain's Wave-0 core compiles here and is unit/bench-tested. (2) the **UE5 project** — created after the headless cores pass, links the core libs.
- **Gates → tests:** translate the spike gate lists (V/WV/RV/G-series) into automated tests; headless gates run in CI, in-engine gates run manually post-M2.1 (EXECUTION-PLAN §4).
- **Repo layout (proposed):** `/docs` (design), `/core` (headless libs + tests), `/ue` (the UE5 project), `/tools` (scripts/CI). Single repo, LFS for `/ue` binaries.

## 5. Interfaces & dependencies
**Depends on (inbound):** Reid's go-ahead to init git + install the toolchain; each domain's Wave-0 core source (they write it, this harness builds/tests it).
**Provides to (outbound):** a working build + test + CI environment for every domain; the headless harness API (how a domain registers its core's tests/benches); the repo + LFS.
**Cross-cutting:** like networking, this touches every domain — but as *infrastructure they build on*, not sim they depend on.

## 6. Task backlog / roadmap
- [Step 0] `git init` + `.gitignore` + LFS + first commit of `/docs` (BT-2 — **needs Reid's go-ahead**).
- [Pre-build] Stand up the headless C++ test harness skeleton (BT-1); pick the test framework; CI wiring.
- [Wave 0] Build/test the four headless cores with the domains: core-engine coord/rebase, world-gen crack-free terrain, physics Kepler+integrator, factory-sim 100k bench.
- [M2.1+] Scaffold the UE5 project; link cores; set up UE build CI + packaging.
- [Done 2026-07-26] Tier-0 base-building art set (BT-9). The **placement, grid-snap and ghost-preview code is another lane's**; this domain shipped the meshes, the module constants and the socket interface it codes against (ASSET-SPECS 4.23).
- [Done 2026-07-26] DW-27 packaging spike (BT-10). `electron/` + `docs/web/PACKAGING.md`. **Not a packaging pipeline**: no depot, no installer, no code signing, no update channel. Those start when Reid has a partner App ID.
- [Next, and it is the biggest single VRAM win left] **KTX2 for the surface maps.** ASSET-SPECS 2.8's trigger was 1 MB of texture payload and the payload is 0.59 MB, so the trigger is still unmet, but the maps are 3.33 MiB of the 4.04 MiB this pass added to VRAM and UASTC would take most of it. `gltf-transform` is not needed: these are standalone PNGs, so it is `toktx` plus `KTX2Loader` in `web/src/assets/Loaders.ts`, which already documents the hook. **Check its determinism before adopting it** - a transcoder is exactly the kind of tool that embeds a version string or threads its encoder, and the byte-identical rebuild is a gate.
- [Later, art] **`Rock` is doing two jobs and the texture pass exposed it.** It is the boulder stone AND the structural deck concrete. One `coarse` map has to be plausible as both, which is why the deck reads as stucco rather than as a cast slab. The honest fix is a `Concrete` palette role with its own family (form-board lines, expansion joints, occasional aggregate), which is closer to `panel` than to `coarse`. Deferred because it re-roles every structural asset.
- [Small, found 2026-07-27] **`render_check.py` silently drops the ground plane on `player_body.glb`.** Its `lowest` scan walks `bpy.data.objects`, which includes the helper icosphere the glTF importer parks in its `glTF_not_exported` collection. On the player that sphere reaches z = -1.0, so the `lowest > -0.05` test is false and `add_ground()` never runs: the character renders standing on nothing, in the review tool whose entire job is to show what the asset looks like. The sphere is excluded from the view layer so it never appears in frame, which is why this survived. Fix is to exclude objects in that collection from the scan. Not fixed on the spot only because renders were in flight against the file.
- [Later] Asset pipeline/cooking; perf-capture tooling for the render-wall + perf-budget gates.
- [Next repo task] **Add a remote** (LFS-capable host) and push; safe now that BT-7 landed. Until then the only off-machine copy is the BT-7 bundle.
- [Urgent, blocks every future VM-side lane push] **Register the VM's deploy key** `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICGPqIl9hunXmBYp0x1e9j4QvR+YB5R9gVilcf4Wijeb claude-dev-vm-orbitalfoundry` with write access on `reidaruss/kerbalfactorio` (BT-31). Needs either Reid running `gh repo deploy-key add --allow-write -R reidaruss/kerbalfactorio <file>` after `gh auth login`, or adding it by hand under the repo's Settings → Deploy keys. Until then every commit made on the VM clone is local-only.
- [Blocking asset-pipeline work on the VM] **Blender on `claude-dev` is 4.0.2 (apt); the pipeline is pinned to 5.0.1 (BT-14, BT-36).** Source 5.0.1 for Linux (tarball from blender.org, not apt) before running any `tools/blender/*.py` build/validate/render script there, or get Reid's sign-off to move the pin.
- [Closed 2026-08-14, BT-100/BT-101] ~~`web/wasm/test/dump_expected.cpp` produces different content/chunk hashes (not different heights) between WinLibs g++ and the VM's GNU 13.3.0 g++ (BT-35).~~ Root-caused: a genuine 1-ULP `sin`/`cos`/`tan`/`exp` libm rounding difference, amplified by the noise pipeline to a worst-case ~1.3e-12 to 3.3e-12 m height delta, invisible to float32 but visible to a raw-double-bits hash. Same class WASM-BRIDGE.md already accepts as Tier B for native-vs-WASM, now confirmed for native-vs-native too. Not fixable without banning transcendentals from `generateQuadMesh`'s height synthesis (a world-gen noise rewrite, texgen.py's BT-21 precedent, out of this domain's charter) — recorded as an honest bound, not chased further. A real, separate, and now-fixed defect was found alongside it: the tool's raw stdout was CRLF on Windows outside of `build.ps1`'s own LF rewrite (BT-100).
- [Deferred] Wire or delete the three gitignored Fab packs (B3D ~71 MB, WaterMaterials ~88 MB, Megaplant_Library ~694 MB skeletal-only); remove from `.gitignore` when wired.

## 7. Open questions & risks
- **R1:** does the user's machine have the VS2022/MSVC + UE5 toolchain installed? (Verify before the build greenlight.)
- **R2:** sharing C++ code between a headless lib and a UE module cleanly (UE's build system vs plain CMake) — keep cores as engine-agnostic libs.
- **R3:** Nextcloud + git in the same folder can fight (sync vs `.git`); confirm the repo location/ignore rules.
- **R5 (opened 2026-07-26, BT-10) and it is not this domain's to fix:** the factory render batch is a fixed 256 instances (`web/src/game/MachineBatch.ts:26`), reached at about **150 machines**, and exceeding it is **silent**: the machines simulate, produce and persist, and are never drawn, with draw calls, triangles, frame time and every budget indicator unchanged. Escalated to Admin for the **rendering** domain (give the batch a growth path) and for **gameplay/UI** (an instance-pool exhaustion line in the HUD budget, next to draw calls). Until it is fixed, no performance claim about a large factory means anything, including the reassuring ones in this spike. Re-running `electron/probes/scale.js` unchanged is the acceptance.
- **R6 (opened 2026-07-26, BT-11) and it is not this domain's to fix: the 4 m rescale needs a CLIENT pass, and one number in it is load-bearing.** `StructureGrid.measureModule` reads the module off the shipped sockets, so most of `web/src/game` follows for free. These do not, in order of consequence.
  1. **`Structures.ts:77` `FLOAT_TOLERANCE_M = 0.55` must be RE-MEASURED, not scaled.** It was set against a **1 m** footprint whose worst measured corner spread on a slope was 0.118 and 0.127 m, comfortably inside a bound whose real driver was the levelling tool's half-voxel dead band. The same slope across a **4 m** footprint spreads roughly four times as far, so the terrain term alone now consumes the whole allowance and an ordinary slope becomes unbuildable, which closes off DW-24's own teaching loop. Re-run `probes/buildtol.js` with the 4 m footprint and re-derive. **This is the one that will look like "foundations stopped working" if it is missed.**
  2. **`Structures.ts:110` module fallback** `{ cellM: 1, deckH: 0.5, wallH: 2.5, wallT: 0.25, storey: 3 }` -> `{ cellM: 4, deckH: 0.5, wallH: 3.5, wallT: 0.25, storey: 4 }`. This is the ONE place a stale 1 m module can survive silently, because everything else measures.
  3. **`StructureGrid.ts:43` `SITE_REACH_M = 32`** is now only 8 cells across a site where it was 32. Suggest 64 (16 cells); tangent-plane departure from a 600 km sphere at 64 m is 3.4 mm, still far below anything visible. **Shared with `MachinePlacement.ts`**, where BT-10 measured it bounding one adopted site at 323 machines, so raising it moves the factory scale test too.
  4. **`StructurePlacement.ts:30` `REACH_M = 12.0`** is three cells at 4 m: you cannot reach the far edge of the cell you are aiming at from inside a room. Suggest 20 to 24, and `FALLBACK_M 3.0` -> 6.0.
  5. Comments that are now wrong: `StructureGrid.ts:45` (`MAX_LEVEL = 3`, "Four is 12 m" is now 16 m; the value can stand), `StructureGrid.ts:10-11` (cites a 1.00 m mesh and a 0.41 m lattice gap; the argument gets STRONGER at 4 m, the numbers are stale), `Structures.ts:52` (quotes the 1 m `buildtol` figures).
  6. Unchanged and worth confirming rather than editing: `BURY_TOLERANCE_FALLBACK_M = 0.50` still equals `DECK_H`, and `swingSecs 0.4167` still matches the shipped clip.
  7. **New, does not exist yet:** nothing in the client places a pillar or knows what an unsupported overhang is. `pillar.glb` should be measured the way `measureModule` measures the module (`PillarFoot/socket_top`, `PillarHead/socket_deck`), never retyped. Whether a pillar is a `StructureKind` at all is a placement-lane design call, since it is auto-generated under an overhang rather than player-placed.
  8. **Costs live in `/core`** (`survival::StructureKind` via `_of_gp_structure_info`), so they are a gameplay call: one foundation now covers **sixteen times** the ground it did, and an unchanged price divides the cost of a base by 16. `Demolition.ts` refunds follow.
- **R7 (opened 2026-07-27, BT-20) and it is not this domain's to fix: armour has NO referent in `/core` at all.** `grep -riE 'armor|armour|equip' core/include` returns nothing, and `web/src` has no equipment concept either: `slot` there means an inventory slot, a hotbar slot or a batch instance index. The meshes ship and validate and nothing can wear one. The concrete shape gameplay needs: `enum class EquipSlot : uint8_t { Head, Chest, Legs, Feet, Count }` with the four values matching the four node names exactly, so the client's lookup is an array index and never a string built at runtime; `ItemCategory::Armour` added to the existing enum; `ItemDef` gaining `EquipSlot slot` plus a stat block for armour rows with `stackMax = 1`; a `std::array<ItemStack, EquipSlot::Count>` on the player with `equip`/`unequip` returning the displaced stack; four new `ItemId`s in the pinned append-only block; and a `setId` so the renderer knows which `.glb` to load. Serialisation is 4 slots x 4 B = 16 B, which the save schema needs a version bump for. Escalated to Admin for **gameplay** and **persistence**. Separately and smaller: **an armoured player still sees unarmoured first-person arms**, because `armour_set` carries the third-person 44-bone rig and the view model is a different 27-bone rig with a different bind pose. Either a second armour file against `FP_BONES` or a decision to accept it.
- **R8 (opened 2026-07-27, BT-21/BT-22) and it is not this domain's to fix: THE MAPS AND THE UVs ARE INERT UNTIL THE CLIENT CHANGES.** The 48 `.glb` files carry UVs, the maps and manifest ship and are served, every gate is green, and **nothing on screen changes**, because all three of the client's `normalize()` functions build a fresh `BufferGeometry` from an attribute allowlist that does not include `uv`, and no material anywhere is given a map. The full hand-off is written up in ASSET-SPECS 2.9; the short form is three changes. **(1)** Copy `uv` through `MachineBatch.ts:71-98`, `NodeBatch.ts:96-120` and `PropLibrary.ts:63-83` - **unconditionally**, never behind `if (src.getAttribute('uv'))`, because a conditional reintroduces the mixed-attribute merge that returns `null` and degrades to one drawn primitive. **(2)** Load the two map sets once, share them, and assign `normalMap` / `roughnessMap` / `metalnessMap` / `aoMap` at `MachineBatch.ts:163-166`, `NodeBatch.ts:225-233` and `PropLibrary.ts:140`, with `RepeatWrapping`, `repeat = 1 / tile_m` from the manifest, the default `NoColorSpace`, and **`aoMap.channel = 0`**, which is the line that will be forgotten because three defaults `aoMap` to a `uv1` that does not exist and the failure looks like a slightly wrong constant tint. **(3)** Pick the family per material from `surfaces.json`. **Two batch paths cannot express a per-role family and one of them is a real defect if ignored:** `NodeBatch` buckets into two materials by `metalness > 0.5`, so Bark, Rock, **Leaf and Grass** all land in `nodes:matte` - and Leaf/Grass are in `flat_roles` and must get no map, because a rock normal map on a foliage card is worse than none. Bucketing by *(family, metalness)* fixes it at a cost of at most 4 materials against a 53-of-150 draw budget. `MachineBatch` merges everything onto one material, where `Rubber` and `Rock` are `coarse` among otherwise `panel` roles; take `panel` for the whole batch first and measure whether anyone notices, since the alternative (select on the `aRole` vertex integer that already exists, inside the `onBeforeCompile` that already exists) is easy to add later, costs one texture fetch and adds **no** new custom shader against the DW-10 cap. Escalated to Admin for **rendering**. Acceptance in this project's own style: same scene and camera captured with the maps attached and detached behind `?maps=0` (standing rule 7) and differenced; a before identical to the after means the UVs never arrived.
- **R4 (opened 2026-07-26, BT-9):** the four structural parts are the first Tier-0 assets with **no referent in the headless headers**. `automation.h`'s `BuildKind` has no `Foundation`/`Wall`/`Floor`/`Door`, `gameplay.h` has no structural items, and there are no `TypeId`s. The meshes ship and validate, but nothing can place one until **factory-sim** adds the `BuildKind` values and `TypeId`s and **gameplay** adds item ids and recipes. Also unanswered and not an art call: does a foundation deform terrain (world-gen/physics voxel interaction) or merely rest on it? Art has published geometry and anchors only. Escalated to Admin.

- **R9 (opened 2026-08-11, BT-60), CLOSED as it was written, recorded because the wrong answer was already in circulation:** BT-38 escalated "texture decode + `CONTEXT_LOST_WEBGL` under SwiftShader" to **rendering/world-gen** as a possible Linux/SwiftShader or Chrome-151 defect. **It was neither.** It was Git LFS pointer files in this domain's own checkout (BT-60). Any lane that read BT-38 and started looking at the renderer was sent the wrong way; the sweep would have burned a night on it. **Every clone and worktree on the VM needs `git lfs pull` once**, and the `.gitattributes` fix must reach `main` before the sweep, or a fresh worktree cut from `main` reproduces the whole thing.
- **R10 (opened 2026-08-11, BT-63) and it is not this domain's to fix: the client holds TWO WebGL contexts at boot, and the second one is what makes SwiftShader drop the first.** `web/src/game/ItemIcons.ts:131` creates its own `THREE.WebGLRenderer` to bake inventory icons and disposes it when done, alongside the main renderer at `web/src/render/Renderer.ts:92`. On this VM the main context is lost at ~0.9 s and restored at ~2.6 s on 3 runs in 4, and that window is exactly the bake. The harness now tolerates that specific shape (BT-63) and it is measurably benign for pixels, so **this is a robustness question, not a correctness one, and nothing is blocked on it**. If **rendering** wants it gone, the fix is to bake the icons on the main renderer into a render target rather than standing up a second context. Do not "fix" it by widening the harness tolerance.

## 8. Subagent delegation log
| Date | Subagent task | Status | Outcome |
|------|---------------|--------|---------|
| 2026-08-19 | **BT-275 recon A: read-only splittability assessment of the five worst line-cap offenders** (`Surfaces.ts`, `TerrainMaterial.ts`, `DebugGameplay.ts`, `TerrainShader.ts`, `Boot.ts`), asked for the largest indivisible unit, the movable cohesive units with line spans, the arithmetic, and a FEASIBLE/RESISTS verdict. Explicitly barred from editing, writing or committing. | Done, one verdict overruled | Confirmed RESISTS for `TerrainMaterial.ts`, `DebugGameplay.ts` and `Boot.ts` with the line arithmetic. Its most valuable finding was on `Surfaces.ts`, which I had provisionally written off: only `manifest` and the three `uv*` counters are genuinely anchored by `let`-reassignment, and every other piece of module state is a `const` container that is only ever mutated, so it is movable. That is what turned the second split from "skip it" into the 1047 -> 365 that landed. **Its `TerrainShader.ts` FEASIBLE verdict was overruled after checking the file directly**: the route it proposed (extract "one or two `main()` sections") carves text out of a 690-line GLSL `main()`, which is the mid-body split the brief forbids; the declarations block alone reaches only ~750. A subagent verdict that depends on a rule interpretation has to be re-derived against the rule, not taken. |
| 2026-08-19 | **BT-275 recon B: read-only probe reconnaissance**: which probes exercise `TerrainArt.glsl.ts`, `SpaceStation.ts` and `StructurePlacement.ts`, their verbatim documented invocations, the canonical `artframe.js` shots, the `pngdiff` invocation, and whether `run.mjs` boots its own server. Explicitly barred from editing. | Done | Returned the exact invocations used for every before/after pair in this lane, and two things that changed how the proof was run: `run.mjs` does NOT start a server (so the lane started and sentinel-checked its own on 4291), and **`terrainart.js` is recorded in the harness census as nondeterministic in isolation** (GREEN/RED/GREEN/RED/RED over five runs), so it was deliberately excluded as a gate. Reconnaissance that tells you which instrument NOT to trust is worth as much as the invocations. |
| 2026-08-16 | `lane/beltcargo-flake` (gameplay-owned, cross-referenced here because the cause is **this domain's probe clock again**, one layer under FS-101, and 86 probe files plus 3 `tools/smoke/*.mjs` harnesses inherit the fix). | Done, `web/src/app/Loop.ts` changed | **FS-101 TOOK THE WALL CLOCK OUT OF THE ACCUMULATOR AND LEFT IT IN THE CLOCK.** `Loop.run` still seeded a synthetic timeline at `performance.now()` and had `frame` recover each step as `(now - lastMs) / 1000`, a subtraction of two large floats whose error is the ULP of the wall clock. At `renderHz = 60` the recovered dt equals `FIXED_DT` to within exactly that error, so `acc >= FIXED_DT` is decided by it. **Measured, `of.run(1.0, 60)` as the first driven call on a fresh page (`drivenAcc` provably 0): quiet 60, 60, 60, 60, 60 ticks; with all twelve logical cores pegged at 100%, 60, 59, 60, 59, 60.** The two short runs are exactly the two whose `performance.now()` was largest (35645.5 and 39737.5 ms against 21678.6 to 28380.8), and replaying those six measured seeds off-line in node reproduces the six browser tick counts exactly. **Fix:** `frame(now)` is now the live rAF clock read and nothing else; the work lives in `step(dt)`, which both paths call; `run` passes `1 / renderHz` directly (bit-identical to the old `dtMs / 1000` at every rate checked). **After: 60 ticks 5 of 5 quiet AND 5 of 5 loaded, 21-call totals at the exact want 1260 in all ten (1259 in 8 of 10 before); `walkdet.js` 989 ticks with bit-identical f64 feet quiet and loaded; `beltcargo.js` green 3 quiet and 3 loaded; `assembler.js` and `factoryshot.js` unregressed.** Note for this domain's own gate work: the sim tick a probe INHERITS before it drives anything still varies with wall clock (measured 83 to 139 across ten sessions on one seed and build), which is untouched by this fix. Full numbers in `docs/web/NUMBERS.md`'s GP-1010 to GP-1024 row. |
| 2026-08-15 | `lane/builddrag-determinism` (factory-sim-owned, cross-referenced here because the cause turned out to be **this domain's probe clock**, not the belt code it was sent to look at). | Done, `web/src/app/Loop.ts` changed | **`of.run(seconds)` WAS NOT A FUNCTION OF ITS OWN ARGUMENTS, and every driven probe in the tree inherits the fix.** `Loop.run` advanced the LIVE accumulator, whose value on entry is the residue of real rAF frames timed by `performance.now()`, so the number of fixed ticks a scripted run yielded was a wall-clock coin flip. Measured by the new `probes/walkdet.js` (FS-100), which strips out every data-dependent thing -- one fixed tape, one fixed run, no aim search, no adaptive walk, no placement: **three same-seed runs advanced 480, 480 and 479 ticks for the identical `of.run(4)`, with the walker's f64 feet bit-identical at equal tick counts and 0.0765 m apart at unequal ones, exactly one tick of a 4.6 m/s walk.** Downstream that one tick moved the build ghost's aim point across 1.000 m cell boundaries at different moments and `probes/assembler.js` laid anywhere from 46 to 64 belt tiles for one gesture over one deterministic 47.8 m walk (GP-836). **The fix is a swap, not a reset** (FS-101): a `drivenAcc` starting at zero and carried from one driven run to the next takes the live accumulator's place for the duration of `run()`. `Loop.run`'s own comment already argued correctly against ZEROING the accumulator (it snaps alpha to 0 and a probe slicing `run()` in pieces would measure its own slicing as jitter); every word of that still holds, because the value carried across driven runs is now the previous driven run's own residue rather than the wall clock's. After: `walkdet.js` 989/989/989 ticks with bit-identical feet, 3/3. **What this changes for existing probes:** a driven run may now land one tick either side of where it used to, once, and then stays there for ever. `assembler.js`, `beltcargo.js` and `factoryshot.js` were re-run and are unregressed or better (`assembler.js`'s stone haul converges 5 of 5 where GP-836 measured roughly 1 in 5). Full numbers in `docs/web/NUMBERS.md`'s FS-99 to FS-113 row and `docs/controllers/factory-sim.md` §8. |
| 2026-08-10 | **Admin's brief: provision Proxmox VM `claude-dev` (10.10.10.36) as the implementation node (D-017)** — toolchain, GitHub access, both builds, ctest, LAN-bound serve as a systemd unit, one smoke probe, Claude Code verification. Worked entirely over ssh/scp from the local machine per the brief's hard constraint (nothing heavy runs locally). | Done, one item blocked outside this session's authority | BT-30 to BT-39. **Landed and verified on the VM**: full toolchain, emsdk 6.0.4 matched to the local machine, anonymous HTTPS clone (HEAD exact match to local/origin), `web/wasm/build.sh` (new, ports `build.ps1`), `web` build green end to end, **41/41 ctest green** (not 21 — the controller banner was stale), systemd user unit `orbital-preview.service` serving `0.0.0.0:4200` verified 200 from the local machine, one `boot.mjs` probe run (55.2 s, stack works, real FAIL on texture decode + WebGL context loss), `~/.local/bin/claude -p` verified (not on PATH for non-interactive ssh). **Blocked**: registering the VM's deploy key for push access was refused by the calling session's own permission classifier as an account/repo-config action needing Reid's explicit go-ahead, so it was left as a named, unregistered public key rather than forced. **Two premises the brief flagged turned out wrong in ways that mattered**: the repo is public (unblocked read immediately, no bundle/scp fallback needed) and apt's Blender (4.0.2) does not match the pinned 5.0.1, which would have silently rewritten every shipped asset's exporter identity if any asset script had been run there. |
| 2026-08-03 | **BT-40 blast radius: map every probe basename to the places in `docs/` that cite it as EVIDENCE**, so a red probe can be priced against the landed claims that rest on it. Read-only, explicitly barred from editing, from git writes, from a browser, a dev server, a build and from running any probe, because the machine was in use. | Done | **228 of 284 probes are cited in docs; 56 are cited nowhere** (dominated by `*shot.js` art probes, the `level*` family and `qolflight1-3`, so a red there supports no landed claim). Its most useful half was unasked-for: it found the docs' own list of probes ALREADY RECORDED AS RED AT HEAD (`research`, `ascent`, `flightabuse`, `stationvisit`, `zerog`, `mapplanner`, `discovery`, `tunnelsink`, `tunnelmouth`, `basereal`, `controls`), which is the seed of BT-41's allowlist and means several reds the sweep finds will be known rather than new. It also found the probes named as THE acceptance for a decision (`autoline`, `demolish`, `tunnelpersist`, `deposit`, `build`, `controls`, `moments`, `tunnellit`, `carrier` 43/43 for CE-37/CE-84, `stationride` 21/21 for CE-86, `airlock` for PH-111, `buildtol` for the DW-24 tolerance constant, `persist`+`tunnelpersist` for the PACKAGING gate) - those are the highest blast radius. And it caught two doc-only names with no probe file behind them, `probes/scale.js` (it is `electron/probes/scale.js`, outside the 284) and a `probes/x.js` placeholder in physics.md, which would otherwise have inflated the denominator |
| 2026-07-27 | **BT-26 sweep: enumerate what `window.__of` actually publishes (chasing spreads through to the declaring interfaces) and diff every one of the ~90 probes against it**, classifying each dead read as in-an-assertion, a comparison of two possibly-undefined values, or report-only, plus a list of probes that return no boolean verdict at all. Read-only, explicitly barred from editing anything and from running a browser or a dev server, because five other lanes are live in this checkout. | Done | Confirmed the `mesh.faces` family and swept for the rest of the class. Scoped read-only on purpose: the sweep is a diff between two moving surfaces and an agent that can edit will fix a symptom in `src/` to make a probe compile, which is the wrong direction (the probes were wrong, the source was fine) |
| 2026-07-27 | **DW-35 recon: map the client's material, texture and UV surface BEFORE a single pixel was authored.** Read-only over `web/src`, explicitly barred from editing. Asked for call sites, whether materials survive load, whether they are shared, how `BatchedMesh` gets multi-material geometry, and what would happen to a mixed has-UV/no-UV set. | Done | **The single highest-value hour of the night, and it changed two decisions that would each have cost a full rebuild to undo.** It found that `MachineBatch` DISCARDS per-file materials (colour to a vertex attribute, name to an integer role), which is why the maps ship as standalone PNGs rather than embedded; that `vertexColors: true` plus an `onBeforeCompile` writing `diffuseColor.rgb` after `<map_fragment>` would have eaten a base-colour map, which is the third independent reason there is no albedo; and that `mergeGeometries(list, false)` returns `null` on mismatched attributes while BOTH call sites swallow it with `?? list[0]`, which is what makes uniform UV coverage a correctness requirement rather than tidiness. It also volunteered the trap unprompted: the obvious `if (src.getAttribute('uv'))` conditional is the thing that would break it |
| 2026-07-27 | **DW-35 renders: wire `surface_preview` into `render_check.py` behind a `--maps` / `--nomaps` flag and produce before/after contact sheets.** Owned `render_check.py` (additive only) and `docs/screenshots/W12_mat_*`; barred from `texgen.py`, `of_lib.py`, `assets/` and from the concurrently-running launch pad lane's files. Explicitly told to JUDGE the result and to name where it looks worse, and explicitly forbidden from changing `texgen.py` so that the byte-identical rebaseline stayed under my control. | Done | Four new camera views sized to the assets, `--nomaps` correctly distinguished from omitting the flag (it strips maps and rewrites the constants, which is what makes the two halves comparable). The sheets are what turned three suspicions into decisions |
| 2026-07-27 | **BT-23, the launch pad hero asset**, against a contract rewritten before any geometry existed. Owned `build_launch_pad.py` and a new `render_launch_pad.py`; `rocket_common.py` additive-only with `rocket_parts.glb` and `lander_landed.glb` as witness assets; explicitly barred from `contracts.json`, `of_lib.py`, `texgen.py`, `surface_preview.py`, `validate_glb.py` and `render_check.py`, the last of which a second agent was editing concurrently. | Done | 24 x 24 x 28 m, 2,576/2,600 LOD0 tris, 257,084 B, byte-identical, five sockets measured off the shipped bytes. Nine renders and a sheet |
| 2026-07-26 | DW-27 spike, research half: Steamworks binding landscape (`steamworks.js` vs `greenworks`), what `init()` requires at runtime, whether App ID 480 is Valve-sanctioned, non-destructive probe for a local Steam install, and what achievements and Cloud would take. Scoped read-only and explicitly barred from creating accounts, spending money or writing credentials. | Done | Node-API binding needs no `electron-rebuild`; Steam client found installed and running on this machine, so `init()` was verifiable the same day; **App ID 480 is community convention, NOT Valve policy**, and the report says so rather than repeating the folklore |
| 2026-07-26 | DW-27 spike, recon half: map the exact `window.__of` and live-object surface a page-side probe can use to seed 200 to 500 machines and a base without touching `web/src`, plus the literal `stats()` field paths and the DW-20 tick-counting pattern. Read-only. | Done | Found `Factory.restore(rows)` (one commit, not O(n^3)) and `Structures.adopt` as the save-load path to seed through, **and flagged `MachineBatch` CAPACITY 256 before a single measurement was taken**, which is what the sweep then confirmed as the wall |
| 2026-07-05 | RETHINK R3: repo surgery (LFS history rewrite, vendor-map untrack, bundle backup) + test-env static-link fix (BT-7/BT-8) | Done | 1,060 LFS objects, git store 2.43 GiB → 1.14 MiB; 21/21 ctest green from PowerShell and Git Bash |
| 2026-07-26 | BT-12, the DW-29 vessel catalogue: two diameter classes, 10 new parts, 4 renames, against a contract written before the geometry. Owned `rocket_common.py` and `build_rocket_parts.py` only; explicitly barred from editing `contracts.json`. | Done | 23 parts, 7132 render tris, 315,028 bytes, validator 3/3 with `lander_landed` rebuilt to prove the shared refactor was inert. Sent back once for a scalloped ring at the adapter joint that I found by rendering the assembly; it fixed the cause (the cone finishing its taper past the class S collar), then **went looking rather than assuming and found two more of the same class**, which is what turned a bug fix into BT-13 |
| 2026-07-26 | BT-11, the DW-32 rescale: 1 m to 4 m, re-derive every part, decide the door, and build the pillar. Owned `structure_common.py`, the four build scripts, the new `build_pillar.py` and `render_structures.py`. Same contract bar. | Done | 5/5 green, byte-identical rebuild, tiling and pillar arithmetic measured off the shipped GLBs, six renders. Its own judgement calls were good: widening the door surround to land ON the wall mullion rather than beside it, and recomposing the cameras rather than scaling them x4 (the plan grew 4x and the storey only 1.33x, so a scaled rig would have framed sky) |
| 2026-07-26 | Tier-0 base-building art set (BT-9). **Not delegated**, deliberately: the four parts are one geometric contract, not four scoped tasks, and splitting them across subagents is precisely how a tiling set ends up with four opinions about the storey pitch. Handled in-controller. | Done | 4 GLBs, 59.3 KB, dist 2.43 → 2.48 MB; 46/46 validator green; 46-script rebuild byte-identical; 4 assembled renders under `docs/screenshots/structures_*.png` |
| 2026-07-27 | **W11 lane A, world dressing**: the two trees, the bush, the plains and forest atlases. Owned `tree_common.py`, `props_common.py` and five build scripts; barred from `of_lib.py` and `contracts.json`. | Done | 6/6 green, all six byte-identical on rebuild, conifer 308 → 584 LOD0 tris. **Sent back once, and the correction is the interesting part**: its first grass redesign was clumps of individually spaced 1 to 2 cm blades, which is the same sub-pixel defect in a new shape. The second pass spawns each fan from a tight radius with 12 cm blades so the bases overlap into a solid core, verified by rendering the same clump at 8 m and 25 m, which is the test that matters and the one the first pass had not run |
| 2026-07-27 | **W11 lane A, the player character**: the first-person view model, the third-person body, and the gait. Owned `rig_common.py` and the two player build scripts, with of_lib additive-only. Bone head and tail positions frozen mid-task so the armour lane could author against them. | Done | Both green, both byte-identical, **and the two witness assets it does not own (`miner.glb`, `foundation.glb`) rebuilt byte-identical to git HEAD**, which is what proved the shared `of_lib.py` palette change was behaviour-preserving. Found three gait bugs by measurement (BT-18). Its own best judgement call: when I flagged two empty first-person frames it did not just reduce the amplitude, it made "no empty frame" a **measured** property, projecting every vertex of every frame of all twelve clips through the eye camera, and reports the worst case as 72.5% of geometry on screen |
| 2026-07-27 | **W11 lane A, belt cargo**: the path convention, `socket_rest`, `Item_Crate`, the item meshes and a new checker. Owned the four belt builders, `belt_curve_common.py`, `build_items_atlas.py` and the new `check_belt_cargo.py`. Same contract bar. | Done | 5/5 green, all five byte-identical over two independent double-build rounds, `selftest` 19 cases with 11 that must fire and 8 that must not. **Found that the published carrying height was not on any surface** and fixed the geometry rather than the socket, which then also uncovered end rollers standing 3 mm proud of the ride plane at every tile seam. Also caught a client trap before it was written: `offsetTiles` is sim capacity, not metres, so multiplying it by a constant would accelerate items 27% through every corner |
| 2026-07-27 | **W11 lane A, armour**: four slots as skinned meshes on the body's own rig, plus `render_armour.py`. Owned only the new build script and the new render tool; explicitly barred from `rig_common.py` and the player builders, which another agent was rewriting concurrently. | Done | 904 tris, 90,840 bytes, green, byte-identical, rig drift 0.00e+00 against the body at nine poses. Found four defects that are **only visible in motion** and fixed the causes rather than the symptoms. **Correctly pushed back on an acceptance rule I had written** (see BT-20): a negative clearance minimum is not a defect on a strapped-on plate, and it explained why rather than just doing it |

| 2026-08-15 | **BT-115/BT-116, the extractCmd-null audit (CE-86's routed finding): census all 91 probes `probeall.mjs` silently skipped, add documented invocations where real, mark the rest, sweep the newly-included set.** `lane/extractcmd-audit`, own worktree. | Done | 79 real (header added), 12 helper/fixture (new `// PROBEALL-EXCLUDE:` marker `probeall.mjs` now checks before `extractCmd`, so exclusion is a visible verdict rather than a silent null), 0 dead. Documented-invocation coverage over all 314 probes: 223 to 302; 0 silently null. Swept the 79 on a locally built/served tree, real D3D11 Chrome: 50 GREEN, 15 NO_VERDICT, 10 RED, 4 NO_OUTPUT. Full attribution and the verbatim fails are in NUMBERS.md's BT-115 row. **Two of the four NO_OUTPUT are `probeall.mjs`'s own defect**, not the probes': its stdout capture is a fixed `cap = 400000` bytes, and `propshadow.js`/`r17_scout.js` both exit 0 with a report larger than that, so the runner hands back a truncated, unparseable JSON string and records NO_OUTPUT for a probe that actually finished and (per RN-1650's independent by-hand run) is GREEN. Not fixed in this lane, routed. Ten real new reds routed with a first-pass instrument/game attribution each; three of them (`controls.js`, `machinepanel.js`, `machineshot.js`) converge on one furnace craft/place path and are very likely one root cause rather than three. |
| 2026-08-15 | **BT-130, the two `probeall.mjs` instrument debts BT-116 routed rather than fixed.** `lane/probeall-debts`, own worktree. | Done | **Debt 1 (the stdout cap):** fixed at the cause, not by raising the number. `run.mjs` prints its whole report as one `console.log(JSON.stringify(...))` call with the verdict-bearing `eval` key AFTER `stats`/`world`/`scene`, so any fixed in-memory byte cap truncates the JSON before `eval` ever lands on a big-scene probe. Considered and rejected: a bigger cap (guessable-around by the next probe), head+tail retention (a single JSON value does not reparse from a splice), capping what probes may print (edits every probe forever). **Fixed: `run()` streams stdout straight to a per-probe file on disk as it arrives and it is read back in full after the process closes**, with an 800 MB `SAFETY_CAP_BYTES` backstop (200x propshadow's 8.4 MB report) against a genuinely runaway process; hitting that backstop is a new, loud `TRUNCATED_OUTPUT` verdict class carrying the byte counts, never a silent fold into NO_OUTPUT. **Debt 2 (the two 240 s timeouts):** standalone re-runs (quiet machine, no wrapper timeout) measured `cantilever.js` GREEN in 238.2 s (1.8 s inside the old cap) and `machineports.js` finishing (real, unrelated RED: `phaseB.inputFell` false, routed not fixed) in 298.5 s, past the cap even completely alone -- **parallelism was never required to explain either timeout**, so the shared default was left untouched. New mechanism instead: a probe's header may carry `// PROBEALL-TIMEOUT: <ms>` in its first 60 lines (same visibility rule as BT-115's `PROBEALL-EXCLUDE`), read by `probeall.mjs` and applied to that probe alone. `cantilever.js` now carries 600000, `machineports.js` 420000, both with margin above the clean baseline for this VM's normal multi-lane contention (a mid-lane re-verification attempt hit 27 concurrent chrome.exe/node.exe from sibling lanes and cantilever.js legitimately needed 355 s that run). **Verified, real D3D11 headless Chrome, locally built and LAN-served (`vite preview --port 4262 --host 0.0.0.0`), processes killed by PID**: the four affected probes re-swept together, verbatim -- `cantilever.js` GREEN 263 s, `machineports.js` RED(1) 278 s, `propshadow.js` GREEN 28 s, `r17_scout.js` NO_VERDICT 27 s, **none NO_OUTPUT**. 5-probe spot batch (`animgate.js`, `aniso.js`, `ambience.js`, `airless.js`, `antenna.js`) unregressed: 4 GREEN, 1 pre-existing RED, all cleanly parsed. `tsc --noEmit` clean, `vite build` clean, `node --check` clean on `probeall.mjs`, `cantilever.js`, `machineports.js`. NUMBERS.md's BT-130 row has the full account. |
| 2026-08-16 | **BT-155 to BT-160, the harness gate census: the quiet serial sweep BT-80..83 attempted and failed (287 of 291 no verdict, 191 SIGKILLed).** `lane/harness-census`, own worktree, 2 concurrent shards per Reid's ruling. | Done, gate NOT flipped (Admin decision) | All 316 probe files, one pass: locally built (`sync-wasm`, `sync-assets`, `tsc --noEmit && vite build`, both clean) and served (`vite preview --port 4600 --strictPort --host 127.0.0.1`), real D3D11 headless Chrome, PIDs from `Start-Process -PassThru` (bash `$!` is a wrapper PID under Git Bash) killed by PID. ~4 hours on a genuinely contended box (16 node.exe/20 chrome.exe mid-run, 100% CPU) -- zero silent nulls, zero TRUNCATED_OUTPUT, BT-130's fixes held under real contention. **217 GREEN, 35 RED, 30 NO_VERDICT, 22 NO_OUTPUT (15 timeouts + 7 crashes), 12 EXCLUDED.** Every one of BT-116's 10 original reds plus its 2 routed findings is now GREEN (`controls.js`, `machinepanel.js`, `machineshot.js`, `rescale.js`, `buildghost.js`, `digore.js`, `craftfull.js`, `balance.js`, `moonsite.js`, `pad.js`, `basesnap.js`, `machineports.js`, `propshadow.js`, `cantilever.js`); only `animgate.js`/`discovery.js` persist, both previously known. The 35 new reds triaged by confidence and clustered by shared symptom (a tool-gate cluster matching GP-890's mechanism, an ascent/orbit CLAMPED cluster, and 13 with no diagnostic text at all, explicitly not defaulted to instrument), none fixed (measurement lane, no probe edited). **THE FLIP-BLOCKING FINDING: `probeall.mjs`'s `verdictOf()` does not recognise a fifth, real, currently-used verdict convention** -- a singular string `fail` field -- so 6 live probes (`airlock.js`, `build.js`, `furnace.js`, `furnacelit.js`, `orbitdeck.js`, `portmigrate.js`) that are actually failing sit filed as NO_VERDICT (non-blocking) instead of RED. Same shape of gap as BT-43's `ok`-only fix. Cross-checked against every GREEN: nothing is falsely green, the gap only under-counts into NO_VERDICT. Recommends fixing that plus `orbitdeck.js`'s missing `--sandbox=1` invocation flag before flipping, so the flipped gate does not ship a known false negative on day one. Full account: `docs/web/HARNESS-CENSUS-2026-08-16.md`; usage in NUMBERS.md's BT-155 row. |
| 2026-08-15 | **BT-175 to BT-187, the census flip prerequisites: fix `verdictOf()`'s missing singular-`fail` convention and `orbitdeck.js`'s missing invocation, re-sweep the affected set, state the flip position.** `lane/verdict-convention`, own worktree. | Done, gate NOT flipped (Admin decision) | Both originally-named blockers fixed and proven both ways on a 38-probe re-sweep (2 shards, sentinel-verified port): 5 probes reclassified NO_VERDICT to RED, 1 to GREEN (`orbitdeck.js`), 24 legitimately report-only NO_VERDICT probes and 8 GREEN/RED controls unchanged. Added a LOUD `UNRECOGNISED_VERDICT_SHAPE` class. `machineports.js`'s timeout margin (420000, 43 ms from its own cap) raised to 600000. Every RED then re-run serially per `probeall.mjs`'s own rule (a same-day cross-lane teardown, GP-984, voided the first attempt mid-run; re-taken on a fresh sentinel-verified port). **A fresh-context verifier caught two mistakes in this lane's own first pass, both corrected in place rather than left standing:** (1) `terrainart.js` was first reported as "reclassified GREEN, a sharded-contention artifact" on one isolated run; the verifier ran it 5 times isolated and got GREEN/RED/GREEN/RED/RED, genuinely nondeterministic (a badly-conditioned assertion against a noisy do-nothing floor), reclassified RED with the 2-of-5 rate recorded rather than assumed fixed by "run it once more, serially." (2) `airlock.js` carried `orbitdeck.js`'s exact missing-invocation defect, unnoticed by this lane's own re-sweep: `extractCmd()` matched a prose line, not a real command, so it had been running at bare defaults; fixed and re-confirmed still genuinely RED under its real flags. A mechanical scan following from that second finding turned up **19 more probes with the same prose-match trap**, named but not fixed, real work for the next lane. **Corrected counts: 218 GREEN / 40 RED / 24 NO_VERDICT / 22 NO_OUTPUT / 12 EXCLUDED**, recorded as a dated, self-correcting amendment to the census document, not a rewrite of the original. 10 of the 22 NO_OUTPUT probes remain undiagnosed (`padgate.js` fixed independently by `main`'s own GP-980..983, accounted for). This lane does not consider the gate flip-ready: the `terrainart.js` correction proves a probe can be a coin flip in isolation, which "re-run serially" alone would not have caught; the BT-184 corpus scan means other probes' current verdicts may themselves rest on a wrong invocation; NO_OUTPUT has no protective allowlist. Does not flip the gate, unchanged from the original brief. Full account, the self-correction, and remaining checklist with owners: `docs/web/HARNESS-CENSUS-2026-08-16.md`'s Amendment section; usage in NUMBERS.md's BT-175 row. |
| 2026-08-18 | **BT-225 to BT-239, close the eighth instance of the undocumented-probe class before it becomes a ninth: build an authoring-time gate rather than another census, give `vmlight.js`/`vmshade.js`/`vmcost.js` real invocations, and move the cascade-0 shot-set gap into a proper decision row.** `lane/probe-authoring-gate`, own worktree. | Done | New `web/tools/smoke/check-invocations.mjs`, wired as `npm run check:probes`, second link of `npm run check` (right after `check:roles`, ahead of the persistently-red `check:limits`). Reuses `probeall.mjs`'s `extractCmd`/`PROSE_ONLY_INVOCATION`/`excludedReason` (the last newly exported), never reimplements the parser. Proven both ways: 3 named offenders before, 0 after, across all 325 probe files. The three probes' invocations verified live rather than guessed: `vmlight.js`/`vmshade.js` need the RN-352 forest site pinned at BOOT (`--lat=-19.85 --lon=-72.7853`), which for `vmshade.js` was a measured requirement, not a style choice (teleporting there from elsewhere failed). **Found, not fixed, and flagged via `spawn_task`: `vmshade.js` is flaky at its own correct invocation** (3 isolated runs: true/false/true), the `terrainart.js` class of badly-conditioned threshold. `RN-1993` added to `rendering.md` section 3 (the RN-1859 form), superseding section 2.9a's prose. Full account and usage: NUMBERS.md's BT-225 to BT-239 row. |
| 2026-08-19 | **BT-255 to BT-259, the `run.mjs --out` / `artframe.js` `station` fix routed from RN-2030's verification.** `lane/artframe-out`, own worktree. Not delegated to a subagent, deliberately: reproducing the bug, forming the fix hypothesis and testing the hang it was nominally protecting against are one continuous chain of dependent evidence, and splitting it across agents would have meant re-deriving the same server/build state twice. | Done | Full account in the BT-255 decision row above. In short: removed the station shot's post-measurement teleport back to the ground, tested (not assumed) that this does not reintroduce the PH-89 settle hang the comment cited as its reason, and proved the fix both by altitude (1.62 m -> 399,868 m) and by `pngdiff` against the same run's own `eval.png` (93.21% -> 5.21%, landing in `forestfloor`'s own natural 6.10% modal band). Audited the other eight canonical poses and found the class nowhere else. Searched docs/ for previously published station `--out` PNGs; found none that were actually the forest, and flagged rendering.md section 7 regardless. |
| 2026-08-19 | **BT-210 to BT-224, per-probe sweep budgets: derive one from measured wall time for every documented probe, subsume the ad hoc markers into one committed file, and define gate semantics for exceeding a budget.** `lane/probe-budgets-2`, own worktree, SECOND attempt (the first attempt's isolation worktree died at 52/325, see ADMIN.md's WORKTREE CORRUPTION INCIDENT row). Not delegated to a subagent: the measurement, the resampling decisions, and the gate-semantics code are one continuous chain against one live server/tree, and splitting it would have meant re-deriving the same build state repeatedly. | Done | Full account in NUMBERS.md's BT-210 to BT-224 row. In short: all 314 documented probes measured on a quiet, non-worktree scratch tree (output kept in the session scratchpad throughout); new `web/tools/smoke/build-probe-budgets.mjs` derives `web/tools/smoke/probe-budgets.json` (soft `budgetMs` 1.25x measured max, hard `hardCapMs` 3x measured max floor 120 s, one rule, no per-probe hand-picking); `probeall.mjs` now reads it instead of scanning probe headers, the three old `PROBEALL-TIMEOUT` markers deleted; a killed run is now a distinct fatal `HUNG` verdict separate from `NO_OUTPUT`, and a completed-but-slow run gets `overBudget: true` without ever becoming a content failure, both proven live plus via two uncommitted local-override cases. `assembler.js` (686.9 s measured) clears its own 860 s budget clean, no NO_OUTPUT. `npm run check` 6/7. The sweep's own process was killed by the environment three separate times and resumed cleanly each time with zero data loss. **Landed on `lane/probe-budgets-2`**, flagged for Admin to rename/merge since the exact branch name `lane/probe-budgets` is still held by the first attempt's dead, still-registered worktree. |

## 9. References
[EXECUTION-PLAN](../EXECUTION-PLAN.md) (§2–4, §8), [REVIEW-2026-06-14](../REVIEW-2026-06-14.md) (Q7 finding). The spike gate lists (V/WV/RV/G-series) across docs/spikes/.
