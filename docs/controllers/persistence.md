# Persistence & Data — Master Controller Context

> **Domain owner:** `persistence-controller` · **Reports to:** Admin · **Phase:** R · **Status:** **R46 (HIGH) CLOSED 2026-07-30 (PS-13 to PS-15): named saves now go through the `writeSlot` choke point, so they carry vessels, the player anchor and `dayT`; old partial named saves load with the gap stated on the row.** Web (Three.js) save path is the live one (`web/src/game/SaveGame.ts` + `Persist.ts` + GP-136's named layer); the June core-era rows below (PS-1 to PS-12) describe the headless C++ container, which the web pivot superseded as the shipping surface. · **Last updated:** 2026-07-30 (PS-13 to PS-15; number block PS-13 to PS-16 taken per Admin brief, next free PS-16)
> Read alongside: [MASTER_PLAN](../MASTER_PLAN.md) · [AGENT_ARCHITECTURE](../AGENT_ARCHITECTURE.md) · [ADMIN](ADMIN.md) · **[persistence-phase1.md](../phase1/persistence-phase1.md)** (the Phase-1 slice save/load design — gates M2.5)

## 1. Mission
Save and restore arbitrarily large worlds cheaply and reliably: regenerate the natural universe from a seed, store only player modifications as diffs, stream chunks on demand, and keep saves forward-compatible as the game evolves.

## 2. Scope & owned subsystems
- **Seed+diff model** — the universe regenerates from a seed; only player changes are stored.
- **Chunked region storage** — world partitioned into streamable, independently loadable/savable chunks.
- **Serialization format** — compact (custom binary) for factory state at scale; structured for player/meta data.
- **World database / save container** — single-player file(s) and server-side store.
- **Streaming** — load/unload chunks tied to core-engine active bands & networking AOI.
- **Versioning & migration** — schema evolution without bricking saves.
- **Non-goals:** generating the world (→ world-gen; we store its *diffs* and seed); deciding what state matters (each domain declares its persistable state).

## 3. Key design decisions
| # | Decision | Rationale | Status | Date |
|---|----------|-----------|--------|------|
| PS-1 | Universe = seed; player changes = diffs (mirrors D-005 model) | Natural world costs ~0 to save; saves stay small | Accepted | 2026-06-14 |
| PS-2 | Chunked region storage, streamed | Worlds exceed memory; must page in/out | Accepted | 2026-06-14 |
| PS-3 | Custom binary (`.ofc`) for factory/world chunks; structured TLV (`.ofd`) for meta/progress | Factorio-scale state needs compactness/speed; meta needs migratability | **Accepted** (settled in Phase-1 design §5) | 2026-06-14 |
| PS-4 | Versioned schema from day one; two-level (container `formatVersion` + per-domain `schemaVersion`); **migration *machinery* deferred** | Saves outlive code; header carries every number a future migrator needs without a format redesign | Accepted | 2026-06-14 |
| PS-5 | **`FChunkKey` = `(BodyId, FaceId, RegionDepth, RegionPath)`**, a coarse **prefix** of world-gen's `FQuadKey`; one save region ≈ one factory-sim chunk proxy | Reuses world-gen's deterministic addressing → no separate spatial index; aligns save residency to core-engine's promote/demote bubble | Accepted (Phase-1 §3) | 2026-06-14 |
| PS-6 | **Single slot = one directory; atomic commit via write-temp-then-rename**, header written last as the commit record, `save0.bak/` retained until swap | Crash mid-save leaves the old *or* new complete slot, never a torn mix (P1-D5) | **Accepted + REALIZED** (Phase-1 §4.3 → built in `persistence_file.h`, see PS-10) | 2026-06-14 |
| PS-7 | **Slice terrain is NOT a diff** — node mining only (D-005/WG-2), so terrain+deposit *placement* regenerate from seed; only **deposit depletion** is a world-gen diff | Headline slice simplification: the whole surface is free to persist | Accepted (Phase-1 §1.2) | 2026-06-14 |
| PS-8 | **On-rails factory chunks persist as `FRailState`**; load restores the proxy state and lets factory-sim's existing `OnPromote` reconstruct (bounded by `bufferCaps`, no dupes) | Reuses factory-sim's demote/promote machinery — persistence adds no new dupe surface (retires R1) | Accepted (Phase-1 §4.4) | 2026-06-14 |
| PS-9 | **Wave-0 headless save/load core BUILT** (`core/include/of/persistence.h` + `core/tests/test_persistence.cpp`): `SaveWriter`/`SaveReader` LE byte cursors (POD + LEB128 varint), versioned envelope `SaveHeader` (PS-4: magic `OFSV` + `formatVersion`=1 + `minReaderVersion` + per-domain `schemaVersion` table), per-domain serialize/deserialize over the 5 domains' OWN pinned types, and a `SaveGame::save(state)->bytes` / `load(bytes)->state` bundling them in the §4.5 load order with length-prefixed skip-capable records (§5.1). Proves M2.5 round-trip in-memory: build a non-trivial slice (deplete deposit, run factory `produced>0`, park craft conic, fill inventory, advance objective), save→load into a fresh state, every field matches; PS-7 verified (buffer **276 B** for the test scene — terrain never stored, regenerates bit-identically from seed); PS-4 header rejects bad magic / too-new version / truncation; save is byte-deterministic. **No additive core hooks needed** — consumes the green cores entirely through existing public APIs. **persistence_tests green; all 7 ctest suites pass.** Atomic single-slot *file* container (§4.3) layers above this in-memory format — deferred to the file-I/O wave. | The format is the gate-critical piece; it round-trips with zero terrain bytes and is forward-compat by construction | Accepted | 2026-06-14 |
| PS-11 | **GAP-4 (slice-e2e) CLOSED — research/tech unlocks now persist.** Added `std::vector<uint16_t> unlockedTechs` (the unlocked-`TechId` id list, stored opaquely per the C-3 "cross-ref by opaque id" discipline — persistence does NOT depend on `research.h`) to `SliceState`'s gameplay record, plus its serialize/deserialize (`saveGameplay`/`loadGameplay`: a `varint` count + each id as `u16`, appended after the objective fields). **Bumped `kSchemaGameplay` 1→2** (Phase-1 has no migration, so `readAndValidate` requires the exact version; the bump marks the new field's format — in-process round-trips unaffected). On reload the unlock set is **RESTORED directly** via `ResearchState::restoreUnlocked(idList)` (a new research.h hook that applies each tech's recipe/entity unlocks WITHOUT a science spend) rather than re-derived by replaying `tryResearch` — which would require reconstructing the exact science the player spent. Proven in `slice_e2e`: 2 unlocked techs round-trip the save and restore into a fresh `ResearchState` with an EMPTY science pool, reopening every gated recipe/entity. **persistence_tests / persistence_file_tests / slice_e2e_tests green; 13/13 ctest suites pass.** Additive: prior persisted fields + load order unchanged. | Research unlocks are monotonic+deterministic so re-derivation was *possible*, but it forced the loader to replay the whole spend sequence; storing the set (the §2.1 `FGameplayPersistState` research field the slice deferred) makes unlocks survive save→reload as state, not as a recomputation. | Accepted | 2026-06-15 |
| PS-12 | **R4 unified UE save, QUEUED NEXT (2026-07-05, RETHINK §4-R4).** The audit ([core-docs-audit.md](../review-2026-06-16/core-docs-audit.md) "THREE persistence paths") found the shipped UE game persists **only terrain edits**, via THREE disjoint paths: (1) the tested atomic headless slots (`persistence_file.h`, **zero UE consumers**), (2) a UE **ad-hoc** `deform_<seed\|slot>.bin` written non-atomically by `SaveDeform`/`LoadDeform` (the only place a player can lose data), (3) the M5.2 voxel-edit piggyback appended to that file. Inventory, factory network, research state, and player position are saved NOWHERE in UE. **Plan (this domain owns it):** ONE UE `WorldSave` subsystem built on the atomic PS-6 container with sections (core state, voxel diff [deform derived per R1], factory, gameplay/research), retiring the ad-hoc file. Owner: persistence, with an Admin-logged contract. | One atomic container replaces three ad-hoc paths and closes the "you can lose data mid-save" gap; it also gives the Phase-P research/costs work a place to persist. | **Accepted, queued next (R4)** | 2026-07-05 |
| PS-13 | **R46 CLOSED: A NAMED SAVE GOES THROUGH THE `writeSlot` CHOKE POINT, so it carries the vessels, the player anchor and the time of day.** `SaveGame.writeSlot` gained an optional `key` parameter (default: the mode's autosave key, exactly the old behaviour) and `SaveSlots.save` calls `writeSlot(snapshotOf(g), namedKey(mode, name))` instead of stamping ONE field (`assisted`) and calling `writeKey` itself. `SaveKeys.writeKey` is demoted to the byte-mover for verbatim copies, and the load path deliberately STAYS on it: restamping a stored slot on its way to the auto key would overwrite the loaded world's vessels, position and time of day with the live session's, which is the same defect mirrored. **Reproduced before fixing, side by side on one scene** (a vessel flown to orbit, left on rails, day pinned 0.42, autosave forced AND a named save made through the real panel, both slots read raw off IndexedDB): pre-fix the auto slot carried `vessels[1]` (fuel `[[0,40],[2,884.14]]`, conic a 685532.05 e 0.0077), `player` {lat 1.9993, lon 143.9972} and `dayT` 0.42102, while the named slot written 131 ms earlier carried NONE of the three under the same "1 built, dug in" summary; loading it restored 0 registry records, the scenario spawn (lon 144 exact) and the solved sun 0.2495. **Post-fix the named slot's fuel and conic JSON are BYTE-IDENTICAL strings to the autosave's**, the anchor matches to 1e-6 deg, and across a driven panel Load plus a real `page.reload()` the vessel returns on rails with **fuel exact (923.002605375252 kg both sides), conic relative delta < 1e-9, the anchor restored to the stamped lat/alt, and the sun at the slot's phase (0.4218 against solve 0.2495)**. **Negative control:** HEAD's `SaveSlots.ts` swapped back into the scratch tree turns the same runner red on **14 checks by name**; the restore is sha1-identical (`ffec0707`). Regression: `dayreload.mjs`, `reload.mjs --setup=probes/savenamed.js` (both phases of GP-137's own proof) and `vesselreload.mjs --phase=orbit` all functionally green on the fixed build. tsc proven alive first (planted error red at its line, revert green). | Two writers enumerating stamped fields independently is the five-copies failure this project keeps paying for: PH-67's "every write in the client comes through this one function" was true when written and false since GP-136, and each field PH-67/PH-68/PH-86 stamped at the choke point silently fell out of the second path. The fix makes the named path enumerate NO fields at all, so the set cannot diverge again. | **Accepted, shipped** | 2026-07-30 |
| PS-14 | **AN OLD PARTIAL NAMED SAVE IS LOADED WITH THE GAP STATED, NEVER REPAIRED FROM THE AUTOSAVE.** Every named save written before PS-13 (Reid's included) lacks `vessels`, `player` and `dayT`. On load nothing crashes and nothing pretends: the absent fields restore the pre-PH-67 defaults (no vessels, the scenario spawn, the solved sun), proven by the runner's strip control against the FIXED build (0 records, anchor `restored: null`, sunT 0.24953 against solve 0.24955). The gap is SAID three ways: `SlotRow.partial` (the probe surface), the sentence appended to the row summary which reaches the DRAWN row before the player presses Load ("(older save: any rockets, position and time of day not in it)", asserted off the element per GP-64), and the load note. **`dayT === undefined` is the discriminator** because the unified writer ALWAYS stamps it, while `vessels` is legitimately absent on a world with no rockets. **Repair from the autosave was considered and REFUSED**: the autosave is a DIFFERENT MOMENT of the world, and splicing its vessels into a named save is PH-68's "two half-saves of different worlds" built on purpose (the named save may predate the launch, the pad, or the fuel state). **`SAVE_VERSION` unmoved** per the WG-29/GP-136 rule: every field is additive and optional, absent is read as what it is, and a bump is refused on MISMATCH and would destroy every world anybody is playing. | Stating beats guessing: a load that silently restores less than the player kept is R46's whole failure mode, so the row that offers the Load button is where the gap is stated. | **Accepted, shipped** | 2026-07-30 |
| PS-15 | **THE INSTRUMENT: [`tools/smoke/namedvessel.mjs`](../../web/tools/smoke/namedvessel.mjs)**, reproduction and proof in one runner: the side-by-side (both slots raw off IndexedDB, reduced by ONE shared function so the two sides cannot be reduced differently), driven panel presses (GP-156's re-query helper, no stale fallback), two real `page.reload()`s, and the strip control that turns the named slot into exactly what a pre-fix named save is. Two harness findings folded in from the pristine run: (1) **`anchor.applied` is true on the DEFAULTED path too** (it means the anchor step RAN); the discriminator for "came from the slot" is `restored !== null`, vesselreload 6b's own form, and the first draft asserted the wrong one; (2) the `[of] surfaces:` console.error is **pre-existing on the pristine freeze of `4a44edb`** (art pipeline: HEAD's client role table against `surfaces.json` regenerated from the working tree's assets by the mandatory `sync-assets` step) and is named out EXACTLY in the runner, never wildcarded; it is also the only page failure on `dayreload`/`savenamed`/`vesselreload` against the fixed build. **Residuals, recorded not chased:** (a) `Persist.saveSlot`'s PH-30 inhibit gate is dead weight today (`inhibitSave` has ZERO callers since the slot learned to describe a vessel), but IF it ever regains one the named path bypasses it, because `SaveSlots` calls `snapshot`+`writeSlot`, not `Persist.saveSlot`; the right home would then be inside `writeSlot`. (b) The 20 s autosave timer can rewrite the auto slot between a forced save and a later read (observed as a `savedAt` ordering inversion during the runs); benign, but an instrument comparing the two slots must compare CONTENT, as this one does. | A probe that reads the store raw is the only one that could ever have caught R46, because every view above the store (the list, the summary, the counters) reported the partial save as complete. | **Accepted, shipped** | 2026-07-30 |
| PS-10 | **Wave-1 atomic single-slot save FILE container BUILT** (`core/include/of/persistence_file.h` + `core/tests/test_persistence_file.cpp`) — realizes PS-6 / Phase-1 §4.1/§4.3 on disk. `SaveToSlot(slotDir, bytes)` / `LoadFromSlot(slotDir, outBytes)` move an OPAQUE `persistence.h` save buffer to/from a single slot **directory** (`<slotDir>/save.ofc`), header-only over `<fstream>`+`<filesystem>` (no game-domain deps — it never parses the payload, §2 boundary). **Atomic commit (§4.3):** stage into `save.tmp` → flush/close → rotate the live `save.ofc`→`save.bak` → `std::filesystem::rename(tmp→ofc)` (atomic same-volume); creates `slotDir` if missing; restores from `.bak` if the commit rename itself fails. **Container guard:** the buffer is wrapped in a fixed envelope — header magic `OFCS` + `containerVersion` + `u64 payloadLen` + payload + trailing footer magic `OFCE` (the in-file commit marker, written LAST) — so a truncated/torn file fails the magic/length/footer check and is **rejected** (returns false) instead of being read as garbage; `LoadFromSlot` then **falls back to `save.bak`**. Tests (4 groups, 45 checks): (1) round-trip a real multi-record save buffer → byte-identical + re-parses via `SaveGame::load`; (2) atomicity — a 2nd save of different bytes loads the new bytes AND preserves the prior slot as `save.bak`; (3) torn-write survival — a stray garbage `save.tmp` + a truncated `save.ofc` → `LoadFromSlot` still returns the last good save from `save.bak`, never the tmp; (4) guard+fallback — a short/corrupt `save.ofc` is rejected and `.bak` is used when present (+ direct `decode` guard cases). Unique temp dir under `temp_directory_path()`, cleaned up. **`persistence_file_tests` green; 8/8 ctest suites pass** (no regression). No core/other-domain files touched (additive: new header + new test + one CMake target). | PS-6 is the last container piece before chunk-keyed region files; write-temp-then-rename + a footer commit marker is the standard crash-safe pattern (§7 research) and now proven headless | Accepted | 2026-06-15 |

## 4. Architecture & approach
- **Seed+diff:** store the world seed + a sparse set of diffs keyed by chunk: terrain edits (voxel patches), deposit depletion, placed/removed factory entities + their state, looted-POI flags, player inventory/progress. On load, regenerate from seed, then apply diffs.
- **Chunking:** the world is partitioned (per body, per region) into chunks that load when entering core-engine's active band / networking AOI and flush when leaving. On-rails factory chunks persist as their rate model + buffers, not per-entity (factory-sim FS-4).
- **Formats:** factory/world chunks → compact binary (versioned headers); player progress, tech, quests, settings → structured (binary or readable) for easier migration. Each domain provides serialize/deserialize for its own state; persistence owns the container, indexing, streaming, and versioning.
- **MP:** server holds the canonical store; clients regenerate from seed and receive diffs via networking (clients don't need the whole save).

## 5. Interfaces & dependencies
**Depends on (inbound):**
- world-gen: the seed + the diff set (terrain edits, depletion, POI flags).
- factory-sim: factory state snapshot (entities, buffers, on-rails rate models).
- gameplay: player progress (tech, quests, inventory, looted flags).
- physics: vessel orbital state (element sets) + active-vessel state.
- core-engine: chunk↔active-band mapping, `UniverseCoord` keys; networking: AOI streaming triggers, server-store role.
**Provides to (outbound) — published contracts (Phase-1 designed — [persistence-phase1.md](../phase1/persistence-phase1.md) §2,§5,§6):**
- **`IPersistable`** — the per-domain serialize/deserialize contract (`DomainId`, `SchemaVersion`, `SaveDiffs(FChunkKey, FSaveWriter&)`, `LoadDiffs(FChunkKey, FSaveReader&, savedVersion)`, `EnumerateDirtyChunks`). Each domain implements it over its OWN pinned state; persistence never interprets the bytes.
- **`FChunkKey`** + `truncate(FQuadKey → region)` mapping rule (PS-5).
- **Save/load orchestration + atomic single-slot container** (`Save(slot)`/`Load(slot)`, PS-6); `FSaveWriter`/`FSaveReader` byte cursors handed to each domain.
- **Versioned header (PS-4)** + the two payload formats (`.ofc` compact binary / `.ofd` structured TLV, PS-3).

**`IPersistable` per-domain state each must serialize (Wave-2 asks — routed by Admin):**
- **core-engine** (`CoreEngine`, GLOBAL): `worldSeed` + observer `FUniverseCoord`/`FFrameId` + `SimTime`/`TickIndex` + floating-origin anchor. Also owes a **save-quiesce tick boundary** (capture all domains at one `TickIndex`).
- **world-gen** (`WorldGen`, CHUNK): **deposit depletion only** — `(depositId, remaining)[]` per region. Terrain NOT saved (regenerates — PS-7).
- **physics** (`Physics`, GLOBAL): craft `FOrbitalElements + Mode + DominantFrame` (~10 doubles) (+`r,v` if active on surface).
- **factory-sim** (`FactorySim`, CHUNK): full per-entity SoA + `TransportLine` contents (active chunk) **or** `FRailState` (on-rails chunk, PS-8); align chunk granule to `FChunkKey.RegionDepth`.
- **gameplay** (`Gameplay`, GLOBAL): inventory + avatar position/body/frame/control-mode + objective progress flags.

**Load order contract (§4.5):** CoreEngine → WorldGen (regen+depletion) → Physics → FactorySim → Gameplay (player placed last into the completed world).

**Open negotiations (flagged to Admin as DEPENDENCIES):** (1) final `RegionDepth` — joint pick with factory-sim + core-engine so 1 save region ≈ 1 factory chunk proxy; (2) the save-quiesce tick handle from core-engine.

## 6. Task backlog / roadmap
- [Phase 0] ✅ Define `Persistable` interface + chunk-key scheme + version header convention (paper design).
- [Phase 1] ✅ **DESIGNED** — concrete seed+diff save/load for the "First Foundry" slice: `IPersistable`, `FChunkKey`, atomic single-slot container, on-rails restore via `FRailState`, versioned header. → [persistence-phase1.md](../phase1/persistence-phase1.md). **Gates M2.5.**
- [Phase 1] ✅ **BUILT (Wave-0 headless core)** — `core/include/of/persistence.h` + `core/tests/test_persistence.cpp` (PS-9): the seed+diff save/load FORMAT round-trips the slice state in memory (round-trip / PS-7 seed+diff / PS-4 versioned-header / determinism — all green).
- [Phase 1] ✅ **BUILT (Wave-1 atomic FILE container)** — `core/include/of/persistence_file.h` + `core/tests/test_persistence_file.cpp` (PS-10): realizes PS-6 / §4.1/§4.3 — `SaveToSlot`/`LoadFromSlot` write the opaque save buffer to a single slot directory with write-temp-then-rename atomic commit, `.bak` rotation, and a magic/length/footer guard that rejects a torn file and falls back to `.bak`. **8/8 ctest suites pass.** Next: wire chunk-keyed region files (`FChunkKey` → one `.ofc` per dirty region, replacing the slice's single global save file) once factory-sim/world-gen expose their per-chunk `IPersistable` payloads at scale; today the slice's tiny diff set rides in one global save buffer.
- [Phase 2] Extend slice save/load to research/tech state; chunk streaming at scale; deposit-diff compaction.
- [Phase 3] Server-side canonical store; client seed+diff sync with networking.
- [Phase 4+] Migration *machinery* at the load boundary (header already designed for it — PS-4); voxel-edit diffs (digging); compression tuning (Phase 5).

## 7. Open questions & risks
- **R1 (RETIRED for the slice):** on-rails factory persistence fidelity → solved by storing factory-sim's `FRailState` and reusing its `OnPromote` reconstruction (bounded by `bufferCaps`, deterministic, no dupes — spike3 G2). Persistence adds no new dupe surface (PS-8, Phase-1 §4.4). Residual: deep-recipe rate-model accuracy is factory-sim's Phase-2 refinement, not ours.
- **R2:** migration burden as schemas evolve in parallel — mitigated by **two-level versioning** (container + per-domain `schemaVersion`) so a domain bumps its format independently; machinery deferred but header carries every number it needs (PS-4).
- **R3:** save size/perf for large built-up worlds; slice-scale is trivial (hundreds of entities); compression deferred (Phase-5) with a reserved per-chunk flag so it's a non-breaking add.
- **OPEN (Wave-2 negotiation):** (1) `FChunkKey.RegionDepth` granule — joint pick with factory-sim + core-engine (1 region ≈ 1 chunk proxy); (2) core-engine save-quiesce tick handle (no torn cross-domain snapshot).

## 8. Subagent delegation log
| Date | Subagent task | Status | Outcome |
|------|---------------|--------|---------|
| 2026-06-14 | Phase-1 slice save/load design (this controller, direct — no subagent spawned) | Done | [persistence-phase1.md](../phase1/persistence-phase1.md) — settled PS-3, added PS-5..PS-8, published `IPersistable`/`FChunkKey`/load-order, routed per-domain Wave-2 asks. |
| 2026-06-14 | Web research (1 scoped thread, direct WebSearch — no subagent): "seed+delta world persistence; chunked save streaming; atomic write; versioned header" | Done | Validated the seed+diff + region-chunk files + versioned envelope + write-temp-then-rename pattern (Minecraft level format/seed; indie versioned/atomic save systems). No design change — confirmed PS-1..PS-4. |
| 2026-06-14 | Wave-0 BUILD (this controller, direct — no subagent): implement the headless seed+diff save/load core + tests; build with g++/CMake/Ninja; verify all 7 suites | Done | `persistence.h` (`SaveWriter`/`SaveReader`/`SaveHeader`/`SaveGame` + per-domain serialize over the 5 green cores' pinned types) + `test_persistence.cpp` (4 groups, 88 checks). `persistence_tests` green; **7/7 ctest suites pass** (no regression). Save buffer = 276 B for the built test scene (PS-7: terrain regenerates from seed, never stored). No additive core hooks required. → PS-9. |
| 2026-06-15 | Wave-1 BUILD (this controller, direct — no subagent): implement the atomic single-slot save FILE container (PS-6) over the in-memory format + tests; build with g++/CMake/Ninja; verify all suites | Done | `persistence_file.h` (`SaveToSlot`/`LoadFromSlot` — write-temp-then-rename atomic commit, `.bak` rotation, `OFCS`/`OFCE` magic+length+footer guard, opaque payload — header-only over `<fstream>`+`<filesystem>`) + `test_persistence_file.cpp` (4 groups, 45 checks: round-trip / atomicity+`.bak` / torn-write survival / guard+fallback). New `persistence_file_tests` CMake target (with a `<9.0` GCC `stdc++fs` link guard — no-op on the local GCC 16.1). `persistence_file_tests` green; **8/8 ctest suites pass** (no regression). Additive only — no core/other-domain files touched. → PS-10. |
| 2026-07-30 | R46 fix (this controller, direct, no subagent): reproduce, unify the named-save write path through `writeSlot`, prove the round trip, answer the old-slot question. Number block PS-13 to PS-16 taken per Admin brief (this file's series ended at PS-12); PS-16 unused, next free. | Done | `web/src/game/SaveGame.ts` (`writeSlot` key parameter), `web/src/game/SaveSlots.ts` (save through `writeSlot`; `SlotRow.partial`; the old-save sentence; the load-note), `web/tools/smoke/namedvessel.mjs` (new runner). Scratch tree `web/dist-psave` (freeze of `4a44edb`, ABI 21 verified, wasm cmp HEAD), deleted at end; ports 5461/5462/5463 mine only, each killed; 4200 untouched. → PS-13, PS-14, PS-15. |

## 9. References
MASTER_PLAN §6 (persistence), §9 (seed+diff), D-005. Factorio save format (custom binary); chunked-world streaming; schema versioning/migration patterns.
