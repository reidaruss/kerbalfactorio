{
  "name": "orbital-foundry-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Orbital Foundry browser client: three.js r185 + TypeScript over the /core WASM simulation.",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "sync-wasm": "node scripts/sync-wasm.mjs",
    "predev": "npm run sync-wasm && npm run sync-assets",
    "dev": "vite",
    "prebuild": "npm run sync-wasm && npm run sync-assets && npm run check:roles",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "check:limits": "node scripts/check-limits.mjs",
    "check:proxies": "node scripts/check-proxies.mjs",
    "check:roles": "node scripts/check-roles.mjs",
    "check:boot": "node tools/smoke/boot.mjs",
    "check:pose": "node tools/smoke/posecheck.mjs",
    "smoke": "node tools/smoke/run.mjs",
    "check": "npm run check:roles && npm run check:proxies && npm run typecheck && npm run check:pose && npm run check:limits && npm run check:boot",
    "sync-assets": "node scripts/sync-assets.mjs"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/three": "0.184.1",
    "playwright-core": "^1.62.0",
    "typescript": "^5.9.2",
    "vite": "^8.1.5"
  }
}
